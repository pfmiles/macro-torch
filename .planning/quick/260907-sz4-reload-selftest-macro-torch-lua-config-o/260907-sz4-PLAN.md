---
phase: quick-260907-sz4-reload-selftest-macro-torch-lua-config-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - macro_torch.lua
  - core/selftest.lua
autonomous: true
requirements:
  - QUICK-260907-SZ4
user_setup:
  - service: wow-client
    why: "In-game smoke test on the user's Windows+Cygwin machine (this host cannot run WoW 1.12/SuperWoW). After the code lands: (1) run build.sh on the game machine to regenerate SM_Extend.lua into the AddOns dirs (established quick-task convention — do NOT rebuild it in this repo); (2) login a druid — after the selftest summary/diagnostics, the config banner prints once with both entries at current=false default=false; (3) invoke the /mt command — the banner prints again after the Druid diagnostics section; (4) run /run macroTorch.cpBuildLog=true then /mt again — the banner now shows true for that entry while rawdiag2Enabled stays false."
estimate:
  tokens: 14000
  raw_tokens: 7000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Once per run: every complete SelfTest:run() ends with printConfigBanner() immediately after printDruidDiag(), so the banner appears on both invocation paths (the deferred ~30-frame PLAYER_ENTERING_WORLD run wired at core/events.lua:74 and the /mt command path at core/selftest.lua:990), and the _selfTestRan dedupe keeps it to one login-time print"
    - "Registry completeness: macroTorch.CONFIG_OPTIONS holds exactly the two surveyed user-tunable globals — macroTorch.cpBuildLog and macroTorch.rawdiag2Enabled — each entry carrying the name literal, default literal (both false, mirroring the nil-guards), a one-line ASCII desc, the /run setter example, and an explicit getter function reference; the banner iterates with ipairs and performs zero string-keyed option resolution"
    - "Banner cannot error the selftest path: each value read goes through pcall with a tostring fallback ('unavailable' on any failure or missing getter); the banner performs no writes — the nil-guarded default assignments remain the only macroTorch.cpBuildLog / macroTorch.rawdiag2Enabled assignments in the file (read-only gate)"
    - "Style continuity: all banner lines use macroTorch.show with the [macro-torch] prefix — white header/end markers and yellow option lines — matching the existing selftest summary (core/selftest.lua:81) and printDruidDiag (classes/druid/diag.lua) conventions"
    - "Purity and syntax: the git diff of both files is strictly additive (zero existing lines changed); bbcheck.js reports BALANCED for macro_torch.lua and core/selftest.lua; every added line passes the Lua 5.0 token gate in the FINAL verification; git diff --check is clean (no trailing whitespace, LF preserved)"
  artifacts:
    - macro_torch.lua
    - core/selftest.lua
  key_links:
    - "SelfTest:run (core/selftest.lua:80-96) — the single choke point both invocation paths funnel through: events.lua:74 (deferred ~30-frame run after PLAYER_ENTERING_WORLD) and the /mt command path (core/selftest.lua:990) — the new macroTorch.printConfigBanner() call is inserted between the printDruidDiag() line (95) and the function-closing end (96); no change to _selfTestRan gating"
    - "macroTorch.printConfigBanner (appended to macro_torch.lua after the _cpBuildLogProbeWarned line 40, below both nil-guards) → for _, opt in ipairs(macroTorch.CONFIG_OPTIONS) → pcall(opt.get) reads → macroTorch.show chat output; the banner reads macroTorch.cpBuildLog / macroTorch.rawdiag2Enabled values but never assigns them, so it stays independent of the nil-guard re-arm logic"
    - "selftest stub note: classes/druid/selftest.lua temporarily replaces macroTorch.show during individual category runs but restores it inside each test; printConfigBanner emits through macroTorch.show (not macroTorch.log) after all tests complete, so no stub interplay and no log-p-persistence noise"
---

<objective>
Give both selftest surfaces (the login/reload deferred run and the /mt command) a
global config banner that lists every user-tunable macroTorch option with its
current value, its default, a one-line purpose, and the /run setter example —
driven by a macroTorch.CONFIG_OPTIONS registry so a future option costs one
added entry, not a banner rewrite. The banner prints immediately after
printDruidDiag(), i.e. after all selftest output ends, once per run on both
paths.

Purpose: a user who flips macroTorch.cpBuildLog or macroTorch.rawdiag2Enabled
in-game has no discoverable readback of global config state after login/reload.
The banner closes that loop at the exact moment selftest output ends, using only
macroTorch.show (chat) in the established [macro-torch] style, and reads option
values via explicit getter references so the selftest path can never error.

Output: macro_torch.lua gains the CONFIG_OPTIONS registry (exactly two entries)
plus printConfigBanner(); core/selftest.lua gains one comment line and one call
line in SelfTest:run(). Everything strictly additive; the nil-guard blocks
(macro_torch.lua lines 22-28 and 29-36) are untouched and remain the only
writers of the two options.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@macro_torch.lua
@core/selftest.lua
@classes/druid/diag.lua
@core/events.lua
@.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js

Grounding facts, verified by reading at planning time (2026-09-07), live anchors:

- macro_torch.lua: license header lines 1-15; `if not macroTorch then macroTorch = {} end` at 18-20; cpBuildLog comment block 22-25 + nil-guard `if macroTorch.cpBuildLog == nil then / macroTorch.cpBuildLog = false` at 26-28; rawdiag2Enabled comment block 29-33 + nil-guard at 34-36; probe-warned reset comment 37-39 + `macroTorch._cpBuildLogProbeWarned = nil` at line 40 (current EOF, LF-terminated). Indent discipline: 4 spaces.
- core/selftest.lua: SelfTest:run() summary line 80-82, FAIL list 84-87, WARN list 89-92; line 94 comment `-- Print Druid skill diagnostics after selftest (quick task 260623-wrh-druid)`; line 95 `    macroTorch.printDruidDiag()`; line 96 `end` closes the function. 4-space indent.
- Invocation paths (verified earlier, do not re-verify): events.lua:74 runs macroTorch.SelfTest:run deferred ~30 frames after PLAYER_ENTERING_WORLD (login/reload); core/selftest.lua:990 is the /mt command path. `macroTorch._selfTestRan` prevents repeat output on zone transitions.
- Style reference: classes/druid/diag.lua printDruidDiag uses `macroTorch.show('[macro-torch] === ... ===', 'white')` headers and `'green'` entry lines; selftest summary uses `'white'`, warnings `'yellow'`, failures `'red'` (core/selftest.lua:75-91). This task's banner uses white headers + yellow entries per spec.
- bbcheck.js exists at .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js and is the repo-established syntax gate (no lua/luac on this host); prior quick plans (260907-0ya, 260907-mhh) ran `node $BB <file>` expecting `BALANCED` + exit 0.
- SM_Extend.lua is a build artifact (user memory sm-extend-is-build-artifact): never rebuild or analyze it here. The executor only touches macro_torch.lua and core/selftest.lua.
- Selftest test stubs temporarily replace macroTorch.show inside individual category runs (classes/druid/selftest.lua) and restore it before SelfTest:run() concludes; printConfigBanner runs after the last test and outputs via macroTorch.show, so no interaction. Do not use macroTorch.log in the banner (log persists to SavedVariables).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append CONFIG_OPTIONS registry + printConfigBanner() to macro_torch.lua</name>
  <files>macro_torch.lua</files>
  <action>Append a strictly additive block to macro_torch.lua immediately AFTER line 40
(`macroTorch._cpBuildLogProbeWarned = nil`, the current EOF; keep the file
LF-terminated with one final newline). Do not modify, wrap, reorder, or recomment
any existing line: the cpBuildLog comment block (22-25) and guard (26-28), the
rawdiag2Enabled comment block (29-33) and guard (34-36), and the probe-warned
reset (37-40) stay byte-identical. Use 4-space indentation to match lines 26-27
and plain ASCII English comments.

Block contents, in order:

1. A 3-4 line comment header naming the quick task (260907-sz4), stating that
   this is the global config options registry and banner, that the complete
   survey of user-tunable globals yields exactly these two entries, and that a
   future option is surfaced by appending one entry.

2. The registry table `macroTorch.CONFIG_OPTIONS = {` containing exactly two
   entries, first cpBuildLog then rawdiag2Enabled (mirroring declaration order
   lines 22-36), each a table with these five fields in this order and with a
   trailing comma on every field line (including the last field) so future
   single-entry appends stay uniform:
   - name: the option name as a string literal — `name = 'macroTorch.cpBuildLog',` and `name = 'macroTorch.rawdiag2Enabled',`
   - default: the literal default, mirroring the nil-guard assignment lines 27 and 35 — `default = false,` in both entries
   - desc: a one-line ASCII string — `desc = 'combo-point build cast log switch (Claw/Shred/Rake interval samples)',` for cpBuildLog and `desc = 'RAWDIAG2 Rip landing forensics master switch',` for rawdiag2Enabled
   - cmd: the in-game setter example — `cmd = '/run macroTorch.cpBuildLog=true',` and `cmd = '/run macroTorch.rawdiag2Enabled=true',`
   - get: an explicit function reference, never string-keyed resolution — `get = function() return macroTorch.cpBuildLog end,` and `get = function() return macroTorch.rawdiag2Enabled end,`
   Close with `}` on its own line.

3. The global banner function `function macroTorch.printConfigBanner()` (same
   style convention as classes/druid/diag.lua:5), implemented exactly as:
   - Header line first: `macroTorch.show('[macro-torch] === Global Config Options ===', 'white')`
   - Iterate `for _, opt in ipairs(macroTorch.CONFIG_OPTIONS) do` (the `_` dummy name is fine; ipairs is the Lua 5.0-safe traversal).
   - Inside the loop, resolve the current value exhaustively: start `current = 'unavailable'`, then `local ok, v = pcall(opt.get)`; on `ok` set `current = tostring(v)`. This must be the only read mechanism — the banner never assigns to either option, never fails when a getter or the value is nil, and applies no formatting to nil.
   - Per entry, print exactly two lines through macroTorch.show, both color 'yellow':
     entry line: `macroTorch.show('[macro-torch] ' .. opt.name .. ' = ' .. current .. ' (default: ' .. tostring(opt.default) .. ') - ' .. opt.desc, 'yellow')`
     setter line: `macroTorch.show('[macro-torch]    set: ' .. opt.cmd, 'yellow')`
   - Close the loop with `end`, then the end marker `macroTorch.show('[macro-torch] === End Config Options ===', 'white')`, then the function-closing `end`.

Contract: the new code READS option values only through the two explicit getter
references (opt.get) — do not use any form of string-keyed indexing into
macroTorch, no writes to macroTorch.cpBuildLog or macroTorch.rawdiag2Enabled
(the nil-guard blocks remain the only assignments in the file), no dependency on
cpBuildLog/rawdiag2 internals beyond returning the current value. Every added
line must satisfy the repo Lua 5.0 syntax constraint (user memory
wow-lua50-syntax — the FINAL verification carries the token gate) and contain
only ASCII characters; no trailing whitespace.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB macro_torch.lua && REG=$(grep -nF 'macroTorch.CONFIG_OPTIONS = {' macro_torch.lua | cut -d: -f1) && DEF=$(grep -nF 'function macroTorch.printConfigBanner()' macro_torch.lua | cut -d: -f1) && test "$REG" -gt 40 && test "$DEF" -gt "$REG" && test "$(grep -v '^--' macro_torch.lua | grep -cF "name = 'macroTorch.cpBuildLog',")" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF "name = 'macroTorch.rawdiag2Enabled',")" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF 'default = false,')" = "2" && test "$(grep -v '^--' macro_torch.lua | grep -cF "cmd = '/run macroTorch.cpBuildLog=true',")" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF "cmd = '/run macroTorch.rawdiag2Enabled=true',")" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF 'get = function() return macroTorch.cpBuildLog end,')" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF 'get = function() return macroTorch.rawdiag2Enabled end,')" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF 'for _, opt in ipairs(macroTorch.CONFIG_OPTIONS) do')" = "1" && test "$(grep -v '^--' macro_torch.lua | grep -cF 'pcall(opt.get)')" = "1" && grep -qF "'[macro-torch] === Global Config Options ==='" macro_torch.lua && grep -qF "'[macro-torch] === End Config Options ==='" macro_torch.lua && grep -qF "tostring(opt.default)" macro_torch.lua && grep -qF "set: ' .. opt.cmd" macro_torch.lua && test "$(grep -v '^--' macro_torch.lua | grep -cE '^[[:space:]]*macroTorch\.(cpBuildLog|rawdiag2Enabled)[[:space:]]*=')" = "2" && if grep -qE 'macroTorch\[opt\.' macro_torch.lua; then echo 'STRING-INDEX FOUND'; exit 1; fi && echo 'G1 CONFIG OK'</automated>
  </verify>
  <done>bbcheck prints `macro_torch.lua: BALANCED` (exit 0). The G1 chain echoes `G1 CONFIG OK`: the registry table and the banner definition both live after line 40 (registry above the function); exactly two entries carry the prescribed name/default/cmd literals with default=false mirroring both nil-guards; both explicit getter references, the ipairs loop and the pcall(opt.get) read are present; the white header/end markers, the yellow entry-format fragment (tostring(opt.default)) and the setter-format fragment are present; the only macroTorch.cpBuildLog / macroTorch.rawdiag2Enabled assignments in the file are the two nil-guard lines (count 2 — banner is read-only); no string-keyed option indexing exists.</done>
</task>

<task type="auto">
  <name>Task 2: Insert printConfigBanner() call into SelfTest:run() tail in core/selftest.lua</name>
  <files>core/selftest.lua</files>
  <action>
In core/selftest.lua, inside macroTorch.SelfTest:run(), insert exactly two new
lines between line 95 (`    macroTorch.printDruidDiag()`) and line 96 (`end`
closing the function), with 4-space indentation:

- Comment line: `    -- Print global config banner after selftest (quick 260907-sz4)`
- Call line: `    macroTorch.printConfigBanner()`

Modify nothing else in the file — line 95 and the closing `end` (and every line
above 95) stay byte-identical; the call is the last statement of run(), so the
banner fires after the diagnostics and after all test stubs have restored
macroTorch.show. This single call site covers both invocation paths: the
deferred login/reload run wired at core/events.lua:74 and the /mt command path
at core/selftest.lua:990; do not add or alter any _selfTestRan handling (the
existing dedupe governs the once-per-login behavior unchanged). The comment
must contain only plain ASCII and must not repeat the exact call literal of the
line below it. LF endings; no trailing whitespace.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB core/selftest.lua && D=$(grep -nF 'macroTorch.printDruidDiag()' core/selftest.lua | grep -vE '^[0-9]+:[[:space:]]*--' | head -1 | cut -d: -f1) && B=$(grep -nF 'macroTorch.printConfigBanner()' core/selftest.lua | grep -vE '^[0-9]+:[[:space:]]*--' | head -1 | cut -d: -f1) && test -n "$B" && test "$B" -gt "$D" && test "$(grep -nF 'macroTorch.printConfigBanner()' core/selftest.lua | grep -vE '^[0-9]+:[[:space:]]*--' | wc -l)" = "1" && test "$(git diff -U0 -- core/selftest.lua | grep -cE '^\+[^+]')" = "2" && echo 'G2 CALL OK'</automated>
  </verify>
  <done>bbcheck prints `core/selftest.lua: BALANCED` (exit 0). The G2 chain echoes `G2 CALL OK`: exactly one functional `macroTorch.printConfigBanner()` call line exists in core/selftest.lua (comment lines filtered), its line number is greater than the `macroTorch.printDruidDiag()` call line, and the selftest diff adds exactly 2 lines (one comment, one call) with zero lines elsewhere in the file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Addon -> chat frame (out) | The only new output surface: banner lines and option state leave the addon through macroTorch.show |
| In-game user -> addon (in) | The /run setter commands mutate the two globals; the banner only observes them (read paths via explicit getters) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-sz4-01 | Information Disclosure | macroTorch.printConfigBanner (chat echo of option state) | low | accept | The banner reveals no secrets — it echoes only the two opt-in debug switches, matching the existing printDruidDiag precedent (classes/druid/diag.lua) that already prints compute state to chat |
| T-sz4-02 | DoS (chat spam) | SelfTest:run tail | low | accept | Exactly 6 fixed banner lines appended once per run; the existing _selfTestRan dedupe is inherited unchanged, so no repeat print on zone transitions |
| T-sz4-03 | Tampering | macroTorch.CONFIG_OPTIONS registry | low | mitigate | Getters are read-only references and the banner contains zero assignments — enforced by the G1 read-only gate (the nil-guards remain the sole writers, still re-arming defaults on every login); registry mutation requires a source-line edit at the same trust level as the rest of the addon |
| T-sz4-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager installs in this task; the T-SC legitimacy row does not apply |
</threat_model>

<verification>
One FINAL chain covering both tasks, additive purity, the Lua 5.0 token gate, LF hygiene, and cross-file call-site scope (run from the repo root):

    cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB macro_torch.lua && node $BB core/selftest.lua && DIFF=$(git diff -U0 -- macro_torch.lua core/selftest.lua) && test "$(printf '%s\n' "$DIFF" | grep -cE '^-[^-]')" = "0" && if printf '%s\n' "$DIFF" | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo 'LUA50 TOKEN FOUND IN DIFF'; exit 1; fi && git diff --check -- macro_torch.lua core/selftest.lua && if grep -qU $'\r' macro_torch.lua core/selftest.lua; then echo 'CR FOUND'; exit 1; else echo 'LF OK'; fi && test "$(grep -rl 'macroTorch.printConfigBanner()' --include='*.lua' macro_torch.lua core/ classes/ entity/ interface_debug.lua 2>/dev/null | wc -l)" = "2" && echo 'FINAL GATE OK'

Must print, in order: `macro_torch.lua: BALANCED`, `core/selftest.lua: BALANCED`, `LF OK`, `FINAL GATE OK`, exit 0. Along with the per-task G1/G2 chains this proves: zero deleted or modified lines in either file (strictly additive), every added line free of the forbidden Lua 5.0 tokens, git diff --check clean with no CR characters, and the banner symbol only ever appears in macro_torch.lua (definition) and core/selftest.lua (call) across all source lua files.
</verification>

<success_criteria>
- bbcheck.js prints BALANCED and exits 0 for macro_torch.lua and core/selftest.lua.
- G1 CONFIG OK (Task 1) and G2 CALL OK (Task 2) chains exit 0.
- FINAL GATE OK: diff is strictly additive, added lines pass the Lua 5.0 token gate, git diff --check clean, LF preserved, banner symbol confined to the two intended files.
- `git status --porcelain` shows only macro_torch.lua and core/selftest.lua modified; SM_Extend.lua untouched (regenerated only on the user's game machine per user_setup).
- In-game smoke test per user_setup: banner appears once after selftest output on login with both entries at current=false default=false, appears again on /mt, and reflects a live toggle after /run macroTorch.cpBuildLog=true.
</success_criteria>

<output>
Create `.planning/quick/260907-sz4-reload-selftest-macro-torch-lua-config-o/260907-sz4-SUMMARY.md` when done
</output>