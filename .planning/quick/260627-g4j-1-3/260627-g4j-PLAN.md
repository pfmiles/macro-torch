# Quick Task 260627-g4j: 修复 spell_trace_immune.lua 日志问题

**Date:** 2026-06-27
**Status:** Ready

## Task 1: 修复日志与实际动作不一致 + land event 日志缺少法术名

**Files:** `core/spell_trace_immune.lua`

**Action:**
1. 删除 line 44 的 premature 日志 `macroTorch.show("recording immune by fail event: ...")` — `recordImmune` 内部已有准确的日志
2. 删除 line 56 的 premature 日志 `macroTorch.show("recording immune by land event: " .. landEvent)` — `recordImmune` 内部已有准确的日志

**Verify:** 免疫检测时只输出 `recordImmune` / `recordDefiniteBleeding` 内部的日志（带颜色），不再有误导性的 premature 日志。

**Done:** 两次日志删除完成，`recordImmune` 日志 "Spell: X is recorded IMMUNE to Y" 包含法术名，准确反映实际写入行为。