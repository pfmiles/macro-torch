---
phase: quick-260825-t86
plan: 01
subsystem: gameplay
tags: [catatk, druid, wow-1.12, lua, energy-discharge, ferocious-bite, selftest]

# Dependency graph
requires:
  - phase: quick-260825-s3v
    provides: "isDischarged per-frame re-entry guard in energyDischargeBeforeBite + OoC-only return guard in cp5Bite (the guard this task absorbs and upgrades)"
provides:
  - "energyDischargeBeforeBite return contract: true = discharge attempted (or re-entry handled), false = no discharge condition matched"
  - "cp5Bite defers the bite verdict on an attempted discharge (OoC and high-energy non-OoC frames alike — holes A/B closed)"
  - "New Category P selftest proving a true discharge return means no bite on the frame"
affects: [catatk-dps, selftest]

tech-stack:
  added: []
  patterns:
    - "Deferral-by-boolean: side-effecting per-frame functions report whether they acted; callers defer re-decided verdicts to the next click instead of guarding a single condition (OoC)"

key-files:
  created: []
  modified:
    - classes/druid/cat.lua
    - core/selftest.lua

key-decisions:
  - "energyDischargeBeforeBite reports true for the isDischarged re-entry guard, the regularAttack branch and the Rake fallback; false for the isPseudoInfiniteEnergy skip and the no-condition tail — no nil returns (locked decision 1)"
  - "cp5Bite's shouldDischarge block became `if macroTorch.energyDischargeBeforeBite(clickContext) then return end`, absorbing the s3v OoC-only guard; the unchanged ooc->readyBite/else->safeBite selection serves the boolean-false fall-through and the shouldDischarge=false direct bites (locked decisions 2-4)"
  - "Single atomic commit containing only classes/druid/cat.lua + core/selftest.lua; SM_Extend.lua regenerated by build.sh stays gitignored (locked decision 8)"

patterns-established:
  - "P-test stub discipline: capture all four collaborators (isRipPresent/safeBite/readyBite/energyDischargeBeforeBite/isFastBattleNotPvp), assert via a captured ok flag inside pcall, restore originals after pcall, UnitClass('player') short-circuit for non-Druid logins"

requirements-completed:
  - QUICK-260825-T86

coverage:
  - id: D1
    description: "energyDischargeBeforeBite exposes the attempted-discharge boolean exactly as decided (true: re-entry guard + both attempt branches; false: isPseudoInfiniteEnergy skip + no-condition tail) with both attempt branches still setting clickContext.isDischarged = true"
    requirement: QUICK-260825-T86
    verification:
      - kind: other
        ref: "build.sh + function-scoped grep gates (3x 'return true', 2x 'return false', 2x 'clickContext.isDischarged = true' within the function)"
        status: pass
      - kind: manual_procedural
        ref: "in-game /mt — full selftest suite green including the existing P-06 cp5Bite regression"
        status: unknown
    human_judgment: true
    rationale: "No headless Lua runtime exists in this environment; the behavioral proof (no nil returns, deferred bite) requires the in-game /mt selftest on the user's next login (established Phase 26 convention, non-blocking)"
  - id: D2
    description: "cp5Bite defers the bite whenever energyDischargeBeforeBite returns true — non-OoC high-energy bypass holes A/B closed — while all legacy bite paths (shouldDischarge=false direct bites, boolean-false fall-through to safeBite) behave exactly as before"
    requirement: QUICK-260825-T86
    verification:
      - kind: other
        ref: "cp5Bite function-scoped grep gates (1x deferral call, 1x 'clickContext.ooc then' for the untouched selection) + git diff -U0 hunks confined to cp5Bite and energyDischargeBeforeBite"
        status: pass
      - kind: manual_procedural
        ref: "in-game /mt — new test 'P: cp5Bite defers the bite when a discharge is attempted' passes with biteCalled == false"
        status: unknown
    human_judgment: true
    rationale: "The new selftest runs only in-game via /mt; static gates prove shape and scope but not the runtime deferral behavior"

# Metrics
duration: 5min
completed: 2026-08-25
status: complete
---

# Phase quick-260825-t86: Unified catAtk discharge-bypass fix (Scheme R, LOCKED) Summary

**energyDischargeBeforeBite now returns whether a discharge attempt was initiated, and cp5Bite defers the bite on that boolean — closing the two high-energy non-OoC bypass holes (A/B) left open by quick 260825-s3v, with a new Category P selftest proving true-return means no bite.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-25T13:11:14Z
- **Completed:** 2026-08-25T13:16:13Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- `energyDischargeBeforeBite` return contract implemented exactly as locked: `return true` for the isDischarged re-entry guard, the regularAttack branch and the Rake fallback; `return false` for the isPseudoInfiniteEnergy skip and the new no-condition tail — zero nil returns, both attempt branches still set `clickContext.isDischarged = true`
- `cp5Bite`'s shouldDischarge block reshaped to `if macroTorch.energyDischargeBeforeBite(clickContext) then return end`, absorbing and deleting the s3v OoC-only guard and its comment — high-energy non-OoC discharge frames (holes A/B) now defer the bite too
- New Category P selftest "P: cp5Bite defers the bite when a discharge is attempted" mirrors P-06's stub/restore structure and asserts `biteCalled == false` when the discharge stub returns true; P-06 remains byte-identical; registration count updated to 7
- `bash build.sh` exits 0; diff confined to the two planned functions in cat.lua plus the selftest insertion below P-06; single atomic commit with only the two code files (SM_Extend.lua gitignored)

## Task Commits

The plan mandates ONE atomic commit for all three tasks (locked decision 8) — no per-task commits.

1. **Task 1: Return-value contract in energyDischargeBeforeBite** — `96e55d1` (fix, single atomic commit covering tasks 1-3)
2. **Task 2: cp5Bite defers the bite on attempted discharge** — `96e55d1` (fix, single atomic commit covering tasks 1-3)
3. **Task 3: New P selftest, P-06 regression, diff gate, build, atomic commit** — `96e55d1` (fix, single atomic commit covering tasks 1-3)

## Files Created/Modified

- `classes/druid/cat.lua` - Return-value contract in `energyDischargeBeforeBite` + deferral reshape of cp5Bite's shouldDischarge block (holes A/B closed)
- `core/selftest.lua` - One new Category P test after P-06 + registration-count comment updated to 7

## Decisions Made

None — plan executed exactly as specified (design was locked pre-dispatch).

## Deviations from Plan

None — plan executed exactly as written. All three tasks, all verification gates, the build, the diff scope, and the single atomic commit matched the plan.

## Issues Encountered

- `classes/druid/cat.lua` uses CRLF line terminators, so sed's `$`-anchored range address (`/^function .../,/^end$/p`) never terminates on bare `end\r` lines and the function window ran to EOF (grep counts 9/11/2 instead of 3/2/2). Resolved by normalizing the stream with `tr -d '\r'` before the sed window — the plan's grep gates then produced the exact expected counts (3/2/2 in Task 1, 1/1/1 in Task 2). No file content was changed by this; the file keeps its CRLF style.
- The plan's in-game `/mt` check is deliberately non-blocking (no headless Lua runtime in this environment) — deferred to the user's next login per the plan's user_setup note.

## User Setup Required

None — no external service configuration required. On the next login, running in-game `/mt` confirms both the existing "P: cp5Bite triggers bite at 5CP in fast battle without Rip or immunity" test and the new "P: cp5Bite defers the bite when a discharge is attempted" test pass with the rest of the suite (non-blocking, established Phase 26 convention).

## Next Phase Readiness

- All six truth must_haves satisfied by code shape, static gates, build and diff scope; the two P-test runtime results await in-game confirmation.
- No blockers; quick task self-contained.

## Self-Check: PASSED

- FOUND: classes/druid/cat.lua (modified, commit 96e55d1)
- FOUND: core/selftest.lua (modified, commit 96e55d1)
- FOUND: 260825-t86-SUMMARY.md
- FOUND: commit 96e55d1 — the single atomic code commit; working tree clean apart from this summary and .planning docs (orchestrator-owned)

---
*Phase: quick-260825-t86*
*Completed: 2026-08-25*