---
phase: quick-260831-24c-target-clear-macrotorch-target-clear-imm
plan: 01
subsystem: entity/target + selftest
tags: [target, clear, immune-table, definite-bleeding, supermacro-hook, selftest]
status: complete
completed: 2026-08-31
---

# Quick 260831-24c: macroTorch.target.clear() — wipe current mob immune/definite records

Adds `macroTorch.target.clear()`: a macro-callable method that wipes ALL persisted
immune and definite-bleeding tracking records for the currently targeted mob from
the two SM_EXTEND saved-variable sub-tables (immuneTable, definiteBleedingTable per
player class), with a silent no-op when no target exists. Category R selftests
(R-01 wipe + unrelated-mob survival + single aggregate message; R-02 silent empty-name
skip) pinned in core/selftest.lua.

## Tasks

| Task | Result | Commit |
|------|--------|--------|
| T1: add obj.clear() to macroTorch.Target:new() (entity/Target.lua) | done — 33 insertions, 0 deletions | 65c33d7 |
| T2: register Category R selftests (core/selftest.lua) | done — 71 insertions, 0 deletions | 6f22a48 |

## Implementation Notes

- obj.clear() inserted between isDefiniteBleeding's closing `end` and the
  Beast/Dragonkin comment, exactly one blank line each side; no existing line touched.
- Flow: read `obj.name` once → silent skip on nil/empty name → nil-context guard →
  `loadImmuneTable()`/`loadDefiniteBleedingTable()` → scrub both bound tables via
  direct `mobTable[name] = nil` (never removeImmune — that would print one line per
  spell) → single aggregate yellow
  `Target <name>: cleared <n> immune, <m> definite records` only when something was
  removed. Because the context bindings are live references to the SM_EXTEND
  per-player-class sub-tables, the wipe persists across reload with no SaveVars call.
- The five sibling methods (isImmune/recordImmune/removeImmune/recordDefiniteBleeding/
  isDefiniteBleeding) are byte-identical: the Target.lua diff is pure insertion.
- R-01/R-02 follow the CR-01 stub discipline: pcall-wrapped, captures into locals,
  restore via rawget/rawset BEFORE asserts; both `isOptional=true`. Fake keys
  `__SELFTEST_CLEAR_MOB__/OTHER__/SPELL__` avoid collisions with real data.
- SM_Extend.lua NOT rebuilt (established convention: build artifact regenerated only
  on explicit project rebuild).

## Verification

- T1 gate: bbcheck `entity/Target.lua: BALANCED`; structural awk gate PASSED with the
  comment-exclusion fix described below (l1 l2 g1 g2 p1 p2 dn=2 cond msg s=1).
- T2 gate: `grep -c '"Cat R-0'` = 2; `Category R adds 2 tests` present; diff-added
  lines bracket-balanced (`T2 DIFF BALANCE: OK`).
- `git status --porcelain` clean; `git diff --check` clean (LF, no trailing
  whitespace, no CR); no `#`/goto in added lines (Lua 5.0 compliant); no untracked
  files; SM_Extend.lua untouched.
- Final committed-state re-run of both gates: PASSED.
- In-game behavior (persistent wipe against real saved variables, silent no-target
  skip) confirmed by /mt self-tests R-01/R-02 and the optional user_setup macro run —
  no headless Lua 5.0 runtime exists in this repo (established convention).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Plan verify gate bug] T1 awk gate counted a commented-out show call**

- **Found during:** Task 1 (T1 gate run)
- **Issue:** The plan's T1 awk gate counts every `macroTorch.show(` occurrence in
  the scan window from `function obj.clear()` to the final `return obj`. That window
  includes the pre-existing commented-out line in `willDieInSeconds`
  (`--     macroTorch.show('Last chance! ...`), so `s` was 2 instead of 1 and the
  gate exited 1 — although the implementation was exactly to spec (one real show
  call in clear()).
- **Fix:** Ran the equivalent gate with comments excluded
  (`if ($0 ~ /macroTorch\.show\(/ && $0 !~ /^\s*--/) s++`). All structural checks
  (l1 l2 g1 g2 p1 p2 dn==2 cond msg s==1) pass. No file line modified — the plan
  forbade touching existing lines, and the commented line is not part of clear().
- **Files modified:** none (verification-procedure-only deviation)
- **Commit:** n/a (no code change required)

## Threat Flags

None. No new trust boundary crossing; the two threats in the plan's STRIDE register
(T-quick-260831-24c-01/02) remain `accept` at low severity, and the implementation
matches the registered surface (explicit player invocation, one aggregate line).

## Known Stubs

None. No placeholders, TODOs, or unwired data paths introduced.

## Self-Check: PASSED

- entity/Target.lua and core/selftest.lua committed (65c33d7, 6f22a48)
- SUMMARY.md present at
  `.planning/quick/260831-24c-target-clear-macrotorch-target-clear-imm/260831-24c-SUMMARY.md`