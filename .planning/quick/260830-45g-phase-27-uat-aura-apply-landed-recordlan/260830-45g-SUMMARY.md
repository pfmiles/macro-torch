---
phase: quick-260830-45g
plan: 01
subsystem: gameplay
tags: [catatk, druid, wow-1.12, lua, land-tracing, print-order, uat-feedback, spell-trace-core]

# Dependency graph
requires:
  - phase: 27-catatk-event-driven-land-tracing-refactor
    provides: "event-driven land framework (recordLandEvent synchronous listener dispatch, onLandEvent registration, the Ferocious Bite renewal listener in classes/druid/Druid.lua) and the land-feedback show calls this task reorders"
provides:
  - "Causal print order in both land sources: the green <spell> cast on <mob> landed: line now prints BEFORE macroTorch.recordLandEvent, so listener-driven renewal output (FB Renewing rake/rip lines) follows the announcement it is a consequence of"
  - "Ordering expectation documented in 27-UAT.md: a tracing spell's own green landed: line prints BEFORE any listener-driven renewal lines it triggers"
affects: [catatk-dps, phase-27-uat]

# Actuals (#2632) — chars/4 over the realized diff of the three task commits, same scale as the estimate
actuals:
  tokens: 786
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Causal-order land announcement: every land entry point announces (green show) BEFORE recordLandEvent, because recordLandEvent dispatches land listeners synchronously and listener output is a consequence of the hit"

key-files:
  created: []
  modified:
    - core/spell_trace_core.lua
    - .planning/phases/27-catatk-event-driven-land-tracing-refactor/27-UAT.md

key-decisions:
  - "onSelfDamageLine (self-hit path, Rake/FB): pairLandIntent first (no print side effects), then the guarded green show block, then recordLandEvent last — comment extended with the Announced BEFORE recordLandEvent rationale"
  - "processRawAuraApply (aura-apply path, Rip/Pounce): show stays guard-less inside if intent (the pairing already carried the loginContext / target-is-attackable guards), moved before recordLandEvent for cross-source causal-order uniformity per the approved A方案"
  - "Three atomic per-task commits (orchestrator directive) rather than one combined commit: each commit contains only the two allowed files, satisfying the plan's file-confinement criterion"
  - "No selftest edits: the reorder changes only the ORDER of the same show calls, and classes/druid/selftest.lua stubs macroTorch.show without asserting print order"

patterns-established:
  - "Announcement-before-consequences ordering applies uniformly to both land sources (self-hit and aura-apply), even though Rip/Pounce have no listeners today"

requirements-completed:
  - QUICK-260830-45G

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "Causal-order land feedback: both land entry points print the green landed line before recordLandEvent's synchronous listener dispatch; 27-UAT.md documents the printed-order contract"
    requirement: QUICK-260830-45G
    verification:
      - kind: other
        ref: "awk order gates T1 (onSelfDamageLine) and T2 (processRawAuraApply) exit 0 — pairLandIntent then show then recordLandEvent; comment grep gates for both new comment fragments pass"
        status: pass
      - kind: other
        ref: "node .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js core/spell_trace_core.lua -> BALANCED (exit 0); git diff --check clean (LF preserved); diff confined to two statement-block moves + two comment extensions + one UAT sentence"
        status: pass
      - kind: manual_procedural
        ref: "in-game Phase 27 UAT (pending, deferred by user): the green FB landed line must appear above the two Renewing lines in the chat frame"
        status: unknown
    human_judgment: true
    rationale: "The visual print-order confirmation runs in-game as part of the already-pending Phase 27 UAT; no headless Lua runtime exists in this environment (established convention, non-blocking per plan user_setup)"

# Metrics
duration: 1min
completed: 2026-08-30
status: complete
---

# Quick 260830-45g: Restore causal order of land-feedback prints

**Restored causal log order in `core/spell_trace_core.lua`: in both
`macroTorch.onSelfDamageLine` and `macroTorch.processRawAuraApply` the green
`<spell> cast on <mob> landed: <time>` confirmation now prints BEFORE
`macroTorch.recordLandEvent`, so the Ferocious Bite landed line precedes the
Renewing rake/rip lines its synchronous land listener emits (announcement
before consequences), with the ordering contract documented in 27-UAT.md.**

## Performance

- **Duration:** ~1 min execution span (3 atomic commits: 94682b3 -> 37989b8 -> 194d907)
- **Tasks:** 3/3 complete
- **Files:** 2 modified (both exactly as planned)

## Deviations from Plan

No Rule 1-4 deviations — plan executed exactly as written.

Commit-granularity note (not a deviation): the orchestrator's constraint
"commit each task atomically" was followed as three atomic per-task commits
instead of the plan's suggested single combined commit. Each commit contains
only the two allowed files, so the success criterion's file-confinement intent
holds; the combined diff `94682b3~1..HEAD` is exactly the two statement-block
moves, two comment extensions, and one UAT sentence.

## Known Stubs

None introduced. No new values, guards, or data flows — a pure statement
reorder plus comments and one doc sentence.

## Threat Flags

None. Per the plan's threat model, log content is unchanged (same show calls,
strings, and guards); only the print order moved, removing a misleading
ordering rather than adding surface.

## Self-Check: PASSED

- [x] `core/spell_trace_core.lua` modified and committed (94682b3, 37989b8)
- [x] `.planning/phases/27-catatk-event-driven-land-tracing-refactor/27-UAT.md` modified and committed (194d907)
- [x] bbcheck.js: `core/spell_trace_core.lua: BALANCED` (exit 0)
- [x] Awk order gates T1 + T2 both exit 0; both new comment fragments present
- [x] `git diff --check` clean; `git status --porcelain` empty after commits
- [x] Untouched per plan: `classes/druid/Druid.lua`, `classes/druid/selftest.lua`, `recordLandEvent`, `onLandEvent`, `finalizeFail`, `pairLandIntent`