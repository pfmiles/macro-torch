---
quick_id: 260914-3vh
slug: revise-planning-catatk-core-principles-m
description: "4-lock revision of .planning/catAtk-core-principles.md (untracked local principles charter, working-tree only): widen rule-3 exception to both bleeds (Rip<=2.3s / Rake<=1.3s) with threshold-asymmetry rationale, add rake-refresh 1.3s Appendix B row, add NOT kill-shot line to rule-8 IF block, bump footer timestamp to 2026-09-14"
date: 2026-09-14
autonomous: true
files:
  - .planning/catAtk-core-principles.md        # 只改 4 个锚定点（工作树-only，永不 commit）
  - .planning/STATE.md                         # quick-flow 行更新（docs commit 范围内）
  - .planning/quick/260914-3vh-revise-planning-catatk-core-principles-m/260914-3vh-SUMMARY.md
---

# PLAN: catAtk 原则文档 4 处锁定修订（工作树-only）

## 目标

对 `.planning/catAtk-core-principles.md`（untracked 本地原则宪章，`.gitignore:34`）执行**恰好 4 处锁定编辑**，把代码侧已落地（quick 260910-ilu / 260914-0ql / 260914-1t0）的行为同步进原则文档：

1. 规则 3 例外从「只保护割裂 2.3s」扩展为「保护双流血：割裂 ≤ 2.3s 或扫击 ≤ 1.3s」，标题改为「流血保护优先于泄能」，并加一句阈值非对称的理由；
2. 附录 B 在「撕咬割裂刷新保护 | 2.3s」行之后新增「撕咬扫击刷新保护 | 1.3s」行；
3. 规则 8 IF 条件块内新增一行 `NOT isKillShotOrLastChance` 排除门（斩杀期暂停一切等待窗类动作，规则 9；代码已实现，仅文档缺行）；
4. 页脚时间戳 `2026-08-23` → `2026-09-14`，作者行不动。

**只做这 4 处，其余字节必须逐字不变**（ZERO-DELTA，由 /tmp 快照 diff 证明）。文档是原则宪章（精华风格）：新增措辞简洁、原则层面，不新增伪代码块、不改规则编号。

## 锁定约束（违反即失败）

1. **该文件永不进 git**：全程禁止 `git add`/`git commit` 该文件；修订只落工作树。git diff/status 不是本任务的验证手段（untracked），一切文档验证用 grep/exact-text + /tmp 快照 diff。
2. **只改 4 个锚定点**，编辑只用 Edit 工具（旧→新逐字替换）；**禁止整体重写（Write）**，禁止修改行尾/行宽/LF 约定。
3. **禁止运行**：`build.sh`、任何 Lua 解释器、bbcheck——纯 markdown 文档，N/A。
4. 文档正文保持中文、精华风格；commit 信息英文（仅 Task 3 的 docs commit）。
5. 改变了工作树后若任何验证失败 → 从 /tmp 快照恢复重做（见「失败即回退」）。

## 背景事实（锚点，2026-09-14 本次 Read 为准；行号仅导航，编辑一律以行内容为锚）

- 文件当前 `wc -l` = **492**（Read 行号 1..493，**末行 `*作者：pf_miles*` 无换行符** —— 不得补加换行符，它是字节级事实，ZERO-DELTA 由 diff 门证明）；```` 围栏计数（`grep -c '^```'`）= **30**；两者除 4 处编辑的影响外不得改变。
- 代码事实（本任务只同步文档，不改代码）：`cp5Bite`（`cat.lua`）已含双流血保护（quick 260914-0ql 扫击 1.3s 门 + 260914-1t0 续期 pin）；`shouldCastFFDuringWaitWindow`（`Druid.lua`）已含 kill-shot 排除门（quick 260910-ilu）。
- 四个锚点当前文本：
  - **E1 规则 3 例外段**（87-88 行，两行整块替换）：
    `**例外 — 割裂保护优先于泄能：**` / `撕咬会刷新目标身上的割裂持续时间。若割裂剩余 ≤ 2.3s，跳过泄能立即撕咬，防止割裂断档（见 \`cp5Bite\`，\`cat.lua\`）。`
  - **E2 附录 B**（454 行后追加一行）：锚行 `| 撕咬割裂刷新保护 | 2.3s | 割裂 ≤ 2.3s 剩余时跳过泄能以保护割裂 |`
  - **E3 规则 8 块**（232-241 行；在 239 行 `AND NOT shouldDoReshift …` 与 240 行 `THEN ff()` 之间插入一行）
  - **E4 页脚**（492 行）：`*最后更新：2026-08-23*  `（行末恰好 2 个空格，markdown 软换行，必须保留）

<!-- planner-discipline-allow: LIT -->
E1/E4 的编辑锚（old 字符串）本身含有下述负向断言中的字面量——锚是 Edit 的工具载荷，负向 grep 只作用于编辑后的文档文件，二者不同域。

## 任务

### Task 1: 快照基线 + 4 处锚定编辑

- **文件**: `.planning/catAtk-core-principles.md`

- **步骤 0 — 基线断言 + 快照**（任一失败即停）:
  ```bash
  f=.planning/catAtk-core-principles.md
  test -f "$f" || { echo "doc missing"; exit 1; }
  git check-ignore "$f"      # 必须输出该路径（证明 igniored/untracked）
  test -z "$(git status --porcelain -- "$f")"   # 无跟踪状态
  test "$(grep -c '^```' "$f")" = "30"          # 围栏基线
  wc -l < "$f"                                   # 记录 N（应为 492；末行无换行符，勿补加）
  # 锚点唯一性预检（每个 old 锚点在全文件恰好出现 1 次）
  for s in \
    '**例外 — 割裂保护优先于泄能：**' \
    '割裂剩余 ≤ 2.3s，跳过泄能立即撕咬，防止割裂断档' \
    '| 撕咬割裂刷新保护 | 2.3s | 割裂 ≤ 2.3s 剩余时跳过泄能以保护割裂 |' \
    '等待比变身回能更划算' \
    '*最后更新：2026-08-23*'; do
    c=$(grep -cF "$s" "$f" || true)
    [ "$c" = "1" ] || { echo "ANCHOR NOT UNIQUE (c=$c): $s"; exit 1; }
  done; echo ANCHORS-OK
  cp "$f" /tmp/catAtk-core-principles.260914-3vh.before.md   # 唯一回退源
  ```

- **编辑 E1 — 规则 3 例外段**（old 为 2 行整块，new 仍为 2 行）:
  old（逐字从文件复制）:
  ```
  **例外 — 割裂保护优先于泄能：**
  撕咬会刷新目标身上的割裂持续时间。若割裂剩余 ≤ 2.3s，跳过泄能立即撕咬，防止割裂断档（见 `cp5Bite`，`cat.lua`）。
  ```
  new:
  ```
  **例外 — 流血保护优先于泄能：**
  撕咬会刷新目标身上已存在的流血（割裂/扫击）持续时间。若割裂剩余 ≤ 2.3s 或扫击剩余 ≤ 1.3s，跳过泄能立即撕咬，防止流血断档（见 `cp5Bite`，`cat.lua`）。阈值非对称是刻意的：扫击断档可随时重补（不消耗连击点），割裂断档则需重建 5 星连击点，故割裂获得更宽的保护窗口。
  ```

- **编辑 E2 — 附录 B 新增行**（在锚行正后方追加一行，镜像邻行格式）:
  old: `| 撕咬割裂刷新保护 | 2.3s | 割裂 ≤ 2.3s 剩余时跳过泄能以保护割裂 |`
  new（两行）:
  ```
  | 撕咬割裂刷新保护 | 2.3s | 割裂 ≤ 2.3s 剩余时跳过泄能以保护割裂 |
  | 撕咬扫击刷新保护 | 1.3s | 扫击 ≤ 1.3s 剩余时跳过泄能以保护扫击 |
  ```

- **编辑 E3 — 规则 8 块新增 NOT kill-shot 门**（插入在 `AND NOT shouldDoReshift` 行与 `THEN ff()` 行之间；`--` 注释标记对齐块内既有 `--` 列）:
  old: `AND NOT shouldDoReshift                          -- 等待比变身回能更划算`（空格数按文件实际字节，从文件复制该行）
  new（两行）:
  ```
  AND NOT shouldDoReshift                          -- 等待比变身回能更划算
  AND NOT isKillShotOrLastChance                   -- 斩杀期暂停所有等待窗类动作（规则9）
  ```

- **编辑 E4 — 页脚时间戳**（保留行末 2 个空格；作者行 493 不动）:
  old: `*最后更新：2026-08-23*  `（行末恰好 2 个空格）
  new: `*最后更新：2026-09-14*  `（行末仍是恰好 2 个空格）

- **验证**（编辑本身的最小自检；全量在 Task 2）:
  ```bash
  f=.planning/catAtk-core-principles.md
  grep -qF '**例外 — 流血保护优先于泄能：**' "$f"
  grep -qF '若割裂剩余 ≤ 2.3s 或扫击剩余 ≤ 1.3s' "$f"
  grep -qF '| 撕咬扫击刷新保护 | 1.3s |' "$f"
  grep -qF 'AND NOT isKillShotOrLastChance' "$f"
  grep -qF '*最后更新：2026-09-14*' "$f"
  echo GOT-4-EDITS
  ```

### Task 2: ZERO-DELTA + 精确文本验证电池（纯 bash，无 git）

- **操作**: 运行下列全部门；任何一条失败 → 按「失败即回退」恢复后重做。
  ```bash
  f=.planning/catAtk-core-principles.md
  b=/tmp/catAtk-core-principles.260914-3vh.before.md

  # (1) 正向精确断言 —— 4 处新文本全部就位
  grep -qF '**例外 — 流血保护优先于泄能：**' "$f"
  grep -qF '撕咬会刷新目标身上已存在的流血（割裂/扫击）持续时间。若割裂剩余 ≤ 2.3s 或扫击剩余 ≤ 1.3s，跳过泄能立即撕咬，防止流血断档' "$f"
  grep -qF '阈值非对称是刻意的：扫击断档可随时重补（不消耗连击点），割裂断档则需重建 5 星连击点，故割裂获得更宽的保护窗口。' "$f"
  grep -qF '| 撕咬扫击刷新保护 | 1.3s | 扫击 ≤ 1.3s 剩余时跳过泄能以保护扫击 |' "$f"
  grep -qF 'AND NOT isKillShotOrLastChance' "$f"
  grep -qF -e '-- 斩杀期暂停所有等待窗类动作（规则9）' "$f"
  grep -qF '*最后更新：2026-09-14*' "$f"

  # (2) 旧文本清空断言
  test "$(grep -cF '割裂保护优先于泄能' "$f" || true)" = "0"
  test "$(grep -cF '若割裂剩余 ≤ 2.3s，跳过泄能立即撕咬，防止割裂断档' "$f" || true)" = "0"
  test "$(grep -cF '2026-08-23' "$f" || true)" = "0"

  # (3) ZERO-DELTA 全文件审计 —— 快照 diff（替代 git）
  N=$(wc -l < "$b"); M=$(wc -l < "$f")
  test $((M - N)) = 2 || { echo "LINE DELTA $((M-N)) != 2"; exit 1; }   # N=492 → 494
  diff -u "$b" "$f" > /tmp/catAtk-3vh.diff || true
  test "$(grep -c '^@@' /tmp/catAtk-3vh.diff)" = "4"     # 恰好 4 个 hunk
  test "$(grep -c '^-[^-]' /tmp/catAtk-3vh.diff)" = "3"  # 恰好 3 行删除(E1 旧段 2 行 + E4 日期行 1 行)
  test "$(grep -c '^+[^+]' /tmp/catAtk-3vh.diff)" = "5"  # 恰好 5 行新增(E1 新段 2 行 + E2 表行 1 + E3 门行 1 + E4 日期行 1)
  # 目视 /tmp/catAtk-3vh.diff：4 个 hunk 仅对应 规则3 / 规则8 / 附录B / 页脚，无任何其它字节变化

  # (4) 结构与风格不变量
  test "$(grep -c '^```' "$f")" = "30"     # 伪代码围栏数不变
  test "$(grep -cF '*作者：pf_miles*' "$f")" = "1"   # 作者行逐字未动
  sed -n '492,493p' "$f" | cat -A          # 目视：时间戳行含 2 尾随空格、作者行原文；末行无换行符 → 作者行末尾无 `$`，不得补加

  # (5) 常量漂移审计（与快照逐 token 对比，代替 git show HEAD:）
  for t in '8.5s' '25s' '75%' '15%' '2.3s' '10s' '9s' '2s' '1s' '1.5s'; do
    a=$(grep -cF "$t" "$b" || true); c=$(grep -cF "$t" "$f" || true)
    test "$a" = "$c" || { echo "CONSTANT DRIFT: $t $a -> $c"; exit 1; }
  done
  echo ALL-TEXT-GATES-PASS
  ```

### Task 3: quick-flow 收尾（SUMMARY + STATE.md 行 + docs commit；文档本体不入库）

- **操作**:
  1. 在 quick 目录写 `260914-3vh-SUMMARY.md`：四编辑对照（含新旧文本要点）、验证电池结果（Task 2 全绿输出）、「文档本体未 commit（工作树-only，untracked）」标注。
  2. STATE.md（用 Edit）：`## Quick Tasks Completed` 表在 `260914-1t0` 行之后追加一行（列对齐现有行，Commit 列为本次 docs commit 的 sha）：
     `| 260914-3vh | catAtk 原则文档 4 处锁定修订：规则3 例外扩展至双流血（扫击 ≤1.3s 并行门）+ 阈值非对称理由、附录B 新增撕咬扫击刷新保护 1.3s 行、规则8 补 NOT isKillShotOrLastChance 排除门、页脚时间戳→2026-09-14；工作树-only 不入 git | 2026-09-14 | <docs-commit-sha> | [260914-3vh-revise-planning-catatk-core-principles-m](./quick/260914-3vh-revise-planning-catatk-core-principles-m/) |`
     并按 quick-flow 惯例更新 frontmatter `last_updated` / `last_activity` / `last_activity_desc`。
  3. docs commit——**显式路径，禁止 `git add -A` / `git add .`**：
     ```bash
     git add .planning/quick/260914-3vh-revise-planning-catatk-core-principles-m/260914-3vh-PLAN.md \
             .planning/quick/260914-3vh-revise-planning-catatk-core-principles-m/260914-3vh-SUMMARY.md \
             .planning/STATE.md
     git commit -m "docs(260914-3vh): 4-lock catAtk principles revision (widen rule-3 bleed protection, add rake 1.3s appendix row, rule-8 kill-shot gate, bump footer timestamp)"
     ```
  4. **绝不 `git add`/`git commit` `.planning/catAtk-core-principles.md`**——修订只留在工作树。
- **验证**:
  - `git status --porcelain -- .planning/catAtk-core-principles.md` 输出为空（未跟踪入仓；文件本身仍在工作树并可被 grep 命中）
  - `git show --stat --name-only HEAD` 只列上述 3 个路径，**不含** `catAtk-core-principles.md`
  - `git log -1 --format=%s` 输出上述英文 commit message
  - `git check-ignore .planning/catAtk-core-principles.md` 仍输出该路径

## 失败即回退

- 任一编辑锚点不唯一/不匹配、或 Task 1/2 任一验证失败 → 立即 `cp /tmp/catAtk-core-principles.260914-3vh.before.md .planning/catAtk-core-principles.md` 恢复，修正后重跑；不得在偏离状态上继续。
- 若 diff 出现第 5 个 hunk / 多余字节变化 → 说明编辑超出清单，恢复快照后只重做 4 处锚定替换。
- Task 3 的 docs commit 若误含文档本体 → `git rm --cached` 修正后重提交；工作树版本保留不动。

## 完成标准

- 4 处编辑全部就位，Task 2 输出 `ALL-TEXT-GATES-PASS`；快照 diff 恰 4 hunk（-3/+5），`wc -l` 492 → 494（末行仍无换行符），围栏 30 不变；
- 原则文档仍在工作树、仍被 `.gitignore` 忽略、从未进入任何 commit；
- docs commit（PLAN/SUMMARY/STATE.md）完成，STATE.md 行含 commit sha。