---
quick_id: 260913-46s
phase: quick
plan: 260913-46s
subsystem: tools
tags: [cpdamage, ooc-bite, cc-decisions, erps, thresholds, 28-OOC-BITE-CRITERIA, lua-5.0]
requires: []
provides: ["OOC-bite criteria auto-calc block in cpdamage screen report and --json-out archive"]
affects: [tools/cpdamage.lua]
status: complete
---

# Quick Task 260913-46s: tools/cpdamage.lua 新增 "OOC-Bite Criteria" CC 咬决策常量自动计算块（--erps 参数 + --json-out 同步补段）Summary

`lua tools/cpdamage.lua <SV> [--erps N] [--json-out <file>]` 的报告在既有 `decisions:` 段之后新增一段 additive 的 "OOC-Bite Criteria"：按 28-OOC-BITE-CRITERIA.md 锁定的公式与读取点，从 aggregate 层自动算出 CC（清晰预兆）咬决策的两条翻转阈值 R*(A/B) = (S−35b)/E 与 R*(D/B) = (C−37b)/E（S=bleed2 shred 均伤、C=各流血层 claw 均伤、b=bite 回归斜率、E=最优 claw 档 avg dmg/e），并按 `--erps`（能量回速，缺省 10/s）给出 bleed2/bleed3 各自的 A/B/D 分段判定（A=CC 直接咬、B=免费 builder 后立即咬、D=泄能再咬·现状）；`--json-out` 归档同步新增顶层 `oocBite` 块且保持严格 JSON。11 字段 cpDamage schema、宏本体、Druid.lua 零改动（diff 为纯 212 insertions / 0 deletions、单文件）；selftest 35→49 项，Lua 5.0.3/5.1.5/5.4.7 三解释器全绿；屏显新段经 pre/post 实跑 diff 实证为纯 additive（零删除行）；bbcheck BALANCED。

## Tasks Completed

### Task 1: 实现 oocBiteCriteria + 屏显/JSON/CLI 接线 + 14 项 selftest（RED→GREEN），一个 commit
- **Commit:** `a8e7852` — `feat(tools): add OOC-bite criteria auto-calc block to cpdamage screen report and json (R*(A/B)/R*(D/B) thresholds, --erps flag, per 28-OOC-BITE-CRITERIA.md read points)`
- **Changes:** 仅 `tools/cpdamage.lua`（+212 行，0 删行）：
  - flag 常量组追加 `FLAG_ERPS = '-' .. '-erps'`、`DEFAULT_ERPS = 10`（拼接拼写，bbcheck 纪律）；
  - 新增纯函数 `oocBiteCriteria(stats, erps)`（decisionLines 之后）：erps=`tonumber(erps) or 10`；regression 不可用（或 stats.biteRegression 缺失）→ `usable=false, note=<reason 或 'no regression'>` 提前返回、E/rAB/rDB/verdicts 全 nil；可用 → `usable=true, b, a` 直通，E=claw 档 0..3 的 max avgEff（n>0 且非 nil，缺失桶安全跳过），rAB={S=shred[2].avgDmg, E, r=(S−35b)/E}（S2 或 E 缺失则 rAB 置 nil），rDB={bleed2/bleed3 各层 claw n>0 时 {C=avgDmg, E=avgEff, r=(C−37b)/E}}（两层全缺则 rDB 置 nil），verdicts 在 rAB 存在时按 `erps >= rAB.r → 'A'`、`elseif erps >= rDB[t].r → 'B'`、`else 'D'` 逐层取值；
  - 新增屏显函数 `printOocBiteBlock(crit)`（printBiteLine 之后）：数字 b/E 走 `fmtEff` %.4f、S/C/a/R* 走 `fmtDmg` %.2f、erps 回显 `tostring`；行序固定 A/B → D/B bleed2 → D/B bleed3 → verdict；降级行文本锁定（regression 不可用 / no bleed2 shred samples / no bleed<N> claw samples / verdict 层 `n/a`）；全部字符串字面量无相邻连字符对、无裸引号；
  - `printReport` 在 decisions for 循环结束与 dropped 警告之间插入 `printOocBiteBlock(res.oocBite)`，其余零改动；
  - `main()`：`local erps = DEFAULT_ERPS`；参数循环在 FLAG_JSON_OUT 分支后插入 FLAG_ERPS 分支（`tonumber(args[i+1])` 为 nil 或 <0 → `error: --erps requires a non-negative energy-per-second number` + printUsage + exit 1）；res 表 decisions 行后追加 `oocBite = oocBiteCriteria(statsRef, erps),`；printUsage 在 json-out usage 行后新增一行（既有三行一字不动）；
  - `runSelftest` 在 decs 断言与 banner 之间插入 `ctrlStats`（数值取自文档本批基准值：claw[2] 563.86/15.24、claw[3] 647.11/17.49、shred[2] 684.95、回归 usable b=6.8545 a=1039.16）+ 14 项断言（顺序锁定，nil 短路由保护空解引用），banner 达 `selftest: ALL 49 PASSED`。
- **RED 见证:** 仅插入 14 项断言（未写实现）时，5.4.7 上非零退出 + `attempt to call a nil value (global 'oocBiteCriteria')` 调用栈 —— 新门实测先失败。
- **GREEN:** 完整实现后 5.4.7 上 `selftest: ALL 49 PASSED` + exit 0。

### Task 2: 全电池验证（零代码变更；无缺陷，无补 commit）
三项关键证明（计划点名证据）：
1. **三解释器 49 项全绿:** 5.0.3 / 5.1.5 / 5.4.7 各跑 `--selftest` 均打印 `selftest: ALL 49 PASSED` 且 exit 0。
2. **Node 严格 JSON:** `lua tools/cpdamage.lua /tmp/luabuild/sv.lua --json-out /tmp/luabuild/out-cc.json --erps 26`（5.0.3）→ Node `JSON.parse` 打印 `STRICT-JSON-OK oocBite erps=26 old keys intact`（`oocBite.erps === 26`，既有顶层键 decisions/aggregate/batches/dropped/source/generatedAt/truncated 完整）；该真实样本 regression n<3，观见正确降级：`OOC-Bite Criteria (...; assumed ERPS = 26/s):` 后仅 `  bite regression unavailable (n<3) - thresholds skipped`。
3. **屏显零删除 additive diff:** 同一输入、同一 `--json-out` 路径（两版本均不带 `--erps` → 默认 10），pre（改动前快照）与 post 版本 stdout `diff` 结果 `grep -c '^<'` = **0**（既有输出零删除）、`grep '^> OOC-Bite Criteria'` 命中（新段以该行起 additive，共 2 行新增 = 段首 + 该样本的降级行）。

其余电池项（全绿）：默认 `--erps` 观测（屏显含 `assumed ERPS = 10/s` 与 OOC-Bite Criteria 段首行）；非法 `--erps` 拒绝 ×3（`abc` / `-3` / 缺参，均 exit 1 + error 行）；bbcheck `tools/cpdamage.lua: BALANCED`；5.0 合规扫描（`git diff --check` 干净；新增/提交行 `#|goto|::` 计数 0；字面 `\x` 转义 0 命中；LF 行尾）。

**额外实证（加强版格式验证）:** 用生成的 63 条夹具复现 ctrlStats 五个基准数（S=684.95、C2=563.86、C3=647.11、b=6.8545、a=1039.16、E2=15.24、E3=17.49），实跑输出的 OOC-Bite 段七行与计划锁定示例**逐字节一致**（含 `-> R* = 25.45/s`、`20.36/s`、`22.50/s` 与 `verdict at ERPS 10/s: bleed2 -> D, bleed3 -> D`）。

## Deviations from Plan

- **Task 2 步骤 5 的 pre 版本来源修正（执行时刻校正，计划意图不变）:** 计划原文 `git show HEAD:tools/cpdamage.lua` 在 Task 1 已 commit 之后取到的将是**新版**（自比对将恒为零 diff）。故在开始任何编辑前（HEAD=2ee0188）先行快照 `git show HEAD:tools/cpdamage.lua > /tmp/luabuild/cpdamage-pre-cc.lua`，additive 口径门即用该快照执行 —— 计划要求"pre 改动前版本"的意图逐字达成。
- 其余与计划完全一致：无 Rule 1-4 类偏差；唯一实现过程中的工作树瞬时笔误（误删一行注释）在 commit 前就地复原，提交 diff 0 删行，不作偏差记录。

## Unrun Verification

无——计划内全部验证（三解释器 selftest、真实 SV 端到端 + Node 严格解析、默认 --erps 观测、非法 --erps 拒绝 ×3、屏显 additive 口径门、bbcheck、Lua 5.0 合规复查）均已实跑完成；全程未运行 build.sh，`SM_Extend.lua` 零读写，宏本体/Druid.lua 零触碰。

## Threat Flags

无——离线分析工具改动，不引入网络端点、鉴权路径或信任边界 schema 变化；`--json-out` 写出路径延续既有行为；`oocBite` 块全部字符串键、verdict 为 'A'/'B'/'D' 字符串，经自家 encodeKey/encodeValue + 严格 decoder 往返断言（selftest 第 13 项）。

## Known Stubs

无——rAB/rDB 缺失与 regression 不可用均走锁定降级文本（`no bleed2 shred samples` / `no bleed<N> claw samples` / `bite regression unavailable (<note>) - thresholds skipped`），非 stub。

## Self-Check: PASSED

- File created/modified exists: `/home/admin/workspace/macro-torch/tools/cpdamage.lua`（工作树 clean，已 commit）
- Commits exist: `a8e7852`（`git log -1` 已核验，单文件 +212/−0）
- 三解释器 `ALL 49 PASSED` ×3、Node `STRICT-JSON-OK`、屏显 additive diff 零删除 —— 全部实跑通过
- 锁定契约复核：
  - 11 字段 schema（validateEntry）、decodeJson、encodeScalar/encodeKey、buildClawShredBuckets/buildOocTiers/computeBiteRegression/buildPerBatchStats 零改动（commit diff 为 0 删行实证）；
  - printClawShredTable/printOocTable/printBiteLine/decisionLines 及全部既有 `io.write` 逐字不动（additive diff 门实证）；
  - 屏显既有段落字节不变（pre/post diff 仅新增段）。