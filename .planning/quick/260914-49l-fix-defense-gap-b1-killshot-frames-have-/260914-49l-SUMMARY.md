---
phase: quick-260914-49l
plan: 260914-49l
quick_id: 260914-49l
slug: fix-defense-gap-b1-killshot-frames-have-
subsystem: catAtk 泄能/斩杀判定（classes/druid/cat.lua + core/selftest.lua）
tags: [catatk, discharge, kill-shot, rule-9, selftest, defense-gap]
status: complete
date: 2026-09-14
one_liner: "energyDischargeBeforeBite 新增斩杀帧显式泄能豁免（规则 9 不再依赖 GCD 占位隐式实现）+ Category P 第 10 条 pin 双文件落地"
key_files:
  created: []
  modified: [classes/druid/cat.lua, core/selftest.lua]
decisions:
  - "斩杀豁免插在 isDischarged 重入检查之后、无限能量豁免之前：重入契约（先 true）不推翻，斩杀豁免优先级最高"
  - "新测试保留真实 energyDischargeBeforeBite（vp9 教训），8 个外围 stub 按 CR-01 于 pcall 内执行、assert 前还原"
commits:
  code: d7a24ed
duration_seconds: 328
actuals:
  tokens: 750
  tasks: 1
  commits: 1
---

# Quick 260914-49l Summary

修复防御缺口 B1：斩杀帧显式泄能豁免 + Category P 第 10 条回归 pin，将所有锁定约束下的验证电池跑绿后原子提交 `d7a24ed`。

## 任务完成（Task 1：唯一任务）

| 变更 | 文件 | 内容 |
|------|------|------|
| 斩杀豁免 | classes/druid/cat.lua | `energyDischargeBeforeBite` 内（原 173 行无限能量豁免之前）5 行纯插入：1 注释 + `if macroTorch.isKillShotOrLastChance(clickContext) then / return false / end` + 空行。零删除，其余字节不动 |
| 第 10 条 pin | core/selftest.lua | 1t0 测试结尾之后纯插入 "P: cp5Bite skips discharge and bites on kill-shot frames" 测试块（真实 energyDischargeBeforeBite 保留，8 个外围 stub：isRipPresent/ripLeft/isRakePresent/rakeLeft 返回 true/9/true/9 钉死双持续时间门，isKillShotOrLastChance 返回 true 命中新豁免，regularAttack/safeBite/readyBite 记录 dischargeAttempted/biteCalled；pcall 内执行、8 个 orig 于所有 assert 之前还原）；计数注释 9→10 单行替换并补 `1 in quick 260914-49l` |

提交：`d7a24ed` — `fix(cat): exempt kill-shot frames from discharge in energyDischargeBeforeBite; add Category P kill-shot pin (defense gap B1)`。只含这两个代码文件（63 insertions / 1 deletion，删除恰为旧计数注释行）；`.planning/` 工件（PLAN.md / 本 SUMMARY / STATE.md）由 orchestrator docs commit 收口，未夹带。

## 验证电池结果

- **GATE 0** 三解释器（5.0.3 / 5.1.5 / 5.4.7）对两文件 loadfile 编译：LOADFILE-OK ×3（Lua 5.0 WoW 1.12 合规）。
- **GATE 1** 12 项结构 grep 全字面计数全部达标（豁免注释 1、killshot if 总数 2、注册名/capture/stub/restore 各 1、ripLeft=9 计 1、rakeLeft=9 计 2、discharge 记录器 1、斩杀断言 1、`adds 10 tests` 1、旧 `adds 9 tests` 0）。
- **GATE 2/3** 删除面与 hunk 范围：真实意图逐项实证（详见偏差 1/2）。
- **GATE 4** bbcheck 全文件门 BALANCED（cat.lua）+ diff 新增行平衡 OK。
- **GATE 5** 新增行无 `#`/`goto`/`::`、无 CJK、两文件 CR 字节 0、`git diff --check` 干净。
- **GATE 6** 变更面 = 恰两个锁定文件（+ orchestrator 的未跟踪 planning 目录，见偏差 3）。

## Deviations from Plan

**代码/文本零偏差**：cat.lua 与 selftest.lua 的两次 Edit 均为计划锁定 before/after 块逐字落地，所有锚点实测吻合，无任何自行发挥。以下 3 处偏差均为验证电池脚本本身的写法缺陷（Rule 3 自动修复——电池意图在真实 diff 上逐项实证成立后以最小修正继续）：

1. **[Rule 3 - GATE 2 过滤表达式]** 计划电池 `grep -v '^---'` 意图排除 diff 文件头，但被删的旧计数注释行内容以 `--` 开头，加 diff 前缀后整行呈现为 `--- Registration count...`，被同一过滤器误删，导致"总删除恰 1" 恒达不到。修正为只排除 `--- a/<file>` 文件头（`grep -vE '^--- a/'`）。意图实证：两文件删除行合计 1、cat.lua 侧 0、该行含 `adds 9 tests`。
2. **[Rule 3 - GATE 3 hunk 正则]** selftest 侧正则硬要求旧侧行号带计数（`-96x,`），但旧的 1 行范围被 git 渲染为单行 hunk `@@ -962 +962,58 @@`（无 `,1`），计划写法必然不匹配。修正为计数可选（`(,| )`）。意图实证：hunk 起行 962 落在锁定范围 957-1065（cat.lua 侧 `@@ -172,0` 原式通过，落在 160-199）。
3. **[Rule 3 - GATE 6 变更面]** `git status --porcelain` 中出现 `?? .planning/quick/260914-49l.../`（planning 目录本 quick 工作目录，执行起点已存在，orchestrator 所有、不得提交）。修正为显式豁免该行后变更面归零。意图实证：除了两个锁定代码文件外无任何其他中间产物。

## Unrun Verification（经 `/mt` 实机电池）

本机无 WoW 客户端（unrun-verify 备案）：

- 用户 Windows+Cygwin 重建 `/mt` 后 Category P 共 10 条应全绿，其中新增 "P: cp5Bite skips discharge and bites on kill-shot frames" 无疑点；若该条红名则说明斩杀豁免失效或 stub 断链（防御缺口 B1 回归）。
- 修复前该测试在实机必然红名（真实函数越过斩杀判定到达 regularAttack 门，对 nil 的 `BITE_E + SHRED_E` 算术报错）；修复后应绿——用户可先验红后验绿。
- quickKeepRip cp>=3 支（cat.lua:351）经同一函数享受同豁免，无独立测试（代码路径推理覆盖，如需独立 pin 另开 quick）。

## Threat Flags

无超出计划 `<threat_model>` 的新安全/信任面。计划 4 项 STRIDE 行按既定 disposition 落地：T-01（stub 污染）CR-01 缓解已实现（pcall + 8 orig 还原先于 assert，GATE 1 计数钉死）；T-02/T-03/T-04 accept 无需改动。

## Self-Check: PASSED

- `classes/druid/cat.lua` 含豁免注释计数 1、`if macroTorch.isKillShotOrLastChance(clickContext) then` 总数 2（GATE 1 实测）。
- `core/selftest.lua` 含新注册名计数 1、`adds 10 tests` 计数 1（GATE 1 实测）。
- 提交 `d7a24ed` 存在：2 files changed, 63 insertions(+), 1 deletion(-)，无 tracked 文件删除，无其余 untracked 产物（仅 planning 目录）。
- 三解释器 LOADFILE-OK ×3（GATE 0 实测）。