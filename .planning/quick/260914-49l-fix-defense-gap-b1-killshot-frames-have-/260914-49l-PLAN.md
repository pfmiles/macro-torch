---
quick_id: 260914-49l
slug: fix-defense-gap-b1-killshot-frames-have-
description: "修复防御缺口 B1：斩杀帧没有显式泄能豁免（规则 9 此前仅靠 tryBiteKillShot 先开火占住 GCD 使泄能施法'碰巧失败'隐式实现；若 raw bite 因距离/GCD 边缘失手，泄能施法会真的打出去违反规则 9）。在 energyDischargeBeforeBite 的 isDischarged 重入检查之后、无限能量豁免之前插入 isKillShotOrLastChance 豁免（return false，本帧不泄能，cp5Bite/quickKeepRip cp>=3 双调用方受益）；core/selftest.lua 新增第 10 条 Category P pin（保留真实 energyDischargeBeforeBite，只 stub 外围），计数注释 9→10。恰两个文件；cat.lua 侧纯插入；唯一删除为旧计数注释行；禁 build.sh、零触碰 SM_Extend.lua"
date: 2026-09-14
phase: quick-260914-49l
plan: 260914-49l
type: execute
wave: 1
depends_on: []
files_modified: [classes/druid/cat.lua, core/selftest.lua]
autonomous: true
requirements: ["B1@quick-260914-49l"]
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "energyDischargeBeforeBite 在 isDischarged 重入检查之后、isPseudoInfiniteEnergy 豁免之前，对 isKillShotOrLastChance==true 的帧直接 return false：本帧零泄能施法，调用方落穿到咬击判定（规则 9 显式化，不再依赖 GCD 占用的隐式实现）"
    - "新测试证明 kill-shot 帧上 cp5Bite 咬击照常（biteCalled==true）且泄能施法零调用（dischargeAttempted==false），测试保留真实 energyDischargeBeforeBite（vp9 教训：整函数 stub 会绕过新豁免行）"
    - "新测试遵守 CR-01：pcall 内执行、8 个 stub 在所有 assert 之前按 orig 引用还原，pcall 失败也不会污染登录会话"
    - "classes/druid/cat.lua 与 core/selftest.lua 经 Lua 5.0.3 / 5.1.5 / 5.4.7 三解释器 loadfile 编译全部通过"
  artifacts:
    - "/home/admin/workspace/macro-torch/classes/druid/cat.lua（energyDischargeBeforeBite 内 5 行纯插入：1 注释 + if/return/end + 空行；零删除，其余字节不动）"
    - "/home/admin/workspace/macro-torch/core/selftest.lua（新 Category P 第 10 条测试块纯插入 + 计数注释 9→10 单行替换；其余字节不动）"
  key_links:
    - "cat.lua energyDischargeBeforeBite 中 isKillShotOrLastChance 豁免与 isDischarged 重入检查的先后序是新豁免的关键连线：重入检查仍优先返回 true（不推翻既往契约），豁免仅拦截真实首入的斩杀帧"
    - "新测试的 isKillShotOrLastChance stub 是新增依赖 key link：不 stub 它则登录环境真实判定走风险路径，stub 值 true 需在 restore 前保持生效"
---

# PLAN: 修复防御缺口 B1 — 斩杀帧显式泄能豁免 + Category P 第 10 条 pin（260914-49l）

## 目标

1. **缺陷修复**：在 `classes/druid/cat.lua` 的 `energyDischargeBeforeBite`（当前 163-198 行）中，isDischarged 重入检查（169-171）之后、无限能量豁免（173-176）之前，插入斩杀帧豁免；`isKillShotOrLastChance` 为 true 时本函数直接 `return false`——本帧不发生泄能施法，cp5Bite/quickKeepRip 落穿到咬击判定，此即规则 9 语义。此前该豁免由 tryBiteKillShot 先开火占住 GCD 隐式实现，raw bite 失手（距离/GCD 边缘）时泄能施法会真的打出。
2. **回归 pin**：`core/selftest.lua` 在 960 行（260914-1t0 rake 续期 pin 结尾）之后、962 行计数注释之前，插入第 10 条 Category P 测试 "P: cp5Bite skips discharge and bites on kill-shot frames"，刻意保留真实 `energyDischargeBeforeBite`。
3. **计数注释**：Category P 计数由 "adds 9 tests" 更新为 "adds 10 tests"（补 `1 in quick 260914-49l`）。

## 背景事实（2026-09-14 实地核对，编辑锚点以此为准，勿另找）

- 工作树 clean（本 quick 起点无未提交改动）。
- `classes/druid/cat.lua` 锚点：163 = `function macroTorch.energyDischargeBeforeBite(clickContext)`；169-171 = isDischarged 重入块；172 = 空行；173-176 = 无限能量豁免块。两个 gating 字面量实测唯一：`    if clickContext.isDischarged then` 计数 1，`Skip discharge when energy regeneration exceeds Shred cost (infinite energy scenario)` 计数 1（129 行 cp5Bite 内是 "Skip discharge if ..." 不同措辞）。
- `core/selftest.lua` 锚点：960 = 260914-1t0 测试结尾 `end, true)`；962 = 计数注释（当前 "adds 9 tests"，含 `2 in 26-01, 4 in 26-02, 1 in quick 260825-t86, 1 in quick 260825-vp9, 1 in quick 260914-1t0`）。grep 实测：`'Category P adds 9 tests'` 计数 1、`'adds 8 tests'` 计数 0（已被 1t0 更新）；`assert(ok, 'cp5Bite discharged or did not bite when rake <=1.3s')` 断言文案计数 1（唯一锚定 1t0 结尾）；新测试名与 `isKillShotOrLastChance = function` / `origKillShot` 等新内容字面量当前计数均为 0（干净插入空间）。
- cat.lua 现状：`if macroTorch.isKillShotOrLastChance(clickContext) then` 精确字面量计数 1（仅在 221 行 tryBiteKillShot），插入豁免后应为 2——GATE 用该绝对计数钉死插入唯一性。
- 工具在位（规划时实测）：Lua 三解释器 `/tmp/luabuild/lua-5.0.3/bin/lua`、`/tmp/luabuild/lua-5.1.5/src/lua`、`/tmp/luabuild/lua-5.4.7/src/lua` 对当前两文件 loadfile 编译均已跑通（LOADFILE-OK×3）；Node `/usr/local/bin/node`；`bbcheck.js`（`.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js`）基线：cat.lua = BALANCED（可用全文件门），selftest.lua = MISMATCH（既有局限，260831-24c SUMMARY 与 260914-1t0 PLAN 已备案，**不得**以 selftest 全文件 bbcheck 作门，改用 diff 新增行平衡脚本）。
- 行尾基线：两文件 CR 字节计数均 0（全 LF，插入行必须保持 LF）。

## 锁定约束（违反即失败）

1. **只改两个文件**：`classes/druid/cat.lua` + `core/selftest.lua`。`SM_Extend.lua`、`classes/druid/Druid.lua` 及仓库其余文件零触碰；**禁止运行任何构建脚本**（build.sh 等）。Commit 只含这两个代码文件。
2. **删除面**：两文件 git diff 删除行合计恰 1 行，且就是旧计数注释行（含 `Category P adds 9 tests`）。cat.lua 侧零删除；新测试块全部纯插入。
3. **插入逐字锁定**（Task 1 的两次 Edit 的 before/after 块逐字执行，缩进 4 空格；任何锚点不匹配说明文件漂移，停下报告，不得自行发挥）。
4. **Lua 5.0 (WoW 1.12) 合规**：新增行无 `#`、无 `goto`、无 `::`；零中文字符；新增字符串字面量内不得含相邻连字符对（`--`）与裸双引号字节（diff 平衡门 strip 顺序所致，260913-46s 先例门规）；注释与断言文案一律英文；行尾 LF。
5. **CR-01 stub 纪律**：新测试在 pcall 内执行、**8 个 stub 均于所有 assert 之前按 orig 引用还原**；真实 `energyDischargeBeforeBite` 不 stub（vp9 教训）。
6. **计数真相**：Category P 总数 10 = 当前 9（2 in 26-01 + 4 in 26-02 + t86 + vp9 + 1t0）+ 本 quick 第 10 条；注释必须列出全部 6 项。

## 任务

### Task 1（唯一任务，原子）: cat.lua 斩杀豁免插入 + selftest 第 10 条 pin + 计数注释 9→10 + 双文件 commit

**文件**: `classes/druid/cat.lua` + `core/selftest.lua`（仅此两个）

**执行步骤（2 次精确 Edit + 验证电池，顺序固定）：**

**(a) cat.lua 斩杀豁免插入**（锚点 169-173 行上下文，一次 Edit 完成）：

    --- before ---
    if clickContext.isDischarged then
        return true
    end

    -- Skip discharge when energy regeneration exceeds Shred cost (infinite energy scenario)
    --- after ---
    if clickContext.isDischarged then
        return true
    end

    -- Kill-shot frames never discharge: rule 9 pauses all discharge logic
    if macroTorch.isKillShotOrLastChance(clickContext) then
        return false
    end

    -- Skip discharge when energy regeneration exceeds Shred cost (infinite energy scenario)

语义说明（写进行动理解，不加码）：`return false` 表示本帧未发起泄能，cp5Bite（150 行）与 quickKeepRip cp>=3 支（351 行）对 false 的既有落穿契约自动咬击；重入检查保持在前，首次返回仍为 true 的既往契约不被推翻。豁免位置在无限能量豁免之前，使斩杀豁免优先级最高。

**(b) selftest.lua 新测试块插入 + 计数注释单行替换**（锚点 958-962 行，一次 Edit 完成）：

    --- before ---
    assert(pcallRes, "cp5Bite rake-renewal test pcall failed")
    assert(ok, 'cp5Bite discharged or did not bite when rake <=1.3s')
end, true)

-- Registration count: Category P adds 9 tests (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86, 1 in quick 260825-vp9, 1 in quick 260914-1t0)
    --- after ---
    assert(pcallRes, "cp5Bite rake-renewal test pcall failed")
    assert(ok, 'cp5Bite discharged or did not bite when rake <=1.3s')
end, true)

-- Quick 260914-49l: pins the rule-9 kill-shot discharge exemption added to
-- energyDischargeBeforeBite. On kill-shot frames cp5Bite must skip the
-- discharge attempt entirely and go straight to the bite verdict. The REAL
-- energyDischargeBeforeBite is kept (260825-vp9 lesson: stubbing the whole
-- function would bypass the new exemption line); its periphery is stubbed so
-- no real spell fires: Rip and Rake are pinned fresh (left 9 > 2.3 / 1.3
-- gates) so no duration gate can fire, and isKillShotOrLastChance returns
-- true to hit the new exemption which returns before any mana arithmetic.
-- regularAttack records any discharge attempt; safeBite/readyBite record the
-- bite. CR-01: run inside pcall, restore BEFORE asserts. Registered as the
-- 10th Category P test.
macroTorch.SelfTest:register("P: cp5Bite skips discharge and bites on kill-shot frames", function()
    if UnitClass('player') ~= 'Druid' then return end
    local origIsRipPresent = macroTorch.isRipPresent
    local origRipLeft = macroTorch.ripLeft
    local origIsRakePresent = macroTorch.isRakePresent
    local origRakeLeft = macroTorch.rakeLeft
    local origKillShot = macroTorch.isKillShotOrLastChance
    local origRegularAttack = macroTorch.regularAttack
    local origSafeBite = macroTorch.safeBite
    local origReadyBite = macroTorch.readyBite
    macroTorch.isRipPresent = function(clickContext) return true end
    macroTorch.ripLeft = function(clickContext) return 9 end
    macroTorch.isRakePresent = function(clickContext) return true end
    macroTorch.rakeLeft = function(clickContext) return 9 end
    macroTorch.isKillShotOrLastChance = function(clickContext) return true end
    local biteCalled = false
    local dischargeAttempted = false
    macroTorch.regularAttack = function(clickContext) dischargeAttempted = true end
    macroTorch.safeBite = function(clickContext) biteCalled = true end
    macroTorch.readyBite = function(clickContext) biteCalled = true end
    local ok, pcallRes = true, true
    pcallRes = pcall(function()
        local ctx = {
            comboPoints = 5,
            isImmuneRip = false,
            ooc = false,
            isBehind = false,
            isPseudoInfiniteEnergy = false
        }
        biteCalled = false
        dischargeAttempted = false
        macroTorch.cp5Bite(ctx)
        ok = (biteCalled == true) and (dischargeAttempted == false)
    end)
    macroTorch.isRipPresent = origIsRipPresent
    macroTorch.ripLeft = origRipLeft
    macroTorch.isRakePresent = origIsRakePresent
    macroTorch.rakeLeft = origRakeLeft
    macroTorch.isKillShotOrLastChance = origKillShot
    macroTorch.regularAttack = origRegularAttack
    macroTorch.safeBite = origSafeBite
    macroTorch.readyBite = origReadyBite
    assert(pcallRes, "cp5Bite kill-shot test pcall failed")
    assert(ok, 'cp5Bite discharged or did not bite on kill-shot frames')
end, true)

-- Registration count: Category P adds 10 tests (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86, 1 in quick 260825-vp9, 1 in quick 260914-1t0, 1 in quick 260914-49l)

**执行路径 Trace（锁定推理）：**

修复后为何绿：① 入口门槛经 isRipPresent stub（true）打开，`or` 短路使真实 isFastBattleNotPvp 不被求值；② shouldDischarge 保持 true（isPseudoInfiniteEnergy=false；rip 门 `9 <= 2.3` false；rake 门 `9 <= 1.3` false）；③ 真实 energyDischargeBeforeBite：isDischarged（ctx 无此字段 → nil）跳过 → **新豁免 isKillShotOrLastChance stub true → return false**，在任何 mana/BITE_E 算术之前返回，无任何真实施法；④ cp5Bite 落穿：ooc=false → safeBite stub → biteCalled=true，dischargeAttempted 保持 false → ok=true。ctx 不含 BITE_E/CLAW_E/mana 字段无妨——新豁免先于所有算术消费点。

修复前为何红（防御缺口实证，纯 Trace；本机无 WoW 环境无法本地执行）：真实函数越过斩杀判定后到达 regularAttack 门，第二/三门对 nil 的 `clickContext.BITE_E + clickContext.SHRED_E` 做算术 → 运行期 pcall 报错 → assert(pcallRes) 失败 → 测试红名。这正是 B1 缺口的性质：斩杀帧没有短路，泄能路径被真实求值。本地 RED 验证不适用（selftest 依赖 WoW API），红/绿终验由用户 /mt 电池完成（unrun-verify）。

**验证 gates（顺序执行，任一失败即停并报告）：**

```bash
cd /home/admin/workspace/macro-torch

# GATE 0: 三解释器 loadfile 编译两文件（不执行，无 WoW 环境依赖）
for B in /tmp/luabuild/lua-5.0.3/bin/lua /tmp/luabuild/lua-5.1.5/src/lua /tmp/luabuild/lua-5.4.7/src/lua; do
  "$B" -e 'assert(loadfile("/home/admin/workspace/macro-torch/classes/druid/cat.lua")); assert(loadfile("/home/admin/workspace/macro-torch/core/selftest.lua"))' || { echo "LOADFILE FAIL: $B"; exit 1; }
  echo "LOADFILE-OK: $B"
done

# GATE 1: 结构 grep 计数（-F 全字面；绝对计数以规划时基线为准）
test "$(grep -cF 'Kill-shot frames never discharge: rule 9 pauses all discharge logic' classes/druid/cat.lua)" = "1" || { echo "FAIL: exemption comment"; exit 1; }
test "$(grep -cF 'if macroTorch.isKillShotOrLastChance(clickContext) then' classes/druid/cat.lua)" = "2" || { echo "FAIL: killshot if count (baseline 1 + new 1)"; exit 1; }
test "$(grep -cF 'P: cp5Bite skips discharge and bites on kill-shot frames' core/selftest.lua)" = "1" || { echo "FAIL: register name"; exit 1; }
test "$(grep -cF 'local origKillShot = macroTorch.isKillShotOrLastChance' core/selftest.lua)" = "1" || { echo "FAIL: killShot capture"; exit 1; }
test "$(grep -cF 'macroTorch.isKillShotOrLastChance = function(clickContext) return true end' core/selftest.lua)" = "1" || { echo "FAIL: killShot stub"; exit 1; }
test "$(grep -cF 'macroTorch.isKillShotOrLastChance = origKillShot' core/selftest.lua)" = "1" || { echo "FAIL: killShot restore"; exit 1; }
test "$(grep -cF 'macroTorch.ripLeft = function(clickContext) return 9 end' core/selftest.lua)" = "1" || { echo "FAIL: ripLeft=9 stub"; exit 1; }
test "$(grep -cF 'macroTorch.rakeLeft = function(clickContext) return 9 end' core/selftest.lua)" = "2" || { echo "FAIL: rakeLeft=9 count (1t0 pin 1 + new 1)"; exit 1; }
test "$(grep -cF 'macroTorch.regularAttack = function(clickContext) dischargeAttempted = true end' core/selftest.lua)" = "1" || { echo "FAIL: discharge recorder"; exit 1; }
test "$(grep -cF 'assert(ok, '\''cp5Bite discharged or did not bite on kill-shot frames'\'')' core/selftest.lua)" = "1" || { echo "FAIL: kill-shot assert"; exit 1; }
test "$(grep -cF 'Category P adds 10 tests' core/selftest.lua)" = "1" || { echo "FAIL: count comment 10"; exit 1; }
test "$(grep -cF 'Category P adds 9 tests' core/selftest.lua)" = "0" || { echo "FAIL: old count still present"; exit 1; }
echo "GATE 1 OK"

# GATE 2: 删除面——两文件合计恰 1 行，且为旧计数注释
test "$(git diff -U0 HEAD -- classes/druid/cat.lua | grep '^-' | grep -v '^---' | wc -l)" = "0" || { echo "FAIL: cat.lua must be pure insertion"; exit 1; }
test "$(git diff -U0 HEAD -- classes/druid/cat.lua core/selftest.lua | grep '^-' | grep -v '^---' | wc -l)" = "1" || { echo "FAIL: total deletion count"; exit 1; }
test "$(git diff -U0 HEAD -- classes/druid/cat.lua core/selftest.lua | grep '^-' | grep -v '^---' | grep -cF 'adds 9 tests')" = "1" || { echo "FAIL: deleted line not old count comment"; exit 1; }

# GATE 3: hunk 范围——cat.lua 全部 @@ 起行落在 160-199（energyDischargeBeforeBite 内）；selftest.lua 落在 957-1065（1t0 结尾到 Category R 注释之前）
test -z "$(git diff -U0 HEAD -- classes/druid/cat.lua | grep '^@@' | grep -vE '^@@ -(1[6-9][0-9]|20[0-9]),')" || { echo "FAIL: cat.lua hunk out of range"; exit 1; }
test -z "$(git diff -U0 HEAD -- core/selftest.lua | grep '^@@' | grep -vE '^@@ -(9[5-9][0-9]|10[0-5][0-9]),')" || { echo "FAIL: selftest hunk out of range"; exit 1; }
echo "GATE 2-3 OK"

# GATE 4: 括号平衡——(a) bbcheck 全文件门仅限 cat.lua（基线 BALANCED，须保持）；(b) selftest.lua 用 diff 新增行平衡脚本（全文件 bbcheck 基线 MISMATCH 属既有局限，勿用）
node .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js classes/druid/cat.lua | grep -F 'classes/druid/cat.lua: BALANCED' || { echo "FAIL: cat.lua bbcheck"; exit 1; }
git diff -U0 HEAD -- classes/druid/cat.lua core/selftest.lua | node -e 'let out="";process.stdin.on("data",d=>out+=d);process.stdin.on("end",()=>{let s=out.split("\n").filter(l=>l[0]==="+"&&l[1]!=="+").map(l=>l.slice(1)).join("\n");s=s.replace(/--\[\[[\s\S]*?\]\]/g," ").replace(/--[^\n]*/g," ").replace(/\"(?:\\.|[^\"\\])*\"/g," \"s\" ").replace(/\x27(?:\\.|[^\x27\\])*\x27/g," \"s\" ");const st=[];let ok=true;for(const c of s){if(c==="["||c==="("||c==="{"){st.push(c)}else if(c==="]"||c===")"||c==="}"){const p=st.pop();if((c==="]"&&p!=="[")||(c===")"&&p!=="(")||(c==="}"&&p!=="{")){ok=false;break}}}if(!ok||st.length!==0){console.log("T4 DIFF BALANCE: MISMATCH");process.exit(1)}console.log("T4 DIFF BALANCE: OK")});' || { echo "FAIL: diff balance"; exit 1; }
echo "GATE 4 OK"

# GATE 5: 新增行 token / CJK / 行尾门 + git diff --check
test "$(git diff -U0 HEAD -- classes/druid/cat.lua core/selftest.lua | grep '^+' | grep -v '^+++' | grep -cE '#|goto |::' || true)" = "0" || { echo "FAIL: 5.0 token"; exit 1; }
test "$(git diff -U0 HEAD -- classes/druid/cat.lua core/selftest.lua | grep '^+' | grep -v '^+++' | grep -c '[一-龥]' || true)" = "0" || { echo "FAIL: CJK"; exit 1; }
test "$(grep -l $'\r' classes/druid/cat.lua core/selftest.lua | wc -l)" = "0" || { echo "FAIL: CR bytes"; exit 1; }
git diff --check || { echo "FAIL: diff check"; exit 1; }

# GATE 6: 变更面唯一
test "$(git status --porcelain | grep -v '^ M classes/druid/cat.lua$' | grep -v '^ M core/selftest.lua$' | wc -l)" = "0" || { echo "FAIL: unexpected files changed"; git status --porcelain; exit 1; }
echo "ALL GATES OK"
```

**完成标准**：
- cat.lua 仅 energyDischargeBeforeBite 内 5 行纯插入（1 注释 + if/return/end + 空行），零删除；selftest.lua 新测试注册于 1t0 结尾之后、计数注释之前，计数注释含 `adds 10 tests`、`quick 260914-49l` 等全部 6 项。
- 全部 gates 输出 ALL GATES OK；三解释器各打印 LOADFILE-OK；cat.lua bbcheck BALANCED；T4 DIFF BALANCE: OK。
- 原子 commit 只含这两个文件，commit message 逐字如下（英文）：

```
fix(cat): exempt kill-shot frames from discharge in energyDischargeBeforeBite; add Category P kill-shot pin (defense gap B1)
```

```
git add classes/druid/cat.lua core/selftest.lua && git commit -m "fix(cat): exempt kill-shot frames from discharge in energyDischargeBeforeBite; add Category P kill-shot pin (defense gap B1)"
```

**precondition**：`/tmp/luabuild` 三解释器（5.0.3/bin、5.1.5/src、5.4.7/src）与 `/usr/local/bin/node` 在位（规划时 2026-09-14 实测：三解释器对两文件 loadfile 编译通过、bbcheck 基线已测、两文件 CR=0）。若执行时缺失，停下报告 unmet precondition，不得降级跳过三解释器验证。

## 威胁模型（STRIDE 简表）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260914-49l-01 | Tampering | selftest 8 个 stub 泄漏污染实机会话（尤其 isKillShotOrLastChance 未还原会把所有帧判成斩杀帧） | medium | mitigate | CR-01：pcall 内执行 + 还原先于所有 assert，还原值逐一引用 capture 时保存的 orig；GATE 1 钉死 capture/stub/restore 各计数 |
| T-260914-49l-02 | DoS | 新豁免误伤非斩杀帧的泄能路径（isKillShotOrLastChance 实现劣化/误判时泄能全停） | low | accept | 豁免仅在既有 tryBiteKillShot/FF/reshift 同级门的同一判定上短路，与规则 9 全链路判定一致；误判影响与既有 call site 同源，无新增攻击面 |
| T-260914-49l-03 | DoS | 断言在登录自测抛错显示红名 | low | accept | 失败仅表现自测红名提示，不进战斗路径；无额外缓解成本 |
| T-260914-49l-04 | Escalation | 经 UI 注入的测试名/文案被执行 | low | accept | 测试名与断言为静态字面量，register 仅登记函数，无 eval 路径 |

包管理器安装：本 quick 零 npm/pip/cargo 安装，package legitimacy 门与 T-SC 行不适用。

## unrun-verify 备案（写进 SUMMARY）

本机无 WoW 客户端。用户 Windows+Cygwin 重建 `/mt` 后：Category P 共 10 条应全绿，其中新增 "P: cp5Bite skips discharge and bites on kill-shot frames" 无疑点；若该条红名则说明斩杀豁免失效或其 stub 断链（防御缺口 B1 回归）；quickKeepRip cp>=3 支（cat.lua:351）经同一函数享受同豁免，无独立测试（以代码路径推理覆盖，如需独立 pin 另开 quick）。修复前该测试在实机必然红名（regularAttack 门 nil 算术报错），修复后应绿——用户可在实机先验红后验绿。

## 输出

执行完成后输出 `260914-49l-SUMMARY.md`（结构同 260913-46s 先例：任务完成 / 偏差 / Unrun Verification / Threat Flags / Self-Check）；STATE.md Quick Tasks 表行由 quick 流程 docs commit 收口，代码 commit 中不得夹带任何 .planning 文件。