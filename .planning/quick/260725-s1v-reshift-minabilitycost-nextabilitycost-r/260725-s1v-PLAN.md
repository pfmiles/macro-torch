---
phase: s1v-reshift-minabilitycost-nextabilitycost-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/cat.lua
autonomous: true
requirements: []

must_haves:
  truths:
    - "getMinimumAffordableAbilityCost 函数重命名为 getNextAbilityCost，所有调用点一致更新"
    - "shouldDoReshift 中的局部变量 minAbilityCost 重命名为 nextAbilityCost"
    - "readyReshift 的第一个参数 minAbilityCost 重命名为 nextAbilityCost"
    - "readyReshift 日志中 nextMoveCost 字段更名为 nextAbilityCost"
    - "readyReshift 日志中新增 earning 字段，值为 clickContext.RESHIFT_ENERGY - macroTorch.player.mana - clickContext.TIGER_E"
    - "selftest 中对 getMinimumAffordableAbilityCost 的引用全部更新为 getNextAbilityCost"
  artifacts:
    - classes/druid/Druid.lua
    - classes/druid/cat.lua
  key_links:
    - "cat.lua:210 调用 Druid.lua:871 的函数名保持一致"
    - "cat.lua:332 日志参数名与函数参数名保持一致"
---

<objective>
将 `getMinimumAffordableAbilityCost` 重命名为更语义化的 `getNextAbilityCost`，同步所有调用点；
在 `readyReshift` 日志中增加 `earning` 字段，直观展示 reshift 的净能量收益。

Purpose: 提升代码可读性，让日志中的能量收益一目了然，便于战斗分析。
Output: Druid.lua 和 cat.lua 完成重命名及日志增强。
</objective>

<execution_context>
@/Users/yue.weny/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/yue.weny/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@classes/druid/Druid.lua
@classes/druid/cat.lua

# Relevant code locations:
# Druid.lua:871  — getMinimumAffordableAbilityCost function definition
# Druid.lua:1285 — selftest: "getMinimumAffordableAbilityCost always returns a value"
# Druid.lua:1293 — selftest body: macroTorch.getMinimumAffordableAbilityCost(ctx)
# Druid.lua:1294-1296 — selftest assertions referencing getMinimumAffordableAbilityCost by name
# cat.lua:190    — reshiftMod: local ... minAbilityCost = macroTorch.shouldDoReshift(...)
# cat.lua:192    — reshiftMod: macroTorch.readyReshift(clickContext, nextMove, minAbilityCost)
# cat.lua:210    — shouldDoReshift: local minAbilityCost, nextMove = macroTorch.getMinimumAffordableAbilityCost(...)
# cat.lua:214    — shouldDoReshift: return ... < minAbilityCost, nextMove, minAbilityCost
# cat.lua:325    — readyReshift function signature: (clickContext, nextMove, minAbilityCost)
# cat.lua:332    — readyReshift log: ', nextMoveCost: ' .. tostring(minAbilityCost)
</context>

<tasks>

<task type="auto">
  <name>Task: Rename getMinimumAffordableAbilityCost → getNextAbilityCost; rename minAbilityCost → nextAbilityCost; add earning to reshift log</name>
  <files>classes/druid/Druid.lua, classes/druid/cat.lua</files>
  <action>

**1. Druid.lua 函数定义 (line 871):**
- 将 `function macroTorch.getMinimumAffordableAbilityCost(clickContext)` 改为 `function macroTorch.getNextAbilityCost(clickContext)`

**2. Druid.lua selftest (lines 1285-1298):**
- line 1285: 将 selftest 注册名中的 `getMinimumAffordableAbilityCost` 改为 `getNextAbilityCost`
- line 1293: 将 `macroTorch.getMinimumAffordableAbilityCost(ctx)` 改为 `macroTorch.getNextAbilityCost(ctx)`
- lines 1294-1296: 将 assert 错误消息中的 `getMinimumAffordableAbilityCost` 改为 `getNextAbilityCost`

**3. cat.lua reshiftMod (lines 190-192):**
- line 190: 将局部变量 `minAbilityCost` 改为 `nextAbilityCost`
- line 192: 将传入 `readyReshift` 的参数 `minAbilityCost` 改为 `nextAbilityCost`

**4. cat.lua shouldDoReshift (lines 210-214):**
- line 210: 将局部变量 `minAbilityCost` 改为 `nextAbilityCost`，将 `macroTorch.getMinimumAffordableAbilityCost(clickContext)` 改为 `macroTorch.getNextAbilityCost(clickContext)`
- line 214: 将 return 语句中的两处 `minAbilityCost` 改为 `nextAbilityCost`

**5. cat.lua readyReshift (lines 325-337):**
- line 325: 将函数签名参数 `minAbilityCost` 改为 `nextAbilityCost`
- line 332: 将日志中 `', nextMoveCost: ' .. tostring(minAbilityCost)` 改为 `', nextAbilityCost: ' .. tostring(nextAbilityCost)`
- line 332（末尾）: 在 `tigerLeft = ' .. macroTorch.tigerLeft(clickContext)` 之后插入 `, ', earning = ' .. tostring(clickContext.RESHIFT_ENERGY - macroTorch.player.mana - clickContext.TIGER_E)`（需要先 Read 当前行确认精确上下文后再 Edit）

**earning 计算逻辑说明：**
RESHIFT_ENERGY 是 reshift 后获得的能量，当前能量 macroTorch.player.mana 会在变形时丢失，变形后需要重新补 Tiger's Fury buff（消耗 TIGER_E 能量），因此净收益 = RESHIFT_ENERGY - 当前能量 - TIGER_E。

  </action>
  <verify>
    <automated>grep -n 'getMinimumAffordableAbilityCost' classes/druid/Druid.lua classes/druid/cat.lua | grep -v '^#' || echo "OK: no remaining old name references"</automated>
  </verify>
  <verify>
    <automated>grep -n 'minAbilityCost' classes/druid/cat.lua | grep -v '^#' || echo "OK: no remaining minAbilityCost references in cat.lua"</automated>
  </verify>
  <verify>
    <automated>grep -n 'nextMoveCost' classes/druid/cat.lua | grep -v '^#' || echo "OK: nextMoveCost renamed to nextAbilityCost"</automated>
  </verify>
  <verify>
    <automated>grep -n 'earning' classes/druid/cat.lua</automated>
  </verify>
  <done>
All occurrences of getMinimumAffordableAbilityCost renamed to getNextAbilityCost across Druid.lua and cat.lua.
All occurrences of minAbilityCost renamed to nextAbilityCost in cat.lua.
readyReshift log: nextMoveCost field renamed to nextAbilityCost; earning field added with correct formula.
Zero remaining references to old names when grepping both files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + log enhancement, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| (none) | — | — | — | — | No security surface change — name-only refactor + log field addition |
</threat_model>

<verification>
- `grep 'getMinimumAffordableAbilityCost'` across Druid.lua and cat.lua returns no results
- `grep 'minAbilityCost'` across cat.lua returns no results
- `grep 'nextMoveCost'` across cat.lua returns no results
- `grep 'getNextAbilityCost'` across Druid.lua and cat.lua returns expected hits (definition + callers + selftest)
- `grep 'earning.*RESHIFT_ENERGY.*mana.*TIGER_E'` in cat.lua confirms earning field exists
- Run `./build.sh` to verify build succeeds
</verification>

<success_criteria>
- [ ] Druid.lua: 函数定义、selftest 注册名、调用点、assert 消息全部使用 getNextAbilityCost
- [ ] cat.lua: 所有 minAbilityCost 变量/参数重命名为 nextAbilityCost
- [ ] cat.lua: readyReshift 日志 nextMoveCost→nextAbilityCost，新增 earning 字段
- [ ] Zero grep hits for old names (getMinimumAffordableAbilityCost, minAbilityCost, nextMoveCost)
- [ ] Build passes via ./build.sh
</success_criteria>

<output>
Create `.planning/quick/260725-s1v-reshift-minabilitycost-nextabilitycost-r/260725-s1v-SUMMARY.md` when done
</output>