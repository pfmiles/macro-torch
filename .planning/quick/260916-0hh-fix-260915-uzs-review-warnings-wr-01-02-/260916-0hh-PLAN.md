---
quick_id: 260916-0hh
slug: fix-260915-uzs-review-warnings-wr-01-02-
description: "修复 260915-uzs 代码评审 3 条 Warning（每条的 Fix 段为逐字规格）：WR-01 core/events.lua RAW_COMBATLOG tap 的 'nergize' 过滤改为大小写不敏感（string.lower 后再 find，任意大小写 raw 行均捕获，tap 保持只读落穿、执行流不变）；WR-02 energy_probe.lua EV 行把裸事件名字段从 TYPE 与 t= 之间移到 t= 之后为 ev= KV，使 8 种线路型全部满足 EPR|<TYPE>|t=... 固定列契约；WR-03 两处 UnitMana 第一返回值补 nil 守卫 e = e or 0 且 POLL 的 d 补 math.floor 对齐 EV 写法。仅改 core/events.lua 与 classes/druid/energy_probe.lua 两文件，形状为 5 换行（5 删 10 增）；不触碰 WINDOWS.md；禁止 build_order.txt/cat.lua/其余文件任何改动"
date: 2026-09-16
phase: quick-260916-0hh
plan: 260916-0hh
type: execute
wave: 1
depends_on: []
files_modified: [core/events.lua, classes/druid/energy_probe.lua]
autonomous: true
requirements: ["WR-01@quick-260915-uzs", "WR-02@quick-260915-uzs", "WR-03@quick-260915-uzs"]
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "EPR|RAW tap 对大小写两种 energize raw 行（如 SPELL_ENERGIZE / SPELL_PERIODIC_ENERGIZE 与任意小写变体）都产生 EPR|RAW 行；probe OFF 时该 tap 依旧零计算零输出（string.lower 在 flag 门内才求值）"
    - "EPR|EV 行格式为 EPR|EV|t=<ts>|ev=<事件名>|earg=...|e=...|m=...|d=<int>|dt=<int>：t= 紧随 EV| 之后，离线管道切分 fields[2] 恒为 t= 字段；其余 7 种线路型逐字节不变"
    - "UnitMana 第一返回值为 nil 时，recordEvLine 与 probeOnUpdate 不再抛错；POLL 错误帧照常 log 一行合法 EPR|POLL 行且 pollAccum 依旧减 1.0（无逐帧重抛循环）"
    - "d/dt 分桶语义不变：EV 的 d 仍为 math.floor(e - lastEnergy[evName])（该行零改动）；POLL 的 d 改为 math.floor(e - (lastPollEnergy or e))，%d 不依赖整数前提"
    - "bash build.sh 成功后 SM_Extend.lua 仍含全 8 线路型：SES/EV/POLL/RAW/CAST/REL 各 1 + PDT 3（tx=RAW×1 + tx=CHAT×2）；新 EV 模板扣到 ev= 顺序，旧模板零残留"
    - "全部探针输出仍走 macroTorch.log；无新存储桶、无节流、无白名单；macroTorch.energyProbe 默认 false 的 nil-guard 未动"
  artifacts:
    - "/home/admin/workspace/macro-torch/core/events.lua（158-160 行 3 行 tap 块重构为 6 行嵌套块，仅此区域改动）"
    - "/home/admin/workspace/macro-torch/classes/druid/energy_probe.lua（recordEvLine 插入 e-guard + EV 模板换新；probeOnUpdate 插入 e-guard + POLL d 补 floor；其余行零改动）"
    - "/home/admin/workspace/macro-torch/.planning/quick/260916-0hh-fix-260915-uzs-review-warnings-wr-01-02-/260916-0hh-SUMMARY.md"
  key_links:
    - "events.lua RAW tap → macroTorch.log：单通道保持；cpDamage gate 与 tier-1 whitelist return 必须依然可达（tap 不加 return、不消费 arg2）"
    - "energy_probe.lua EPR|EV 模板 → 离线 pipe-split 解析器 fields[2]=='t=...' 列契约（解析器是探针唯一下游）"
    - "recordEvLine / probeOnUpdate → UnitMana 双返回值契约（SuperWoW）：两处第一返回值均守卫"
    - "energy_probe.lua → build_order.txt 拼接位（本次不改 build_order.txt；build 冒烟门证明接线完好）"
---

# PLAN: 修复 260915-uzs 评审 WR-01/02/03（core/events.lua + classes/druid/energy_probe.lua 两文件）

## 目标

1. **WR-01**（`core/events.lua:158`，规格 = 评审 WR-01 Fix 段 + 锁定口径"case-insensitive，双写皆捕"）：把 RAW tap 的单条件 `if macroTorch.energyProbe and arg2 and string.find(arg2, 'nergize') then` 重构为"外层 flag 门 + `local lum = string.lower(arg2 or '')` + 在 lower 结果上 find"的嵌套形状。Lua 5.0 core 库含 `string.lower`/`string.find`，无语法风险；lower 后匹配'nezy 变大写'两种 casing 皆命中。tap 保持纯只读透传（无 return、不消费/改写 arg1/arg2、不改后续 cpDamage gate 与 tier-1 可达性）；`string.lower` 只在 flag 门内求值，OFF 时零计算。
2. **WR-02**（`classes/druid/energy_probe.lua:57`，规格 = 评审 WR-02 Fix 段）：EV 模板改为 `EPR|EV|t=%.3f|ev=%s|earg=%s|e=%s|m=%s|d=%d|dt=%d`，事件名移到 `t=` 之后成为 `ev= KV`；`t=` 紧随 TYPE，8 种线路型头字段序全部对齐。实参顺序对应调整为 `now, evName, tostring(evArg), tostring(e), tostring(m), d, dt`，其余锚位不动。
3. **WR-03**（`classes/druid/energy_probe.lua:48-50` 与 `107-109`，规格 = 评审 WR-03 Fix 段）：两处 UnitMana 调用后、任何算术之前，插入 `e = e or 0`（`m = m or 0` 已有保留）；POLL 的 `local d = e - (lastPollEnergy or e)` 改为 `local d = math.floor(e - (lastPollEnergy or e))` 对齐 EV 已有 floor。EV 的 d/dt 计算行（第 50 行）零改动。

## 背景事实（2026-09-16 规划时实地取证，编辑锚点一律以本节为准）

- 工作树 clean，HEAD = `89eddcd`（评审落地区）。`git status --porcelain` 为空。
- 锚点字节级核对过（本次 Read 原文，LF、无行尾空格、无 tab），行号如下：
  - `core/events.lua`（共 219 行）：158 = tap if；159 = log 行（12 空格缩进）；160 = `        end`（8 空格）；157 = `elseif event == "RAW_COMBATLOG" then`；161 起为 cpDamage gate 注释；168 = cpDamage gate if；176-178 = tier-1 whitelist return。
  - `classes/druid/energy_probe.lua`（共 117 行，文件尾无换行）：48 = `local e, m = UnitMana('player')`；49 = `m = m or 0`；50 = `local d = math.floor(e - (lastEnergy[evName] or e))`；57 = EV 模板行（4 空格缩进）；107 = 第二处 UnitMana；108 = 第二处 `m = m or 0`；109 = `local d = e - (lastPollEnergy or e)`；117 = 最后一行 SetScript（不得触碰，文件尾换行状态保持现状不加）。
- 工具在位（规划时实测）：`/usr/bin/luac`；`/tmp/luabuild/lua-5.0.3/bin/lua`、`/tmp/luabuild/lua-5.1.5/src/lua`、`/tmp/luabuild/lua-5.4.7/src/lua` 三个解释器 loadfile 可用。
- 行型模板实证：`EPR|REL` 在 `classes/druid/cat.lua:413`（本 quick 不碰该文件）；PDT 模板字面量在 events.lua:126/131/180 三处，`build.sh` 重建后 SM_Extend.lua 中该字面量应恰 3 次。
- `SM_Extend.lua` 是 git-ignored 构建产物（`git check-ignore` 实证），恢复性重跑 `bash build.sh` 不产生工作树改动；本机 `$OSTYPE` 非 cygwin，build.sh 结尾不拷贝。
- WINDOWS.md 判定：open 9 条中仅 entry 9（quick-260915-uzs，EPR|PDT 双传输电池）与本批次相邻，其文案不引用 nergize/EV 模板/UnitMana 守卫，与修复后代码零矛盾——**本 quick 不改 WINDOWS.md**。先前 SUMMARY 内的 unrun-verify 电池（含"nergize 行出现 EPR|RAW"条目）在 WR-01 修复后只会更容易通过，亦不改写（见文末备案）。

## 锁定约束（违反即失败）

1. **只改两个文件**：`core/events.lua`、`classes/druid/energy_probe.lua`。`build_order.txt`、`cat.lua`、`SM_Extend.lua`、`.planning/STATE.md`、`.planning/WINDOWS.md` 及仓库其余文件零触碰。
2. **删除面恰 5 行且内容锁定**（= 换行面）：不允许超出下列 5 行之外的任何删除/重写：
   1. events.lua 旧 tap if 行（8 空格起）；
   2. events.lua 旧 EPR|RAW log 行（12 空格起）；
   3. events.lua 旧 `end` 行（8 空格）；
   4. energy_probe.lua 旧 EV 模板行；
   5. energy_probe.lua 旧 POLL d 行。
   纯插入只有两条：两处 `    e = e or 0`。换行后行数锁定：events.lua = 222、energy_probe.lua = 119。
3. **执行流保持**：WR-01 重构不加 return、不移动 log 调用离开 RAW 分支头部、不改变 cpDamage gate（168 行起）与 tier-1 whitelist return（176-178）两条语句的任何字节。评审 focus (a) 的"只读落穿"结论必须继续成立。
4. **Lua 5.0(WoW 1.12) 合规**：新增行无 `#`、无 `goto`、无 `::`；`string.lower`/`string.find` 属 5.0 core；零中文字符进代码；行尾 LF。
5. **评审 Info 条目不顺手修**：IN-01（log 守卫一致性）、IN-02（头注释措辞）、IN-03（管道转义）、IN-04（EOF 换行）全部不动；energy_probe.lua 文件尾无换行现状保持（若编辑器自动补了换行导致 117 行之后出现 diff，必须报告并回退该变更）。
6. **探针输出面不变**：仍全经 `macroTorch.log`，无新存储桶、无节流、无白名单、`macroTorch.energyProbe` nil-guard 默认 false 不动。
7. **原子性**：任务 2 的 commit 只含上述两个代码文件；任何 `.planning` 文件（含 SUMMARY.md）不进入代码 commit（SUMMARY 由 quick 流程 docs 收口）。

## 任务

### Task 1: 三处锁定 Edit（events.lua tap 重构 + energy_probe.lua 两处守卫/floor/EV 模板）

**文件**: `core/events.lua`、`classes/druid/energy_probe.lua`（仅此两个）

**执行步骤（4 次精确 Edit，顺序固定；任何锚点不匹配=文件漂移，停下报告，不得自行发挥）：**

**(a) events.lua RAW tap 重构**（换 158-160 三行为 6 行嵌套块）：

    --- before ---
        if macroTorch.energyProbe and arg2 and string.find(arg2, 'nergize') then
            macroTorch.log(string.format("EPR|RAW|t=%.3f|ch=%s|txt=%s", GetTime(), tostring(arg1), tostring(arg2)))
        end
    --- after ---
        if macroTorch.energyProbe and arg2 then
            local lum = string.lower(arg2 or '')
            if string.find(lum, 'nergize') then
                macroTorch.log(string.format("EPR|RAW|t=%.3f|ch=%s|txt=%s", GetTime(), tostring(arg1), tostring(arg2)))
            end
        end

**(b) energy_probe.lua recordEvLine 处插入 e 守卫**（锚点含 50 行 floor 句以消歧，两处 UnitMana 文本相同）：

    --- before ---
    local e, m = UnitMana('player')
    m = m or 0
    local d = math.floor(e - (lastEnergy[evName] or e))
    --- after ---
    local e, m = UnitMana('player')
    e = e or 0
    m = m or 0
    local d = math.floor(e - (lastEnergy[evName] or e))

**(c) energy_probe.lua EV 模板换新**（整行替换）：

    --- before ---
    macroTorch.log(string.format("EPR|EV|%s|t=%.3f|earg=%s|e=%s|m=%s|d=%d|dt=%d", evName, now, tostring(evArg), tostring(e), tostring(m), d, dt))
    --- after ---
    macroTorch.log(string.format("EPR|EV|t=%.3f|ev=%s|earg=%s|e=%s|m=%s|d=%d|dt=%d", now, evName, tostring(evArg), tostring(e), tostring(m), d, dt))

**(d) energy_probe.lua probeOnUpdate 处插入 e 守卫 + POLL d 补 floor**（锚点含 109 行旧 d 行以消歧）：

    --- before ---
    local e, m = UnitMana('player')
    m = m or 0
    local d = e - (lastPollEnergy or e)
    --- after ---
    local e, m = UnitMana('player')
    e = e or 0
    m = m or 0
    local d = math.floor(e - (lastPollEnergy or e))

**执行路径 Trace（评审 Fix 段直译）**：WR-01 为何零风险——`string.lower` 幂等：大写行 lower 后含 `'nergize'` 命中；小写行 lower 后原样命中；混合大小写亦命中。外层 `macroTorch.energyProbe and arg2` 短路保持 OFF 零求值。WR-03 为何消除放大——守卫后 e 恒非 nil，POLL 减法不再抛错；`pollAccum = pollAccum - 1.0`（112 行）位于守卫之后照常执行，错误帧不积存逐帧重抛；`lastPollEnergy = e` 用守卫后的 0 覆盖旧值，d=0-e 旧值产生一次负 delta 行（合法回退帧，评审指定的 exactly 写法，不另作特殊化）。

**验证 gates（顺序执行，任一失败即停并报告）**：

```bash
cd /home/admin/workspace/macro-torch

# GATE A: 三解释器 loadfile + luac 语法门（每文件）
for B in /tmp/luabuild/lua-5.0.3/bin/lua /tmp/luabuild/lua-5.1.5/src/lua /tmp/luabuild/lua-5.4.7/src/lua; do
  for F in core/events.lua classes/druid/energy_probe.lua; do
    "$B" -e "assert(loadfile('/home/admin/workspace/macro-torch/$F'))" || { echo "LOADFILE FAIL: $B $F"; exit 1; }
  done
  echo "LOADFILE-OK: $B"
done
/usr/bin/luac -p core/events.lua && /usr/bin/luac -p classes/druid/energy_probe.lua || { echo "LUAC FAIL"; exit 1; }

# GATE C: 结构与行数锁定
test "$(awk 'END{print NR}' core/events.lua)" = "222" || { echo "FAIL: events.lua line count"; exit 1; }
test "$(awk 'END{print NR}' classes/druid/energy_probe.lua)" = "119" || { echo "FAIL: energy_probe.lua line count"; exit 1; }
test "$(grep -cF 'local lum = string.lower' core/events.lua)" = "1" || { echo "FAIL: lum"; exit 1; }
test "$(grep -cF "if string.find(lum, 'nergize') then" core/events.lua)" = "1" || { echo "FAIL: lower find"; exit 1; }
test "$(grep -nF 'local lum = string.lower' core/events.lua | cut -d: -f1)" -lt "$(grep -nF 'if macroTorch.cpDamageLog and arg1' core/events.lua | cut -d: -f1)" || { echo "FAIL: tap must precede cpDamage gate"; exit 1; }
test "$(grep -cF 'if macroTorch.cpDamageLog and arg1 == ' core/events.lua)" = "1" || { echo "FAIL: cpDamage gate"; exit 1; }
test "$(grep -cF 'e = e or 0' classes/druid/energy_probe.lua)" = "2" || { echo "FAIL: e guards"; exit 1; }
test "$(grep -cF 'm = m or 0' classes/druid/energy_probe.lua)" = "2" || { echo "FAIL: m guards"; exit 1; }
test "$(grep -cF 'local d = math.floor(e - (lastEnergy[evName] or e))' classes/druid/energy_probe.lua)" = "1" || { echo "FAIL: EV d line must be untouched"; exit 1; }
test "$(grep -cF 'local d = math.floor(e - (lastPollEnergy or e))' classes/druid/energy_probe.lua)" = "1" || { echo "FAIL: POLL floor"; exit 1; }
test "$(grep -cF 'EPR|EV|t=%.3f|ev=%s|earg=%s|e=%s|m=%s|d=%d|dt=%d' classes/druid/energy_probe.lua)" = "1" || { echo "FAIL: new EV template"; exit 1; }
test "$(grep -cF '", now, evName, tostring(evArg), tostring(e), tostring(m), d, dt))"' classes/druid/energy_probe.lua)" = "1" || { echo "FAIL: EV arg order"; exit 1; }

# GATE D: 删除面 / hunk 范围 / 变更面 / diff 卫生
git diff -U0 HEAD -- core/events.lua classes/druid/energy_probe.lua | grep '^-' | grep -v '^---' | sed 's/^-//' > /tmp/0hh_del.txt
test "$(wc -l < /tmp/0hh_del.txt)" = "5" || { echo "FAIL: deletion count $(wc -l < /tmp/0hh_del.txt)"; cat /tmp/0hh_del.txt; exit 1; }
test "$(grep -cF "string.find(arg2, 'nergize')" /tmp/0hh_del.txt)" = "1" || { echo "FAIL: old tap if not deleted"; exit 1; }
test "$(grep -cF 'EPR|RAW' /tmp/0hh_del.txt)" = "1" || { echo "FAIL: old RAW log line not deleted"; exit 1; }
test "$(grep -cx '        end' /tmp/0hh_del.txt)" = "1" || { echo "FAIL: old tap end not deleted"; exit 1; }
test "$(grep -cF 'EPR|EV|%s|t=' /tmp/0hh_del.txt)" = "1" || { echo "FAIL: old EV template not deleted"; exit 1; }
test "$(grep -cF 'e - (lastPollEnergy or e)' /tmp/0hh_del.txt)" = "1" || { echo "FAIL: old POLL d not deleted"; exit 1; }
test -z "$(git diff -U0 HEAD -- core/events.lua | grep '^@@' | grep -vE '^@@ -1[5-9][0-9],')" || { echo "FAIL: events.lua hunk out of 150-199"; exit 1; }
test -z "$(git diff -U0 HEAD -- classes/druid/energy_probe.lua | grep '^@@' | grep -vE '^@@ -([4-9][0-9]|1[01][0-9]),')" || { echo "FAIL: energy_probe.lua hunk out of 40-119"; exit 1; }
test "$(git status --porcelain)" = " M classes/druid/energy_probe.lua
 M core/events.lua" || { echo "FAIL: unexpected working-tree files"; git status --porcelain; exit 1; }
git diff --check || { echo "FAIL: diff check"; exit 1; }
echo "GATE A/C/D OK"
```

**完成标准**：
- 4 次 Edit 全部按锁定 before/after 落位；第 50 行（EV d）、56/59 行区间外、112/113 行（pollAccum 减法与 POLL log）零改动。
- GATE A：三解释器各打印 LOADFILE-OK，luac 两文件通过；GATE C/D 全部 PASS，输出 `GATE A/C/D OK`。
- 五条删除行与写入枚举一一对应，无第 6 行删除；无任何 `return` 出现在 tap 块内。

### Task 2: build 冒烟 + 8 线路型契约 + 原子 commit

**文件**: 同 Task 1（验证阶段重建 `SM_Extend.lua` 属 git-ignored 产物）

**执行步骤**：
1. repo root 跑 `bash build.sh`（exit 0；本机非 cygwin 不拷贝）。
2. 对重建后的 `SM_Extend.lua` 跑 GATE B（见下）。
3. 原子 commit（逐字 message，英文）：

```
git add core/events.lua classes/druid/energy_probe.lua && git commit -m "fix(260916-0hh): case-insensitive RAW energize tap; EV line ev= KV after t=; UnitMana e nil-guard + POLL floor (WR-01/02/03)"
```

4. 写出 `260916-0hh-SUMMARY.md`（本目录，结构同上两先例：任务完成 / 偏差 / Unrun Verification / Threat Flags / Self-Check）；**SUMMARY 与 STATE.md 行不进入上面的代码 commit**，留给 quick 流程 docs commit 收口。

**验证 gates（顺序执行，任一失败即停并报告）**：

```bash
cd /home/admin/workspace/macro-torch

# GATE B: 构建产物 8 线路型契约（fix 前判定基准 = 260915-uzs REVIEW 验证表口径）
bash build.sh || { echo "FAIL: build.sh"; exit 1; }
test "$(grep -cF 'EPR|SES|t=' SM_Extend.lua)" = "1" || { echo "FAIL: SES count"; exit 1; }
test "$(grep -cF 'EPR|POLL|t=' SM_Extend.lua)" = "1" || { echo "FAIL: POLL count"; exit 1; }
test "$(grep -cF 'EPR|RAW|t=' SM_Extend.lua)" = "1" || { echo "FAIL: RAW count"; exit 1; }
test "$(grep -cF 'EPR|CAST|t=' SM_Extend.lua)" = "1" || { echo "FAIL: CAST count"; exit 1; }
test "$(grep -cF 'EPR|REL|t=' SM_Extend.lua)" = "1" || { echo "FAIL: REL count"; exit 1; }
test "$(grep -cF 'EPR|PDT|t=%.3f|tx=%s|ch=%s|txt=%s' SM_Extend.lua)" = "3" || { echo "FAIL: PDT count"; exit 1; }
test "$(grep -cF 'EPR|EV|t=%.3f|ev=%s|earg=%s|e=%s|m=%s|d=%d|dt=%d' SM_Extend.lua)" = "1" || { echo "FAIL: new EV template in build"; exit 1; }
test "$(grep -cF 'EPR|EV|t=%.3f|ev=%s' SM_Extend.lua)" = "1" || { echo "FAIL: EV t=-then-ev= ordering"; exit 1; }
test "$(grep -cF 'EPR|EV|%s|t=' SM_Extend.lua)" = "0" || { echo "FAIL: old EV template leaking into build"; exit 1; }
test "$(grep -cF 'EPR|EV|%s|t=' classes/druid/energy_probe.lua)" = "0" || { echo "FAIL: old EV template in source"; exit 1; }

# GATE E: commit 原子性（恰两文件、无 .planning 夹带）
test "$(git show --name-only --format= HEAD)" = "classes/druid/energy_probe.lua
core/events.lua" || { echo "FAIL: commit file set"; git show --name-only --format= HEAD; exit 1; }
echo "GATE B/E OK"
```

**完成标准**：
- build.sh exit 0；SM_Extend.lua 含 SES/EV/POLL/RAW/CAST/REL 各 1 + PDT 3，新 EV 模板带 ev= 且 t= 在 ev= 之前，旧模板在产物与源码中零残留；输出 `GATE B/E OK`。
- commit 恰含 `core/events.lua`、`classes/druid/energy_probe.lua` 两文件；`git status --porcelain` 在 commit 后为空（SM_Extend.lua ignored 不计）。
- SUMMARY.md 已写到本目录并含 Unrun Verification 段（见下）。

## 威胁模型（STRIDE 简表）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0hh-01 | Tampering | core/events.lua RAW tap（arg2 → string.lower/find → %s log） | low | accept | arg2 只经纯函数 lower/find 匹配并原文 %s 落盘，无 eval/无状态改写；与修复前攻击面相同（评审 focus (a) 已证 tap 只读） |
| T-0hh-02 | DoS | energy_probe.lua recordEvLine/probeOnUpdate（UnitMana 异常返回值） | low | mitigate | WR-03：`e = e or 0` 两处守卫消灭 nil 算术抛错与 POLL 逐帧重抛放大；剩余异常源（macroTorch.log 内部）与评审基线一致 |
| T-0hh-03 | Information Disclosure | 探针 EPR| 行内容（能量值/事件文本） | low | accept | macroTorch.energyProbe nil-guard 默认 false 未动（定律 5）；开关为用户显式 /run 开启 |
| T-0hh-SC | Tampering | npm/pip/cargo installs | — | N/A | 本 quick 零包管理器安装，package legitimacy 门不适用 |

## unrun-verify 备案（写进 SUMMARY，不改 WINDOWS.md）

本机无 WoW 客户端。静态门全绿后，用户 Windows+Cygwin 重建 `/mt` 实机验证点（承接 quick-260915-uzs 既有电池，本次修复使其更容易通过，无条目需改写）：
1. `/run macroTorch.energyProbe = true` 后，出现大写 `SPELL_ENERGIZE`/`SPELL_PERIODIC_ENERGIZE` raw 行时应看到 `EPR|RAW` 行（WR-01 修复点；若仍无则属于未检测到该类 raw 事件，而非大小写失配）。
2. EPR|EV 行目测格式 `EPR|EV|t=<ts>|ev=UNIT_ENERGY|...`（t= 紧随 EV|，事件名在 ev= 后）。
3. POLL 行在能量显示正常期间与既有一致；d 恒为整数。
4. WINDOWS.md ledger 无新增行；后续若 260915 系列电池实机全绿，由用户另行 close。

## 输出

执行完成后输出 `260916-0hh-SUMMARY.md`（结构同 260915-uzs/260914-1t0 先例：任务完成 / 偏差 / Unrun Verification / Threat Flags / Self-Check）；STATE.md Quick Tasks 表行由 quick 流程 docs commit 收口，代码 commit 中不得夹带任何 .planning 文件。