---
phase: quick-260909-4ep-rawdiag2-scout-rip-arm-combat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/events.lua
  - core/spell_trace_core.lua
autonomous: true
requirements:
  - QUICK-260909-4EP
user_setup:
  - service: wow-client
    why: "The arm window change only manifests in game, on the user's Windows+Cygwin machine (this host cannot run WoW 1.12/SuperWoW). Follow-up is user-side only: (1) run build.sh on Windows+Cygwin to regenerate SM_Extend.lua into both AddOns dirs (per established quick-task convention this repo's SM_Extend.lua is NOT rebuilt here); (2) on the next CD forensics session set /run macroTorch.rawdiag2Enabled=true and cast an in-combat Rip — the scout must now stay armed until combat exit, emitting no '[RAWDIAG2] scout disarmed' line at all; segments are marked by one '[RAWDIAG2] scout armed by Rip cast record, until combat exit' line per fight (the rotation casts Rip only at the fight's start, Ferocious Bite refreshes), and long-fight refresh-apply lines plus second-cat ticks keep streaming into MACRO_TORCH_LOG (LOG_MAX_SIZE 2000 raised in game). Evidence gathering is weekly-CD paced (user memory convention)."
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Timed disarm fully removed from core/events.lua: no GetTime-elapsed compare against a fixed second count survives, no `_rawdiag2Active = false` assignment remains anywhere in the scout block, and the disarm log statement is gone with the branch — the capture body now runs directly under the `if scoutActive then` guard with no inner condition."
    - "One window per combat: arming still happens on a recorded in-combat Rip cast (spell_trace_core.lua resets _rawdiag2Lines/_rawdiag2Samples to zero, sets _rawdiag2Active = true, stamps _rawdiag2Start); the scout stays armed until combat exit wipes macroTorch.context (combat_context.lua reset on leaving combat), so each fight's armed log block segments the dump."
    - "Counters and timestamp retained: _rawdiag2Lines still increments per dumped line and feeds the `line=` label, _rawdiag2Samples still gates the first-20 unconditional dump, and _rawdiag2Start is still written at arm — only its reader (the timed compare) disappears."
    - "Honest instrumentation text: the arming log literal in core/spell_trace_core.lua reads 'until combat exit', its arming comment block (126-134 at planning time) no longer claims a restarted sixty-second window, and the events.lua scout header describes the arm-on-Rip-cast, disarm-on-combat-exit contract with no timed wording."
    - "Sterile scope: only the two core Lua files change; SM_Extend.lua is byte-identical and not rebuilt; added lines carry no Lua-5.0-forbidden tokens; LF endings preserved; comments and messages are English only."
  artifacts:
    - core/events.lua
    - core/spell_trace_core.lua
  key_links:
    - "spell_trace_core.lua arming hook (recordCastTable, `if spell == 'Rip' and macroTorch.rawdiag2Enabled ...`) -> events.lua RAW_COMBATLOG scout — the single arm point whose literal and comment now state the until-combat-exit contract."
    - "events.lua scout -> macroTorch.context lifecycle (combat_context.lua reset) — the disarm path is the context wipe, so the scout block itself carries no disarm code anymore."
    - "macroTorch.log -> MACRO_TORCH_LOG rotating buffer (interface_debug.lua:103, capped by macroTorch.LOG_MAX_SIZE, user-raised to 2000 in game) — the volume sink that makes a full-fight armed dump safe now that the timed bound is gone."
---

<!-- planner-discipline-allow: 60 -->
<!-- planner-discipline-allow: 60s -->
<!-- planner-discipline-allow: > 60 -->
<!-- planner-discipline-allow: 60s window -->
<!-- planner-discipline-allow: scout disarmed after -->
<!-- planner-discipline-allow: _rawdiag2Active = false -->
<!-- planner-discipline-allow: GetTime() - (scoutCtx._rawdiag2Start -->
<!-- planner-discipline-allow: else -->
<!-- planner-discipline-allow: 150 -->

<objective>
Replace the RAWDIAG2 forensics scout's fixed sixty-second arm window with
per-combat arming: the scout arms on the fight's in-combat Rip cast and stays
armed until combat exit.

Purpose: the player rotation casts Rip only once per fight (at the start;
relies on Ferocious Bite refreshes afterwards), so the timed disarm branch
expired mid-fight in long combats and silently muted the refresh-apply lines
and the second-cat tick evidence — the exact evidence quick 260907-mhh built
the scout to capture. Deleting the timed-disarm branch and letting the
capture body run directly under the scoutActive guard converts the window to
one arm segment per combat, terminated by macroTorch.context being wiped on
leaving combat. The 150-line cap was already removed by quick 260909-2kd; the
line/sample counters and the _rawdiag2Start stamp stay as arming-reset stats.

Output: core/events.lua (timed-disarm branch deleted, capture body dedented
one level, scout header comment rewritten) and core/spell_trace_core.lua
(arming log literal and 126-134 comment block updated to the until-combat-exit
contract). SM_Extend.lua is NOT modified and NOT rebuilt (established
quick-task convention; the user rebuilds on their game machine). No build
script is run in this plan.
</objective>

<execution_context>@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@core/events.lua
@core/spell_trace_core.lua

Grounding facts, all verified by reading the working tree at planning time
(HEAD 3f23472, tree clean; the previous quick's commits dfd7bd7 and 139250b
are already in history — do not re-derive these anchors blindly, but re-verify
with git status before editing since the plan executes against the live tree).
Repo conventions: English comments, LF endings, Lua 5.0 only (no `#` length
operator, no goto, no labels — every added line must carry no `#`, no
`goto`, no `::`; memory: wow-lua50-syntax), and SM_Extend.lua is a build
artifact never edited (memory: sm-extend-is-build-artifact). No Lua
interpreter exists on this host; node v24 +
.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js
(bracket-balance gate, prints `BALANCED` / exit 0) is the repo's established
syntax proxy. Do NOT run build.sh or any build script.

- core/events.lua, RAW_COMBATLOG scout block (planning-time lines 135-179):
  header comment at 136-145 — line 136 keeps the provenance tag
  `-- [RAWDIAG2 quick 260907-mhh] forensics scout, placed BEFORE the tier-1
  channel`; the timed-disarm sentences span 139-141 and flow into the
  untouched tail at 142-145 (`-- first 20 events of a fresh arm ...` through
  `-- exit wipes it, never persisted); output persists via macroTorch.log.`).
  Guard at 146-148: `local scoutActive = ...` /
  `if scoutActive then` / `local scoutCtx = macroTorch.context` — all KEEP.
  Timed-disarm branch at 149-153 (DELETE all five lines: the
  GetTime-difference compare against the constant sixty, the false
  assignment, the two-line disarm log statement, and the closing `else`).
  Capture body at 154-176 (the `-- serialize every event arg verbatim`
  comment through the `end` closing `if interesting`): KEEP byte-identical
  but dedent every line by exactly 4 spaces. The orphaned `end` at 177
  (previously closing the deleted branch) DELETE. Line 178 (`end` closing
  `if scoutActive`) and the `-- end of RAWDIAG2 scout block` trailer at 179
  KEEP at their current depth.
- core/spell_trace_core.lua: arming hook code at 135-139
  (`_rawdiag2Lines = 0`, `_rawdiag2Samples = 0`, `_rawdiag2Active = true`,
  `_rawdiag2Start = GetTime()`) KEEP byte-identical. Arming log literal on
  line 140 spells '60s window' (EDIT to 'until combat exit'). Arming comment
  block at 126-134: lines 126-128 and 133-134 KEEP; lines 129-132 claim the
  arm "restarts the 60s window" and "each cast anchors a fresh full window"
  (EDIT — the refresh now comes from Ferocious Bite, not a recast, so the
  window is the whole fight). The pair-ledger comment at 362 ("armed only
  inside a live scout window") remains accurate — the armed period now runs
  until combat exit — do NOT touch it.
- No other file references the timed disarm: grep shows zero rawdiag2 hits in
  core/selftest.lua and interface_debug.lua; macro_torch.lua only carries the
  rawdiag2Enabled config banner (no timed wording). The .planning/ hits are
  archival and out of scope. Zero '150' tokens survive in either file from
  the previous quick — the battery gate re-asserts that.
- macroTorch.context is wiped on leaving combat (combat_context.lua resets the
  whole table), which is now the scout's only disarm path; the scout code
  itself carries no disarm assignment anymore.

Token gate note for the executor: the verify battery region-counts the scout
block for the digit pair '6'+'0' (excluding only the provenance line
`[RAWDIAG2 quick 260907-mhh]`). All replacement comment text below is
pre-vetted: paste verbatim, do not add dates, quick ids, or any digits to
new comment text, and do not introduce `#`, `goto`, or `::` anywhere.
</context>

<tasks>

<task type="auto">
  <name>Delete the timed-disarm branch from the RAWDIAG2 scout and run the capture body directly under the scoutActive guard (core/events.lua)</name>
  <files>core/events.lua</files>
  <action>
Edit ONLY core/events.lua. English comments, LF endings, Lua 5.0 (no
`#`/goto/:: in any added line — all replacement text below is pre-vetted).
Do NOT run build.sh or any build script. Do NOT touch spell_trace_core.lua,
SM_Extend.lua, or any other file. Match the exact strings below (line
numbers are pre-edit anchors); the tree must still be clean at HEAD 3f23472
before you start.

EDIT A — scout header comment. Preserve the provenance first line
(`-- [RAWDIAG2 quick 260907-mhh] forensics scout, placed BEFORE the tier-1
channel`), the whitelist sentence, and the keyword/state tail. Replace
exactly these three lines (8-space indent):

        -- active on the target (multi-cat)? The scout auto-disarms after a 60s window
        -- only — quick 260909-2kd removed the former line-count cap, which melee-scrum
        -- 'fades' noise could trip inside the window and end the sample early. The

with exactly these six lines (8-space indent), pasted verbatim (they carry
no digits on purpose; the battery region-gate forbids new dated text):

        -- active on the target (multi-cat)? The scout arms on the fight's Rip cast and
        -- stays armed until combat exit — macroTorch.context is wiped on leaving combat
        -- (combat_context), so each fight is one capture segment. The rotation casts
        -- Rip only at the fight's start (refreshes come from Ferocious Bite, not a
        -- recast), so a fixed timed disarm that expired mid-fight silently muted the
        -- refresh-apply lines and the second-cat tick evidence. The

The replacement's last word "The" must continue the untouched tail
`-- first 20 events of a fresh arm are dumped unconditionally as field-layout`
sentence.

EDIT B — delete the timed-disarm branch. Remove exactly these five lines,
which follow `local scoutCtx = macroTorch.context` directly (the condition
compares GetTime elapsed against the constant sixty; the log statement is
the disarm dump-stat line):

            if (GetTime() - (scoutCtx._rawdiag2Start or GetTime())) > 60 then
                scoutCtx._rawdiag2Active = false
                macroTorch.log('[RAWDIAG2] scout disarmed after 60s window, dumped: ' ..
                    tostring(scoutCtx._rawdiag2Lines or 0) .. ' lines', 'yellow')
            else

EDIT C — capture body becomes the direct body under scoutActive. The 23
lines running from the comment `-- serialize every event arg verbatim,
nil-safe` through the `end` that closes the `if interesting` block lose
exactly 4 leading spaces each (16-space base to 12-space base), with every
character otherwise identical: the nil-safe arg1..argN serialization loop
(ending at the first nil arg, Lua 5.0 style), the RAWDIAG2_KEYWORDS filter,
the first-20 `_rawdiag2Samples` gate, the `_rawdiag2Lines`/`_rawdiag2Samples`
increments, and the `[RAWDIAG2] line=` statement. Then delete the now-orphaned
`end` one line below that block (it previously closed the branch you removed
in EDIT B). The `end` closing `if scoutActive` and the
`-- end of RAWDIAG2 scout block` trailer stay put at their current depth.

Resulting structure (brackets stay balanced):

        local scoutActive = macroTorch.context and macroTorch.context._rawdiag2Active
        if scoutActive then
            local scoutCtx = macroTorch.context
            -- serialize every event arg verbatim, nil-safe: ...
            ... capture body, unconditional ...
        end

The counter/timestamp roles are unchanged: `_rawdiag2Lines` still counts
every dumped line for the `line=` label, `_rawdiag2Samples` still gates the
first-20 dump, and `_rawdiag2Start` (written by spell_trace_core.lua at arm)
loses its only reader here — which is the intended deletion of the timed
compare. Do not renumber, reorder, or reformat anything outside EDIT A/B/C.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB core/events.lua && test "$(grep -cF '> 60' core/events.lua)" = "0" && test "$(grep -cF '60s' core/events.lua)" = "0" && test "$(grep -cF 'scout disarmed after' core/events.lua)" = "0" && test "$(grep -cF '_rawdiag2Active = false' core/events.lua)" = "0" && test "$(grep -cF 'GetTime() - (scoutCtx._rawdiag2Start' core/events.lua)" = "0" && test "$(grep -cF 'if scoutActive then' core/events.lua)" = "1" && test "$(grep -cF '_rawdiag2Lines' core/events.lua)" -ge "1" && test "$(grep -cF '_rawdiag2Samples' core/events.lua)" -ge "1" && test "$(sed -n '/forensics scout, placed BEFORE/,/end of RAWDIAG2 scout block/p' core/events.lua | grep '60' | grep -vF '260907-mhh' | wc -l)" = "0" && test "$(sed -n '/forensics scout, placed BEFORE/,/end of RAWDIAG2 scout block/p' core/events.lua | grep -cF 'else')" = "0" && test "$(sed -n '/local scoutCtx = macroTorch.context/,+4p' core/events.lua | grep -c 'serialize every event arg')" = "1" && test "$(git diff --numstat -- core/events.lua | awk '{print $2-$1}')" = "3" && { if git diff -U0 -- core/events.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo "TOKEN GATE FAILED"; exit 1; fi; } && git diff --check && echo "T1 GATE OK"</automated>
  </verify>
  <done>
bbcheck prints `core/events.lua: BALANCED` (exit 0). No `> 60` compare, no
'60s' reference, no 'scout disarmed after' disarm log, no
`_rawdiag2Active = false` assignment, and no GetTime-elapsed condition
survive in core/events.lua; the `if scoutActive then` guard remains (count 1);
both counters' names remain; the scout block (bounded by `forensics scout,
placed BEFORE` and `end of RAWDIAG2 scout block`) contains no '6'+'0' digits
beyond the provenance tag line and no `else`; the serialization comment
follows `local scoutCtx = macroTorch.context` directly (unconditional body);
the numstat removed-minus-added is exactly 3 (32 removed / 29 added: 5 branch
+ 1 orphan end + 3 header old lines vs 6 header new lines); added lines carry
no `#`/goto/:: and `git diff --check` is clean — `T1 GATE OK` echoes.
  </done>
</task>

<task type="auto">
  <name>Update the RAWDIAG2 arming literal and comment block in core/spell_trace_core.lua to the until-combat-exit contract</name>
  <files>core/spell_trace_core.lua</files>
  <action>
Edit ONLY core/spell_trace_core.lua. Do NOT run build.sh or any build
script. Do NOT touch core/events.lua or SM_Extend.lua. English comments, LF
endings, Lua 5.0 (no `#`/goto/:: in added lines — replacement text below is
pre-vetted and carries no digits).

EDIT A — arming comment block. Keep the provenance line and the first three
sentences byte-identical (`-- [RAWDIAG2 quick 260907-mhh] arm the
RAW_COMBATLOG forensics scout on a` through `-- events.lua's RAW_COMBATLOG
branch, before the channel whitelist.`), and keep the last two lines
byte-identical (`-- macroTorch.context: combat exit wipes it, nothing is
persisted, and the` / `-- gating shares no state with any other diagnostic
switch.`). Replace exactly these three lines (4-space indent):

    -- resets the per-window line/sample counters and restarts the 60s window, so
    -- in a repeat-cast fight each cast anchors a fresh full window (the decisive
    -- multi-cat overlap evidence is per-cast). State lives only in

with exactly these five lines (4-space indent), pasted verbatim:

    -- resets the line/sample counters and the capture runs until combat exit —
    -- nothing disarms it before leaving combat. The rotation casts Rip once per
    -- fight (refreshes come from Ferocious Bite, not a recast), so each fight
    -- anchors one full capture (the decisive multi-cat overlap evidence is
    -- per-fight). State lives only in

EDIT B — arming log literal. Replace exactly this line (8-space indent):

        macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, 60s window', 'green')

with exactly this line (8-space indent):

        macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, until combat exit', 'green')

Keep the arming code byte-identical (the four context assignments
`_rawdiag2Lines = 0`, `_rawdiag2Samples = 0`, `_rawdiag2Active = true`,
`_rawdiag2Start = GetTime()` retain their exact roles and order). Leave the
pair-ledger comment at planning-time line 362 ("armed only inside a live
scout window") byte-identical — it is still accurate because the armed
period now runs until combat exit. The `until combat exit` literal is the
segment marker: one armed line per fight segments the dump now that no
disarm line exists.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB core/spell_trace_core.lua && grep -qF "macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, until combat exit', 'green')" core/spell_trace_core.lua && test "$(sed -n '120,145p' core/spell_trace_core.lua | grep -cF 'until combat exit')" -ge "1" && test "$(grep -cF '60s' core/spell_trace_core.lua)" = "0" && test "$(grep -cF '60s window' core/spell_trace_core.lua)" = "0" && test "$(grep -cF '_rawdiag2Active = true' core/spell_trace_core.lua)" = "1" && test "$(grep -cF '_rawdiag2Lines' core/spell_trace_core.lua)" = "1" && test "$(grep -cF '_rawdiag2Samples' core/spell_trace_core.lua)" = "1" && test "$(grep -cF '_rawdiag2Start' core/spell_trace_core.lua)" = "1" && test "$(git diff --numstat -- core/spell_trace_core.lua | awk '{print $1-$2}')" = "2" && { if git diff -U0 -- core/spell_trace_core.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo "TOKEN GATE FAILED"; exit 1; fi; } && git diff --check && echo "T2 GATE OK"</automated>
  </verify>
  <done>
bbcheck prints `core/spell_trace_core.lua: BALANCED` (exit 0). The arming
literal reads `[RAWDIAG2] scout armed by Rip cast record, until combat exit`;
the arming comment block (lines 120-145) contains the until-combat-exit
contract with no '60s' wording; no '60s' survives anywhere in the file; the
arming code is intact (one `_rawdiag2Active = true`, one each of
`_rawdiag2Lines`/`_rawdiag2Samples`/`_rawdiag2Start` write); the numstat is
exactly 6 added / 4 removed (added-minus-removed 2); added lines carry no
`#`/goto/:: and `git diff --check` is clean — `T2 GATE OK` echoes.
  </done>
</task>

<task type="auto">
  <name>Closing battery: syntax proxy, Lua 5.0 token gate, LF, English, diff hygiene (no file edits unless a gate fails)</name>
  <files>core/events.lua, core/spell_trace_core.lua</files>
  <action>Run the whole-repo hygiene battery over the combined uncommitted diff of the
two edited files — leave the working tree in its edited, UNcommitted state so
the diff gates in this task's verify measure real content (the quick
orchestrator makes the atomic commit after the plan closes). Do NOT run
build.sh or any build script, and do NOT touch SM_Extend.lua. If a battery
gate fails, fix the minimal offending line in the file the gate names
(English comment, LF, Lua 5.0 token), then re-run this task's verify and the
failed edit task's verify.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB core/events.lua core/spell_trace_core.lua && test "$(git diff -U0 HEAD -- core/events.lua core/spell_trace_core.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -cE '\bgoto\b|#|::')" = "0" && test "$(git diff --check | wc -l)" = "0" && if grep -q $'\r' core/events.lua core/spell_trace_core.lua; then echo "CRLF FOUND"; exit 1; fi && test "$(git diff -U0 HEAD -- core/events.lua core/spell_trace_core.lua | grep -E '^\+' | grep -vE '^\+\+' | LC_ALL=C grep -cP '[\x{4e00}-\x{9fff}]')" = "0" && git diff --quiet HEAD -- SM_Extend.lua && test "$(git diff --name-only HEAD -- '*.lua' | grep -cvE '^core/(events|spell_trace_core)\.lua$')" = "0" && if grep -q -- '150' core/events.lua core/spell_trace_core.lua; then echo "STALE 150 TOKEN FOUND"; exit 1; fi && echo "BATTERY OK"</automated>
  </verify>
  <done>
Both bbcheck runs print `BALANCED` (exit 0). The combined added lines carry
no `#`, `goto`, or `::`; `git diff --check` is clean and no CR byte exists in
either file (LF preserved); no CJK character appears in added lines (English
comments and messages only); SM_Extend.lua is byte-identical to HEAD; the
only Lua files in the working-tree diff are core/events.lua and
core/spell_trace_core.lua; no '150' token survives (previous quick's cap
stays gone) — `BATTERY OK` echoes; the working tree carries exactly the two
edited files uncommitted, ready for the orchestrator's atomic commit without
SM_Extend.lua.
  </done>
</task>

</tasks>

<threat_model>## Trust Boundaries

None crossed. This plan deletes a timed-disarm branch inside the addon's own
optional forensics scout; it reads no new inputs, writes nothing new,
installs no packages (no package-legitimacy gate applies), and touches no
network or privilege context. The only persisted output remains the
pre-existing macroTorch.log -> MACRO_TORCH_LOG SavedVariables path.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260909-4ep-01 | Denial of Service | core/events.lua RAWDIAG2 scout: dump volume now bounded only by combat length | low | accept | Still bounded by design: the dump stays keyword-filtered after the first 20 samples per arm, macroTorch.log's rotating buffer (macroTorch.LOG_MAX_SIZE, user-raised to 2000 in game per session) drops oldest lines first, arm remains opt-in (rawdiag2Enabled defaults false), and all _rawdiag2* state is wiped on combat exit — worst case old samples rotate out, the same failure mode as existing production diagnostic lines. |
| T-quick-260909-4ep-02 | Information Disclosure | Raw event arg dump (other players' GUIDs/spell lines) | low | accept | Unchanged from quick 260907-mhh: lines land only in the player's own SavedVariables log while the player is actively engaged in a forensics session; the same data is already visible to the client in its own combat log; opt-in flag plus combat-exit wipe remain in force. |

No npm/pip/cargo install tasks exist in this plan, so the T-*-SC package-legitimacy row does not apply.
</threat_model>

<verification>
After the three tasks, confirm the combined diff is exactly right:

```bash
cd /home/admin/workspace/macro-torch && git status --porcelain
# exactly: ` M core/events.lua`, ` M core/spell_trace_core.lua` — nothing else;
# SM_Extend.lua MUST NOT appear
```

Re-run the T1 and T2 gates plus this battery; all must exit 0 with their
`T1 GATE OK` / `T2 GATE OK` / `BATTERY OK` echoes. The T1 gate proves the
whole timed-disarm semantics are gone (no `> 60` compare, no 'scout disarmed
after' log, no `_rawdiag2Active = false`, no '6'+'0' digits in the scout
block beyond the provenance tag, no `else` in the block, capture body
unconditional and directly under scoutActive), and that the counters and
timestamp (per-contract retention) survive. The T2 gate proves the arm
literal and comment block state the until-combat-exit contract and the
arming code is byte-identical. No build script is run anywhere in this plan;
in-game re-verification (rebuild SM_Extend.lua on the user's Cygwin machine,
next CD session watching one armed segment per fight with no disarm line) is
the user_setup follow-up — it cannot be automated on this host and is
weekly-CD paced.
</verification>

<success_criteria>
- T1 gate exits 0: bbcheck BALANCED on core/events.lua; the timed disarm (compare, assignment, disarm log, `else`) is fully gone; capture runs unconditionally under `if scoutActive then`; region gate shows no '6'+'0' digits beyond the provenance tag; numstat removed-minus-added exactly 3; no `#`/goto/:: in added lines.
- T2 gate exits 0: bbcheck BALANCED on core/spell_trace_core.lua; arming literal reads 'until combat exit'; arming comment block updated with zero remaining '60s' tokens; four arming assignments intact; numstat added-minus-removed exactly 2.
- Battery exits 0: combined diff adds no `#`/goto/:: and no CJK; `git diff --check` clean; no CR bytes (LF preserved); SM_Extend.lua untouched; only the two core Lua files changed; no stale '150' tokens.
- One atomic conventional commit contains only core/events.lua and core/spell_trace_core.lua, e.g. `refactor(druid): RAWDIAG2 scout stays armed until combat exit, drop timed disarm (quick 260909-4ep)`.
- In-game validation is staged per user_setup (rebuild SM_Extend.lua on the user's Windows+Cygwin machine, then the next CD forensics session observes one '[RAWDIAG2] scout armed by Rip cast record, until combat exit' segment per fight with refresh-apply and second-cat tick lines captured for the whole fight) — evidence collection, not an offline gate.
</success_criteria>

<output>
Create `.planning/quick/260909-4ep-rawdiag2-scout-60s-disarm-rip-arm-combat/260909-4ep-SUMMARY.md` when done
</output>