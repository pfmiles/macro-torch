# Quick Task 260804-0bz: Fix Pounce Savagery Duration

**Created:** 2026-08-04
**Mode:** quick
**Status:** planned

## Summary

Pounce 的 duration 和 ERPS tickInterval 未考虑 Idol of Savagery 的影响，导致宏计算的 debuff 剩余时间与实际游戏不符（18s vs 16.2s）。

## Tasks

### Task 1: Fix pounceLeft() and computePounce_Erps() in Druid.lua

**Files:** `classes/druid/Druid.lua`

**Actions:**
1. `pounceLeft()` (line ~1116): 添加 savagery 判断，duration `* 0.9`，与 `rakeLeft()`/`ripLeft()` 模式一致
2. `computePounce_Erps()` (line ~622): 添加 savagery 判断，tickInterval `* 0.9`，与 `computeRake_Erps()`/`computeRip_Erps()` 模式一致

### Task 2: Record lastPounceEquippedSavagery at cast sites

**Files:** `classes/druid/combo.lua`, `classes/druid/leveling.lua`

**Actions:**
1. `combo.lua` (line ~139): `player.pounce()` 后记录 `lastPounceEquippedSavagery`
2. `leveling.lua` (line ~76): `player.pounce()` 后记录 `lastPounceEquippedSavagery`