---
phase: quick-260909-2kd-rawdiag2-forensics-scout-150-150-24-fade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/events.lua
  - core/spell_trace_core.lua
autonomous: true
requirements:
  - QUICK-260909-2KD
user_setup:
  - service: wow-client
    why: "The cap removal only manifests in game, on the user's Windows+Cygwin machine (this host cannot run WoW 1.12/SuperWoW). Follow-up is user-side only: (1) run build.sh on Windows+Cygwin to regenerate SM_Extend.lua into both AddOns dirs (per established quick-task convention this repo's SM_Extend.lua is NOT rebuilt here); (2) on the next CD forensics session set /run macroTorch.rawdiag2Enabled=true and cast an in-combat Rip to arm — the scout must now stay armed for the FULL 60s window regardless of dumped line count, emitting exactly one '[RAWDIAG2] scout disarmed after 60s window' line with the dumped count; (3) the MACRO_TORCH_LOG buffer (macroTorch.LOG_MAX_SIZE, per-session /run config) is already raised to 2000 in game so the larger dump fits. Evidence gathering is weekly-CD paced (user memory convention)."
must_haves:
  truths:
    - "Time-only disarm: the scout disarms on the 60s window and nothing else — exactly one `_rawdiag2Active = false` assignment remains in core/events.lua, and no code path compares a line counter against a threshold anymore."
    - "Counter-as-stat survives: `_rawdiag2Lines` still increments on every dumped line and feeds only the disarm line's `dumped: N lines` stat and the `line=` label; `_rawdiag2Samples` still gates the first-20 unconditional dump; nothing is gated on either counter."
    - "Honest instrumentation text: the arming log line in core/spell_trace_core.lua and the scout header comment in core/events.lua both describe a 60s window with no line cap; the token '150' no longer appears anywhere in either file."
    - "Sterile scope: only core/events.lua and core/spell_trace_core.lua change (7 removed + 5 added lines and 1 removed + 1 added line respectively); the pair ledger, ctx stamp, keyword filter, sample gate, tier-1 whitelist and every production branch are byte-identical; SM_Extend.lua is untouched and not rebuilt."
  artifacts:
    - core/events.lua
    - core/spell_trace_core.lua
  key_links:
    - "events.lua RAW_COMBATLOG scout: `if (GetTime() - (scoutCtx._rawdiag2Start or GetTime())) > 60 then` disarm (planning-time lines 147-150) now takes the dump body as its `else` — deleting the elseif merges the dump path under the single time gate."
    - "spell_trace_core.lua:140 arming literal <-> events.lua:139-142 scout comment — the single 60s-only window contract both texts must state (the arming comment block at spell_trace_core.lua:126-134 already carries no cap reference and stays untouched)."
    - "events.lua `_rawdiag2Lines` increment (planning-time line 174) -> disarm dump stat (planning-time line 150) — the retained read-only stat path that justifies keeping the counter."
    - "macroTorch.log -> MACRO_TORCH_LOG rotating buffer (interface_debug.lua:103, capped by macroTorch.LOG_MAX_SIZE, user-raised to 2000 per session) — the volume sink that makes the uncapped 60s window safe."
---

<objective>
Remove the 150-line acquisition cap from the RAWDIAG2 forensics scout so the
60s time window becomes its ONLY disarm condition.

Purpose: on the live machine a melee scrum floods the armed window with
'fades' keyword noise and hit the 150-line cap in ~24 seconds — the window
closed early, wasting a weekly-CD forensics opportunity (the multi-cat Rip
landing arbitration from quick 260907-mhh). The user has already raised the
MACRO_TORCH_LOG buffer to 2000 in game, so the line-count cap no longer
protects anything; it only shortens the evidence window. Keep the
_rawdiag2Lines counter as a dump statistic (the disarm line still reports how
many lines were dumped), change nothing else in the instrumentation.

Output: core/events.lua (cap branch deleted, scout comment rewritten to
60s-only) and core/spell_trace_core.lua (arming log literal updated to
'60s window'). SM_Extend.lua is NOT modified and NOT rebuilt (established
quick-task convention; the user rebuilds on their game machine).
</objective>

<execution_context>@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@core/events.lua
@core/spell_trace_core.lua

Grounding facts, all verified by reading the working tree at planning time
(HEAD f39c667b, both target files clean — do not re-derive these anchors
blindly; the plan's anchors are live). Repo conventions: English comments, LF
endings, Lua 5.0 only (no `#` length operator, no goto, no labels — every
added line must contain no `#`, no `goto`, no `::`; memory: wow-lua50-syntax),
and SM_Extend.lua is a build artifact never edited (memory: sm-extend-is-
build-artifact). No Lua interpreter exists on this host; node v24 +
.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js
(bracket-balance gate, prints `BALANCED` / exit 0) is the repo's established
syntax proxy. Do NOT run build.sh (it regenerates SM_Extend.lua).

- core/events.lua, RAW_COMBATLOG scout block: comment header at planning-time
  lines 136-143 (the disarm sentence spans 139-141, containing the
  150-line cap clause). `if (GetTime() - ...) > 60 then` 60s disarm branch at
  147-150 (KEEP byte-identical). The `elseif (scoutCtx._rawdiag2Lines or 0)
  >= 150 then` branch at 151-154 (DELETE — the cap). Dump body at 155-178:
  serialization loop, RAWDIAG2_KEYWORDS filter (table literal at line 26),
  first-20 sample gate, counter increments at 173-175 (`_rawdiag2Lines` and
  `_rawdiag2Samples`) and the `[RAWDIAG2] line=` statement at 176-177 — all
  KEEP byte-identical. Closing `end`/`end` at 179-180 and the `-- end of
  RAWDIAG2 scout block` trailer at 181 stay put.
- core/spell_trace_core.lua: arming hook at 135-141, log literal on line 140
  spelling '60s / 150-line window' (EDIT to '60s window'). The arming comment
  block at 126-134 says only "resets the per-window line/sample counters and
  restarts the 60s window" — no cap mention, still accurate, do NOT touch.
  The pair-ledger comment at 362 ("armed only inside a live scout window") is
  likewise cap-free and accurate — do NOT touch.
- No other file references the cap: grep over macro_torch.lua (config banner
  for rawdiag2Enabled) and core/selftest.lua both show zero 150/window
  mentions; the .planning/ hits (samples, historical quick/phase artifacts)
  are archival and out of scope.
- macroTorch.log (interface_debug.lua:103) appends to MACRO_TORCH_LOG.messages;
  the per-session cap macroTorch.LOG_MAX_SIZE (quick 260907-vve, default 500)
  has been raised to 2000 in game by the user — the volume safety net after
  the line cap is gone. interface_debug.lua is NOT edited.

Token gate note for the executor: the plan-level token gate greps the diff
+lines for goto/#/:: — the replacement comment text below is already vetted
clean. The bare token '150' must not survive in either file after editing;
the replacement comment spells the removed cap as "line-count cap" so the
absence gate stays green.
</context>

<!-- planner-discipline-allow: 150 -->

<tasks>

<task type="auto">
  <name>Remove the 150-line disarm branch from the RAWDIAG2 scout in core/events.lua and rewrite its header comment to 60s-only</name>
  <files>core/events.lua</files>
  <action>
Edit ONLY core/events.lua (planning-time anchors at HEAD f39c667b, clean
tree; match on the exact strings below, line numbers are pre-edit anchors).
English comments, LF endings, Lua 5.0 (no `#`/goto/:: in any added line).
Do NOT touch spell_trace_core.lua, SM_Extend.lua, or any other file.

EDIT A — rewrite the disarm sentence in the scout header comment. Replace
exactly these three lines (8-space indent):

        -- active on the target (multi-cat)? The scout auto-disarms after a 60s window
        -- or once _rawdiag2Lines reaches the 150-line cap; the first 20 events of a
        -- fresh arm are dumped unconditionally as field-layout samples, after that

with exactly these five lines (8-space indent):

        -- active on the target (multi-cat)? The scout auto-disarms after a 60s window
        -- only — quick 260909-2kd removed the former line-count cap, which melee-scrum
        -- 'fades' noise could trip inside the window and end the sample early. The
        -- first 20 events of a fresh arm are dumped unconditionally as field-layout
        -- samples, after that

The replacement deliberately says "line-count cap" — the digits '150' must
not appear anywhere in the new text. Leave the provenance tag on the first
comment line (`[RAWDIAG2 quick 260907-mhh]`) unchanged, and leave the
remaining comment lines (only RAWDIAG2_KEYWORDS matches / State lives in
macroTorch.context / never persisted / macroTorch.log) unchanged.

EDIT B — delete the 150-line disarm branch. Remove exactly these four lines
(12/16-space indent, directly under the 60s branch):

            elseif (scoutCtx._rawdiag2Lines or 0) >= 150 then
                scoutCtx._rawdiag2Active = false
                macroTorch.log('[RAWDIAG2] scout disarmed after 150-line cap, dumped: ' ..
                    tostring(scoutCtx._rawdiag2Lines or 0) .. ' lines', 'yellow')

Resulting structure (the dump becomes the else of the 60s gate — one `if`
/`else`/`end`, brackets stay balanced):

            if (GetTime() - (scoutCtx._rawdiag2Start or GetTime())) > 60 then
                scoutCtx._rawdiag2Active = false
                macroTorch.log('[RAWDIAG2] scout disarmed after 60s window, dumped: ' ..
                    tostring(scoutCtx._rawdiag2Lines or 0) .. ' lines', 'yellow')
            else
                -- serialize every event arg verbatim, nil-safe: ... (unchanged)

KEEP byte-identical: the 60s branch (if/assign/log above), the whole dump
body (the nil-safe arg serialization via the `_G['arg' .. ai]` loop, the
keyword filter over RAWDIAG2_KEYWORDS, the first-20 sample gate, the two
counter increments `scoutCtx._rawdiag2Lines = (scoutCtx._rawdiag2Lines or 0)
+ 1` and the `_rawdiag2Samples` twin, and the `[RAWDIAG2] line=` statement),
the closing `end`s, and the `-- end of RAWDIAG2 scout block` trailer comment.
This is exactly why both counters survive: `_rawdiag2Lines` still counts
every dumped line and is printed in the disarm line's `dumped: N lines`
stat, and `_rawdiag2Samples` still gates the first-20 unconditional dump —
only the threshold comparison disappears. Do not renumber, reorder or
re-format anything outside the two edits.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB core/events.lua && test "$(grep -c '_rawdiag2Active = false' core/events.lua)" = "1" && test "$(grep -c "tostring(scoutCtx._rawdiag2Lines or 0) .. ' lines'" core/events.lua)" = "1" && grep -qF "if (GetTime() - (scoutCtx._rawdiag2Start or GetTime())) > 60 then" core/events.lua && grep -qF "scoutCtx._rawdiag2Lines = (scoutCtx._rawdiag2Lines or 0) + 1" core/events.lua && grep -qF "scoutCtx._rawdiag2Samples = (scoutCtx._rawdiag2Samples or 0) + 1" core/events.lua && grep -qF "former line-count cap, which melee-scrum" core/events.lua && ! grep -q -- '150' core/events.lua && test "$(git diff --numstat -- core/events.lua | awk '{print $1 " " $2}')" = "7 5" && { if git diff -U0 -- core/events.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo "TOKEN GATE FAILED"; exit 1; fi; } && git diff --check && echo "T1 GATE OK"</automated>
  </verify>
  <done>
bbcheck prints `core/events.lua: BALANCED` (exit 0). Exactly ONE disarm assignment survives (the 60s branch), exactly ONE disarm dump-stat line survives (`tostring(scoutCtx._rawdiag2Lines or 0) .. ' lines'`), the 60s time gate line is present, both counter increments are present, the new comment fragment is present, no line in the file contains '150', the numstat is exactly `7 5` (7 removed / 5 added), and the diff +lines carry no `#`/goto/:: and no whitespace errors — `T1 GATE OK` echoes.
  </done>
</task>

<task type="auto">
  <name>Update the RAWDIAG2 arming log literal in core/spell_trace_core.lua to '60s window'</name>
  <files>core/spell_trace_core.lua</files>
  <action>
Edit ONLY core/spell_trace_core.lua. One-line change at the arming log
(planning-time line 140). Replace exactly this line (8-space indent):

        macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, 60s / 150-line window', 'green')

with exactly this line (8-space indent):

        macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, 60s window', 'green')

Nothing else in this file changes. Verified at planning time and already
accurate — leave byte-identical: the arming comment block at lines 126-134
("resets the per-window line/sample counters and restarts the 60s window"
still holds because both counters survive as dump stats, and it never
mentions a line cap anyway) and the pair-ledger comment at line 362 ("inside
a live scout window"). Do NOT add, remove or reword any comment. Do NOT
touch core/events.lua or SM_Extend.lua.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB core/spell_trace_core.lua && grep -qF "macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, 60s window', 'green')" core/spell_trace_core.lua && ! grep -q -- '150' core/spell_trace_core.lua && test "$(git diff --numstat -- core/spell_trace_core.lua | awk '{print $1 " " $2}')" = "1 1" && { if git diff -U0 -- core/spell_trace_core.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo "TOKEN GATE FAILED"; exit 1; fi; } && git diff --check && echo "T2 GATE OK"</automated>
  </verify>
  <done>
bbcheck prints `core/spell_trace_core.lua: BALANCED` (exit 0). The new arming literal `[RAWDIAG2] scout armed by Rip cast record, 60s window` is present, no line in the file contains '150', the numstat is exactly `1 1`, the diff +line carries no `#`/goto/:: and `git diff --check` is clean — `T2 GATE OK` echoes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed. This plan deletes a counter threshold inside the addon's own
optional forensics scout; it reads no new inputs, writes nothing new, installs
no packages (no package-legitimacy gate applies), and touches no network or
privilege context. The only persisted output remains the pre-existing
macroTorch.log -> MACRO_TORCH_LOG SavedVariables path.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260909-2kd-01 | Denial of Service | core/events.lua RAWDIAG2 scout: uncapped dump volume inside the 60s window | low | accept | Still bounded by design: 60s is now the only window bound and the dump stays keyword-filtered after the first 20 samples; macroTorch.log's rotating buffer (macroTorch.LOG_MAX_SIZE, user-raised to 2000 in game per session) drops oldest lines first — worst case old samples rotate out, the same failure mode as the existing production diagnostic lines. Arm remains opt-in (rawdiag2Enabled defaults false) and all _rawdiag2* state is wiped on combat exit. |
| T-quick-260909-2kd-02 | Information Disclosure | Raw event arg dump (other players' GUIDs/spell lines) | low | accept | Unchanged from quick 260907-mhh: lines land only in the player's own SavedVariables log while the player is actively engaged in a forensics session; the same data is already visible to the client in its own combat log; opt-in flag plus combat-exit wipe remain in force. |

No npm/pip/cargo install tasks exist in this plan, so the T-*-SC package-legitimacy row does not apply.
</threat_model>

<verification>After both tasks, confirm the combined diff is exactly right:

```bash
cd /home/admin/workspace/macro-torch && git status --porcelain
# exactly: ` M core/events.lua`, ` M core/spell_trace_core.lua` — nothing else;
# SM_Extend.lua MUST NOT appear

cd /home/admin/workspace/macro-torch && git diff --check
# exit 0, no output — LF preserved, no trailing whitespace
```

Whole-diff Lua 5.0 token gate (no `#`, no goto, no `::` in any added line):

```bash
cd /home/admin/workspace/macro-torch && if git diff -U0 -- core/events.lua core/spell_trace_core.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo "TOKEN GATE FAILED"; exit 1; else echo "TOKEN GATE OK"; fi
```

Cap-token absence across both files (the only surviving description of the
removed cap is the word "cap" — the digits must be gone file-wide):

```bash
cd /home/admin/workspace/macro-torch && if grep -q -- '150' core/events.lua core/spell_trace_core.lua; then echo "STALE 150 TOKEN FOUND"; exit 1; else echo "CAP TOKEN CLEAR"; fi
```

Re-run the T1 and T2 gates; both must exit 0 with their `GATE OK` echo.
In-game re-verification (rebuild on the user's Cygwin machine, next CD
session running the full 60s window) is the user_setup follow-up — it cannot
be automated on this host and is weekly-CD paced.
</verification>

<success_criteria>
- T1 gate exits 0: bbcheck BALANCED on core/events.lua; exactly 1 disarm assignment and 1 disarm dump-stat line; 60s gate line and both counter increments present; new comment fragment present; zero '150' tokens; numstat exactly `7 5`; added lines carry no #/goto/::.
- T2 gate exits 0: bbcheck BALANCED on core/spell_trace_core.lua; new '60s window' arming literal present; zero '150' tokens; numstat exactly `1 1`.
- Combined: `git status --porcelain` shows exactly the two source files (SM_Extend.lua absent), `git diff --check` clean, whole-diff token gate OK, cap-token gate clear of '150'.
- In-game validation is staged per user_setup (rebuild SM_Extend.lua on the user's Windows+Cygwin machine, then the next CD forensics session observes the scout staying armed for the full 60s) — evidence collection, not an offline gate.
- One atomic conventional commit containing only the two allowed files, e.g. `refactor(druid): RAWDIAG2 scout drops 150-line cap, 60s window only (quick 260909-2kd)`.
</success_criteria>

<output>Create `.planning/quick/260909-2kd-rawdiag2-forensics-scout-150-150-24-fade/260909-2kd-SUMMARY.md` when done
</output>