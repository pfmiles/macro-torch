---
quick_id: 260823-gg8
slug: catatk-core-principles-md-phase
description: "update .planning/catAtk-core-principles.md with Phase 26 fast-battle (8.5s) tier — embedded update, essence style, no implementation details"
date: 2026-08-23
---

# PLAN: catAtk 核心原则文档补充「速战」战斗分层

## 目标

将 Phase 26 新增的「速战」战斗分层（fast battle，8.5s 死亡预测，非 PvP）以嵌入式方式补充到 `.planning/catAtk-core-principles.md`。纯文档更新，不改任何 Lua 代码。

## 锁定约束（必须遵守，违反即失败）

1. **只改一个文件**：`.planning/catAtk-core-principles.md`。
2. **不新增独立规则编号**：现有 14 条规则编号与测试 ID（R/PF 系列）映射保持不变；速战内容嵌入规则 5。
3. **精华风格**：指导性方针 + 核心逻辑描述。**禁止**新增详细伪代码块；**禁止**写实现细节（lazy cache 实现、guard 判断顺序、healthMax <= teamDPS 公式、PvP 排除的实现顺序等）。
4. **术语锁定**：速战 = fast battle (8.5s) = `isFastBattleNotPvp`；快战 = trivial battle (25s) = `isTrivialBattle`。文件现有「快战」表述一律逐字保留不动。「速战」档所有表述必须带「非 PvP」限定。

## 背景事实（来源：STATE.md 与 Phase 26 决策，仅供原则层引用）

- `isFastBattleNotPvp`（`Druid.lua`）：8.5s 死亡预测且非 PvP 时为速战；纯直伤策略，跳过所有流血（突袭/扫击/割裂），仅撕碎/爪击攒星 → 5 星撕咬（STATE.md:77）。
- 流血无法回本原理：扫击持续 9s、割裂至少 10s（5 星 18s），均超过 8.5s 死亡阈值 → 所有能量转为直伤。
- SelfTest Category P 共 6 个测试（P-01 ~ P-06，`core/selftest.lua`）。

## 任务

行号基于本次 Read（2026-08-23）的当前文件；Task 2 一律以行内容为锚点定位，行号仅作参考。

### Task 1: 规则 5 两档改三档 — 速战档 + 战斗时长分层总览表

- **文件**: `.planning/catAtk-core-principles.md`
- **范围**: 规则 5 区块（当前第 112–135 行）。规则标题（112 行）、既有伪代码块（118–124 行）、相关函数表既有行均不做破坏性改动。
- **操作**:
  1. 在「**原则：**」（114 行）之后、第一条 bullet 之前，插入简化总览表（4 行，函数名用反引号，与附录 D 风格一致）：

     | 层级 | 阈值 | 策略 | 核心函数 |
     |------|------|------|----------|
     | 斩杀 | 2s | 有星就咬 | `isKillShotOrLastChance` |
     | 速战 | 8.5s（非 PvP） | 纯直伤，零流血 | `isFastBattleNotPvp` |
     | 快战 | 25s / PvP | 低星割裂（辅助） | `isTrivialBattle` |
     | 正常 | > 25s | 5CP 割裂（最大化） | — |

  2. 将「原则」两条 bullet 扩展为三档，**顺序为 速战 → 快战 → 正常**（与总览表升序时长一致）：
     - 新增速战档 bullet（原则层表述，不含实现细节）：
       **速战（8.5s 死亡预测，非 PvP）：** 完全跳过所有流血（突袭/扫击/割裂）——目标预计 8.5s 内死亡时流血无法回本：扫击持续 9s、割裂至少 10s（5 星 18s），均超过死亡阈值——所有能量转为直伤：撕碎/爪击攒星 → 5 星撕咬；斩杀条件（规则 9）优先。
     - 既有快战 bullet（116 行「**快战（短）战斗 / PvP：** …」）逐字保留。
     - 既有正常 bullet（115 行「**正常（长）战斗：** …」）逐字保留。
  3. 相关函数表（126–131 行）新增一行：`| `isFastBattleNotPvp` (`Druid.lua`) | 速战判定（8.5s 死亡预测，非 PvP） |`，放在 `isTrivialBattle` 行（131 行）之后。
  4. 核心洞察（133 行）原句逐字保留，末尾追加一句收束：速战是该逻辑的极限：当剩余寿命短于最短流血时长（割裂基础 10s）时，流血连「辅助增益」价值都不再成立——全部能量直接转化为直伤。
- **验证**:
  - `grep -n "速战" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md` 命中 ≥ 4 处（总览表行、bullet、函数表行、核心洞察句）
  - `grep -n "isFastBattleNotPvp" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md` 命中规则 5 区块
  - 伪代码块数不变：编辑前先记录 `grep -c '^```' /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md`（当前 = 28），编辑后必须仍为 28

### Task 2: 附录 B + 附录 D 同步更新 + 最后更新日期

- **文件**: `.planning/catAtk-core-principles.md`
- **操作**（全部以行内容为锚点）:
  1. **附录 B 关键常量表**：在含「快战阈值」的行（当前 435 行）与含「斩杀预测」的行（当前 436 行）之间插入一行，格式与相邻行完全一致：
     `| 速战阈值 | 8.5s | 预计存活 < 8.5s 且非 PvP → 纯直伤（跳过流血） | `isFastBattleNotPvp` (`Druid.lua`) |`
  2. **附录 D 原则→代码可追溯矩阵**：定位含「时长自适应割裂」的行（当前 460 行），替换为（函数与文件一一对应，用顿号分隔）：
     `| 5 | 时长自适应割裂 | `isFastBattleNotPvp`、`shouldCastRip`、`quickKeepRip`、`keepRip` | `Druid.lua`、`Druid.lua`、`cat.lua`、`cat.lua` |`
  3. **SelfTest 覆盖表**（当前 473–483 行）：在含「4, 5」的行（当前 477 行）之后、含「6」的行之前插入一行：`| 5（速战档） | P-01 ~ P-06 |`
  4. **底部日期**：将 `*最后更新：2026-07-29*`（当前 487 行）改为 `*最后更新：2026-08-23*`。
- **验证**:
  - `grep -n "速战阈值" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md` 命中附录 B 表
  - `grep -n "P-01" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md` 命中 SelfTest 覆盖表
  - 附录 D 中含「时长自适应割裂」的行同时含 `isFastBattleNotPvp`
  - `grep -n "最后更新" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md` 显示 2026-08-23

### Task 3: 一致性校验 + 单文件提交

- **操作**:
  1. 实现细节泄漏负向检查（以下 token 在原则文件中的计数必须全部为 0，注意 grep 目标只限原则文件本身）：
     `grep -c -E "lazy|healthMax|isCanAttack|teamDPS|FIELD_FUNC_MAP" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md`
  2. 伪代码块数不变：`grep -c '^```' /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md` == 28
  3. `git status --porcelain` → 仅 `.planning/catAtk-core-principles.md` 被修改，无任何 Lua 文件
  4. `git diff -- .planning/catAtk-core-principles.md` 人工复核：仅四个区域有变化（规则 5 区块、附录 B 表、附录 D 两处、最后更新行）；「快战」「isTrivialBattle」「25s」原有表述未被改动；新表格列对齐无破损
  5. 提交（英文 commit message，与仓库惯例一致）：
     `git add .planning/catAtk-core-principles.md && git commit -m "docs(catAtk): add fast-battle (8.5s) tier to core principles"`
- **验证**:
  - `git show --stat HEAD` 仅含 `planning/catAtk-core-principles.md`
  - `git log -1 --format=%s` 输出为 `docs(catAtk): add fast-battle (8.5s) tier to core principles`