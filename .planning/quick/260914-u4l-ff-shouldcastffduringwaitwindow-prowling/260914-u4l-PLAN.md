---
quick_id: 260914-u4l
slug: ff-shouldcastffduringwaitwindow-prowling
subsystem: druid-cat
description: "为 macroTorch.shouldCastFFDuringWaitWindow 基础排除块新增 prowling 排除臂（or clickContext.prowling，紧跟 ooc 臂后第二行）+ 2 行英注释；classes/druid/selftest.lua 在 WR-01 FF pin 之后、-- End of Batch 2 之前插入 47 行 prowling 拒绝 pin（与 WR-01 同帧仅差 prowling=true）。两文件纯插入零删除，单 commit。潜行帧 FF 填充会打破隐形并开怪，摧毁 Pounce/Ravage 起手；修复位置由用户裁定在决策函数而非 keepFF/safeFF"
date: 2026-09-14
phase: quick
plan: 260914-u4l
type: execute
wave: 1
depends_on: []
files_modified: [classes/druid/Druid.lua, classes/druid/selftest.lua]
autonomous: true
requirements: ["prowling-ff-exclusion@quick-260914-u4l"]
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "shouldCastFFDuringWaitWindow 基础排除块在 ooc 臂之后新增第二臂 or clickContext.prowling：prowling==true 的帧直接 return false，等待窗 FF 填充零触发；prowling 为 nil/false 的帧判定路径与改动前逐字节等价（nil 臂短路行为相同），PvE 行为不变"
    - "新 selftest pin 与 WR-01 使用完全相同的帧（含 razor edge 能量条件全满足），唯一差异 prowling=true：WR-01 判 true、新 pin 判 false——这一反义差额只能由新增 prowling 排除臂产生（shouldDoReshift 未 stub，其自身在 cat.lua 的 prowling 排除对旧链同样返回 false 即不触发外层排除，见 Trace）"
    - "新 pin 遵守 CR-01：pcall 内执行，4 个函数 stub + 2 个 own-key shadow 全部在 assert 之前按 rawget 快照 / 引用还原（rawset 恢复 own-key），pcall 失败也不污染登录会话"
    - "classes/druid/Druid.lua 与 classes/druid/selftest.lua 经 Lua 5.0.3 / 5.1.5 / 5.4.7 三解释器 loadfile 编译全通过且 bbcheck 保持 BALANCED（规划时基线：两文件均 BALANCED，必须保持）"
  artifacts:
    - "classes/druid/Druid.lua：shouldCastFFDuringWaitWindow 基础排除块内纯插入 3 行（1 个 or 臂 + 2 行英注释，位于 1173 行 if clickContext.ooc 与 1174 行 isImmune 臂之间）；零删除；其上方 -- 基础排除条件 / Kill-shot 注释与块内其余行字节不动"
    - "classes/druid/selftest.lua：WR-01 块结尾（827 行 end, true)）之后、829 行 -- End of Batch 2 注释之前插入 47 行新 register 块；零删除；WR-01 与其余既有测试一字不动"
  key_links:
    - "新臂 or clickContext.prowling 与 nil 容错：生产上下文总带 prowling（combo.lua 的 clickContext.prowling = player.isProwling）；既有 FF selftests 构造 ctx 不含该字段（nil → falsy → 不排除）仍须全绿——旧 pin 缺字段 + 新 pin 含字段成对钉死 nil 容错"
    - "新 pin 与 WR-01 的同帧镜像（除 prowling=true 外所有 stub/字段/断言结构相同）：true/false 反义即是 prowling 臂的回归差分信号"
---

# PLAN: 潜行帧禁止 FF 等待窗填充（260914-u4l）

## 目标

1. **代码**：在 `classes/druid/Druid.lua` 的 `macroTorch.shouldCastFFDuringWaitWindow` 基础排除块（当前 1173-1178 行）内，`if clickContext.ooc` 之后新增第二臂 `or clickContext.prowling`，臂后跟 2 行英注释。仅此一处改动，函数其余逻辑零变化。
2. **回归 pin**：`classes/druid/selftest.lua` 在 WR-01 FF pin 块结尾（827 行 `end, true)`）之后、829 行 `-- End of Batch 2` 注释之前，插入 48 行（47 行 pin 块 + 1 分隔空行）：与 WR-01 完全同帧、唯一增加 `prowling = true`，断言 `shouldCastFFDuringWaitWindow(ctx)` 返回 false。方向正确型 pin（修复前红 / 修复后绿，本机无 WoW，红名预检归 unrun-verify）。

## 决策记录（为什么在这里修）

FF（Faerie Fire (Feral)）是 hostile instant 法术——潜行中施放它打破隐形并开怪，摧毁 Pounce/Ravage 起手。当前守卫不对称：oocMod 在潜行时跳过（`if not clickContext.prowling or target.isAttackingMe`，combo.lua），keepRake 要求 isFightStarted（cat.lua），但 FF 路径（keepFF → shouldCastFFDuringWaitWindow → safeFF）两条守卫都没有。风险暴露主要在 PvP 潜行等待（能量等待窗带 [15,20]、无 Tiger 时 min=TIGER_E；tick 粒度使 PvE 暴露趋近于零）。**修复位置由用户裁定**：改决策函数 `shouldCastFFDuringWaitWindow`，NOT keepFF/safeFF。

## 锁定约束（违反即失败）

1. **只改两个文件**：`classes/druid/Druid.lua` + `classes/druid/selftest.lua`。`SM_Extend.lua`、`.planning/catAtk-core-principles.md`（untracked，**零文件系统交互**——用户明确裁定 prowling 排除是实现细节不进原则文档）及仓库其余文件零触碰；**禁止运行 build.sh**。
2. **零删除面**：两文件 git diff 删除行合计恰 0 行（纯插入，GATE 2）。Druid.lua 净增恰 3 行（1 臂 + 2 注释）；selftest.lua 净增恰 48 行 = 47 行 pin 块 + 1 行块尾分隔空行（GATE 6）。
3. **插入逐字锁定**：Task 1 的两次 Edit 的 before/after 块逐字执行（Druid.lua 4 空格缩进，selftest.lua tab 缩进）；任何锚点不匹配说明文件漂移，停下报告，不得自行发挥。
4. **不改** `keepFF` / `safeFF` / `oocMod` / `keepRake` / `keepTigerFury`。本 quick 只碰 `shouldCastFFDuringWaitWindow` 的基础排除块与那一个新 selftest。
5. **Lua 5.0 (WoW 1.12) 合规**：新增行无 `#`、无 `goto `、无 `::`；零中文字符；英文注释；LF 行尾（两文件 CR 字节基线已实测为 0，必须保持 0）。
6. **CR-01 stub 纪律**：新 pin 内 pcall 执行、全部 stub / own-key shadow 于 assert 之前按快照还原（rawget 快照 → rawset 恢复；函数 stub 引用赋值还原）；还原顺序与 WR-01 一致。
7. **PvE 零回归**：新臂对 prowling 为 nil/false（即全部既有 PvE 帧）按短路语义完全等价——不新增任何 PvE 可达行为。
8. **提交**：单 commit 恰含上述两个文件（planner 选择单 commit：pin 与代码臂同 commit，不存在测试红名的中间态）；commit message 逐字锁定（见 Task 1）。代码 commit 严禁夹带任何 `.planning` 文件（SUMMARY/STATE 行由 quick 流程 docs commit 收口，与 260914-49l 相同）。

## 背景事实（2026-09-14 实地核对，编辑锚点以此为准，勿另找）

- 工作树 clean（本 quick 起点无未提交改动）。
- `classes/druid/Druid.lua` 锚点（已用 cat -A 实测，4 空格缩进）：1171 = `    -- 基础排除条件`；1172 = `    -- Kill-shot phase pauses all debuff maintenance, including FF fill (rule 9)`；1173 = `    if clickContext.ooc`；1174 = `            or macroTorch.target.isImmune('Faerie Fire (Feral)')`；1175 = shouldDoReshift 臂；1176 = isKillShotOrLastChance 臂 + ` then`；1177 = `        return false`；1178 = `    end`。字面量基线：`or clickContext.prowling` 计数 0（改后 1）；`clickContext.prowling` 计数 2（改后 3，另两处在文件其他位置，勿动）。
- `classes/druid/selftest.lua` 锚点（tab 缩进，已用 cat -A 实测）：766 = WR-01 register 行；825-826 = WR-01 两条 assert；827 = `\tend, true)`；828 = 空行；829 = `\t-- End of Batch 2 — all catAtk principle regression tests complete`（含全角 em-dash）。WR-01 块系 0db2f43（260914-nth）所加。本 quick 插入点：829 行之前。新字面量基线全部为 0（干净插入空间）；既有字面量基线：`prowling = true` = 1（另一测试用，勿动）→ 改后 2；`macroTorch.computeErps = function(clickContext) return 60 end` = 1 → 2；`local origImmune = rawget(macroTorch.target, 'isImmune')` = 1 → 2；`rawset(player, 'mana', savedMana)` = 1 → 2。
- R series / categories 不受影响：WR-01 为独立测试，不属于计数序列，**不存在**需要改的注册计数注释（勿新增任何 `Registration count` 注释）。
- 工具在位（规划时 2026-09-14 实测）：三解释器 `/tmp/luabuild/lua-5.0.3/bin/lua`、`/tmp/luabuild/lua-5.1.5/src/lua`、`/tmp/luabuild/lua-5.4.7/src/lua` 对两文件 loadfile 编译已跑通一次（LOADFILE-OK×3，脚本见 GATE 0）；`/usr/local/bin/node` 在位；`bbcheck.js`（`.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js`）对**这两个文件**基线均为 BALANCED（与本 quick 相关的两个文件均可用全文件门，与 49l 的 core/selftest MISMATCH 情形不同，勿混淆）。
- 行尾基线：两文件 CR 字节 0（全 LF）。

## 前置条件（precondition）

`/tmp/luabuild` 三解释器（5.0.3/bin、5.1.5/src、5.4.7/src）与 `/usr/local/bin/node`、bbcheck.js 在位（规划时已实测全绿）。执行时若缺失，停下报告 unmet precondition，不得降级跳过三解释器验证。

## 任务

### Task 1（唯一代码任务，原子）: Druid.lua 排除臂 + selftest prowling pin + 验证电池 + 单 commit

**文件**: `classes/druid/Druid.lua` + `classes/druid/selftest.lua`（仅此两个）

**(a) Druid.lua 排除臂插入**（一次 Edit，锚点 1173-1174 两行，4 空格缩进）：

```
--- before ---
    if clickContext.ooc
            or macroTorch.target.isImmune('Faerie Fire (Feral)')
--- after  ---
    if clickContext.ooc
            or clickContext.prowling
            -- Stealth must never be broken by FF fill: hostile instant that breaks prowl and opens combat
            -- mirrors keepRake isFightStarted guard and oocMod prowling guard
            or macroTorch.target.isImmune('Faerie Fire (Feral)')
```

语义（写进行动理解，不加码）：新臂紧跟 ooc 臂成为第二行；prowling 帧在 OR 链处短路 return false，永不触达 isImmune / shouldDoReshift / isKillShot / 任何能量算术。nil/false 帧该臂求值为 nil/false，短路行为与改动前相同（PvE 零变化）。既有头注释 `-- 基础排除条件` 与 `-- Kill-shot phase pauses...` 字节不动。

**(b) selftest.lua 新 pin 块插入**（一次 Edit，锚点 825-829 五行，tab 缩进；before 替换为 before+新块，净效果纯插入 48 行 = 47 行 pin 块 + 1 分隔空行）：

```
--- before ---
		assert(ok, "WR-01 errored: " .. tostring(res))
		assert(res, "expected true: essence 0-energy FF fill window (SHRED_E=60)")
	end, true)

	-- End of Batch 2 — all catAtk principle regression tests complete
--- after  ---
		assert(ok, "WR-01 errored: " .. tostring(res))
		assert(res, "expected true: essence 0-energy FF fill window (SHRED_E=60)")
	end, true)

	-- This frame satisfies all WR-01 energy-window conditions (the razor edge), so a true verdict would mean the prowling arm alone was exercised
	macroTorch.SelfTest:register("FF wait window: prowling frame refused (stealth never FF-fills)", function()
		local ctx = {
			ooc = false,
			prowling = true,
			isPseudoInfiniteEnergy = true,
			isBehind = true,
			isRakePresent = true,
			isRipPresent = true,
			isPouncePresent = true,
			SHRED_E = 60,
			CLAW_E = 45,
			RESHIFT_ENERGY = 40,
			TIGER_E = 30,
			isTigerPresent = true,
			isTrivialBattle = false,
			isFastBattleNotPvp = false,
			isImmuneRip = true,
			comboPoints = 1,
		}
		-- Stub discipline mirrors WR-01 (block directly above); all restores
		-- happen BEFORE the asserts (CR-01); shouldDoReshift is NOT stubbed
		local player = macroTorch.player
		local savedBehind = rawget(player, 'isBehindAttackJustFailed')
		player.isBehindAttackJustFailed = false
		local savedMana = rawget(player, 'mana')
		player.mana = 0
		local origKillShot = macroTorch.isKillShotOrLastChance
		macroTorch.isKillShotOrLastChance = function(clickContext) return false end
		local origErps = macroTorch.computeErps
		macroTorch.computeErps = function(clickContext) return 60 end
		local origImmune = rawget(macroTorch.target, 'isImmune')
		macroTorch.target.isImmune = function(spellName) return false end
		local origSpellExist = macroTorch.isSpellExist
		macroTorch.isSpellExist = function(spellName, bookType) return true end
		local ok, res = pcall(function()
			return macroTorch.shouldCastFFDuringWaitWindow(ctx) == false
		end)
		rawset(player, 'isBehindAttackJustFailed', savedBehind)
		rawset(player, 'mana', savedMana)
		macroTorch.isKillShotOrLastChance = origKillShot
		macroTorch.computeErps = origErps
		rawset(macroTorch.target, 'isImmune', origImmune)
		macroTorch.isSpellExist = origSpellExist
		assert(ok, "prowling pin errored: " .. tostring(res))
		assert(res, "expected false: prowling frame must never FF-fill")
	end, true)

	-- End of Batch 2 — all catAtk principle regression tests complete
```

以上 after 块内新增内容为：47 行锁定 pin 块（L1 注释行至 `end, true)`）+ 块尾 1 空行分隔（共净增 48 行）。注册名、断言消息逐字锁定：
- register 名：`"FF wait window: prowling frame refused (stealth never FF-fills)"`
- crash 断言：`assert(ok, "prowling pin errored: " .. tostring(res))`
- 判定断言：`assert(res, "expected false: prowling frame must never FF-fill")`

**执行路径 Trace（锁定推理）**：

- **修复后为何绿**：① ooc=false 未短路 → OR 链第二臂 `clickContext.prowling`==true → 整个排除块 return false，isImmune / shouldDoReshift / isKillShot / 能量算术全被跳过；② pcall 体返回 `false == false` → true，res=true → 两条 assert 均过；③ 还原先于 assert，pcall 失败也不污染会话。
- **修复前为何红**（防御方向论证，纯 Trace）：无 prowling 臂时 OR 链继续：isImmune stub false → shouldDoReshift **未 stub**，实测 cat.lua 的 shouldDoReshift 自身含 `clickContext.prowling` 排除（cat.lua 该函数第 14 行附近 `... or clickContext.prowling or ...`）返回 false（=无需 reshift，不触发外层排除）→ isKillShot stub false → 进入窗口算术 → 复用 WR-01 razor edge 帧（erps=60、SHRED_E=60、mana=0、isBehind=true、isPseudoInfiniteEnergy=true → waitSeconds=60/60=1.0 ≥ 1.0）→ 返回 true → pcall 体 `true == false` → res=false → `assert(res, ...)` 红名。即该 pin 在臂缺失时必红，方向正确。
- **nil 容错成对论证**：生产路径 combo.lua 恒写 `clickContext.prowling = player.isProwling`；既有 FF 测试（含 WR-01）构造 ctx 不含 prowling → nil → falsy → 不排除 → 必须继续判 true/原判（旧 pin 缺字段侧）；新 pin 用 `prowling = true` 钉死排除侧。两侧合计覆盖 nil 容忍契约。
- 本机无 WoW 环境：红/绿终验由用户 /mt 电池完成（unrun-verify），本地只做三解释器编译 + 静态门。

**验证电池（顺序执行，任一失败即停并报告；在仓库根 /home/admin/workspace/macro-torch 执行）：**

```bash
cd /home/admin/workspace/macro-torch

# GATE 0: 三解释器 loadfile 编译两文件（不执行，无 WoW 环境依赖）
for B in /tmp/luabuild/lua-5.0.3/bin/lua /tmp/luabuild/lua-5.1.5/src/lua /tmp/luabuild/lua-5.4.7/src/lua; do
  "$B" -e 'assert(loadfile("/home/admin/workspace/macro-torch/classes/druid/Druid.lua")); assert(loadfile("/home/admin/workspace/macro-torch/classes/druid/selftest.lua"))' || { echo "LOADFILE FAIL: $B"; exit 1; }
  echo "LOADFILE-OK: $B"
done

# GATE 1: 结构 grep 绝对计数（-F 全字面；规划时基线见 背景事实）
test "$(grep -cF 'or clickContext.prowling' classes/druid/Druid.lua)" = "1" || { echo "FAIL: prowling arm count"; exit 1; }
test "$(grep -cF 'Stealth must never be broken by FF fill' classes/druid/Druid.lua)" = "1" || { echo "FAIL: comment L1"; exit 1; }
test "$(grep -cF 'mirrors keepRake isFightStarted guard and oocMod prowling guard' classes/druid/Druid.lua)" = "1" || { echo "FAIL: comment L2"; exit 1; }
test "$(grep -cF "or macroTorch.target.isImmune('Faerie Fire (Feral)')" classes/druid/Druid.lua)" = "1" || { echo "FAIL: isImmune arm invariance"; exit 1; }
test "$(grep -cF 'FF wait window: prowling frame refused (stealth never FF-fills)' classes/druid/selftest.lua)" = "1" || { echo "FAIL: register name"; exit 1; }
test "$(grep -cF 'a true verdict would mean the prowling arm alone was exercised' classes/druid/selftest.lua)" = "1" || { echo "FAIL: razor-edge note"; exit 1; }
test "$(grep -cF 'return macroTorch.shouldCastFFDuringWaitWindow(ctx) == false' classes/druid/selftest.lua)" = "1" || { echo "FAIL: pcall body"; exit 1; }
test "$(grep -cF 'prowling pin errored: ' classes/druid/selftest.lua)" = "1" || { echo "FAIL: crash assert"; exit 1; }
test "$(grep -cF 'expected false: prowling frame must never FF-fill' classes/druid/selftest.lua)" = "1" || { echo "FAIL: verdict assert"; exit 1; }
test "$(grep -cF 'prowling = true' classes/druid/selftest.lua)" = "2" || { echo "FAIL: prowling=true count (baseline 1 + new 1)"; exit 1; }
test "$(grep -cF 'macroTorch.computeErps = function(clickContext) return 60 end' classes/druid/selftest.lua)" = "2" || { echo "FAIL: computeErps count"; exit 1; }
test "$(grep -cF "local origImmune = rawget(macroTorch.target, 'isImmune')" classes/druid/selftest.lua)" = "2" || { echo "FAIL: origImmune count"; exit 1; }
test "$(grep -cF "rawset(player, 'mana', savedMana)" classes/druid/selftest.lua)" = "2" || { echo "FAIL: mana restore count"; exit 1; }
test "$(grep -cF 'WR-01 FF wait window: essence 0-energy edge accepted (SHRED_E=60)' classes/druid/selftest.lua)" = "1" || { echo "FAIL: WR-01 register invariance"; exit 1; }
test "$(grep -cF 'expected true: essence 0-energy FF fill window (SHRED_E=60)' classes/druid/selftest.lua)" = "1" || { echo "FAIL: WR-01 assert invariance"; exit 1; }
test "$(grep -cF -- '-- End of Batch 2' classes/druid/selftest.lua)" = "1" || { echo "FAIL: End of Batch 2 invariance"; exit 1; }
echo "GATE 1 OK"

# GATE 2: 删除面——两文件合计 0 行删除（纯插入）
test "$(git diff -U0 HEAD -- classes/druid/Druid.lua classes/druid/selftest.lua | grep '^-[^-]' | wc -l)" = "0" || { echo "FAIL: deletion surface"; exit 1; }

# GATE 3: hunk 范围——Druid.lua 全部 @@ 落在 1160-1189；selftest.lua 落在 820-869
test -z "$(git diff -U0 HEAD -- classes/druid/Druid.lua | grep '^@@' | grep -vE '^@@ -(11[6-8][0-9]),' || true)" || { echo "FAIL: Druid hunk range"; exit 1; }
test -z "$(git diff -U0 HEAD -- classes/druid/selftest.lua | grep '^@@' | grep -vE '^@@ -(8[2-6][0-9]),' || true)" || { echo "FAIL: selftest hunk range"; exit 1; }
echo "GATE 2-3 OK"

# GATE 4: bbcheck 全文件门——两文件基线 BALANCED（规划时实测），必须保持
node .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js classes/druid/Druid.lua classes/druid/selftest.lua | tee /dev/stderr | grep -cF 'BALANCED' | grep -q '^2$' || { echo "FAIL: bbcheck"; exit 1; }

# GATE 5: 新增行 token / CJK / 行尾门 + git diff --check
test "$(git diff -U0 HEAD -- classes/druid/Druid.lua classes/druid/selftest.lua | grep '^+' | grep -v '^+++' | grep -cE '#|goto |::' || true)" = "0" || { echo "FAIL: 5.0 token"; exit 1; }
test "$(git diff -U0 HEAD -- classes/druid/Druid.lua classes/druid/selftest.lua | grep '^+' | grep -v '^+++' | LC_ALL=C grep -cP '[^\x00-\x7F]' || true)" = "0" || { echo "FAIL: non-ASCII bytes (CJK gate)"; exit 1; }
test "$(grep -l $'\r' classes/druid/Druid.lua classes/druid/selftest.lua | wc -l)" = "0" || { echo "FAIL: CR bytes"; exit 1; }
git diff --check || { echo "FAIL: diff check"; exit 1; }

# GATE 6: 新增行总数 + 变更面唯一
test "$(git diff -U0 HEAD -- classes/druid/Druid.lua | grep '^+[^+]' | wc -l)" = "3" || { echo "FAIL: Druid added-line count"; exit 1; }
test "$(git diff -U0 HEAD -- classes/druid/selftest.lua | grep '^+[^+]' | wc -l)" = "48" || { echo "FAIL: selftest added-line count"; exit 1; }
test "$(git status --porcelain | grep -v '^ M classes/druid/Druid.lua$' | grep -v '^ M classes/druid/selftest.lua$' | wc -l)" = "0" || { echo "FAIL: unexpected files changed"; git status --porcelain; exit 1; }
test ! -e SM_Extend.lua || { echo "FAIL: SM_Extend.lua exists (build.sh must not have run)"; exit 1; }
test -z "$(git status --porcelain -- .planning/catAtk-core-principles.md)" || { echo "FAIL: principles doc touched"; exit 1; }

# GATE 7: Druid.lua 最终形态逐行钉死（4 空格缩进；1174-1181 行）
test "$(sed -n '1173p' classes/druid/Druid.lua)" = '    if clickContext.ooc' || { echo "FAIL: 1173"; exit 1; }
test "$(sed -n '1174p' classes/druid/Druid.lua)" = '            or clickContext.prowling' || { echo "FAIL: 1174 prowling arm"; exit 1; }
test "$(sed -n '1175p' classes/druid/Druid.lua)" = '            -- Stealth must never be broken by FF fill: hostile instant that breaks prowl and opens combat' || { echo "FAIL: 1175 comment L1"; exit 1; }
test "$(sed -n '1176p' classes/druid/Druid.lua)" = '            -- mirrors keepRake isFightStarted guard and oocMod prowling guard' || { echo "FAIL: 1176 comment L2"; exit 1; }
test "$(sed -n '1177p' classes/druid/Druid.lua)" = "            or macroTorch.target.isImmune('Faerie Fire (Feral)')" || { echo "FAIL: 1177 isImmune arm"; exit 1; }
test "$(sed -n '1178p' classes/druid/Druid.lua)" = '            or macroTorch.shouldDoReshift(clickContext)' || { echo "FAIL: 1178"; exit 1; }
test "$(sed -n '1179p' classes/druid/Druid.lua)" = '            or macroTorch.isKillShotOrLastChance(clickContext) then' || { echo "FAIL: 1179"; exit 1; }
test "$(sed -n '1180p' classes/druid/Druid.lua)" = '        return false' || { echo "FAIL: 1180"; exit 1; }
test "$(sed -n '1181p' classes/druid/Druid.lua)" = '    end' || { echo "FAIL: 1181"; exit 1; }
test "$(sed -n '1171p' classes/druid/Druid.lua)" = '    -- 基础排除条件' || { echo "FAIL: 1171 header invariant"; exit 1; }
test "$(sed -n '1172p' classes/druid/Druid.lua)" = '    -- Kill-shot phase pauses all debuff maintenance, including FF fill (rule 9)' || { echo "FAIL: 1172 header invariant"; exit 1; }
echo "ALL GATES OK"
```

**完成标准**：
- Druid.lua 基础排除块 3 行纯插入（臂 + 2 注释，位置 1174-1176），其余字节不动；selftest.lua 48 行纯插入（47 行新 register 块 + 1 分隔空行，位于 `-- End of Batch 2` 之前），WR-01 与其余测试一字不动。
- 全部 gates 输出 ALL GATES OK；三解释器各打印 LOADFILE-OK；bbcheck 两文件 BALANCED。
- 原子 commit 只含这两个文件，commit message 逐字如下（英文）：

```bash
git add classes/druid/Druid.lua classes/druid/selftest.lua
git commit -m "fix(260914-u4l): exclude prowling frames from FF wait-window fill; add stealth-refusal selftest pin"
```

**验证提交**：
- `git log -1 --format=%s` 输出 `fix(260914-u4l): exclude prowling frames from FF wait-window fill; add stealth-refusal selftest pin`
- `git show --stat HEAD` 仅含 `classes/druid/Druid.lua`（3 insertions）与 `classes/druid/selftest.lua`（48 insertions），两文件 0 deletions。

### Task 2: 提交后全量电池复跑 + 变更面汇总

- **操作**:
  1. `git status --porcelain` 输出为空（commit 后无残留）。
  2. 对已提交状态复跑 GATE 0（三解释器 loadfile）与 GATE 4（bbcheck），确认 HEAD 版本编译与平衡全绿。
  3. `git diff HEAD~1 --check` 无输出；`git diff HEAD~1 -U0 | grep '^-[^-]' | wc -l` 输出 `0`（跨两文件零删除复核）。
  4. `git diff HEAD~1 -U0 -- classes/druid/Druid.lua classes/druid/selftest.lua | grep '^+[^+]' | wc -l` 输出 `51`（3 + 48）。
  5. `git log -1 --format=%s` 复核 commit message 逐字一致（Task 1 的验证提交已锁定）。
  6. 编写 `/home/admin/workspace/macro-torch/.planning/quick/260914-u4l-ff-shouldcastffduringwaitwindow-prowling/260914-u4l-SUMMARY.md`，结构照 260913-46s 先例：任务完成 / 偏差 / Unrun Verification / Threat Flags / Self-Check。**SUMMARY 与 STATE.md 表行不加进代码 commit**（quick 流程 docs commit 收口，与 260914-49l 相同）。
- **验证**:
  - `git status --porcelain | wc -l` 输出 `0`
  - 上述第 3/4 步计数输出 `0` 与 `50`
  - SUMMARY.md 存在于上述绝对路径

## 失败即回退

- 任一 gate 失败 → 不提交，修正至通过；若 `git status --porcelain` 出现预期外文件 → 立即 `git checkout -- <file>` 撤销并排查（`.planning/catAtk-core-principles.md` 尤其一字节都不能动，该文件 untracked 无 checkout 兜底）。
- 锚点不匹配（Edit 失败）→ 说明文件已漂移，停止并报告实际内容，禁止自行发挥。
- 全程禁止 build.sh；SM_Extend.lua 出现即失败。

## 威胁模型（STRIDE 简表）

| Trust Boundary | Description |
|----------|-------------|
| 无新边界 | 本改动是决策函数内一个只读条件臂：只读 clickContext.prowling，不写任何状态、不接外部输入、不新增调用路径 |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260914-u4l-01 | Tampering | selftest 6 个 stub / own-key shadow 泄漏污染实机会话（isKillShotOrLastChance / computeErps / target.isImmune / isSpellExist / player.mana / isBehindAttackJustFailed 未还原） | medium | mitigate | CR-01：pcall 内执行，rawget 快照 + rawset / 引用赋值还原，全部还原先于两条 assert；GATE 1 钉死 capture/restore 字面量计数 |
| T-260914-u4l-02 | DoS | 新臂误伤非潜行帧（若生产上下文在 PvE 曾错误置真 prowling，FF 填充全停） | low | accept | 生产赋值链为 combo.lua `player.isProwling`（非潜行恒 false/nil）；nil/false 臂短路语义与改动前逐字节等价；新旧 pin 一含一不含字段成对覆盖 nil 容错 |
| T-260914-u4l-03 | DoS | 断言在登录自测抛错红名 | low | accept | 失败仅表现自测红名提示，不进战斗路径 |
| T-260914-u4l-04 | Escalation | 经注册名 / 断言文案注入执行 | low | accept | 全部静态字面量，register 仅登记函数，无 eval 路径 |

包管理器安装：本 quick 零 npm/pip/cargo 安装，package legitimacy 门与 T-SC 行不适用。

## unrun-verify 备案（写进 SUMMARY）

本机无 WoW 客户端。用户 Windows+Cygwin 重建 `/mt` 后：① PvP 潜行等待期间（能量等待窗带 [15,20]、无 Tiger 时 min=TIGER_E）应不再发出 FF 填充，隐形保持、Pounce/Ravage 起手可用；② selftest 中 FF 相关 pin 全绿：WR-01 仍判 true（无 prowling 字段 → nil 容忍路径），新 pin "FF wait window: prowling frame refused (stealth never FF-fills)" 判 false——若新 pin 红名说明 prowling 臂失效或 stub 断链；③ PvE 行为无变化（旧 pin 全绿即证明）。方向预证：臂缺失时新 pin 必红（Trace 已锁），修复后应绿。

## 偏差模板

以下任一情况构成偏差，必须停在原地、回报用户裁决，不得继续：
1. 变更文件超出 `classes/druid/Druid.lua` 与 `classes/druid/selftest.lua` 两个文件。
2. 任一文件出现删除行（GATE 2 非零）。
3. 新增行数不等于 3（Druid）/ 48（selftest = 47 行 pin + 1 分隔空行），或新增内容偏离锁定块（改字、加减注释、移动臂位置）。
4. lock 的 keepFF/safeFF/oocMod/keepRake/keepTigerFury 被触碰。
5. build.sh 被运行、SM_Extend.lua 出现、`.planning/catAtk-core-principles.md` 产生任何变动。
6. commit message 与锁定文案不一致，或代码 commit 夹带 .planning 文件。
7. 三解释器 / bbcheck / 任一 gate 无法按原样执行而被降级跳过。
已发生的偏差须在 SUMMARY.md `## 偏差` 一节逐条记录（含用户裁决引用），未发生则写「无」。

## 输出

执行完成后输出 `/home/admin/workspace/macro-torch/.planning/quick/260914-u4l-ff-shouldcastffduringwaitwindow-prowling/260914-u4l-SUMMARY.md`（结构见 Task 2）；STATE.md Quick Tasks 表行由 quick 流程 docs commit 收口，代码 commit 中不得夹带任何 .planning 文件。