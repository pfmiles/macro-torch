---
quick_id: 260914-1t0
slug: fix-quick-260914-0ql-code-review-wr-01-t
description: "修复 quick 260914-0ql 代码评审 WR-01：P 系列测试 'P: cp5Bite still bites when no discharge condition matches'（260825-vp9 泄能洞 A/B liveness pin）未 stub rakeLeft，新 1.3s rake 门在登录自测以真实 rakeLeft=0 触发，静默架空了测试刻意保留的真实 energyDischargeBeforeBite no-condition fall-through 路径。同时落实 0ql PLAN 备案项：新增一条 'P: cp5Bite skips discharge to renew rake when rake <=1.3s' pin，并将 Category P 计数注释 8→9。单文件 core/selftest.lua；修复为纯插入 3 行；唯一删除为计数注释行；禁止运行 build.sh、零触碰 classes 与 SM_Extend.lua"
date: 2026-09-14
phase: quick-260914-1t0
plan: 260914-1t0
type: execute
wave: 1
depends_on: []
files_modified: [core/selftest.lua]
autonomous: true
requirements: ["WR-01@quick-260914-0ql"]
estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "P-03（260825-vp9 pin）恢复调用真实 energyDischargeBeforeBite：stub rakeLeft=9 使 1.3s 门不触发，mana=50 布点重归活跃，泄能洞 A/B fall-through 回归 pin 重新有效"
    - "新测试证明 rake 在场且 rakeLeft<=1.3 时 cp5Bite 对 energyDischargeBeforeBite 零调用（dischargeCalled==false）且咬击照常（biteCalled==true）"
    - "两个受影响测试均遵守 CR-01：所有 stub 在任何 assert 触发之前按 orig 引用还原，pcall 失败也不会污染会话"
    - "core/selftest.lua 经 Lua 5.0.3 / 5.1.5 / 5.4.7 三解释器 loadfile 编译全部通过"
  artifacts:
    - "/home/admin/workspace/macro-torch/core/selftest.lua（3 行 P-03 插入 + 新 P 测试注册 + 9 计数注释；其余字节不动）"
  key_links:
    - "cat.lua:141 rake 门对 macroTorch.rakeLeft 的求值是新增依赖 key link：P-03 不 stub 它则登录环境真实 rakeLeft=0 短路掉真实泄能路径"
    - "新测试 pin 住 shouldDischarge=false 直接落位 safeBite/readyBite 的接线（跳过 energyDischargeBeforeBite 整块）"
---

# PLAN: 修复 260914-0ql 评审 WR-01 + 新增 1.3s rake 续期自测 pin（core/selftest.lua 单文件）

## 目标

1. **WR-01 修复**：为 `core/selftest.lua` 的 P-03（"P: cp5Bite still bites when no discharge condition matches"，260825-vp9 liveness pin，当前 864-904 行）补 `macroTorch.rakeLeft` 的 capture/stub/restore 三行，使新 rake 门（cat.lua:141，`rakeLeft(clickContext) <= 1.3`）在登录自测不触发，测试刻意保留的**真实** `energyDischargeBeforeBite` no-condition fall-through 路径恢复被覆盖。
2. **新 pin**：按 260914-0ql PLAN「需与用户讨论项」备案口径，新增 "P: cp5Bite skips discharge to renew rake when rake <=1.3s"：stub isRakePresent→true、rakeLeft→1.0、ripLeft→4、energyDischargeBeforeBite（记录调用）、safeBite/readyBite（记录咬击），断言咬击发生且泄能 stub 零调用。
3. **计数注释**：906 行 Category P 注册计数注释由 "adds 8 tests" 更新为 "adds 9 tests"（补 `1 in quick 260914-1t0`）。

## 背景事实（2026-09-14 实地核对，编辑锚点以此为准，勿另找）

- 工作树 clean（本 quick 起点无未提交改动）。
- `core/selftest.lua` 行锚点（今日 Read 核对）：864 = P-03 register 行；866-871 capture 区；872-878 stub 区；896-901 restore 区（897 = `macroTorch.ripLeft = origRipLeft`）；902-903 断言；904 = `end, true)`；906 = 计数注释（当前 "adds 8 tests"）。822-854 为 P-02（"defers the bite when a discharge is attempted"），其与 P-03 之外的区域**零触碰**。
- `grep` 实测：全文无任何既有 `macroTorch.rakeLeft` / `origRakeLeft` 引用（干净插入空间）；`local origIsRakePresent` 唯一在 868 行；`macroTorch.ripLeft = function(clickContext) return 4 end` 唯一在 873 行；`macroTorch.ripLeft = origRipLeft` 唯一在 897 行。
- 工具在位（规划时实测）：Lua 三解释器 `/tmp/luabuild/lua-5.0.3/bin/lua`、`/tmp/luabuild/lua-5.1.5/src/lua`、`/tmp/luabuild/lua-5.4.7/src/lua` 对当前 selftest.lua `loadfile` 编译均已跑通（LOADFILE-OK×3）；Node `/usr/local/bin/node`；本 quick 的括号平衡门用 260831-24c T2 先例的 diff 新增行 node 脚本——**注意** `bbcheck.js` 对 selftest.lua 全文件基线即输出 MISMATCH（既有局限，260831-24c SUMMARY 已备案），不得以全文件 bbcheck 作为门，亦不得运行 build.sh。
- 行尾基线：CR 字节计数 0（selftest.lua 全 LF，插入行必须保持 LF）。
- WR-01 机理（260914-0ql-REVIEW.md WR-01 段）：P-03 stub 了 isRipPresent→true、ripLeft→4、isRakePresent→true 但没 stub rakeLeft；登录环境无目标 → 真实 `rakeLeft` 返回 0 → `0 <= 1.3` 成立 → `shouldDischarge=false` → `if shouldDischarge then` 整块跳过 → 真实排水函数不再被调用，测试虽绿但守护的 fall-through 回归 pin 被静默架空。

## 锁定约束（违反即失败）

1. **只改一个文件**：`core/selftest.lua`。`classes/druid/cat.lua`、`classes/druid/Druid.lua`、`SM_Extend.lua` 及仓库其余文件零触碰；**禁止运行任何构建脚本**（build.sh 等）。Commit 只含这一个代码文件。
2. **删除面**：git diff 删除行必须恰 1 行，且就是旧的计数注释行（"adds 8 tests"）。三处 P-03 修复与新测试块全部为纯插入。
3. **插入逐字锁定**（Task 1 的四处 Edit 的 before/after 块逐字执行，缩进 4 空格；任何锚点不匹配说明文件漂移，停下报告，不得自行发挥）。
4. **Lua 5.0(WoW 1.12) 合规**：新增行无 `#`、无 `goto`、无 `::`；零中文字符；新增**字符串字面量**内不得含相邻连字符对（`--`）与裸双引号字节（diff 平衡门 strip 顺序所致，260913-46s 先例门规）；注释与断言文案一律英文；行尾 LF。
5. **CR-01 stub 纪律**：两处测试均在 pcall 内执行、**还原先于所有 assert**；还原值引用 capture 时保存的 orig 局部变量。
6. **计数真相**：Category P 总数 9 = 当前 8（2 in 26-01 + 4 in 26-02 + t86 + vp9）+ 本 quick 1；注释必须列出全部 5 项。

## 任务

### Task 1（唯一任务，原子）: P-03 补 rakeLeft stub 三行 + 新增 rake 续期 pin + 计数注释 8→9 + 单文件 commit

**文件**: `core/selftest.lua`（仅此一个）

**执行步骤（4 次精确 Edit，顺序固定）：**

**(a) P-03 capture 插入**（锚点 868 行上下文）：

    --- before ---
    local origIsRakePresent = macroTorch.isRakePresent
    local origSafeBite = macroTorch.safeBite
    --- after ---
    local origIsRakePresent = macroTorch.isRakePresent
    local origRakeLeft = macroTorch.rakeLeft
    local origSafeBite = macroTorch.safeBite

**(b) P-03 stub 插入**（锚点 873-874 行上下文）：

    --- before ---
    macroTorch.ripLeft = function(clickContext) return 4 end
    macroTorch.isRakePresent = function(clickContext) return true end
    --- after ---
    macroTorch.ripLeft = function(clickContext) return 4 end
    macroTorch.rakeLeft = function(clickContext) return 9 end
    macroTorch.isRakePresent = function(clickContext) return true end

**(c) P-03 restore 插入**（锚点 897-898 行上下文）：

    --- before ---
    macroTorch.ripLeft = origRipLeft
    macroTorch.isRakePresent = origIsRakePresent
    --- after ---
    macroTorch.ripLeft = origRipLeft
    macroTorch.rakeLeft = origRakeLeft
    macroTorch.isRakePresent = origIsRakePresent

**(d) 新测试块插入 + 计数注释更新**（锚点 902-906 行，一次 Edit 完成）：

    --- before ---
    assert(pcallRes, "cp5Bite liveness test pcall failed")
    assert(ok, 'cp5Bite did not bite when no discharge condition matched')
end, true)

-- Registration count: Category P adds 8 tests (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86, 1 in quick 260825-vp9)
    --- after ---
    assert(pcallRes, "cp5Bite liveness test pcall failed")
    assert(ok, 'cp5Bite did not bite when no discharge condition matched')
end, true)

-- Quick 260914-1t0: pins the 1.3s Rake renewal gate added in quick 260914-0ql.
-- With Rake present and rakeLeft <= 1.3s, cp5Bite must skip the discharge
-- attempt entirely (the discharge stub must never be called) and go straight
-- to the bite so Ferocious Bite lands in time to renew Rake. Rip is pinned
-- fresh (ripLeft 4 > 2.3) so the 2.3s Rip gate stays open; only the Rake gate
-- can fire. Stub/restore style mirrors the P-06/P-03 pattern (CR-01: restore
-- BEFORE asserts inside a pcall guard) so no real spell fires in the login
-- self-test. Registered as the 9th Category P test.
macroTorch.SelfTest:register("P: cp5Bite skips discharge to renew rake when rake <=1.3s", function()
    if UnitClass('player') ~= 'Druid' then return end
    local origIsRipPresent = macroTorch.isRipPresent
    local origRipLeft = macroTorch.ripLeft
    local origIsRakePresent = macroTorch.isRakePresent
    local origRakeLeft = macroTorch.rakeLeft
    local origDischarge = macroTorch.energyDischargeBeforeBite
    local origSafeBite = macroTorch.safeBite
    local origReadyBite = macroTorch.readyBite
    macroTorch.isRipPresent = function(clickContext) return true end
    macroTorch.ripLeft = function(clickContext) return 4 end
    macroTorch.isRakePresent = function(clickContext) return true end
    macroTorch.rakeLeft = function(clickContext) return 1.0 end
    local biteCalled = false
    local dischargeCalled = false
    macroTorch.safeBite = function(clickContext) biteCalled = true end
    macroTorch.readyBite = function(clickContext) biteCalled = true end
    macroTorch.energyDischargeBeforeBite = function(clickContext)
        dischargeCalled = true
        return false
    end
    local ok, pcallRes = true, true
    pcallRes = pcall(function()
        local ctx = {
            comboPoints = 5,
            isImmuneRip = false,
            ooc = false,
            isPseudoInfiniteEnergy = false
        }
        biteCalled = false
        dischargeCalled = false
        macroTorch.cp5Bite(ctx)
        ok = (biteCalled == true) and (dischargeCalled == false)
    end)
    macroTorch.isRipPresent = origIsRipPresent
    macroTorch.ripLeft = origRipLeft
    macroTorch.isRakePresent = origIsRakePresent
    macroTorch.rakeLeft = origRakeLeft
    macroTorch.safeBite = origSafeBite
    macroTorch.readyBite = origReadyBite
    macroTorch.energyDischargeBeforeBite = origDischarge
    assert(pcallRes, "cp5Bite rake-renewal test pcall failed")
    assert(ok, 'cp5Bite discharged or did not bite when rake <=1.3s')
end, true)

-- Registration count: Category P adds 9 tests (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86, 1 in quick 260825-vp9, 1 in quick 260914-1t0)

**执行路径 Trace（锁进推理，vp9 先例口径）：**

P-03 修复后为何通过：① 入口门槛经 isRipPresent stub（true）打开，`or` 短路使真实 isFastBattleNotPvp 不被求值；② shouldDischarge 保持 true（isPseudoInfiniteEnergy=false，rip 门 `4 <= 2.3` 为 false，**rake 门 `9 <= 1.3` 为 false**）；③ 真实 energyDischargeBeforeBite 运行：regularAttack 三门全不中（ooc=false；mana(50) < 80 < 95；isBehind=false），rake 分支被 `not isRakePresent(false)` 短路 → 返回 false，无任何真实施法；④ 落回不变选择：ooc=false → safeBite stub → biteCalled=true → ok；⑤ rawset 还原删除 mana own key（origMana=nil），活 UnitMana accessor 继续有效——泄能洞 A/B pin 恢复效力。

新测试为何通过：① 同一入口门槛经 isRipPresent stub 打开；② rip 门 `4 <= 2.3` false 不触发；③ **rake 门 `1.0 <= 1.3` true 触发 → shouldDischarge=false**；④ `if shouldDischarge then` 跳过，discharge stub 零调用（dischargeCalled 保持 false）；⑤ ooc=false → safeBite stub → biteCalled=true → ok=true。ctx 不含 BITE_E/CLAW_E/mana 字段无妨——真实排水函数已被 stub 替换，这些字段无任何消费者。

**验证 gates（顺序执行，任一失败即停并报告）：**

```bash
cd /home/admin/workspace/macro-torch

# GATE 0: 三解释器 loadfile 编译（不执行，无 WoW 环境依赖）
for B in /tmp/luabuild/lua-5.0.3/bin/lua /tmp/luabuild/lua-5.1.5/src/lua /tmp/luabuild/lua-5.4.7/src/lua; do
  "$B" -e 'assert(loadfile("/home/admin/workspace/macro-torch/core/selftest.lua"))' || { echo "LOADFILE FAIL: $B"; exit 1; }
  echo "LOADFILE-OK: $B"
done

# GATE 1: 结构 grep 计数（-F 全字面，comma 与括号均字面匹配）
test "$(grep -cF 'macroTorch.rakeLeft = function(clickContext) return 9 end' core/selftest.lua)" = "1" || { echo "FAIL: P-03 rakeLeft=9 stub"; exit 1; }
test "$(grep -cF 'macroTorch.rakeLeft = function(clickContext) return 1.0 end' core/selftest.lua)" = "1" || { echo "FAIL: new-test rakeLeft=1.0 stub"; exit 1; }
test "$(grep -cF 'local origRakeLeft = macroTorch.rakeLeft' core/selftest.lua)" = "2" || { echo "FAIL: origRakeLeft captures"; exit 1; }
test "$(grep -cF 'macroTorch.rakeLeft = origRakeLeft' core/selftest.lua)" = "2" || { echo "FAIL: rakeLeft restores"; exit 1; }
test "$(grep -cF 'P: cp5Bite skips discharge to renew rake when rake <=1.3s' core/selftest.lua)" = "1" || { echo "FAIL: register name"; exit 1; }
test "$(grep -cF 'Category P adds 9 tests' core/selftest.lua)" = "1" || { echo "FAIL: count comment 9"; exit 1; }
test "$(grep -cF 'adds 8 tests' core/selftest.lua)" = "0" || { echo "FAIL: old count still present"; exit 1; }
echo "GATE 1 OK"

# GATE 2: 删除面恰 1 行且为旧计数注释
test "$(git diff -U0 HEAD -- core/selftest.lua | grep '^-' | grep -v '^---' | wc -l)" = "1" || { echo "FAIL: deletion count"; exit 1; }
test "$(git diff -U0 HEAD -- core/selftest.lua | grep '^-' | grep -v '^---' | grep -cF 'adds 8 tests')" = "1" || { echo "FAIL: deleted line not old count comment"; exit 1; }

# GATE 3: hunk 范围——所有 @@ 行首必须落在 864-999 区间（822-854 的 P-02 等区域零触碰）
test -z "$(git diff -U0 HEAD -- core/selftest.lua | grep '^@@' | grep -vE '^@@ -8[6-9][0-9],|^@@ -9[0-9][0-9],')" || { echo "FAIL: hunk out of allowed range"; exit 1; }
echo "GATE 2-3 OK"

# GATE 4: diff 新增行括号平衡（260831-24c T2 先例脚本，bbcheck 整文件 MISMATCH 属既有局限，勿用）
git diff -U0 HEAD -- core/selftest.lua | node -e 'let out="";process.stdin.on("data",d=>out+=d);process.stdin.on("end",()=>{let s=out.split("\n").filter(l=>l[0]==="+"&&l[1]!=="+").map(l=>l.slice(1)).join("\n");s=s.replace(/--\[\[[\s\S]*?\]\]/g," ").replace(/--[^\n]*/g," ").replace(/\"(?:\\.|[^\"\\])*\"/g," \"s\" ").replace(/\x27(?:\\.|[^\x27\\])*\x27/g," \"s\" ");const st=[];let ok=true;for(const c of s){if(c==="["||c==="("||c==="{"){st.push(c)}else if(c==="]"||c===")"||c==="}"){const p=st.pop();if((c==="]"&&p!=="[")||(c===")"&&p!=="(")||(c==="}"&&p!=="{")){ok=false;break}}}if(!ok||st.length!==0){console.log("T4 DIFF BALANCE: MISMATCH");process.exit(1)}console.log("T4 DIFF BALANCE: OK")});' || { echo "FAIL: diff balance"; exit 1; }

# GATE 5: 新增行 token / CJK / 行尾门 + git diff --check
test "$(git diff -U0 HEAD -- core/selftest.lua | grep '^+' | grep -v '^+++' | grep -cE '#|goto |::' || true)" = "0" || { echo "FAIL: 5.0 token"; exit 1; }
test "$(git diff -U0 HEAD -- core/selftest.lua | grep '^+' | grep -v '^+++' | grep -c '[一-龥]' || true)" = "0" || { echo "FAIL: CJK"; exit 1; }
test "$(grep -c $'\r' core/selftest.lua || true)" = "0" || { echo "FAIL: CR bytes"; exit 1; }
git diff --check || { echo "FAIL: diff check"; exit 1; }
echo "GATE 4-5 OK"

# GATE 6: 变更面唯一
test "$(git status --porcelain | grep -v '^ M core/selftest.lua$' | wc -l)" = "0" || { echo "FAIL: unexpected files changed"; git status --porcelain; exit 1; }
echo "ALL GATES OK"
```

**完成标准**：
- P-03 含 3 行 rakeLeft 插入（capture 9 行序位 / stub 684 段位 / restore 紧跟 ripLeft 还原），其余行均未被 diff 触及；新测试注册于 904 行之后、计数注释之前；计数注释含 `adds 9 tests` 与 `quick 260914-1t0`。
- 全部门输出 ALL GATES OK；三解释器各打印 LOADFILE-OK；T4 DIFF BALANCE: OK。
- 原子 commit 只含 core/selftest.lua，commit message 逐字如下（英文）：

```
fix(selftest): stub rakeLeft to restore vp9 no-discharge liveness pin; add rake <=1.3s renewal pin (WR-01)
```

```
git add core/selftest.lua && git commit -m "fix(selftest): stub rakeLeft to restore vp9 no-discharge liveness pin; add rake <=1.3s renewal pin (WR-01)"
```

**precondition**：`/tmp/luabuild` 三解释器与 `/usr/local/bin/node` 在位（规划时实测通）。若执行时缺失，停下报告 unmet precondition，不得降级跳过三解释器验证。

## 威胁模型（STRIDE 简表）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260914-1t0-01 | Tampering | selftest stub 泄漏污染实机会话（rakeLeft/energyDischargeBeforeBite 未还原） | medium | mitigate | CR-01：pcall 内执行+还原先于断言，还原值逐一引用 capture 时保存的 orig；P-03 与新测试两处同规约，Gate 1 钉死 2 capture/2 restore 计数 |
| T-260914-1t0-02 | DoS | 断言在登录自测抛错显示红名 | low | accept | 失败仅表现自测红名提示，不进战斗路径；无额外缓解成本 |
| T-260914-1t0-03 | Escalation | 经 UI 注入的测试名/文案被执行 | low | accept | 测试名与断言为静态字面量，register 仅登记函数，无 eval 路径 |

包管理器安装：本 quick 零 npm/pip/cargo 安装，package legitimacy 门与 T-SC 行不适用。

## unrun-verify 备案（写进 SUMMARY）

本机无 WoW 客户端。用户 Windows+Cygwin 重建 `/mt` 后：Category P 共 9 条应全绿，其中 "P: cp5Bite still bites when no discharge condition matches" 与新增 "P: cp5Bite skips discharge to renew rake when rake <=1.3s" 无疑点；若前者红名则说明泄能洞 A/B 回归（pin 生效），若后者红名则说明 1.3s 门失效或其 stub 断链。

## 输出

执行完成后输出 `260914-1t0-SUMMARY.md`（结构同 260913-46s 先例：任务完成 / 偏差 / Unrun Verification / Threat Flags / Self-Check）；STATE.md Quick Tasks 表行由 quick 流程 docs commit 收口，代码 commit 中不得夹带任何 .planning 文件。