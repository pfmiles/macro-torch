---
quick_id: 260914-1t0
phase: quick-260914-1t0
plan: 260914-1t0
subsystem: core-selftest
tags: [selftest, cp5Bite, rake-gate, liveness-pin, wr-01, lua-5.0, cr-01]
requires: ["WR-01@quick-260914-0ql"]
provides: ["P-03 vp9 no-discharge liveness pin restored (rakeLeft stubbed) + new P pin for the 1.3s rake renewal gate (Category P 8->9)"]
affects: [core/selftest.lua]
status: complete
actuals:
  tokens: 720
  tasks: 1
  commits: 1
---

# Quick Task 260914-1t0: 修复 quick 260914-0ql 评审 WR-01 + 新增 1.3s rake 续期自测 pin（core/selftest.lua 单文件）Summary

`core/selftest.lua` 的 P-03 登录自测（"P: cp5Bite still bites when no discharge condition matches"）补上 `macroTorch.rakeLeft` 的 capture/stub/restore 三行（stub 返回 9 > 1.3），使 quick 260914-0ql 引入的 1.3s rake 门（cat.lua:141）不再在无目标的登录环境短路该测试刻意保留的**真实** `energyDischargeBeforeBite` no-condition fall-through 路径 —— 260825-vp9 泄能洞 A/B liveness pin 恢复效力；同时按 0ql PLAN 备案口径新增 "P: cp5Bite skips discharge to renew rake when rake <=1.3s" pin：stub isRipPresent/isRakePresent→true、ripLeft→4、rakeLeft→1.0，断言 rake 在场且 rakeLeft<=1.3 时泄能 stub 零调用（dischargeCalled==false）且咬击照常（biteCalled==true）；计数注释 8→9（`adds 9 tests ... 1 in quick 260914-1t0`）。纯插入 3 行 + 新测试块，唯一删除为旧计数注释行，单文件 57 insertions / 1 deletion；Lua 5.0.3/5.1.5/5.4.7 三解释器 loadfile 编译全绿；锁定 diff 门（删除面恰 1 行、hunk 范围 864-999、新增行括号平衡、token/CJK/LF、git diff --check）全绿；`classes/druid/cat.lua`、`SM_Extend.lua` 零触碰、未运行任何构建脚本。

## Tasks Completed

### Task 1（唯一任务，原子）: P-03 补 rakeLeft stub 三行 + 新增 rake 续期 pin + 计数注释 8→9

- **Commit:** `418d664` — `fix(selftest): stub rakeLeft to restore vp9 no-discharge liveness pin; add rake <=1.3s renewal pin (WR-01)`
- **Changes:** 仅 `core/selftest.lua`（57 insertions / 1 deletion，1058 行）：
  - 四处 Edit 的锚点均与计划锁定前/后块逐字匹配，代码零偏离：
    - (a) L869：P-03 capture 区插入 `local origRakeLeft = macroTorch.rakeLeft`（origIsRakePresent 与 origSafeBite 之间）；
    - (b) L875：P-03 stub 区插入 `macroTorch.rakeLeft = function(clickContext) return 9 end`（ripLeft=4 与 isRakePresent=true 之间）；
    - (c) L900：P-03 restore 区插入 `macroTorch.rakeLeft = origRakeLeft`（ripLeft 还原之后、isRakePresent 还原之前，还原先于全部 assert，CR-01）；
    - (d) L909-961：新测试块（register "P: cp5Bite skips discharge to renew rake when rake <=1.3s" + 7 项 capture + 5 项 stub + pcall 执行 + 逐项还原先于 assert）；L962：计数注释 8→9。
- **Verification battery:** GATE 0-6 全绿（明细见下）。
- **CR-01:** 两处测试均 pcall 内执行、还原先于所有 assert；Gate 1 钉死 2 capture / 2 restore 计数，均通过。
- **Lua 5.0 合规:** 新增行 `#|goto |::` 计数 0、CJK 计数 0、全文件 CR 字节 0（全 LF）、新增字符串字面量无相邻 `--` 对、无裸双引号字节。

## Verification Gates（逐项实测记录）

| Gate | 内容 | 结果 |
|------|------|------|
| GATE 0 | Lua 5.0.3 / 5.1.5 / 5.4.7 三解释器 `loadfile` 编译 core/selftest.lua | LOADFILE-OK ×3 |
| GATE 1 | 结构 grep 计数（rakeLeft=9 stub ×1、rakeLeft=1.0 stub ×1、origRakeLeft capture ×2、restore ×2、register 名 ×1、`adds 9 tests` ×1、`adds 8 tests` ×0） | GATE 1 OK |
| GATE 2 | 删除面恰 1 行且为旧计数注释（含脚本修正，见偏差 1） | GATE 2-3 OK（修正后实测：删除 1 行 = `-- Registration count: Category P adds 8 tests ...`） |
| GATE 3 | 全部 hunk 范围 864-999（含脚本修正，见偏差 2） | GATE 2-3 OK（实测 hunk 起点 868/873/897/906，全部在界内；P-02 822-854 零 hunk） |
| GATE 4 | diff 新增行括号平衡（260831-24c 先例 node 脚本） | T4 DIFF BALANCE: OK |
| GATE 5 | 新增行 token/CJK 门 + 全文件 CR 字节门 + `git diff --check` | GATE 4-5 OK（均 0 命中，diff --check 干净） |
| GATE 6 | 变更面唯一（含脚本修正，见偏差 3） | ALL GATES OK（代码变更唯一文件 core/selftest.lua） |

## Deviations from Plan

三道验证脚本缺陷就地修正（Rule 1 - Bug，均不改计划锁定代码：4 处 Edit 的前/后块逐字执行，代码 diff 与计划锁定 diff 逐字一致）：

1. **GATE 2 过滤器缺陷** — 计划原文 `grep '^-' | grep -v '^---'` 会把**真正的删除行**一并滤掉：被删行本身是 Lua 注释（`-- Registration...`），加上 diff 删除前缀后成 `--- Registration...`，恰好命中 `^---`（与 `--- a/core/selftest.lua` 文件头同形）。实测该管道计数为 0 而非计划的 1，使门不可能通过。修正：仅排除 diff 文件头 `grep -vF -- '--- a/core/selftest.lua'`，其余管道不动。修正后实测：删除恰 1 行且为旧计数注释。
2. **GATE 3 正则缺陷** — 计划原文 `^@@ -8[6-9][0-9],|^@@ -9[0-9][0-9],` 要求负号侧坐标后必须紧跟 `,COUNT`，但 git 对 1 行变更输出 `@@ -906 +909,54`（无逗号计数），导致 906 这一合法 hunk 反而被判越界。修正：`(,| )` 双形式匹配（`,COUNT` 或裸空格）。修正后实测：4 个 hunk 起点全部落在 864-999。
3. **GATE 6 未计及编排目录** — `git status --porcelain` 比计划假设多一行 `?? .planning/quick/260914-1t0-fix-quick-260914-0ql-code-review-wr-01-t/`（编排器在任务开始前放入的计划目录，本 quick 起点即存在，按约束留给 orchestrator 收口、不进入本代码 commit）。修正：排除该确切行。修正后实测：代码变更面唯一 = core/selftest.lua。
- 其余与计划完全一致：4 处 Edit 锚点与锁定块逐字匹配、P-03 三行序位（capture/stub/restore）正确、新测试注册于 904 行之后且计数注释之前、commit message 逐字、`classes/` 与 `SM_Extend.lua` 零读写、未运行 build.sh。

## Unrun Verification

本机无 WoW 客户端，实机自测电池无法本地执行（计划备案口径）。用户 Windows+Cygwin 重建 `/mt` 后：

- Category P 共 9 条应全绿（登录环境 `UnitClass('player') == 'Druid'` 时执行）。
- 若 "P: cp5Bite still bites when no discharge condition matches" 红名 → 泄能洞 A/B 回归（本 pin 恢复效力即为此目的）。
- 若 "P: cp5Bite skips discharge to renew rake when rake <=1.3s" 红名 → 1.3s 门失效或该测试 stub 断链。
- 已记入 windows ledger（entry id 8，unrun-verify）。

## Threat Flags

无新增面——本 quick 为测试文件纯插入（+新建 self-test 注册），无网络端点、鉴权路径或信任边界 schema 变化。计划威胁模型 T-260914-1t0-01（stub 泄漏污染实机会话）已按 CR-01 缓解：两处测试均在 pcall 内执行、还原先于断言、还原值逐一引用 capture 保存的 orig，GATE 1 的 2 capture / 2 restore 计数实测通过；T-260914-1t0-02/03 计划已 accept。

## Known Stubs

无——新测试的 stub（safeBite/readyBite/energyDischargeBeforeBite）在 pcall 内即刻还原，不落持久状态；断言失败仅表现为自测红名（计划威胁模型 T-260914-1t0-02 accept）。

## Self-Check: PASSED

- File modified exists: `/home/admin/workspace/macro-torch/core/selftest.lua`（1058 行，已 commit，工作树无其他代码变更）
- Commits exist: `418d664`（`git log -1` 已核验，单文件 +57/−1）
- 锚点复核: P-03 capture L869 / stub L875 / restore L900；新测试 register L917、capture L922、restore L954；计数注释 L962（`Category P adds 9 tests ... 1 in quick 260914-1t0`）
- 三解释器 LOADFILE-OK ×3、GATE 1-6 全绿、T4 DIFF BALANCE: OK、`git diff --check` 干净 —— 全部实跑通过
- 计划锁定契约复核：diff 删除面恰 1 行 = 旧计数注释；全部 4 个 hunk 落在 864-999；classes/、SM_Extend.lua 零触碰（`git status` 仅 core/selftest.lua 一处代码变更）