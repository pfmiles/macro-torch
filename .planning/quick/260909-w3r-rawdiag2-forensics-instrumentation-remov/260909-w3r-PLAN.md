---
phase: quick-260909-w3r-rawdiag2-forensics-instrumentation-remov
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - macro_torch.lua
  - classes/druid/cat.lua
  - core/events.lua
  - core/spell_trace_core.lua
autonomous: true
requirements:
  - QUICK-260909-W3R
user_setup:
  - service: wow-client
    why: "Established quick-task convention: this repo's SM_Extend.lua is never rebuilt here (no build script runs in this plan). On the user's Windows+Cygwin machine: (1) run build.sh to regenerate SM_Extend.lua into both AddOns dirs; (2) in game run /mt — the login banner now lists four CONFIG_OPTIONS entries with no rawdiag2Enabled line; (3) a normal catAtk fight leaves MACRO_TORCH_LOG free of [RAWDIAG2 ...] lines. In-game re-verification is weekly-CD paced (user memory convention)."
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Zero diag2 residue: a case-insensitive grep for rawdiag2 across every .lua under the repo root — excluding SM_Extend.lua and everything under .planning/ — returns zero matches. The master switch, its nil-guard, the CONFIG_OPTIONS entry, the RAWDIAG2_KEYWORDS table, the RAW_COMBATLOG scout, the safeRip ctx stamp, the recordCastTable arming hook, the rawdiag2IntentDepth helper, and all four pair-ledger logs are gone, comments included."
    - "Production land pipeline intact: core/events.lua's RAW_COMBATLOG branch is now cpDamage gate -> tier-1 channel-whitelist return -> aura-apply pattern dispatch, byte-identical apart from the deleted scout; core/spell_trace_core.lua's processRawAuraApply still finds the apply marker, enforces GUID ownership, keeps the accepted-residual-risk comment, pairs via pairLandIntent, and announces/records the land — identical semantics, no forensics text."
    - "Safe Rip cast path intact: classes/druid/cat.lua safeRip's readiness gate, the live-state show() line, the rip('ready') cast, and the lastRipEquippedSavagery / lastRipAtCp snapshot writes survive byte-identical; only the [RAWDIAG2 ctx] stamp block is gone."
    - "Config registry honest: exactly four CONFIG_OPTIONS entries remain (cpBuildLog, COWER_THREAT_THRESHOLD, LOG_MAX_SIZE, cpDamageLog), the data-driven banner prints those four, and the adjacent comment count nouns agree with reality (four entries / one boolean option / two options above)."
    - "Hygiene: bbcheck prints BALANCED for all four files; no added line carries #, goto, or ::; LF endings preserved (git diff --check clean, no CR bytes); SM_Extend.lua and .planning/samples/p1.txt are byte-identical to the task-start baseline; no build script is run; only the four files appear in git status; comments and commit message are English; HUMAN-UAT.md is untouched."
  artifacts:
    - macro_torch.lua
    - classes/druid/cat.lua
    - core/events.lua
    - core/spell_trace_core.lua
  key_links:
    - "macro_torch.lua CONFIG_OPTIONS registry -> printConfigBanner — the banner is data-driven, so deleting the entry automatically drops it from the login printout; zero other call sites reference the switch field."
    - "core/events.lua RAW_COMBATLOG branch -> cpDamage gate (onCpDamageLine) -> tier-1 whitelist return -> processRawAuraApply dispatch — the dispatch chain where the scout used to occupy the prefix slot."
    - "core/spell_trace_core.lua recordCastTable intent seed -> pairLandIntent -> processRawAuraApply show + recordLandEvent — the production land chain the arming hook, depth helper, and pair ledger adorned."
    - "classes/druid/cat.lua safeRip show() -> macroTorch.player.rip('ready') -> lastRipEquippedSavagery / lastRipAtCp writes — the cast path the ctx stamp instrumented."
---

<!-- planner-discipline-allow: rawdiag2 -->
<!-- planner-discipline-allow: RAWDIAG2 -->
<!-- planner-discipline-allow: _rawdiag2 -->

<objective>
Delete every piece of the RAWDIAG2 Rip-landing forensics instrumentation:
the master switch and its config-banner entry (macro_torch.lua), the safeRip
decision stamp (classes/druid/cat.lua), the keyword table and RAW_COMBATLOG
scout (core/events.lua), and the arming hook, intent-depth helper, and pair
ledger (core/spell_trace_core.lua).

Purpose: the unified landing-verdict scheme is correct under BOTH
hypotheses the scout was built to arbitrate (apply line suppressed / not
suppressed by an allied feral's active Rip), and the multi-cat arbitration
question is answered by design now — the forensics device has served its
purpose and adds only log-buffer pressure and code surface. Everything
production-grade must survive byte-identical: the cpDamage gate, the tier-1
whitelist, the aura-apply dispatch, safeRip's cast path, and
processRawAuraApply's pairing core (including the accepted-residual-risk
comment, which states a real design risk and carries no forensics wording).

Output: the four Lua files with all 42 planning-time rawdiag2 references
removed; nothing else changes. SM_Extend.lua and .planning/samples/p1.txt
are not touched, no build script is run, and no UAT document is modified.
</objective>

<execution_context>@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>Grounding facts, all verified by a live read at planning time (HEAD
c4725a2, `git status --porcelain` empty, .gitattributes enforces eol=lf on
all four files; grep tallies: macro_torch.lua 8, classes/druid/cat.lua 7,
core/events.lua 12, core/spell_trace_core.lua 15 — every hit falls inside a
deletion zone below). Zone line numbers are pre-edit anchors; they are
exact because the tree is clean at c4725a2 — assert that before editing and
halt if the tree is dirty or HEAD moved.

Planning-reality vs briefing (MUTABLE-SCOPE AUTHORITY: live read wins):

- The banner currently holds FIVE CONFIG_OPTIONS entries because phase 28
  added cpDamageLog (live lines 106-112) after the briefing was drafted.
  After deleting the rawdiag2Enabled entry, FOUR entries remain (cpBuildLog,
  COWER_THREAT_THRESHOLD, LOG_MAX_SIZE, cpDamageLog). The briefing's "3
  items" is not achievable without deleting another entry — out of scope;
  delete exactly the one rawdiag2Enabled entry and verify the count is 4.
- Zone boundaries at live HEAD differ slightly from the briefing's
  approximate ranges: macro_torch.lua 29-36 and 85-91; events.lua 23-26 and
  136-176; spell_trace_core.lua 126-142, 213-224, and the ledger pieces
  364-367 / 370-372 / 377-379 / 399-406. Use these.
- The accepted-residual-risk comment (spell_trace_core.lua live 382-386)
  is diag2-ERA wording but diag2-free: it documents the real multi-feral
  pairing risk, fail-wins resolution, and the accepted suppression case per
  debug decisions 2 and 6. KEEP IT VERBATIM — no rewording needed.
- Three adjacent count nouns in macro_torch.lua become false once the
  switch is gone. Reword them (Z3 below) rather than leaving stale counts.
- classes/druid/HUMAN-UAT.md line 206 still names rawdiag2Enabled. User
  constraint: do NOT modify any UAT document this task. Leave it; the
  phase-end verifier revisits UAT docs per established convention. All
  grep gates below are scoped to .lua files, so no UAT/gate conflict.

Repo conventions (memory-backed): English comments and commit message; LF
line endings (`.gitattributes` eol=lf); Lua 5.0 only — no `#` length
operator, no goto, no `::` labels in any added line (all replacement text
below is pre-vetted); SM_Extend.lua is a build artifact never edited
(gitignored, so `git status` cannot guard it — guard via the checksum
baseline described in Task 1); .planning/samples is gitignored too, same
guard; the repo's bracket-balance gate is
`node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js <file>`
printing `BALANCED` / exit 0. Do NOT run build.sh or any build script.

Em-dash note: two verbatim zone segments below contain U+2014 em dashes
("city —" style). Reproduce quote/dash characters byte-for-byte when
copying old_strings; the executor's own Read of each file is authoritative
for the long zones, whose boundaries are pinned by their unique first and
last lines.

Delete-by-zone contract: each zone is "delete these exact live lines
inclusive of the first and last; the named neighbors above and below must
survive byte-identical; no blank-line surgery, renumbering, or dedent of
anything outside the zone (pure deletion leaves no orphan indent here)."

## macro_torch.lua

Z1 (live 29-36) — switch preamble + nil-guard, full verbatim block to
delete:

-- RAWDIAG2 forensics master switch (quick 260907-mhh WR-02 fix): false by default,
-- set macroTorch.rawdiag2Enabled = true in game (SuperMacro body) to arm the Rip
-- landing forensics scout on every recorded in-combat Rip cast (and the safeRip ctx
-- stamp). The nil-guard re-arms the default on every login; toggling mid-session
-- needs no reload so incidental fights between the test and the log export stay silent.
if macroTorch.rawdiag2Enabled == nil then
    macroTorch.rawdiag2Enabled = false
end

Survives above: line 28 `end` (closing the cpBuildLog nil-guard); survives
below: line 37 `-- Cower worldboss threat threshold (quick 260907-tuh): 75 percent by default,`.

Z2 (live 85-91) — banner registry entry, full verbatim block to delete:

    {
        name = 'macroTorch.rawdiag2Enabled',
        default = false,
        desc = 'RAWDIAG2 Rip landing forensics master switch',
        cmd = '/run macroTorch.rawdiag2Enabled=true',
        get = function() return macroTorch.rawdiag2Enabled end,
    },

Survives above: line 84 `    },` (closing the cpBuildLog entry); survives
below: line 92 `    {` (opening the COWER_THREAT_THRESHOLD entry).

Z3 (three exact rewrites of preserved neighbor comments, one Edit each):

old: -- survey of user-tunable globals yields exactly these five entries; a future option
new: -- survey of user-tunable globals yields exactly these four entries; a future option

old: -- for the current session (no SavedVariables persistence), same as the two
     -- boolean options above.
new: -- for the current session (no SavedVariables persistence), same as the one
     -- boolean option above.

old: -- (no SavedVariables persistence), same as the three options above. The consumer
new: -- (no SavedVariables persistence), same as the two options above. The consumer

(Z3 payload carries no digit other than the count nouns, no Lua tokens, and
no new executable code — comment-only.)

## classes/druid/cat.lua

Z1 (live 434-454) — the [RAWDIAG2 ctx] decision stamp. 21 lines: first
line is

        -- [RAWDIAG2 quick 260907-mhh] stamp the Rip cast decision inputs BEFORE the

last two lines are

                ' t=' .. string.format('%.3f', GetTime()), 'yellow')
        end

Delete everything from the first line through that trailing `end`
(inclusive). Survives above: line 433 (the show() call's final continuation
line ending `.. 's')`); survives below: line 455
`        macroTorch.player.rip('ready')` and lines 456-457 (the
lastRipEquippedSavagery / lastRipAtCp snapshot writes). Inside the zone the
executor's Read shows the exact body (an em dash appears in the "arbiter
field — freshly computed" line; copy bytes verbatim).

## core/events.lua

Z1 (live 23-26) — keyword table + preamble, full verbatim block to delete:

-- [RAWDIAG2 quick 260907-mhh] keyword filter for the RAW_COMBATLOG forensics
-- dump. The scout below dumps only while macroTorch.context._rawdiag2Active is
-- armed; recordCastTable in spell_trace_core.lua arms it on a recorded Rip cast.
local RAWDIAG2_KEYWORDS = { 'Rip', 'Rake', 'Bite', 'afflicted', 'fades' }

Survives above: line 21 `local frame = CreateFrame("Frame")` and blank line
22; survives below: blank line 27 and line 28
`-- frame:RegisterEvent("PLAYER_LOGIN")`.

Z2 (live 136-176) — the entire forensics scout block. 41 lines: first line
is

        -- [RAWDIAG2 quick 260907-mhh] forensics scout, placed BEFORE the tier-1 channel

last line is

        -- end of RAWDIAG2 scout block (arming hook: spell_trace_core.lua recordCastTable)

Delete everything between and including those two lines, in one contiguous
old_string (the executor copies the block from their own Read; it contains
one em dash in the "stays armed until combat exit —" comment line).
Survives above: line 135 `    elseif event == "RAW_COMBATLOG" then`
(branch header, 4-space indent — keep); survives below: line 177
`        -- [cpDamage] phase 28 channel gate: dispatch SELF_DAMAGE raw lines to`
— the cpDamage gate (`if macroTorch.cpDamageLog and arg1 ==
'CHAT_MSG_SPELL_SELF_DAMAGE' and arg2 then` / `onCpDamageLine`), the tier-1
channel whitelist return, and the `processRawAuraApply` dispatch all stay
byte-identical at their current indentation.

## core/spell_trace_core.lua

Z1 (live 126-142) — recordCastTable arming hook. 17 lines: first line is

    -- [RAWDIAG2 quick 260907-mhh] arm the RAW_COMBATLOG forensics scout on a

last line is

        macroTorch.log('[RAWDIAG2] scout armed by Rip cast record, until combat exit', 'green')

Delete the whole block inclusive (one em dash appears in the "capture runs
until combat exit —" comment line). Survives above: line 125
`    macroTorch.loginContext.intentTable[spell][mob].push({ state = 'pending', castAt = GetTime(), landAt = nil })`;
survives below: line 143 `end` (closes recordCastTable).

Z2 (live 213-224) — rawdiag2IntentDepth helper. 12 lines: first line is

-- [RAWDIAG2 quick 260907-mhh] read-only pending-intent stack depth for a spell

last two lines are

    return 0
end

Delete the whole block inclusive. Survives above: line 212 `end` (closes
pairLandIntent); survives below: line 225 `-- cpDamage pairing (phase 28):
purge/pair skeleton copied from pairLandIntent`.

Z3a (live 364-367) — ledger preamble + armed gate (4 lines, delete):

    -- [RAWDIAG2 quick 260907-mhh] pair ledger armed only inside a live scout window.
    -- Combat exit wipes macroTorch.context, so the armed flag cannot leak into a
    -- later fight or into a selftest run (out-of-combat /mt never arms it).
    local armed = macroTorch.context and macroTorch.context._rawdiag2Active

Survives below: line 368 `    local markerPos = string.find(rawText, ' is afflicted by ')`.

Z3b (live 370-372) — inside `if not markerPos then`: delete exactly these
three lines, shrinking the branch to its bare `return nil` (the branch
header at 369 and `return nil` at 373 stay):

        if armed then
            macroTorch.log('[RAWDIAG2 pair] ' .. spellName .. ' no-pair:no-apply-marker', 'yellow')
        end

Z3c (live 377-379) — inside the guid-mismatch branch: delete exactly these
three lines, shrinking the branch to its bare `return nil` (the branch
header at 376 and `return nil` at 380 stay):

        if armed then
            macroTorch.log('[RAWDIAG2 pair] ' .. spellName .. ' no-pair:guid-mismatch target=' .. tostring(targetGuid) .. ' line=' .. guid, 'yellow')
        end

KEEP VERBATIM: live 382-386, the accepted-residual-risk comment (multi-feral
pairing risk / fail-wins / suppression acceptance). It has no forensics
wording; do not rewrite, do not delete.

Z3d (live 399-406) — the two trailing ledger log blocks, delete in full:

    if armed and intent then
        macroTorch.log('[RAWDIAG2 pair] ' .. spellName .. ' pair-ok guid=' .. guid ..
            ' t=' .. string.format('%.3f', now), 'green')
    end
    if armed and not intent then
        macroTorch.log('[RAWDIAG2 pair] ' .. spellName .. ' no-pair:no-intent t=' ..
            string.format('%.3f', now), 'yellow')
    end

Survives below: line 407 `    return intent` and line 408 `end`.
Survives above (after Z3c): the residual-risk comment lines 382-386.
The `if intent then` block at 388-398 (show + recordLandEvent) stays
byte-identical.

After Z3a-d, processRawAuraApply contains no `armed` identifier anywhere;
the pairing core (string.find marker, guid ownership compare, pairLandIntent
call, landing announcement, recordLandEvent) is untouched.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the RAWDIAG2 master switch, banner entry, and safeRip decision stamp (macro_torch.lua, classes/druid/cat.lua)</name>
  <files>macro_torch.lua, classes/druid/cat.lua</files>
  <action>
Preflight (before any edit): cd /home/admin/workspace/macro-torch; assert
`git status --porcelain` is empty and `git rev-parse HEAD` equals c4725a2
(halt and report if not); then record the untouched-artifact baseline ONCE
for the whole plan: `sha256sum SM_Extend.lua .planning/samples/p1.txt >
/tmp/w3r-baseline.sha` (Task 2 and the closing battery reuse it).

Edit ONLY macro_torch.lua and classes/druid/cat.lua. English comments, LF
endings, Lua 5.0 (no `#`/goto/:: in added lines — the only added lines are
the three Z3 comment rewrites, pre-vetted). Do NOT run build.sh or any
build script. Do NOT touch core/events.lua, core/spell_trace_core.lua,
SM_Extend.lua, .planning/samples/p1.txt, or any UAT document
(HUMAN-UAT.md line 206's stale switch mention stays untouched per user
constraint).

macro_torch.lua — apply the Z1, Z2, Z3 zones from the context section,
each as one Edit whose old_string is copied verbatim from your fresh Read:

- Z1: delete the switch preamble + nil-guard (live 29-36, full block
  quoted in context). Leaves `end` (cpBuildLog guard) directly above the
  Cower comment.
- Z2: delete the rawdiag2Enabled CONFIG_OPTIONS entry (live 85-91, full
  block quoted in context; the entry opens at `    {` and closes at
  `    },` on live 91). Do NOT delete any other entry — the banner must
  land at exactly four entries (cpBuildLog, COWER_THREAT_THRESHOLD,
  LOG_MAX_SIZE, cpDamageLog).
- Z3: three exact comment rewrites (five->four entries, two->one boolean
  option, three->two options above) so preserved neighbors stay factually
  honest. Each is a one-for-one comment-line replacement with zero code
  change.

classes/druid/cat.lua — apply Z1: delete the [RAWDIAG2 ctx] stamp (live
434-454, boundaries pinned in context; copy the block verbatim from your
fresh Read, em dash included). safeRip's gate, the Rip!!! show() line, the
rip('ready') cast, and the two snapshot writes below must survive
byte-identical.

Do not renumber or reformat anything else; outside Z3 there are zero added
lines in this task.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && FAIL=0 && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node "$BB" macro_torch.lua | grep -q BALANCED || FAIL=1 && node "$BB" classes/druid/cat.lua | grep -q BALANCED || FAIL=1 && [ "$(grep -rnic 'rawdiag2' macro_torch.lua classes/druid/cat.lua 2>/dev/null | grep -vc ':0')" = "0" ] || FAIL=1 && [ "$(grep -c "name = 'macroTorch\." macro_torch.lua)" = "4" ] || FAIL=1 && [ "$(grep -cF 'yields exactly these four entries' macro_torch.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'same as the one' macro_torch.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'same as the two options above' macro_torch.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.COWER_THREAT_THRESHOLD == nil' macro_torch.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.LOG_MAX_SIZE == nil' macro_torch.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.cpDamageLog == nil' macro_torch.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'function macroTorch.safeRip' classes/druid/cat.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF "macroTorch.player.rip('ready')" classes/druid/cat.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.context.lastRipAtCp = clickContext.comboPoints' classes/druid/cat.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.loginContext.lastRipEquippedSavagery = savageryNow' classes/druid/cat.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'Rip!!! At cp: ' classes/druid/cat.lua)" = "1" ] || FAIL=1 && git diff --check || FAIL=1 && echo "T1 GATE FAIL=$FAIL"</automated>
  </verify>
  <done>
T1 gate exits with FAIL=0: bbcheck prints `macro_torch.lua: BALANCED` and
`classes/druid/cat.lua: BALANCED`; a case-insensitive count of rawdiag2
references in exactly these two files is zero; the banner registry holds
exactly 4 entry names; the three rewritten count-noun comments are present
(four entries / same as the one / same as the two options above) with no
stale `five entries` or `three options above` tokens left; the COWER,
LOG_MAX_SIZE, and cpDamageLog nil-guards each survive exactly once;
safeRip survives with its show() line, rip('ready') cast, and both snapshot
writes intact; git diff --check is clean. Only macro_torch.lua and
classes/druid/cat.lua are modified, and /tmp/w3r-baseline.sha exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the RAW combat-log scout, keyword table, arming hook, intent-depth helper, and pair ledger (core/events.lua, core/spell_trace_core.lua)</name>
  <files>core/events.lua, core/spell_trace_core.lua</files>
  <action>
Execute AFTER Task 1 (precondition: /tmp/w3r-baseline.sha exists and
contains two checksum lines; stop if missing).

Edit ONLY core/events.lua and core/spell_trace_core.lua. English comments,
LF endings, Lua 5.0 (this task adds zero lines — pure deletion). Do NOT run
build.sh or any build script. Do NOT touch macro_torch.lua,
classes/druid/cat.lua, SM_Extend.lua, .planning/samples/p1.txt, or any UAT
document. The baseline checksum file /tmp/w3r-baseline.sha must not be
recreated here (it holds the pre-edit hashes).

core/events.lua — apply Z1 then Z2 from the context section, each as one
Edit copied verbatim from your fresh Read:

- Z1: delete the RAWDIAG2_KEYWORDS preamble + table (live 23-26, full block
  quoted in context). `local frame = CreateFrame("Frame")` above and the
  registerEvent comment below survive.
- Z2: delete the entire forensics scout (live 136-176) in one contiguous
  old_string, leaving the branch header `elseif event == "RAW_COMBATLOG"
  then` at live 135 immediately followed by the cpDamage gate comment at
  live 177. The cpDamage gate (cpDamageLog guard + onCpDamageLine), the
  tier-1 channel whitelist return, and the processRawAuraApply dispatch are
  NOT part of the zone; they stay byte-identical with their current
  indentation.

core/spell_trace_core.lua — apply the five zones from the context section:

- Z1: delete the recordCastTable arming hook (live 126-142, one contiguous
  old_string). The intent-seed push above and the function-closing `end`
  below survive.
- Z2: delete the rawdiag2IntentDepth helper (live 213-224). pairLandIntent's
  `end` above and the cpDamage pairing comment below survive.
- Z3a: delete the ledger preamble + armed gate (live 364-367). The
  marker-string parse below survives.
- Z3b / Z3c: inside the no-marker and guid-mismatch branches, delete only
  the three-line `if armed then ... end` wrappers; each branch shrinks to
  its bare `return nil` with the branch header and return untouched.
- KEEP VERBATIM the accepted-residual-risk comment (live 382-386) and the
  whole `if intent then` landing block (show + recordLandEvent).
- Z3d: delete the two trailing ledger log blocks (live 399-406, full block
  quoted in context), leaving `return intent` and the function's `end`
  intact.

After the zones, processRawAuraApply contains no `armed` identifier and no
macroTorch.log call anywhere in the raw-apply path; the pairing core is
unchanged. Do not renumber, dedent, or reformat anything outside the zones.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && FAIL=0 && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && [ -f /tmp/w3r-baseline.sha ] || FAIL=1 && node "$BB" core/events.lua | grep -q BALANCED || FAIL=1 && node "$BB" core/spell_trace_core.lua | grep -q BALANCED || FAIL=1 && [ "$(grep -rnic 'rawdiag2' core/events.lua core/spell_trace_core.lua 2>/dev/null | grep -vc ':0')" = "0" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.onCpDamageLine(arg2, GetTime())' core/events.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF "arg1 ~= 'CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE' and arg1 ~= 'CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE'" core/events.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.processRawAuraApply(spellName, arg2, macroTorch.target.guid, GetTime())' core/events.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF "elseif event == \"RAW_COMBATLOG\" then" core/events.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.recordCastTable(macroTorch._pendingCastSpellName)' core/events.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF "push({ state = 'pending', castAt = GetTime(), landAt = nil })" core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'function macroTorch.recordCastTable' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'function macroTorch.pairLandIntent' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'function macroTorch.pairCpDamageIntent' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'local markerPos = string.find(rawText' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'local intent = macroTorch.pairLandIntent(spellName, now)' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'macroTorch.recordLandEvent(spellName, now)' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && [ "$(grep -cF 'accepted residual risk' core/spell_trace_core.lua)" = "1" ] || FAIL=1 && git diff --check || FAIL=1 && echo "T2 GATE FAIL=$FAIL"</automated>
  </verify>
  <done>
T2 gate exits with FAIL=0: bbcheck prints `core/events.lua: BALANCED` and
`core/spell_trace_core.lua: BALANCED`; a case-insensitive count of rawdiag2
references in exactly these two files is zero; the cpDamage gate
(onCpDamageLine call), the tier-1 dual-channel return, the aura-apply
dispatch, the RAW_COMBATLOG branch header, and the UNIT_CASTEVENT
recordCastTable bridge each survive exactly once in events.lua; in
spell_trace_core.lua the intent seed push, recordCastTable, pairLandIntent,
pairCpDamageIntent, the apply-marker parse, the pairLandIntent call, the
recordLandEvent call, and the accepted-residual-risk comment each survive
exactly once; git diff --check is clean. The baseline checksum file is
present; only these two files were modified in this task.
  </done>
</task>

</tasks>

<threat_model>## Trust Boundaries

None crossed. Pure deletion of opt-in diagnostics inside the addon: no new
inputs, outputs, dependencies, persistence paths, or privilege context. The
pre-existing macroTorch.log -> MACRO_TORCH_LOG SavedVariables path shrinks
(fewer writers once the scout, ctx stamp, and pair ledger are gone).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260909-w3r-01 | Tampering | core/events.lua RAW_COMBATLOG dispatch order: deleting the scout could displace the cpDamage gate or tier-1 whitelist | medium | mitigate | Deletion confined to live 136-176 with the `elseif event == "RAW_COMBATLOG" then` header untouched; T2 gate pins onCpDamageLine = 1, tier-1 dual-channel return = 1, and processRawAuraApply dispatch = 1. |
| T-quick-260909-w3r-02 | Tampering | core/spell_trace_core.lua processRawAuraApply: partial ledger removal could damage the production pairing path (marker parse, guid ownership, pairLandIntent) | medium | mitigate | Deletion confined to the `armed` gate and the four pair logs (Z3a-d); the accepted-residual-risk comment and the `if intent then` landing block stay verbatim; T2 gate pins marker parse, pairLandIntent call, recordLandEvent call, and the residual-risk comment each = 1. |
| T-quick-260909-w3r-03 | Information Disclosure | classes/druid/HUMAN-UAT.md:206 still names the removed switch in its with-cpDamageLog warning | low | accept | User constraint forbids UAT-doc edits this task; the reminder is moot after removal (its guarded hazard — two switches contending for the rotating log buffer — can no longer occur); the phase-end verifier revisits UAT docs per established convention. |
| T-quick-260909-w3r-04 | Denial of Service | macroTorch.printConfigBanner reading a stale registry entry | low | accept | Cannot occur: the whole registry entry is deleted (T1 gate asserts exactly 4 entries and zero remaining switch names), and printConfigBanner already wraps every getter in pcall. |

No npm/pip/cargo install tasks exist in this plan, so the T-*-SC package-legitimacy row does not apply.
</threat_model>

<verification>
After both tasks, run the closing battery; it must echo `BATTERY FAIL=0`.

```bash
cd /home/admin/workspace/macro-torch
BB=/home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js
FAIL=0
# G1: zero diag2 residue across every source .lua (SM_Extend.lua and .planning excluded)
mapfile -t SRC < <(find /home/admin/workspace/macro-torch -name '*.lua' -not -path '*/.planning/*' -not -name 'SM_Extend.lua')
[ "$(grep -rnic 'rawdiag2' "${SRC[@]}" 2>/dev/null | grep -vc ':0')" = "0" ] || FAIL=1
# G2: bracket balance on the four edited files
for f in macro_torch.lua classes/druid/cat.lua core/events.lua core/spell_trace_core.lua; do
  node "$BB" "$f" | grep -q BALANCED || FAIL=1
done
# G3: Lua 5.0 token gate on added lines only
if git diff -U0 -- '*.lua' | grep -E '^\+' | grep -vE '^\+\+' | grep -qEn 'goto|#|::'; then FAIL=1; fi
# G4: LF + whitespace hygiene
git diff --check || FAIL=1
if git diff | grep -q $'\r'; then FAIL=1; fi
# G5: scope — exactly the four source files modified, nothing else in porcelain
[ "$(git status --porcelain | grep -c '^ M')" = "4" ] || FAIL=1
if git status --porcelain | grep -qE 'SM_Extend|samples'; then FAIL=1; fi
# G6: byte-identity of the untouched (gitignored) artifacts vs the Task-1 baseline
sha256sum -c /tmp/w3r-baseline.sha --quiet || FAIL=1
# G7: preserved seams re-pinned globally
[ "$(grep -cF 'macroTorch.onCpDamageLine' core/events.lua)" -ge "1" ] || FAIL=1
[ "$(grep -cF 'function macroTorch.pairLandIntent' core/spell_trace_core.lua)" = "1" ] || FAIL=1
[ "$(grep -cF 'function macroTorch.pairCpDamageIntent' core/spell_trace_core.lua)" = "1" ] || FAIL=1
[ "$(grep -cF 'accepted residual risk' core/spell_trace_core.lua)" = "1" ] || FAIL=1
[ "$(grep -cF "macroTorch.player.rip('ready')" classes/druid/cat.lua)" = "1" ] || FAIL=1
[ "$(grep -cF 'macroTorch.printConfigBanner' macro_torch.lua)" = "1" ] || FAIL=1
echo "BATTERY FAIL=$FAIL"
```

G1 is the description gate (all spellings of the switch group cleared from
source); G5/G6 together prove SM_Extend.lua and .planning/samples/p1.txt
are byte-identical to the task-start baseline (both are gitignored, so
porcelain alone cannot see them) and that no build script ran. G3/G4 prove
Lua 5.0 tokens absent from added lines and LF preserved. No build script
appears anywhere in this plan; UAT documents are untouched by design and
must not appear in porcelain.
</verification>

<success_criteria>
- T1 gate exits 0: bbcheck BALANCED on macro_torch.lua and classes/druid/cat.lua; rawdiag2 count zero in both; banner at exactly 4 entries; all three count-noun rewrites present with no stale counts; COWER/LOG_MAX_SIZE/cpDamageLog guards intact; safeRip's cast path byte-intact.
- T2 gate exits 0: bbcheck BALANCED on core/events.lua and core/spell_trace_core.lua; rawdiag2 count zero in both; cpDamage gate, tier-1 whitelist, aura-apply dispatch, cast bridge, intent seed, pairLandIntent/pairCpDamageIntent, marker parse, recordLandEvent, and the residual-risk comment all pinned present.
- Battery exits 0 (G1-G7): zero repo-wide source residue; 4x BALANCED; token gate clean; LF clean; porcelain shows exactly the four files; SM_Extend.lua and samples/p1.txt byte-identical to baseline; preserved seams re-pinned.
- One atomic conventional commit contains only the four files, English message, e.g. `refactor: remove RAWDIAG2 landing forensics instrumentation — unified landing verdict covers both apply-line hypotheses (quick 260909-w3r)`.
- In-game validation is staged per user_setup (rebuild SM_Extend.lua on the user's Windows+Cygwin machine, then /mt shows four banner entries with no rawdiag2Enabled line and a normal fight emits no [RAWDIAG2 ...] log lines) — evidence collection on the user's machine, not an offline gate.
</success_criteria>

<output>Create `.planning/quick/260909-w3r-rawdiag2-forensics-instrumentation-remov/260909-w3r-SUMMARY.md` when done
</output>