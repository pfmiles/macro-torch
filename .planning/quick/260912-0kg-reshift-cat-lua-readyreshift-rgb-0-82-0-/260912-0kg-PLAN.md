---
quick_id: 260912-0kg
slug: reshift-cat-lua-readyreshift-rgb-0-82-0-
description: "Two announcement-line render-color tweaks (display layer only, zero judgment-logic change), following the 29-04 precedent: add 'coffee' (RGB 0.82/0.71/0.55) and 'violet' (RGB 0.86/0.44/0.58) custom RGB arms to macroTorch.show in interface_debug.lua, pass the new color names at the two call sites (cat.lua readyReshift 'Reshift!!!', Druid.lua safeFF 'FF!!!'), sync the macroTorch.log @param color-list comment"
date: 2026-09-12
---

# PLAN: Reshift/FF 两条通告行颜色微调（纯渲染层）

## 目标

把两条战斗中通告行的显示颜色从默认白改为指定色：`Reshift!!!` 行渲浅咖啡色、`FF!!!` 行渲浅紫红。实现完全沿用 29-04 gap-closure 建立的自定义 RGB 色臂先例（`macroTorch.show` 映射表新增色触点 + 调用点传色名），不改任何判定逻辑。宏显示层共享同一张映射表，此次只新增两个色臂、只改两个锁定调用点。

## 锁定约束（必须遵守，违反即失败）

1. **只改 3 个文件**：`interface_debug.lua`（色臂 + @param 注释）、`classes/druid/cat.lua`（`readyReshift` 的 show 调用）、`classes/druid/Druid.lua`（`safeFF` 的 show 调用）。其余一律不碰。
2. **色键名与 RGB 值逐字锁定**：色键字符串为 `'coffee'` 与 `'violet'`；`coffee = r 0.82 / g 0.71 / b 0.55`，`violet = r 0.86 / g 0.44 / b 0.58`。不得调换、不得取整。
   - `id` 字段沿用 29-04 惯例（blue→`custom_blue`、green→`custom_green`）：`'custom_coffee'` / `'custom_violet'`。
3. **既有五臂一字不动**：`white/red/yellow/blue/green` 的逐字字面量零改动（T-02 自测以真实 show 断言这五臂，纯增臂不扰动其 planted 断言）。新增两臂插在 `'green'` 臂之后、`end` 之前。
4. **只改两个锁定调用点**：`cat.lua` 的 `readyReshift`（"Reshift!!! energy" 行）传 `'coffee'`；`Druid.lua` 的 `safeFF`（"FF!!!" 行）传 `'violet'`。`bear.lua:61` 的 "Reshift!!! Rage" 行**不在**本次范围，不得改动。
5. **不新增/不翻译注释**：`macroTorch.log` 的 `@param color` 行是既有中文注释，只在颜色列表尾部追加 `, "coffee", "violet"`（纯 ASCII 增量），其中的中文逐字保留。不新增任何注释行；`interface_debug.lua:87-89` 的 Color-label contract 注释**不改**（属 D-14 绿/蓝协议内容）。
6. **Lua 5.0 合规**：新增行不得含 `#` 长度运算符、`goto`、`::` label。代码内不出现字符串拼接语法外的任何语言特性。
7. **禁止运行任何构建脚本**（build.sh 等）；`SM_Extend.lua` 是被 gitignore 的构建产物，全程零触碰（用户 Windows+Cygwin 重建后自然带新色）。
8. LF 行尾；提交信息一律英文，两次原子提交（先渲染层、后调用点）。

## 背景事实（来自本次实地读取 2026-09-12，编辑锚点以此为准）

- `interface_debug.lua` 当前映射表（101-102 行附近）：
  ```lua
      elseif 'green' == col then
          c = { r = 0, g = 1, b = 0, id = 'custom_green' }
      end
  ```
  新增臂插在 `c = { r = 0, g = 1, b = 0, id = 'custom_green' }` 行与 `end` 行之间。
- `macroTorch.log` 的既有注释行（当前第 108 行）：
  `---@param color string 可选颜色: "white"(默认), "red", "yellow", "blue", "green"`
- `cat.lua` `readyReshift` 的 show 调用（394-400 行）末行为：
  `.. ', earning = ' .. tostring(clickContext.RESHIFT_ENERGY - macroTorch.player.mana - clickContext.TIGER_E))`
  旧尾 `TIGER_E))` → 新尾 `TIGER_E), 'coffee')`（tostring 的闭括号与 show 的闭括号之间加第二参数）。
- `Druid.lua` `safeFF` 的 show 调用（1512-1516 行）末行为：
  `.. ', at energy: ' .. macroTorch.player.mana .. ', cp: ' .. tostring(clickContext.comboPoints))`
  旧尾 `comboPoints))` → 新尾 `comboPoints), 'violet')`。
- `'coffee'` / `'violet'` 在仓库 .lua 源文件内零既有出现（无 grep 撞名风险）。
- 本机无 Lua 解释器（仅 node）：行为级验证靠静态电池（bbcheck / Lua 5.0 令牌门 / CRLF / diff 口径门），实机观感由用户 Windows+Cygwin 重建后 `/mt` 目视（unrun-verify 口径）。三目标文件的令牌门与 CRLF 基线均已实测为 0。
- bbcheck 工具路径：`.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js`。

## 任务

编辑一律以**行内容为锚点**（old→new 逐字替换），行号仅作导航参考。

### Task 1: interface_debug.lua 新增 coffee/violet 两臂 + 同步 @param 颜色列表（渲染层）

- **文件**: `interface_debug.lua`
- **操作**（两处）:
  1. 在 `        c = { r = 0, g = 1, b = 0, id = 'custom_green' }` 行之后、`    end` 行之前插入四行：
     ```lua
         elseif 'coffee' == col then
             c = { r = 0.82, g = 0.71, b = 0.55, id = 'custom_coffee' }
         elseif 'violet' == col then
             c = { r = 0.86, g = 0.44, b = 0.58, id = 'custom_violet' }
     ```
  2. `---@param color string 可选颜色: "white"(默认), "red", "yellow", "blue", "green"` 行末追加
     `, "coffee", "violet"`（同一条既有行，仅追加 ASCII；行内既有中文逐字保留）。
- **验证**（全部命中才通过）:
  - `grep -n -A 1 "'coffee' == col" interface_debug.lua` 的下一行恰为 `        c = { r = 0.82, g = 0.71, b = 0.55, id = 'custom_coffee' }`
  - `grep -n -A 1 "'violet' == col" interface_debug.lua` 的下一行恰为 `        c = { r = 0.86, g = 0.44, b = 0.58, id = 'custom_violet' }`
  - `grep -c "'coffee' == col" interface_debug.lua` 输出 `1`；`grep -c "'violet' == col" interface_debug.lua` 输出 `1`
  - `grep -cF '"green", "coffee", "violet"' interface_debug.lua` 输出 `1`（@param 列表追加就位）
  - 既有五臂零扰动: `git diff -U0 HEAD -- interface_debug.lua | grep -cE "custom_blue|custom_green|ChatTypeInfo\["` 输出 `0`
  - diff 新增行 CJK 门: `git diff -U0 HEAD -- interface_debug.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep -c '[一-龥]'` 输出 `1`，且该唯一含 CJK 的新增行就是追加后的 @param 行（由上行 `-cF '"green", "coffee", "violet"'` 与下一条共证）；diff 删除侧: `git diff -U0 HEAD -- interface_debug.lua | grep '^[-]' | grep -v '^[-][-][-]'` 唯一输出 = 旧 @param 行原文
  - bbcheck: `node .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js interface_debug.lua` 输出 BALANCED
  - Lua 5.0 令牌门: `grep -v '^[[:space:]]*--' interface_debug.lua | grep -c '#\|goto \|::'` 输出 `0`（grep 返回码 1 属预期）
  - CRLF: `grep -c $'\r' interface_debug.lua` 输出 `0`
  - `git diff --check` 无输出（LF + 无行尾空白）
- **提交**:
  `git add interface_debug.lua && git commit -m "feat(announce): add coffee/violet custom RGB arms to macroTorch.show"`
- **验证提交**:
  - `git log -1 --format=%s` 输出上述消息
  - `git show --stat HEAD` 仅含 `interface_debug.lua`，且为 `4 insertions(+), 1 deletion(-)`（两臂 4 行新增 + @param 行替换）
- **完成**: interface_debug.lua 映射表由 5 臂扩至 7 臂，两新臂 RGB 字面量与锁定值逐字一致；@param 颜色列表含 coffee/violet；既有五臂与契约注释零字节扰动。

### Task 2: 两个锁定调用点传新色名（cat.lua / Druid.lua）

- **文件**: `classes/druid/cat.lua`、`classes/druid/Druid.lua`（各一处单行尾改）
- **操作**:
  1. `cat.lua` `readyReshift` 内的 show 调用，把
     `.. ', earning = ' .. tostring(clickContext.RESHIFT_ENERGY - macroTorch.player.mana - clickContext.TIGER_E))`
     行尾 `TIGER_E))` 改为 `TIGER_E), 'coffee')`（仅追加第二参数，整条消息串零改动）。
  2. `Druid.lua` `safeFF` 内的 show 调用，把
     `.. ', at energy: ' .. macroTorch.player.mana .. ', cp: ' .. tostring(clickContext.comboPoints))`
     行尾 `comboPoints))` 改为 `comboPoints), 'violet')`。
  3. 明确不动 `bear.lua:61`（"Reshift!!! Rage" 行不在本次范围）。
- **验证**（全部命中才通过）:
  - `grep -c "TIGER_E), 'coffee')" classes/druid/cat.lua` 输出 `1`
  - `grep -c "comboPoints), 'violet')" classes/druid/Druid.lua` 输出 `1`
  - 变更面钉死: `git diff -U0 HEAD -- classes/druid/cat.lua classes/druid/Druid.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep -vc "coffee\|violet"` 输出 `0`（每条新增行都含 coffee 或 violet，即恰好两处第二参改动）；新增行 CJK 门: 同上管道换成 `grep -c '[一-龥]'` 输出 `0`
  - `git diff --stat` 恰两文件（cat.lua + Druid.lua），各 `1 insertion(+), 1 deletion(-)`
  - bbcheck 两文件: `node .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js classes/druid/cat.lua classes/druid/Druid.lua` 输出 BALANCED
  - Lua 5.0 令牌门: `grep -v '^[[:space:]]*--' classes/druid/cat.lua classes/druid/Druid.lua | grep -c '#\|goto \|::'` 输出 `0`
  - CRLF: `grep -c $'\r' classes/druid/cat.lua classes/druid/Druid.lua` 输出 `0 0`（各文件独立计数）
  - `git diff --check` 无输出
- **提交**:
  `git add classes/druid/cat.lua classes/druid/Druid.lua && git commit -m "feat(announce): pass coffee/violet color names at readyReshift/safeFF call sites"`
- **验证提交**:
  - `git log -1 --format=%s` 输出上述消息
  - `git show --stat HEAD` 仅含 `classes/druid/cat.lua` 与 `classes/druid/Druid.lua`
- **完成**: Reshift!!! 行传 `'coffee'`、FF!!! 行传 `'violet'`；两函数消息内容与判定逻辑零改动；bear.lua 与其余 show 调用点零改动。

### Task 3: 收尾一致性电池（三文件联合）

- **操作**: 全部静态门重跑 + 全局一致性核对：
  1. `git status --porcelain` 输出为空（两次提交后零残留；`SM_Extend.lua` 未被触碰——build.sh 未运行）。
  2. `git diff HEAD~2 --shortstat` 输出恰为 ` 3 files changed, 7 insertions(+), 3 deletions(-)`（Task 1 的 4 增 1 删 + Task 2 的 2 增 2 删；任何偏离 = 范围漂移）。
  3. `git diff HEAD~2 --stat` 仅三行：`interface_debug.lua`、`classes/druid/cat.lua`、`classes/druid/Druid.lua`。
  4. 三文件联合 bbcheck BALANCED、令牌门 0、CRLF 0（命令同 Task 1/2，文件列表为三文件）。
  5. `git diff --check` 无输出；`git log -2 --format='%s'` 两条均为英文、与 Task 1/2 锁定的消息逐字一致。
  6. 用户侧口径备案：本机静态电池全绿后，颜色实机观感由用户 Windows+Cygwin 重建后运行 `/mt` 目视（unrun-verify，本机无 Lua 解释器）。
- **验证**: 上述 1-5 各条逐项命中。
- **完成**: 全树干净；变更面恰 3 个预期文件、7 增 3 删；判定逻辑与自测注册零扰动（T-02 断言的既有五臂逐字未动）。

## 失败即回退

- Task 1 验证任一失败 → 不提交，修正至通过；若 diff 出现预期外文件 → 立即 `git checkout -- <file>` 撤销并排查。
- Task 2 若 `git diff --stat` 出现 `bear.lua` 或任何第三文件 → 立即整体撤销该文件改动，仅保留两个锁定文件后重验。
- Task 3 shortstat 偏离 `7 insertions(+), 3 deletions(-)` → 说明出现隐藏范围漂移，`git diff HEAD~2` 全量目视定位后处理；必要时 `git reset --hard HEAD~2` 整体回退重做。
- 全程禁止 build.sh 与对 `SM_Extend.lua` 的任何读写；如需语法自证仅允许 bbcheck / 令牌门 / grep / 目视。