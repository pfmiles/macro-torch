---
phase: quick-260907-0ya-macrotorch-false-true-macrotorch-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - macro_torch.lua
  - classes/druid/Druid.lua
  - classes/druid/selftest.lua
autonomous: true
requirements:
  - QUICK-260907-0YA
user_setup:
  - service: wow-client
    why: "Optional non-blocking in-game confirmation + the actual measurement the feature exists for: (1) run the addon (SuperMacro bound catAtk) and type /mt — S-01..S-03 must pass; (2) create and press a SuperMacro with body `macroTorch.cpBuildLog = true` (press again with `false` to stop, no reload needed, the switch is live), attack a training dummy with catAtk for the measurement window, verify each accepted Claw/Shred/Rake cast prints one `[cpBuild] ` line in chat; (3) after ReloadUI, open WTF/Account/<account>/SavedVariables/SuperMacro.lua — the MACRO_TORCH_LOG.messages table holds the lines for offline interval analysis (t is GetTime(); a t decrease marks a session boundary). Toggling off restores completely silent, unchanged rotation behavior. Prerequisite: the Rake spell (texture 'Ability_Druid_Rake') must sit on an action bar — the same prerequisite the existing macroTorch.isGcdOk rotation logic already has; without it GCD-ready casts are simply not logged."
must_haves:
  truths:
    - "With macroTorch.cpBuildLog false (the default, re-armed on every login by the macro_torch.lua nil-guard), every macro path behaves exactly as today: the `macroTorch.cpBuildLog and ...` short-circuit means cpBuildLogSample is never invoked, no extra API calls, no output, and no loop file (cat.lua/combo.lua) is modified at all"
    - "With macroTorch.cpBuildLog true, exactly one `[cpBuild] <Skill> t=<GetTime> cp=<n> e=<n>` line is appended to chat and to MACRO_TORCH_LOG.messages for every Claw/Shred/Rake cast the client accepts (GCD-ready at the sampled moment, readiness/resource/range checks passed), wherever the cast comes from (catAtk, catLeveling, druidMobTagging) because all three flow through the single choke point per skill: obj.claw/obj.shred/obj.rake"
    - "Cast attempts that are GCD-blocked, or that fail _castSpell's checks, or casts of other skills (Rip/Ferocious Bite/FF/Pounce/...) append nothing"
  artifacts:
    - macro_torch.lua
    - classes/druid/Druid.lua
    - classes/druid/selftest.lua
  key_links:
    - "obj.claw / obj.shred / obj.rake (one choke point per skill, grounded by a live call-site scan: cat.lua:50-58, cat.lua:411, combo.lua:398, leveling.lua:92-213 and no CastSpellByName bypass) -> macroTorch.cpBuildLogSample (pre-cast GCD probe must precede the cast or the started GCD falsifies it) -> macroTorch.cpBuildLogEvent -> macroTorch.log (interface_debug.lua:103) -> MACRO_TORCH_LOG SavedVariable"
    - "macroTorch.cpBuildLog -> nil-guarded default false in macro_torch.lua (first file of build_order.txt), so the switch is armed at addon load before any macro body can run"
    - "GCD probe macroTorch.player.isActionCooledDown('Ability_Druid_Rake') -> same action-slot 'Ability_Druid_Rake' texture scan as macroTorch.isGcdOk (Druid.lua:1263), so the log's cast-acceptance gate agrees with the rotation's own GCD semantics"
---

<objective>
Add a macroTorch-namespaced boolean switch `macroTorch.cpBuildLog` (default false,
set true by hand in game, re-toggleable any time) that turns on combo-point
building cast logging for the cat-form DPS rotation: every accepted
Claw/Shred/Rake cast appends one persisted `[cpBuild] <Skill> t=<GetTime()>
cp=<n> e=<n>` line through the existing `macroTorch.log` (interface_debug.lua)
so the user can later compute, offline, the average wait interval k between two
consecutive combo-building casts and its window distribution, stratified by
combo points and energy — the measurement needed to settle the double-bleed
maintenance criterion (双保判据).

Purpose: give the player an opt-in, zero-cost-when-off measurement tap at the
three combo-building skills' single choke points, without touching the loop
semantics of catAtk at all (R4 locked).

Output: `macro_torch.lua` gains the nil-guarded default-false switch;
`classes/druid/Druid.lua` gains two tiny helpers (cpBuildLogSample,
cpBuildLogEvent) and a two-line hook inside each of obj.claw/obj.shred/obj.rake;
`classes/druid/selftest.lua` gains Category S (S-01..S-03). No other file
changes. SM_Extend.lua is NOT rebuilt (established convention: the build
artifact is only regenerated on an explicit project rebuild; recent quick tasks
did not touch it).
</objective>

<execution_context>@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@interface_debug.lua
@classes/druid/Druid.lua
@classes/druid/selftest.lua

Grounding facts, all verified by reading at planning time (do not re-verify
blindly; the plan's anchors are live):

- Persistence already exists (R3 satisfied — nothing to wrap): `macroTorch.log(a,
  color)` at interface_debug.lua:103 shows in chat AND appends tostring(a) to
  `MACRO_TORCH_LOG.messages` (SavedVariable, capped 500 via macroTorch.tableLen
  round; initialized lines 17-20 and re-guarded inside log). Logs land in
  WTF/.../SavedVariables/SuperMacro.lua on logout/reload. DO NOT modify this
  file.
- Single choke point per skill: every Claw/Shred/Rake cast in the repo flows
  through `obj.claw(mode, rank)` / `obj.shred` / `obj.rake` in
  `macroTorch.Druid:new()` (classes/druid/Druid.lua lines 25-35), which delegate
  to `obj._castSpell` (entity/Player.lua:38-85). Live call-site scan: cat.lua
  50/52/56/58 (macroTorch.regularAttack — the single Claw/Shred decision point),
  cat.lua:411 (safeRake), combo.lua:398 (druidMobTagging), leveling.lua
  92/95/166/196/202/208/213 (catLeveling). No CastSpellByName('Claw/Shred/Rake')
  bypass exists. Hooking the three methods is therefore ONE choke point per
  skill and covers every caller while the toggle is on.
- GCD is NOT covered by `obj.isSpellReady` (SpellReady + GetSpellCooldown==0,
  entity/Player.lua:177-181 + biz_util.lua:150-158) — regularAttack issues its
  cast without a GCD check today, so a log line written naively at cast-issue
  time would include phantom entries under fast key spam. The codebase's proven
  GCD probe is the action-slot texture scan `isActionCooledDown` (defined
  obj-level at entity/Player.lua:186, impl interface_debug.lua:24-31, slots
  1..172). `macroTorch.isGcdOk(clickContext)` (Druid.lua:1263-1268) uses exactly
  `macroTorch.player.isActionCooledDown('Ability_Druid_Rake')`. Reuse the same
  probe, clickContext-free, sampled BEFORE the cast.
- Sampled state: `macroTorch.player.comboPoints` (DRUID_FIELD_FUNC_MAP,
  Druid.lua:316-318 → GetComboPoints() or 0) and `macroTorch.player.mana`
  (Unit field map → UnitMana) give pre-cast cp/energy for stratification.
  GetTime() gives seconds with ms precision; it resets per login, so a t
  decrease in a log file marks a session boundary (analysis segments on that).
- Druid.lua line anchors: `-- Cat form skills (Type A: enemy target only)` at
  line 24; claw/shred/rake at lines 25-35 (byte-exact current bodies shown in
  Task 1); `macroTorch.Druid:new()` closes with `    return obj` / `end` at
  lines 310-311; line 313 is `-- player fields to function mapping` followed by
  `macroTorch.DRUID_FIELD_FUNC_MAP = {` at 314 — the helpers go in the gap
  (line 312), i.e. between line 311 and line 313.
- macro_torch.lua is 20 lines; its last lines (17-20) are the comment
  `-- some machanisms impl` and `if not macroTorch then macroTorch = {} end`.
  It is the first file in build_order.txt, so addon-load init belongs here.
- classes/druid/selftest.lua: whole file body wrapped in
  `if UnitClass('player') == 'Druid' then` (line 19) ... `end` (file-closing
  line 987). Category Q end: Q-10 register closes `end, true)` at line 985.
  Insertion point for Category S: the blank line region immediately before the
  final `end` at line 987 (after line 985, leaving line 986 blank). Q-block
  registers are indented with TABS — match them. Stub discipline = CR-01 used
  by Q-02..Q-10 and P-02 (rawget snapshot, restore via raw assignment BEFORE
  any assert so a failing assert can never pollute the session).
- Verification tooling grounded live at planning time: node v24 +
  `.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js`
  prints `BALANCED` and exits 0 on all three unmodified target files. No
  lua/luac/luajit exists on this host (established: syntax gate = bbcheck +
  token gates + in-game /mt).
- Repo conventions: English comments, LF endings, Lua 5.0 only (no `#` length
  operator — use macroTorch.tableLen / table.insert — no goto, no long-string
  escapes), comments explain intent, no changes to cat.lua/combo.lua (R4
  locked: loop semantics untouched).
</context>

<tasks>

<task type="auto">
  <name>Add macroTorch.cpBuildLog switch (macro_torch.lua) and the Claw/Shred/Rake cast-log hooks (Druid.lua)</name>
  <files>macro_torch.lua, classes/druid/Druid.lua</files>
  <action>
Edit only the two listed files. Do NOT touch cat.lua, combo.lua, entity/Player.lua, interface_debug.lua, core/events.lua or SM_Extend.lua — R4 (locked): the switch off must leave loop behavior byte-identical, and the established convention says quick tasks do not rebuild SM_Extend.lua.

PART 1 — macro_torch.lua (R1 locked: boolean switch under the macroTorch namespace, default false): append, after the current final line (line 20, the `end` of the `if not macroTorch then` guard), one blank line and then the block:

1. Comment: `-- combo-point building cast log switch (quick 260907-0ya): false by default, set`
2. Comment: `-- macroTorch.cpBuildLog = true in game (SuperMacro body) to record every accepted`
3. Comment: `-- Claw/Shred/Rake cast through macroTorch.log for offline interval analysis.`
4. Comment: `-- The nil-guard re-arms the default on every login; toggling mid-session needs no reload.`
5. `if macroTorch.cpBuildLog == nil then`
6. `    macroTorch.cpBuildLog = false`
7. `end`

The nil-guard (not unconditional assignment) is load-bearing: anything the player sets in game later in the session wins; only a fresh addon load re-arms false.

PART 2 — classes/druid/Druid.lua, two edits:

EDIT A — replace the three skill method bodies at lines 25-35 (between the line-24 comment `-- Cat form skills (Type A: enemy target only)` and the `function obj.rip` at line 37). Current bodies, byte-exact, each one `return obj._castSpell(...)` line. Replace them with (4-space indent, keep the `{ en = ..., zh = ... }` locale tables byte-identical to today, keep `macroTorch.computeClaw_E` etc. references, keep the blank lines between methods, add no trailing whitespace):

`function obj.claw(mode, rank)` followed by:
1. `local cpLog = macroTorch.cpBuildLog and macroTorch.cpBuildLogSample() or nil`
2. `local cast = obj._castSpell({ en = 'Claw', zh = '爪击' }, mode, nil, macroTorch.computeClaw_E, false, rank)`
3. `if cast and cpLog and cpLog.gcdOk then`
4. `macroTorch.cpBuildLogEvent('Claw', cpLog)`
5. `end`
6. `return cast`
7. `end`

Then `function obj.shred(mode, rank)` with the identical shape but `'Shred'`, `'撕碎'`, macroTorch.computeShred_E, and the line-4 call `macroTorch.cpBuildLogEvent('Shred', cpLog)`. Then `function obj.rake(mode, rank)` with `'Rake'`, `'斜掠'`, macroTorch.computeRake_E, and `macroTorch.cpBuildLogEvent('Rake', cpLog)`.

Why two lines instead of one return: the GCD/cp/energy sample (line 1) MUST happen before _castSpell — the instant cast momentarily starts the GCD, and a post-cast sample would report gcdOk=false and silently drop the line; the `and ... or nil` short-circuit makes the off-state a pure local-nil assignment (no API calls at all, R4's identical-behavior guarantee).

EDIT B — insert the two helpers in the gap between line 311 (the `end` closing macroTorch.Druid:new) and line 313 (the `-- player fields to function mapping` comment), with one blank line before and after the block. File-indent level 0, plain tabs nowhere (file uses 4-space indentation at this level):

1. Comment: `-- Sample the pre-cast state for the cpBuild log (quick 260907-0ya). Only invoked`
2. Comment: `-- when macroTorch.cpBuildLog is true. The GCD probe must run BEFORE the cast`
3. Comment: `-- because casting starts the GCD and would falsify the reading; the probe reuses`
4. Comment: `-- the same action-slot 'Ability_Druid_Rake' texture scan as macroTorch.isGcdOk.`
5. `function macroTorch.cpBuildLogSample()`
6. `return {`
7. `t = GetTime(),`
8. `cp = macroTorch.player.comboPoints,`
9. `e = macroTorch.player.mana,`
10. `gcdOk = macroTorch.player.isActionCooledDown('Ability_Druid_Rake')`
11. `}`
12. `end`
13. blank line
14. Comment: `-- Emit one persisted [cpBuild] line per accepted Claw/Shred/Rake cast.`
15. Comment: `-- The field order skill t cp e is fixed; offline tooling parses it for the`
16. Comment: `-- inter-cast interval k and its window distribution (R2 locked).`
17. `function macroTorch.cpBuildLogEvent(skillName, s)`
18. `macroTorch.log('[cpBuild] ' .. skillName .. ' t=' .. s.t .. ' cp=' .. s.cp .. ' e=' .. s.e)`
19. `end`

The format satisfies R2: skill name and cast timestamp mandatory, combo points and energy included as suggested. R3 is satisfied by macroTorch.log itself (persisted through MACRO_TORCH_LOG; already verified to exist — no new persistence machinery).

Explicitly do NOT: add any GCD/resource/range check to _castSpell or to any caller (that would change loop semantics if the flag is off — R4); log Rip/Ferocious Bite/FF/Pounce (R2 is Claw/Shred/Rake only); use the # operator, goto, or any Lua 5.1+ syntax anywhere; use non-ASCII characters in code except the pre-existing zh locale literals which stay byte-identical.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB macro_torch.lua && node $BB classes/druid/Druid.lua && grep -A2 'cpBuildLog == nil' macro_torch.lua | grep -q 'macroTorch.cpBuildLog = false' && test "$(grep -v '^--' classes/druid/Druid.lua | grep -c 'macroTorch.cpBuildLogSample() or nil')" = "3" && test "$(grep -vE '^--' classes/druid/Druid.lua | grep -cE "cpBuildLogEvent\\('(Claw|Shred|Rake)'")" = "3" && grep -qF "t = GetTime()," classes/druid/Druid.lua && grep -qF "cp = macroTorch.player.comboPoints," classes/druid/Druid.lua && grep -qF "e = macroTorch.player.mana," classes/druid/Druid.lua && grep -qF "gcdOk = macroTorch.player.isActionCooledDown('Ability_Druid_Rake')" classes/druid/Druid.lua && grep -qF "macroTorch.log('[cpBuild] ' .. skillName .. ' t=' .. s.t .. ' cp=' .. s.cp .. ' e=' .. s.e)" classes/druid/Druid.lua && echo "T1 GATE OK"
    </automated>
  </verify>
  <done>
bbcheck prints BALANCED and exits 0 for both macro_torch.lua and classes/druid/Druid.lua. All awk/grep gates exit 0 in one '&&' chain: the nil-guarded `macroTorch.cpBuildLog = false` default exists; Druid.lua contains exactly 3 `macroTorch.cpBuildLogSample() or nil` short-circuit hook lines (one per skill method), exactly 3 `macroTorch.cpBuildLogEvent(` calls, the sample helper returns the four mandated fields (t/cp/e/gcdOk with GetTime/comboPoints/mana/isActionCooledDown('Ability_Druid_Rake')), and the event helper's log call formats `[cpBuild] ` + skill + ` t=` + ` cp=` + ` e=`. git diff shows only the three wrapper bodies replaced, the two helpers inserted, and no change to any other function in the file.
  </done>
</task>

<task type="auto">
  <name>Register Category S selftests (S-01 switch default, S-02 line format, S-03 off/GCD log gating) in classes/druid/selftest.lua</name>
  <files>classes/druid/selftest.lua</files>
  <action>
In `classes/druid/selftest.lua`, insert the Category S block in the blank-line region AFTER the Q-10 register (closes `end, true)` at line 985) and BEFORE the file-closing `end` at line 987 (the closer of the `if UnitClass('player') == 'Druid' then` guard from line 19). Leave line 985 and line 987 untouched; do not modify any other line of the file. TABS for indentation exactly like the neighboring Q registrations; English comments; LF endings; Lua 5.0 only (no `#` operator, no goto).

The block is: one 3-line header comment, three `macroTorch.SelfTest:register(...)` calls (all `isOptional=true`, matching the stub-based convention), one trailing count comment. Register name strings must be byte-identical to the ones below — the verification gate greps them.

Header comment (3 lines):

1. `-- Category S: cpBuildLog combo-point cast logging (quick 260907-0ya, 3 tests)`
2. `-- S-03 follows the CR-01 stub discipline: snapshot via rawget, install own-key`
3. `-- shadows, capture into locals, restore via raw assignment BEFORE any assert.`

Test S-01, name string exactly `"Cat S-01: cpBuildLog switch defaults to false"`:
- Body: `assert(macroTorch.cpBuildLog == false,` then the continuation line `"cpBuildLog should default to false, got " .. tostring(macroTorch.cpBuildLog))`. This pins R1's default (macro_torch.lua runs at addon load, before the deferred selftest run).

Test S-02, name string exactly `"Cat S-02: cpBuildLogEvent emits the fixed [cpBuild] line format"`:
- Locals: `local savedLog = macroTorch.log` and `local captured = nil`.
- Stub `macroTorch.log = function(a)` with body `captured = tostring(a)` and `end`.
- `local pcallRes = pcall(function()` then call `macroTorch.cpBuildLogEvent('Claw', { t = 12.34, cp = 3, e = 62 })` then the pcall `end)`.
- Restore BEFORE asserts: `macroTorch.log = savedLog`.
- Asserts: `assert(pcallRes, "S-02 pcall failed")` and `assert(captured == '[cpBuild] Claw t=12.34 cp=3 e=62', "S-02 unexpected log line: " .. tostring(captured))`. The expected literal pins the parseable format (skill, then t, then cp, then e — R2).

Test S-03, name string exactly `"Cat S-03: claw() logs only when switch on and GCD ready"`:
- Locals: `local player = macroTorch.player`; `local savedSwitch = macroTorch.cpBuildLog`; `local savedLog = macroTorch.log`; `local savedCast = rawget(player, '_castSpell')`; `local savedActionCd = rawget(player, 'isActionCooledDown')`; `local captured = {}`; plus counters `local nOn, nGcd, nOff = 0, 0, 0`.
- Stub `macroTorch.log = function(a)` with body `table.insert(captured, tostring(a))` and `end` (log appends are cumulative by design; counters read the delta).
- Stub `player._castSpell = function()` returning `true` and `end` — own-key shadow on the Druid instance whose metatable chain normally exposes the Player singleton's real _castSpell; this is the CR-01 pattern (rawget snapshot was nil, the nil restore below deletes the shadow).
- Stub `player.isActionCooledDown = function()` returning `true` and `end`.
- Inside `local pcallRes = pcall(function())`: first `macroTorch.cpBuildLog = true`, then `player.claw('ready')`, then `nOn = macroTorch.tableLen(captured)` (expect 1: switch on, GCD ready, cast stubbed true -> one log). Then re-stub `player.isActionCooledDown = function()` returning `false` and `end`, call `player.claw('ready')`, then `nGcd = macroTorch.tableLen(captured) - nOn` (expect 0: the pending cast is never accepted so no line is emitted). Then `macroTorch.cpBuildLog = false`, call `player.claw('ready')`, then `nOff = macroTorch.tableLen(captured) - nOn - nGcd` (expect 0: off-state emits nothing and — per the short-circuit — never even samples). Close the pcall `end)`.
- Restore BEFORE asserts (CR-01): `rawset(player, '_castSpell', savedCast)`; `rawset(player, 'isActionCooledDown', savedActionCd)`; `macroTorch.log = savedLog`; `macroTorch.cpBuildLog = savedSwitch`.
- Asserts: `assert(pcallRes, "S-03 pcall failed")`; `assert(nOn == 1, "expected exactly one cpBuild log when the switch is on and GCD is ready, got " .. tostring(nOn))`; `assert(nGcd == 0, "expected no cpBuild log when GCD is not ready, got " .. tostring(nGcd))`; `assert(nOff == 0, "expected no cpBuild log when the switch is off, got " .. tostring(nOff))`.
- Each register call closes `end, true)` exactly like the neighboring Q registers.

Trailing count comment: `-- Registration count: Category S adds 3 tests (quick 260907-0ya)`.

Rationale for S-03's shape: claw('ready') drives the real obj.claw wrapper (real hook code under test), with real game calls GetTime/GetComboPoints/UnitMana safe in-game, and only _castSpell + isActionCooledDown + log stubbed. The three phases lock the three user requirements: on+ready logs exactly one (R2), GCD-blocked logs none (correct interval data, no phantom entries), off logs none and short-circuits the sample (R4 behavioral proof). No combat state is required, so it runs on a fresh login like the other optional categories.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && BB=.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js && node $BB classes/druid/selftest.lua && test "$(grep -c '"Cat S-0' classes/druid/selftest.lua)" = "3" && grep -qF 'Category S adds 3 tests' classes/druid/selftest.lua && grep -qF "rawset(player, '_castSpell', savedCast)" classes/druid/selftest.lua && grep -qF "assert(captured == '[cpBuild] Claw t=12.34 cp=3 e=62'" classes/druid/selftest.lua && awk '/-- Category S:/,/Category S adds 3 tests/ {if ($0 ~ /macroTorch\.cpBuildLog == false/) a=1; if ($0 ~ /restore via raw assignment BEFORE any assert/) b=1; if ($0 ~ /nOn == 1/) c=1; if ($0 ~ /nGcd == 0/) d=1; if ($0 ~ /nOff == 0/) e=1} END {exit !(a&&b&&c&&d&&e)}' classes/druid/selftest.lua && echo "T2 GATE OK"
    </automated>
  </verify>
  <done>bbcheck prints `classes/druid/selftest.lua: BALANCED` (exit 0). grep counts exactly 3 `"Cat S-0` register lines (S-01..S-03), finds the `Category S adds 3 tests` count comment, finds the CR-01 `_castSpell` rawset-restore line and the byte-exact expected-format literal; the awk range gate (scanning only `-- Category S:` through the count comment) confirms the block asserts the false default, declares the restore-before-asserts discipline, and carries the nOn==1 / nGcd==0 / nOff==0 assertions. git diff shows only the Category S insertion region between line 985 and line 987 added; no other line in the file changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed. The feature writes only to the addon's own MACRO_TORCH_LOG
SavedVariable and chat via the existing macroTorch.log, is opt-in via an
explicit in-game assignment by the player, and only records the player's own
combat statistics on demand. No external input is consumed, no package
installs, no privilege context.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260907-0ya-01 | Information Disclosure | classes/druid/Druid.lua cpBuildLogEvent chat+log lines (skill, time, cp, energy) | low | accept | Only the player's own cast metadata, shown in their own chat and persisted in their own WTF SavedVariables file; the feature is off by default and requires a deliberate in-game opt-in; data is exactly what the existing macroTorch.show diagnostic lines already print episode-wise |
| T-quick-260907-0ya-02 | Denial of Service | macroTorch.log bounded buffer (MACRO_TORCH_LOG.maxSize = 500) | low | accept | A long measurement session rotates the oldest lines by design (existing macroTorch.log loop); worst case the player loses the oldest logged interval samples, no storage or UI impact since the buffer cap existed before this feature |
</threat_model>

<verification>
After both tasks, confirm the combined diff is exactly right:

```bash
cd /home/admin/workspace/macro-torch && git status --porcelain
# source files: exactly `M macro_torch.lua`, `M classes/druid/Druid.lua`,
# `M classes/druid/selftest.lua` — nothing else, SM_Extend.lua must NOT appear

cd /home/admin/workspace/macro-torch && git diff --check
# exit 0, no output — LF preserved, no trailing whitespace

cd /home/admin/workspace/macro-torch && git diff --stat
# roughly: macro_torch.lua +7, Druid.lua ~+45/-15, selftest.lua +75
```

Lua 5.0 token gate over the whole diff (requirement 5 mandatory syntax step,
alongside the two bbcheck runs which are the repo's established syntax gate
proxy in the absence of a headless Lua 5.0 runtime):

```bash
cd /home/admin/workspace/macro-torch && if git diff -U0 -- macro_torch.lua classes/druid/Druid.lua classes/druid/selftest.lua | grep -E '^\+' | grep -vE '^\+\+' | grep -qE '\bgoto\b|#|::'; then echo "T3 FORBIDDEN TOKEN FOUND"; exit 1; else echo "T3 LUA50 TOKEN GATE OK"; fi
# must print T3 LUA50 TOKEN GATE OK (no goto, no length #, no labels added)
```

Re-run the T1 and T2 task gates; both must exit 0 with their `GATE OK` echo.
Loop-semantics proof (R4): `git diff --stat -- classes/druid/cat.lua classes/druid/combo.lua entity/Player.lua core/events.lua` shows no changes at all —
the rotation files are untouched; the off-state short-circuit in the wrappers
is the one-line guard `macroTorch.cpBuildLog and ...` which adds a nil check
only at the three hooks. In-game behavior (S-01..S-03 on /mt, plus the manual
toggle + training-dummy measurement and SavedVariables retrieval) is confirmed
per the user_setup note — the measurement itself is the user's intended
follow-up and cannot be automated offline.
</verification>

<success_criteria>
- T1 gate exits 0: bbcheck BALANCED on both macro_torch.lua and classes/druid/Druid.lua; nil-guarded `macroTorch.cpBuildLog = false` present; exactly 3 `cpBuildLogSample() or nil` hooks and 3 `cpBuildLogEvent(` calls; sample helper has t/cp/e/gcdOk fields; event helper formats `[cpBuild] ` + skill + ` t=` + ` cp=` + ` e=` through macroTorch.log.
- T2 gate exits 0: bbcheck BALANCED on classes/druid/selftest.lua; exactly 3 `"Cat S-0` registrations; `Category S adds 3 tests` comment; CR-01 rawset-restore line; expected-format literal present; S-01/S-03 assertion set complete within the Category S range.
- T3 token gate prints `T3 LUA50 TOKEN GATE OK` — the diff adds no `goto`, no `#`, no labels (Lua 5.0 constraint, user memory wow-lua50-syntax).
- `git status --porcelain` shows only the three source files; SM_Extend.lua not rebuilt; `git diff --check` clean (LF).
- cat.lua, combo.lua, Player.lua, core/events.lua, interface_debug.lua untouched — loop semantics byte-identical when the switch is off (R4).
- One atomic conventional commit, e.g. `feat(druid): add macroTorch.cpBuildLog switch and Claw/Shred/Rake cast logging (quick 260907-0ya)`, containing only the three allowed files.
</success_criteria>

<output>Create `.planning/quick/260907-0ya-macrotorch-false-true-macrotorch-log/260907-0ya-SUMMARY.md` when done
</output>