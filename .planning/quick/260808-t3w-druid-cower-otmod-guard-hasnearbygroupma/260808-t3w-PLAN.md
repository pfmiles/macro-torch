# Quick Task 260808-t3w: druid cower otMod guard — hasNearbyGroupMates

**Date:** 2026-08-08
**Mode:** quick

## Description

在 `biz_util.lua` 新增 `hasNearbyGroupMates(rangeYards)` 工具函数，并修改 `classes/druid/cat.lua` 的 `otMod`：
(a) 添加附近队友距离守卫（无队友在 60 码内 → 跳过降仇恨）
(b) 无敌药水后加 `return`（避免同 tick 继续尝试 cower）

## Tasks

### Task 1: 新增 hasNearbyGroupMates 工具函数

- **files:** `biz_util.lua`
- **action:** 在 `filterGroupMates` 函数之后（约第 248 行）插入 `hasNearbyGroupMates(rangeYards)` 函数。使用 `isFunctionExist('UnitXP')` 守卫，UnitXP 不可用时返回 `true`（退化放行）。
- **verify:** 函数可通过 `macroTorch.hasNearbyGroupMates(60)` 调用，逻辑正确。

### Task 2: 修改 otMod 添加守卫

- **files:** `classes/druid/cat.lua`
- **action:** (a) 在 `isInGroup` 检查后添加 `hasNearbyGroupMates(60)` 守卫；(b) 无敌药水 `player.use('Invulnerability Potion', true)` 后添加 `return`。
- **verify:** otMod 逻辑正确——附近无队友时跳过、无敌药水后 return。