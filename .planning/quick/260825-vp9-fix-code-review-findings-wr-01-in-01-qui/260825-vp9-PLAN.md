---
phase: quick-260825-vp9-fix-code-review-findings-wr-01-in-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/cat.lua
  - core/selftest.lua
autonomous: true
requirements:
  - QUICK-260825-VP9
user_setup:
  - service: wow-client
    why: "The self-test suite (incl. P-06, the t86 discharge-defer test and the new IN-01 liveness test) only runs in-game via /mt; no headless Lua runtime exists in this environment (established convention from quick 260825-t86). Non-blocking — confirmed on the user's next login."
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "quickKeepRip honors the energyDischargeBeforeBite boolean exactly like cp5Bite: a true return (discharge attempted) defers the bite verdict to the next keystroke; a false return falls through to safeBite unchanged (WR-01)"
    - "The new P selftest proves liveness: with shouldDischarge == true and the REAL energyDischargeBeforeBite returning false (ooc=false, isBehind=false, mana below BITE_E+CLAW_E, isRakePresent stubbed true, ripLeft stubbed > 2.3), cp5Bite still falls through and calls the bite (IN-01)"
    - "P-06 and the t86 'cp5Bite defers the bite when a discharge is attempted' test stay byte-identical and keep passing; the Category P registration count comment reads 8 (updated from 7)"
  artifacts:
    - classes/druid/cat.lua
    - core/selftest.lua
  key_links:
    - "quickKeepRip cp>=3 branch -> energyDischargeBeforeBite boolean -> return on true (bite deferred), safeBite on false"
    - "New selftest real-discharge-false -> cp5Bite fall-through -> safeBite stub biteCalled marker (liveness, no real spell fires)"
    - "catAtk module order (oocMod/termMod run before quickKeepRip in the same keystroke) means the deferred next-click verdict naturally re-enters termMod/cp5Bite, whose deferral is already correct — no deadlock once the OoC proc subsides"
---

<objective>
Close the two remaining code-review findings of quick 260825-t86 (review
`.planning/quick/260825-t86-catatk-bug-a-b-r-locked-energydischargeb/260825-t86-REVIEW.md`,
commit 5d37068):

1. **WR-01** — `quickKeepRip` (classes/druid/cat.lua, currently lines 337-338)
   calls `macroTorch.energyDischargeBeforeBite(clickContext)`, discards the new
   boolean return and immediately calls `macroTorch.safeBite(clickContext)`,
   keeping the OoC discharge-bypass hole open on its own bite path. Fix: honor
   the return contract exactly as `cp5Bite` does — the single guard insertion
   `if macroTorch.energyDischargeBeforeBite(clickContext) then return end`
   placed before `safeBite` (a discharge-attempt frame defers the bite verdict;
   the next keystroke re-samples it, mirroring the cp5Bite pattern).
2. **IN-01** — the selftest suite never exercises the `return false` tail of the
   real `energyDischargeBeforeBite` with `shouldDischarge == true` (the
   no-condition-matched fall-through the deferral fix relies on for liveness).
   Fix: one new Category P test mirroring P-06's structure, leaving the
   discharge helper real, asserting `cp5Bite` still falls through and bites.

Purpose: the t86 boolean contract was correct in `cp5Bite` but the second call
site still bit through an attempted discharge; and the liveness invariant ("a
false return must still reach the bite") had zero coverage — a future regression
making the false tail accidentally defer would pass the suite silently.

Locked fix spec (do NOT redesign — implemented exactly as decided):
- WR-01 is the single guard insertion mirroring cp5Bite; only quickKeepRip's
  cp>=3 branch changes, nothing else in cat.lua.
- IN-01 test stubs only what P-06-style tests stub, leaves
  `energyDischargeBeforeBite` REAL, and pins `macroTorch.player.mana` with the
  rawget/own-key/rawset pattern (the mana property is a live `UnitMana`
  metatable accessor — see context notes).
- Both existing P tests (P-06 and t86's discharge-defer test) must stay
  byte-identical and passing; the registration count comment is updated 7 -> 8.
- Code comments and the commit message in English (ASCII preferred);
  classes/druid/cat.lua is CRLF — the edit must keep the file all-CRLF.
- `bash build.sh` must exit 0; one atomic commit containing only
  classes/druid/cat.lua + core/selftest.lua (SM_Extend.lua is a gitignored
  build artifact, never staged).

Output: modified `classes/druid/cat.lua` (quickKeepRip guard per WR-01) and
`core/selftest.lua` (one new Category P liveness test per IN-01 + count comment
update), verified and committed atomically.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@classes/druid/cat.lua
@core/selftest.lua
@classes/druid/combo.lua
@.planning/quick/260825-t86-catatk-bug-a-b-r-locked-energydischargeb/260825-t86-REVIEW.md

# The fix spec is complete and LOCKED — see the <objective>. Do not redesign.

# Current file state (line anchors, pre-change):
#   cat.lua 113-151: cp5Bite — the reference deferral shape for WR-01:
#                    `if shouldDischarge then ... if macroTorch.energyDischargeBeforeBite(clickContext)
#                    then return end ... end` at 134-142, followed by the unchanged
#                    ooc -> readyBite / else -> safeBite selection at 145-149
#   cat.lua 152-187: energyDischargeBeforeBite — return contract: isDischarged re-entry
#                    guard -> true (158-160), isPseudoInfiniteEnergy skip -> false (163-165),
#                    regularAttack branch -> true (168-174), Rake fallback -> true (179-183),
#                    no-condition tail -> false (185-186)
#   cat.lua 332-350: quickKeepRip — WR-01 target. cp>=3 branch currently at 336-340:
#                    `macroTorch.energyDischargeBeforeBite(clickContext)` at 337,
#                    `macroTorch.safeBite(clickContext)` at 338, `return` at 339.
#                    The dispatch is combo.lua 163-167 (module 10: only when
#                    not isFastBattleNotPvp and isTrivialBattleOrPvp); clickContext
#                    is a fresh table rebuilt per keystroke at combo.lua 55, so
#                    isDischarged is frame-scoped with no reset logic.
#   selftest.lua 783-815: P-06 "P: cp5Bite triggers bite at 5CP in fast battle without
#                    Rip or immunity" — must stay byte-identical.
#   selftest.lua 817-852: t86 test "P: cp5Bite defers the bite when a discharge is
#                    attempted" — must stay byte-identical.
#   selftest.lua 854: registration count comment — "Category P adds 7 tests
#                    (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86)" -> becomes 8.
#   selftest.lua is LF; classes/druid/cat.lua is CRLF (473/473 lines carry CR).

# Key implementation facts (verified):
#   - macroTorch.isRipPresent / ripLeft / isRakePresent are plain macroTorch.*
#     global functions (Druid.lua 1095/1104/1130) — stubbing by capture+assign
#     works exactly like P-06 stubs isRipPresent.
#   - macroTorch.player.mana is NOT a plain field: the player object's metatable
#     (core/class.lua classMetatable -> entity/Unit.lua UNIT_FIELD_FUNC_MAP)
#     resolves mana through a live UnitMana(self.ref) accessor on every read.
#     The new test must pin it with an own-key shadow: snapshot via
#     `rawget(macroTorch.player, 'mana')`, assign 50, restore via
#     `rawset(macroTorch.player, 'mana', origMana)` — the nil restore deletes
#     the own key so the live accessor stays active (same pattern as the
#     Phase 26-03 CR-01 fix for macroTorch.target.isPlayerControlled, which uses
#     `rawget(macroTorch.target, ...)` at selftest.lua 706).
#   - classMetatable defines only __index (no __newindex), so plain assignment
#     to the absent key writes a real own key — the shadow works.
#   - With isRipPresent stubbed true (short-circuit) and isRakePresent stubbed
#     true (short-circuit), macroTorch.isFastBattleNotPvp is provably NEVER
#     evaluated in this test — no stub needed for it, and no regularAttack/
#     safeRake stub is needed because both cast branches are provably skipped.
</context>

<tasks>

<task type="auto">
  <name>Task 1: quickKeepRip honors the discharge boolean guard (WR-01)</name>
  <files>classes/druid/cat.lua</files>
  <action>
    Edit ONLY `macroTorch.quickKeepRip` (classes/druid/cat.lua, currently lines
    332-350, CRLF line endings). ONE localized Edit; every other line of the
    file stays exactly as-is. Anchor by content, not line number.

    Replace the two lines that currently read exactly
        macroTorch.energyDischargeBeforeBite(clickContext)
        macroTorch.safeBite(clickContext)
    (currently lines 337-338, 8-space indent, inside the `if clickContext.comboPoints
    >= 3 and ...` branch) with the guarded shape that mirrors cp5Bite (WR-01,
    locked — the exact review fix):

        -- Discharge attempted -> defer the bite verdict to the next click (WR-01:
        -- same contract as cp5Bite; re-sampled on the next keystroke so a rejected
        -- discharge cast is never followed by a same-frame bite at full energy)
        if macroTorch.energyDischargeBeforeBite(clickContext) then
            return
        end
        macroTorch.safeBite(clickContext)

    Rules (locked, non-negotiable):
    - This is the single WR-01 change: the guard `if ... then return end`
      inserted before `safeBite`. Do NOT touch the branch's `if` condition line,
      the trailing `return`, the `-- For cp >= 3: discharge and bite` comment
      above the branch, or any other function/line (diff-scope gate below). The
      next-click verdict needs no extra wiring: catAtk runs oocMod/termMod
      before quickKeepRip, so the re-sampled verdict naturally enters
      termMod/cp5Bite, whose deferral is already correct.
    - Existing comments (incl. Chinese) stay untouched; the new comment is the
      one above, English/ASCII.
    - CRLF preservation: the file must remain ALL-CRLF after the edit. If the
      edit leaves mixed/LF endings, normalize the whole file with
      `perl -pi -e 's/\r?\n/\r\n/g' classes/druid/cat.lua` (idempotent) and
      re-run the CRLF gate before proceeding.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && sed -n '/^function macroTorch.quickKeepRip/,/^function macroTorch.keepRake/p' classes/druid/cat.lua && echo --counts-- && sed -n '/^function macroTorch.quickKeepRip/,/^function macroTorch.keepRake/p' classes/druid/cat.lua | grep -c 'if macroTorch.energyDischargeBeforeBite(clickContext) then' && sed -n '/^function macroTorch.quickKeepRip/,/^function macroTorch.keepRake/p' classes/druid/cat.lua | grep -c 'macroTorch.safeBite(clickContext)' && sed -n '/^function macroTorch.quickKeepRip/,/^function macroTorch.keepRake/p' classes/druid/cat.lua | grep -c 'return' && sed -n '/^function macroTorch.quickKeepRip/,/^function macroTorch.keepRake/p' classes/druid/cat.lua | grep -c 'defer the bite verdict to the next click' && printf 'CR=%s lines=%s\n' "$(grep -c $'\r' classes/druid/cat.lua)" "$(wc -l < classes/druid/cat.lua)" && git diff -U0 -- classes/druid/cat.lua | grep '^@@'</automated>
    Expected: the sed window shows quickKeepRip's cp>=3 branch in the guarded
    shape (guard comment, `if ... then return end`, then safeBite); the four
    grep counts print 1 / 1 / 3 / 1 in order (one new guard line, one safeBite,
    three returns total = guard return + branch tail return + cp<3 guard return,
    one new comment); CR=lines (both equal — all-CRLF intact); exactly ONE
    `@@` diff hunk, in the quickKeepRip region. If any count differs or the
    hunk touches another function, fix before proceeding.
  </verify>
  <done>
    quickKeepRip's cp>=3 branch is exactly the guarded shape mirroring cp5Bite:
    on a true return from energyDischargeBeforeBite the function returns (bite
    deferred to the next keystroke — WR-01 closed); on false it falls through to
    the unchanged safeBite. The file stays all-CRLF and the diff is a single
    hunk confined to quickKeepRip.
  </done>
</task>

<task type="auto">
  <name>Task 2: IN-01 liveness selftest, count comment 7->8, build, atomic commit</name>
  <files>core/selftest.lua</files>
  <action>
    Edit ONLY core/selftest.lua as follows.

    (a) Insert one new registration BETWEEN the t86 test's closing `end, true)`
        (line 852, the test "P: cp5Bite defers the bite when a discharge is
        attempted") and the `-- Registration count:` comment (line 854), with
        one blank line on each side (same visual style as the two neighbors).
        Insert EXACTLY this block (LF file, English/ASCII):

        -- Quick 260825-vp9: liveness of the discharge-defer contract (IN-01). With
        -- shouldDischarge == true and the REAL energyDischargeBeforeBite finding no
        -- discharge condition (non-OoC, not behind, energy below BITE_E + CLAW_E,
        -- Rake already present), cp5Bite must still fall through and bite — the
        -- no-condition fall-through the deferral fix relies on. Stub/restore style
        -- mirrors P-06 so no real spell fires in the login self-test. The discharge
        -- helper stays real; mana is pinned via an own-key shadow (macroTorch.player.mana
        -- is a live UnitMana metatable accessor, so rawget/rawset preserves it).
        macroTorch.SelfTest:register("P: cp5Bite still bites when no discharge condition matches", function()
            if UnitClass('player') ~= 'Druid' then return end
            local origIsRipPresent = macroTorch.isRipPresent
            local origRipLeft = macroTorch.ripLeft
            local origIsRakePresent = macroTorch.isRakePresent
            local origSafeBite = macroTorch.safeBite
            local origReadyBite = macroTorch.readyBite
            local origMana = rawget(macroTorch.player, 'mana')
            macroTorch.isRipPresent = function(clickContext) return true end
            macroTorch.ripLeft = function(clickContext) return 4 end
            macroTorch.isRakePresent = function(clickContext) return true end
            local biteCalled = false
            macroTorch.safeBite = function(clickContext) biteCalled = true end
            macroTorch.readyBite = function(clickContext) biteCalled = true end
            macroTorch.player.mana = 50
            local ok, pcallRes = true, true
            pcallRes = pcall(function()
                local ctx = {
                    comboPoints = 5,
                    isImmuneRip = false,
                    ooc = false,
                    isPseudoInfiniteEnergy = false,
                    isBehind = false,
                    BITE_E = 35,
                    CLAW_E = 45,
                    SHRED_E = 60,
                    RAKE_E = 35
                }
                biteCalled = false
                macroTorch.cp5Bite(ctx)
                ok = (biteCalled == true)
            end)
            macroTorch.isRipPresent = origIsRipPresent
            macroTorch.ripLeft = origRipLeft
            macroTorch.isRakePresent = origIsRakePresent
            macroTorch.safeBite = origSafeBite
            macroTorch.readyBite = origReadyBite
            rawset(macroTorch.player, 'mana', origMana)
            assert(pcallRes, "cp5Bite liveness test pcall failed")
            assert(ok, 'cp5Bite did not bite when no discharge condition matched')
        end, true)

        Trace why it passes / why it is safe (lock in this reasoning):
        1. cp5Bite gate opens via the isRipPresent stub (isImmuneRip=false, and
           `or` short-circuits before the real isFastBattleNotPvp — the helper
           is never evaluated).
        2. shouldDischarge stays true: ctx.isPseudoInfiniteEnergy=false; the
           ripLeft guard reads isRipPresent=true and ripLeft=4, so `4 <= 2.3`
           is false.
        3. The REAL energyDischargeBeforeBite runs: isDischarged is nil (passes),
           isPseudoInfiniteEnergy=false (passes); the regularAttack gate fails on
           all three subconditions — ooc=false, mana(50) < BITE_E+SHRED_E(95)
           with isBehind=false, mana(50) < BITE_E+CLAW_E(80); the Rake branch
           short-circuits on `not isRakePresent(clickContext)` being false (stub
           true). It returns false — no cast function runs, no real spell fires.
        4. cp5Bite falls through to the unchanged selection: ooc=false -> safeBite
           stub -> biteCalled=true, so ok stays true. The rawset restore deletes
           the mana own key (origMana is nil), keeping the live UnitMana accessor
           active for the rest of the suite.

    (b) Update the registration count comment. Line 854 currently reads:
        -- Registration count: Category P adds 7 tests (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86)
        Change it to exactly:
        -- Registration count: Category P adds 8 tests (2 in 26-01, 4 in 26-02, 1 in quick 260825-t86, 1 in quick 260825-vp9)

    (c) Gate sequence, in order:
        1. P-06 and t86 test untouched: `git diff -U0 -- core/selftest.lua` must
           show changed lines only BELOW line 852 (zero changed lines within
           779-852).
        2. Build: `bash /home/admin/workspace/macro-torch/build.sh` must exit 0.
           SM_Extend.lua is a gitignored build artifact — never staged.
        3. Diff scope (locked): cat.lua hunks confined to quickKeepRip (Task 1
           already gated), selftest.lua hunks only the new registration + the
           count comment. `git status --porcelain` shows exactly
           classes/druid/cat.lua and core/selftest.lua modified.
        4. Atomic commit (locked): ONE commit containing ONLY the two code
           files, English ASCII conventional message:
           fix(cat): quickKeepRip defers bite on attempted discharge (WR-01); cp5Bite discharge-false liveness selftest (IN-01)
           `git add classes/druid/cat.lua core/selftest.lua` then `git commit -m "<the message>"`.
           No .planning files and no SM_Extend.lua in this commit.
        5. Human check (non-blocking, next login): in-game `/mt` — "P: cp5Bite
           triggers bite at 5CP in fast battle without Rip or immunity",
           "P: cp5Bite defers the bite when a discharge is attempted" and the
           new "P: cp5Bite still bites when no discharge condition matches"
           must all pass with the rest of the suite.
  </action>
  <verify>
    <automated>cd /home/admin/workspace/macro-torch && grep -c 'P: cp5Bite still bites when no discharge condition matches' core/selftest.lua && grep -c 'P: cp5Bite' core/selftest.lua && grep -c 'Category P adds 8 tests' core/selftest.lua && grep -c 'rawget(macroTorch.player' core/selftest.lua && grep -c 'rawset(macroTorch.player' core/selftest.lua && sed -n '/Quick 260825-vp9/,/end, true)/p' core/selftest.lua && bash build.sh && echo BUILD-OK && git diff -U0 -- core/selftest.lua | grep '^@@' && git status --porcelain</automated>
    Expected: the first five grep counts print 1 / 3 / 1 / 1 / 1 in order (one
    new registration name; three `P: cp5Bite` names total = P-06 + t86 + new;
    updated count comment; the mana rawget snapshot and rawset restore); the
    sed window prints the complete new test exactly as specified; BUILD-OK
    prints (build.sh exit 0); the `@@` hunks are at/below the insertion region
    (nothing inside lines 779-852); status lists exactly classes/druid/cat.lua
    and core/selftest.lua (SM_Extend.lua absent — gitignored). If any count
    differs, fix before committing.
  </verify>
  <done>
    The new P test is registered after the t86 test in P-06's style, passes by
    the traced path (real discharge returns false, bite still fires), and no
    real spell can fire in the login self-test; P-06 and the t86 test are
    byte-identical; the count comment reads 8; build.sh exits 0; the single
    atomic English-message commit contains only classes/druid/cat.lua and
    core/selftest.lua.
  </done>
</task>

</tasks>

<verification>
- `bash build.sh` exits 0 (SM_Extend.lua regenerated, gitignored, not staged).
- Function-scoped gates: quickKeepRip window has 1 x `if macroTorch.energyDischargeBeforeBite(clickContext) then`,
  1 x `macroTorch.safeBite(clickContext)`, 3 x `return`, 1 x the new English
  deferral comment; the cat.lua diff is a single hunk inside quickKeepRip.
- classes/druid/cat.lua stays all-CRLF (CR count == line count).
- `git diff -U0` hunks: cat.lua in quickKeepRip only; selftest.lua below line
  852 only (P-06 at 783-815 and the t86 test at 817-852 untouched).
- `git status --porcelain` shows only classes/druid/cat.lua + core/selftest.lua.
- In-game `/mt` on next login: all three P: cp5Bite tests green (non-blocking;
  no headless Lua runtime in this environment).
</verification>

<success_criteria>
- quickKeepRip honors the discharge boolean exactly like cp5Bite: true ->
  defer the bite verdict to the next keystroke, false -> safeBite as before
  (WR-01 closed; the OoC discharge-bypass hole on this path is gone).
- One new Category P selftest proves the IN-01 liveness invariant: the real
  energyDischargeBeforeBite returning false on all conditions missed still lets
  cp5Bite fall through and bite — the finisher path cannot deadlock.
- P-06 and the t86 discharge-defer test are byte-identical and passing; the
  registration count comment reads 8.
- Single atomic English-message commit containing only the two code files;
  build.sh exits 0; cat.lua remains all-CRLF.
</success_criteria>

<output>
Create `.planning/quick/260825-vp9-fix-code-review-findings-wr-01-in-01-qui/260825-vp9-SUMMARY.md` when done
</output>