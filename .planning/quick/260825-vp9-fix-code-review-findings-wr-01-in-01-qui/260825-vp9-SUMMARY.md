---
phase: quick-260825-vp9
plan: 01
subsystem: gameplay
tags: [catatk, druid, wow-1.12, lua, energy-discharge, ferocious-bite, selftest]

# Dependency graph
requires:
  - phase: quick-260825-t86
    provides: "energyDischargeBeforeBite boolean return contract + cp5Bite deferral guard (the reference shape this task mirrors on the second call site) + P-06/t86 discharge-defer selftests"
provides:
  - "quickKeepRip honors the discharge boolean exactly like cp5Bite: true return (attempted discharge) defers the bite verdict to the next keystroke, false falls through to safeBite (WR-01 closed)"
  - "New Category P liveness selftest: with shouldDischarge == true and the REAL energyDischargeBeforeBite returning false on all conditions missed, cp5Bite still falls through and bites (IN-01 — the no-condition fall-through has coverage)"
affects: [catatk-dps, selftest]

# Actuals — pair with the plan's estimate when present (this quick task carried no estimate; recorded for calibration)
actuals:
  tokens: 716
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Discharge-deferral contract applied at every bite call site: both cp5Bite and quickKeepRip consume the energyDischargeBeforeBite boolean identically; the next keystroke re-samples the verdict via termMod/cp5Bite, whose deferral was already correct"

key-files:
  created: []
  modified:
    - classes/druid/cat.lua
    - core/selftest.lua

key-decisions:
  - "WR-01 is the single guard insertion in quickKeepRip's cp>=3 branch only: `if macroTorch.energyDischargeBeforeBite(clickContext) then return end` placed before safeBite, mirroring cp5Bite byte-shape (locked fix spec — no redesign)"
  - "IN-01 test keeps energyDischargeBeforeBite REAL, stubs only the P-06-style collaborators, and pins macroTorch.player.mana via rawget snapshot + own-key assignment + rawset restore — the mana property is a live UnitMana metatable accessor, so the nil restore deletes the own key and the accessor stays active (same pattern as the Phase 26-03 CR-01 target.isPlayerControlled fix)"
  - "Single atomic English-message commit containing only classes/druid/cat.lua + core/selftest.lua; SM_Extend.lua regenerated by build.sh stays gitignored and unstaged (locked commit spec)"

patterns-established:
  - "P-test stub discipline extended: restore-section also covers ripLeft/isRakePresent/readyBite captures, and metatable-accessor fields are pinned by rawget/own-key/rawset so live __index accessors survive the test"

requirements-completed:
  - QUICK-260825-VP9

coverage:
  - id: D1
    description: "quickKeepRip's cp>=3 branch honors the discharge boolean exactly like cp5Bite — a true return (discharge attempted) defers the bite verdict to the next keystroke, a false return falls through to the unchanged safeBite (WR-01 closed on the second call site)"
    requirement: QUICK-260825-VP9
    verification:
      - kind: other
        ref: "function-scoped gates: quickKeepRip window has 1x discharge-guard if, 1x safeBite, 3x return, 1x new deferral comment; single diff hunk @@ -337 +337,6 confinement; bash build.sh exit 0"
        status: pass
      - kind: other
        ref: "CRLF integrity gate: CR count == line count (478/478) after the edit — file remains all-CRLF"
        status: pass
      - kind: manual_procedural
        ref: "in-game /mt — full selftest suite green on the user's next login (non-blocking; no headless Lua runtime in this environment)"
        status: unknown
    human_judgment: true
    rationale: "The behavioral proof (deferred bite on a discharge-attempt frame) runs only in-game via /mt; static gates + build + CRLF checks prove structure but not runtime behavior"
  - id: D2
    description: "New Category P selftest 'P: cp5Bite still bites when no discharge condition matches' proves liveness: with shouldDischarge == true and the REAL energyDischargeBeforeBite returning false (non-OoC, not behind, mana 50 below BITE_E+CLAW_E, Rake present), cp5Bite still falls through and calls the bite (IN-01); P-06 and the t86 discharge-defer test stay byte-identical; registration count comment reads 8"
    requirement: QUICK-260825-VP9
    verification:
      - kind: other
        ref: "grep gates: 1x new registration name, 3x 'P: cp5Bite' names total, 1x 'Category P adds 8 tests', 1x rawget mana snapshot, 1x rawset mana restore; selftest diff hunk starts at old line 854 (zero changed lines within 779-852); bash build.sh exit 0"
        status: pass
      - kind: manual_procedural
        ref: "in-game /mt — all three P: cp5Bite tests green, mana accessor unaffected for remaining tests (restore verified by rawset nil-delete pattern)"
        status: unknown
    human_judgment: true
    rationale: "The liveness invariant is only truly exercised in the in-game selftest; the static trace proves the stub path but the real UnitMana accessor and no-cast guarantee need the live client (non-blocking, established convention)"
  - id: D3
    description: "Single atomic commit 592284c with only classes/druid/cat.lua and core/selftest.lua (57 insertions, 2 deletions); no deleted tracked files, no untracked leftovers, SM_Extend.lua absent from the commit"
    requirement: QUICK-260825-VP9
    verification:
      - kind: other
        ref: "git show 592284c --numstat (2 files) + post-commit deletion check (none) + git status --porcelain clean"
        status: pass
    human_judgment: false

# Metrics
duration: 9min
completed: 2026-08-25
status: complete
---

# Quick 260825-vp9: Fix code review findings WR-01 / IN-01 Summary

**quickKeepRip now defers the bite on an attempted discharge (WR-01) and a new Category P liveness selftest covers the no-discharge-condition fall-through bite (IN-01), committed atomically in 592284c**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-25T14:55:30Z
- **Completed:** 2026-08-25T15:04:45Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- WR-01 closed: `macroTorch.quickKeepRip` (classes/druid/cat.lua) now consumes the `energyDischargeBeforeBite` boolean contract exactly like `cp5Bite` — a true return (discharge attempted) defers the bite verdict to the next keystroke instead of immediately calling `safeBite`, closing the OoC discharge-bypass hole on the second bite path. Only the cp>=3 branch changed.
- IN-01 closed: new Category P selftest "P: cp5Bite still bites when no discharge condition matches" exercises the real `energyDischargeBeforeBite` returning false with `shouldDischarge == true` (non-OoC, not behind, mana 50 below BITE_E+CLAW_E=BITE_E+SHRED_E, Rake present) and asserts the bite still fires; mana is pinned via rawget/own-key/rawset because `macroTorch.player.mana` is a live UnitMana metatable accessor.
- P-06 and the t86 discharge-defer test remain byte-identical (selftest diff hunk starts at old line 854); registration count comment updated 7 -> 8.
- classes/druid/cat.lua stays all-CRLF after the edit (478 CR / 478 lines); `bash build.sh` exits 0; single atomic English-ASCII commit 592284c contains only the two code files.

## Task Commits

Per the locked commit spec, both tasks share one atomic commit:

1. **Task 1: quickKeepRip honors the discharge boolean guard (WR-01)** - `592284c`
2. **Task 2: IN-01 liveness selftest, count comment 7->8, build, atomic commit** - `592284c` (fixed message: `fix(cat): quickKeepRip defers bite on attempted discharge (WR-01); cp5Bite discharge-false liveness selftest (IN-01)`)

**Plan metadata:** the pre-dispatch PLAN commit `5a61738` was already made by the orchestrator before execution.

## Files Created/Modified

- `classes/druid/cat.lua` - quickKeepRip cp>=3 branch carries the WR-01 guard (3-line English comment + `if ... then return end` + unchanged safeBite/tail return); CRLF preserved
- `core/selftest.lua` - new IN-01 liveness registration after the t86 test + count comment 7->8; P-06 and t86 tests untouched

## Decisions Made

None - followed the locked plan as specified (single guard insertion mirroring cp5Bite, P-06-style stub discipline with rawget/rawset mana pinning, single atomic commit).

## Deviations from Plan

None - plan executed exactly as written. No auto-fixes were needed: the harness Edit preserved CRLF line endings, so the plan's sanctioned perl normalization fallback was not required. Implementation note: the test block in the PLAN carries 8-space document indentation; it was inserted at the file's column-0 convention (identical to P-06/t86) — the block content itself is byte-exact per spec.

## Issues Encountered

None. The pre-plan state facts all held: cat.lua was all-CRLF (473/473), selftest.lua all-LF, anchors at the expected lines (P-06 783-815, t86 test 820-852, count comment 854), and `bash build.sh` regenerated the gitignored SM_Extend.lua without surfacing it in status.

## Known Stubs

None - the new selftest is fully wired (real discharge helper, real assert paths); no placeholder data, TODO markers, or unconnected components were introduced.

## User Setup Required

The in-game `/mt` human check is non-blocking and lands on the user's next login (wow-client, as declared in the plan's `user_setup`):

- Run `/mt` in WoW and confirm all three P: cp5Bite tests pass along with the rest of the suite: "P: cp5Bite triggers bite at 5CP in fast battle without Rip or immunity", "P: cp5Bite defers the bite when a discharge is attempted", and the new "P: cp5Bite still bites when no discharge condition matches".

## Next Phase Readiness

- The t86 boolean contract is now uniformly applied at every bite call site (cp5Bite + quickKeepRip); no known open code-review findings remain for this contract.
- Any future bite-path changes must mirror the `if macroTorch.energyDischargeBeforeBite(clickContext) then return end` deferral shape; the new P test guards the false-tail liveness invariant against accidental deferral regressions.

## Self-Check: PASSED

- `260825-vp9-SUMMARY.md` exists at `.planning/quick/260825-vp9-fix-code-review-findings-wr-01-in-01-qui/`
- `classes/druid/cat.lua` and `core/selftest.lua` exist and are the only files in commit `592284c`

---
*Task: 260825-vp9 (WR-01 + IN-01)*
*Completed: 2026-08-25*