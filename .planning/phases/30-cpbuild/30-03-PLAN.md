---
phase: 30-cpbuild
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [tools/cpbuild.lua]
autonomous: true
requirements: []   # phase_req_ids=null — requirements coverage gate skipped by orchestrator (recorded visible skip, 2026-09-10)
user_setup: []
# specless probe: skipped — phase has no requirement IDs mapped (phase_req_ids=null), visible skip recorded

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low   # derived: calibration sample_count=0 (less than 3 means low), factor=1

must_haves:
  truths:
    - "tools/cpbuild.lua is a self-contained Lua 5.0 script that reads a SuperMacro SavedVariables file, extracts MACRO_TORCH_LOG, sandboxes it, and reports cpBuild statistics (D-07 route A: harness copied from tools/cpdamage.lua, zero edits to cpdamage.lua)"
    - "k statistics respect the chain-break rule (adjacent [cpBuild] t gaps over 30s excluded) and bucket into the six locked intervals <1.5 / 1.5-2 / 2-3 / 3-5 / 5-10 / >=10 (D-08)"
    - "T statistics produce the mean T̄, a distribution histogram, and dual pass rates P(T <= D-1) computed for both D (default 9) and 0.9*D (Savagery-adjusted) (D-08)"
    - "The ok/fail window matrix and the dropped (badLines) statistic are reported, and --selftest / --json-out / --rake-dur CLI flags mirror the cpdamage CLI contract (D-07/D-08)"
  artifacts:
    - path: "tools/cpbuild.lua"
      provides: "offline analyzer: PREFIX_BUILD / PREFIX_T / BREAK_THRESHOLD / DRAKE_DEFAULT / SAVAGERY_FACTOR / FLAG_SELFTEST / FLAG_JSON_OUT / FLAG_RAKE_DUR constants, copied harness (countList/readAll/lineNumberOf/extractMacroTorchLog/loadBlockSandboxed/getMessages/decodeJson/encodeScalar/encodeValue), parseSamples, buildKStats, buildTStats, calculatePassRates, printReport, writeJsonOut, runSelftest, printUsage, main"
      contains: "parseSamples"
  key_links:
    - from: "tools/cpbuild.lua parseSamples"
      to: "classes/druid/Druid.lua emitters (plan 30-01)"
      via: "fixed field-order tokenizers consuming the exact '[cpBuild] skill t= cp= e=' and '[cpBuildT] ok t=' / '[cpBuildT] fail' line contracts"
      pattern: "PREFIX_BUILD"
    - from: "tools/cpbuild.lua CLI"
      to: "classes/druid/HUMAN-UAT.md Phase 30 part 5 (plan 30-02)"
      via: "the three flag forms documented in the usage text and executed by the user's Windows+Cygwin Lua"
      pattern: "FLAG_RAKE_DUR"
---

<objective>
Create `tools/cpbuild.lua` — the self-contained offline analyzer (route A, D-07): copy the tools/cpdamage.lua harness verbatim (readAll / extractMacroTorchLog / loadBlockSandboxed / getMessages / CLI / selftest battery), then swap the recognition/parsing/statistics layers for the cpBuild family per D-08: per-skill cast counts, k (inter-cast interval) mean + six-bucket histogram with 30s chain-break splitting, T̄ mean + T distribution histogram, dual pass rates P(T <= D-1) for D and 0.9*D, ok/fail window matrix, and dropped-line statistics, with --selftest / --json-out / --rake-dur CLI flags mirroring cpdamage. tools/cpdamage.lua stays byte-untouched.

Purpose: Give the user a reusable offline answer machine for the double-keep viability question (T̄ + measured pass rate) without importing logs into chat.
Output: New file `tools/cpbuild.lua` (Lua 5.0 dialect, self-contained, no external dependencies, git-tracked).
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-cpbuild/30-CONTEXT.md
@.planning/phases/30-cpbuild/30-PATTERNS.md
@tools/cpdamage.lua
@classes/druid/Druid.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness copy + cpBuild recognition chain and fixed-field terse parser</name>
  <files>tools/cpbuild.lua</files>
  <read_first>
    - tools/cpdamage.lua — FULL file (1364 lines; it is the verbatim source of the harness and the grammar template). The exact copy ranges per the pattern map: header + dialect contract comment (1-37), countList (42-48), readAll (51-65), lineNumberOf (68-80), extractMacroTorchLog (95-199, with its brace-walk comment contract 82-95), loadBlockSandboxed (207-236), getMessages (246-269), decodeJson (280-524), encodeScalar (527-564), encodeValue (566-601). The recognition-chain shape to replicate for the terse parser: parseEntries (675-719).
    - .planning/phases/30-cpbuild/30-PATTERNS.md — assignment 8 (route-A copy paradigm, flag-literal concatenation rule 28-37, the cpBuild-fixture precedent 1177-1193) + Shared Pattern 6 (Lua 5.0 discipline).
    - .planning/phases/30-cpbuild/30-CONTEXT.md — D-07 (route A, zero-touch cpdamage.lua), D-08 (statistics contract incl. BREAK_THRESHOLD 30s file-header constant), D-10 + the inherited D-15 dialect rules quoted in the cpdamage header.
    - classes/druid/Druid.lua — the two emitters whose exact output this parser consumes (cpBuildLogEvent at 366-371: `[cpBuild] <skill> t=<t> cp=<cp> e=<e>`; the plan 30-01 DKI block: `[cpBuildT] ok t=<sec>` with 3-decimal seconds, `[cpBuildT] fail`).
  </read_first>
  <action>
    Create /home/admin/workspace/macro-torch/tools/cpbuild.lua from scratch (route A - D-07). Structure:

    1. License header block identical in shape to cpdamage.lua's (the Apache comment), then a purpose comment naming phase 30 and the two consumed line families, then the dialect-contract comment replicating cpdamage's wording (Lua 5.0: no length operator, no table-form string.gsub replacement, no %q JSON output, math mod via the % operator, args read straight off the arg table, self-contained with no external dependencies).

    2. Header constants, exact values:
    - MAX_FILE_BYTES = 32 * 1024 * 1024, MAX_ENTRIES = 50000
    - PREFIX_BUILD = '[cpBuild] ' (9 chars), PREFIX_T = '[cpBuildT] ' (10 chars)
    - BREAK_THRESHOLD = 30 (D-08 chain-break split threshold; a FILE-HEADER constant, not a game option)
    - DRAKE_DEFAULT = 9 (D_rake seconds default), SAVAGERY_FACTOR = 0.9 (Savagery snapshot multiplier)
    - FLAG_SELFTEST = '-' .. '-selftest', FLAG_JSON_OUT = '-' .. '-json-out', FLAG_RAKE_DUR = '-' .. '-rake-dur' — flag literals spelled via concatenation so no source string contains an adjacent hyphen pair (cpdamage 32-37 rule; bbcheck's parenthesis gate strips line comments before short strings).

    3. Copy the harness functions VERBATIM from the read_first cpdamage ranges: countList, readAll, lineNumberOf, extractMacroTorchLog, loadBlockSandboxed, getMessages, decodeJson, encodeScalar, encodeValue. Adapt only human-facing strings that brand the [cpDamage] family (comments and printed notes may say [cpBuild] instead); do not restructure, do not add logic, do not touch cpdamage.lua. The decodeJson/encodeScalar/encodeValue trio is copied even though the cpBuild lines are terse, because the --json-out archive (Task 2) needs the encoder.

    4. New function parseSamples(messages) — the recognition chain for BOTH families, replicating cpdamage's parseEntries chain shape (prefix check via string.sub against the prefix length, badLines counter, pcall-guarded body decode, MAX_ENTRIES cap with a once-per-run truncation warning, silent skip of every other line). Semantics:
    - A line whose first 9 chars equal PREFIX_BUILD is a cast line: strip the prefix, then tokenize the remaining string by the field-order contract fixed in Druid.lua — skill token first (non-empty, no spaces), then the literal `t=` key and a tonumber-able value, then `cp=`, then `e=` — space-separated in that exact order. A line failing the tokenization (wrong token count, non-numeric t/cp/e) increments badLines and is skipped (a bad sample never aborts the batch).
    - A line whose first 10 chars equal PREFIX_T is a T line: the remainder must be exactly `ok t=<number>` (parse the number right of the `t=` token) or exactly `fail`. Anything else under this prefix increments badLines.
    - PREFIX ordering note: `[cpBuildT] ...` can never match the 9-char PREFIX_BUILD because the ninth chars differ (']' vs 'T') — the two prefix checks are disjoint by length; no swallow hazard.
    - Returns a package: casts (ordered list of { skill = string, t = number, cp = number, e = number }), okTs (ordered list of the ok numbers), failCount (number of fail lines), badLines, truncated (boolean). Pure: reads messages, never mutates them, no reliance on pairs ordering.

    Hard constraints: Lua 5.0 dialect — no length-operator glyph, no label/jump syntax, no table-form string.gsub, anywhere in the file (code AND comments); the copied harness already obeys this (cpdamage.lua is clean file-wide) and the new layers must too. English comments. Do not reimplement anything the harness already provides (bracket walk, sandbox, messages ring are verbatim copies). tools/cpdamage.lua must show zero diff when this task completes.
  </action>
  <acceptance_criteria>
    - Source assertion: all 9 harness function anchors present (countList, readAll, lineNumberOf, extractMacroTorchLog, loadBlockSandboxed, getMessages, decodeJson, encodeScalar, encodeValue).
    - Source assertion: the 6 header constants present with the exact listed values, and the 3 flag literals spelled via hyphen-pair concatenation.
    - Source assertion: function parseSamples present; its body contains the PREFIX_BUILD substring check, the PREFIX_T substring check, the `t=` tokenizer, the badLines increment, and the MAX_ENTRIES cap.
    - Source assertion: tools/cpdamage.lua shows zero diff.
    - Behavior assertion (runtime, user-side per D-14): feeding a messages ring containing [cpBuild] Claw t= / [cpBuildT] ok t= / [cpBuildT] fail / one malformed line yields casts, okTs, failCount and badLines == 1.
  </acceptance_criteria>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/tools/cpbuild.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>no ': BALANCED' line in the bbcheck output, or the chain exits non-zero</fails_when>
    <automated>for t in "function countList" "function readAll" "function extractMacroTorchLog" "function loadBlockSandboxed" "function getMessages" "function decodeJson" "function encodeScalar" "function encodeValue" "function parseSamples" "PREFIX_BUILD = '[cpBuild] '" "PREFIX_T = '[cpBuildT] '" "BREAK_THRESHOLD = 30" "FLAG_RAKE_DUR = '-' .. '-rake-dur'"; do grep -qF "$t" /home/admin/workspace/macro-torch/tools/cpbuild.lua || { echo "MISSING: $t"; exit 1; }; done; echo "all anchors present"</automated>
    <fails_when>a "MISSING:" line prints (any one of the 13 anchors absent), or non-zero exit</fails_when>
    <automated>! grep -E 'goto |::' /home/admin/workspace/macro-torch/tools/cpbuild.lua</automated>
    <fails_when>grep matches a line (Lua 5.0 label/jump syntax present)</fails_when>
    <automated>test "$(grep -c '#' /home/admin/workspace/macro-torch/tools/cpbuild.lua)" = "0"</automated>
    <fails_when>non-zero exit — a length-operator glyph exists anywhere in the new file (code or comment; the file must stay clean file-wide)</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff HEAD --quiet -- tools/cpdamage.lua</automated>
    <fails_when>non-zero exit — tools/cpdamage.lua was modified (D-07 zero-touch fence broken)</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>any output line (SM_Extend.lua, the build artifact, must never be hand-edited — D-11)</fails_when>
  </verify>
  <reversibility rating="costly">The parse contract (fixed field-order tokenizer over the exact [cpBuild] / [cpBuildT] line grammar) is shared with the client emitters in plan 30-01 and the selftest pins in plan 30-02; changing the grammar later means a coordinated three-file edit plus re-running the battery. The grammar itself was user-locked (D-06/D-08), so no checkpoint re-ask.</reversibility>
  <done>tools/cpbuild.lua exists with the license block, dialect contract, 6 header constants, 3 concatenated flag literals, the 8 harness functions copied verbatim, and parseSamples; all 13 anchors verified; bbcheck BALANCED; diff --check clean; Lua 5.0 gates green; tools/cpdamage.lua untouched. Commit with scope prefix cpBuild.</done>
</task>

<task type="auto">
  <name>Task 2: Statistics layer — k buckets + chain breaks, T̄ + T histogram, dual pass rates, report, json-out, CLI main</name>
  <files>tools/cpbuild.lua</files>
  <read_first>
    - tools/cpbuild.lua — the file produced by Task 1 (its parseSamples package shape and the copied harness functions).
    - tools/cpdamage.lua — report/decision/json layering shape: printReport (1074-1109), writeJsonOut (1111-1127), printUsage (1262-1267), main (1274-1364 + the main(arg) call at 1364). Also the encodeScalar/encodeValue contract the writer reuses.
    - .planning/phases/30-cpbuild/30-CONTEXT.md — D-08 (per-skill cast counts; k mean + histogram with the 1.5s judgment line; chain-break exclusion at the 30s header constant; T̄; T distribution histogram; pass rate P(T <= D_rake minus 1) with D_rake default 9 and the Savagery 0.9 factor as parameter plus dual output; ok/fail window matrix; dropped statistics; --selftest and --json-out aligned to cpdamage).
    - .planning/phases/30-cpbuild/30-PATTERNS.md — assignment 8 (stats-section conventions: pure functions, never mutate entries, fresh result tables, explicit iteration, no pairs-order reliance).
  </read_first>
  <action>    Extend tools/cpbuild.lua with the statistics and CLI layers (after the parser from Task 1; no changes to the copied harness):

    1. Pure stats functions — every function reads its inputs as-is, never mutates them, builds fresh result tables, iterates 1..n with ipairs (no pairs-order reliance):
    - buildKStats(casts): walk casts in order; for i from 1 to count minus 1 compute k = casts[i+1].t - casts[i].t. Chain-break rule (D-08): k > BREAK_THRESHOLD or k <= 0 counts as one break (breaks counter, the interval EXCLUDED from k statistics — the gap spans combat boundaries or a relog) — otherwise the interval enters the buckets. Bucket edges exactly { 1.5, 2, 3, 5, 10 } with labels exactly '[<1.5)', '[1.5,2)', '[2,3)', '[3,5)', '[5,10)', '[>=10)', the 1.5 line being the D-08 judgment line. Returns { samples, mean, breaks, below15, buckets = list of { label, count } } where samples is the counted interval count, mean is the arithmetic mean over counted intervals (nil when samples = 0), below15 is the count of k < 1.5.
    - buildTStats(okTs): T̄ = arithmetic mean of okTs (nil when empty), samples = count, and a T distribution histogram with bucket edges { 4, 6, 8, 10, 12 } and labels exactly '[<4)', '[4,6)', '[6,8)', '[8,10)', '[10,12)', '[>=12)' (planner-chosen edges surrounding the 8s/7.1s pass thresholds; they live as a header-local constant table so the user can retune them — record this choice in the SUMMARY).
    - calculatePassRates(okTs, failCount, drake): d = tonumber(drake) or DRAKE_DEFAULT; for both rows — rakeDur row with cutoff = d - 1 and savagery row with cutoff = d * SAVAGERY_FACTOR - 1 — compute passed = count of ok t &lt;= cutoff and denominator = count(okTs) + failCount (fail windows count as not attained, D-04 bypass 1), rate = passed / denominator (nil when denominator = 0). Returns { rakeDur = { d, cutoff, passed, denominator, rate }, savagery = { d, cutoff, passed, denominator, rate } }.
    - perSkillCasts(casts): fresh table of spell-name to count (Claw / Shred / Rake plus any other token that appears), plus total.
    2. printReport(res): plain text sections in cpdamage's report style — source, cast counts per skill, k stats (mean, samples, breaks, below-1.5 share, six bucket lines), T stats (T̄, samples, six bucket lines), the dual pass-rate lines (both cutoffs always printed — the D-08 双档输出), window matrix (ok count vs fail count), dropped (badLines) and truncated warning. Numbers formatted with the same %-based idiom as cpdamage's report helpers (no %q, no table-form gsub — the dialect).
    3. writeJsonOut(path, res): archive encoding through the copied encodeScalar/encodeValue; keys exactly: generatedAt (os.date '!%Y-%m-%dT%H:%M:%SZ'), source, castCounts { claw, shred, rake, total }, kStats { samples, mean, breaks, below15, buckets }, tStats { samples, mean, buckets }, passRates { rakeDur, savagery }, windowMatrix { ok, fail }, dropped { badLines }, truncated.
    4. printUsage(): the three forms — plain path, path plus FLAG_JSON_OUT plus file, FLAG_SELFTEST alone — each of which may also carry FLAG_RAKE_DUR plus a number of seconds; mirror cpdamage's usage text style.
    5. main(args): cpdamage's CLI arg-loop extended with the FLAG_RAKE_DUR option (requires a following numeric; non-numeric or missing value prints an error plus usage and exits 1) and a drake variable defaulting to DRAKE_DEFAULT; chain: readAll (missing/oversized exits 1) then extractMacroTorchLog (the friendly empty-log note exits 0 exactly like cpdamage) then getMessages then parseSamples, assemble res with the stats calls, printReport, optional writeJsonOut with the 'json result written to' note. Call main(arg) at the file tail. runSelftest lands in Task 3; main's selftest arm must already exist (call it exactly like cpdamage does).
  </action>
  <acceptance_criteria>
    - Source assertion: buildKStats, buildTStats, calculatePassRates, perSkillCasts, printReport, writeJsonOut, printUsage, main present; the file tail calls main(arg).
    - Source assertion: k bucket labels and T bucket labels carry the exact locked strings; BREAK_THRESHOLD participates in the chain-break comparison; SAVAGERY_FACTOR participates in the savagery cutoff; the denominator sums ok count plus fail count.
    - Source assertion: writeJsonOut routes all values through encodeScalar/encodeValue (no ad-hoc string building of numbers); the 12 archive keys listed above are present as key literals.
    - Source assertion: main handles FLAG_RAKE_DUR (missing value error path exits 1, non-numeric error path exits 1) and the empty-log path exits 0.
    - Behavior assertion (runtime, user-side per D-14): a sample run prints k mean/buckets/breaks, T̄, both pass-rate lines, the ok/fail matrix, and dropped counts; --json-out writes a parseable archive.
  </acceptance_criteria>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/tools/cpbuild.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>no ': BALANCED' line in the bbcheck output, or the chain exits non-zero</fails_when>
    <automated>for t in "function buildKStats" "function buildTStats" "function calculatePassRates" "function perSkillCasts" "function printReport" "function writeJsonOut" "function printUsage" "function main" "main(arg)" "1.5)" "=10)" "4)" "=12)" "DRAKE_DEFAULT = 9" "SAVAGERY_FACTOR = 0.9"; do grep -qF "$t" /home/admin/workspace/macro-torch/tools/cpbuild.lua || { echo "MISSING: $t"; exit 1; }; done; echo "all anchors present"</automated>
    <fails_when>a "MISSING:" line prints (any one of the 14 anchors absent), or non-zero exit</fails_when>
    <automated>! grep -E 'goto |::' /home/admin/workspace/macro-torch/tools/cpbuild.lua && test "$(grep -c '#' /home/admin/workspace/macro-torch/tools/cpbuild.lua)" = "0"</automated>
    <fails_when>grep matches a label/jump line, or the length-operator glyph count is non-zero</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff HEAD --quiet -- tools/cpdamage.lua</automated>
    <fails_when>non-zero exit — tools/cpdamage.lua was modified (D-07 zero-touch fence broken)</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>any output line (SM_Extend.lua, the build artifact, must never be hand-edited — D-11)</fails_when>
  </verify>
  <done>Stats, report, json-out, usage and main present with the locked constants, labels, key set and exit codes; all 14 anchors verified; bbcheck BALANCED; diff --check clean; Lua 5.0 gates green; tools/cpdamage.lua untouched. Commit with scope prefix cpBuild.</done>
</task>

<task type="auto">
  <name>Task 3: runSelftest battery — in-file fixture with chain break, ok/fail matrix, malformed lines</name>
  <files>tools/cpbuild.lua</files>
  <read_first>
    - tools/cpbuild.lua — the file produced by Tasks 1-2 (parseSamples / stats / report / main as they now stand).
    - tools/cpdamage.lua — runSelftest (1129-1260) as the battery paradigm: the check(cond, label) helper (FAIL line + exit 1 on any miss), the DQ = string.char(34) escaping, the ntext/btext helpers, the line(n, raw) fixture builder used with the MACRO_TORCH_LOG block shape, the ALL PASSED banner plus exit 0, and the fixture-assertion style at 1177-1193 (message count, entry count, badLines count).
    - .planning/phases/30-cpbuild/30-PATTERNS.md — assignment 8 (fixture section; how cpdamage embeds real [cpBuild] lines through line(n, raw) and treats them as the primary input inverted).
  </read_first>
  <action>
    Add function runSelftest() to tools/cpbuild.lua and make main's selftest arm call it (the arm already exists from Task 2). Battery contract:

    - Copy the battery skeleton from cpdamage verbatim: check(cond, label) helper; DQ escaping; local fixture builders. The cpBuild fixtures embed REAL lines through the line(n, raw) helper — terse lines, so bare quotes are not needed anywhere in fixture bodies, but keep the DQ helper for the block scaffolding exactly as cpdamage builds it.
    - Fixture content contract (pick your own concrete numbers, then compute every expected value BY HAND from the embedded t values — every check compares a hand-derived expectation; recompute rather than guessing): a MACRO_TORCH_LOG block containing (a) at least 4 consecutive [cpBuild] lines inside one chain with t gaps that land in at least three different buckets including one gap under 1.5, (b) one [cpBuild] line whose t gap from the previous exceeds 30 seconds (chain break), (c) two [cpBuildT] ok lines whose t values straddle the two cutoffs so the two pass rates DIFFER (one ok t below 7.1, one between 7.1 and 8.0 — cutoffs for the default drake 9), (d) one [cpBuildT] fail line, (e) one malformed [cpBuildT] line (non-numeric remainder after the ok t= token) and one malformed [cpBuild] line (bad cp= value), (f) one unrelated line that must be skipped silently.
    - Required checks (each with an explicit label): message ring count equals the fixture line count; parseSamples yields the hand-computed cast count, okT count, failCount == 1, badLines == 2; buildKStats reports breaks == 1, samples == interval count between same-chain lines, the hand-computed mean, below15 == 1, and each of the six bucket counts equals the hand-computed occupancy; buildTStats mean equals the two-okT mean; calculatePassRates with drake 9 yields rakeDur.passed and savagery.passed that DIFFER (the straddle check) with denominator == 3; windowMatrix ok == 2 and fail == 1; json archive: build res via the stats calls, writeJsonOut to a temp path, readAll it back, assert it round-trips (non-nil, contains the passRates key literal and the trailing brace position), then remove the temp file; calculatePassRates with drake 10 recomputes both cutoffs (cutoff d-1 = 9 and 0.9-streaked 8) and the passed counts shift accordingly — assert the hand-derived values.
    - Finishing contract identical to cpdamage: on any miss print 'selftest: FAIL - ' plus label and exit 1; on clean run print the single line 'selftest: ALL ' plus the passed count plus ' PASSED' and exit 0. main's selftest arm returns right after runSelftest so it touches no external files. Keep the whole battery under the same Lua 5.0 discipline (no length-operator glyph, no label/jump syntax, no table-form gsub, English comments).

    Runtime note (D-14, visible): this battery cannot execute on the planning box (no local Lua); the automated gates below certify structure, and the LIVE --selftest run is the user's first HUMAN-UAT step (Phase 30 part 5). This is the recorded, accepted verification split for this phase, same as phase 28-03's byte-simulation practice but with static gates standing in for simulation.
  </action>
  <acceptance_criteria>
    - Source assertion: function runSelftest present; main's selftest arm calls it and returns; the FAIL exit-1 and ALL PASSED exit-0 contracts are coded exactly as cpdamage's.
    - Source assertion: the fixture embeds at least one line() entry for each of the five fixture families (chained [cpBuild], chain-broken [cpBuild], [cpBuildT] ok, [cpBuildT] fail, one malformed line per family), and check labels reference breaks, below15, mean, buckets, passRates, windowMatrix, badLines, json round-trip and the drake override.
    - Source assertion: every check's expectation is a literal derived from the embedded t values (no cross-fixture constants).
    - Behavior assertion (runtime, user-side per D-14): `lua tools/cpbuild.lua --selftest` on the Windows+Cygwin box prints ALL PASSED and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/tools/cpbuild.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>no ': BALANCED' line in the bbcheck output, or the chain exits non-zero</fails_when>
    <automated>for t in "function runSelftest" "local function check(cond, label)" "[cpBuild] Claw t=" "[cpBuildT] ok t=" "[cpBuildT] fail" "breaks ==" "below15 ==" "passRates" "windowMatrix" "badLines ==" "PASSED"; do grep -qF "$t" /home/admin/workspace/macro-torch/tools/cpbuild.lua || { echo "MISSING: $t"; exit 1; }; done; echo "all battery anchors present"</automated>
    <fails_when>a "MISSING:" line prints (any one of the 11 battery anchors absent), or non-zero exit</fails_when>
    <automated>test "$(grep -c 'break' /home/admin/workspace/macro-torch/tools/cpbuild.lua)" -ge 3</automated>
    <fails_when>non-zero exit — the chain-break exclusion (breaks variable, break comparison, and its check) is not threaded through parser, stats and battery</fails_when>
    <automated>! grep -E 'goto |::' /home/admin/workspace/macro-torch/tools/cpbuild.lua && test "$(grep -c '#' /home/admin/workspace/macro-torch/tools/cpbuild.lua)" = "0"</automated>
    <fails_when>grep matches a label/jump line, or the length-operator glyph count is non-zero</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff HEAD --quiet -- tools/cpdamage.lua</automated>
    <fails_when>non-zero exit — tools/cpdamage.lua was modified (D-07 zero-touch fence broken)</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>any output line (SM_Extend.lua, the build artifact, must never be hand-edited — D-11)</fails_when>
  </verify>
  <done>The selftest battery is complete with all locked anchors; bbcheck BALANCED; diff --check clean; Lua 5.0 gates green; tools/cpdamage.lua untouched; SM_Extend.lua untouched. tools/cpbuild.lua is feature-complete per D-07/D-08 and ready for the user-side --selftest run. Commit with scope prefix cpBuild, then create the 30-03-SUMMARY.md per the plan output block, recording the planner-chosen T-histogram edges { 4, 6, 8, 10, 12 } as a header constant and the D-14 runtime-verification split.</done>
</task>

</tasks>

<threat_model>## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted SavedVariables file → analyzer | user-supplied local file content flows through extractMacroTorchLog / loadBlockSandboxed / parsers |
| CLI arguments → filesystem | --json-out target path and the SavedVariables path are user-controlled; the script writes one file and reads one file |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-07 | Elevation of Privilege | loadBlockSandboxed / extractMacroTorchLog (tools/cpbuild.lua) | low | mitigate | The block is executed in an empty setfenv sandbox exactly as copied from cpdamage (a phase-28-proven chain); the brace-walk extractor is string/comment aware; a crafted file can at worst distort analyzer output for the local user who supplied it, not execute code outside the sandbox |
| T-30-08 | Denial of Service | readAll / parseSamples (tools/cpbuild.lua) | low | mitigate | 32MB file ceiling rejects oversized inputs before reading (readAll copied verbatim); MAX_ENTRIES cap with truncation warning bounds hostile message rings; parseSamples is O(n) single pass with no allocation growth beyond the caps |
| T-30-09 | Tampering | writeJsonOut output path | low | accept | The script writes only to the explicitly passed --json-out path and prints the result; the invoking user owns the machine and the path — client-local tooling, no boundary crossed |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; bbcheck.js pre-exists from phase 27 and node is invoked read-only |
</threat_model>

<verification>
Plan-level checks (automated here): bbcheck BALANCED + diff --check per task; anchor sets (13 + 14 + 11); Lua 5.0 gates (label/jump absence and length-operator glyph absence, file-wide on the new file); tools/cpdamage.lua zero-diff fence; SM_Extend.lua porcelain empty.
Runtime checks (user-side, per D-14 — recorded, not runnable here): `lua tools/cpbuild.lua --selftest` ALL PASSED and a full `lua tools/cpbuild.lua <SavedVariables>` report run, both on the Windows+Cygwin box, tracked by the HUMAN-UAT.md Phase 30 part 5 (plan 30-02).
</verification>

<success_criteria>
- tools/cpbuild.lua ships the harness copy, terse parser, k/T/pass-rate statistics, report, json-out archive, CLI with the three flags, and a complete selftest battery
- every static gate green; tools/cpdamage.lua byte-untouched; SM_Extend.lua untouched
- the file reads line-for-line analyzable by a Lua 5.0 interpreter on the user's machine (dialect contract replicated from cpdamage)
</success_criteria>

<output>
Create `.planning/phases/30-cpbuild/30-03-SUMMARY.md` when done. Record in it: the planner-chosen T-histogram bucket edges { 4, 6, 8, 10, 12 } (header constant, user-tunable), the archive key set, the flag names, and the D-14 note that the --selftest live run belongs to the user's Windows+Cygwin machine via HUMAN-UAT.md part 5.
</output>