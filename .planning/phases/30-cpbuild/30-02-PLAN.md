---
phase: 30-cpbuild
plan: 02
type: execute
wave: 2
depends_on: ['30-01']
files_modified: [classes/druid/selftest.lua, classes/druid/HUMAN-UAT.md]
autonomous: true
requirements: []   # phase_req_ids=null — requirements coverage gate skipped by orchestrator (recorded visible skip, 2026-09-10)
user_setup: []
# specless probe: skipped — phase has no requirement IDs mapped (phase_req_ids=null), visible skip recorded

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low   # derived: calibration sample_count=0 (less than 3 means low), factor=1

must_haves:
  truths:
    - "Eight Category S-05..S-12 stubbed tests exist, each registered with trailing isOptional=true, each following the CR-01 snapshot, shadow, capture, restore-before-assert discipline (D-13)"
    - "The six DKI state-machine transitions plus the global reset plus the D-01 switch gating are each pinned by a stubbed test driving macroTorch.cpBuildDkiTick with captured globals GetComboPoints / GetTime / macroTorch.log"
    - "classes/druid/HUMAN-UAT.md carries a Phase 30 section embedding the D-09 target-state-loop precondition and the D-14 Windows+Cygwin runtime protocol"
    - "The phase-wide static battery is green: bbcheck BALANCED on all touched Lua files, diff --check clean, read-only files (tools/cpdamage.lua, classes/druid/cat.lua, macro_torch.lua, core/periodic.lua, interface_debug.lua) byte-untouched, SM_Extend.lua not hand-edited"
  artifacts:
    - path: "classes/druid/selftest.lua"
      provides: "Category S-05..S-12 registrations (8 tests) inside the existing Druid-only wrapper, plus the updated Category S registration-count comment"
      contains: "Cat S-05:"
    - path: "classes/druid/HUMAN-UAT.md"
      provides: "## Phase 30: real-machine acceptance checklist (D-09 precondition + D-14 protocol + analysis run + troubleshooting)"
      contains: "## Phase 30:"
  key_links:
    - from: "classes/druid/selftest.lua (S-05..S-12)"
      to: "macroTorch.cpBuildDkiTick / macroTorch.resetCpBuildDki (plan 30-01 symbols)"
      via: "direct function calls after replacing macroTorch.cpBuildDki with a fresh state table and shadowing the three globals"
      pattern: "macroTorch.cpBuildDkiTick"
    - from: "classes/druid/HUMAN-UAT.md Phase 30 section"
      to: "tools/cpbuild.lua CLI (plan 30-03)"
      via: "analysis-run steps invoking lua tools/cpbuild.lua with the selftest / json-out / rake-dur flags"
      pattern: "tools/cpbuild.lua"
---

<objective>
Pin the DKI state machine with stubbed selftests (D-13) and close the phase with the real-machine acceptance protocol plus the static battery. Task 1 authors Category S-05..S-12 (8 tests) covering the six locked state-machine transitions, the global reset, and the D-01 switch gate, all under the CR-01 stub discipline. Task 2 appends the Phase 30 section to classes/druid/HUMAN-UAT.md (D-09 target-state-loop precondition, D-14 Windows+Cygwin runtime protocol) and runs the phase-wide static battery over every touched file.

TDD note (visible, recorded): this plan is not `type: tdd` because the only runtime for these tests is the WoW 1.12 client on the user's Windows+Cygwin machine (D-14 — this Linux box has no Lua and no client), so a RED state can never be observed by the executor. The suite is authored as post-implementation pinning tests executed by the user via /mt; static gates below certify everything this environment can run.

Purpose: Give the state machine its only executable verification surface (game-client /mt) and give the user the exact real-machine steps to sample and analyze [cpBuildT] telemetry.
Output: Modified `classes/druid/selftest.lua` (+8 registrations with updated count comment), modified `classes/druid/HUMAN-UAT.md` (+Phase 30 section), phase-wide battery green.
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cpbuild/30-CONTEXT.md
@.planning/phases/30-cpbuild/30-PATTERNS.md
@classes/druid/selftest.lua
@classes/druid/Druid.lua
@classes/druid/HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category S-05..S-12 — 8 stubbed DKI state-machine tests with CR-01 discipline</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua — the Category S block as the canonical discipline reference: header comment (1326-1329), S-02 macroTorch.log capture variant (1335-1347), S-03 full rawget-snapshot shape (1349-1387), S-04 (1389-1421), the Category S registration-count comment (1423), and the file-shape context (whole file sits inside `if UnitClass('player') == 'Druid' then`; Category U footer closes the file).
    - .planning/phases/30-cpbuild/30-PATTERNS.md — assignment 7 (selftest.lua) + Shared Pattern 5 (CR-01 discipline, generalized to plain globals per assignment 7's note: no rawget for plain globals — `local savedGCP = GetComboPoints; GetComboPoints = function() ... end; ... GetComboPoints = savedGCP` restore before any assert).
    - .planning/phases/30-cpbuild/30-CONTEXT.md — D-13 (S-04+ stubbed unit tests: six transitions + global reset + switch gating; isOptional=true) and D-04 (the transition table the tests pin).
    - classes/druid/Druid.lua — the DKI block from plan 30-01: macroTorch.cpBuildDki fields (state/t0/prevCp), macroTorch.cpBuildDkiTick gate order and emission strings, macroTorch.resetCpBuildDki.
  </read_first>
  <action>    In classes/druid/selftest.lua, insert 8 new registrations INSIDE the existing Druid-only wrapper (the file already sits inside `if UnitClass('player') == 'Druid' then`), placed immediately after the S-04 registration's `end, true)` line and BEFORE the existing count comment "-- Registration count: Category S adds 4 tests (quick 260907-0ya + WR-01 fix)". In the same edit, REPLACE that count comment with exactly: -- Registration count: Category S adds 12 tests (quick 260907-0ya + WR-01 fix + phase 30 DKI 8 tests). Add a one-line category banner comment above S-05 (mirroring the Category S banner style at 1326-1329) stating Category S-05+ covers the phase-30 DKI state machine.

    The 8 registrations, titles locked exactly (these are the acceptance anchors):

    - "Cat S-05: DKI WAIT_ANCHOR anchors on a 3-to-1 down-jump" — stub GetComboPoints returning scripted values: a seeding tick returning 3 (records prevCp), then a tick returning 1; GetTime returns a fixed value; assert state = 'BUILDING', t0 equals the fixed time, and zero captured log lines.
    - "Cat S-06: DKI cp=0 back-look anchors alive target, cancels dead target" — two sub-drives on two fresh state tables: (a) target stubbed { isCanAttack = true }, ticks 2 then 0 → anchors BUILDING with t0 set; (b) target stubbed { isCanAttack = false }, ticks 2 then 0 → stays WAIT_ANCHOR, t0 nil, no captured lines.
    - "Cat S-07: DKI BUILDING re-reach of 5 persists one ok line and goes DONE" — seed cpBuildDki = { state = 'BUILDING', t0 = 99.0, prevCp = 4 }; GetComboPoints returns 5; GetTime returns 105.5; assert exactly one captured line, equal to '[cpBuildT] ok t=6.500', and state = 'DONE'; assert no extra line on a second tick with cp still 5.
    - "Cat S-08: DKI BUILDING mid-window down-jump emits fail and re-anchors" — seed { state='BUILDING', t0=99.0, prevCp=4 }; GetComboPoints returns 1; GetTime returns 110.0; assert captured equals { '[cpBuildT] fail' } (one line), state still 'BUILDING', t0 = 110.0.
    - "Cat S-09: DKI kill-shot truncation emits fail and returns to WAIT_ANCHOR" — seed { state='BUILDING', t0=99.0, prevCp=3 }; target.isCanAttack = false; GetComboPoints returns 0; assert exactly one captured line '[cpBuildT] fail', state 'WAIT_ANCHOR', t0 nil.
    - "Cat S-10: DKI DONE stays silent until the next down-jump re-anchors" — seed { state='DONE', t0=nil, prevCp=5 }; tick 1 with cp=5 (silent), tick 2 with cp=1 (down-jump re-anchor); assert zero captured lines across both ticks, final state 'BUILDING', t0 = GetTime stub value.
    - "Cat S-11: DKI global reset discards the active window to WAIT_ANCHOR" — seed a mid-window state { state='BUILDING', t0=123.0, prevCp=3 }; call macroTorch.resetCpBuildDki() directly; assert state 'WAIT_ANCHOR', t0 nil, prevCp nil, zero captured lines.
    - "Cat S-12: DKI tick is zero-API when the switch is off or combat is idle" — with a GetComboPoints stub incrementing a local calls counter: (a) macroTorch.cpBuildLog = false, tick → calls = 0 and no lines; (b) cpBuildLog = true but macroTorch.inCombat = false, tick → calls = 0 and no lines; (c) control arm: cpBuildLog = true and inCombat = true, tick → calls = 1 (proves the stub would have counted).

    CR-01 discipline, UNIFORM across all 8 (locked variable names — the verification greps anchor on them): every test snapshots all of — savedGCP for the GetComboPoints global, savedGTime for GetTime, savedLog for macroTorch.log, savedInCombat for macroTorch.inCombat, savedCpBuildLog for macroTorch.cpBuildLog, savedTarget for macroTorch.target, savedDki for macroTorch.cpBuildDki; installs its own shadows (macroTorch.log captured into a `captured` local array; macroTorch.cpBuildDki REPLACED with a fresh table { state = 'WAIT_ANCHOR', t0 = nil, prevCp = nil } so no domain value leaks across tests; macroTorch.target replaced with a plain stub table { isCanAttack = ... }); drives ticks inside one pcall; restores ALL of them (plain assignment: GetComboPoints = savedGCP, GetTime = savedGTime, macroTorch.log = savedLog, macroTorch.inCombat = savedInCombat, macroTorch.cpBuildLog = savedCpBuildLog, macroTorch.target = savedTarget, macroTorch.cpBuildDki = savedDki) BEFORE the FIRST assert — exactly the S-03 order (snapshot, shadow, capture, restore, then asserts). Every registration ends with `, true)` (isOptional=true), matches existing formatting (one register call per line, English test titles and assert messages), and follows the project Lua 5.0 discipline (no length-operator glyph, no label/jump syntax anywhere in added text — titles, asserts, and comments all plain ASCII).

    Hard constraints: no changes anywhere else in the file (no edits to existing categories, no new wrapper); do NOT add stub helpers outside the test bodies; the count comment update is part of this same edit.
  </action>
  <acceptance_criteria>
    - Source assertion: grep count of the 8 titles equals 8 — `grep -cE 'Cat S-(0[5-9]|1[0-2]):' classes/druid/selftest.lua` returns 8.
    - Source assertion: each of the 7 restore statements appears exactly 8 times in the file (uniform discipline): GetComboPoints = savedGCP, GetTime = savedGTime, macroTorch.log = savedLog, macroTorch.inCombat = savedInCombat, macroTorch.cpBuildLog = savedCpBuildLog, macroTorch.target = savedTarget, macroTorch.cpBuildDki = savedDki.
    - Source assertion: every restore line precedes the first assert of its test (CR-01 order preserved; verified by reading each test body during authoring).
    - Source assertion: the count comment now reads "Category S adds 12 tests (quick 260907-0ya + WR-01 fix + phase 30 DKI 8 tests)" and no "adds 4 tests" Category S count comment remains.
    - Source assertion: all 8 register calls carry the trailing true third argument; all 8 sit inside the existing Druid-only wrapper.
    - Behavior assertion (user-side /mt, per D-14): the 8 tests pass on the Windows+Cygwin client with zero red lines; they exercise cpBuildDkiTick exactly as plan 30-01 implemented it (gate order, transition table, emission strings).
  </acceptance_criteria>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/selftest.lua &amp;&amp; git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>no ': BALANCED' line in the bbcheck output, or the chain exits non-zero</fails_when>
    <automated>test "$(grep -cE 'Cat S-(0[5-9]|1[0-2]):' /home/admin/workspace/macro-torch/classes/druid/selftest.lua)" = "8"</automated>
    <fails_when>non-zero exit — the 8 new titles are not all present with their exact locked names</fails_when>
    <automated>for r in "GetComboPoints = savedGCP" "GetTime = savedGTime" "macroTorch.log = savedLog" "macroTorch.inCombat = savedInCombat" "macroTorch.cpBuildLog = savedCpBuildLog" "macroTorch.target = savedTarget" "macroTorch.cpBuildDki = savedDki"; do test "$(grep -cF "$r" /home/admin/workspace/macro-torch/classes/druid/selftest.lua)" = "8" || { echo "DISCIPLINE-BREAK: $r"; exit 1; }; done; echo "restore discipline uniform"</automated>
    <fails_when>a "DISCIPLINE-BREAK:" line prints — some restore statement is missing from some test</fails_when>
    <automated>grep -qF "Category S adds 12 tests" /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>grep exits non-zero — the updated 12-test count comment is absent</fails_when>
    <automated>! grep -E 'goto |::' /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>grep matches a line (Lua 5.0 label/jump syntax introduced)</fails_when>
    <automated>test "$(git -C /home/admin/workspace/macro-torch diff HEAD -- classes/druid/selftest.lua | grep '^+' | grep -v '^\+\+\+' | grep -c '#')" = "0"</automated>
    <fails_when>non-zero exit — the diff added a line containing the length-operator glyph</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>any output line (SM_Extend.lua, the build artifact, must never be hand-edited — D-11)</fails_when>
  </verify>
  <done>All 8 registrations present with locked titles, uniform CR-01 discipline (7 restore statements x8), count comment updated to 12, all gates green, bbcheck BALANCED, diff --check clean, SM_Extend.lua untouched. Commit with scope prefix cpBuild.</done>
</task>

<task type="auto">
  <name>Task 2: HUMAN-UAT.md Phase 30 section (D-09 precondition + D-14 protocol) + phase-wide static battery</name>
  <files>classes/druid/HUMAN-UAT.md</files>
  <read_first>
    - classes/druid/HUMAN-UAT.md — full file; the Phase 28 (171-225) and Phase 29 (227-272) section shape to replicate (## heading, numbered Prerequisites / Pre-Test / protocol / Expected Outcomes and Troubleshooting parts, closing 完成信号 line).
    - .planning/phases/30-cpbuild/30-CONTEXT.md — D-09 (the target-state-loop precondition, its exact wording and rationale), D-14 (Windows+Cygwin runtime; no local Lua), D-01 (switch protocol), D-06 (line protocol), D-08 (analyzer flags).
    - plans 30-01 / 30-03 artifacts: the [cpBuildT] line formats, the tools/cpbuild.lua CLI (flags selftest / json-out / rake-dur).
  </read_first>
  <action>    Append a new section to classes/druid/HUMAN-UAT.md, heading exactly "## Phase 30: cpBuild 双保判定 -- 实机 UAT 闭环", containing these six numbered parts in the file's existing checklist style (each item a "- [ ]" checkbox):

    1. Prerequisites（前提）: WoW 1.12.1（Turtle WoW）+ SuperWoW 客户端; Windows+Cygwin 下 ./build.sh 成功重建产物; Cygwin 内可用 lua 5.x 解释器用于分析脚本; and the D-09 precondition written out in full: 打桩必须在目标态循环下进行 — 双流血由 bite 刷新维持的目标态循环, 不是现行每次窗口硬打 Rake 补贴一星且起始耗能 32e 的循环, 因为现行循环会系统性低估真实 T̄ 并混淆能量轮廓 (record the rationale verbatim-adjacent to D-09's phrasing).
    2. Pre-Test（自测预检）: 游戏内 /mt 全绿无红色 FAIL — Category S-05..S-12 共 8 条全过 (非可选其它项黄 warning 可容忍); 登录横幅仍为 CONFIG_OPTIONS 4 项 (本 phase 未增删配置项).
    3. 采集协议: /run macroTorch.cpBuildLog=true 热开; 目标态循环打骷髅; 观察聊天通道出现 [cpBuildT] ok t= 行 (咬击后约数秒) 与偶发 [cpBuildT] fail 行 (窗口内低星斩杀咬截断); 战斗中途切换目标或脱战时不新增任何行 (D-06 不写行).
    4. 开关门控验证: /run macroTorch.cpBuildLog=false 后继续循环 — 无任何新 [cpBuild]/[cpBuildT] 行, 帧表现无变化 (D-01 零 API 路径).
    5. 分析运行: lua tools/cpbuild.lua --selftest 输出 ALL PASSED; 从 SavedVariables 导出 SuperMacro 存档文件后运行 lua tools/cpbuild.lua &lt;SavedVariables 路径&gt; (可选 --json-out out.json 与 --rake-dur 9), 核对输出含 k 均值 + 六档直方图 + 断链计数 + T̄ + T 分布直方图 + 双档达标率 (9s 与 8.1s 档) + ok/fail 窗口矩阵.
    6. Expected Outcomes / Troubleshooting（期望结果与排查）: ok 行 cadence 大致等于窗口周期; fail 与 ok 之比即未达标率; 无 ok 无 fail — 查开关是否开启、目标是否为可攻击骷髅、GCD 探针警告是否出现; 行数骤停 — 查 MACRO_TORCH_LOG 裁剪上限 macroTorch.LOG_MAX_SIZE.

    Close with a 完成信号 line mirroring the Phase 29 section: 六节清单全部勾选后, 将 /mt 汇总行、[cpBuildT] 行样例与分析脚本输出回复给 verifier, 由 verifier 汇入阶段末 30-UAT.md.

    Battery (run the following; the plan is complete only when all are green). The allowed diff set for the whole phase is exactly five files: classes/druid/Druid.lua, core/events.lua, classes/druid/selftest.lua, classes/druid/HUMAN-UAT.md, tools/cpbuild.lua — everything else in the working tree must be untouched (the battery asserts this). Keep HUMAN-UAT.md additions in the file's existing tone; no structural edits above the appended section.
  </action>
  <acceptance_criteria>
    - Source assertion: grep -qF "## Phase 30:" on classes/druid/HUMAN-UAT.md succeeds; the section is the LAST one in the file.
    - Source assertion: the D-09 precondition is present with its two anchor tokens 目标态循环 and T̄ (grep finds 目标态循环 at least once inside the Phase 30 section).
    - Source assertion: the D-14 runtime protocol is present: the strings Windows+Cygwin and lua tools/cpbuild.lua --selftest both appear in the section.
    - Source assertion: the D-01 and D-06 protocol anchors appear: macroTorch.cpBuildLog=false and [cpBuildT] ok t= and [cpBuildT] fail referenced in parts 3/4.
    - Battery assertion: bbcheck BALANCED x4 (Druid.lua, events.lua, selftest.lua, tools/cpbuild.lua), git diff --check clean, read-only file fence green (tools/cpdamage.lua / classes/druid/cat.lua / macro_torch.lua / core/periodic.lua / interface_debug.lua byte-unchanged), SM_Extend.lua porcelain empty, working-tree diff restricted to the five allowed files.
  </acceptance_criteria>
  <verify>
    <automated>grep -qF "## Phase 30:" /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md &amp;&amp; grep -qF "目标态循环" /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md &amp;&amp; grep -qF "Windows+Cygwin" /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md &amp;&amp; grep -qF "lua tools/cpbuild.lua --selftest" /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md</automated>
    <fails_when>non-zero exit — one of the four Phase-30 anchors (heading, D-09 precondition, D-14 environment, analyzer selftest step) is missing</fails_when>
    <automated>for f in classes/druid/Druid.lua core/events.lua classes/druid/selftest.lua tools/cpbuild.lua; do node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js "/home/admin/workspace/macro-torch/$f" || exit 1; done &amp;&amp; git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>any file lacks its ': BALANCED' line (bbcheck prints one per file), or the chain exits non-zero</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff HEAD --quiet -- tools/cpdamage.lua classes/druid/cat.lua macro_torch.lua core/periodic.lua interface_debug.lua</automated>
    <fails_when>non-zero exit — at least one read-only file shows a diff (the D-07 zero-touch fence and the catAtk decision fence are broken)</fails_when>
    <automated>for f in $(git -C /home/admin/workspace/macro-torch diff HEAD --name-only); do case "$f" in classes/druid/Druid.lua|core/events.lua|classes/druid/selftest.lua|classes/druid/HUMAN-UAT.md|tools/cpbuild.lua|.planning/*) ;; *) echo "UNEXPECTED: $f"; exit 1;; esac; done</automated>
    <fails_when>an "UNEXPECTED:" line prints — a file outside the five allowed source files (plus .planning artifacts) is modified</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua; echo "porcelain-lines: $(git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua | wc -l)"</automated>
    <fails_when>the printed porcelain-lines count is non-zero (SM_Extend.lua, the build artifact, was touched — D-11)</fails_when>
  </verify>
  <done>HUMAN-UAT.md carries the Phase 30 section with all four anchors; the phase-wide battery is green on every task's gates: bbcheck BALANCED across the four Lua files, diff --check clean, read-only fence intact, SM_Extend.lua untouched, diff restricted to the five allowed files. Commit with scope prefix cpBuild, then create the 30-02-SUMMARY.md per the plan output block, noting in it the D-09 elaboration for the user's real-machine run.</done>
</task>

</tasks>

<threat_model>## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selftest shadow code → session state | stubbed globals and own-key shadows temporarily replace live session functions during /mt |
| doc change → runtime behavior | HUMAN-UAT.md instructs the user's real-machine steps; a wrong instruction misleads data collection, not code |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Spoofing / session pollution | classes/druid/selftest.lua shadows (GetComboPoints, GetTime, macroTorch.log, macroTorch.inCombat, macroTorch.cpBuildLog, macroTorch.target, macroTorch.cpBuildDki) | low | mitigate | CR-01 discipline mandated uniformly: every test restores all seven shadows via plain assignment BEFORE its first assert, so even a failing test cannot leave a stub installed session-wide (the U-09/P-02 freeze-bug class is structurally excluded); tests run under the existing pcall-wrapped SelfTest:run |
| T-30-06 | Information Disclosure | HUMAN-UAT.md Phase 30 instructions | low | accept | The doc references only protocol facts already in the locked design (D-01/D-06/D-08/D-09/D-14); no secrets, no account data, no new channels beyond the D-01-gated log |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase; bbcheck.js pre-exists from phase 27 and node is invoked read-only |
</threat_model>

<verification>
Plan-level checks:
- 8-title count gate, 7x8 restore-discipline gate, 12-test count-comment gate, Lua 5.0 gates, bbcheck + diff --check on selftest.lua (Task 1)
- 4 Phase-30 UAT anchors, bbcheck battery x4, read-only fence, allowed-diff-set gate, SM_Extend.lua porcelain empty (Task 2)
Runtime checks (user-side, per D-14 — recorded, not runnable here): /mt full suite including S-05..S-12 on Windows+Cygwin; the HUMAN-UAT.md Phase 30 checklist is the live protocol.
</verification>

<success_criteria>
- classes/druid/selftest.lua carries S-05..S-12 with uniform CR-01 discipline and the updated count comment
- classes/druid/HUMAN-UAT.md carries the Phase 30 checklist with the D-09 precondition and D-14 protocol
- phase-wide battery green; the phase diff is restricted to the five allowed files
</success_criteria>

<output>
Create `.planning/phases/30-cpbuild/30-02-SUMMARY.md` when done. Record in it: the 8 test titles and what each pins, the CR-01 variable-name convention (savedGCP / savedGTime / savedLog / savedInCombat / savedCpBuildLog / savedTarget / savedDki), and the D-09 target-state-loop precondition for the user's real-machine run (also embedded in HUMAN-UAT.md).
</output>