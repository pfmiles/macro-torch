---
phase: 30-cpbuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [classes/druid/Druid.lua, core/events.lua]
autonomous: true
requirements: []   # phase_req_ids=null — requirements coverage gate skipped by orchestrator; design contract is 30-CONTEXT.md D-01..D-14 (recorded visible skip, 2026-09-10)
user_setup: []
# specless probe: skipped — phase has no requirement IDs mapped (phase_req_ids=null), visible skip recorded

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low   # derived: calibration sample_count=0 (less than 3 means low), factor=1

must_haves:
  truths:
    - "With macroTorch.cpBuildLog on and combat active, a bite CP down-jump anchors a measurement window and the first re-reach of 5 combo points persists exactly one '[cpBuildT] ok t=<sec>' line through macroTorch.log (D-04/D-05/D-06)"
    - "A mid-window down-jump persists exactly one '[cpBuildT] fail' line and re-anchors the next window (D-04 bypass 1)"
    - "With macroTorch.cpBuildLog off, the 0.1s poll tick returns before calling GetComboPoints — the sampling and polling path makes zero client API calls (D-01)"
    - "PLAYER_TARGET_CHANGED or combat exit (PLAYER_REGEN_ENABLED) discards the in-flight window without persisting any line (D-04 bypass 2)"
    - "The poll is a load-time-registered 0.1s periodic task whose body self-gates, exactly like maintainTHV / maintainLandTables (D-05)"
  artifacts:
    - path: "classes/druid/Druid.lua"
      provides: "DKI state machine (macroTorch.cpBuildDki table with state/t0/prevCp fields, macroTorch.cpBuildDkiTick driver, macroTorch.resetCpBuildDki global reset, load-time registration of periodic task 'cpBuildDkiPoll')"
      contains: "macroTorch.cpBuildDki"
    - path: "core/events.lua"
      provides: "two DKI global-reset call sites inside the existing PLAYER_TARGET_CHANGED and PLAYER_REGEN_ENABLED branches"
      contains: "macroTorch.resetCpBuildDki"
  key_links:
    - from: "core/events.lua (PLAYER_REGEN_ENABLED branch)"
      to: "macroTorch.resetCpBuildDki"
      via: "direct call placed immediately after macroTorch.onCombatExit() inside the existing branch"
      pattern: "onCombatExit"
    - from: "classes/druid/Druid.lua (load-time registerPeriodicTask entry)"
      to: "macroTorch.cpBuildDkiTick"
      via: "task = macroTorch.cpBuildDkiTick inside the periodic task registration"
      pattern: "registerPeriodicTask"
    - from: "macroTorch.cpBuildDkiTick emission"
      to: "interface_debug.lua macroTorch.log"
      via: "macroTorch.log call with the exact '[cpBuildT] ok t=' / '[cpBuildT] fail' strings (dual channel: chat + MACRO_TORCH_LOG)"
      pattern: "macroTorch.log"
---

<objective>
Deliver the DKI (double-keep instrumentation) live timer end-to-end, per the frozen protocol in 30-CONTEXT.md D-01/D-04/D-05/D-06: a 0.1s-polling three-state machine (WAIT_ANCHOR / BUILDING / DONE) that measures T from a bite anchor to the first re-reach of 5 combo points and persists `[cpBuildT] ok t=<sec>` or `[cpBuildT] fail` lines through macroTorch.log, gated by the existing `macroTorch.cpBuildLog` switch, with global-reset hooks on target change and combat exit. This is the tracer plan — ONE vertical slice proving the full chain (poll tick → state machine → emitted line → reset hook), production-quality, not a prototype.

Purpose: Prove the DKI architecture end-to-end on the cheapest possible slice before expansion tasks (selftests in 30-02, offline analyzer in 30-03) build on it. After this plan, a user who turns the switch on in-game gets real [cpBuildT] telemetry; with the switch off, the addon behaves exactly as before.
Output: Modified `classes/druid/Druid.lua` (DKI state + tick + registration) and `core/events.lua` (two reset hooks). Specless probe: skipped — no requirement IDs mapped for this phase (visible skip recorded in frontmatter).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cpbuild/30-CONTEXT.md
@.planning/phases/30-cpbuild/30-PATTERNS.md
@classes/druid/Druid.lua
@core/events.lua
@core/periodic.lua
</context>

## Artifacts this phase produces (catalog — plan 30-01 is the tracer slice of this set; 30-02 and 30-03 build the rest)

| Symbol / artifact | Kind | Home | Notes |
|---|---|---|---|
| `macroTorch.cpBuildDki` | state table | classes/druid/Druid.lua | fields: `state` ('WAIT_ANCHOR' / 'BUILDING' / 'DONE'), `t0` (anchor GetTime), `prevCp` (previous poll sample). Stable home OUTSIDE macroTorch.context (onCombatExit swaps context) |
| `macroTorch.cpBuildDkiTick` | function | classes/druid/Druid.lua | 0.1s poll driver; self-gating per D-01/D-05 |
| `macroTorch.resetCpBuildDki` | function | classes/druid/Druid.lua | global reset (D-04 bypass 2), zero API calls |
| `'cpBuildDkiPoll'` | periodic task name | classes/druid/Druid.lua | `registerPeriodicTask('cpBuildDkiPoll', { interval = 0.1, task = macroTorch.cpBuildDkiTick })`, load-time static |
| `[cpBuildT] ok t=<sec>` / `[cpBuildT] fail` | log line protocols | emitted via macroTorch.log | t formatted `string.format('%.3f', t1 - t0)` |
| reset call sites | 2 wired calls | core/events.lua | PLAYER_TARGET_CHANGED branch + PLAYER_REGEN_ENABLED branch |
| `Cat S-05` … `Cat S-12` selftests | 8 registrations | classes/druid/selftest.lua | plan 30-02 |
| `tools/cpbuild.lua` symbols (`PREFIX_BUILD`/`PREFIX_T`/`BREAK_THRESHOLD`/`DRAKE_DEFAULT`/`SAVAGERY_FACTOR`/`FLAG_SELFTEST`/`FLAG_JSON_OUT`/`FLAG_RAKE_DUR`/`parseSamples`/stats/report/`runSelftest`/`main`) | CLI + library | tools/cpbuild.lua | plan 30-03 |
| `## Phase 30:` section | UAT checklist | classes/druid/HUMAN-UAT.md | plan 30-02 |

<tasks>

<task type="tracer">
  <name>Task 1: DKI three-state live timer — poll, anchor, ok/fail emission, load-time registration</name>
  <files>classes/druid/Druid.lua</files>
  <read_first>
    - classes/druid/Druid.lua — current state of the whole file; the wrapper short-circuit gate shape (25-58), the D-06 range-lock note + ferocious_bite body (64-74), cpBuildLogSample (348-364) and cpBuildLogEvent (366-371) as the sibling emitters, cpDamageSample cheap-first gate ordering (373-436), the comboPoints accessor (439-443). All insertion points identified by FUNCTION NAME, not line number (line numbers shift as the file grows).
    - .planning/phases/30-cpbuild/30-PATTERNS.md — assignment 1 (Druid.lua) and Shared Patterns 1 (gate), 2 (emission channel), 3 (0.1s task + pcall driver).
    - .planning/phases/30-cpbuild/30-CONTEXT.md — D-01 (switch gate, zero API when off), D-04 (three states + two bypasses), D-05 (0.1s poll vehicle), D-06 (line protocol).
    - core/periodic.lua — scheduler behavior (98-149): static registration, `times == nil` runs forever, pcall-guarded driver.
    - interface_debug.lua — macroTorch.log dual write (106-124).
  </read_first>
  <action>
    Per D-07's sibling decision D-15 and the pattern map: the DKI state lives in a dedicated table OUTSIDE macroTorch.context, because onCombatExit swaps the whole context table (combat_context.lua 21-27). In classes/druid/Druid.lua, insert the DKI block IMMEDIATELY AFTER the cpBuildLogEvent function definition ends (the function at ~366-371; the insertion point is right before the comment block "-- Pre-cast snapshot for one [cpDamage] entry (phase 28)"). Insert, in this exact order:

    (a) A plain-English comment naming the phase-30 provenance and the D-01/D-04/D-06 protocol locks, followed by an unconditional load-time init: macroTorch.cpBuildDki = { state = 'WAIT_ANCHOR', t0 = nil, prevCp = nil }. Plain assignment (not a nil guard): the file re-executes every login/reload, which re-arms the state exactly like the macro_torch.lua nil-guard flags do.

    (b) function macroTorch.resetCpBuildDki() — sets macroTorch.cpBuildDki.state = 'WAIT_ANCHOR', macroTorch.cpBuildDki.t0 = nil, macroTorch.cpBuildDki.prevCp = nil. No arguments, no returns, no client API calls (it is the D-04 bypass-2 global reset; called from core/events.lua in Task 2).

    (c) function macroTorch.cpBuildDkiTick() — the 0.1s poll driver, a pure function over macroTorch.cpBuildDki plus stubbable globals. Gate order is LOCKED, cheapest first, mirroring cpDamageSample:
      1) `if not macroTorch.cpBuildLog then return end` — FIRST statement (D-01: switch off means the tick returns before any API call; this is the zero-cost path).
      2) `if not macroTorch.inCombat then return end` — combat idle gate (D-05 "不注册/空转" idle arm; a plain field read, still zero API).
      3) `local cp = GetComboPoints() or 0` — call the GLOBAL GetComboPoints directly (D-05 accessor contract = `GetComboPoints() or 0`, Druid.lua 441-443; calling the global is what makes D-13's stub-and-capture selftests possible).
      4) Bookkeeping: local prevCp = macroTorch.cpBuildDki.prevCp; then macroTorch.cpBuildDki.prevCp = cp.
      5) Transition logic — implement EXACTLY this table (locked protocol, no re-design):

      Down-jump predicate (all states): prevCp ~= nil and prevCp &gt; 1 and cp &lt;= 1.
      cp=0 back-look (applies whenever a down-jump lands on cp == 0): macroTorch.toBoolean(macroTorch.target.isCanAttack) — truthy = a real bite (no remnant talent); falsy = target dead/gone, the candidate is cancelled.

      WAIT_ANCHOR + down-jump: cp == 1 → anchor. cp == 0 and back-look truthy → anchor. cp == 0 and back-look falsy → stay WAIT_ANCHOR (no state change, no line, no count — D-04 explicit cancel). Anchor = macroTorch.cpBuildDki.t0 = GetTime(); macroTorch.cpBuildDki.state = 'BUILDING'.

      BUILDING + cp &gt;= 5 (this branch is checked BEFORE the down-jump branch): t1 = GetTime(); call macroTorch.log with the single string built by concatenating the literal prefix, the ok token, the t token, the equals sign, and string.format('%.3f', t1 - macroTorch.cpBuildDki.t0) — the exact persisted line is `[cpBuildT] ok t=<sec>` per D-06, with the seconds value ALWAYS formatted to 3 decimals so the offline parser (plan 30-03) can tonumber it; then macroTorch.cpBuildDki.state = 'DONE'. One window yields exactly one sample; no second emission in the same window.

      BUILDING + down-jump (truncation, D-04 bypass 1 — counts the fail denominator): cp == 1 → call macroTorch.log with the exact single string `[cpBuildT] fail`, then re-anchor (t0 = GetTime()) and STAY in 'BUILDING' (the interrupting bite anchors the next window; windows are mutually exclusive). cp == 0 and back-look truthy → identical: fail line + re-anchor, stay BUILDING. cp == 0 and back-look falsy (kill-shot bite on a dying target) → call macroTorch.log with the exact single string `[cpBuildT] fail` once (the window is truncated, so the fail denominator still counts per D-04 bypass 1), BUT do NOT re-anchor: state = 'WAIT_ANCHOR' — the target is dead and the combat-end reset (Task 2) owns the follow-up.

      DONE + down-jump: cp == 1 → anchor (t0 = GetTime(), state = 'BUILDING'). cp == 0 and back-look truthy → anchor. cp == 0 and back-look falsy → state = 'WAIT_ANCHOR', no anchor, no line. Until a down-jump occurs, DONE is fully silent (one window = one sample per D-04).

      (d) load-time static registration, placed at file top level immediately after the DKI functions block: macroTorch.registerPeriodicTask('cpBuildDkiPoll', { interval = 0.1, task = macroTorch.cpBuildDkiTick }). Static load-time registration is the project shape (Target.lua:158 'maintainTHV', spell_trace_core.lua:389 'maintainLandTables'); do NOT register/unregister conditionally — the in-tick self-gates above are the zero-cost path. The periodic driver is already pcall-guarded (periodic.lua 140-149), so a tick error can never kill the frame loop.

    Hard constraints (all locked): do NOT modify cpBuildLogSample, cpBuildLogEvent, cpDamageSample, ferocious_bite, or any wrapper body — only the insertion above. do NOT touch macro_torch.lua (D-01 keeps the switch unchanged; no new CONFIG_OPTIONS entry; the 30s chain-break constant belongs to tools/cpbuild.lua, not here). Comments in English (project convention). Lua 5.0 syntax discipline per D-10: no length-operator glyph, no label/jump syntax, in code OR comments — the file currently carries two legacy decision-comment glyphs elsewhere, do not add any new ones in this block. Keep the whole block beside the cpBuild family for discoverability.
  </action>
  <acceptance_criteria>
    - Source assertion: macroTorch.cpBuildDki init line assigns a table whose literal field list is state = 'WAIT_ANCHOR', t0 = nil, prevCp = nil.
    - Source assertion: macroTorch.cpBuildDkiTick first statements are the cpBuildLog nil-check and the inCombat nil-check, in that order, BEFORE any GetComboPoints call.
    - Source assertion: the ONLY client API calls in the whole block are GetComboPoints() and GetTime(); the emission calls macroTorch.log exactly twice across the two emission points (ok branch and fail branch).
    - Source assertion: the persisted strings are byte-exact `[cpBuildT] ok t=` .. string.format('%.3f', t1 - t0) and `[cpBuildT] fail` — grep finds both literals.
    - Source assertion: string.format('%.3f', t1 - macroTorch.cpBuildDki.t0) appears exactly once (t is always 3-decimals, archive-stable contract with plan 30-03).
    - Source assertion: macroTorch.registerPeriodicTask('cpBuildDkiPoll', { interval = 0.1, task = macroTorch.cpBuildDkiTick }) appears exactly once at file top level; no other registerPeriodicTask call added anywhere in the file.
    - Behavior assertion (grep-verifiable wiring, runtime checkable by the user's /mt later): with cpBuildLog true + inCombat true, three tick drives scripted as 3..1..5 combo points produce exactly one captured log line starting with the ok prefix.
  </acceptance_criteria>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/Druid.lua &amp;&amp; git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>no ': BALANCED' line in the bbcheck output, or the chain exits non-zero (git diff --check prints a whitespace error line)</fails_when>
    <automated>for t in "macroTorch.cpBuildDki = {" "function macroTorch.cpBuildDkiTick" "function macroTorch.resetCpBuildDki" "registerPeriodicTask('cpBuildDkiPoll'" "task = macroTorch.cpBuildDkiTick" "[cpBuildT] ok t=" "string.format('%.3f', t1 - macroTorch.cpBuildDki.t0)" "[cpBuildT] fail"; do grep -qF "$t" /home/admin/workspace/macro-torch/classes/druid/Druid.lua || { echo "MISSING: $t"; exit 1; }; done; echo "all anchors present"</automated>
    <fails_when>a "MISSING:" line is printed (any one of the eight anchors absent), or non-zero exit</fails_when>
    <automated>! grep -E 'goto |::' /home/admin/workspace/macro-torch/classes/druid/Druid.lua</automated>
    <fails_when>grep matches a line (label/jump syntax present anywhere in the file)</fails_when>
    <automated>test "$(git -C /home/admin/workspace/macro-torch diff HEAD -- classes/druid/Druid.lua | grep '^+' | grep -v '^\+\+\+' | grep -c '#')" = "0"</automated>
    <fails_when>non-zero exit — the diff added a line containing the length-operator glyph (Lua 5.0 gate, scoped to added lines so the two legacy comment glyphs elsewhere do not trip it)</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>any output line (SM_Extend.lua, the build artifact, must never be hand-edited — D-11)</fails_when>
  </verify>
  <reversibility rating="costly">The [cpBuildT] ok t= / fail persisted-line protocol is a cross-artifact contract (client emitter here + offline parser in plan 30-03 + selftest pins in 30-02); changing it later means a coordinated edit across three files and re-running the battery — but user locked the protocol in D-06, so no checkpoint re-ask.</reversibility>
  <done>The DKI state table, tick, reset function and load-time registration exist in classes/druid/Druid.lua exactly as specified; all eight presence anchors verified; bbcheck BALANCED; diff --check clean; Lua 5.0 gates green; SM_Extend.lua untouched. Commit with scope prefix cpBuild.</done>
</task>

<task type="auto">
  <name>Task 2: Global reset wiring — PLAYER_TARGET_CHANGED and combat-exit hooks in core/events.lua</name>
  <files>core/events.lua</files>
  <read_first>
    - core/events.lua — full file; the PLAYER_TARGET_CHANGED branch (73-81) with its niling idiom, the PLAYER_REGEN_ENABLED / PLAYER_REGEN_DISABLED branches (90-93), the frozen RegisterEvent surface (23-46), and the gated-dispatch precedent in the RAW_COMBATLOG branch (130-148).
    - .planning/phases/30-cpbuild/30-PATTERNS.md — assignment 4 (events.lua) and Shared Pattern 4 (reset hooks); note the onCombatExit context-swap remark in assignment 4's supplementary note.
    - .planning/phases/30-cpbuild/30-CONTEXT.md — D-04 bypass 2 (global reset, highest priority: PLAYER_TARGET_CHANGED or combat exit returns the machine to WAIT_ANCHOR, discard without counting, history books untouched).
    - classes/druid/Druid.lua — the DKI block added in Task 1 (macroTorch.resetCpBuildDki signature).
  </read_first>
  <action>    Per D-04 bypass 2, insert TWO calls of macroTorch.resetCpBuildDki() into core/events.lua — both inside EXISTING handler branches, and ONLY those branches:

    1. PLAYER_TARGET_CHANGED: add the call as the FIRST statement of the branch, immediately after `elseif event == 'PLAYER_TARGET_CHANGED' then` and BEFORE the existing `if macroTorch.player.isInCombat and macroTorch.target.isCanAttack then` block. Rationale: bypass 2 has the highest priority, so ANY target change discards the in-flight window unconditionally (even out of combat, where it is a harmless no-op field reset). The existing in-combat niling block below stays byte-for-byte unchanged.

    2. PLAYER_REGEN_ENABLED: add the call inside the branch, on the line immediately AFTER `macroTorch.onCombatExit()`. Do NOT touch onCombatExit itself (core/combat_context.lua is not in this phase's file set) — the events hook clears the DKI explicitly because the DKI state lives outside macroTorch.context and is therefore NOT auto-cleared by the context-table swap.

    Frozen-surface constraints: add ZERO new frame:RegisterEvent calls and do not modify any other handler branch; the DKI poll needs no event registration (it rides the periodic task from Task 1). No new globals in this file. English comments only; a one-line comment above each inserted call stating it is the D-04 bypass-2 reset is recommended but must reuse the existing comment style. Lua 5.0 discipline per D-10 in any added text.
  </action>
  <acceptance_criteria>
    - Source assertion: macroTorch.resetCpBuildDki appears in core/events.lua exactly 2 times (one per branch; grep count equals 2).
    - Source assertion: inside the PLAYER_TARGET_CHANGED branch (region from the elseif line to the next elseif line), the reset call appears exactly once and precedes the `if macroTorch.player.isInCombat` line.
    - Source assertion: inside the PLAYER_REGEN_ENABLED branch (region from its elseif line to the PLAYER_REGEN_DISABLED elseif line), the reset call appears exactly once, on a line after macroTorch.onCombatExit().
    - Source assertion: the RegisterEvent surface is frozen — grep count of RegisterEvent tokens equals 20 (the grounded pre-phase baseline; the four commented-out registrations at lines 23/34-36 are included in that count and stay).
    - Behavior assertion (runtime, /mt-side): a target swap or combat exit during BUILDING discards the window (state WAIT_ANCHOR, no line persisted); the reset clears state only, never the already-persisted samples — since the sample history lives in MACRO_TORCH_LOG, which the reset never touches.
  </acceptance_criteria>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/core/events.lua &amp;&amp; git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>no ': BALANCED' line in the bbcheck output, or the chain exits non-zero</fails_when>
    <automated>test "$(grep -c 'macroTorch.resetCpBuildDki' /home/admin/workspace/macro-torch/core/events.lua)" = "2" &amp;&amp; test "$(sed -n "/elseif event == 'PLAYER_TARGET_CHANGED' then/,/elseif event == 'SPELLCAST_START' then/p" /home/admin/workspace/macro-torch/core/events.lua | grep -c 'macroTorch.resetCpBuildDki')" = "1" &amp;&amp; test "$(sed -n "/elseif event == 'PLAYER_REGEN_ENABLED' then/,/elseif event == 'PLAYER_REGEN_DISABLED' then/p" /home/admin/workspace/macro-torch/core/events.lua | grep -c 'macroTorch.resetCpBuildDki')" = "1"</automated>
    <fails_when>non-zero exit — call count is not exactly 2, or either region does not contain its single reset call</fails_when>
    <automated>test "$(grep -c 'RegisterEvent' /home/admin/workspace/macro-torch/core/events.lua)" = "20"</automated>
    <fails_when>non-zero exit — the event-registration surface changed from the grounded baseline of 20</fails_when>
    <automated>! grep -E 'goto |::' /home/admin/workspace/macro-torch/core/events.lua</automated>
    <fails_when>grep matches a line (Lua 5.0 label/jump syntax introduced)</fails_when>
    <automated>test "$(git -C /home/admin/workspace/macro-torch diff HEAD -- core/events.lua | grep '^+' | grep -v '^\+\+\+' | grep -c '#')" = "0"</automated>
    <fails_when>non-zero exit — the diff added a line containing the length-operator glyph</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>any output line (SM_Extend.lua, the build artifact, must never be hand-edited — D-11)</fails_when>
  </verify>
  <done>Both reset hooks wired exactly as specified; region and count gates green; RegisterEvent surface frozen at 20; bbcheck BALANCED on events.lua; diff --check clean; SM_Extend.lua untouched. The tracer slice is complete end-to-end: switch-on telemetry persists through macroTorch.log, and both reset paths discard in-flight windows. Commit with scope prefix cpBuild.</done>
</task>

</tasks>

<threat_model>## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → macroTorch.log | untrusted user session state crosses into the persisted MACRO_TORCH_LOG line stream |
| combat events → DKI state | PLAYER_TARGET_CHANGED / PLAYER_REGEN_ENABLED mutate the measurement state machine |
| periodic driver → tick body | 0.1s frame callback executes addon code under pcall |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Information Disclosure | macroTorch.cpBuildDkiTick emission via macroTorch.log | low | mitigate | [cpBuildT] lines carry only combo-point timing telemetry, same exposure class as the existing [cpBuild]/[cpDamage] lines; the whole channel stays behind the D-01 switch (default false, per-login re-arm) so nothing is emitted without an explicit session opt-in |
| T-30-02 | Denial of Service | 0.1s poll tick (classes/druid/Druid.lua) | low | mitigate | Cheap-first gate order locked (switch check then combat check before GetComboPoints); with the switch off the tick is two field reads; with the switch off or idle this is the zero-cost path per D-05; the pcall-guarded periodic driver (periodic.lua 140-149) contains any tick error so the frame loop survives |
| T-30-03 | Tampering | macroTorch.cpBuildDki state table | low | mitigate | The table is written only by the tick and resetCpBuildDki; combat-exit cannot corrupt it because the state lives outside the swapped macroTorch.context; a raised privilege in the client could forge lines but the log is client-local diagnostics, not a security boundary |
| T-30-04 | Elevation of Privilege | core/events.lua reset hooks | low | accept | The two inserted calls only nil fields on the DKI table (a pure field-reset closure); no new event registrations, no new globals, no API calls — attack surface unchanged from any other handler branch |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase: bbcheck.js pre-exists from phase 27 and node is invoked read-only |
</threat_model>

<verification>
Phase-plan level checks (this plan):
- bbcheck BALANCED on both modified files
- git diff --check clean on both
- Lua 5.0 gates (label/jump token absence file-wide; length-operator glyph absence in added lines)
- SM_Extend.lua untouched (git status porcelain empty); core/combat_context.lua and macro_torch.lua not in the diff
- all 8 presence anchors of Task 1 + the 2 reset call sites of Task 2
Runtime checks (user-side, per D-14 — recorded, not runnable here): the /mt selftest additions land in plan 30-02; the real-machine [cpBuildT] observation protocol lands in classes/druid/HUMAN-UAT.md (plan 30-02 Task 2).
</verification>

<success_criteria>
- classes/druid/Druid.lua carries macroTorch.cpBuildDki / macroTorch.cpBuildDkiTick / macroTorch.resetCpBuildDki and the static 'cpBuildDkiPoll' registration with the exact gate order
- core/events.lua carries exactly the two reset call sites; no new event registration
- both files pass bbcheck + diff --check + Lua 5.0 gates; SM_Extend.lua and all read-only files untouched
- git log shows the two cpBuild-scoped commits
</success_criteria>

<output>
Create `.planning/phases/30-cpbuild/30-01-SUMMARY.md` when done. Record in it: the locked transition table as implemented (WAIT_ANCHOR/BUILDING/DONE + two bypasses), the state-home decision (dedicated macroTorch.cpBuildDki, outside context), the gate order, and the D-14 note that runtime verification of the tick behavior rides the 30-02 selftests on the user's Windows+Cygwin machine.
</output>