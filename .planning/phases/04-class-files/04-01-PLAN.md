---
phase: 04-class-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/cat.lua
  - classes/druid/bear.lua
  - classes/druid/utility.lua
autonomous: true
requirements:
  - R6
  - R8

must_haves:
  truths:
    - "classes/druid/Druid.lua exists with constructor, FIELD_FUNC_MAP, energy constants, shared helpers, SpellTrace/SelfTest registrations"
    - "classes/druid/cat.lua exists with all cat-only standalone functions"
    - "classes/druid/bear.lua exists with all bear-only standalone functions"
    - "classes/druid/utility.lua exists with druidBuffs, druidStun, druidDefend, druidControl, pokemonLoad"
    - "Total function count across 4 target files equals function count in SM_Extend_Druid.lua (92 top-level + catAtk inner = same logic preserved)"
    - "No function body is edited -- every function is extracted verbatim from SM_Extend_Druid.lua"
  artifacts:
    - path: "classes/druid/Druid.lua"
      provides: "Druid class definition, shared helpers, registrations"
      min_lines: 500
    - path: "classes/druid/cat.lua"
      provides: "cat form combat functions"
      min_lines: 600
    - path: "classes/druid/bear.lua"
      provides: "bear form combat functions including bearAtk"
      min_lines: 200
    - path: "classes/druid/utility.lua"
      provides: "druid utility functions (buffs, stun, defend, control, pokemonLoad)"
      min_lines: 150
  key_links:
    - from: "classes/druid/cat.lua"
      to: "classes/druid/Druid.lua"
      via: "macroTorch global namespace"
      pattern: "build_order.txt ordering: Druid.lua before cat.lua"
    - from: "classes/druid/bear.lua"
      to: "classes/druid/Druid.lua"
      via: "macroTorch global namespace (safeFF, isFightStarted, etc.)"
      pattern: "build_order.txt ordering: Druid.lua before bear.lua"
---

<objective>
Split SM_Extend_Druid.lua (1870 lines) into 4 files under classes/druid/ per D-01 boundary decisions and D-03 naming convention.

Purpose: Isolate cat/bear/utility code into separate files while keeping shared helpers and class definition in Druid.lua. This is the primary structural change of Phase 4.

Output: 4 new files in classes/druid/ directory, each containing a subset of the original SM_Extend_Druid.lua functions extracted verbatim (no logic edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (R6: classes/ directory; R8: Druid logic preservation)
@.planning/phases/04-class-files/04-CONTEXT.md (D-01: function-to-file assignment; D-03: snake_case subdirectory)
@.planning/phases/04-class-files/04-RESEARCH.md (Druid Line Range Audit section for function boundaries)
@.planning/phases/04-class-files/04-PATTERNS.md (copy-paste split strategy; key patterns)
@SM_Extend_Druid.lua (source file, 1870 lines -- the single source of truth for all Druid logic)
@build_order.txt (current build manifest, shows existing file ordering conventions)
</context>

<interfaces>
<!-- Key types, patterns, and conventions from the codebase that the executor needs -->
<!-- Extracted from SM_Extend_Druid.lua and build_order.txt -->

All functions use `macroTorch.*` global namespace. File splitting does not change visibility.
Concatenation order in build_order.txt guarantees definition before call.

Apache 2.0 license header (lines 1-15 of SM_Extend_Druid.lua) is required on Druid.lua only.
Satellite files (cat, bear, utility) do NOT need their own license header per PATTERNS.md.

Naming convention (D-03): subdirectory is snake_case `classes/druid/`, main file is PascalCase `Druid.lua`.
Satellite files: `cat.lua`, `bear.lua`, `utility.lua` (snake_case, matching entity/ and core/ conventions).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create classes/druid/ directory and extract Druid.lua from SM_Extend_Druid.lua</name>
  <files>classes/druid/Druid.lua</files>
  <read_first>SM_Extend_Druid.lua (lines 1-255, 308-373, 390-537, 539-583, 599-637, 767-830, 851-882, 908-968, 979-1022, 1110-1115, 1127-1256, 1339-1344, 1359-1371, 1385-1398, 1752-1870)</read_first>
  <acceptance_criteria>
    <item>Directory classes/druid/ exists</item>
    <item>classes/druid/Druid.lua exists and starts with Apache 2.0 license header (lines 1-15 verbatim from SM_Extend_Druid.lua)</item>
    <item>Druid.lua contains the complete macroTorch.Druid:new() constructor (lines 19-230 verbatim) including inner functions: showEnergyUsageSet, isRelicEquipped, ensureRelicEquipped, equipRelic, prowl, trackHumanoids, catAtk</item>
    <item>Druid.lua contains DRUID_FIELD_FUNC_MAP (lines 232-253 verbatim)</item>
    <item>Druid.lua contains macroTorch.druid = macroTorch.Druid:new() and macroTorch.registerPlayerClass("DRUID", macroTorch.Druid) (lines 254-255)</item>
    <item>Druid.lua contains shared helper functions: computeNormalRelic, selectFerocityOrEmeraldRot, recoverNormalRelic, computeClaw_E, computeShred_E, computeRake_E, computeRake_Duration, computeTiger_E, computeTiger_Duration, computeRake_Erps, computeRip_Erps, computePounce_Erps</item>
    <item>Druid.lua contains SpellTrace:register() block (5 calls, lines 482-503)</item>
    <item>Druid.lua contains consumeDruidBattleEvents</item>
    <item>Druid.lua contains shared decision helpers: shouldUseShred, isTrivialBattleOrPvp, isTrivialBattle, combatUrgentHPRestore, isFightStarted, isKillShotOrLastChance, computeErps, shouldCastFFDuringWaitWindow, getMinimumAffordableAbilityCost, shouldCastRip, shouldUseBite</item>
    <item>Druid.lua contains cross-form helpers: isNearBy, isGcdOk, safeFF, tigerSelfGCD</item>
    <item>Druid.lua contains status-check functions: isTigerPresent, tigerLeft, isRipPresent, ripLeft, isRakePresent, rakeLeft, isFFPresent, ffLeft, isDemoralizingRoarPresent, isPouncePresent, pounceLeft</item>
    <item>Druid.lua contains SelfTest:register() block (25 calls, lines 1752-1870 verbatim as the LAST block in the file)</item>
    <item>Druid.lua does NOT contain any cat-only functions (listed in Task 2), bear-only functions (listed in Task 3), or utility functions (listed in Task 4)</item>
    <item>command: `grep -c "^function macroTorch\." classes/druid/Druid.lua` returns the count of shared/class-def functions (expected: ~40, must not include any cat/bear/utility function)</item>
  </acceptance_criteria>
  <action>
Create directory `classes/druid/`, then create `classes/druid/Druid.lua` by extracting from SM_Extend_Druid.lua.

**Method per D-02 and PATTERNS.md:** Start by copying SM_Extend_Druid.lua to classes/druid/Druid.lua. Then remove function blocks that belong in cat.lua, bear.lua, and utility.lua. This "subtractive" approach guarantees no function body is edited -- only deletion of whole function blocks.

**Functions to KEEP in Druid.lua (everything NOT listed in Tasks 2/3/4):**
- License header (lines 1-15)
- Class declaration (line 17): `macroTorch.Druid = macroTorch.Player:new()`
- Constructor `macroTorch.Druid:new()` (lines 19-230) including ALL inner `function obj.*()` definitions:
  - showEnergyUsageSet, isRelicEquipped, ensureRelicEquipped, equipRelic, prowl, trackHumanoids, catAtk
  - These inner functions stay intact inside the constructor body
- DRUID_FIELD_FUNC_MAP (lines 232-253)
- Instance creation + registerPlayerClass (lines 254-255)
- Section header comment before each function block (e.g., `--` lines above functions)
- Shared helpers: computeNormalRelic(308), selectFerocityOrEmeraldRot(344), recoverNormalRelic(373)
- Energy constants: computeClaw_E(390), computeShred_E(400), computeRake_E(405), computeRake_Duration(414), computeTiger_E(422), computeTiger_Duration(430), computeRake_Erps(436), computeRip_Erps(453), computePounce_Erps(470)
- SpellTrace:register block (5 calls, lines 482-503), consumeDruidBattleEvents(506)
- shouldUseShred(539)
- isTrivialBattleOrPvp(599), isTrivialBattle(605), combatUrgentHPRestore(616), isFightStarted(626)
- isKillShotOrLastChance(767)
- computeErps(851)
- shouldCastFFDuringWaitWindow(908), getMinimumAffordableAbilityCost(938)
- shouldCastRip(979), shouldUseBite(1000)
- isNearBy(1110)
- Status checks: isTigerPresent(1127), tigerLeft(1136), isRipPresent(1152), ripLeft(1161), isRakePresent(1187), rakeLeft(1196), isFFPresent(1216), ffLeft(1224), isDemoralizingRoarPresent(1241), isPouncePresent(1248), pounceLeft(1256)
- isGcdOk(1339)
- safeFF(1359)
- tigerSelfGCD(1385)
- SelfTest:register block (25 calls, lines 1752-1870)

**Functions to REMOVE from Druid.lua (moved to cat/bear/utility in Tasks 2/3/4):**
See Task 2 for cat functions, Task 3 for bear functions, Task 4 for utility functions.
For each function to remove, delete its entire block from `function macroTorch.Foo(...)` through its matching `end` keyword (top-level `end` at same indentation level).
Also remove any blank lines immediately preceding the removed function (but preserve them if they separate two kept functions).

**CAUTION -- do NOT remove these functions that look cat-only but are listed in D-01 as Druid.lua shared:**
- shouldUseShred (used by both cat and bear logic paths -- grep confirms regularAttack and bearRegularAttack both call it indirectly)
- isTrivialBattleOrPvp, isTrivialBattle (used by shouldCastRip and shouldUseBite -- shared)
- combatUrgentHPRestore, isFightStarted (used by both cat and bear)
- isKillShotOrLastChance (used by termMod, shouldCastRip, shouldUseBite)
- computeErps (used by shouldDoReshift, shouldCastFFDuringWaitWindow, getMinimumAffordableAbilityCost)
- shouldCastFFDuringWaitWindow, getMinimumAffordableAbilityCost (used by reshift logic -- shared)
- shouldCastRip, shouldUseBite (shared decision logic)
- isNearBy (called by shouldUseShred, shouldCastFFDuringWaitWindow, getMinimumAffordableAbilityCost)
- isGcdOk (called by readyClaw in cat and readyMaul in bear)
- safeFF (called by keepFF in cat and used directly in bear)
- tigerSelfGCD (used by safeTigerFury in cat and bearAtk in bear)
- All status-check functions (isRipPresent, ripLeft, etc.): called by shared helpers and cross-form logic
  </action>
  <verify>
    <automated>
      # Verify Druid.lua has all shared functions and no cat/bear/utility functions
      echo "=== Druid.lua function check ===" && \
      echo "Druid constructor: $(grep -c 'function macroTorch.Druid:new' classes/druid/Druid.lua)" && \
      echo "FIELD_FUNC_MAP: $(grep -c 'DRUID_FIELD_FUNC_MAP' classes/druid/Druid.lua)" && \
      echo "registerPlayerClass: $(grep -c 'registerPlayerClass' classes/druid/Druid.lua)" && \
      echo "shouldUseShred: $(grep -c 'shouldUseShred' classes/druid/Druid.lua)" && \
      echo "shouldCastRip: $(grep -c 'shouldCastRip' classes/druid/Druid.lua)" && \
      echo "shouldUseBite: $(grep -c 'shouldUseBite' classes/druid/Druid.lua)" && \
      echo "computeErps: $(grep -c 'computeErps' classes/druid/Druid.lua)" && \
      echo "safeFF: $(grep -c 'safeFF' classes/druid/Druid.lua)" && \
      echo "SpellTrace:register: $(grep -c 'SpellTrace:register' classes/druid/Druid.lua)" && \
      echo "SelfTest:register: $(grep -c 'SelfTest:register' classes/druid/Druid.lua)" && \
      echo "=== Must NOT have cat-only ===" && \
      echo "keepRip: $(grep -c 'keepRip' classes/druid/Druid.lua) (expect 0)" && \
      echo "keepRake: $(grep -c 'keepRake' classes/druid/Druid.lua) (expect 0)" && \
      echo "regularAttack: $(grep -c 'regularAttack' classes/druid/Druid.lua) (expect 0)" && \
      echo "=== Must NOT have bear-only ===" && \
      echo "bearAtk: $(grep -c 'bearAtk' classes/druid/Druid.lua) (expect 0)" && \
      echo "bearAoe: $(grep -c 'bearAoe' classes/druid/Druid.lua) (expect 0)" && \
      echo "=== Must NOT have utility ===" && \
      echo "druidBuffs: $(grep -c 'druidBuffs' classes/druid/Druid.lua) (expect 0)" && \
      echo "pokemonLoad: $(grep -c 'pokemonLoad' classes/druid/Druid.lua) (expect 0)" && \
      echo "=== Line count ===" && \
      wc -l classes/druid/Druid.lua
    </automated>
  </verify>
  <done>
    classes/druid/Druid.lua exists with ~500-600 lines containing: license header, constructor with all inner functions including catAtk, DRUID_FIELD_FUNC_MAP, instance creation + registerPlayerClass, all shared helpers (energy computation, status checks, decision helpers), SpellTrace:register() block (5 calls), SelfTest:register() block (25 calls). No cat-only functions, no bear-only functions, no utility functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract cat.lua -- all cat-only standalone functions</name>
  <files>classes/druid/cat.lua</files>
  <read_first>SM_Extend_Druid.lua (lines 257-307, 586-597, 639-667, 669-674, 675-706, 708-726, 728-765, 832-841, 843-849, 884-906, 970-977, 1024-1035, 1037-1076, 1078-1096, 1098-1108, 1119-1125, 1272-1283, 1286-1304, 1310-1337, 1346-1357, 1373-1383, 1400-1406, 1408-1423, 1482-1500)</read_first>
  <acceptance_criteria>
    <item>classes/druid/cat.lua exists, no license header needed (satellite file per PATTERNS.md)</item>
    <item>First line is a comment: `-- Druid cat form combat functions (extracted from SM_Extend_Druid.lua)`</item>
    <item>cat.lua contains ALL of these functions (extracted verbatim, no edits to function bodies): burstMod, regularAttack, otMod, termMod, cp5Bite, energyDischargeBeforeBite, oocMod, tryBiteKillShot, reshiftMod, shouldDoReshift, keepTigerFury, keepRip, dischargeEnergyChangeRelicAndRip, quickKeepRip, keepRake, keepFF, readyReshift, safeShred, readyShred, safeClaw, readyClaw, safeRake, safeRip, safeBite, readyBite, safeTigerFury, safePounce, readyCower, safeCower, atkPowerBurst</item>
    <item>command: `grep -c "^function macroTorch\." classes/druid/cat.lua` returns 30</item>
    <item>Verification command lists in task 1 pass: keepRip(0), keepRake(0), etc. in Druid.lua</item>
    <item>command: `grep "^function macroTorch\." classes/druid/cat.lua | sort` is exactly: atkPowerBurst, burstMod, cp5Bite, dischargeEnergyChangeRelicAndRip, energyDischargeBeforeBite, keepFF, keepRake, keepRip, keepTigerFury, oocMod, otMod, quickKeepRip, readyBite, readyClaw, readyCower, readyReshift, readyShred, regularAttack, reshiftMod, safeBite, safeClaw, safeCower, safePounce, safeRake, safeRip, safeShred, safeTigerFury, shouldDoReshift, termMod, tryBiteKillShot</item>
  </acceptance_criteria>
  <action>
Create `classes/druid/cat.lua` containing ONLY cat-specific standalone `macroTorch.*` functions extracted verbatim from SM_Extend_Druid.lua. No license header needed (Druid.lua is the entry file).

Start the file with a comment line: `-- Druid cat form combat functions (extracted from SM_Extend_Druid.lua)`

Extract each function by its complete block: from `function macroTorch.Foo(...)` through its matching top-level `end`. Preserve any blank lines and comments between/before functions exactly as they appear in the source. The functions can appear in the same relative order as SM_Extend_Druid.lua.

**Cat-only functions to extract (30 total):**

1. **burstMod** (line 257): rush/trinket activation module
2. **regularAttack** (line 586): Shred/Claw decision and execution
3. **otMod** (line 639): Cower/warning when approaching threat threshold
4. **termMod** (line 669): entry point for finisher decision, calls cp5Bite/energyDischargeBeforeBite
5. **cp5Bite** (line 675): Ferocious Bite at 5 combo points with timing optimization
6. **energyDischargeBeforeBite** (line 708): energy management before Bite cast
7. **oocMod** (line 728): Omen of Clarity handling -- Shred if behind or Claw if front
8. **tryBiteKillShot** (line 832): Bite during kill shot window
9. **reshiftMod** (line 843): reshift entry module, delegates to shouldDoReshift
10. **shouldDoReshift** (line 884): reshift decision logic (note: actual function name, not canDoReshift)
11. **keepTigerFury** (line 970): Tiger's Fury buff maintenance
12. **keepRip** (line 1024): 5-CP Rip maintenance with relic dance
13. **dischargeEnergyChangeRelicAndRip** (line 1037): energy discharge + relic swap + Rip cast sequence
14. **quickKeepRip** (line 1078): 1-2 CP Rip for quick battles/PvP
15. **keepRake** (line 1098): Rake DoT maintenance
16. **keepFF** (line 1119): Faerie Fire during free GCDs for ooc procs
17. **readyReshift** (line 1272): execute Reshift ability
18. **safeShred** (line 1286): energy check + readyShred delegation
19. **readyShred** (line 1290): Shred cast if spell ready
20. **safeClaw** (line 1298): energy check + readyClaw delegation
21. **readyClaw** (line 1302): Claw cast if spell ready
22. **safeRake** (line 1310): GCD/energy/range check + Savagery snapshot + Rake cast
23. **safeRip** (line 1323): GCD/energy/range check + Savagery snapshot + Rip cast
24. **safeBite** (line 1346): energy check + readyBite delegation
25. **readyBite** (line 1350): Bite cast if spell ready
26. **safeTigerFury** (line 1373): GCD + tigerSelfGCD check + Tiger's Fury cast
27. **safePounce** (line 1400): Pounce cast from stealth
28. **readyCower** (line 1408): Cower cast (threat reduction)
29. **safeCower** (line 1417): energy check + readyCower delegation
30. **atkPowerBurst** (line 1482): trinket + consumable attack power burst

**DO NOT include these functions** (they stay in Druid.lua per D-01 as shared/cross-form):
- isTrivialBattleOrPvp(599), isTrivialBattle(605), combatUrgentHPRestore(616), isFightStarted(626) -- shared battle detection
- isKillShotOrLastChance(767) -- shared kill shot prediction
- computeErps(851) -- shared energy calculation
- shouldCastFFDuringWaitWindow(908), getMinimumAffordableAbilityCost(938) -- shared decision
- shouldCastRip(979), shouldUseBite(1000) -- shared decision
- isNearBy(1110) -- cross-form proximity check
- isGcdOk(1339) -- cross-form GCD check
- safeFF(1359) -- cross-form FF cast
- tigerSelfGCD(1385) -- cross-form Tiger GCD tracking
- All is*Present and *Left status check functions -- cross-form
  </action>
  <verify>
    <automated>
      # Verify cat.lua has all cat-only functions and nothing else
      echo "=== Cat function count ===" && \
      FUNC_COUNT=$(grep -c "^function macroTorch\." classes/druid/cat.lua) && \
      echo "Cat functions: $FUNC_COUNT (expect 30)" && \
      test "$FUNC_COUNT" -eq 30 && echo "PASS: 30 functions" || echo "FAIL: expected 30 got $FUNC_COUNT" && \
      echo "=== Key cat functions ===" && \
      grep -c "function macroTorch.keepRip" classes/druid/cat.lua && \
      grep -c "function macroTorch.keepRake" classes/druid/cat.lua && \
      grep -c "function macroTorch.regularAttack" classes/druid/cat.lua && \
      grep -c "function macroTorch.atkPowerBurst" classes/druid/cat.lua && \
      grep -c "function macroTorch.shouldDoReshift" classes/druid/cat.lua && \
      echo "=== Must NOT have shared ===" && \
      echo "shouldUseShred: $(grep -c 'shouldUseShred' classes/druid/cat.lua) (expect 0)" && \
      echo "shouldCastRip: $(grep -c 'shouldCastRip' classes/druid/cat.lua) (expect 0)" && \
      echo "line count:" && wc -l classes/druid/cat.lua
    </automated>
  </verify>
  <done>
    classes/druid/cat.lua exists with exactly 30 cat-only standalone macroTorch.* functions extracted verbatim. No shared/cross-form functions. No license header.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract bear.lua and utility.lua</name>
  <files>classes/druid/bear.lua, classes/druid/utility.lua</files>
  <read_first>SM_Extend_Druid.lua (lines 1425-1473, 1555-1716, 1502-1553, 1623-1631, 1718-1750)</read_first>
  <acceptance_criteria>
    <item>classes/druid/bear.lua exists, first line is `-- Druid bear form combat functions (extracted from SM_Extend_Druid.lua)`</item>
    <item>bear.lua contains exactly 17 functions: safeMaul, readyMaul, safeSavageBite, readySavageBite, readyGrowl, safeDemoralizingRoar, readyDemoralizingRoar, safeSwipe, readySwipe, bearOocMod, bearOtMod, bearDebuffMod, bearFFMod, bearRegularAttack, bearReshiftMod, bearAoe, bearAtk</item>
    <item>command: `grep -c "^function macroTorch\." classes/druid/bear.lua` returns 17</item>
    <item>classes/druid/utility.lua exists, first line is `-- Druid utility functions (extracted from SM_Extend_Druid.lua)`</item>
    <item>utility.lua contains exactly 5 functions: druidBuffs, druidStun, druidDefend, druidControl, pokemonLoad</item>
    <item>command: `grep -c "^function macroTorch\." classes/druid/utility.lua` returns 5</item>
    <item>Druid.lua no longer contains any bear or utility functions (verification from Task 1 confirms keepRip(0), bearAtk(0), druidBuffs(0), pokemonLoad(0))</item>
  </acceptance_criteria>
  <action>
Create `classes/druid/bear.lua` and `classes/druid/utility.lua` from SM_Extend_Druid.lua. No license headers (satellite files).

**classes/druid/bear.lua (17 functions):**

Start with comment: `-- Druid bear form combat functions (extracted from SM_Extend_Druid.lua)`

Extract these functions verbatim in source order:
1. **safeMaul** (line 1425): energy/rage check for Maul
2. **readyMaul** (line 1429): Maul cast
3. **safeSavageBite** (line 1437): rage check for Savage Bite
4. **readySavageBite** (line 1441): Savage Bite cast
5. **readyGrowl** (line 1449): Growl (taunt) cast
6. **safeDemoralizingRoar** (line 1457): rage check for Demo Roar
7. **readyDemoralizingRoar** (line 1461): Demo Roar cast
8. **safeSwipe** (line 1469): rage check for Swipe
9. **readySwipe** (line 1473): Swipe cast
10. **bearOocMod** (line 1555): Omen of Clarity (bear form): Swipe if ooc
11. **bearOtMod** (line 1563): threat management (bear/tank): Growl if losing aggro
12. **bearDebuffMod** (line 1585): Demo Roar maintenance
13. **bearFFMod** (line 1592): Faerie Fire during free GCDs (bear form)
14. **bearRegularAttack** (line 1602): normal bear attack: Maul + Savage Bite rage dump
15. **bearReshiftMod** (line 1615): bear form reshift when rage is 0
16. **bearAoe** (line 1633): AoE: Swipe or Demo Roar based on rage
17. **bearAtk** (line 1651): main bear entry point -- full rotation logic

**classes/druid/utility.lua (5 functions):**

Start with comment: `-- Druid utility functions (extracted from SM_Extend_Druid.lua)`

Extract these functions verbatim in source order:
1. **druidBuffs** (line 1502): MotW + Thorns + Nature's Grasp self-buff
2. **druidStun** (line 1515): bear form Bash/Charge stun logic
3. **druidDefend** (line 1536): Barkskin + Frenzied Regen defensive cooldowns
4. **druidControl** (line 1623): bear form control abilities (note: appears between bear functions in source)
5. **pokemonLoad** (line 1718): pet/minion summoning system (Gnomish Battle Chicken, Arcanite Dragonling, etc.)

**AFTER extraction, verify Druid.lua no longer has these functions:**
In Druid.lua, the following function blocks should have been removed (during Task 1):
- All bear safe/ready pairs (safeMaul through readySwipe)
- All bear modules (bearOocMod through bearAoe, bearAtk)
- All utility functions (druidBuffs, druidStun, druidDefend, druidControl, pokemonLoad)
  </action>
  <verify>
    <automated>
      # Verify bear.lua
      echo "=== Bear function count ===" && \
      BEAR_COUNT=$(grep -c "^function macroTorch\." classes/druid/bear.lua) && \
      echo "Bear functions: $BEAR_COUNT (expect 17)" && \
      test "$BEAR_COUNT" -eq 17 && echo "PASS: 17 functions" || echo "FAIL: expected 17 got $BEAR_COUNT" && \
      grep -c "function macroTorch.bearAtk" classes/druid/bear.lua && \
      grep -c "function macroTorch.bearAoe" classes/druid/bear.lua && \
      grep -c "function macroTorch.bearOocMod" classes/druid/bear.lua && \
      echo "=== Utility function count ===" && \
      UTIL_COUNT=$(grep -c "^function macroTorch\." classes/druid/utility.lua) && \
      echo "Utility functions: $UTIL_COUNT (expect 5)" && \
      test "$UTIL_COUNT" -eq 5 && echo "PASS: 5 functions" || echo "FAIL: expected 5 got $UTIL_COUNT" && \
      grep -c "function macroTorch.druidBuffs" classes/druid/utility.lua && \
      grep -c "function macroTorch.pokemonLoad" classes/druid/utility.lua && \
      echo "=== Cross-verify Druid.lua is clean ===" && \
      echo "bear functions in Druid.lua: $(grep -c 'bearAtk\|bearAoe\|bearOocMod\|bearOtMod\|bearDebuffMod\|bearFFMod\|bearRegularAttack\|bearReshiftMod' classes/druid/Druid.lua) (expect 0)" && \
      echo "utility in Druid.lua: $(grep -c 'druidBuffs\|druidStun\|druidDefend\|druidControl\|pokemonLoad' classes/druid/Druid.lua) (expect 0)" && \
      echo "line counts:" && \
      wc -l classes/druid/bear.lua classes/druid/utility.lua
    </automated>
  </verify>
  <done>
    classes/druid/bear.lua (17 bear-only functions) and classes/druid/utility.lua (5 utility functions) exist with all functions extracted verbatim. Druid.lua contains no bear or utility functions. The 4-file Druid split is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system (build.sh reads build_order.txt) | build_order.txt entries control which files are concatenated; no user input |
| File concatenation | Private, non-hostile environment -- no code injection risk from local files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | build.sh file concatenation | accept | Source files are version-controlled; attacker would need git write access to inject |
| T-04-02 | Spoofing | build_order.txt paths | accept | Paths point to local files within repo; no remote fetch |
| T-04-03 | Elevation | None | accept | No privilege escalation possible in WoW addon Lua sandbox |
</threat_model>

<verification>
```bash
# Total function count across all 4 files should match original
ORIG=$(grep -c "^function macroTorch\." SM_Extend_Druid.lua)
DRU=$(grep -c "^function macroTorch\." classes/druid/Druid.lua)
CAT=$(grep -c "^function macroTorch\." classes/druid/cat.lua)
BEAR=$(grep -c "^function macroTorch\." classes/druid/bear.lua)
UTIL=$(grep -c "^function macroTorch\." classes/druid/utility.lua)
NEW=$((DRU + CAT + BEAR + UTIL))
echo "Original: $ORIG, New total: $NEW (DRU=$DRU CAT=$CAT BEAR=$BEAR UTIL=$UTIL)"
# Should be: NEW = ORIG (all 92 functions preserved exactly once)

# No function duplicates across files
echo "Functions appearing in multiple files:"
grep -h "^function macroTorch\." classes/druid/Druid.lua classes/druid/cat.lua classes/druid/bear.lua classes/druid/utility.lua | sort | uniq -d
# Expected: empty output (no duplicates)

# Line count sanity
wc -l classes/druid/*.lua
# Druid.lua ~500-600, cat.lua ~600-700, bear.lua ~200-250, utility.lua ~150-200
```
</verification>

<success_criteria>
- All 92 top-level macroTorch.* functions from SM_Extend_Druid.lua appear exactly once across the 4 new files
- No function body was edited -- all extractions are verbatim
- Druid.lua contains constructor with all inner functions (including catAtk), FIELD_FUNC_MAP, shared helpers, registrations
- cat.lua contains 30 cat-only standalone functions
- bear.lua contains 17 bear-only functions including bearAtk
- utility.lua contains 5 utility functions
- SM_Extend_Druid.lua is still present (deleted later in Plan 03 during atomic commit)
</success_criteria>

<output>
Create .planning/phases/04-class-files/04-01-SUMMARY.md when done
</output>