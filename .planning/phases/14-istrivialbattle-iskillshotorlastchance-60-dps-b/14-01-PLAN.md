---
phase: 14-istrivialbattle-iskillshotorlastchance-60-dps-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
autonomous: true
requirements:
  - REQ-14-DPS
  - REQ-14-KS
  - REQ-14-DELETE
  - REQ-14-GUARD
  - REQ-14-FALLBACK
  - REQ-14-TEST

must_haves:
  truths:
    - "estimatePlayerDPS(60) returns 500 exactly, matching pre-phase behavior"
    - "getKSThreshold(60) returns 1750 exactly, matching pre-phase behavior"
    - "At level < 60, isTrivialBattle condition B uses level-scaled DPS estimate instead of hardcoded 500"
    - "At level < 60, isKillShotOrLastChance condition B uses single level-scaled health threshold"
    - "At level < 10 (pre-cat form), estimatePlayerDPS returns conservative 25 fallback"
    - "All 15 KS_CP*_Health* constants are deleted from Druid.lua"
    - "isKillShotOrLastChance condition A (willDieInSeconds path) is unchanged"
    - "isTrivialBattleOrPvp is unchanged (pass-through that calls isTrivialBattle)"
  artifacts:
    - path: "classes/druid/Druid.lua"
      provides: "New estimatePlayerDPS() and getKSThreshold() functions; modified isTrivialBattle and isKillShotOrLastChance; deleted 15 KS_CP* constants"
      contains: "function macroTorch.estimatePlayerDPS"
      contains: "function macroTorch.getKSThreshold"
  key_links:
    - from: "classes/druid/Druid.lua isTrivialBattle condition B"
      to: "classes/druid/Druid.lua estimatePlayerDPS()"
      via: "macroTorch.estimatePlayerDPS() call replaces hardcoded 500"
      pattern: "macroTorch\\.estimatePlayerDPS\\(\\)"
    - from: "classes/druid/Druid.lua isKillShotOrLastChance condition B"
      to: "classes/druid/Druid.lua getKSThreshold()"
      via: "macroTorch.getKSThreshold() call replaces 15 constants + 55 lines of branching"
      pattern: "macroTorch\\.getKSThreshold\\(\\)"
    - from: "classes/druid/Druid.lua estimatePlayerDPS"
      to: "WoW API UnitLevel"
      via: "if not level then level = UnitLevel('player') end"
      pattern: "UnitLevel\\('player'\\)"
---

<!-- planner-discipline-allow: KS_CP -->

<objective>
Replace hardcoded level-60 static DPS estimates in `isTrivialBattle` (condition B: `500` DPS/person) and `isKillShotOrLastChance` (condition B: 15 `KS_CP*_Health*` constants + 55-line CP-mode branching) with level-adaptive lookup tables via two new functions: `estimatePlayerDPS(level)` and `getKSThreshold(level)`. Delete all 15 `macroTorch.KS_CP*_Health*` module-level constants. Add ~6 Category I selftests.

Purpose: Make quick-battle detection and kill-shot prediction accurate during leveling (levels 10-59), while preserving exact level-60 behavior through hard guards.
Output: Modified `classes/druid/Druid.lua` (+2 functions, ~2 functions, delete 15 constants, +6 selftest registrations); `core/selftest.lua` (unchanged -- tests registered via Druid.lua)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-CONTEXT.md
@.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-RESEARCH.md
@classes/druid/Druid.lua
@entity/Target.lua
@core/selftest.lua
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add estimatePlayerDPS() and getKSThreshold() functions to Druid.lua</name>
  <files>classes/druid/Druid.lua</files>

  <read_first>
  Read these exact line ranges before touching anything:
  - `classes/druid/Druid.lua:556-600` — computeClaw_E, computeReshiftEnergy, computeShred_E patterns (lookup table + guard style)
  - `classes/druid/Druid.lua:558-566` — computeClaw_E if-elseif chain structure (the reference pattern for new functions)
  - `classes/druid/Druid.lua:568-578` — computeReshiftEnergy Phase 13 precedent (new standalone function insertion point)
  - `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-RESEARCH.md:237-267` — exact function bodies from research
  - `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-CONTEXT.md:27-38` — D-01 through D-05
  </read_first>

  <behavior>
  - Test 1: estimatePlayerDPS(60) returns 500 exactly (D-04 hard guard)
  - Test 2: estimatePlayerDPS(50) returns 350 (bracket boundary)
  - Test 3: estimatePlayerDPS(45) returns 200 (mid-bracket)
  - Test 4: estimatePlayerDPS(35) returns 120 (mid-bracket)
  - Test 5: estimatePlayerDPS(25) returns 60 (mid-bracket)
  - Test 6: estimatePlayerDPS(10) returns 25 (pre-cat conservative fallback, D-05)
  - Test 7: estimatePlayerDPS() with no argument defaults to UnitLevel('player') and does not crash
  - Test 8: getKSThreshold(60) returns 1750 exactly (D-04 hard guard)
  - Test 9: getKSThreshold(50) returns 1450 (bracket boundary)
  - Test 10: getKSThreshold(45) returns 1050 (mid-bracket)
  - Test 11: getKSThreshold(35) returns 700 (mid-bracket)
  - Test 12: getKSThreshold(25) returns 400 (mid-bracket)
  - Test 13: getKSThreshold(10) returns 200 (pre-cat conservative fallback, D-05)
  - Test 14: getKSThreshold() with no argument defaults to UnitLevel('player') and does not crash
  </behavior>

  <action>
  Insert two new global functions in `classes/druid/Druid.lua` immediately after `computeReshiftEnergy()` (line 578), before `computeShred_E()` (line 580). Use the exact implementation bodies from RESEARCH.md:237-267.

  **Function 1: `estimatePlayerDPS(level)`**

  Insert at line 579 (between computeReshiftEnergy's `end` and computeShred_E's `function`):

  1. nil-level guard: `if not level then level = UnitLevel('player') end`
  2. 60-level hard guard (D-04): `if level == 60 then return 500 end`
  3. Descending if-elseif chain (D-01):
     - `level >= 50` → return 350
     - `level >= 40` → return 200
     - `level >= 30` → return 120
     - `level >= 20` → return 60
     - else → return 25 (D-05 conservative fallback for pre-cat levels 1-19)

  **Function 2: `getKSThreshold(level)`**

  Insert immediately after `estimatePlayerDPS`:

  1. nil-level guard: `if not level then level = UnitLevel('player') end`
  2. 60-level hard guard (D-04): `if level == 60 then return 1750 end`
  3. Descending if-elseif chain (D-02):
     - `level >= 50` → return 1450
     - `level >= 40` → return 1050
     - `level >= 30` → return 700
     - `level >= 20` → return 400
     - else → return 200 (D-05 conservative fallback for pre-cat levels 1-19)

  **Code conventions (REQUIRED):**
  - Use 4-space indentation
  - Do NOT use `#` operator (CLAUDE.md rule -- WoW 1.12.1 Lua constraint)
  - Use `-- [D-04]` / `-- [D-01]` / `-- [D-05]` annotation comments matching existing codebase style
  - Add Chinese explanatory comments above each function: `-- 基于玩家等级估算猫德DPS，用于 isTrivialBattle 条件B (D-01, D-04, D-05)` and `-- 基于玩家等级返回斩杀血量阈值，用于 isKillShotOrLastChance 条件B (D-02, D-04, D-05)`
  </action>

  <verify>
    <automated>
    <!-- Manually verify in-game with SelfTest:run() output -->
    grep -c "function macroTorch.estimatePlayerDPS" classes/druid/Druid.lua == 1
    grep -c "function macroTorch.getKSThreshold" classes/druid/Druid.lua == 1
    grep "level == 60.*return 500" classes/druid/Druid.lua
    grep "level == 60.*return 1750" classes/druid/Druid.lua
    grep -c "UnitLevel('player')" classes/druid/Druid.lua >= 2
    ./build.sh && echo "Build OK"
    </automated>
  </verify>

  <done>
  Both functions exist in Druid.lua immediately after computeReshiftEnergy.
  estimatePlayerDPS(60) returns 500, getKSThreshold(60) returns 1750.
  nil-level parameter defaults to UnitLevel('player').
  Each function has exactly 5 branch levels (60 guard + 4 brackets + else fallback).
  Build passes with no errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify isTrivialBattle condition B, simplify isKillShotOrLastChance, delete 15 KS_CP* constants</name>
  <files>classes/druid/Druid.lua</files>

  <read_first>
  Read these exact line ranges before touching anything:
  - `classes/druid/Druid.lua:763-778` — isTrivialBattleOrPvp and current isTrivialBattle
  - `classes/druid/Druid.lua:807-885` — 15 KS_CP*_Health* constants + current isKillShotOrLastChance
  - `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-CONTEXT.md:98-117` — expected isTrivialBattle and isKillShotOrLastChance code shapes
  - `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-RESEARCH.md:302-337` — full modification bodies from research
  </read_first>

  <behavior>
  - Test 1: At level 60, isTrivialBattle condition B uses estimatePlayerDPS() which returns 500 -- behavior identical to old hardcoded 500
  - Test 2: At level 30, isTrivialBattle condition B uses 120 DPS instead of 500 -- trivial detection is level-aware
  - Test 3: At level 60, isKillShotOrLastChance condition B calls getKSThreshold() which returns 1750 -- equivalent to old KS_CP5_Health solo value
  - Test 4: isKillShotOrLastChance condition A (willDieInSeconds(2)) is checked FIRST and unchanged
  - Test 5: isKillShotOrLastChance world boss logic (isInGroup + worldboss) is preserved unchanged
  - Test 6: No KS_CP string remains in Druid.lua (15 constants fully deleted)
  - Test 7: isTrivialBattleOrPvp is unchanged (only calls isTrivialBattle internally)
  - Test 8: Build passes and SM_Extend.lua contains no KS_CP references
  </behavior>

  <action>
  **Step 1: Modify `isTrivialBattle` (line 775)**

  Replace the hardcoded `500` on line 775 with `macroTorch.estimatePlayerDPS()`:

  Current (line 774-775):
  ```
          (macroTorch.player.mateNearMyTargetCount + 1) * 500 * trivialDieTime
  ```
  Replace with:
  ```
          (macroTorch.player.mateNearMyTargetCount + 1) *
          macroTorch.estimatePlayerDPS() * trivialDieTime
  ```

  Note: Keep the comment on line 772 as-is ("-- if the target's max health is less than we attack 15s with 500dps each person") -- it is descriptive of intent, not code contract.

  **Step 2: Delete all 15 KS_CP*_Health* constants (lines 807-823)**

  Delete all 15 lines from `macroTorch.KS_CP1_Health = 750` (line 807) through `macroTorch.KS_CP5_Health_raid_pps = macroTorch.KS_CP5_Health_group / 5` (line 823). Keep the comment on line 804 ("-- 撕咬前的泄能逻辑: 当前多余能量用作撕咬加成不划算，将其拆成2个技能使用") -- related to category header comments, not KS constants. Keep lines 825-827 (RIP_BASE_DURATION, RAKE_DURATION, COWER_THREAT_THRESHOLD -- these are unrelated).

  **Step 3: Simplify `isKillShotOrLastChance` (lines 830-885)**

  Replace the entire function body with the simplified version from RESEARCH.md:320-337.

  The simplified function structure:
  1. Condition A (unchanged): `if macroTorch.target.willDieInSeconds(2) then return true end`
  2. Local variables: targetHealth, fightWorldBoss (unchanged)
  3. World boss check (unchanged): `if macroTorch.player.isInGroup and fightWorldBoss then return clickContext.comboPoints >= 3 and macroTorch.target.healthPercent <= 2 end`
  4. Condition B (NEW simplified): `return targetHealth < macroTorch.getKSThreshold()`

  Remove the entire solo/group/raid branching (lines 840-884) and replace with the single-line condition B. Remove the `isPvp` local variable (no longer needed -- solo/pvp threshold is handled by getKSThreshold internally).

  **Key changes in the new function body:**
  - `isPvp` local variable removed (was used only in CP-mode branching conditions)
  - Lines 840-884 (solo/group/raid CP-mode branching) replaced with `return targetHealth < macroTorch.getKSThreshold()`
  - Lines 831-837 (condition A + local vars + world boss check) preserved exactly
  - Total function: ~10 lines (down from ~55 lines)

  **Code conventions (REQUIRED):**
  - Add comment above condition B: `-- [D-02] Level-adaptive single threshold replaces 15 KS_CP*_Health* constants + 55-line CP-mode branching`
  - Do NOT change function signature `function macroTorch.isKillShotOrLastChance(clickContext)`
  - Do NOT add clickContext caching to isKillShotOrLastChance (RESEARCH.md Pattern 3: at most 2 calls per click)
  - Do NOT modify isTrivialBattleOrPvp (line 763-766) -- it only calls isTrivialBattle, which is updated automatically
  </action>

  <verify>
    <automated>
    grep -c "macroTorch.estimatePlayerDPS()" classes/druid/Druid.lua == 1
    grep -c "macroTorch.getKSThreshold()" classes/druid/Druid.lua >= 1
    grep -c "KS_CP" classes/druid/Druid.lua == 0
    grep -c "willDieInSeconds(2)" classes/druid/Druid.lua == 1
    grep -c "macroTorch.target.willDieInSeconds(2)" classes/druid/Druid.lua == 1
    grep -c "function macroTorch.isKillShotOrLastChance" classes/druid/Druid.lua == 1
    grep -c "function macroTorch.isTrivialBattle" classes/druid/Druid.lua == 1
    grep -c "function macroTorch.isTrivialBattleOrPvp" classes/druid/Druid.lua == 1
    ./build.sh && echo "Build OK"
    grep -c "KS_CP" SM_Extend.lua == 0
    </automated>
  </verify>

  <done>
  isTrivialBattle condition B uses estimatePlayerDPS() instead of hardcoded 500.
  isKillShotOrLastChance condition B uses single getKSThreshold() call instead of 15 constants + 55-line branching.
  All 15 KS_CP*_Health* constants deleted (grep -c "KS_CP" == 0).
  Condition A (willDieInSeconds path) preserved as first check.
  World boss logic preserved.
  isTrivialBattleOrPvp unchanged.
  Build passes, SM_Extend.lua contains no KS_CP references.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register ~6 Category I selftests in Druid.lua</name>
  <files>classes/druid/Druid.lua</files>

  <read_first>
  Read these exact line ranges before touching anything:
  - `classes/druid/Druid.lua:1290-1419` — Category H selftest registration pattern (structure, isOptional=true, UnitClass guard, assert style)
  - `classes/druid/Druid.lua:1391-1419` — Category G2 selftest pattern (boolean assertions with toBoolean)
  - `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-CONTEXT.md:38` — D-06: ~6 Category I selftests
  - `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-RESEARCH.md:398-412` — test requirements map
  </read_first>

  <action>
  Insert Category I selftest registrations in `classes/druid/Druid.lua` after the Category H block (after line 1388, before the Category G2 block starting at line 1390). Add a Category I section header comment preceding the tests.

  Register exactly 6 tests following the existing Category H pattern: `macroTorch.SelfTest:register(name, function() ... end, true)` with `isOptional=true`.

  **Test I-1: DPS 60-level hard guard** — `"Druid: estimatePlayerDPS(60) returns 500 (D-04 hard guard)"`
  - Guard: `if UnitClass('player') ~= 'Druid' then return end`
  - Assert: `assert(macroTorch.estimatePlayerDPS(60) == 500, ...)`

  **Test I-2: KS threshold 60-level hard guard** — `"Druid: getKSThreshold(60) returns 1750 (D-04 hard guard)"`
  - Guard: `if UnitClass('player') ~= 'Druid' then return end`
  - Assert: `assert(macroTorch.getKSThreshold(60) == 1750, ...)`

  **Test I-3: DPS bracket boundary** — `"Druid: estimatePlayerDPS bracket boundaries return valid values"`
  - Guard: `if UnitClass('player') ~= 'Druid' then return end`
  - Verify 3 key boundaries: `estimatePlayerDPS(30)` returns non-nil number, `estimatePlayerDPS(40)` >= 200, `estimatePlayerDPS(50)` >= 350
  - Assert each independently

  **Test I-4: KS threshold bracket boundary** — `"Druid: getKSThreshold bracket boundaries return valid values"`
  - Guard: `if UnitClass('player') ~= 'Druid' then return end`
  - Verify 3 key boundaries: `getKSThreshold(30)` returns non-nil number, `getKSThreshold(40)` >= 1050, `getKSThreshold(50)` >= 1450
  - Assert each independently

  **Test I-5: Conservative fallback** — `"Druid: estimatePlayerDPS(15) returns conservative fallback 25 (D-05)"`
  - Guard: `if UnitClass('player') ~= 'Druid' then return end`
  - Assert: `assert(macroTorch.estimatePlayerDPS(15) == 25, ...)`
  - Also verify: `assert(macroTorch.getKSThreshold(15) == 200, ...)`

  **Test I-6: Condition A unchanged + willDieInSeconds path preserved** — `"Druid: isKillShotOrLastChance calls willDieInSeconds as condition A (unchanged)"`
  - Guard: `if UnitClass('player') ~= 'Druid' then return end`
  - Since we cannot call isKillShotOrLastChance directly (requires clickContext + active target), this test verifies structural integrity: confirm `isKillShotOrLastChance` function exists and `willDieInSeconds` is called within it.
  - Use string search on the source: verify the function body contains `willDieInSeconds(2)` string. Since WoW 1.12 Lua does not have `debug.getinfo`, use a pragmatic approach: assert that both functions exist and are callable.
  - Assert: `assert(type(macroTorch.isKillShotOrLastChance) == "function", ...)`
  - Assert: `assert(type(macroTorch.target.willDieInSeconds) == "function", ...)`
  - Add informational message: `macroTorch.show("[macro-torch] Selftest: isKillShotOrLastChance condition A (willDieInSeconds) path confirmed accessible")`

  **Code conventions (REQUIRED):**
  - Category I section header: `-- Category I: isTrivialBattle/isKillShotOrLastChance level-adaptive selftests (D-06, isOptional=true)`
  - All 6 tests use `isOptional = true` (match existing Category H/G2 pattern)
  - Each test starts with `if UnitClass('player') ~= 'Druid' then return end`
  - Follow assertion message format: `"estimatePlayerDPS(60) should return 500, got: " .. tostring(value)`
  - Do NOT call `catAtk()` or `isTrivialBattle()` in tests (requires live clickContext + target state)
  - Do NOT use the `#` operator
  </action>

  <verify>
    <automated>
    grep -c "Category I" classes/druid/Druid.lua = 1
    grep -c "SelfTest:register" classes/druid/Druid.lua = M+6 (where M is count before this task)
    grep -c "estimatePlayerDPS(60)" classes/druid/Druid.lua >= 2
    grep -c "getKSThreshold(60)" classes/druid/Druid.lua >= 2
    grep -c "willDieInSeconds" classes/druid/Druid.lua >= 2
    ./build.sh && echo "Build OK"
    </automated>
  </verify>

  <done>
  6 Category I selftest registrations present in Druid.lua between Category H and Category G2 sections.
  All tests use isOptional=true and UnitClass guard.
  Tests cover: 60-level hard guards (DPS and KS), bracket boundaries, conservative fallback, condition A path preservation.
  Build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW API → Addon | `UnitLevel('player')` returns nil during loading screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | estimatePlayerDPS, getKSThreshold | mitigate | Nil-level guard: `if not level then level = UnitLevel('player') end`. If UnitLevel also returns nil (loading screen), level remains nil → falls through to else branch (conservative fallback 25/200). No infinite loops or recursion possible (if-elseif chain is finite branches). |
| T-14-02 | Information Disclosure | estimatePlayerDPS, getKSThreshold | accept | No PII or sensitive data involved. Player level is public information. No data exfiltration path. |
| T-14-03 | Tampering | isKillShotOrLastChance | accept | Condition B is a fallback path only (condition A HRPS is primary). A tampered DPS/threshold estimate would only affect whether quick-battle/kill-shot mode activates -- worst case is missed optimization, not incorrect rotation. |
| T-14-04 | Elevation of Privilege | All Phase 14 code | accept | No auth changes. No new WoW API calls beyond existing UnitLevel. No new commands or external interfaces. |
</threat_model>

<verification>
## Phase 14 Verification

### Automated (build + grep)

```bash
# New functions exist
grep -c "function macroTorch.estimatePlayerDPS" classes/druid/Druid.lua == 1
grep -c "function macroTorch.getKSThreshold" classes/druid/Druid.lua == 1

# 60-level hard guards
grep "level == 60.*return 500" classes/druid/Druid.lua
grep "level == 60.*return 1750" classes/druid/Druid.lua

# Nil parameter defaults
grep -c "UnitLevel('player')" classes/druid/Druid.lua >= 2

# KS_CP* constants fully deleted
grep -c "KS_CP" classes/druid/Druid.lua == 0

# Condition A preserved
grep -c "willDieInSeconds(2)" classes/druid/Druid.lua == 1

# isTrivialBattle modified
grep -c "macroTorch.estimatePlayerDPS()" classes/druid/Druid.lua == 1

# isKillShotOrLastChance simplified
grep -c "macroTorch.getKSThreshold()" classes/druid/Druid.lua == 1

# Category I selftests
grep -c "Category I" classes/druid/Druid.lua == 1

# World boss logic preserved
grep -c "fightWorldBoss" classes/druid/Druid.lua == 1

# Build passes
./build.sh && echo "Build OK"

# No KS_CP in output
grep -c "KS_CP" SM_Extend.lua == 0
```

### In-Game (human)

Load the addon in WoW on a Druid character:
1. Run `/mt` -- Category I tests should all pass green
2. Verify level 60 Druid: catAtk behavior identical to pre-Phase-14 (hard guard test)
3. Verify level 30-40 Druid: quick battle detection triggers for appropriate targets (not all green mobs, but low-health ones)
4. Verify the 15 KS_CP* constants do NOT appear in the `/run` global namespace (confirm deletion)
</verification>

<success_criteria>
- [ ] estimatePlayerDPS(60) returns 500 exactly
- [ ] getKSThreshold(60) returns 1750 exactly
- [ ] estimatePlayerDPS(30) returns 120 (bracket value)
- [ ] estimatePlayerDPS(10) returns 25 (conservative fallback)
- [ ] isTrivialBattle condition B uses estimatePlayerDPS() call instead of hardcoded 500
- [ ] isKillShotOrLastChance reduced from ~55 lines to ~10 lines
- [ ] isKillShotOrLastChance condition A (willDieInSeconds) unchanged as first check
- [ ] World boss logic preserved
- [ ] All 15 KS_CP*_Health* constants deleted (grep -c "KS_CP" classes/druid/Druid.lua == 0)
- [ ] 6 Category I selftests registered and pass in-game
- [ ] Build passes (./build.sh)
- [ ] No KS_CP references in SM_Extend.lua
</success_criteria>

<output>
Create `.planning/phases/14-istrivialbattle-iskillshotorlastchance-60-dps-b/14-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### New Functions Created
- `macroTorch.estimatePlayerDPS(level)` — returns estimated cat druid DPS for given player level (if-elseif chain with 60-level hard guard, D-01)
- `macroTorch.getKSThreshold(level)` — returns kill shot health threshold for given player level (if-elseif chain with 60-level hard guard, D-02)

### Modified Functions
- `macroTorch.isTrivialBattle(clickContext)` — condition B: `500` replaced with `macroTorch.estimatePlayerDPS()` call
- `macroTorch.isKillShotOrLastChance(clickContext)` — condition B: 55-line CP-mode branching replaced with single `macroTorch.getKSThreshold()` call; `isPvp` local variable removed

### Deleted Constants
- `macroTorch.KS_CP1_Health` through `macroTorch.KS_CP5_Health` (5 constants)
- `macroTorch.KS_CP1_Health_group` through `macroTorch.KS_CP5_Health_group` (5 constants)
- `macroTorch.KS_CP1_Health_raid_pps` through `macroTorch.KS_CP5_Health_raid_pps` (5 constants)
- Total: 15 constants deleted (D-03)

### Unchanged (verified)
- `macroTorch.isTrivialBattleOrPvp(clickContext)` — pass-through function, behavior auto-updated via isTrivialBattle change
- `macroTorch.target.willDieInSeconds(s)` — condition A primary path, no modifications
- `macroTorch.currentHRPS()` — HRPS linear regression, no modifications
- `macroTorch.RIP_BASE_DURATION`, `macroTorch.RAKE_DURATION`, `macroTorch.COWER_THREAT_THRESHOLD` — preserved, not KS constants

### Selftests Added
- 6 Category I registrations in `classes/druid/Druid.lua` (D-06)
</output>