---
phase: 10-Druid-combo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/utility.lua
  - build_order.txt
autonomous: true
requirements:
  - D-05
  - D-17
  - D-20
  - D-21

must_haves:
  truths:
    - "catAtk no longer contains bear form routing logic"
    - "utility.lua contains only druidBuffs (no druidStun/druidDefend/druidControl)"
    - "build_order.txt includes combo.lua after utility.lua"
    - "build.sh produces SM_Extend.lua with all symbols intact after cleanup"
  artifacts:
    - path: "classes/druid/Druid.lua"
      provides: "catAtk without bear routing, no clickContext.isInBearForm cache"
      contains: "function obj.catAtk"
    - path: "classes/druid/utility.lua"
      provides: "druidBuffs only (3 functions deleted)"
      exports:
        - "macroTorch.druidBuffs"
    - path: "build_order.txt"
      provides: "Updated build order with combo.lua entry"
      contains: "classes/druid/combo.lua"
  key_links:
    - from: "catAtk in Druid.lua"
      to: "bearAtk in bear.lua"
      via: "NOW REMOVED — routing moved to druidAtk in combo.lua"
    - from: "build_order.txt"
      to: "classes/druid/combo.lua"
      via: "new line after classes/druid/utility.lua"
---

<objective>
Remove bear form routing from catAtk in Druid.lua, delete 3 unused functions (druidStun/druidDefend/druidControl) from utility.lua, and add combo.lua to build_order.txt.

Purpose: Complete the TODO of extracting bear routing from catAtk (D-05), clean up obsolete utility functions (D-17), and wire the new combo.lua into the build system (D-20). druidBuffs stays in utility.lua (D-21).

Output: Modified Druid.lua (bear routing removed), trimmed utility.lua (druidBuffs only), updated build_order.txt (combo.lua added)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-5-druid-druidatk-druidaoe-druidheal-druiddefend-druidcontrol/10-CONTEXT.md
@.planning/phases/10-5-druid-druidatk-druidaoe-druidheal-druiddefend-druidcontrol/10-RESEARCH.md
@classes/druid/Druid.lua
@classes/druid/utility.lua
@build_order.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove bear routing from Druid.lua catAtk, delete old functions from utility.lua</name>
  <files>classes/druid/Druid.lua, classes/druid/utility.lua</files>
  <read_first>
    - classes/druid/Druid.lua:340-428 — full catAtk function body, see lines 348 (isInBearForm cache) and 380-384 (bear routing)
    - classes/druid/utility.lua:1-57 — full file with druidBuffs (KEEP) and druidStun/druidDefend/druidControl (DELETE)
    - .claude/CLAUDE.md — dot syntax convention, ref field inheritance
  </read_first>
  <action>
    **Druid.lua changes — per D-05:**

    1. Delete line 348: `clickContext.isInBearForm = player.isInBearForm`
       - This cache was only consumed by the bear routing at lines 380-384. cat.lua module functions that reference `clickContext.isInBearForm` (burstMod at cat.lua:26/379/384, atkPowerBurst at cat.lua:379/384) will receive nil for it since catAtk no longer passes it. These guards check `not clickContext.isInBearForm` to skip bear-inappropriate buffs. Since catAtk is now only called when in cat form (routing happens in druidAtk), these guards become dead code but are harmless. Keep `clickContext.isInCatForm` at line 349 since burstMod and other modules use it.

    2. Delete lines 380-384: the bear routing block
       ```
       -- roughly bear form logic branch, TODO ...
       if clickContext.isInBearForm then
           macroTorch.bearAtk(clickContext.rough)
           return
       end
       ```
       This is the core change per D-05. catAtk should no longer route to bearAtk.

    **utility.lua changes — per D-17/D-21:**

    3. Delete macroTorch.druidStun() — lines 13-32. Logic is now merged into druidControl in combo.lua.

    4. Delete macroTorch.druidDefend() — lines 33-48. Replaced by new druidDefend in combo.lua.

    5. Delete macroTorch.druidControl() — lines 49-56. Replaced by new druidControl in combo.lua.

    6. KEEP macroTorch.druidBuffs() — lines 1-12 (per D-21: druidBuffs stays in utility.lua unchanged)
  </action>
  <verify>
    <automated>
grep -n "if clickContext.isInBearForm then" classes/druid/Druid.lua
# EXPECTED: no output (line deleted)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "clickContext.isInBearForm = player.isInBearForm" classes/druid/Druid.lua` returns 0
    - `grep "if clickContext.isInBearForm then" classes/druid/Druid.lua` returns 0
    - `grep "TODO 其实bear形态逻辑应该完全从catAtck逻辑中剥离出来" classes/druid/Druid.lua` returns 0
    - `grep "function macroTorch.druidBuffs" classes/druid/utility.lua` returns 1 (KEPT)
    - `grep "function macroTorch.druidStun" classes/druid/utility.lua` returns 0 (DELETED)
    - `grep "function macroTorch.druidDefend" classes/druid/utility.lua` returns 0 (DELETED)
    - `grep "function macroTorch.druidControl" classes/druid/utility.lua` returns 0 (DELETED)
    - `grep "function macroTorch.druidStun" classes/druid/combo.lua` returns 0 (not recreated — logic inlined in druidControl)
    - catAtk function in Druid.lua still exists and is syntactically valid (if-block removal between burstMod call and openerMod comment leaves surrounding code intact)
  </acceptance_criteria>
  <done>Bear routing removed from catAtk, 3 old functions deleted from utility.lua.</done>
</task>

<task type="auto">
  <name>Task 2: Add combo.lua to build_order.txt and verify build</name>
  <files>build_order.txt</files>
  <read_first>
    - build_order.txt:1-52 — current build order, especially the Druid section (lines 27-30)
    - classes/druid/combo.lua — new file created in Plan 01, verify it exists before proceeding
  </read_first>
  <action>
    **build_order.txt change — per D-20:**

    Insert `classes/druid/combo.lua` on a new line after line 30 (`classes/druid/utility.lua`).

    Resulting Druid section should be:
    ```
    classes/druid/Druid.lua
    classes/druid/cat.lua
    classes/druid/bear.lua
    classes/druid/utility.lua
    classes/druid/combo.lua
    ```

    Rationale (per D-20): combo.lua depends on utility.lua (druidBuffs, future-proofing) and global functions from cat.lua/bear.lua (bearAtk, bearAoe, burstMod, etc.). All dependency sources precede combo.lua in this order.

    **Build verification:**
    Run `./build.sh` and confirm SM_Extend.lua is produced without errors.
    Verify combo symbols are in the output:
    - `grep "function macroTorch.druidAtk" SM_Extend.lua`
  </action>
  <verify>
    <automated>grep "classes/druid/combo.lua" build_order.txt | grep -q "." && ./build.sh && grep "function macroTorch.druidAtk" SM_Extend.lua | grep -q "."</automated>
  </verify>
  <acceptance_criteria>
    - `grep "classes/druid/combo.lua" build_order.txt` returns exactly 1 match
    - `classes/druid/combo.lua` appears after `classes/druid/utility.lua` in build_order.txt
    - `./build.sh` exits with code 0 (no ERROR lines from strict mode)
    - `grep "function macroTorch.druidAtk" SM_Extend.lua` returns a match
    - `grep "function macroTorch.druidAoe" SM_Extend.lua` returns a match
    - `grep "function macroTorch.druidHeal" SM_Extend.lua` returns a match
    - `grep "function macroTorch.druidDefend" SM_Extend.lua` returns a match
    - `grep "function macroTorch.druidControl" SM_Extend.lua` returns a match
    - `grep "function macroTorch.druidStun" SM_Extend.lua` returns 0 (deleted from utility.lua, NOT present in combo.lua)
    - `grep "function macroTorch.druidBuffs" SM_Extend.lua` returns a match (still exists in utility.lua)
    - `grep "function macroTorch.catAtk" SM_Extend.lua` returns a match (catAtk still exists in Druid.lua)
    - `grep "function macroTorch.bearAtk" SM_Extend.lua` returns a match (bearAtk still exists in bear.lua)
  </acceptance_criteria>
  <done>build_order.txt updated, build.sh succeeds, all symbols present in SM_Extend.lua.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Client-side WoW addon in WoW's Lua sandbox. No network, auth, or data boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service | catAtk (Druid.lua) after bear routing removal | accept | Removing 4 lines from catAtk cannot introduce new execution paths. WoW script timeout handles any remaining issues. |
| T-10-04 | Information Disclosure | utility.lua after function deletion | accept | Three functions deleted with zero callers (RESEARCH.md verified: no external references). No runtime impact. |
| T-10-SC | Tampering | (no npm/pip/cargo installs) | N/A | No external package installs in this phase. |
</threat_model>

<verification>
After Task 2 completes:

```bash
# Regression: catAtk still exists and is functional (bear routing removed)
grep "function obj.catAtk" classes/druid/Druid.lua
grep "function macroTorch.catAtk" SM_Extend.lua

# Regression: druidBuffs still exists
grep "function macroTorch.druidBuffs" classes/druid/utility.lua
grep "function macroTorch.druidBuffs" SM_Extend.lua

# Old functions gone from everywhere
grep "function macroTorch.druidStun\|function macroTorch.druidDefend\|function macroTorch.druidControl" SM_Extend.lua
# EXPECTED: no results (present only in combo.lua as new implementations, not old stubs)

# New functions present in build artifact
grep -c "function macroTorch.druidAtk\|function macroTorch.druidAoe\|function macroTorch.druidHeal\|function macroTorch.druidDefend\|function macroTorch.druidControl" SM_Extend.lua
# EXPECTED: 5
```
</verification>

<success_criteria>
- catAtk no longer contains bear form routing or isInBearForm cache assignment
- utility.lua contains only druidBuffs (3 functions deleted)
- build_order.txt has combo.lua after utility.lua
- `./build.sh` succeeds (strict mode, no file-not-found errors)
- SM_Extend.lua contains all 5 new combo methods
- SM_Extend.lua no longer contains deleted old utility functions
</success_criteria>

<output>
Create `.planning/phases/10-5-druid-druidatk-druidaoe-druidheal-druiddefend-druidcontrol/10-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| (modified) `obj.catAtk` — bear routing removed | instance method | `classes/druid/Druid.lua` (lines 348, 380-384 deleted) |
| (modified) `macroTorch.druidBuffs` — retained | global function | `classes/druid/utility.lua` |
| (deleted) `macroTorch.druidStun` | removed | `classes/druid/utility.lua` |
| (deleted) `macroTorch.druidDefend` | removed | `classes/druid/utility.lua` |
| (deleted) `macroTorch.druidControl` | removed | `classes/druid/utility.lua` |
| `classes/druid/combo.lua` entry in build order | build config | `build_order.txt` |