---
phase: 02-events-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/spell_trace_immune.lua
  - core/events.lua
autonomous: true
requirements: [R3, R6]

must_haves:
  truths:
    - "spell_trace_immune.lua 包含 loadImmuneTable、loadDefiniteBleedingTable 和 spellsImmuneTracing（含 registerPeriodicTask）"
    - "events.lua 包含独立的 OnEvent Frame、14 个事件注册、eventHandle 函数和 SetScript 调用"
    - "eventHandle 内部通过 if-elseif dispatch 调用 combat_context.lua 和 spell_trace_core.lua 的函数"
    - "所有迁移的函数保持 macroTorch.* 全局命名不变，外部调用方不受影响"
  artifacts:
    - path: "core/spell_trace_immune.lua"
      provides: "Spell immune 追踪 + 免疫/流血确定性表加载"
      min_lines: 70
      exports:
        - "function macroTorch.spellsImmuneTracing"
        - "function macroTorch.loadImmuneTable"
        - "function macroTorch.loadDefiniteBleedingTable"
    - path: "core/events.lua"
      provides: "事件帧 + 事件注册 + eventHandle dispatch"
      min_lines: 100
      exports:
        - "function macroTorch.eventHandle"
        - "local frame = CreateFrame(\"Frame\")"
  key_links:
    - from: "core/spell_trace_immune.lua"
      to: "core/periodic.lua"
      via: "macroTorch.registerPeriodicTask('spellsImmuneTracing', ...)"
      pattern: "registerPeriodicTask.*spellsImmuneTracing"
    - from: "core/spell_trace_immune.lua"
      to: "core/spell_trace_core.lua"
      via: "consumeFailEvent/consumeLandEvent 在 spellsImmuneTracing 中调用"
      pattern: "macroTorch\\.consumeFailEvent|macroTorch\\.consumeLandEvent"
    - from: "core/events.lua"
      to: "core/combat_context.lua"
      via: "eventHandle 中的 PLAYER_REGEN_ENABLED/DISABLED dispatch"
      pattern: "macroTorch\\.onCombatExit|macroTorch\\.onCombatEnter"
    - from: "core/events.lua"
      to: "core/spell_trace_core.lua"
      via: "eventHandle 中的 UNIT_CASTEVENT/CHAT_MSG dispatch"
      pattern: "macroTorch\\.recordCastTable|macroTorch\\.CheckDodgeParryBlockResist"
    - from: "entity/Target.lua"
      to: "core/spell_trace_immune.lua"
      via: "loadImmuneTable/loadDefiniteBleedingTable 调用"
      pattern: "macroTorch\\.loadImmuneTable|macroTorch\\.loadDefiniteBleedingTable"
---

<objective>
创建 Phase 2 的免疫追踪和事件层文件：`core/spell_trace_immune.lua`（免疫追踪 + 确定性表加载）和 `core/events.lua`（事件帧 + 事件注册 + eventHandle dispatch）。

Purpose: 完成 battle_event_queue.lua 剩余内容的拆分，建立独立的事件帧和免疫追踪模块。
Output: 两个新的 core/ 模块文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-events-system/02-CONTEXT.md
@.planning/phases/02-events-system/02-RESEARCH.md
@battle_event_queue.lua
@core/periodic.lua
@core/class.lua
@entity/Target.lua
@core/spell_trace_core.lua
@core/combat_context.lua
</context>

<interfaces>
<!-- spell_trace_immune.lua 的调用方需要知道的接口 -->

entity/Target.lua 调用（共 5 处）:
```lua
-- Target.lua:29,39,50 — 在 isImmune, recordImmune, removeImmune 中调用
macroTorch.loadImmuneTable()
-- Target.lua:59,70 — 在 recordDefiniteBleeding, isDefiniteBleeding 中调用
macroTorch.loadDefiniteBleedingTable()
```

events.lua 的 eventHandle 调用 spell_trace_immune 的函数:
```lua
-- 不需要 — spellsImmuneTracing 通过 registerPeriodicTask 自动运行，eventHandle 不直接调用
```

<!-- events.lua 是被 WoW 客户端事件驱动的入口点 -->
<!-- 调用方都是 WoW FrameXML 引擎（OnEvent callback），不需要手动调用 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create core/spell_trace_immune.lua</name>
  <files>core/spell_trace_immune.lua</files>
  <read_first>
    battle_event_queue.lua — 源文件（行 165-202: spellsImmuneTracing + registerPeriodicTask; 行 434-468: loadImmuneTable + loadDefiniteBleedingTable）
    core/spell_trace_core.lua — 确认 consumeFailEvent/consumeLandEvent 签名（spellsImmuneTracing 依赖这些函数）
    core/periodic.lua — 参考 license header 格式
    entity/Target.lua — 确认 loadImmuneTable/loadDefiniteBleedingTable 调用方式
  </read_first>
  <action>
    创建 core/spell_trace_immune.lua，从 battle_event_queue.lua 精确迁移免疫追踪和确定性表加载函数。按 D-02 权威决策，loadImmuneTable 和 loadDefiniteBleedingTable 放入此文件（而非 combat_context.lua）。

    文件内容按以下顺序组装：
    1. Apache 2.0 license header（16 行）
    2. function macroTorch.spellsImmuneTracing()（battle_event_queue.lua 行 166-199，逐字复制）
       - 包含对 macroTorch.loadImmuneTable() 的调用
       - 包含 consumeFailEvent 和 consumeLandEvent 的遍历逻辑
       - 包含 macroTorch.show 调试调用
    3. macroTorch.registerPeriodicTask('spellsImmuneTracing', { interval = 0.1, task = macroTorch.spellsImmuneTracing })（行 201-202）
    4. function macroTorch.loadImmuneTable()（行 435-449，逐字复制）
    5. function macroTorch.loadDefiniteBleedingTable()（行 453-468，逐字复制）
    6. 文件末尾加一个空行

    迁移规则：
    - 每个函数的代码体逐字复制（包括注释），不做任何逻辑修改
    - 函数签名保持完全不变（macroTorch.* 全局函数）
    - 函数间用一个空行分隔
    - 不迁移任何 cast/fail/land 表管理函数（属于 spell_trace_core.lua）
    - 不迁移事件相关代码（Frame 创建、eventHandle 等）
  </action>
  <verify>
    <automated>bash -c '
errors=0
for func in spellsImmuneTracing loadImmuneTable loadDefiniteBleedingTable; do
  grep -q "function macroTorch.$func" core/spell_trace_immune.lua || { echo "MISSING: macroTorch.$func"; errors=$((errors+1)); }
done
# Verify spell_trace_core functions NOT here
grep -q "function macroTorch.recordCastTable" core/spell_trace_immune.lua && echo "WRONG: recordCastTable in immune" && errors=$((errors+1))
grep -q "function macroTorch.CheckDodgeParryBlockResist" core/spell_trace_immune.lua && echo "WRONG: CheckDodgeParryBlockResist in immune" && errors=$((errors+1))
# Verify events stuff NOT here
grep -q "function macroTorch.eventHandle" core/spell_trace_immune.lua && echo "WRONG: eventHandle in immune" && errors=$((errors+1))
# Verify registerPeriodicTask call
grep -q "registerPeriodicTask.*spellsImmuneTracing" core/spell_trace_immune.lua || { echo "MISSING: spellsImmuneTracing registration"; errors=$((errors+1)); }
# Line count
lines=$(wc -l < core/spell_trace_immune.lua)
if [ "$lines" -gt 100 ]; then echo "OVER 100: $lines lines"; errors=$((errors+1)); fi
exit $errors
'</automated>
  </verify>
  <acceptance_criteria>
    - core/spell_trace_immune.lua 存在，≤ 100 行，含有 Apache 2.0 license header
    - spellsImmuneTracing, loadImmuneTable, loadDefiniteBleedingTable 三个函数完整迁移
    - registerPeriodicTask('spellsImmuneTracing', ...) 注册调用存在
    - spellsImmuneTracing 内部正确调用 consumeFailEvent 和 consumeLandEvent（依赖 spell_trace_core.lua 在 build_order.txt 中先加载）
    - loadImmuneTable 正确访问 SM_EXTEND.immuneTable[playerCls] 并赋值到 macroTorch.context.immuneTable
    - loadDefiniteBleedingTable 正确访问 SM_EXTEND.definiteBleedingTable[playerCls] 并赋值到 macroTorch.context.definiteBleedingTable
    - entity/Target.lua 的 5 处 loadImmuneTable/loadDefiniteBleedingTable 调用在新文件位置下功能完整
  </acceptance_criteria>
  <done>core/spell_trace_immune.lua 创建完成，免疫追踪和确定性表加载已迁移。entity/Target.lua 的 loadImmuneTable (x3) + loadDefiniteBleedingTable (x2) 调用不受影响。</done>
</task>

<task type="auto">
  <name>Task 2: Create core/events.lua</name>
  <files>core/events.lua</files>
  <read_first>
    battle_event_queue.lua — 源文件（行 44-70: Frame 创建 + 14 个事件注册; 行 71-148: eventHandle 函数; 行 150: SetScript 调用）
    core/combat_context.lua — 确认 onCombatExit/onCombatEnter/onPlayerEnteringWorld 函数签名（eventHandle 调用这些函数）
    core/spell_trace_core.lua — 确认 CheckDodgeParryBlockResist 和 recordCastTable 签名（eventHandle 调用这些函数）
    core/class.lua — 参考 license header 格式
    .planning/phases/02-events-system/02-CONTEXT.md — 确认 D-01 集中式事件注册决策、D-03 直接函数调用决策
  </read_first>
  <action>
    创建 core/events.lua，从 battle_event_queue.lua 迁移事件帧基础设施和 eventHandle 函数。按 D-01 采用集中式事件注册 + if-elseif dispatch。

    文件内容按以下顺序组装：
    1. Apache 2.0 license header（16 行）
    2. 事件帧创建和注册（battle_event_queue.lua 行 44-69，逐字复制）:
       - local frame = CreateFrame("Frame")
       - frame:RegisterEvent("PLAYER_ENTERING_WORLD")
       - frame:RegisterEvent("PLAYER_TARGET_CHANGED")
       - frame:RegisterEvent("SPELLCAST_START")
       - frame:RegisterEvent("SPELLCAST_STOP")
       - frame:RegisterEvent("SPELLCAST_FAILED")
       - frame:RegisterEvent("SPELLCAST_INTERRUPTED")
       - frame:RegisterEvent("PLAYER_REGEN_ENABLED")
       - frame:RegisterEvent("PLAYER_REGEN_DISABLED")
       - frame:RegisterEvent("CHAT_MSG_COMBAT_SELF_MISSES")
       - frame:RegisterEvent("CHAT_MSG_SPELL_SELF_DAMAGE")
       - frame:RegisterEvent("CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE")
       - frame:RegisterEvent("CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE")
       - frame:RegisterEvent("UI_ERROR_MESSAGE")
       - SUPERWOW_STRING 条件注册 "UNIT_CASTEVENT"
    3. function macroTorch.eventHandle()（行 71-148，大部分逐字复制，但战斗进出和进入世界分支改为调用 combat_context 函数）:
       - PLAYER_ENTERING_WORLD 分支：调用 macroTorch.onPlayerEnteringWorld()（替代原来的 inline 逻辑）
         - 原代码: `macroTorch.player = macroTorch.initPlayer(); macroTorch.loginContext = {}`
         - 改为: `macroTorch.onPlayerEnteringWorld()`
         - 添加预留注释: `-- Phase 3: macroTorch.SelfTest:run()`
       - PLAYER_TARGET_CHANGED: 保持不变（逐字复制，含 macroTorch.show 调试调用）
       - SPELLCAST_START/STOP/FAILED/INTERRUPTED: 保持不变（空分支或注释）
       - PLAYER_REGEN_ENABLED 分支：调用 macroTorch.onCombatExit()（替代原来的 inline 逻辑）
         - 原代码: 行 94-100
         - 改为: `macroTorch.onCombatExit()`
       - PLAYER_REGEN_DISABLED 分支：调用 macroTorch.onCombatEnter()（替代原来的 inline 逻辑）
         - 原代码: 行 101-107
         - 改为: `macroTorch.onCombatEnter()`
       - CHAT_MSG_COMBAT_SELF_MISSES / CHAT_MSG_SPELL_SELF_DAMAGE: 保持不变（逐字复制）
       - PLAYER_DEAD: 保持不变
       - CHAT_MSG_SPELL_PERIODIC_SELF_BUFFS: 保持不变
       - CHAT_MSG_SPELL_AURA_GONE_SELF: 保持不变
       - CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE: 保持不变（空分支）
       - CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE: 保持不变（空分支）
       - UNIT_CASTEVENT: 保持不变（逐字复制，含 recordCastTable 调用）
       - RAW_COMBATLOG: 保持不变（注释掉的调试代码）
       - UI_ERROR_MESSAGE: 保持不变（逐字复制，含 behindAttackFailedTime 记录）
       - CHAT_MSG_COMBAT_CREATURE_VS_SELF_MISSES: 保持不变
    4. frame:SetScript("OnEvent", macroTorch.eventHandle)（行 150，逐字复制）
    5. 文件末尾加一个空行

    关键约束：
    - 保留所有现有的注释（包括注释掉的事件注册如 PLAYER_LOGIN, PLAYER_DEAD 等）
    - 保留 macroTorch.show 调试调用
    - 保留 SUPERWOW_STRING 条件判断
    - eventHandle 内部 3 个分支改用 combat_context 函数调用（onCombatExit, onCombatEnter, onPlayerEnteringWorld），其余分支保持原样
  </action>
  <verify>
    <automated>bash -c '
errors=0
# Verify eventHandle exists
grep -q "function macroTorch.eventHandle" core/events.lua || { echo "MISSING: eventHandle"; errors=$((errors+1)); }
# Verify frame creation
grep -q "CreateFrame.*Frame" core/events.lua || { echo "MISSING: CreateFrame"; errors=$((errors+1)); }
# Verify SetScript
grep -q "SetScript.*OnEvent.*macroTorch.eventHandle" core/events.lua || { echo "MISSING: SetScript OnEvent"; errors=$((errors+1)); }
# Verify combat_context function calls (not inline)
grep -q "macroTorch.onCombatExit" core/events.lua || { echo "MISSING: onCombatExit call"; errors=$((errors+1)); }
grep -q "macroTorch.onCombatEnter" core/events.lua || { echo "MISSING: onCombatEnter call"; errors=$((errors+1)); }
grep -q "macroTorch.onPlayerEnteringWorld" core/events.lua || { echo "MISSING: onPlayerEnteringWorld call"; errors=$((errors+1)); }
# Verify critical event registrations exist
for evt in PLAYER_ENTERING_WORLD PLAYER_TARGET_CHANGED SPELLCAST_START PLAYER_REGEN_ENABLED PLAYER_REGEN_DISABLED CHAT_MSG_COMBAT_SELF_MISSES UNIT_CASTEVENT UI_ERROR_MESSAGE; do
  grep -q "RegisterEvent.*$evt" core/events.lua || { echo "MISSING RegisterEvent: $evt"; errors=$((errors+1)); }
done
# Verify spell_trace_core functions NOT defined here (only called)
grep -q "function macroTorch.recordCastTable" core/events.lua && echo "WRONG: recordCastTable defined in events" && errors=$((errors+1))
grep -q "function macroTorch.CheckDodgeParryBlockResist" core/events.lua && echo "WRONG: CheckDodgeParryBlockResist defined in events" && errors=$((errors+1))
# Line count
lines=$(wc -l < core/events.lua)
if [ "$lines" -gt 140 ]; then echo "OVER 140: $lines lines (warn, should be ~120)"; fi
exit $errors
'</automated>
  </verify>
  <acceptance_criteria>
    - core/events.lua 存在，约 120-140 行，含有 Apache 2.0 license header
    - 独立的 local frame = CreateFrame("Frame") 存在（与 periodic.lua 的 frame 独立，均为 local 变量不冲突）
    - 14 个事件全部注册（含 SUPERWOW_STRING 条件注册的 UNIT_CASTEVENT）
    - eventHandle 函数完整，if-elseif dispatch 正确调用 combat_context 和 spell_trace_core 函数
    - PLAYER_REGEN_ENABLED 分支调用 macroTorch.onCombatExit()（非 inline）
    - PLAYER_REGEN_DISABLED 分支调用 macroTorch.onCombatEnter()（非 inline）
    - PLAYER_ENTERING_WORLD 分支调用 macroTorch.onPlayerEnteringWorld() + 预留 SelfTest:run() 注释
    - SetScript("OnEvent", macroTorch.eventHandle) 正确绑定
    - 所有注释掉的保留事件注册（PLAYER_LOGIN, PLAYER_DEAD 等）保持原样
  </acceptance_criteria>
  <done>core/events.lua 创建完成，事件帧 + eventHandle 已迁移。所有 14 个事件注册正确，eventHandle 通过直接函数调用 dispatch 到 combat_context 和 spell_trace_core 模块。</done>
</task>

</tasks>

<verification>
```bash
# Plan 02-02 整体验证

# spell_trace_immune.lua 验证
grep -c "function macroTorch.spellsImmuneTracing" core/spell_trace_immune.lua       # 期望: 1
grep -c "function macroTorch.loadImmuneTable" core/spell_trace_immune.lua            # 期望: 1
grep -c "function macroTorch.loadDefiniteBleedingTable" core/spell_trace_immune.lua  # 期望: 1
grep -c "registerPeriodicTask.*spellsImmuneTracing" core/spell_trace_immune.lua      # 期望: 1

# events.lua 验证
grep -c "function macroTorch.eventHandle" core/events.lua                # 期望: 1
grep -c "CreateFrame.*Frame" core/events.lua                             # 期望: 1
grep -c "SetScript.*OnEvent" core/events.lua                             # 期望: 1
grep -c "macroTorch.onCombatExit" core/events.lua                        # 期望: 1
grep -c "macroTorch.onCombatEnter" core/events.lua                       # 期望: 1
grep -c "macroTorch.onPlayerEnteringWorld" core/events.lua               # 期望: 1

# 验证不应在事件文件中的函数定义
grep -c "function macroTorch.recordCastTable" core/events.lua            # 期望: 0
grep -c "function macroTorch.CheckDodgeParryBlockResist" core/events.lua # 期望: 0

# 验证所有 20 个函数在三个源文件中完整覆盖（不含 combat_context.lua 的 3 个）
echo "=== spell_trace_core functions ==="
for func in setSpellTracing setSpellTracingByName setTraceSpellImmune setTraceSpellImmuneByName maintainLandTables recordCastTable recordFailTable computeLandTable consumeLandEvent consumeFailEvent peekCastEvent peekFailEvent peekLandEvent landTableAnyMatch landTableAllMatch CheckDodgeParryBlockResist; do
  grep -l "function macroTorch.$func" core/spell_trace_core.lua || echo "MISSING: $func in spell_trace_core"
done

echo "=== spell_trace_immune functions ==="
for func in spellsImmuneTracing loadImmuneTable loadDefiniteBleedingTable; do
  grep -l "function macroTorch.$func" core/spell_trace_immune.lua || echo "MISSING: $func in spell_trace_immune"
done

echo "=== events functions ==="
for func in eventHandle; do
  grep -l "function macroTorch.$func" core/events.lua || echo "MISSING: $func in events"
done
```
</verification>

<success_criteria>
- core/spell_trace_immune.lua 和 core/events.lua 均存在且含 Apache 2.0 license
- spell_trace_immune.lua ≤ 100 行，含 3 个函数 + registerPeriodicTask
- events.lua 约 120-140 行，含独立 eventFrame + 14 个事件注册 + eventHandle + SetScript
- eventHandle 的 3 个分支改用 combat_context 函数调用（onCombatExit/onCombatEnter/onPlayerEnteringWorld），其余分支保持原样
- 外部调用方完整：entity/Target.lua 的 loadImmuneTable (x3) + loadDefiniteBleedingTable (x2) 在 spell_trace_immune.lua 中定义
- SM_Extend_Druid.lua 的 setSpellTracing (x4) + setTraceSpellImmune (x4) 在 spell_trace_core.lua 中定义
</success_criteria>

<output>
Create `.planning/phases/02-events-system/02-02-SUMMARY.md` when done
</output>