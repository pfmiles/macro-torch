---
phase: 02-events-system
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - build_order.txt
autonomous: true
requirements: [R3, R6]

must_haves:
  truths:
    - "build_order.txt 不再包含 battle_event_queue.lua 条目"
    - "build_order.txt 中 core/spell_trace.lua 已被替换为 core/spell_trace_core.lua + core/spell_trace_immune.lua"
    - "build_order.txt 中 core/ 文件顺序为 periodic → combat_context → spell_trace_core → spell_trace_immune → events（符合 D-05）"
    - "battle_event_queue.lua 文件已删除"
    - "./build.sh 执行成功，生成的 SM_Extend.lua 包含所有迁移的函数"
  artifacts:
    - path: "build_order.txt"
      provides: "声明式构建顺序列表"
      contains: "core/spell_trace_core.lua"
      contains: "core/spell_trace_immune.lua"
      not_contains: "battle_event_queue.lua"
      not_contains: "core/spell_trace.lua"
  key_links:
    - from: "core/spell_trace_immune.lua"
      to: "core/spell_trace_core.lua"
      via: "build_order.txt 加载顺序（immune 在 core 之后）"
      pattern: "(spell_trace_core.*spell_trace_immune|spell_trace_immune.*spell_trace_core)"
    - from: "core/events.lua"
      to: "core/combat_context.lua"
      via: "build_order.txt 加载顺序（events 在所有 core/ 之后）"
      pattern: "combat_context.*events"
---

<objective>
Phase 2 收尾：更新 build_order.txt（移除 battle_event_queue.lua + 拆分 core/spell_trace.lua 为两个文件），删除 battle_event_queue.lua 源文件，验证构建成功。

Purpose: 完成 Phase 2 拆分，确保构建系统正确加载全部 4 个新 core/ 模块，旧文件完全清理。
Output: 更新后的 build_order.txt，deleted battle_event_queue.lua，验证通过的构建产物。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-events-system/02-CONTEXT.md
@.planning/phases/02-events-system/02-RESEARCH.md
@build_order.txt
@build.sh
</context>

<interfaces>
<!-- build_order.txt 的结构参考 -->

当前 build_order.txt 第 17-18 行:
```
# combat events (Phase 2 will split this)
battle_event_queue.lua
```

当前 build_order.txt 第 24 行:
```
core/spell_trace.lua
```

目标状态:
- 移除第 18 行 battle_event_queue.lua
- 第 24 行 core/spell_trace.lua 替换为两行：
  ```
  core/spell_trace_core.lua
  core/spell_trace_immune.lua
  ```
- 最终 core/ 相关行顺序（D-05）: periodic → combat_context → spell_trace_core → spell_trace_immune → events
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update build_order.txt and delete battle_event_queue.lua</name>
  <files>build_order.txt</files>
  <read_first>
    build_order.txt — 完整读取，确认所有行的当前位置
    build.sh — 确认容错模式 `[ -f "$line" ] && cat` 在 Phase 2 仍然有效
  </read_first>
  <action>
    更新 build_order.txt 以反映 Phase 2 拆分结果：

    需要做的修改（按 D-04 和 D-05）:
    1. 删除 battle_event_queue.lua 条目。当前在第 18 行：
       ```
       battle_event_queue.lua
       ```
       删除这行，同时可以删除前面的注释行 "# combat events (Phase 2 will split this)" （第 17 行）。

    2. 在当前行 24 的 core/spell_trace.lua 位置，替换为两行：
       ```
       core/spell_trace_core.lua
       core/spell_trace_immune.lua
       ```

    3. 验证最终 core/ 相关文件的顺序符合 D-05：
       ```
       core/class.lua
       core/periodic.lua
       ... (entity/ files)
       core/combat_context.lua
       core/spell_trace_core.lua
       core/spell_trace_immune.lua
       core/events.lua
       core/selftest.lua
       ```

    确认容错模式：build.sh 中的 `[ -f "$line" ] && cat` 语法无需修改。Phase 2 创建的 4 个新文件现在都存在，build 会自动包含它们。

    4. 删除 battle_event_queue.lua 文件：
       在 build_order.txt 更新完成后，运行 `git rm battle_event_queue.lua` 删除源文件。

       **删除前确认**：运行验证脚本确认所有 20 个 macroTorch.* 函数在 4 个新文件中已重新定义（见 verify 命令）。
  </action>
  <verify>
    <automated>bash -c '
errors=0

# 1. battle_event_queue.lua 不在 build_order.txt 中
grep -q "battle_event_queue.lua" build_order.txt && { echo "FAIL: battle_event_queue.lua still in build_order.txt"; errors=$((errors+1)); }

# 2. spell_trace_core 和 spell_trace_immune 在 build_order.txt 中
grep -q "core/spell_trace_core.lua" build_order.txt || { echo "FAIL: spell_trace_core.lua not in build_order.txt"; errors=$((errors+1)); }
grep -q "core/spell_trace_immune.lua" build_order.txt || { echo "FAIL: spell_trace_immune.lua not in build_order.txt"; errors=$((errors+1)); }

# 3. 旧的 spell_trace.lua 不在 build_order.txt 中
grep -q "^core/spell_trace.lua$" build_order.txt && { echo "FAIL: core/spell_trace.lua (singular) still in build_order.txt"; errors=$((errors+1)); }

# 4. D-05 顺序验证
order=$(grep -n "core/periodic.lua\|core/combat_context.lua\|core/spell_trace_core.lua\|core/spell_trace_immune.lua\|core/events.lua" build_order.txt | cut -d: -f1 | tr "\n" " ")
echo "Core file order (line numbers): $order"
# 检查顺序单调递增
prev=0; for n in $order; do if [ "$n" -le "$prev" ]; then echo "FAIL: order violation at line $n (prev=$prev)"; errors=$((errors+1)); fi; prev=$n; done

# 5. 函数完整性：新旧文件覆盖全部 20 个函数
echo "=== Checking all 20 functions defined in new files ==="
all_ok=true
for func in \
  "setSpellTracing:spell_trace_core" \
  "setSpellTracingByName:spell_trace_core" \
  "setTraceSpellImmune:spell_trace_core" \
  "setTraceSpellImmuneByName:spell_trace_core" \
  "maintainLandTables:spell_trace_core" \
  "recordCastTable:spell_trace_core" \
  "recordFailTable:spell_trace_core" \
  "computeLandTable:spell_trace_core" \
  "consumeLandEvent:spell_trace_core" \
  "consumeFailEvent:spell_trace_core" \
  "peekCastEvent:spell_trace_core" \
  "peekFailEvent:spell_trace_core" \
  "peekLandEvent:spell_trace_core" \
  "landTableAnyMatch:spell_trace_core" \
  "landTableAllMatch:spell_trace_core" \
  "CheckDodgeParryBlockResist:spell_trace_core" \
  "spellsImmuneTracing:spell_trace_immune" \
  "loadImmuneTable:spell_trace_immune" \
  "loadDefiniteBleedingTable:spell_trace_immune" \
  "eventHandle:events" \
  "onCombatExit:combat_context" \
  "onCombatEnter:combat_context" \
  "onPlayerEnteringWorld:combat_context"; do
  func_name="${func%%:*}"
  file_suffix="${func##*:}"
  found=$(grep -l "function macroTorch.$func_name" core/"$file_suffix".lua 2>/dev/null)
  if [ -z "$found" ]; then
    echo "MISSING: macroTorch.$func_name (expected in core/${file_suffix}.lua)"
    all_ok=false
  fi
done
$all_ok || errors=$((errors+1))

# 6. battle_event_queue.lua 已删除
if [ -f battle_event_queue.lua ]; then echo "FAIL: battle_event_queue.lua still exists"; errors=$((errors+1)); fi

# 7. build.sh 执行成功
./build.sh && echo "Build OK" || { echo "FAIL: build.sh failed"; errors=$((errors+1)); }

# 8. 产物验证：关键符号在 SM_Extend.lua 中
grep -q "function macroTorch.eventHandle" SM_Extend.lua || { echo "FAIL: eventHandle not in SM_Extend.lua"; errors=$((errors+1)); }
grep -q "function macroTorch.CheckDodgeParryBlockResist" SM_Extend.lua || { echo "FAIL: CheckDodgeParryBlockResist not in SM_Extend.lua"; errors=$((errors+1)); }
grep -q "function macroTorch.loadImmuneTable" SM_Extend.lua || { echo "FAIL: loadImmuneTable not in SM_Extend.lua"; errors=$((errors+1)); }

exit $errors
'</automated>
  </verify>
  <acceptance_criteria>
    - build_order.txt 不再包含 battle_event_queue.lua 条目
    - build_order.txt 中 core/spell_trace.lua（单数）已被替换为 core/spell_trace_core.lua + core/spell_trace_immune.lua
    - build_order.txt 中 core/ 文件顺序：periodic → combat_context → spell_trace_core → spell_trace_immune → events（D-05）
    - battle_event_queue.lua 文件已从磁盘删除
    - ./build.sh 成功执行，无错误
    - SM_Extend.lua 产物中包含 eventHandle、CheckDodgeParryBlockResist、loadImmuneTable 等关键符号
    - 所有 23 个 macroTorch.* 函数（20 个迁移 + 3 个新提取的 combat_context 函数）在新文件中完整定义
  </acceptance_criteria>
  <done>build_order.txt 已更新（移除 battle_event_queue.lua，拆分 spell_trace 为两个文件），battle_event_queue.lua 已删除，build.sh 成功验证。</done>
</task>

</tasks>

<verification>
```bash
# Phase 2 最终汇总验证（与 ROADMAP.md Phase 2 汇总验证对齐）

# 1. 旧文件已删除
test ! -f battle_event_queue.lua || echo "FAIL: battle_event_queue.lua still exists"

# 2. 新模块非空且合理大小
wc -l core/events.lua core/combat_context.lua core/spell_trace_core.lua core/spell_trace_immune.lua
# 期望: 每个 ≤250 行

# 3. 关键函数分布正确
grep -c "function macroTorch.eventHandle" core/events.lua                           # 期望: 1
grep -c "function macroTorch.loadImmuneTable" core/spell_trace_immune.lua            # 期望: 1
grep -c "function macroTorch.CheckDodgeParryBlockResist" core/spell_trace_core.lua   # 期望: 1

# 4. 构建成功
./build.sh && echo "Build OK"

# 5. 产物中关键符号存在
grep -c "function macroTorch.eventHandle" SM_Extend.lua                              # 期望: 1
grep -c "function macroTorch.loadImmuneTable" SM_Extend.lua                          # 期望: 1
grep -c "function macroTorch.CheckDodgeParryBlockResist" SM_Extend.lua               # 期望: 1
```
</verification>

<success_criteria>
- build_order.txt 不包含 battle_event_queue.lua，spell_trace 已拆分为两个文件
- battle_event_queue.lua 文件和 build_order.txt 条目已完全删除
- ./build.sh 成功生成 SM_Extend.lua
- 产物中包含所有迁移的 macroTorch.* 函数
- 4 个新 core/ 模块各 ≤ 250 行
</success_criteria>

<output>
Create `.planning/phases/02-events-system/02-03-SUMMARY.md` when done
</output>