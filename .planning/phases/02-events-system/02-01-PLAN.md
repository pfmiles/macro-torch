---
phase: 02-events-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/combat_context.lua
  - core/spell_trace_core.lua
autonomous: true
requirements: [R3, R6]

must_haves:
  truths:
    - "combat_context.lua 包含 PLAYER_REGEN_ENABLED/DISABLED 的 inCombat flag 和 context 生命周期管理"
    - "spell_trace_core.lua 包含完整的 cast/fail/land 表管理系统和 CheckDodgeParryBlockResist"
    - "所有从 battle_event_queue.lua 迁移到 spell_trace_core.lua 的函数保持 macroTorch.* 全局命名不变"
    - "spell_trace_core.lua 中的 registerPeriodicTask 调用正常工作（引用 periodic.lua 已定义的函数）"
  artifacts:
    - path: "core/combat_context.lua"
      provides: "战斗进出状态管理（inCombat flag + context 生命周期）"
      min_lines: 20
    - path: "core/spell_trace_core.lua"
      provides: "Spell trace 核心表管理 + dodge/parry/block/resist 检测"
      min_lines: 200
      exports:
        - "macroTorch.DEBUFF_LAND_LAG"
        - "macroTorch.tracingSpells"
        - "macroTorch.traceSpellImmunes"
        - "function macroTorch.setSpellTracing"
        - "function macroTorch.setSpellTracingByName"
        - "function macroTorch.setTraceSpellImmune"
        - "function macroTorch.setTraceSpellImmuneByName"
        - "function macroTorch.maintainLandTables"
        - "function macroTorch.recordCastTable"
        - "function macroTorch.recordFailTable"
        - "function macroTorch.computeLandTable"
        - "function macroTorch.consumeLandEvent"
        - "function macroTorch.consumeFailEvent"
        - "function macroTorch.peekCastEvent"
        - "function macroTorch.peekFailEvent"
        - "function macroTorch.peekLandEvent"
        - "function macroTorch.landTableAnyMatch"
        - "function macroTorch.landTableAllMatch"
        - "function macroTorch.CheckDodgeParryBlockResist"
  key_links:
    - from: "core/spell_trace_core.lua"
      to: "core/periodic.lua"
      via: "macroTorch.registerPeriodicTask('maintainLandTables', ...)"
      pattern: "registerPeriodicTask.*maintainLandTables"
    - from: "core/spell_trace_core.lua"
      to: "core/periodic.lua"
      via: "macroTorch.LRUStack"
      pattern: "macroTorch\\.LRUStack:new"
    - from: "SM_Extend_Druid.lua"
      to: "core/spell_trace_core.lua"
      via: "setSpellTracing/setTraceSpellImmune 调用"
      pattern: "macroTorch\\.setSpellTracing|macroTorch\\.setTraceSpellImmune"
---

<objective>
创建 Phase 2 的基础层文件：`core/combat_context.lua`（战斗进出状态管理）和 `core/spell_trace_core.lua`（spell trace 核心表管理 + dodge/parry/block/resist 检测）。

Purpose: 将 battle_event_queue.lua 的战斗状态逻辑和 spell trace 数据层迁移到独立模块，建立后续文件（spell_trace_immune.lua, events.lua）的基础。
Output: 两个新的 core/ 模块文件，包含从 battle_event_queue.lua 精确提取的函数定义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-events-system/02-CONTEXT.md
@.planning/phases/02-events-system/02-RESEARCH.md
@battle_event_queue.lua
@core/periodic.lua
@core/class.lua
@entity/Target.lua
@core/class.lua
</context>

<interfaces>
<!-- spell_trace_core.lua 的调用方需要知道的接口 -->
<!-- 这些函数定义在本 plan 中，被 SM_Extend_Druid.lua 和 spell_trace_immune.lua 调用 -->

SM_Extend_Druid.lua 调用（共 8 处）:
```lua
macroTorch.setSpellTracing(spellGuid, spellName)
macroTorch.setTraceSpellImmune(spellName, spellDebuffTexture)
```

spell_trace_immune.lua 调用（spellsImmuneTracing 内部）:
```lua
macroTorch.consumeFailEvent(spellName, callback)  -- callback receives {timestamp, failType}
macroTorch.consumeLandEvent(spellName, callback)  -- callback receives timestamp
```

events.lua 调用:
```lua
macroTorch.CheckDodgeParryBlockResist("target", event, arg1)
macroTorch.recordCastTable(spellName)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create core/combat_context.lua</name>
  <files>core/combat_context.lua</files>
  <read_first>
    battle_event_queue.lua — 源文件（行 94-107: PLAYER_REGEN_ENABLED/DISABLED 处理逻辑）
    core/periodic.lua — 参考 license header 格式和模块结构
  </read_first>
  <action>
    创建 core/combat_context.lua，从 battle_event_queue.lua 提取战斗进出状态管理逻辑。

    文件内容：
    1. Apache 2.0 license header（16 行，与 core/class.lua 格式一致）
    2. 战斗进出状态管理逻辑，提取自 battle_event_queue.lua 行 94-107 的 PLAYER_REGEN_ENABLED/DISABLED event 分支。将事件回调参数（event, arg1）的依赖移除——combat_context.lua 提供独立的 macroTorch.* 函数供 events.lua 调用，不直接访问 WoW event 全局变量。

    提取逻辑：
    - 创建 `function macroTorch.onCombatExit()`: 设置 macroTorch.inCombat = false; 重置 macroTorch.context = {}; 调用 macroTorch.show('Exiting combat!')
    - 创建 `function macroTorch.onCombatEnter()`: 初始化 macroTorch.context = {} (如果不存在); 设置 macroTorch.inCombat = true; 调用 macroTorch.show('Entering combat!')
    - 创建 `function macroTorch.onPlayerEnteringWorld()`: 调用 macroTorch.player = macroTorch.initPlayer(); 初始化 macroTorch.loginContext = {}

    注意：PLAYER_ENTERING_WORLD 的完整处理保留在 events.lua 的 eventHandle 中（含 SelfTest:run 预留）。combat_context.lua 只提供被 eventHandle 调用的独立函数。

    文件末尾加一个空行。
  </action>
  <verify>
    <automated>grep -c "function macroTorch.onCombatExit" core/combat_context.lua | grep -q 1 && grep -c "function macroTorch.onCombatEnter" core/combat_context.lua | grep -q 1 && grep -c "function macroTorch.onPlayerEnteringWorld" core/combat_context.lua | grep -q 1 && grep -c "macroTorch.inCombat" core/combat_context.lua | grep -q 2 && grep -c "macroTorch.context" core/combat_context.lua | grep -q 4</automated>
  </verify>
  <acceptance_criteria>
    - core/combat_context.lua 文件存在，含有 Apache 2.0 license header
    - 包含三个函数：onCombatExit(), onCombatEnter(), onPlayerEnteringWorld()
    - onCombatExit 设置 inCombat=false + context={}
    - onCombatEnter 设置 context={} (if nil) + inCombat=true
    - onPlayerEnteringWorld 调用 initPlayer() + 初始化 loginContext
    - 文件不含任何 WoW event 全局变量引用（event, arg1 等）
    - 文件约 30-40 行（含 license）
  </acceptance_criteria>
  <done>core/combat_context.lua 创建完成，三个战斗状态管理函数可供 events.lua 的 eventHandle 直接调用。</done>
</task>

<task type="auto">
  <name>Task 2: Create core/spell_trace_core.lua</name>
  <files>core/spell_trace_core.lua</files>
  <read_first>
    battle_event_queue.lua — 源文件（完整读取，所有 spell trace 核心函数）
    core/periodic.lua — 确认 registerPeriodicTask 签名和 LRUStack 可用
    core/class.lua — 参考 license header 格式
  </read_first>
  <action>
    创建 core/spell_trace_core.lua，从 battle_event_queue.lua 精确迁移所有 spell trace 核心函数。

    文件内容按以下顺序组装：
    1. Apache 2.0 license header（16 行）
    2. DEBUFF_LAND_LAG 常量声明：`macroTorch.DEBUFF_LAND_LAG = 0.2`
    3. tracingSpells 初始化（if not then init end）和 setSpellTracing/setSpellTracingByName（行 4-21）
    4. traceSpellImmunes 初始化（if not then init end）和 setTraceSpellImmune/setTraceSpellImmuneByName（行 23-42）
    5. maintainLandTables 函数 + registerPeriodicTask 调用（行 153-162）
    6. recordCastTable（行 205-225）
    7. recordFailTable（行 229-252）
    8. computeLandTable（行 254-289）
    9. consumeLandEvent（行 291-300）
    10. consumeFailEvent（行 302-311）
    11. peekCastEvent（行 313-322）
    12. peekFailEvent（行 324-333）
    13. peekLandEvent（行 335-344）
    14. landTableAnyMatch（行 346-355）
    15. landTableAllMatch（行 357-366）
    16. CheckDodgeParryBlockResist（行 369-432）
    17. 文件末尾加一个空行

    迁移规则：
    - 每个函数的代码体逐字复制（包括注释和 macroTorch.show 调试调用），不做任何逻辑修改
    - 函数签名保持完全不变（macroTorch.* 全局函数）
    - 函数间用一个空行分隔
    - 不迁移 spellsImmuneTracing 和其 registerPeriodicTask（属于 spell_trace_immune.lua）
    - 不迁移 loadImmuneTable 和 loadDefiniteBleedingTable（属于 spell_trace_immune.lua）
    - 不迁移 Frame 创建/事件注册/eventHandle/SetScript（属于 events.lua）

    250 行限制处理：原始代码约 235 行 + 16 行 license = 251 行。将函数组间的双空行压缩为单空行，确保总行数 ≤ 250。
  </action>
  <verify>
    <automated>bash -c '
errors=0
for func in setSpellTracing setSpellTracingByName setTraceSpellImmune setTraceSpellImmuneByName maintainLandTables recordCastTable recordFailTable computeLandTable consumeLandEvent consumeFailEvent peekCastEvent peekFailEvent peekLandEvent landTableAnyMatch landTableAllMatch CheckDodgeParryBlockResist; do
  if ! grep -q "function macroTorch.$func" core/spell_trace_core.lua; then
    echo "MISSING: macroTorch.$func"
    errors=$((errors+1))
  fi
done
# Verify immune/loading functions NOT here (belong in spell_trace_immune.lua)
grep -q "function macroTorch.spellsImmuneTracing" core/spell_trace_core.lua && echo "WRONG: spellsImmuneTracing in spell_trace_core" && errors=$((errors+1))
grep -q "function macroTorch.loadImmuneTable" core/spell_trace_core.lua && echo "WRONG: loadImmuneTable in spell_trace_core" && errors=$((errors+1))
grep -q "function macroTorch.loadDefiniteBleedingTable" core/spell_trace_core.lua && echo "WRONG: loadDefiniteBleedingTable in spell_trace_core" && errors=$((errors+1))
# Verify events stuff NOT here
grep -q "function macroTorch.eventHandle" core/spell_trace_core.lua && echo "WRONG: eventHandle in spell_trace_core" && errors=$((errors+1))
# Line count check
lines=$(wc -l < core/spell_trace_core.lua)
if [ "$lines" -gt 250 ]; then echo "OVER 250: $lines lines"; errors=$((errors+1)); fi
# Verify registerPeriodicTask call
grep -q "registerPeriodicTask.*maintainLandTables" core/spell_trace_core.lua || { echo "MISSING: maintainLandTables registration"; errors=$((errors+1)); }
exit $errors
'</automated>
  </verify>
  <acceptance_criteria>
    - core/spell_trace_core.lua 存在，≤ 250 行，含有 Apache 2.0 license header
    - 17 个 macroTorch.* 函数全部包含且签名不变
    - DEBUFF_LAND_LAG 常量存在
    - tracingSpells 和 traceSpellImmunes 初始化块存在
    - registerPeriodicTask('maintainLandTables', ...) 注册调用存在
    - spellsImmuneTracing, loadImmuneTable, loadDefiniteBleedingTable 不在此文件中
    - eventHandle 和 CreateFrame/RegisterEvent/SetScript 不在此文件中
    - 所有函数保持原始逻辑不变（逐字复制，包括注释和 macroTorch.show 调用）
  </acceptance_criteria>
  <done>core/spell_trace_core.lua 创建完成，17 个 spell trace 核心函数全部迁移，existing external callers（SM_Extend_Druid.lua 的 setSpellTracing x4 + setTraceSpellImmune x4）引用的函数均已正确定义。</done>
</task>

</tasks>

<verification>
```bash
# Plan 02-01 整体验证
# 验证 combat_context.lua 存在且包含所有必需函数
grep -c "function macroTorch.onCombatExit" core/combat_context.lua
grep -c "function macroTorch.onCombatEnter" core/combat_context.lua
grep -c "function macroTorch.onPlayerEnteringWorld" core/combat_context.lua

# 验证 spell_trace_core.lua 包含所有 17 个核心函数
for func in setSpellTracing setSpellTracingByName setTraceSpellImmune setTraceSpellImmuneByName maintainLandTables recordCastTable recordFailTable computeLandTable consumeLandEvent consumeFailEvent peekCastEvent peekFailEvent peekLandEvent landTableAnyMatch landTableAllMatch CheckDodgeParryBlockResist; do
  grep -q "function macroTorch.$func" core/spell_trace_core.lua || echo "MISSING: $func"
done

# 验证不应在此文件中的函数
grep -c "function macroTorch.spellsImmuneTracing" core/spell_trace_core.lua  # 期望: 0
grep -c "function macroTorch.loadImmuneTable" core/spell_trace_core.lua       # 期望: 0
grep -c "function macroTorch.loadDefiniteBleedingTable" core/spell_trace_core.lua  # 期望: 0
grep -c "function macroTorch.eventHandle" core/spell_trace_core.lua           # 期望: 0

# 验证行数限制
wc -l core/spell_trace_core.lua  # 期望: ≤250
```
</verification>

<success_criteria>
- core/combat_context.lua 和 core/spell_trace_core.lua 均存在且含 Apache 2.0 license
- combat_context.lua 约 30-40 行，含 3 个独立战斗状态管理函数
- spell_trace_core.lua ≤ 250 行，含 17 个 spell trace 核心函数 + DEBUFF_LAND_LAG 常量 + 初始化块
- registerPeriodicTask('maintainLandTables', ...) 调用正确
- 无 immune 相关函数渗入（loadImmuneTable, loadDefiniteBleedingTable, spellsImmuneTracing 都不在此文件）
- 无事件处理代码渗入（eventHandle, CreateFrame, RegisterEvent 都不在此文件）
</success_criteria>

<output>
Create `.planning/phases/02-events-system/02-01-SUMMARY.md` when done
</output>