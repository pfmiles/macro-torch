---
phase: 07-druid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/bear.lua
  - classes/druid/utility.lua
autonomous: true
requirements:
  - REQ-07-SEMANTIC
  - REQ-07-REPLACE
  - REQ-07-BEAR-OR
  - REQ-07-RESERVED
  - REQ-07-SELFTEST

must_haves:
  truths:
    - "5 个语义化形态判断方法在 DRUID_FIELD_FUNC_MAP 中定义，全部委托给 self.isFormActive"
    - "isInBearForm 同时检查 Bear Form 和 Dire Bear Form（OR 逻辑）"
    - "isInTravelForm/isInAquaticForm/isInCasterForm 标注 -- reserved for future expansion"
    - "Druid.lua 中 3 处 isFormActive 硬编码替换为语义方法"
    - "bear.lua 中 2 处 isFormActive 硬编码替换为语义方法"
    - "utility.lua 中 2 处 isFormActive 硬编码替换为语义方法"
    - "5 个 SelfTest 注册（Category G2），覆盖全部 5 个新方法，命名含 'isIn' 前缀"
    - "entity/Player.lua 中 isFormActive 保持不变"
  artifacts:
    - path: "classes/druid/Druid.lua"
      provides: "DRUID_FIELD_FUNC_MAP 新增 5 条目 + 5 SelfTest 注册 + 3 调用替换"
      contains: "isInCatForm"
    - path: "classes/druid/bear.lua"
      provides: "2 处 isFormActive → isInBearForm 替换"
      contains: "isInBearForm"
    - path: "classes/druid/utility.lua"
      provides: "2 处 isFormActive → isInBearForm 替换"
      contains: "isInBearForm"
  key_links:
    - from: "classes/druid/Druid.lua isInCatForm FIELD_FUNC_MAP entry"
      to: "entity/Player.lua isFormActive"
      via: "metatable __index chain → self.isFormActive"
      pattern: "self\\.isFormActive\\('Cat Form'\\)"
    - from: "classes/druid/Druid.lua isInBearForm FIELD_FUNC_MAP entry"
      to: "entity/Player.lua isFormActive"
      via: "metatable __index chain → self.isFormActive × 2 (OR)"
      pattern: "self\\.isFormActive\\('Bear Form'\\) or self\\.isFormActive\\('Dire Bear Form'\\)"
---

<objective>
在保留 `isFormActive` 通用方法的同时，为 Druid 类新增 5 个语义化形态判断方法（`isInCatForm`/`isInBearForm`/`isInTravelForm`/`isInAquaticForm`/`isInCasterForm`），并替换 7 处 `isFormActive` 硬编码调用。

Purpose: 消除 `classes/druid/` 下 3 个文件中散落的形态名字符串硬编码，将形态判断语义集中到 DRUID_FIELD_FUNC_MAP 中，与已有的 `isOoc`/`isProwling`/`isBerserk` 模式对齐。纯重构 — 不改变任何战斗逻辑。
Output: 3 个文件修改（Druid.lua, bear.lua, utility.lua），~30 行新增 + ~14 行替换。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-druid/07-CONTEXT.md
@.planning/phases/07-druid/07-RESEARCH.md
@entity/Player.lua
@classes/druid/Druid.lua
@classes/druid/bear.lua
@classes/druid/utility.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 5 个 DRUID_FIELD_FUNC_MAP 形态判断条目</name>
  <files>classes/druid/Druid.lua</files>
  <read_first>
    - classes/druid/Druid.lua:434-452 — 现有 DRUID_FIELD_FUNC_MAP 条目（isOoc/isProwling/isBerserk/humanFormMana）作为插入位置参考
    - entity/Player.lua:158-169 — isFormActive 当前实现，确认委托目标
  </read_first>
  <action>
    在 DRUID_FIELD_FUNC_MAP 中 `isBerserk` 条目（第 447 行末尾）之后、`humanFormMana` 条目（第 449 行）之前插入 5 个新条目：

    - `['isInCatForm']` — 委托 `self.isFormActive('Cat Form')` (D-01)
    - `['isInBearForm']` — 委托 `self.isFormActive('Bear Form') or self.isFormActive('Dire Bear Form')` (D-02)
    - `['isInTravelForm']` — 委托 `self.isFormActive('Travel Form')`，标注 `-- reserved for future expansion` (D-03)
    - `['isInAquaticForm']` — 委托 `self.isFormActive('Aquatic Form')`，标注 `-- reserved for future expansion` (D-03)
    - `['isInCasterForm']` — 委托 `self.isFormActive('Moonkin Form')`，标注 `-- reserved for future expansion` (D-03)

    每个条目 3-5 行，遵循现有 FIELD_FUNC_MAP 的缩进风格（4 空格缩进，`['key'] = function(self)` 模式）。`isInBearForm` 的 OR 逻辑使用单独的 `return self.isFormActive('Bear Form') or self.isFormActive('Dire Bear Form')` 一行。
  </action>
  <verify>
    <automated>grep -c "isInCatForm\|isInBearForm\|isInTravelForm\|isInAquaticForm\|isInCasterForm" classes/druid/Druid.lua | grep -q "5"</automated>
  </verify>
  <acceptance_criteria>
    - classes/druid/Druid.lua 的 DRUID_FIELD_FUNC_MAP 中包含 5 个新 ['isInXxxForm'] 条目
    - 每个条目使用 `function(self)` 模式，委托给 `self.isFormActive('FormName')`
    - isInBearForm 使用 OR 逻辑组合 Bear Form 和 Dire Bear Form
    - isInTravelForm/isInAquaticForm/isInCasterForm 各包含 `-- reserved for future expansion` 注释
    - 新条目插入在 isBerserk 之后、humanFormMana 之前
    - entity/Player.lua 未被修改
  </acceptance_criteria>
  <done>5 个 FIELD_FUNC_MAP 条目正确插入，`./build.sh` 成功</done>
</task>

<task type="auto">
  <name>Task 2: 替换 7 处 isFormActive 硬编码调用</name>
  <files>
    classes/druid/Druid.lua
    classes/druid/bear.lua
    classes/druid/utility.lua
  </files>
  <read_first>
    - classes/druid/Druid.lua:348-349 — catAtk 中 clickContext 初始化的 2 处 isFormActive 调用
    - classes/druid/Druid.lua:531 — recoverNormalRelic 中 1 处 isFormActive 调用
    - classes/druid/bear.lua:66,102 — bearAoe 和 bearAtk 中各 1 处 isFormActive 调用
    - classes/druid/utility.lua:15,39 — druidStun 和 druidDefend 中各 1 处 isFormActive 调用
    - classes/druid/Druid.lua:381 — clickContext.isInBearForm 引用（已缓存，确认不需修改）
  </read_first>
  <action>
    替换 7 处 `isFormActive` 硬编码调用为语义方法属性访问 (D-04)：

    在 classes/druid/Druid.lua:
    - 第 348 行: `player.isFormActive('Dire Bear Form')` → `player.isInBearForm`
    - 第 349 行: `player.isFormActive('Cat Form')` → `player.isInCatForm`
    - 第 531 行: `player.isFormActive('Cat Form')` → `player.isInCatForm`

    在 classes/druid/bear.lua:
    - 第 66 行: `macroTorch.player.isFormActive('Dire Bear Form')` → `macroTorch.player.isInBearForm`
    - 第 102 行: `player.isFormActive('Dire Bear Form')` → `player.isInBearForm`

    在 classes/druid/utility.lua:
    - 第 15 行: `macroTorch.player.isFormActive('Dire Bear Form')` → `macroTorch.player.isInBearForm`
    - 第 39 行: `macroTorch.player.isFormActive('Dire Bear Form')` → `macroTorch.player.isInBearForm`

    替换是纯机械操作 — 属性访问替代方法调用。保留周围的赋值/条件结构不变（如 `clickContext.isInBearForm = player.isInBearForm`、`if not macroTorch.player.isInBearForm then`）。
  </action>
  <verify>
    <automated>
# 确认 druid/ 下不再有 isFormActive 调用（FIELD_FUNC_MAP 内部委托除外）
ISFORM_COUNT=$(grep -c "isFormActive" classes/druid/Druid.lua classes/druid/bear.lua classes/druid/utility.lua 2>/dev/null || echo 0)
# 期望: 仅 Druid.lua 中 5 个新 FIELD_FUNC_MAP 条目内部的 isFormActive 调用
test "$ISFORM_COUNT" -eq 5 &amp;&amp; echo "PASS: 7 replacements complete, 5 FIELD_FUNC_MAP delegations remain"
    </automated>
  </verify>
  <acceptance_criteria>
    - classes/druid/Druid.lua 中不含 `player.isFormActive(` 调用（只有 FIELD_FUNC_MAP 中 `self.isFormActive(...)` 作为委托实现）
    - classes/druid/bear.lua 中不含 `isFormActive(` 调用
    - classes/druid/utility.lua 中不含 `isFormActive(` 调用
    - 所有替换保留原有变量赋值和条件结构不变
    - 第 381 行的 `clickContext.isInBearForm` 引用保持不动（它读取的是已缓存的 clickContext 字段，不是 isFormActive 调用）
    - `./build.sh` 成功
  </acceptance_criteria>
  <done>7 处 isFormActive 硬编码全部替换为语义方法属性访问，`./build.sh` 成功</done>
</task>

<task type="auto">
  <name>Task 3: 更新 Category G2 SelfTest 注册</name>
  <files>classes/druid/Druid.lua</files>
  <read_first>
    - classes/druid/Druid.lua:1266-1277 — 现有 Category G2 SelfTest stubs（isCatForm/isBearForm without "In" prefix）
    - classes/druid/Druid.lua:1235-1264 — Category G1 SelfTest entries as pattern reference
  </read_first>
  <action>
    修改 Category G2（第 1266-1277 行）的 SelfTest 注册，对齐 D-01 命名 `isInCatForm`/`isInBearForm`：

    1. 更新 Category G2 注释标题：`-- Category G2: Form detection (2 items, isOptional=true)` → `-- Category G2: Form detection semantic methods (5 items, isOptional=true)`

    2. 重命名现有 2 个测试：
       - 测试标题: `"Druid: isCatForm exists"` → `"Druid: DRUID_FIELD_FUNC_MAP isInCatForm exists"`
       - 属性访问: `macroTorch.player.isCatForm` → `macroTorch.player.isInCatForm`
       - 断言失败消息: `"isCatForm not boolean"` → `"isInCatForm not boolean"`
       - 测试标题: `"Druid: isBearForm exists"` → `"Druid: DRUID_FIELD_FUNC_MAP isInBearForm exists"`
       - 属性访问: `macroTorch.player.isBearForm` → `macroTorch.player.isInBearForm`
       - 断言失败消息: `"isBearForm not boolean"` → `"isInBearForm not boolean"`

    3. 在重命名的 isInBearForm 测试之后新增 3 个测试：
       - `"Druid: DRUID_FIELD_FUNC_MAP isInTravelForm exists"` — 验证 `macroTorch.toBoolean(macroTorch.player.isInTravelForm)` 返回 boolean，`isOptional=true`，断言消息 `"isInTravelForm not boolean: "`
       - `"Druid: DRUID_FIELD_FUNC_MAP isInAquaticForm exists"` — 验证 `macroTorch.toBoolean(macroTorch.player.isInAquaticForm)` 返回 boolean，`isOptional=true`，断言消息 `"isInAquaticForm not boolean: "`
       - `"Druid: DRUID_FIELD_FUNC_MAP isInCasterForm exists"` — 验证 `macroTorch.toBoolean(macroTorch.player.isInCasterForm)` 返回 boolean，`isOptional=true`，断言消息 `"isInCasterForm not boolean: "`

    每个新测试遵循 Category G1 的 `UnitClass('player') ~= 'Druid'` 提前返回模式，并包含 `type(val) == "boolean"` 断言。floating-index 注释在第 1278 行末尾（文件结束），新测试插入在 isInBearForm 测试之后、文件结束之前。
  </action>
  <verify>
    <automated>
# 确认 5 个 SelfTest 注册全部存在，且命名含 "isIn" 前缀
grep -c "SelfTest:register.*isIn.*Form" classes/druid/Druid.lua | grep -q "5" &amp;&amp; echo "PASS: 5 isIn Form SelfTest registrations"
# 确认旧命名已清理
grep "isCatForm exists\|isBearForm exists" classes/druid/Druid.lua || echo "PASS: old names removed"
    </automated>
  </verify>
  <acceptance_criteria>
    - Category G2 注释标题更新为 "(5 items, isOptional=true)"
    - 原有 2 个测试（isCatForm/isBearForm）重命名为 isInCatForm/isInBearForm
    - 新增 3 个测试：isInTravelForm, isInAquaticForm, isInCasterForm
    - 全部 5 个测试使用 `isOptional=true`
    - 全部 5 个测试包含 `UnitClass('player') ~= 'Druid'` 提前返回
    - 全部 5 个测试使用 `macroTorch.toBoolean(...)` 包装并断言 `type(val) == "boolean"`
    - `./build.sh` 成功
  </acceptance_criteria>
  <done>5 个 SelfTest 注册完成，Category G2 完整覆盖全部 5 个形态判断语义方法</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| -- | No new trust boundaries — this is a pure refactoring phase within existing code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | DRUID_FIELD_FUNC_MAP integrity | accept | FIELD_FUNC_MAP entries are additive, machanism unchanged from existing isOoc/isProwling pattern; no new attack surface |
| T-07-02 | Denial of Service | FIELD_FUNC_MAP lazy evaluation | accept | Each new method delegates to existing `isFormActive` which already handles nil/empty iteration safely (returns false for zero forms); no new failure mode |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase |
</threat_model>

<verification>
## Phase-level verification

```bash
# 1. 5 个 FIELD_FUNC_MAP 条目全部存在
grep -c "isInCatForm\|isInBearForm\|isInTravelForm\|isInAquaticForm\|isInCasterForm" classes/druid/Druid.lua

# 2. druid/ 下仅有 FIELD_FUNC_MAP 内部的 isFormActive 调用（5 个）
grep -c "isFormActive" classes/druid/Druid.lua classes/druid/bear.lua classes/druid/utility.lua
# 期望: Druid.lua 中有 5 (FIELD_FUNC_MAP 委托), bear.lua 0, utility.lua 0

# 3. isInBearForm 包含 OR 逻辑
grep "isFormActive.*Bear Form.*or.*isFormActive.*Dire Bear Form" classes/druid/Druid.lua

# 4. 3 个 reserved 注释存在
grep -c "reserved for future expansion" classes/druid/Druid.lua
# 期望: 3

# 5. 5 个 SelfTest 注册存在
grep -c "SelfTest:register.*isIn.*Form" classes/druid/Druid.lua
# 期望: 5

# 6. 构建成功
./build.sh && echo "Build OK"

# 7. 产物中符号可达
grep "isInCatForm\|isInBearForm\|isInTravelForm\|isInAquaticForm\|isInCasterForm" SM_Extend.lua
```
</verification>

<success_criteria>
- [ ] 5 个 FIELD_FUNC_MAP 条目在 isBerserk 之后、humanFormMana 之前正确插入
- [ ] isInBearForm 正确实现 OR 逻辑（Bear Form | Dire Bear Form）
- [ ] 3 个 zero-caller 方法均标注 `-- reserved for future expansion`
- [ ] druid/Druid.lua、druid/bear.lua、druid/utility.lua 中 7 处硬编码全部替换
- [ ] entity/Player.lua 未被修改
- [ ] 5 个 SelfTest 注册全部更新/新增（Category G2: 5 items）
- [ ] `./build.sh` 成功
- [ ] `grep "isFormActive" classes/druid/Druid.lua classes/druid/bear.lua classes/druid/utility.lua` 仅在 FIELD_FUNC_MAP 委托中出现
</success_criteria>

<output>
Create `.planning/phases/07-druid/07-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New FIELD_FUNC_MAP entries (lazy computed properties on macroTorch.Druid instances)

| Symbol | Location | Type | Delegates to |
|--------|----------|------|--------------|
| `macroTorch.DRUID_FIELD_FUNC_MAP['isInCatForm']` | classes/druid/Druid.lua | function(self) → boolean | `self.isFormActive('Cat Form')` |
| `macroTorch.DRUID_FIELD_FUNC_MAP['isInBearForm']` | classes/druid/Druid.lua | function(self) → boolean | `self.isFormActive('Bear Form') or self.isFormActive('Dire Bear Form')` |
| `macroTorch.DRUID_FIELD_FUNC_MAP['isInTravelForm']` | classes/druid/Druid.lua | function(self) → boolean | `self.isFormActive('Travel Form')` |
| `macroTorch.DRUID_FIELD_FUNC_MAP['isInAquaticForm']` | classes/druid/Druid.lua | function(self) → boolean | `self.isFormActive('Aquatic Form')` |
| `macroTorch.DRUID_FIELD_FUNC_MAP['isInCasterForm']` | classes/druid/Druid.lua | function(self) → boolean | `self.isFormActive('Moonkin Form')` |

### New SelfTest registrations

| Symbol | Location | Type |
|--------|----------|------|
| `SelfTest:register("Druid: DRUID_FIELD_FUNC_MAP isInCatForm exists", ...)` | classes/druid/Druid.lua | SelfTest (renamed from isCatForm) |
| `SelfTest:register("Druid: DRUID_FIELD_FUNC_MAP isInBearForm exists", ...)` | classes/druid/Druid.lua | SelfTest (renamed from isBearForm) |
| `SelfTest:register("Druid: DRUID_FIELD_FUNC_MAP isInTravelForm exists", ...)` | classes/druid/Druid.lua | SelfTest (new) |
| `SelfTest:register("Druid: DRUID_FIELD_FUNC_MAP isInAquaticForm exists", ...)` | classes/druid/Druid.lua | SelfTest (new) |
| `SelfTest:register("Druid: DRUID_FIELD_FUNC_MAP isInCasterForm exists", ...)` | classes/druid/Druid.lua | SelfTest (new) |

### Files modified

| File | Change type |
|------|-------------|
| `classes/druid/Druid.lua` | 5 FIELD_FUNC_MAP entries added + 3 isFormActive calls replaced + 2 SelfTest renamed + 3 SelfTest added |
| `classes/druid/bear.lua` | 2 isFormActive calls replaced |
| `classes/druid/utility.lua` | 2 isFormActive calls replaced |

### Files NOT modified (but relevant)

| File | Why unchanged |
|------|---------------|
| `entity/Player.lua` | `isFormActive` retained as generic fallback per D-01 |
| `build_order.txt` | No file additions/removals |
| `build.sh` | No build config changes |