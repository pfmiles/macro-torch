---
phase: 21-catAtk-maintainability
plan: "03"
type: execute
wave: 3
depends_on:
  - "21-02"
files_modified:
  - classes/druid/cat.lua
autonomous: true
requirements:
  - REQ-21-KEEPRAKE-CLEANUP

must_haves:
  truths:
    - "keepRake function in cat.lua contains a [SIDE EFFECT] comment block above the atkPowerBurst call explaining why ATK burst items are consumed here rather than in burstMod"
    - "The comment block covers: (1) AP snapshot maximizes Rake bleed damage, (2) burstMod handles manual Shift-key coordination while this is an automated optimization for high-value targets"
    - "Zero executable code lines changed in keepRake — the function body is byte-for-byte identical to pre-phase state except for new comment lines"
    - "./build.sh produces SM_Extend.lua with the keepRake annotation preserved in output"
  artifacts:
    - path: "classes/druid/cat.lua"
      provides: "keepRake function with ATK burst side-effect annotation clarifying design rationale"
  key_links: []
---

<objective>
Annotate the ATK burst side-effect in keepRake with a detailed comment block explaining why atkPowerBurst is called here (Item 4). Per D-08 Scheme A: add a comment block only, zero code changes.

Purpose: The keepRake function is named as a debuff maintenance module, but it secretly consumes ATK burst items (trinkets, potions) for high-value targets. Without documentation, a future maintainer refactoring or extracting the atkPowerBurst call could break the AP snapshot optimization. The comment block makes the design intent explicit.

Output: cat.lua keepRake function with [SIDE EFFECT] comment block before the atkPowerBurst call. Single git commit per D-09: `docs(catAtk): annotate ATK burst side effect in keepRake`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/catAtk-phaseA-maintainability.md
@.planning/phases/21-catAtk-maintainability/21-CONTEXT.md
@.planning/catAtk-core-principles.md
</context>

<tasks>

<task type="auto">
  <name>Add [SIDE EFFECT] comment block above atkPowerBurst call in keepRake (Item 4)</name>
  <read_first>
  - classes/druid/cat.lua (lines 300-314 — keepRake function, focus on lines 310-312 where atkPowerBurst is called)
  - .planning/catAtk-phaseA-maintainability.md (Entry 4: Scheme A comment block template)
  - .planning/catAtk-core-principles.md (Rule 14: burst coordination, last paragraph documenting keepRake's auto ATK burst)
  </read_first>
  <files>classes/druid/cat.lua</files>
  <action>
  Add a multi-line comment block directly ABOVE the existing ATK burst condition block in keepRake (currently lines 310-312 in cat.lua). Place the new comment block between the closing `end` of the guard conditions (line 308/309) and the existing `if` statement for ATK burst (line 310).

  Insert the following comment block exactly as shown:

  ```
  -- [SIDE EFFECT] ATK Power burst for Rake on priority targets
  -- Rake benefits from AP snapshotting; consuming burst items here
  -- maximizes bleed damage for the entire Rake duration.
  -- This is placed in keepRake rather than burstMod because:
  --   1. burstMod handles manual (Shift-key) burst coordination
  --   2. This is an automated optimization for high-value targets
  ```

  The comment block uses 6 Lua single-line comment lines (`--`). Each line is independent; there is no multi-line comment block syntax.

  Do NOT modify any executable code. Do NOT change the `if` condition, the atkPowerBurst call, any guard conditions, any earlier or later code in the function. Only insert these 6 comment lines.

  The result should look like:

  ```
      -- guard conditions...
      if not macroTorch.isFightStarted(clickContext) or ... then
          return
      end
      -- [SIDE EFFECT] ATK Power burst for Rake on priority targets
      -- Rake benefits from AP snapshotting; consuming burst items here
      -- maximizes bleed damage for the entire Rake duration.
      -- This is placed in keepRake rather than burstMod because:
      --   1. burstMod handles manual (Shift-key) burst coordination
      --   2. This is an automated optimization for high-value targets
      if ((macroTorch.target.classification == 'worldboss' and macroTorch.isRipPresent(clickContext) and not clickContext.isTargetDummy) or macroTorch.target.isPlayerControlled) and macroTorch.isNearBy(clickContext) then
          macroTorch.atkPowerBurst(clickContext)
      end
      macroTorch.safeRake(clickContext)
  ```

  The atkPowerBurst call and the safeRake call below it remain identical in content and position.
  </action>
  <verify>
    <automated>./build.sh 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
  - keepRake function in cat.lua contains the line: `-- [SIDE EFFECT] ATK Power burst for Rake on priority targets`
  - The comment block contains all three rationale sentences: (1) AP snapshotting, (2) placed in keepRake rather than burstMod, (3) burstMod handles manual + this is automated
  - The atkPowerBurst call on line 310-312 is UNCHANGED (git diff shows only `--` comment line additions in keepRake)
  - No other function in cat.lua is modified (git diff --stat shows only keepRake comment insertion)
  - `./build.sh` exits 0 and produces non-empty SM_Extend.lua
  - `grep "SIDE EFFECT" SM_Extend.lua | grep -v '^#'` returns 1 (comment survives build concatenation)
  </acceptance_criteria>
  <reversibility rating="reversible">Pure comment addition; reverting is a single git revert with zero logic impact. Per D-08.</reversibility>
  <done>keepRake ATK burst side-effect documented with [SIDE EFFECT] comment block; no code changes; build passes</done>
</task>

<task type="auto">
  <name>Final cross-file verification: all 4 Phase 21 items complete across all 3 files</name>
  <read_first>
  - classes/druid/combo.lua (verify isPseudoInfiniteEnergy line + comment numbering + KillShot comments)
  - classes/druid/cat.lua (verify keepRake annotation + all isPseudoInfiniteEnergy reads + KillShot comments)
  - classes/druid/Druid.lua (verify shouldUseShred isPseudoInfiniteEnergy read)
  - .planning/catAtk-phaseA-maintainability.md (cross-reference all 4 entries against completed state)
  </read_first>
  <files>classes/druid/combo.lua, classes/druid/cat.lua, classes/druid/Druid.lua</files>
  <action>
  Run comprehensive verification that all 4 Phase 21 items are complete and consistent:

  1. Build: `./build.sh` must exit 0
  2. Comment numbering: `grep -E "^[[:space:]]*--[[:space:]]*[0-9]+\." classes/druid/combo.lua | grep -v '^#'` shows continuous 0..12
  3. KillShot comments: grep for `KillShot` in combo.lua and cat.lua — at least 4 comment occurrences total
  4. isPseudoInfiniteEnergy: grep for the field name in all 3 files — exactly 1 assignment (combo.lua) + 4 reads (cat.lua) + 1 read (Druid.lua) = 6 total references
  5. Zero remaining old comparison: `grep "computeErps.*>=.*SHRED_E" classes/druid/combo.lua classes/druid/cat.lua classes/druid/Druid.lua | grep -v '^#'` must return empty
  6. Side-effect annotation: `grep "SIDE EFFECT" classes/druid/cat.lua | grep -v '^#'` returns 1
  7. Preserved comparisons: shouldDoReshift, shouldCastFFDuringWaitWindow, recoverNormalRelic remain unchanged
  8. Build output: `grep "isPseudoInfiniteEnergy" SM_Extend.lua | grep -v '^#' | wc -l` >= 6
  </action>
  <verify>
    <automated>./build.sh 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
  - `./build.sh` exits 0
  - greps from action steps 2-8 all pass
  - 3 files touched: combo.lua (comments + isPseudoInfiniteEnergy), cat.lua (comments + isPseudoInfiniteEnergy reads + keepRake annotation), Druid.lua (isPseudoInfiniteEnergy read)
  - Total line changes match expectation: ~40 lines (~25 comment additions/changes + ~5 comparison replacements + ~1 field assignment + build lines)
  - Ready for the single git commit for Item 4 (Items 1-2 already committed in 21-01, Item 3 committed in 21-02)
  </acceptance_criteria>
  <done>All 4 Phase 21 items verified: 0-12 numbering, KillShot comments, isPseudoInfiniteEnergy centralization, keepRake annotation; build passes; ready for commit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 21-03 is a pure comment addition in keepRake — no new code, no input handling, no output changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering | Build output correctness | low | accept | Comment-only change; build concatenation is deterministic. Output verified via `./build.sh` success gate and `grep "SIDE EFFECT" SM_Extend.lua` survival check. |
</threat_model>

<verification>
- Run `./build.sh` and confirm exit code 0
- Phase 21 pre-post checklist (from catAtk-phaseA-maintainability.md):
  - [ ] Comment numbering 0-12 in combo.lua
  - [ ] KillShot comments at oocMod/termMod call sites + function heads
  - [ ] isPseudoInfiniteEnergy computed once, read in 5 locations
  - [ ] keepRake [SIDE EFFECT] comment block present
  - [ ] 3 implicit comparisons preserved
  - [ ] Zero behavior change — `git diff` shows only comments and variable reference changes
</verification>

<success_criteria>
- All 4 Phase 21 items complete and verified
- `./build.sh` succeeds with all changes propagated to SM_Extend.lua
- Third and final git commit: `docs(catAtk): annotate ATK burst side effect in keepRake`
- Phase 21 is complete — 3 commits total matching D-09 commit strategy
</success_criteria>

<output>
Create `.planning/phases/21-catAtk-maintainability/21-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Category | Symbol / Location | Description |
|----------|-------------------|-------------|
| **Comment added** | `cat.lua:310` (keepRake, above atkPowerBurst call) | 6-line `[SIDE EFFECT]` comment block explaining: (1) AP snapshot maximization for Rake bleed, (2) separation of concerns from burstMod's manual Shift-key coordination, (3) this is an automated optimization for high-value targets |