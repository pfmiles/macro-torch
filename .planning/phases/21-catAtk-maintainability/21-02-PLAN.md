---
phase: 21-catAtk-maintainability
plan: "02"
type: execute
wave: 2
depends_on:
  - "21-01"
files_modified:
  - classes/druid/combo.lua
  - classes/druid/cat.lua
  - classes/druid/Druid.lua
autonomous: true
requirements:
  - REQ-21-ISINFINITEENERGY

must_haves:
  truths:
    - "clickContext.isPseudoInfiniteEnergy is computed exactly once in catAtk() after clickContext init fields are set and before any module call"
    - "clickContext.isPseudoInfiniteEnergy evaluates to macroTorch.computeErps(clickContext) >= clickContext.SHRED_E"
    - "cat.lua oocMod reads clickContext.isPseudoInfiniteEnergy instead of calling macroTorch.computeErps(clickContext) >= clickContext.SHRED_E inline"
    - "cat.lua cp5Bite reads clickContext.isPseudoInfiniteEnergy instead of calling macroTorch.computeErps(clickContext) >= clickContext.SHRED_E inline"
    - "cat.lua energyDischargeBeforeBite reads clickContext.isPseudoInfiniteEnergy instead of calling macroTorch.computeErps(clickContext) >= clickContext.SHRED_E inline"
    - "cat.lua dischargeEnergyChangeRelicAndRip sets local skipDischarge from clickContext.isPseudoInfiniteEnergy instead of computing erps >= SHRED_E from a local erps variable; the local erps variable is retained for the two energy-overflow calculations below it"
    - "Druid.lua shouldUseShred sets local infiniteEnergy from clickContext.isPseudoInfiniteEnergy instead of computing erps >= SHRED_E from a local erps variable; the local erps variable is retained for the energyIn1s calculation below it"
    - "The 3 implicit comparisons (shouldDoReshift, shouldCastFFDuringWaitWindow, recoverNormalRelic) remain UNCHANGED per D-07"
    - "./build.sh produces SM_Extend.lua with all changes and zero errors"
  artifacts:
    - path: "classes/druid/combo.lua"
      provides: "clickContext.isPseudoInfiniteEnergy field computation — single source of truth for infinite energy determination in catAtk()"
    - path: "classes/druid/cat.lua"
      provides: "4 module functions (oocMod, cp5Bite, energyDischargeBeforeBite, dischargeEnergyChangeRelicAndRip) reading clickContext.isPseudoInfiniteEnergy"
    - path: "classes/druid/Druid.lua"
      provides: "shouldUseShred reading clickContext.isPseudoInfiniteEnergy instead of local erps comparison"
  key_links:
    - from: "classes/druid/combo.lua (clickContext.isPseudoInfiniteEnergy assignment line)"
      to: "classes/druid/cat.lua (oocMod, cp5Bite, energyDischargeBeforeBite, dischargeEnergyChangeRelicAndRip)"
      via: "clickContext table passed as function argument — all cat.lua functions receive the same clickContext"
      pattern: "clickContext\.isPseudoInfiniteEnergy"
    - from: "classes/druid/combo.lua (clickContext.isPseudoInfiniteEnergy assignment line)"
      to: "classes/druid/Druid.lua (shouldUseShred)"
      via: "clickContext table passed as function argument — shouldUseShred receives clickContext directly"
      pattern: "clickContext\.isPseudoInfiniteEnergy"
---

<objective>
Centralize the "infinite energy" determination into clickContext.isPseudoInfiniteEnergy (Item 3). Replace 5 scattered `macroTorch.computeErps(clickContext) >= clickContext.SHRED_E` comparisons with reads from the pre-computed field, while preserving 3 implicit comparisons that express different semantics per D-07.

Purpose: The "infinite energy" check appears in 5+ locations across 3 files. Any change to the threshold would require synchronized edits. Centralizing into a single computation in catAtk() makes the intent explicit and the threshold a single point of change.

Output: combo.lua with isPseudoInfiniteEnergy computation (1 new line); cat.lua with 4 functions reading clickContext.isPseudoInfiniteEnergy; Druid.lua shouldUseShred reading clickContext.isPseudoInfiniteEnergy. Single git commit per D-09: `refactor(catAtk): centralize isPseudoInfiniteEnergy in clickContext`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/catAtk-phaseA-maintainability.md
@.planning/phases/21-catAtk-maintainability/21-CONTEXT.md
@.planning/catAtk-core-principles.md
</context>

<tasks>

<task type="tracer">
  <name>Add isPseudoInfiniteEnergy computation in combo.lua + replace oocMod check in cat.lua (thin slice through 2 of 3 layers)</name>
  <read_first>
  - classes/druid/combo.lua (lines 100-144 — clickContext init and module call area)
  - classes/druid/cat.lua (lines 156-164 — oocMod function with the inline erps >= SHRED_E check)
  - classes/druid/Druid.lua (lines 802-831 — computeErps function definition, understand caching behavior)
  - .planning/catAtk-phaseA-maintainability.md (Entry 3: target locations and "unchanged" list)
  </read_first>
  <files>classes/druid/combo.lua, classes/druid/cat.lua</files>
  <action>
  The tracer proves the architecture: a field set once in clickContext is correctly readable by a downstream module function.

  **Step 1 — combo.lua:** Add one line AFTER the last clickContext init field is set (after line 108 `clickContext.isTargetDummy = ...`) and BEFORE the first module call at line 110 `-- 0.idol recover`. Insert at an appropriate blank line between the init block and the module call block:
  ```
  clickContext.isPseudoInfiniteEnergy = macroTorch.computeErps(clickContext) >= clickContext.SHRED_E
  ```
  The exact insertion position is Claude's Discretion but MUST be after all clickContext fields are initialized (lines 53-108) and before any module calls consume it (line 110+). Place on its own line with no trailing comment needed.

  **Step 2 — cat.lua oocMod (line 162):** Replace the inline comparison:
  - BEFORE: `if macroTorch.computeErps(clickContext) >= clickContext.SHRED_E then`
  - AFTER: `if clickContext.isPseudoInfiniteEnergy then`

  Do NOT change anything else in oocMod. Do NOT touch the comment on line 160-161. Do NOT change the `then return end` block body.

  This thin slice (combo.lua entry + cat.lua oocMod consumption) proves the pattern works end-to-end before the expansion task replaces the remaining 4 locations.
  </action>
  <verify>
    <automated>./build.sh 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
  - combo.lua contains line: `clickContext.isPseudoInfiniteEnergy = macroTorch.computeErps(clickContext) >= clickContext.SHRED_E`
  - The isPseudoInfiniteEnergy line appears AFTER clickContext init block and BEFORE any module call
  - cat.lua oocMod line 162 reads `if clickContext.isPseudoInfiniteEnergy then` (NOT `macroTorch.computeErps(clickContext) >= clickContext.SHRED_E`)
  - `grep "macroTorch.computeErps(clickContext) >= clickContext.SHRED_E" classes/druid/cat.lua | grep -v '^#' | wc -l` returns 3 (cp5Bite, energyDischargeBeforeBite, and dischargeEnergyChangeRelicAndRip still have old pattern — expansion task replaces them)
  - `./build.sh` exits 0 and produces non-empty SM_Extend.lua
  </acceptance_criteria>
  <done>clickContext.isPseudoInfiniteEnergy computed in catAtk entry; oocMod reads it correctly; build passes</done>
</task>

<task type="auto">
  <name>Replace remaining 4 isPseudoInfiniteEnergy references across cat.lua and Druid.lua</name>
  <read_first>
  - classes/druid/cat.lua (lines 106-136 cp5Bite, lines 137-155 energyDischargeBeforeBite, lines 242-280 dischargeEnergyChangeRelicAndRip)
  - classes/druid/Druid.lua (lines 697-730 shouldUseShred function)
  - .planning/catAtk-phaseA-maintainability.md (Entry 3: "不改动的位置" — the 3 preserved comparisons)
  </read_first>
  <files>classes/druid/cat.lua, classes/druid/Druid.lua</files>
  <action>
  Replace the 4 remaining explicit isPseudoInfiniteEnergy comparisons. Every replacement MUST preserve surrounding logic exactly — only the comparison expression changes.

  **cat.lua cp5Bite (line 116):**
  - BEFORE: `if macroTorch.computeErps(clickContext) >= clickContext.SHRED_E then`
  - AFTER: `if clickContext.isPseudoInfiniteEnergy then`

  **cat.lua energyDischargeBeforeBite (line 139):**
  - BEFORE: `if macroTorch.computeErps(clickContext) >= clickContext.SHRED_E then`
  - AFTER: `if clickContext.isPseudoInfiniteEnergy then`

  **cat.lua dischargeEnergyChangeRelicAndRip (line 252):**
  - The current code has TWO local variables: `local erps = macroTorch.computeErps(clickContext)` on line 251 and `local skipDischarge = erps >= clickContext.SHRED_E` on line 252.
  - Keep `local erps = macroTorch.computeErps(clickContext)` — it is still needed for the two energy overflow projections on lines 259 and 269 (`macroTorch.player.mana + erps * 2.5 > 100` and `macroTorch.player.mana + erps * 2 - clickContext.RIP_E > 100`).
  - Change ONLY the skipDischarge assignment on line 252:
    BEFORE: `local skipDischarge = erps >= clickContext.SHRED_E`
    AFTER: `local skipDischarge = clickContext.isPseudoInfiniteEnergy`

  **Druid.lua shouldUseShred (lines 698-700):**
  - The current code has TWO local variables: `local erps = macroTorch.computeErps(clickContext)` on line 699 and `local infiniteEnergy = erps >= clickContext.SHRED_E` on line 700.
  - Keep `local erps = macroTorch.computeErps(clickContext)` — it is still needed for `local energyIn1s = erps * 1` on line 710.
  - Change ONLY the infiniteEnergy assignment on line 700:
    BEFORE: `local infiniteEnergy = erps >= clickContext.SHRED_E`
    AFTER: `local infiniteEnergy = clickContext.isPseudoInfiniteEnergy`

  **VERIFY NO false positives — D-07 preserved locations MUST remain unchanged:**
  - `shouldDoReshift` (cat.lua:206-214): uses `math.ceil(projectedEnergy) < nextAbilityCost` — different semantics, DO NOT TOUCH.
  - `shouldCastFFDuringWaitWindow` (Druid.lua:842-873): uses `projectedEnergy >= minAbilityCost and currentEnergy < minAbilityCost` — different semantics, DO NOT TOUCH.
  - `recoverNormalRelic` (Druid.lua:424-439): uses `energy + erps * 2.5 <= 100` — different semantics, DO NOT TOUCH.
  </action>
  <verify>
    <automated>./build.sh 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
  - cat.lua cp5Bite line 116: `if clickContext.isPseudoInfiniteEnergy then`
  - cat.lua energyDischargeBeforeBite line 139: `if clickContext.isPseudoInfiniteEnergy then`
  - cat.lua dischargeEnergyChangeRelicAndRip: `local skipDischarge = clickContext.isPseudoInfiniteEnergy` (erps local still present above it)
  - Druid.lua shouldUseShred: `local infiniteEnergy = clickContext.isPseudoInfiniteEnergy` (erps local still present above it)
  - `grep "macroTorch.computeErps(clickContext) >= clickContext.SHRED_E" classes/druid/combo.lua classes/druid/cat.lua classes/druid/Druid.lua | grep -v '^#' | wc -l` returns 0 (ALL 5 explicit comparisons replaced)
  - `grep "clickContext.isPseudoInfiniteEnergy" classes/druid/combo.lua classes/druid/cat.lua classes/druid/Druid.lua | grep -v '^#' | wc -l` returns 6 (1 assignment + 5 reads)
  - shouldDoReshift, shouldCastFFDuringWaitWindow, recoverNormalRelic are UNCHANGED (git diff confirms no modifications in those functions)
  - `./build.sh` exits 0 and produces non-empty SM_Extend.lua
  </acceptance_criteria>
  <reversibility rating="costly">Refactoring touches 3 files across 2 layers; reverting requires coordinated undo across all 5 locations. However, the logic is semantically identical since clickContext fields are all computed from already-available data, and the field name stays within the same table. Per D-03 classification.</reversibility>
  <done>All 5 explicit isPseudoInfiniteEnergy comparisons replaced with clickContext reads; 3 implicit comparisons preserved; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 21-02 is a pure refactor — replaces 5 inline erps >= SHRED_E comparisons with a pre-computed clickContext field. No new code paths, no input, no output changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Tampering | Build output correctness | low | accept | isPseudoInfiniteEnergy derives from computeErps which is already called identically at each old call site and caches its result via clickContext.computeErps. The pre-computation is semantically equivalent; the cached value is identical to each inline call. Build output verified via `./build.sh` success gate. |
</threat_model>

<verification>
- Run `./build.sh` and confirm exit code 0
- Confirm zero remaining inline `erps >= SHRED_E` comparisons: `grep -r "computeErps.*>=.*SHRED_E" classes/druid/ | grep -v '^#'`
- Confirm exactly 6 isPseudoInfiniteEnergy references: 1 in combo.lua (assignment), 4 in cat.lua (reads), 1 in Druid.lua (read)
- Confirm 3 preserved functions unchanged: `git diff classes/druid/cat.lua classes/druid/Druid.lua -- shouldDoReshift shouldCastFFDuringWaitWindow recoverNormalRelic`
- Visual inspection: the isPseudoInfiniteEnergy assignment in combo.lua is placed after clickContext init block and before module calls
</verification>

<success_criteria>
- clickContext.isPseudoInfiniteEnergy set exactly once in catAtk() entry
- 5 module locations read the field instead of recomputing
- 3 implicit comparisons preserved unchanged
- `./build.sh` succeeds
- One git commit: `refactor(catAtk): centralize isPseudoInfiniteEnergy in clickContext`
</success_criteria>

<output>
Create `.planning/phases/21-catAtk-maintainability/21-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Category | Symbol / Location | Description |
|----------|-------------------|-------------|
| **Field added** | `clickContext.isPseudoInfiniteEnergy` (combo.lua catAtk function) | New computed boolean field: `macroTorch.computeErps(clickContext) >= clickContext.SHRED_E`. Single source of truth for pseudo-infinite-energy determination. |
| **Reference replaced** | `cat.lua:162` (oocMod) | `macroTorch.computeErps(clickContext) >= clickContext.SHRED_E` replaced with `clickContext.isPseudoInfiniteEnergy` |
| **Reference replaced** | `cat.lua:116` (cp5Bite) | `macroTorch.computeErps(clickContext) >= clickContext.SHRED_E` replaced with `clickContext.isPseudoInfiniteEnergy` |
| **Reference replaced** | `cat.lua:139` (energyDischargeBeforeBite) | `macroTorch.computeErps(clickContext) >= clickContext.SHRED_E` replaced with `clickContext.isPseudoInfiniteEnergy` |
| **Reference replaced** | `cat.lua:252` (dischargeEnergyChangeRelicAndRip) | `local skipDischarge = erps >= clickContext.SHRED_E` replaced with `local skipDischarge = clickContext.isPseudoInfiniteEnergy` (local `erps` retained for overflow calculations) |
| **Reference replaced** | `Druid.lua:700` (shouldUseShred) | `local infiniteEnergy = erps >= clickContext.SHRED_E` replaced with `local infiniteEnergy = clickContext.isPseudoInfiniteEnergy` (local `erps` retained for energyIn1s calculation) |