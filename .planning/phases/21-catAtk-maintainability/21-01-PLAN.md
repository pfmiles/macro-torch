---
phase: 21-catAtk-maintainability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/combo.lua
  - classes/druid/cat.lua
autonomous: true
requirements:
  - REQ-21-COMMENTS

must_haves:
  truths:
    - "combo.lua module call comments are numbered 0-12 in continuous execution order matching catAtk-core-principles.md Rule 7 priority table"
    - "combo.lua oocMod call site (line ~144) has comment explaining KillShot dual-entry design: Omen of Clarity triggers preempt termMod with free KillShot/Bite"
    - "combo.lua termMod call site (line ~149) has comment explaining KillShot > 5CP Bite priority and OoC-consumed skip behavior"
    - "cat.lua oocMod function head (line ~156) documents OoC module's KillShot-first priority and free-skill strategy"
    - "cat.lua termMod function head (line ~97) documents KillShot > regular 5CP Bite termination logic"
    - "./build.sh produces SM_Extend.lua with all comment changes present and zero errors"
  artifacts:
    - path: "classes/druid/combo.lua"
      provides: "catAtk main entry with corrected comment numbering 0-12 and KillShot design intent comments at oocMod/termMod call sites"
    - path: "classes/druid/cat.lua"
      provides: "oocMod and termMod function head comments documenting KillShot dual-entry design"
  key_links: []
---

<objective>
Fix catAtk comment numbering continuity (Item 1) and add KillShot dual-entry design intent comments (Item 2). Both are pure comment changes -- zero behavior impact, zero DPS impact.

Purpose: Comments numbered out of order (7 before 6) cause confusion reading the module execution flow. The KillShot dual-entry pattern (oocMod + termMod) has no documentation explaining why two call sites exist. Both are 100% comment-only changes per D-01 and D-02.

Output: combo.lua with corrected 0-12 numbering and KillShot comments at oocMod/termMod call sites; cat.lua oocMod/termMod function heads with design intent comments. Single git commit per D-03 + D-09: `docs(catAtk): fix comment numbering and add KillShot design intent comments`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/catAtk-phaseA-maintainability.md
@.planning/phases/21-catAtk-maintainability/21-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Fix catAtk comment numbering 7/6 swap to 6/7 in combo.lua (Item 1)</name>
  <read_first>
  - classes/druid/combo.lua (lines 144-174, module call area in catAtk)
  - .planning/catAtk-phaseA-maintainability.md (Entry 1: before/after comment table)
  </read_first>
  <files>classes/druid/combo.lua</files>
  <action>
  Fix the inverted comment numbering at the oocMod/termMod call sites in catAtk() (combo.lua lines 144-150):
  
  1. Line 144: Change comment from `-- 7.oocMod: 没有潜行且ooc 或 前行但目标正在攻击我` to `-- 6.oocMod: 清晰预兆优先 — OoC 触发时用免费技能打 KillShot/Bite`
  2. Line 149: Change comment from `-- 6.termMod: 终结技模块，实际上这里就只是bite模块，因为rip在单独自己的模块里处理了` to `-- 7.termMod: 普通 GCD 终结技 — KillShot > 5CP Bite；若 OoC 已消费则此处跳过`
  
  This fixes the confusing 7-before-6 inversion. The numbering change is limited to lines 144 and 149 only. All subsequent numbered comments (8.otMod, 9.keepTigerFury, 10.debuffMod, 11.regularAttack, 12.reshiftMod) are already correct and do NOT change.
  
  Do NOT touch any executable code. Do NOT change function calls, logic, or control flow. Verify with grep that only comment lines changed.
  </action>
  <verify>
    <automated>./build.sh 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
  - combo.lua line 144 contains `6.oocMod` (not 7.oocMod)
  - combo.lua line 149 contains `7.termMod` (not 6.termMod)
  - `grep -c " 6\.oocMod\| 7\.termMod\| 8\.otMod\| 9\.keepTigerFury\|10\.debuffMod\|11\.regularAttack\|12\.reshiftMod" classes/druid/combo.lua | grep -v '^#'` returns 7 (one per module comment line)
  - `./build.sh` exits 0 and produces non-empty SM_Extend.lua
  </acceptance_criteria>
  <done>oocMod/termMod comments in combo.lua show correct 6/7 numbering; build passes</done>
</task>

<task type="auto">
  <name>Add KillShot dual-entry design intent comments in combo.lua and cat.lua (Item 2)</name>
  <read_first>
  - classes/druid/combo.lua (lines 144-150 for oocMod/termMod call sites)
  - classes/druid/cat.lua (lines 97-105 for termMod function, lines 156-174 for oocMod function)
  - .planning/catAtk-phaseA-maintainability.md (Entry 2: target comment text)
  </read_first>
  <files>classes/druid/combo.lua, classes/druid/cat.lua</files>
  <action>
  Add design intent comments for the KillShot dual-entry pattern per D-02:
  
  **combo.lua call sites:**
  - The oocMod comment at line 144 is already updated by Task 1 to `-- 6.oocMod: 清晰预兆优先 — OoC 触发时用免费技能打 KillShot/Bite`. Verify it matches; if not, adjust to this exact text.
  - The termMod comment at line 149 is already updated by Task 1 to `-- 7.termMod: 普通 GCD 终结技 — KillShot > 5CP Bite；若 OoC 已消费则此处跳过`. Verify it matches; if not, adjust to this exact text.
  
  **cat.lua function heads -- add NEW comment lines directly before the existing function body:**
  
  1. Before `termMod` function (cat.lua line 97): Insert a new comment line ABOVE the existing `-- [NEW GUARD]` line:
     `-- termMod 终结技模块：KillShot 斩杀优先 > 5CP Bite；若 oocMod 中 OoC 已消费 KillShot 则此处仅处理常规 5 星撕咬`
  
  2. Before `oocMod` function (cat.lua line 156): Insert new comment lines ABOVE the `if not clickContext.ooc` guard, replacing or preceding the existing inline comment:
     `-- oocMod (Omen of Clarity) 模块：节能施法状态下优先用免费技能`
     `-- KillShot 仍是最优先，其次才是常规攒星或 5CP 撕咬`
  
  All additions are Lua single-line comments (`--`). Do NOT touch any executable code, logic, guards, or function signatures. The new comment lines must be inserted directly above their respective functions, preserving all existing code exactly.
  </action>
  <verify>
    <automated>./build.sh 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
  - combo.lua line 144 contains `6.oocMod` and `KillShot`
  - combo.lua line 149 contains `7.termMod` and `KillShot`
  - cat.lua line 97 (or near): new comment above termMod containing `KillShot 斩杀优先`
  - cat.lua line 156 (or near): new comment above oocMod containing `Omen of Clarity` and `KillShot`
  - `grep -c "KillShot" classes/druid/combo.lua classes/druid/cat.lua | grep -v '^#'` shows at least 4 comment occurrences (2 in combo.lua + 2 in cat.lua)
  - `./build.sh` exits 0 and produces non-empty SM_Extend.lua
  - `grep "KillShot" SM_Extend.lua | grep -v '^#' | wc -l` >= 4 (comments survive build concatenation)
  </acceptance_criteria>
  <reversibility rating="reversible">Pure comment additions; reverting is a single git revert with zero logic impact. Per D-03.</reversibility>
  <done>All 4 KillShot design intent comments present: 2 combo.lua call sites + 2 cat.lua function heads; 0-12 numbering continuous; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 21-01 contains only comment additions and numbering fixes -- zero new code paths, zero new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | Build output (SM_Extend.lua) | low | accept | Comment-only changes pose no tampering risk. Build system concatenation is deterministic; output verified via `./build.sh` success gate. |
</threat_model>

<verification>
- Run `./build.sh` and confirm exit code 0
- Grep combo.lua for continuous 0-12 numbering: `grep -E "^[[:space:]]*--[[:space:]]*[0-9]+\." classes/druid/combo.lua | grep -v '^#'`
- Grep for KillShot comments in both files: `grep -c "KillShot" classes/druid/combo.lua classes/druid/cat.lua | grep -v '^#'`
- Confirm no executable code changed: `git diff` shows only `--` comment line additions/modifications
</verification>

<success_criteria>
- catAtk module call comments numbered continuously 0-12 in execution order
- oocMod and termMod call sites in combo.lua document KillShot dual-entry design
- oocMod and termMod function definitions in cat.lua document their role in the KillShot pattern
- `./build.sh` succeeds
- One git commit: `docs(catAtk): fix comment numbering and add KillShot design intent comments`
</success_criteria>

<output>
Create `.planning/phases/21-catAtk-maintainability/21-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Category | Symbol / Location | Description |
|----------|-------------------|-------------|
| **Comment modified** | `combo.lua:144` | Module call comment renumbered from 7 to 6: `6.oocMod` with KillShot intent |
| **Comment modified** | `combo.lua:149` | Module call comment renumbered from 6 to 7: `7.termMod` with KillShot > Bite priority |
| **Comment added** | `cat.lua:97` (above termMod) | Function head comment: `termMod 终结技模块：KillShot 斩杀优先` |
| **Comment added** | `cat.lua:156` (above oocMod) | Function head comment: `oocMod (Omen of Clarity) 模块` with KillShot priority |
| **Comment sequence** | `combo.lua:144-174` | Full module numbering now 0-12 continuous matching Rule 7 priority table |