---
phase: 01-classmetatable-entity
plan: 06
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified: [build_order.txt, build.sh]
autonomous: true
requirements: [R7]
user_setup: []

must_haves:
  truths:
    - "build_order.txt 存在且包含所有当前文件和 Phase 2-4 未来文件的路径"
    - "build.sh 读取 build_order.txt 按序拼接，对不存在的文件静默跳过"
    - "build.sh 保留 Cygwin 拷贝逻辑"
    - "build.sh 不再使用 grep -v 黑名单"
    - "`./build.sh` 执行成功（容错模式跳过未创建文件）"
  artifacts:
    - path: "build_order.txt"
      provides: "声明式构建文件列表"
      min_lines: 25
    - path: "build.sh"
      provides: "读取 build_order.txt 的容错模式构建脚本"
  key_links:
    - from: "build.sh → while read line"
      to: "build_order.txt"
      via: "逐行读取并 cat"
      pattern: "while.*read.*line"
    - from: "build.sh → [ -f \"$line\" ] && cat"
      to: "SM_Extend.lua"
      via: "容错拼接"
      pattern: "cat.*>>.*SM_Extend"
---

<objective>
创建声明式构建系统：`build_order.txt` 定义全量文件列表，`build.sh` 读取并容错拼接生成 SM_Extend.lua。

Purpose: 消除 build.sh 中的 `grep -v` 黑名单，以声明式文件列表驱动构建。per D-09 一次性写出所有 Phase 2-4 未来文件路径，per D-10 容错模式跳过未创建文件。
Output: build_order.txt + 重写后的 build.sh，`./build.sh` 成功生成 SM_Extend.lua。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md — build target: SM_Extend.lua, no require constraint
@.planning/ROADMAP.md — Phase 1.5 tasks T1.5.1, T1.5.2; Phase 2-4 future file paths
@.planning/REQUIREMENTS.md — R7 (声明式构建系统) 验收标准
@.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-09, D-10, D-11 locked decisions
@.planning/codebase/STRUCTURE.md — current build concatenation order
@CLAUDE.md — build system documentation

<interfaces>
From Plans 01-05 outputs after completion:
```
core/class.lua, core/periodic.lua
entity/Unit.lua, entity/Player.lua, entity/Target.lua, entity/Pet.lua
entity/TargetTarget.lua, entity/TargetPet.lua, entity/PetTarget.lua
entity/Group.lua, entity/Raid.lua
```

Current build.sh Cygwin copy logic to preserve (lines 32-35):
```sh
if [[ "$OSTYPE" == "cygwin" ]]; then
    cp $target /cygdrive/d/games/TurtleWoW/Interface/AddOns/SuperMacro/
fi
```

Required build order (dependencies: core before entity, base classes before subclasses):
1. macro_torch.lua (namespace init)
2. impl_util.lua (utilities)
3. biz_util.lua (business utilities)
4. core/class.lua (classMetatable factory — entity files depend on this)
5. core/periodic.lua (periodic task scheduler — Target.lua calls registerPeriodicTask)
6. entity/Unit.lua (base entity — all other entities inherit from this)
7. entity/Player.lua, entity/Target.lua, entity/Pet.lua (extends Unit)
8. entity/TargetTarget.lua, entity/TargetPet.lua, entity/PetTarget.lua (extends Unit)
9. entity/Group.lua, entity/Raid.lua (standalone)
10. battle_event_queue.lua (combat events — depends on entity singletons)
11. texture_map.lua, interface_debug.lua (support modules)
12. SM_Extend_*.lua (class files — depend on entity + core + events)
13. core/events.lua, core/combat_context.lua, core/spell_trace.lua, core/selftest.lua (Phase 2-3 future)
14. classes/Druid.lua, classes/Druid/*, classes/*.lua (Phase 4 future)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 build_order.txt，包含全量文件列表</name>
  <files>build_order.txt</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/.planning/ROADMAP.md — Phase 2-4 file lists
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-09, D-10 全量策略
  </read_first>
  <action>
创建 `build_order.txt`，per D-09 一次性全量写出所有 Phase 1-4 的文件路径。每行一个文件路径，空行和 `#` 注释行在 build.sh 中被忽略。

per D-10: 容错模式 — 当前不存在的文件（core/events.lua, classes/Druid.lua 等 Phase 2-4 文件）将被 build.sh 静默跳过。

完整文件列表（按依赖关系排列）：

```
macro_torch.lua
impl_util.lua
biz_util.lua
# core/ infrastructure (Phase 1)
core/class.lua
core/periodic.lua
# entity/ layer (Phase 1 migrated)
entity/Unit.lua
entity/Player.lua
entity/Target.lua
entity/Pet.lua
entity/TargetTarget.lua
entity/TargetPet.lua
entity/PetTarget.lua
entity/Group.lua
entity/Raid.lua
# combat events (Phase 2 will split this)
battle_event_queue.lua
texture_map.lua
interface_debug.lua
# core/ Phase 2 future files
core/events.lua
core/combat_context.lua
core/spell_trace.lua
# core/ Phase 3 future file
core/selftest.lua
# current class files (Phase 4 will move to classes/)
SM_Extend_Druid.lua
SM_Extend_Hunter.lua
SM_Extend_Mage.lua
SM_Extend_Priest.lua
SM_Extend_Rogue.lua
SM_Extend_Warlock.lua
SM_Extend_Warrior.lua
# classes/ Phase 4 future files
classes/Druid.lua
classes/Druid/cat.lua
classes/Druid/bear.lua
classes/Druid/utility.lua
classes/Hunter.lua
classes/Mage.lua
classes/Priest.lua
classes/Rogue.lua
classes/Warlock.lua
classes/Warrior.lua
```

关键顺序保证：
- core/ 在 entity/ 之前（entity 依赖 classMetatable 和 registerPeriodicTask）
- entity/Unit.lua 在所有 entity/ 子类之前（继承关系）
- battle_event_queue.lua 在 entity/ 之后（eventHandle 使用 macroTorch.player/target 全局单例）
- 现有 SM_Extend_*.lua 文件在 entity/ + battle + texture + debug 之后
- 未来 Phase 2-4 的 core/ 和 classes/ 文件在当前所有文件之后（注释分隔）
</action>
  <verify>
    <automated>test $(wc -l < /Users/yue.weny/finalanswer/macro-torch/macro-torch/build_order.txt | tr -d ' ') -gt 25</automated>
  </verify>
  <acceptance_criteria>
    - build_order.txt 行数 > 25
    - 第一行是 `macro_torch.lua`
    - 包含所有 Phase 1 已创建/已迁移文件路径
    - 包含所有 Phase 2-4 未来文件路径（容错模式跳过）
    - 包含 `battle_event_queue.lua`（Phase 2 前需要）
    - 包含所有 7 个现有 SM_Extend_*.lua 文件
    - 顺序满足依赖关系（core → entity → battle → classes）
  </acceptance_criteria>
  <done>build_order.txt 创建完成，包含全量文件列表，按依赖序排列</done>
</task>

<task type="auto">
  <name>Task 2: 重写 build.sh，读取 build_order.txt，容错模式拼接</name>
  <files>build.sh</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/build.sh — 当前 38 行文件，需保留 Cygwin 拷贝逻辑
  </read_first>
  <action>
per D-10: 完全重写 `build.sh`，读取 `build_order.txt` 按序拼接，容错模式。

删除所有旧的硬编码文件列表（macro_torch.lua, impl_util.lua, interface_debug.lua, Unit.lua 的手动 cat 行）和 grep -v 黑名单。

新 build.sh 逻辑：
1. 若 SM_Extend.lua 已存在则删除
2. while read 循环逐行读取 build_order.txt
3. 跳过空行和 `#` 注释行
4. 对存在的文件，先追加一个换行符然后 cat 到 target（`[ -f "$line" ] && printf '\n' >> "$target" && cat "$line" >> "$target"`）
5. 保留 Cygwin 拷贝逻辑（含被注释掉的第二个 cp 行）

关键要求：
- 使用 `while IFS= read -r line` 逐行读取以保留空格
- 使用 `[ -z "$line" ]` 跳过空行
- 使用 `[ "${line#\#}" != "$line" ]` 跳过 `#` 注释行
- 使用 `[ -f "$line" ]` 容错守卫，静默跳过不存在文件
- Cygwin 检测使用 `[[ "$OSTYPE" == "cygwin" ]]`
</action>
  <verify>
    <automated>/Users/yue.weny/finalanswer/macro-torch/macro-torch/build.sh && test -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/SM_Extend.lua && echo "Build OK"</automated>
  </verify>
  <acceptance_criteria>
    - `./build.sh` 执行成功，返回码 0
    - 产物 `SM_Extend.lua` 存在且非空
    - `grep "grep -v" build.sh` 无结果（旧黑名单已删除）
    - `grep "build_order.txt" build.sh` 返回匹配行（读取声明式列表）
    - `grep "while.*read.*line" build.sh` 返回匹配行
    - `grep '\[ -f.*\].*cat' build.sh` 返回匹配行（容错守卫）
    - Cygwin 拷贝逻辑保留
    - SM_Extend.lua 产物中包含 `function macroTorch.classMetatable`
  </acceptance_criteria>
  <done>build.sh 已重写为声明式容错模式，build_order.txt 驱动拼接，构建成功</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build.sh → SM_Extend.lua | 文件拼接输出，无用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06a | Tampering | build_order.txt | accept | 文件路径为静态列表，无外部输入注入点 |
| T-01-06b | Denial of Service | build.sh → 文件缺失 | mitigate | 容错模式 `[ -f "$line" ]` 跳过缺失文件，构建不中断 |
</threat_model>

<verification>
```bash
# build_order.txt 存在且包含关键路径
grep -c "core/class.lua\|entity/Unit.lua\|battle_event_queue.lua\|SM_Extend_Druid.lua" build_order.txt
# 期望: 4

# build_order.txt 包含未来 Phase 2-4 路径
grep -c "core/events.lua\|core/combat_context.lua\|core/spell_trace.lua\|core/selftest.lua\|classes/" build_order.txt
# 期望: >=8

# build.sh 重构完成
grep "while.*read.*line\|build_order.txt" build.sh  # 新逻辑存在
grep "grep -v" build.sh  # 期望: 无结果 (旧黑名单已删除)

# 构建成功
./build.sh && test -f SM_Extend.lua && echo "Build OK"

# 产物中包含关键符号
grep -c "function macroTorch.classMetatable\|function macroTorch.initPlayer\|function macroTorch.registerPeriodicTask" SM_Extend.lua
# 期望: 3
```
</verification>

<success_criteria>
- build_order.txt 包含完整的文件列表（当前 + 未来 Phase 2-4）
- build.sh 读取 build_order.txt，容错模式跳过不存在文件
- ./build.sh 成功生成 SM_Extend.lua
- 产物中包含 classMetatable、initPlayer、registerPeriodicTask 等关键符号
</success_criteria>

<output>
Create `.planning/phases/01-classmetatable-entity/01-06-SUMMARY.md` when done
</output>