---
phase: 01-classmetatable-entity
plan: 05
type: execute
wave: 2
depends_on: [01-01]
files_modified: [entity/Pet.lua, entity/TargetTarget.lua, entity/TargetPet.lua, entity/PetTarget.lua, entity/Group.lua, entity/Raid.lua]
autonomous: true
requirements: [R1, R6]
user_setup: []

must_haves:
  truths:
    - "Pet.lua 文件移动到 entity/Pet.lua，metatable 替换为 classMetatable(self, \"PET_FIELD_FUNC_MAP\")，根目录 Pet.lua 已删除"
    - "TargetTarget.lua 移动到 entity/TargetTarget.lua，setmetatable(obj, self) 模式替换为 classMetatable，根目录文件已删除"
    - "TargetPet.lua 移动到 entity/TargetPet.lua，setmetatable(obj, self) 模式替换为 classMetatable，根目录文件已删除"
    - "PetTarget.lua 移动到 entity/PetTarget.lua，setmetatable(obj, self) 模式替换为 classMetatable，根目录文件已删除"
    - "Group.lua 移动到 entity/Group.lua，内容不变，根目录文件已删除"
    - "Raid.lua 移动到 entity/Raid.lua，内容不变，根目录文件已删除"
  artifacts:
    - path: "entity/Pet.lua"
      provides: "Pet 类，使用 classMetatable(self, \"PET_FIELD_FUNC_MAP\")"
    - path: "entity/TargetTarget.lua"
      provides: "TargetTarget 类，使用 classMetatable(self, nil)（无 FIELDMAP）"
    - path: "entity/TargetPet.lua"
      provides: "TargetPet 类，使用 classMetatable(self, nil)（无 FIELDMAP）"
    - path: "entity/PetTarget.lua"
      provides: "PetTarget 类，使用 classMetatable(self, nil)（无 FIELDMAP）"
    - path: "entity/Group.lua"
      provides: "Group 占位（空壳），内容不变"
    - path: "entity/Raid.lua"
      provides: "Raid 占位（空壳），内容不变"
  key_links:
    - from: "entity/Pet.lua → setmetatable(obj, macroTorch.classMetatable(...))"
      to: "core/class.lua → macroTorch.classMetatable"
      via: "globally available function"
      pattern: "macroTorch%.classMetatable"
---

<objective>
迁移 6 个 Entity 文件到 entity/ 目录：Pet.lua（含 metatable 替换）、TargetTarget.lua、TargetPet.lua、PetTarget.lua（setmetatable(obj, self) 模式替换为 classMetatable）、Group.lua、Raid.lua（内容不变的纯移动）。

Purpose: 完成 entity/ 目录中所有 9 个实体文件的重组，统一 metatable 构造方式。
Output: entity/Pet.lua, entity/TargetTarget.lua, entity/TargetPet.lua, entity/PetTarget.lua, entity/Group.lua, entity/Raid.lua 全部就位，根目录原文件已删除。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md — project constraints
@.planning/ROADMAP.md — Phase 1.3 tasks T1.3.4, T1.3.5, T1.3.6
@.planning/REQUIREMENTS.md — R1 (统一 Metatable), R6 (entity/ 目录)
@.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-01, D-05 locked decisions
@.planning/codebase/STRUCTURE.md — current file layout
@.planning/codebase/ARCHITECTURE.md — Entity inheritance chain

<interfaces>
From Plan 01-01 outputs:
- `macroTorch.classMetatable(cls, fieldMapName)` — factory in core/class.lua

Current metatable patterns to replace:

Pet.lua:101-114 — standard pattern (same as Unit/Player):
```lua
setmetatable(obj, {
    __index = function(t, k)
        if macroTorch.PET_FIELD_FUNC_MAP[k] ~= nil then
            return macroTorch.PET_FIELD_FUNC_MAP[k](t)
        end
        local class_val = self[k]
        if class_val ~= nil then
            return class_val
        end
    end
})
```

TargetTarget.lua:19-24 — uses setmetatable(obj, self) pattern:
```lua
function macroTorch.TargetTarget:new()
    local obj = {}
    self.__index = self
    setmetatable(obj, self)
    return obj
end
```

PetTarget.lua:20-25 — same setmetatable(obj, self) pattern.
TargetPet.lua:19-24 — same setmetatable(obj, self) pattern.

TargetTarget/PetTarget/TargetPet 没有自己的 FIELD_FUNC_MAP，继承 Unit 的 UNIT_FIELD_FUNC_MAP 通过原型链。替换为 classMetatable(self, nil)：fieldMapName 传 nil 跳过 fieldMap 查找，回退到 self[k]，self 的 metatable 链自动处理 UNIT_FIELD_FUNC_MAP 和 Unit class 方法。与 setmetatable(obj, self) 语义完全等价。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 Pet.lua to entity/Pet.lua，替换 metatable</name>
  <files>entity/Pet.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/Pet.lua — FULL file (134 lines)，关注 setmetatable (lines 101-114) 和 singleton init (line 134)
  </read_first>
  <action>
将 `Pet.lua` 移动到 `entity/Pet.lua`，并做 metatable 替换：

1. 复制 Pet.lua 全部内容到 entity/Pet.lua
2. per D-01: 将手写 setmetatable 模板（当前 lines 101-114）替换为单行：
```
setmetatable(obj, macroTorch.classMetatable(self, "PET_FIELD_FUNC_MAP"))
```
3. 文件其余所有代码完全不变：license header、macroTorch.Pet 类定义、Pet:new() 中所有实例方法（aggressiveMode, attack, dismiss 等）、PET_FIELD_FUNC_MAP（isAttackActive 字段）、macroTorch.pet singleton
4. 删除根目录的 `Pet.lua`
</action>
  <verify>
    <automated>grep "macroTorch.classMetatable" /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/Pet.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.classMetatable" entity/Pet.lua` 返回匹配行
    - `grep "setmetatable(obj, {" entity/Pet.lua` 无结果
    - `grep "macroTorch.pet = macroTorch.Pet:new()" entity/Pet.lua` 返回匹配行（singleton 保留）
    - `test ! -f Pet.lua` 返回 true
  </acceptance_criteria>
  <done>Pet.lua 已迁移到 entity/Pet.lua，metatable 替换为 classMetatable(self, "PET_FIELD_FUNC_MAP")</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 TargetTarget.lua to entity/TargetTarget.lua，替换 setmetatable(obj, self) 模式</name>
  <files>entity/TargetTarget.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/TargetTarget.lua — FULL file (27 lines)，理解 setmetatable(obj, self) + self.__index = self 模式
  </read_first>
  <action>
将 `TargetTarget.lua` 移动到 `entity/TargetTarget.lua`，替换 metatable 模式：

当前 TargetTarget:new() 使用 `setmetatable(obj, self)` + `self.__index = self`——self 是 `macroTorch.Unit:new("targettarget")` 返回的 Unit 原型实例。

替换步骤：
1. 删除 `self.__index = self` 行
2. 将 `setmetatable(obj, self)` 替换为：
```
setmetatable(obj, macroTorch.classMetatable(self, nil))
```

为什么 classMetatable(self, nil)：TargetTarget 没有自己的 FIELDMAP。classMetatable(self, nil) 跳过 fieldMap 查找 → 回退到 self[k]，self 的 metatable 链自动处理 UNIT_FIELD_FUNC_MAP 和 Unit class 方法。与 setmetatable(obj, self) 语义完全等价。

3. 删除根目录 TargetTarget.lua
</action>
  <verify>
    <automated>grep "macroTorch.classMetatable" /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/TargetTarget.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.classMetatable" entity/TargetTarget.lua` 返回匹配行
    - `grep "self.__index = self" entity/TargetTarget.lua` 无结果
    - `grep "setmetatable(obj, self)" entity/TargetTarget.lua` 无结果
    - `grep "macroTorch.targettarget = macroTorch.TargetTarget:new()" entity/TargetTarget.lua` 返回匹配行
    - `test ! -f TargetTarget.lua` 返回 true
  </acceptance_criteria>
  <done>TargetTarget.lua 已迁移，setmetatable(obj, self) 模式替换为 classMetatable(self, nil)</done>
</task>

<task type="auto">
  <name>Task 3: 批量迁移 TargetPet, PetTarget, Group, Raid 到 entity/</name>
  <files>entity/TargetPet.lua, entity/PetTarget.lua, entity/Group.lua, entity/Raid.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/TargetPet.lua — 27 lines，setmetatable(obj, self) 模式
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/PetTarget.lua — 28 lines，setmetatable(obj, self) 模式
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/Group.lua — 18 lines，空壳 macroTorch.group = {}
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/Raid.lua — 18 lines，空壳 macroTorch.raid = {}
  </read_first>
  <action>
批量处理 4 个文件：

**TargetPet.lua → entity/TargetPet.lua：**
- 与 TargetTarget 相同的 `setmetatable(obj, self)` + `self.__index = self` 模式
- 删除 `self.__index = self`，将 `setmetatable(obj, self)` 替换为 `setmetatable(obj, macroTorch.classMetatable(self, nil))`
- 删除根目录 TargetPet.lua

**PetTarget.lua → entity/PetTarget.lua：**
- 相同的替换模式
- 删除 `self.__index = self`，setmetatable(obj, self) → setmetatable(obj, macroTorch.classMetatable(self, nil))
- 删除根目录 PetTarget.lua

**Group.lua → entity/Group.lua：**
- 仅 `macroTorch.group = {}` 一行空壳，无 class，无 metatable
- 纯文件移动，内容完全不变
- 删除根目录 Group.lua

**Raid.lua → entity/Raid.lua：**
- 仅 `macroTorch.raid = {}` 一行空壳，无 class，无 metatable
- 纯文件移动，内容完全不变
- 删除根目录 Raid.lua
</action>
  <verify>
    <automated>test -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/TargetPet.lua && test -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/PetTarget.lua && test -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/Group.lua && test -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/Raid.lua && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - entity/TargetPet.lua 存在，使用 classMetatable(self, nil)
    - entity/PetTarget.lua 存在，使用 classMetatable(self, nil)
    - entity/Group.lua 存在，内容为 macroTorch.group = {}（不变）
    - entity/Raid.lua 存在，内容为 macroTorch.raid = {}（不变）
    - 根目录 TargetPet.lua, PetTarget.lua, Group.lua, Raid.lua 全部已删除
  </acceptance_criteria>
  <done>4 个文件已迁移到 entity/，TargetPet/PetTarget 使用 classMetatable(self, nil)，Group/Raid 不变</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯文件重组和等价变换 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05a | 全部 | entity/Pet.lua, entity/TargetTarget.lua, entity/TargetPet.lua, entity/PetTarget.lua | accept | 纯等价变换，语义 preserved |
</threat_model>

<verification>
```bash
# 6 个文件已迁移到 entity/
test -f entity/Pet.lua && test -f entity/TargetTarget.lua
test -f entity/TargetPet.lua && test -f entity/PetTarget.lua
test -f entity/Group.lua && test -f entity/Raid.lua

# 根目录原文件已删除
test ! -f Pet.lua && test ! -f TargetTarget.lua
test ! -f TargetPet.lua && test ! -f PetTarget.lua
test ! -f Group.lua && test ! -f Raid.lua

# classMetatable 用于需要它的文件
grep -c "macroTorch.classMetatable" entity/Pet.lua entity/TargetTarget.lua entity/TargetPet.lua entity/PetTarget.lua
# 期望: 4

# 旧 metatable 模式已清除
grep "setmetatable(obj, self)" entity/TargetTarget.lua entity/TargetPet.lua entity/PetTarget.lua
# 期望: 无结果
```
</verification>

<success_criteria>
- 6 个 entity 文件已从根目录迁移到 entity/ 目录
- Pet 使用 classMetatable(self, "PET_FIELD_FUNC_MAP")
- TargetTarget, TargetPet, PetTarget 使用 classMetatable(self, nil)
- Group, Raid 内容不变（空壳）
- 根目录原文件已全部删除
</success_criteria>

<output>
Create `.planning/phases/01-classmetatable-entity/01-05-SUMMARY.md` when done
</output>