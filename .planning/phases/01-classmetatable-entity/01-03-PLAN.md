---
phase: 01-classmetatable-entity
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [core/periodic.lua]
autonomous: true
requirements: [R6]
user_setup: []

must_haves:
  truths:
    - "`macroTorch.LRUStack` 完整代码从 event_stack.lua 迁入 core/periodic.lua"
    - "`macroTorch.ES_FIELD_FUNC_MAP` 从 event_stack.lua 迁入 core/periodic.lua"
    - "LRUStack 的 `new()` 使用 `macroTorch.classMetatable(nil, \"ES_FIELD_FUNC_MAP\")` 替代手写 metatable"
    - "`macroTorch.registerPeriodicTask`、`macroTorch.removePeriodicTask`、`macroTorch.setRepeat` 从 battle_event_queue.lua 迁入 core/periodic.lua"
    - "`macroTorch.onPeriodicUpdate()` 从 battle_event_queue.lua 迁入 core/periodic.lua"
    - "core/periodic.lua 有独立的 `local frame = CreateFrame(\"Frame\")` 承载 OnUpdate"
    - "event_stack.lua 文件已删除"
    - "battle_event_queue.lua 中已迁移代码已删除"
  artifacts:
    - path: "core/periodic.lua"
      provides: "LRUStack + ES_FIELD_FUNC_MAP + periodic task 调度系统 + 独立 OnUpdate Frame"
      min_lines: 120
  key_links:
    - from: "core/periodic.lua → LRUStack:new()"
      to: "core/class.lua → macroTorch.classMetatable(nil, ...)"
      via: "setmetatable(obj, macroTorch.classMetatable(nil, \"ES_FIELD_FUNC_MAP\"))"
      pattern: "classMetatable%(nil,"
    - from: "core/periodic.lua → frame:SetScript(\"OnUpdate\", ...)"
      to: "macroTorch.onPeriodicUpdate()"
      via: "OnUpdate handler → pcall(macroTorch.onPeriodicUpdate)"
      pattern: "onPeriodicUpdate"
---

<objective>
迁移 LRUStack + ES_FIELD_FUNC_MAP 从 event_stack.lua 到 core/periodic.lua（使用 classMetatable(nil)），迁移 registerPeriodicTask/removePeriodicTask/setRepeat/onPeriodicUpdate + 独立 OnUpdate Frame 从 battle_event_queue.lua 到 core/periodic.lua，然后清理删除 event_stack.lua 和 battle_event_queue.lua 中已迁移代码。

Purpose: 建立独立的 periodic task 调度模块，使用 classMetatable 统一 LRUStack 的 metatable 构造。per D-14/D-15 实现 Frame 分离。
Output: core/periodic.lua（完整 periodic task 系统 + 独立 OnUpdate Frame），event_stack.lua 已删除，battle_event_queue.lua 已精简
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md — project constraints (Lua 5.1, no require)
@.planning/ROADMAP.md — Phase 1.2 task details (T1.2.1 through T1.2.4, all in one plan)
@.planning/REQUIREMENTS.md — R3 (战斗事件系统模块化), R6 (core/ 目录)
@.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-12, D-13, D-14, D-15 locked decisions
@.planning/codebase/ARCHITECTURE.md — LRUStack data structure, periodic task scheduler, OnUpdate handler
@.planning/codebase/CONVENTIONS.md — naming conventions
@.planning/codebase/STRUCTURE.md — file locations

<interfaces>
From Plan 01-01 outputs:
- `macroTorch.classMetatable(cls, fieldMapName)` — factory in core/class.lua (used with cls=nil for LRUStack)

From event_stack.lua — FULL file content to migrate (75 lines):
- Lines 1-58: macroTorch.LRUStack class with hand-written setmetatable
- Lines 59-74: macroTorch.ES_FIELD_FUNC_MAP

From battle_event_queue.lua — code blocks to EXTRACT (copy then delete from source):
- Lines 155-158: frame.lastUpdate, frame.leastUpdateInterval, macroTorch.periodicTasks init
- Lines 161-175: macroTorch.onPeriodicUpdate() function
- Lines 177-189: macroTorch.registerPeriodicTask, macroTorch.removePeriodicTask, macroTorch.setRepeat
- Lines 190-200: frame:SetScript("OnUpdate", ...) — the OnUpdate handler with pcall wrapping

per D-14: periodic.lua creates independent Frame — NOT reusing battle_event_queue.lua's frame.
per D-15: OnUpdate code block and OnEvent handler are zero-coupled, extraction is atomic.
per D-12/D-13: LRUStack uses classMetatable(nil, "ES_FIELD_FUNC_MAP") — cls=nil triggers nil-guard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 core/periodic.lua，迁移 LRUStack + ES_FIELD_FUNC_MAP，使用 classMetatable</name>
  <files>core/periodic.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/event_stack.lua — FULL file (75 lines)，理解 LRUStack 完整结构
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-12, D-13 decisions
  </read_first>
  <action>
创建 `core/periodic.lua`，写入两部分内容：

**Part 1: LRUStack + ES_FIELD_FUNC_MAP 迁移**

per D-12: 从 `event_stack.lua` 完整迁移 `macroTorch.LRUStack` 类和 `macroTorch.ES_FIELD_FUNC_MAP` 到 core/periodic.lua。

per D-12/D-13: 关键改动 — 将 LRUStack:new() 中当前的手写 setmetatable 模板（event_stack.lua:9-22）替换为：
```lua
setmetatable(obj, macroTorch.classMetatable(nil, "ES_FIELD_FUNC_MAP"))
```

cls 传 nil 因为 LRUStack 没有父类。classMetatable 的 nil-guard（Plan 01 中实现）确保 cls 为 nil 时跳过 class method fallback。

保持以下内容完全不变：
- LRUStack:new() 的构造逻辑（maxSize, elements 初始化）
- push, pop, anyMatch, allMatch 实例方法
- ES_FIELD_FUNC_MAP 全部字段（size, top, isEmpty）

文件头包含 Apache 2.0 license block comment。

**注意：** event_stack.lua 本身暂不删除——在 Task 3 的清理步骤中统一删除。此 task 只负责在新文件中创建内容。
</action>
  <verify>
    <automated>grep "function macroTorch.LRUStack:new" /Users/yue.weny/finalanswer/macro-torch/macro-torch/core/periodic.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "function macroTorch.LRUStack:new" core/periodic.lua` 返回匹配行
    - `grep "classMetatable(nil," core/periodic.lua` 返回匹配行（LRUStack 使用 classMetatable(nil, ...)）
    - ES_FIELD_FUNC_MAP 三个字段（size, top, isEmpty）全部迁入
    - push, pop, anyMatch, allMatch 四个实例方法全部迁入
  </acceptance_criteria>
  <done>LRUStack + ES_FIELD_FUNC_MAP 已迁入 core/periodic.lua，使用 classMetatable(nil) 统一 metatable 构造</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 registerPeriodicTask / removePeriodicTask / setRepeat / onPeriodicUpdate + 独立 OnUpdate Frame</name>
  <files>core/periodic.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/battle_event_queue.lua — lines 155-200 (OnUpdate 代码块, ~45 行)，理解函数签名和 Frame 配置
  </read_first>
  <action>
在 `core/periodic.lua` 中（LRUStack + ES_FIELD_FUNC_MAP 之后）追加以下内容，全部从 battle_event_queue.lua 迁移：

per D-14/D-15: 创建独立的 OnUpdate Frame（不与 battle_event_queue.lua 共享）：
```lua
local frame = CreateFrame("Frame")
```

迁移以下代码块，保持所有逻辑完全不变：

1. **Frame 时间状态初始化** (battle_event_queue.lua:155-159):
   - `frame.lastUpdate = 0`
   - `frame.leastUpdateInterval = 0.1`
   - `macroTorch.periodicTasks` 初始化（`if not macroTorch.periodicTasks then ...`）

2. **macroTorch.onPeriodicUpdate() 函数** (battle_event_queue.lua:161-175):
   - 完整函数体，遍历 periodicTasks 并执行

3. **macroTorch.registerPeriodicTask(name, task)** (battle_event_queue.lua:177-179):
   - 完整函数体

4. **macroTorch.removePeriodicTask(name)** (battle_event_queue.lua:181-183):
   - 完整函数体

5. **macroTorch.setRepeat(name, interval, times, func)** (battle_event_queue.lua:186-189):
   - 完整函数体

6. **OnUpdate 脚本** (battle_event_queue.lua:190-200):
   - `frame:SetScript("OnUpdate", function() ... end)`
   - 包含 pcall 包裹的 `macroTorch.onPeriodicUpdate` 调用和错误处理
   - `frame.lastUpdate = GetTime()` 更新逻辑

**关键约束**：
- 所有函数签名保持不变，全局命名不变（macroTorch.registerPeriodicTask 等）
- OnUpdate 间隔逻辑（0.1s leastUpdateInterval）不变
- pcall 错误处理逻辑不变（错误消息格式："onPeriodicUpdate执行错误: "）
- `local frame = CreateFrame("Frame")` 是 periodic.lua 内独有的局部变量，不与 battle_event_queue.lua 的 frame 冲突
</action>
  <verify>
    <automated>grep "function macroTorch.registerPeriodicTask\|function macroTorch.onPeriodicUpdate\|SetScript.*OnUpdate" /Users/yue.weny/finalanswer/macro-torch/macro-torch/core/periodic.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "function macroTorch.registerPeriodicTask" core/periodic.lua` 返回匹配行
    - `grep "function macroTorch.removePeriodicTask" core/periodic.lua` 返回匹配行
    - `grep "function macroTorch.setRepeat" core/periodic.lua` 返回匹配行
    - `grep "function macroTorch.onPeriodicUpdate" core/periodic.lua` 返回匹配行
    - `grep "SetScript.*OnUpdate" core/periodic.lua` 返回匹配行
    - `grep "CreateFrame.*Frame" core/periodic.lua` 返回匹配行（独立 Frame）
    - `grep "pcall.*onPeriodicUpdate" core/periodic.lua` 返回匹配行
  </acceptance_criteria>
  <done>registerPeriodicTask/removePeriodicTask/setRepeat/onPeriodicUpdate + 独立 OnUpdate Frame 全部迁入 core/periodic.lua</done>
</task>

<task type="auto">
  <name>Task 3: 清理：删除 event_stack.lua，从 battle_event_queue.lua 删除已迁移代码</name>
  <files>event_stack.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/event_stack.lua — 确认完整内容已迁移（对比 core/periodic.lua）
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/battle_event_queue.lua — lines 155-200，确认迁移边界精确
  </read_first>
  <action>
执行两项清理操作：

**清理 1: 删除 event_stack.lua**

`event_stack.lua` 的全部内容（LRUStack + ES_FIELD_FUNC_MAP）已迁入 `core/periodic.lua`（Task 1），原文件删除。

**清理 2: 从 battle_event_queue.lua 删除已迁移代码**

从 `battle_event_queue.lua` 中删除以下已迁入 `core/periodic.lua` 的代码块（per D-14）：
- Lines 155-159: `frame.lastUpdate`, `frame.leastUpdateInterval`, `macroTorch.periodicTasks` 初始化
- Lines 161-175: `macroTorch.onPeriodicUpdate()` 函数
- Lines 177-179: `macroTorch.registerPeriodicTask()` 函数
- Lines 181-183: `macroTorch.removePeriodicTask()` 函数
- Lines 186-189: `macroTorch.setRepeat()` 函数
- Lines 190-200: `frame:SetScript("OnUpdate", ...)` OnUpdate handler

**注意：** battle_event_queue.lua 中保留的内容：
- Lines 1-70: spell tracing setup（DEBUFF_LAND_LAG, tracingSpells, setSpellTracing 等）—— Phase 2 迁移
- Lines 44-56: frame 创建和事件注册（CreateFrame, RegisterEvent）—— Phase 2 迁移
- Lines 71-153: eventHandle() 函数（含已修改的 PLAYER_ENTERING_WORLD handler）—— Phase 2 迁移
- Lines 202-518: maintainLandTables, spellsImmuneTracing, recordCastTable, loadImmuneTable 等—— Phase 2 迁移

删除边界：只删除 periodic task scheduling 相关代码块，不碰其他代码。
</action>
  <verify>
    <automated>test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/event_stack.lua && echo "event_stack.lua deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f event_stack.lua` 返回 true（文件已删除）
    - `grep "registerPeriodicTask\|setRepeat\|onPeriodicUpdate\|frame:SetScript.*OnUpdate" battle_event_queue.lua` 无结果
    - battle_event_queue.lua 中 `frame:SetScript("OnEvent", ...)` (line 153) 保留
    - battle_event_queue.lua 中 eventHandle() 函数保留
    - battle_event_queue.lua 总行数减少约 45 行
  </acceptance_criteria>
  <done>event_stack.lua 已删除，battle_event_queue.lua 中 periodic task 代码已清除</done>
</task>

</tasks>

<threat_model>
本 plan 迁移现有代码到新文件并创建独立 OnUpdate Frame。OnUpdate 中的 pcall 错误处理保留不变。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OnUpdate → onPeriodicUpdate | 帧回调调用 periodic task 函数；pcall 包裹确保单个 task 错误不中断框架 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03a | Denial of Service | OnUpdate frame → onPeriodicUpdate | mitigate | pcall 包裹已存在，保留不变；单个 task 异常不影响其他 task |
| T-01-03b | Information Disclosure | 无 | accept | 迁移操作不引入新数据流 |
</threat_model>

<verification>
```bash
# event_stack.lua 已删除
test ! -f event_stack.lua

# periodic.lua 包含所有关键函数和独立 Frame
grep -c "function macroTorch.LRUStack:new\|function macroTorch.registerPeriodicTask\|function macroTorch.removePeriodicTask\|function macroTorch.setRepeat\|function macroTorch.onPeriodicUpdate\|CreateFrame.*Frame\|SetScript.*OnUpdate" core/periodic.lua
# 期望: 7

# LRUStack 使用 classMetatable(nil)
grep "classMetatable(nil," core/periodic.lua

# battle_event_queue.lua 已清除 periodic task 代码
grep "registerPeriodicTask\|setRepeat\|onPeriodicUpdate\|frame:SetScript.*OnUpdate" battle_event_queue.lua
# 期望: 无结果
```
</verification>

<success_criteria>
- event_stack.lua 已删除（所有内容迁入 core/periodic.lua）
- core/periodic.lua 包含完整的 periodic task 调度系统 + 独立 OnUpdate Frame
- LRUStack 使用 classMetatable(nil) 统一 metatable 构造
- battle_event_queue.lua 已清除已迁移代码
</success_criteria>

<output>
Create `.planning/phases/01-classmetatable-entity/01-03-SUMMARY.md` when done
</output>