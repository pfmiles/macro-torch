---
phase: 01-classmetatable-entity
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified: [entity/Unit.lua, entity/Player.lua, entity/Target.lua]
autonomous: true
requirements: [R1, R6]
user_setup: []

must_haves:
  truths:
    - "Unit.lua 文件移动到 entity/Unit.lua，根目录 Unit.lua 已删除"
    - "Player.lua 文件移动到 entity/Player.lua，根目录 Player.lua 已删除"
    - "Target.lua 文件移动到 entity/Target.lua，根目录 Target.lua 已删除"
    - "Unit.lua new() 中手写 setmetatable 模板替换为 `macroTorch.classMetatable(self, \"UNIT_FIELD_FUNC_MAP\")`"
    - "Player.lua new() 中手写 setmetatable 模板替换为 `macroTorch.classMetatable(self, \"PLAYER_FIELD_FUNC_MAP\")`"
    - "Target.lua new() 中手写 setmetatable 模板替换为 `macroTorch.classMetatable(self, \"TARGET_FIELD_FUNC_MAP\")`"
  artifacts:
    - path: "entity/Unit.lua"
      provides: "Unit 基类（含 UNIT_FIELD_FUNC_MAP），使用 classMetatable"
      min_lines: 240
    - path: "entity/Player.lua"
      provides: "Player 类（含 PLAYER_FIELD_FUNC_MAP），使用 classMetatable"
      min_lines: 680
    - path: "entity/Target.lua"
      provides: "Target 类（含 TARGET_FIELD_FUNC_MAP），使用 classMetatable"
      min_lines: 270
  key_links:
    - from: "entity/Unit.lua → setmetatable(obj, macroTorch.classMetatable(...))"
      to: "core/class.lua → macroTorch.classMetatable"
      via: "globally available function"
      pattern: "macroTorch%.classMetatable%(self, \"UNIT_FIELD_FUNC_MAP\"%)"
    - from: "entity/Player.lua → setmetatable(obj, macroTorch.classMetatable(...))"
      to: "core/class.lua → macroTorch.classMetatable"
      via: "globally available function"
      pattern: "macroTorch%.classMetatable%(self, \"PLAYER_FIELD_FUNC_MAP\"%)"
---

<objective>
将 Unit.lua、Player.lua、Target.lua 三个文件移动到 entity/ 目录，各文件 new() 方法中的手写 setmetatable 模板统一替换为一行 macroTorch.classMetatable(self, FIELDMAP) 调用。

Purpose: per D-01 以最简工厂统一 metatable 构造方式，消除 9 行重复模板。完成 entity/ 核心三个类的迁移。
Output: entity/Unit.lua, entity/Player.lua, entity/Target.lua，根目录原文件已删除
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md — project constraints (backward compatible, no require)
@.planning/ROADMAP.md — Phase 1.3 tasks T1.3.1, T1.3.2, T1.3.3
@.planning/REQUIREMENTS.md — R1 (统一 Metatable), R6 (entity/ 目录)
@.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-01, D-02, D-03 locked decisions
@.planning/codebase/ARCHITECTURE.md — Field Resolution Order, inheritance chain
@CLAUDE.md — project-specific guidelines, build system, metatable patterns

<interfaces>
From Plan 01-01 outputs:
- `macroTorch.classMetatable(cls, fieldMapName)` — factory in core/class.lua

Current metatable templates to replace:

Unit.lua:96-109 — current template:
```lua
setmetatable(obj, {
    __index = function(t, k)
        if macroTorch.UNIT_FIELD_FUNC_MAP[k] ~= nil then
            return macroTorch.UNIT_FIELD_FUNC_MAP[k](t)
        end
        local class_val = self[k]
        if class_val ~= nil then
            return class_val
        end
    end
})
```

Player.lua:469-482 — current template:
```lua
setmetatable(obj, {
    __index = function(t, k)
        if macroTorch.PLAYER_FIELD_FUNC_MAP[k] ~= nil then
            return macroTorch.PLAYER_FIELD_FUNC_MAP[k](t)
        end
        local class_val = self[k]
        if class_val ~= nil then
            return class_val
        end
    end
})
```

Target.lua:25-35 — current template:
```lua
setmetatable(obj, {
    __index = function(t, k)
        if macroTorch.TARGET_FIELD_FUNC_MAP[k] ~= nil then
            return macroTorch.TARGET_FIELD_FUNC_MAP[k](t)
        end
        local class_val = self[k]
        if class_val ~= nil then
            return class_val
        end
    end
})
```

Target.lua 有特殊点：lines 23-24 有 `self.__index = self` 行，这是原始注释遗留模式，metatable 替换为 classMetatable 后该行不再需要（classMetatable 的 __index 回退到 cls[k] 自动处理）。

per D-01: 每个文件仅替换 setmetatable 调用为一行 `macroTorch.classMetatable(self, "FIELD_MAP_NAME")`，其余代码不变。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 Unit.lua → entity/Unit.lua，替换 metatable</name>
  <files>entity/Unit.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/Unit.lua — FULL file (252 lines)，理解完整文件结构和 setmetatable 位置 (lines 96-109)
  </read_first>
  <action>
将 `Unit.lua` 移动到 `entity/Unit.lua`，并做 metatable 替换：

1. 复制 Unit.lua 全部内容到 entity/Unit.lua
2. per D-01: 在 `new()` 方法中，将手写 setmetatable 模板（当前 lines 96-109 的 `setmetatable(obj, { __index = function(t, k) ... })` 整个块）替换为单行：
```lua
setmetatable(obj, macroTorch.classMetatable(self, "UNIT_FIELD_FUNC_MAP"))
```
3. 文件其余所有代码（license header、macroTorch.Unit = {} 类定义、Unit:new() 构造器、实例方法、UNIT_FIELD_FUNC_MAP 全表）完全不变
4. 删除根目录的 `Unit.lua`

替换后的语义精确等价：classMetatable 的 __index 先查 `macroTorch.UNIT_FIELD_FUNC_MAP[k]`，再回退到 `self[k]`（即 Unit class 的方法/字段），与当前 hand-written pattern 完全一致。
</action>
  <verify>
    <automated>grep "macroTorch.classMetatable" /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/Unit.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.classMetatable" entity/Unit.lua` 返回匹配行
    - `grep "setmetatable(obj, {" entity/Unit.lua` 无结果（手写模板已替换）
    - `grep "UNIT_FIELD_FUNC_MAP" entity/Unit.lua | grep -v "^#" | grep -c "UNIT_FIELD_FUNC_MAP"` >= 2（定义 + classMetatable 调用各一次）
    - `test ! -f Unit.lua` 返回 true（根目录文件已删除）
    - entity/Unit.lua 文件总行数约 245 行（比原 Unit.lua 少约 7 行，因 setmetatable 模板压缩为 1 行）
  </acceptance_criteria>
  <done>Unit.lua 已迁移到 entity/Unit.lua，手写 metatable 模板替换为 classMetatable(self, "UNIT_FIELD_FUNC_MAP")</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 Player.lua → entity/Player.lua，替换 metatable</name>
  <files>entity/Player.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/Player.lua — FULL file (692 lines)，关注 setmetatable 位置 (lines 469-482) 和全局单例初始化 (line 535)
  </read_first>
  <action>
将 `Player.lua` 移动到 `entity/Player.lua`，并做 metatable 替换：

1. 复制 Player.lua 全部内容到 entity/Player.lua
2. per D-01: 在 `new()` 方法中，将手写 setmetatable 模板（当前 lines 469-482 的 `setmetatable(obj, { __index = function(t, k) ... })` 整个块）替换为单行：
```lua
setmetatable(obj, macroTorch.classMetatable(self, "PLAYER_FIELD_FUNC_MAP"))
```
3. 删除 lines 465-467 的 impl hint 注释（`-- impl hint: original '__index' & metatable setting:` 等 3 行），因为 classMetatable 已实现
4. 保留 line 535: `macroTorch.player = macroTorch.Player:new()` — per D-08 不修改此默认初始化
5. 文件其余所有代码（license header、macroTorch.Player、Player:new() 中所有实例方法、PLAYER_FIELD_FUNC_MAP 全表、末尾全局辅助函数 castIfBuffAbsent 等）完全不变
6. 删除根目录的 `Player.lua`

替换后语义精确等价：__index 先查 `macroTorch.PLAYER_FIELD_FUNC_MAP[k]`，回退到 `self[k]`（Player class 方法/字段），再通过 Player class 自身的 metatable 链回退到 Unit 层。
</action>
  <verify>
    <automated>grep "macroTorch.classMetatable" /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/Player.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.classMetatable" entity/Player.lua` 返回匹配行
    - `grep "setmetatable(obj, {" entity/Player.lua` 无结果（手写模板已替换）
    - `grep "macroTorch.player = macroTorch.Player:new()" entity/Player.lua` 返回匹配行（默认初始化保留）
    - `test ! -f Player.lua` 返回 true（根目录文件已删除）
    - entity/Player.lua 文件约 685 行
  </acceptance_criteria>
  <done>Player.lua 已迁移到 entity/Player.lua，metatable 模板替换为 classMetatable(self, "PLAYER_FIELD_FUNC_MAP")，默认 init 保留</done>
</task>

<task type="auto">
  <name>Task 3: 迁移 Target.lua → entity/Target.lua，替换 metatable</name>
  <files>entity/Target.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/Target.lua — FULL file (276 lines)，关注 setmetatable 位置 (lines 25-35) 和 self.__index = self (lines 23-24)
  </read_first>
  <action>
将 `Target.lua` 移动到 `entity/Target.lua`，并做 metatable 替换：

1. 复制 Target.lua 全部内容到 entity/Target.lua
2. per D-01: 在 `new()` 方法中，将手写 setmetatable 模板（当前 lines 25-35）替换为单行：
```lua
setmetatable(obj, macroTorch.classMetatable(self, "TARGET_FIELD_FUNC_MAP"))
```
3. 删除 lines 23-24 的 `self.__index = self` 行（classMetatable 已通过 `cls[k]` 提供类方法回退，不再需要设置 self.__index）
4. 文件其余所有代码完全不变——包括：
   - license header
   - `macroTorch.Target = macroTorch.Unit:new("target")` 类定义
   - Target:new() 中 isImmune, recordImmune, removeImmune, recordDefiniteBleeding, isDefiniteBleeding, willDieInSeconds 实例方法
   - `macroTorch.target = macroTorch.Target:new()` singleton
   - TARGET_FIELD_FUNC_MAP 空表
   - maintainTHV, currentHRPS 等全局辅助函数
5. 删除根目录的 `Target.lua`
</action>
  <verify>
    <automated>grep "macroTorch.classMetatable" /Users/yue.weny/finalanswer/macro-torch/macro-torch/entity/Target.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.classMetatable" entity/Target.lua` 返回匹配行
    - `grep "setmetatable(obj, {" entity/Target.lua` 无结果（手写模板已替换）
    - `grep "self.__index = self" entity/Target.lua` 无结果（已删除）
    - `test ! -f Target.lua` 返回 true（根目录文件已删除）
    - entity/Target.lua 文件约 269 行
  </acceptance_criteria>
  <done>Target.lua 已迁移到 entity/Target.lua，metatable 替换为 classMetatable(self, "TARGET_FIELD_FUNC_MAP")</done>
</task>

</tasks>

<threat_model>
本 plan 仅执行文件移动 + metatable 一行替换，不修改业务逻辑。无新威胁面。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯文件重组和 metatable 模式统一，不改变数据流或事件处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04a | 全部 | entity/Unit.lua, entity/Player.lua, entity/Target.lua | accept | 纯等价变换，semantics preserved by design |
</threat_model>

<verification>
```bash
# 文件已迁移到 entity/
test -f entity/Unit.lua && test -f entity/Player.lua && test -f entity/Target.lua
# 根目录原文件已删除
test ! -f Unit.lua && test ! -f Player.lua && test ! -f Target.lua
# 所有三个文件使用 classMetatable
grep -c "macroTorch.classMetatable" entity/Unit.lua entity/Player.lua entity/Target.lua
# 期望: 3
# 手写 metatable 模板不再存在
grep "setmetatable(obj, {" entity/Unit.lua entity/Player.lua entity/Target.lua
# 期望: 无结果
```
</verification>

<success_criteria>
- Unit.lua, Player.lua, Target.lua 三个文件已从根目录移动到 entity/ 目录
- 每个文件的手写 setmetatable + __index 模板已替换为一行 classMetatable 调用
- 所有业务逻辑保持不变
</success_criteria>

<output>
Create `.planning/phases/01-classmetatable-entity/01-04-SUMMARY.md` when done
</output>