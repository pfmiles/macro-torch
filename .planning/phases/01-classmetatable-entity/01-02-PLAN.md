---
phase: 01-classmetatable-entity
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [battle_event_queue.lua, SM_Extend_Druid.lua]
autonomous: true
requirements: [R2]
user_setup: []

must_haves:
  truths:
    - "`macroTorch.player = macroTorch.druid` 替换逻辑不再存在于 battle_event_queue.lua"
    - "PLAYER_ENTERING_WORLD 事件处理中调用 `macroTorch.initPlayer()`"
    - "SM_Extend_Druid.lua 末尾 `macroTorch.druid = macroTorch.Druid:new()` 之后追加 registerPlayerClass 调用"
  artifacts:
    - path: "battle_event_queue.lua"
      provides: "事件处理，PLAYER_ENTERING_WORLD 中接入 initPlayer()"
      min_lines: 500
    - path: "SM_Extend_Druid.lua"
      provides: "Druid 类注册到 initPlayer 注册表"
  key_links:
    - from: "battle_event_queue.lua PLAYER_ENTERING_WORLD handler"
      to: "core/class.lua → macroTorch.initPlayer()"
      via: "函数调用"
      pattern: "macroTorch%.initPlayer%(%)"
    - from: "SM_Extend_Druid.lua:271+"
      to: "core/class.lua → macroTorch.registerPlayerClass()"
      via: "函数调用"
      pattern: "macroTorch%.registerPlayerClass"
---

<objective>
删除 battle_event_queue.lua 中的 Druid 多态 hack（macroTorch.player = macroTorch.druid），在 PLAYER_ENTERING_WORLD 中改用 macroTorch.initPlayer()。同时在 SM_Extend_Druid.lua 中注册 Druid 类到 PLAYER_CLASS_REGISTRY。

Purpose: 消除运行时实例替换 hack，实现正确的多态初始化。
Output: battle_event_queue.lua（hack 已删除 + initPlayer 接入），SM_Extend_Druid.lua（Druid 类已注册）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md — project constraints
@.planning/ROADMAP.md — Phase 1.4 task details (T1.4.1, T1.4.2)
@.planning/REQUIREMENTS.md — R2 (多态初始化) 验收标准
@.planning/phases/01-classmetatable-entity/01-CONTEXT.md — D-05, D-07, D-08 locked decisions
@.planning/codebase/ARCHITECTURE.md — entry points section for PLAYER_ENTERING_WORLD context
@.planning/codebase/STRUCTURE.md — global singleton locations

<interfaces>
From Plan 01-01 outputs:
- `macroTorch.PLAYER_CLASS_REGISTRY` — table in core/class.lua
- `macroTorch.registerPlayerClass(className, classTable)` — function in core/class.lua
- `macroTorch.initPlayer()` — function in core/class.lua, returns Player or Druid instance

From battle_event_queue.lua:76-78 — code to DELETE:
```lua
if 'Druid' == macroTorch.player.class then
    macroTorch.player = macroTorch.druid
end
```

From battle_event_queue.lua:74-79 — PLAYER_ENTERING_WORLD handler context:
```lua
elseif event == 'PLAYER_ENTERING_WORLD' then
    -- on player entering world
    if 'Druid' == macroTorch.player.class then
        macroTorch.player = macroTorch.druid
    end
    macroTorch.loginContext = {}
```

From SM_Extend_Druid.lua:271 — location to add registerPlayerClass AFTER:
```lua
macroTorch.druid = macroTorch.Druid:new()
```

From CONTEXT.md D-08: Player.lua:535 `macroTorch.player = macroTorch.Player:new()` is PRESERVED as default init — do NOT touch this line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 battle_event_queue.lua 中的 Druid 多态 hack，接入 initPlayer</name>
  <files>battle_event_queue.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/battle_event_queue.lua — 全文，特别关注 lines 71-79 (eventHandle PLAYER_ENTERING_WORLD handler)
  </read_first>
  <action>
在 `battle_event_queue.lua` 的 `eventHandle()` 函数中，修改 PLAYER_ENTERING_WORLD 分支处理：

per D-07: 删除 lines 76-78 的三行 Druid 替换代码：
```lua
if 'Druid' == macroTorch.player.class then
    macroTorch.player = macroTorch.druid
end
```

per D-07: 在 `macroTorch.loginContext = {}` 之前插入一行：
```lua
macroTorch.player = macroTorch.initPlayer()
```

修改后的 PLAYER_ENTERING_WORLD 分支变为：
```lua
elseif event == 'PLAYER_ENTERING_WORLD' then
    macroTorch.player = macroTorch.initPlayer()
    macroTorch.loginContext = {}
```

注意：`macroTorch.loginContext = {}` 必须保留，initPlayer() 在它之前调用。
</action>
  <verify>
    <automated>grep "macroTorch.player = macroTorch.druid" /Users/yue.weny/finalanswer/macro-torch/macro-torch/battle_event_queue.lua; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.player = macroTorch.druid" battle_event_queue.lua` 无结果
    - `grep "macroTorch.initPlayer" battle_event_queue.lua` 返回匹配行
    - PLAYER_ENTERING_WORLD 分支中 `macroTorch.initPlayer()` 在 `macroTorch.loginContext = {}` 之前
    - 其余事件分支代码保持不变
  </acceptance_criteria>
  <done>Druid 多态 hack 已删除，PLAYER_ENTERING_WORLD 中接入 initPlayer() 调用</done>
</task>

<task type="auto">
  <name>Task 2: SM_Extend_Druid.lua 中注册 Druid 到 initPlayer 注册表</name>
  <files>SM_Extend_Druid.lua</files>
  <read_first>
- /Users/yue.weny/finalanswer/macro-torch/macro-torch/SM_Extend_Druid.lua — 仅需读取文件末尾 lines 265-275，确认 Druid singleton 初始化位置
  </read_first>
  <action>
per D-05: 在 `SM_Extend_Druid.lua` 文件末尾，`macroTorch.druid = macroTorch.Druid:new()` 行之后，新增一行注册调用：

```lua
macroTorch.registerPlayerClass("DRUID", macroTorch.Druid)
```

这行代码在 Druid singleton 初始化之后执行，确保 `macroTorch.Druid` 已存在。

per D-05 的说明：Druid 注册时使用类名 "DRUID"（UnitClass('player') 返回的职业名），classTable 传 `macroTorch.Druid`（类原型表）。

不需要修改文件中任何其他内容。这行注册代码仅当 Druid 文件被构建系统拼接时才生效。
</action>
  <verify>
    <automated>grep "registerPlayerClass" /Users/yue.weny/finalanswer/macro-torch/macro-torch/SM_Extend_Druid.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep "macroTorch.registerPlayerClass" SM_Extend_Druid.lua` 返回匹配行
    - 注册调用在 `macroTorch.druid = macroTorch.Druid:new()` 之后
    - 参数为 `("DRUID", macroTorch.Druid)`
    - 文件中其他代码未修改
  </acceptance_criteria>
  <done>Druid 类已注册到 PLAYER_CLASS_REGISTRY，Druid 登录时 initPlayer() 返回 Druid 实例</done>
</task>

</tasks>

<threat_model>
本 plan 仅修改事件处理中的初始化逻辑和一行动态注册，不引入新的事件监听、外部输入或信任边界。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW event system → PLAYER_ENTERING_WORLD handler | 事件系统传入数据，但 initPlayer() 仅调用 WoW API 查职业名，无外部数据注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02a | Tampering | PLAYER_ENTERING_WORLD handler | accept | initPlayer() 内部仅调用 UnitClass API 查表，无用户可控输入 |
| T-01-02b | Denial of Service | initPlayer fallback | accept | fallback 到 Player:new() 确保非 Druid 职业正常运行；PLAYER_CLASS_REGISTRY 为空时不会崩溃 |
</threat_model>

<verification>
```bash
# 旧 hack 已删除
grep "macroTorch.player = macroTorch.druid" battle_event_queue.lua  # 期望: 无结果

# initPlayer 已接入
grep "macroTorch.initPlayer" battle_event_queue.lua  # 期望: 有结果

# Druid 已注册
grep "macroTorch.registerPlayerClass" SM_Extend_Druid.lua  # 期望: 有结果
```
</verification>

<success_criteria>
- Druid 多态 hack 不再存在于代码库中
- PLAYER_ENTERING_WORLD 使用 initPlayer() 初始化 macroTorch.player
- Druid 类已注册到 PLAYER_CLASS_REGISTRY
</success_criteria>

<output>
Create `.planning/phases/01-classmetatable-entity/01-02-SUMMARY.md` when done
</output>