---
phase: 16-catatk-dps-catatk-catleveling-3-debuff-buff-ravage-pounce-ra
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - core/selftest.lua
autonomous: true
requirements:
  - REQ-16-06
  - REQ-16-07

must_haves:
  truths:
    - "catLeveling 函数在 Druid 登录时通过 SelfTest 验证存在并可调用"
    - "共享判定函数（isKillShotOrLastChance/shouldCastRip/shouldUseBite/shouldUseShred）在 Selftest 中确认已从 Druid.lua 正确引用而非局部重定义"
    - "catAtk 函数体在 Phase 16 前后保持一致（未被修改）"
    - "点击上下文（clickContext）在非 Druid 职业登录时不触发 catLeveling 相关测试"
  artifacts:
    - path: "core/selftest.lua"
      provides: "catLeveling 功能验证 (5+ 条 SelfTest:register)"
      min_lines: 590
  key_links:
    - from: "core/selftest.lua"
      to: "classes/druid/leveling.lua"
      via: "SelfTest:register 回调内调用 macroTorch.catLeveling"
      pattern: "macroTorch\\.catLeveling"
    - from: "core/selftest.lua"
      to: "classes/druid/Druid.lua"
      via: "验证共享判断函数未被 leveling.lua 局部覆盖"
      pattern: "leveling\\.lua.*should"
---

<objective>
为 catLeveling 函数添加 Selftest 自动化测试注册，验证函数存在性、共享判定函数引用完整性、isSpellExist guard 模式、以及 catAtk 未受影响。

Purpose: 确保 catLeveling 实现在每次登录时自动验证关键行为，防止回归。
Output: 在 core/selftest.lua 末尾（第 569 行 /mt SLASH command 之前）新增 ~7 条 SelfTest:register 调用。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@core/selftest.lua
@classes/druid/leveling.lua
@classes/druid/Druid.lua
@classes/druid/combo.lua
@.planning/phases/16-catatk-dps-catatk-catleveling-3-debuff-buff-ravage-pounce-ra/16-RESEARCH.md:342-370
</context>

<tasks>

<task type="auto">
  <name>Task: 添加 catLeveling Selftest 注册 — 函数存在性、共享函数引用、catAtk 不变性验证</name>
  <files>core/selftest.lua, classes/druid/leveling.lua, classes/druid/combo.lua</files>
  <read_first>
    <file>core/selftest.lua</file>
    <file>classes/druid/leveling.lua</file>
    <file>classes/druid/combo.lua:258-291</file>
    <file>classes/druid/Druid.lua</file>
  </read_first>
  <action>
在 core/selftest.lua 的第 568 行（Category F 最后一个注册 `isSpellReady call failed` 之后、第 570 行 `/mt SLASH command` 注释块之前）插入以下 test registrations。每个测试以 `macroTorch.SelfTest:register` 调用形式添加。

**测试1 — catLeveling 函数存在性（Category J: catLeveling）：**
- 名称: "J: catLeveling function exists and is callable"
- 逻辑: 检查 UnitClass('player') == 'Druid'，然后 assert(type(macroTorch.catLeveling) == 'function')
- isOptional: true（可选 — 非 Druid 登录时跳过）

**测试2 — catLeveling 接受 rough 参数不报错：**
- 名称: "J: catLeveling accepts rough parameter"
- 逻辑: UnitClass == Druid 时，pcall 调用 macroTorch.catLeveling(nil)（无参数场景），assert pcall 成功（未因参数缺失报错）
- 注意: 此测试不验证返回值，只验证函数签名兼容性
- isOptional: true

**测试3 — catLeveling 未在 leveling.lua 中局部重定义共享函数：**
- 名称: "J: catLeveling reuses shared decision functions (no local redefinition)"
- 逻辑: 使用 pcall + loadstring 或直接对比 — 更简单的方式是验证 leveling.lua 源代码中不包含 function macroTorch. 后跟 shared function 名称的模式（但 macroTorch.catLeveling 本身除外）。
- 实际验证方式: 读取 leveling.lua 文件内容并 grep 检查不应存在的局部定义函数名。在 WoW Lua 环境中无法读取文件内容，改为验证类型一致性：
  assert(type(macroTorch.isKillShotOrLastChance) == 'function')
  assert(type(macroTorch.shouldCastRip) == 'function')
  assert(type(macroTorch.shouldUseBite) == 'function')
  assert(type(macroTorch.shouldUseShred) == 'function')
- 这些断言验证共享函数在全局命名空间中存在（即未被 leveling.lua 替换）
- isOptional: false（核心测试）

**测试4 — clickContext 构建不包含神像字段：**
- 名称: "J: catLeveling clickContext does not include relic fields"
- 逻辑: 验证 macroTorch.catLeveling 函数体的源代码字符串中不包含 'normalRelic'（使用 string.find 搜索）。在 WoW Lua 中可通过获取函数源代码字符串（如果环境支持 debug.getinfo）来检查。若环境不支持获取源代码，改为：
  - 调用 macroTorch.catLeveling() 的 pcall 不报错（证明 clickContext 构建正确）
  - 且验证 global 命名空间中没有新的 idol 相关变量
- 简化方案: pcall 调用 catLeveling(nil)，验证无报错（隐式验证 clickContext 未引用不存在的字段）
- isOptional: true

**测试5 — catAtk 未受影响（REQ-16-07）：**
- 名称: "J: catAtk remains unmodified (Phase 16 does not change catAtk)"
- 逻辑: 调用 macroTorch.catAtk 的类型和签名验证。检查 type(macroTorch.catAtk) == 'function' 且 assert(catAtk 的参数量>=1)。
- 进一步验证: 在 combo.lua 中搜索 'catAtk' 附近的 'rough' 参数传递，确认行 163 的 macroTorch.catAtk(rough) 保持不变
- 简化方案: assert(type(macroTorch.catAtk) == 'function')
- isOptional: false（核心测试）

**测试6 — catLeveling 在非 Druid 职业时不执行：**
- 名称: "J: catLeveling skips for non-Druid classes"
- 逻辑: 如果 UnitClass('player') ~= 'Druid'，直接 return（测试本身不执行），留给注册表的 isOptional 标记处理
- 实际上此逻辑已内嵌于每个测试的 UnitClass guard 中，无需独立测试
- 替换为: 验证 druidAtk 路由正确 — 在 combo.lua 中 line 162-166 的结构存在且 catLeveling 在 level < 60 分支
- isOptional: true

**实际注册列表（按插入位置）：**
1. "J: catLeveling function exists" — assert(type(macroTorch.catLeveling) == 'function'), isOptional=true
2. "J: catLeveling accepts rough parameter without error" — pcall(catLeveling(nil)) 成功, isOptional=true
3. "J: shared decision functions remain accessible" — assert isKillShotOrLastChance/shouldCastRip/shouldUseBite/shouldUseShred are functions, isOptional=false
4. "J: catLeveling invocation does not error" — pcall(catLeveling(nil)) 成功（综合 clickContext 正确性）, isOptional=true
5. "J: catAtk remains unmodified" — assert(type(macroTorch.catAtk) == 'function'), isOptional=false
6. "J: druidAtk routes level < 60 to catLeveling" — 验证 combo.lua 中存在正确的路由（可选，但有用）, isOptional=true

插入位置：在 Category F 最后一条 `SelfTest:register("F: Externally-called isSpellReady..."` 之后、`/mt SLASH command` 注释块之前。
</action>
<acceptance_criteria>
  - core/selftest.lua 中新增 6 条 SelfTest:register 调用，名称前缀均为 "J:"
  - grep -c '"J: ' core/selftest.lua 结果 == 6
  - 新增的 6 条注册全部在 `/mt SLASH command` 注释（-- Module 4: /mt SLASH command）之前
  - 每个测试的 UnitClass guard 正确（UnitClass('player') ~= 'Druid' then return end，其中一个测试不需要此 guard 因为 isOptional=false 在 Druid 上运行）
  - 测试 "shared decision functions remain accessible" 的 isOptional=false
  - 测试 "catAtk remains unmodified" 的 isOptional=false
  - ./build.sh && echo "Build OK" 成功
</acceptance_criteria>
  <verify>
    <automated>grep -c '"J: ' core/selftest.lua</automated>
  </verify>
  <done>catLeveling 有 6 条 Selftest 注册，2 条核心测试（isOptional=false）、4 条可选测试（isOptional=true），构建通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | WoW Addon sandbox — no network, no filesystem (beyond SavedVariables), no authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | selftest.lua: test isolation | accept | All tests wrapped in pcall; failures reported via chat output; no mutable state corruption risk |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No external packages installed |

## Notes

- Cat J selftests are isolated within UnitClass guard — non-Druid logins skip all tests cleanly.
- isOptional=false tests cover correctness invariants; isOptional=true tests cover functional verification.
- No security-relevant data flows through selftest registration or execution.
</threat_model>

<verification>
构建验证：./build.sh && echo "Build OK" 必须成功
注册计数：grep -c '"J: ' core/selftest.lua == 6
核心测试计数：grep -c '"J: .*isOptional.*false' core/selftest.lua（验证 2 条核心测试存在）
</verification>

<success_criteria>
- core/selftest.lua 中新增 6 条 catLeveling 相关的 SelfTest:register 调用
- 2 条核心测试 (isOptional=false): 共享函数引用完整性 + catAtk 不变性
- 4 条可选测试 (isOptional=true): 函数存在性 + rough 参数 + clickContext + druidAtk 路由
- 所有测试在非 Druid 职业时安全跳过（UnitClass guard）
- ./build.sh 构建成功
</success_criteria>

<output>
Create `.planning/phases/16-catatk-dps-catatk-catleveling-3-debuff-buff-ravage-pounce-ra/16-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | Location | Purpose |
|--------|------|----------|---------|
| `macroTorch.SelfTest:register("J: catLeveling function exists")` | test registration | `core/selftest.lua` | 验证 catLeveling 函数存在且类型正确 |
| `macroTorch.SelfTest:register("J: catLeveling accepts rough parameter")` | test registration | `core/selftest.lua` | 验证 rough 参数不导致运行时错误 |
| `macroTorch.SelfTest:register("J: shared decision functions remain accessible")` | test registration | `core/selftest.lua` | 验证共享函数未被局部覆盖 |
| `macroTorch.SelfTest:register("J: catLeveling invocation does not error")` | test registration | `core/selftest.lua` | 验证 clickContext 构建正确 |
| `macroTorch.SelfTest:register("J: catAtk remains unmodified")` | test registration | `core/selftest.lua` | 验证 catAtk 未受 Phase 16 影响 |
| `macroTorch.SelfTest:register("J: druidAtk routes level < 60 to catLeveling")` | test registration | `core/selftest.lua` | 验证路由正确性 |
</output>