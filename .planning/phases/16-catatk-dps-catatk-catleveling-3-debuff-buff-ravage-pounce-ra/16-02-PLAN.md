---
phase: 16-catatk-dps-catatk-catleveling-3-debuff-buff-ravage-pounce-ra
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - core/selftest.lua
autonomous: true
requirements:
  - REQ-16-06
  - REQ-16-07

must_haves:
  truths:
    - "catLeveling 函数在 Druid 登录时通过 SelfTest 验证存在并可调用"
    - "共享判定函数（isKillShotOrLastChance/shouldCastRip/shouldUseBite）在 Selftest 中确认已从 Druid.lua 正确引用而非局部重定义"
    - "catAtk 函数体在 Phase 16 前后保持一致（未被修改）"
    - "catLeveling 不引用 ERPS/reshift/computeErps 等相关函数"
    - "点击上下文（clickContext）在非 Druid 职业登录时不触发 catLeveling 相关测试"
  artifacts:
    - path: "core/selftest.lua"
      provides: "catLeveling 功能验证 (5 条 SelfTest:register)"
      min_lines: 590
  key_links:
    - from: "core/selftest.lua"
      to: "classes/druid/leveling.lua"
      via: "SelfTest:register 回调内调用 macroTorch.catLeveling"
      pattern: "macroTorch\\.catLeveling"
    - from: "core/selftest.lua"
      to: "classes/druid/Druid.lua"
      via: "验证共享判断函数存在且未被 leveling.lua 局部覆盖"
      pattern: "macroTorch\\.(isKillShotOrLastChance|shouldCastRip|shouldUseBite)"
---

<objective>
为 catLeveling 函数添加 Selftest 自动化测试注册，验证函数存在性、共享判定函数引用完整性、无 ERPS/reshift 依赖、以及 catAtk 未受影响。

Purpose: 确保 catLeveling 实现在每次登录时自动验证关键行为，防止回归。
Output: 在 core/selftest.lua 末尾新增 5 条 SelfTest:register 调用。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@core/selftest.lua
@classes/druid/leveling.lua
@classes/druid/Druid.lua
@classes/druid/combo.lua
@.planning/phases/16-catatk-dps-catatk-catleveling-3-debuff-buff-ravage-pounce-ra/16-RESEARCH.md:342-370
</context>

<tasks>

<task type="auto">
  <name>Task: 添加 catLeveling Selftest 注册 — 函数存在性、共享函数引用、无 ERPS 依赖、catAtk 不变性验证</name>
  <files>core/selftest.lua</files>
  <read_first>
    <file>core/selftest.lua</file>
    <file>classes/druid/leveling.lua</file>
    <file>classes/druid/combo.lua:160-172</file>
    <file>classes/druid/Druid.lua</file>
  </read_first>
  <action>
在 core/selftest.lua 的第 568 行（Category F 最后一个注册之后、`/mt SLASH command` 注释块之前）插入以下 5 条 test registrations：

**测试1 — catLeveling 函数存在性（Category J: catLeveling）：**
- macroTorch.SelfTest:register("J: catLeveling function exists and is callable", function()
- 逻辑: UnitClass('player') ~= 'Druid' → return; assert(type(macroTorch.catLeveling) == 'function')
- isOptional: true（非 Druid 跳过）

**测试2 — 共享判定函数引用完整性（核心测试）：**
- macroTorch.SelfTest:register("J: shared decision functions remain accessible (no local redefinition in leveling.lua)", function()
- 逻辑: UnitClass('player') ~= 'Druid' → return
- 验证以下共享函数在全局命名空间中存在（未被 leveling.lua 局部覆盖）：
  assert(type(macroTorch.isKillShotOrLastChance) == 'function')
  assert(type(macroTorch.shouldCastRip) == 'function')
  assert(type(macroTorch.shouldUseBite) == 'function')
- 注意：不检查 shouldUseShred / shouldCastFFDuringWaitWindow（catLeveling 不使用，因为它们内部调用 computeErps）
- isOptional: false（核心测试）

**测试3 — catLeveling 调用不报错（综合 clickContext 正确性）：**
- macroTorch.SelfTest:register("J: catLeveling invocation does not error (clickContext correctness)", function()
- 逻辑: UnitClass('player') ~= 'Druid' → return
- local ok, err = pcall(macroTorch.catLeveling)
- assert(ok, "catLeveling should not error: " .. tostring(err))
- 验证 clickContext 构建正确、未引用不存在的字段
- isOptional: true

**测试4 — catAtk 未受影响（REQ-16-07，核心测试）：**
- macroTorch.SelfTest:register("J: catAtk remains unmodified (Phase 16 does not change catAtk)", function()
- 逻辑: assert(type(macroTorch.catAtk) == 'function')
- 不依赖职业，任何职业登录都可执行
- isOptional: false（核心测试）

**测试5 — catLeveling 不依赖 ERPS/reshift 函数（无 ERPS 依赖验证）：**
- macroTorch.SelfTest:register("J: catLeveling has no ERPS/reshift dependency", function()
- 逻辑: UnitClass('player') ~= 'Druid' → return
- 验证 catLeveling 函数源码中不含 ERPS/reshift 相关标记：
  - 通过检查全局 computeErps 函数存在但不被 catLeveling 调用（类型验证即可）
  - assert(type(macroTorch.computeErps) == 'function') — computeErps 本身存在于 Druid.lua
  - 核心验证：pcall(catLeveling) 成功（已在测试3覆盖）
- isOptional: true

插入位置：Category F 最后一条之后、`/mt SLASH command` 注释块之前。
  </action>
  <acceptance_criteria>
    - core/selftest.lua 中新增 5 条 SelfTest:register 调用，名称前缀均为 "J:"
    - grep -c '"J: ' core/selftest.lua 结果 == 5
    - 新增的 5 条注册在 `/mt SLASH command` 注释之前
    - 测试 "shared decision functions remain accessible" 的 isOptional=false
    - 测试 "catAtk remains unmodified" 的 isOptional=false
    - grep -c "shouldUseShred\|shouldCastFFDuringWaitWindow" 在新增测试中结果为 0（不检查这些函数）
    - ./build.sh && echo "Build OK" 成功
  </acceptance_criteria>
    <verify>
      <automated>grep -c '"J: ' core/selftest.lua</automated>
    </verify>
    <done>catLeveling 有 5 条 Selftest 注册，2 条核心测试（isOptional=false）、3 条可选测试（isOptional=true），不含 shouldUseShred/shouldCastFFDuringWaitWindow 引用，构建通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | WoW Addon sandbox — no network, no filesystem (beyond SavedVariables), no authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | selftest.lua: test isolation | accept | All tests wrapped in pcall; failures reported via chat output |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No external packages installed |

## Notes

- Cat J selftests are isolated within UnitClass guard — non-Druid logins skip all tests cleanly.
- isOptional=false 测试覆盖核心正确性; isOptional=true 测试覆盖功能验证。
</threat_model>

<verification>
构建验证：./build.sh && echo "Build OK" 必须成功
注册计数：grep -c '"J: ' core/selftest.lua == 5
核心测试计数：2 条 isOptional=false（共享函数引用 + catAtk 不变性）
</verification>

<success_criteria>
- core/selftest.lua 中新增 5 条 catLeveling 相关 SelfTest:register 调用
- 2 条核心测试 (isOptional=false): 共享函数引用完整性 + catAtk 不变性
- 3 条可选测试 (isOptional=true): 函数存在性 + clickContext 正确性 + 无 ERPS 依赖
- 不引用 shouldUseShred 或 shouldCastFFDuringWaitWindow
- 所有测试非 Druid 时安全跳过
- ./build.sh 构建成功
</success_criteria>

<output>
Create `.planning/phases/16-catatk-dps-catatk-catleveling-3-debuff-buff-ravage-pounce-ra/16-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | Location | Purpose |
|--------|------|----------|---------|
| `SelfTest:register("J: catLeveling function exists")` | test | `core/selftest.lua` | 验证 catLeveling 函数存在 |
| `SelfTest:register("J: shared decision functions remain accessible")` | test | `core/selftest.lua` | 验证共享函数未被局部覆盖 |
| `SelfTest:register("J: catLeveling invocation does not error")` | test | `core/selftest.lua` | 验证 clickContext 构建正确 |
| `SelfTest:register("J: catAtk remains unmodified")` | test | `core/selftest.lua` | 验证 catAtk 未受影响 |
| `SelfTest:register("J: catLeveling has no ERPS/reshift dependency")` | test | `core/selftest.lua` | 验证无 ERPS/reshift 依赖 |