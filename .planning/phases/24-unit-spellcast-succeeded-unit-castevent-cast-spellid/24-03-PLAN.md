---
phase: 24-unit-spellcast-succeeded-unit-castevent-cast-spellid
plan: "03"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - core/selftest.lua
autonomous: true
requirements: [REQ-24-SELFTEST]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Category K tests reference the new name-keyed tracingSpells and do not test current_casting_spell lifecycle"
    - "Category N tests reference the deprecated SPELL_ID_AUTO_CORRECT and resolveSpellId/lodSpellIdMap as legacy functions"
    - "All Category K and N tests pass via /mt self-test"
    - "SPELL_NAME_TO_ID static table is verified to still exist (retained for legacy compatibility)"
    - "build.sh produces a valid SM_Extend.lua"
  artifacts:
    - core/selftest.lua
  key_links:
    - "Category K: tracingSpells structure validation → name-keyed (spellName → true)"
    - "Category N: SPELL_ID_AUTO_CORRECT deprecation awareness → tests acknowledge default-false legacy status"
---

<objective>
Update selftest Category K (spellId mapping system) and Category N (SPELL_ID_AUTO_CORRECT switch) to reflect the Phase 24 refactoring. Category K tests must validate the new name-keyed tracingSpells. Category N tests must acknowledge that SPELL_ID_AUTO_CORRECT, resolveSpellId, loadSpellIdMap, and current_casting_spell are now legacy/deprecated.

Purpose: Ensure the self-test suite accurately reflects the new architecture so that /mt self-test passes without false failures.
Output: Updated core/selftest.lua with revised Category K (5 tests) and Category N (5 tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@core/selftest.lua
@core/spell_trace_core.lua
@core/spell_trace_immune.lua
@macro_torch.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Category K selftests (spellId mapping system)</name>
  <files>core/selftest.lua</files>
  <read_first>
File: core/selftest.lua, lines 614-668 (Category K: 5 tests)

Current Category K tests:
- K1 (line 620): "SPELL_NAME_TO_ID table exists with all 8 keys" — verifies static mapping (core, isOptional=false)
- K2 (line 635): "resolveSpellId function exists and resolves known spells" — verifies function + 4 spell lookups (core, false)
- K3 (line 648): "resolveSpellId returns nil for unknown spell name" — nil test (optional, true)
- K4 (line 654): "loadSpellIdMap function exists and is callable" — function existence + pcall (core, false)
- K5 (line 662): "current_casting_spell is defined" — lifecycle verification (optional, true)

The section header comment (lines 614-618) must also be updated.

After Phase 24:
- tracingSpells is now name-keyed (spellName → true), not spellId-keyed
- resolveSpellId, loadSpellIdMap, current_casting_spell are DEPRECATED but still exist
- SPELL_NAME_TO_ID still exists (static baseline table, retained)
- The self-test must verify the new tracingSpells structure
  </read_first>
  <action>
Rewrite Category K to validate the new Phase 24 architecture. Keep 5 tests total, same core/optional split. Update the section header comment.

**Replace lines 614-668** with new Category K tests:

```
-- Category K: spell trace registration system tests (5 tests, 3 core + 2 optional)
-- ============================================================
-- [CITED: 24-03-PLAN.md Task 1]
-- Phase 24: tracingSpells is now name-keyed (spellName → true).
-- Verifies name-keyed tracingSpells structure, setSpellTracing behavior,
-- and confirms legacy spellId infrastructure is retained as deprecated.

macroTorch.SelfTest:register("K: tracingSpells is a table with name-keyed entries (not spellId-keyed)", function()
    assert(type(macroTorch.tracingSpells) == "table",
        "tracingSpells is not a table, got: " .. type(macroTorch.tracingSpells))
    -- After SpellTrace:register, land-tracing spells should have entries keyed by name
    -- with value true. We verify all 4 Druid land-tracing spells.
    assert(macroTorch.tracingSpells["Pounce"] == true,
        "tracingSpells['Pounce'] should be true, got: " .. tostring(macroTorch.tracingSpells["Pounce"]))
    assert(macroTorch.tracingSpells["Rake"] == true,
        "tracingSpells['Rake'] should be true, got: " .. tostring(macroTorch.tracingSpells["Rake"]))
    assert(macroTorch.tracingSpells["Rip"] == true,
        "tracingSpells['Rip'] should be true, got: " .. tostring(macroTorch.tracingSpells["Rip"]))
    assert(macroTorch.tracingSpells["Ferocious Bite"] == true,
        "tracingSpells['Ferocious Bite'] should be true, got: " .. tostring(macroTorch.tracingSpells["Ferocious Bite"]))
    -- Verify NO spellId keys remain (all keys should be strings, not numbers)
    for k, _ in pairs(macroTorch.tracingSpells) do
        assert(type(k) == "string",
            "tracingSpells key should be string (spellName), got: " .. type(k) .. " = " .. tostring(k))
    end
end, false)

macroTorch.SelfTest:register("K: setSpellTracing accepts spellName only and sets tracingSpells[name]=true", function()
    assert(type(macroTorch.setSpellTracing) == "function",
        "setSpellTracing is not a function, got: " .. type(macroTorch.setSpellTracing))
    -- Test with a temporary entry: setSpellTracing takes only spellName (no spellGuid)
    local testName = "__SELFTEST_K2_TEMP__"
    macroTorch.setSpellTracing(testName)
    assert(macroTorch.tracingSpells[testName] == true,
        "setSpellTracing should set tracingSpells['" .. testName .. "'] = true")
    -- Cleanup
    macroTorch.tracingSpells[testName] = nil
end, false)

macroTorch.SelfTest:register("K: SPELL_NAME_TO_ID static table retained (legacy, still exists)", function()
    assert(type(macroTorch.SPELL_NAME_TO_ID) == "table",
        "SPELL_NAME_TO_ID is not a table, got: " .. type(macroTorch.SPELL_NAME_TO_ID))
    -- Verify key entries still exist (retained for legacy compatibility)
    assert(macroTorch.SPELL_NAME_TO_ID["Pounce"] == 9827, "Pounce spellId mismatch")
    assert(macroTorch.SPELL_NAME_TO_ID["Rake"] == 9904, "Rake spellId mismatch")
    assert(macroTorch.SPELL_NAME_TO_ID["Rip"] == 9896, "Rip spellId mismatch")
    assert(macroTorch.SPELL_NAME_TO_ID["Ferocious Bite"] == 31018, "Ferocious Bite spellId mismatch")
end, false)

macroTorch.SelfTest:register("K: resolveSpellId retained as deprecated function (legacy compatibility)", function()
    assert(type(macroTorch.resolveSpellId) == "function",
        "resolveSpellId is not a function (deprecated but must still exist): " .. type(macroTorch.resolveSpellId))
    -- Basic smoke test: should still resolve known spells
    assert(macroTorch.resolveSpellId("Rake") == 9904,
        "resolveSpellId('Rake') expected 9904, got: " .. tostring(macroTorch.resolveSpellId("Rake")))
    -- nil for unknown is still expected
    local result = macroTorch.resolveSpellId("NonexistentSpell_XYZ123")
    assert(result == nil,
        "resolveSpellId for unknown spell should return nil, got: " .. tostring(result))
end, true)

macroTorch.SelfTest:register("K: loadSpellIdMap retained as deprecated function (legacy compatibility)", function()
    assert(type(macroTorch.loadSpellIdMap) == "function",
        "loadSpellIdMap is not a function (deprecated but must still exist): " .. type(macroTorch.loadSpellIdMap))
    -- Must be callable without error (nil-safe with loginContext and SPELL_ID_AUTO_CORRECT guards)
    local ok, err = pcall(macroTorch.loadSpellIdMap)
    assert(ok, "loadSpellIdMap should not error: " .. tostring(err))
end, true)
```

The key changes from the old Category K:
- K1 (old): SPELL_NAME_TO_ID table verification → K3 (new): retained as "legacy, still exists"
- K2 (old): resolveSpellId function test → K4 (new): retained as "deprecated but must still exist"
- K3 (old): resolveSpellId nil test → merged into K4 (new)
- K4 (old): loadSpellIdMap function test → K5 (new): retained as "deprecated but must still exist"
- K5 (old): current_casting_spell lifecycle test → REMOVED (bridge variable no longer used)
- NEW K1: tracingSpells name-keyed structure verification
- NEW K2: setSpellTracing signature and behavior test

The `for k, _ in pairs(macroTorch.tracingSpells)` loop in K1 verifies that no numeric spellId keys remain — all keys must be strings.
  </action>
  <verify>
    <automated>grep -c "SelfTest:register.*\"K:" core/selftest.lua</automated>
    <automated>grep "tracingSpells.*spellName.*true" core/selftest.lua</automated>
    <automated>grep "setSpellTracing.*spellName" core/selftest.lua</automated>
    <automated>! grep "current_casting_spell" core/selftest.lua</automated>
    <automated>grep "SPELL_NAME_TO_ID.*legacy" core/selftest.lua</automated>
    <automated>./build.sh && echo "Build OK"</automated>
  </verify>
  <done>
- Category K has 5 tests: 3 core (K1 tracingSpells structure, K2 setSpellTracing behavior, K3 SPELL_NAME_TO_ID retained) + 2 optional (K4 resolveSpellId deprecated, K5 loadSpellIdMap deprecated)
- K1 verifies all 4 Druid land-tracing spells exist as name-keyed entries with value true, and verifies no numeric keys remain
- K2 verifies setSpellTracing accepts a single spellName parameter and sets tracingSpells[name]=true
- K3 verifies SPELL_NAME_TO_ID static table still exists (legacy compatibility)
- K4 verifies resolveSpellId still works (legacy compatibility)
- K5 verifies loadSpellIdMap still works (legacy compatibility)
- current_casting_spell is no longer referenced in any selftest
- Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Category N selftests (SPELL_ID_AUTO_CORRECT switch)</name>
  <files>core/selftest.lua</files>
  <read_first>
File: core/selftest.lua, lines 790-875 (Category N: 5 tests)

Current Category N tests:
- N1 (line 794): "SPELL_ID_AUTO_CORRECT default value is false" — default value check (optional, true)
- N2 (line 799): "resolveSpellId() returns static value when switch is false" — switch=false test (optional, true)
- N3 (line 821): "resolveSpellId() returns corrected value when switch is true and corrected value exists" — switch=true test (optional, true)
- N4 (line 857): "loadSpellIdMap() function exists and is callable without error" — existence + pcall (optional, true)
- N5 (line 865): "current_casting_spell is nil after mode='ready' _castSpell when switch is true" — lifecycle test (optional, true)

After Phase 24:
- SPELL_ID_AUTO_CORRECT is DEPRECATED (still exists, defaults to false)
- current_casting_spell bridge is removed entirely
- resolveSpellId and loadSpellIdMap are deprecated but retained
- N5 (current_casting_spell test) must be removed
  </read_first>
  <action>
Rewrite Category N to reflect the deprecated status of the spellId auto-correction system. Remove the current_casting_spell test. Keep 4 tests total, all optional. Update the section header comment.

**Replace lines 790-875** with new Category N tests:

```
-- Category N: SPELL_ID_AUTO_CORRECT deprecated switch verification (4 tests, all isOptional=true)
-- ============================================================
-- [CITED: 24-03-PLAN.md Task 2]
-- Phase 24: SPELL_ID_AUTO_CORRECT, resolveSpellId, and loadSpellIdMap are deprecated
-- but retained for legacy compatibility. current_casting_spell bridge is removed.

macroTorch.SelfTest:register("N: SPELL_ID_AUTO_CORRECT is deprecated and defaults to false", function()
    assert(macroTorch.SPELL_ID_AUTO_CORRECT == false,
        "SPELL_ID_AUTO_CORRECT should be false by default (deprecated since Phase 24), got: " .. tostring(macroTorch.SPELL_ID_AUTO_CORRECT))
    -- Verify the variable exists as a boolean (legacy compatibility)
    assert(type(macroTorch.SPELL_ID_AUTO_CORRECT) == "boolean",
        "SPELL_ID_AUTO_CORRECT should be a boolean, got: " .. type(macroTorch.SPELL_ID_AUTO_CORRECT))
end, true)

macroTorch.SelfTest:register("N: resolveSpellId returns static value when SPELL_ID_AUTO_CORRECT is false", function()
    if UnitClass('player') ~= 'Druid' then return end
    local spellName = "Rake"
    local staticId = macroTorch.SPELL_NAME_TO_ID[spellName]
    assert(staticId, "SPELL_NAME_TO_ID['Rake'] is nil")
    local ok, result = pcall(function()
        local saved = macroTorch.SPELL_ID_AUTO_CORRECT
        macroTorch.SPELL_ID_AUTO_CORRECT = false
        local ret = macroTorch.resolveSpellId(spellName)
        macroTorch.SPELL_ID_AUTO_CORRECT = saved
        return ret
    end)
    if not ok then
        macroTorch.SPELL_ID_AUTO_CORRECT = false
        assert(false, "resolveSpellId pcall failed when switch=false: " .. tostring(result))
    end
    assert(result == staticId,
        "resolveSpellId with switch=false should return static value " .. tostring(staticId) ..
        ", got: " .. tostring(result))
end, true)

macroTorch.SelfTest:register("N: resolveSpellId returns corrected value when switch is true (legacy)", function()
    if UnitClass('player') ~= 'Druid' then return end
    if not macroTorch.loginContext then return end
    local spellName = "Rake"
    local staticId = macroTorch.SPELL_NAME_TO_ID[spellName]
    assert(staticId, "SPELL_NAME_TO_ID['Rake'] is nil")
    local testValue = staticId + 10000
    local ok, result = pcall(function()
        if not macroTorch.loginContext.spellIdMap then
            macroTorch.loginContext.spellIdMap = {}
        end
        local prev = macroTorch.loginContext.spellIdMap[spellName]
        macroTorch.loginContext.spellIdMap[spellName] = testValue
        local ret = macroTorch.resolveSpellId(spellName)
        if prev then
            macroTorch.loginContext.spellIdMap[spellName] = prev
        else
            macroTorch.loginContext.spellIdMap[spellName] = nil
        end
        return ret
    end)
    if not ok then
        if macroTorch.loginContext and macroTorch.loginContext.spellIdMap then
            macroTorch.loginContext.spellIdMap[spellName] = nil
        end
        assert(false, "resolveSpellId pcall failed when switch=true: " .. tostring(result))
    end
    assert(result == testValue,
        "resolveSpellId with switch=true should return corrected value " .. tostring(testValue) ..
        ", got: " .. tostring(result))
end, true)

macroTorch.SelfTest:register("N: loadSpellIdMap no-ops when SPELL_ID_AUTO_CORRECT is false", function()
    if not macroTorch.loginContext then return end
    assert(type(macroTorch.loadSpellIdMap) == "function",
        "loadSpellIdMap is not a function (deprecated but must still exist): " .. type(macroTorch.loadSpellIdMap))
    -- With SPELL_ID_AUTO_CORRECT=false (default), calling loadSpellIdMap should be a safe no-op
    -- (it returns immediately due to the guard on line 109 of spell_trace_immune.lua)
    local ok, err = pcall(macroTorch.loadSpellIdMap)
    assert(ok, "loadSpellIdMap should not error (no-op when switch is false): " .. tostring(err))
end, true)

-- Registration count: Category N adds 4 tests (4 optional)
```

Key changes from old Category N:
- N1: Updated to "deprecated and defaults to false" language, added type check
- N2+N3: Preserved with updated comments — the legacy correction path still works when SPELL_ID_AUTO_CORRECT is true
- N4: Updated to "no-ops when SPELL_ID_AUTO_CORRECT is false" — clarifies the Phase 24 behavior
- N5 (old): current_casting_spell lifecycle test — REMOVED (bridge variable no longer exists)
- Section header updated with new citation and Phase 24 context
- Registration count comment updated: 4 tests (was 5)
  </action>
  <verify>
    <automated>grep -c "SelfTest:register.*\"N:" core/selftest.lua</automated>
    <automated>grep "deprecated.*Phase 24" core/selftest.lua</automated>
    <automated>! grep "current_casting_spell" core/selftest.lua</automated>
    <automated>grep "Category N adds 4 tests" core/selftest.lua</automated>
    <automated>./build.sh && echo "Build OK"</automated>
  </verify>
  <done>
- Category N has 4 tests (all optional): N1 SPELL_ID_AUTO_CORRECT default/type, N2 resolveSpellId switch=false, N3 resolveSpellId switch=true, N4 loadSpellIdMap no-op
- N1 verifies SPELL_ID_AUTO_CORRECT is false by default and is a boolean
- N2+N3 preserve the legacy correction path verification (works when enabled)
- N4 explicitly calls out the no-op behavior when SPELL_ID_AUTO_CORRECT is false
- Old N5 (current_casting_spell lifecycle) is removed
- No references to current_casting_spell remain in selftest.lua
- Category N comment updated: "4 tests" (was "5 tests")
- Build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Selftest updates are read-only verification; no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-05 | Tampering | selftest K1 tracingSpells key-type check | low | accept | The key-type assertion verifies all tracingSpells keys are strings; a malicious modification to inject numeric keys would be caught by the type-check loop. However, addon code runs in the WoW sandbox and cannot be tampered with by external actors. Accept. |
</threat_model>

<verification>
- [ ] Category K has exactly 5 tests (3 core + 2 optional)
- [ ] Category N has exactly 4 tests (all optional)
- [ ] No `current_casting_spell` references anywhere in selftest.lua
- [ ] tracingSpells name-keyed structure is verified in K1
- [ ] Legacy spellId infrastructure marked as deprecated/retained in tests
- [ ] `./build.sh` produces valid SM_Extend.lua
</verification>

<success_criteria>
- Category K tests validate the new name-keyed architecture
- Category N tests acknowledge deprecation but preserve legacy compatibility checks
- All old references to current_casting_spell are removed from selftest
- Self-test suite accurately reflects Phase 24 architecture
</success_criteria>

<output>
Create `.planning/phases/24-unit-spellcast-succeeded-unit-castevent-cast-spellid/24-03-SUMMARY.md` when done
</output>