---
phase: 24-unit-spellcast-succeeded-unit-castevent-cast-spellid
plan: "02"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - entity/Player.lua
  - core/spell_trace_core.lua
  - core/spell_trace_immune.lua
  - macro_torch.lua
  - core/combat_context.lua
autonomous: true
requirements: [REQ-24-CLEANUP]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Player._castSpell no longer sets current_casting_spell (the entire bridge block is removed)"
    - "SPELL_ID_AUTO_CORRECT, resolveSpellId, _spellIdMonitored, and loadSpellIdMap are marked -- DEPRECATED but not deleted"
    - "loadSpellIdMap call in combat_context.lua is gated behind SPELL_ID_AUTO_CORRECT check (no-op when false)"
    - "The -- DEPRECATED markers explain that these functions are retained only for the legacy auto-correction path"
    - "cast→land→immune chain no longer references any spellId-based data structure"
    - "build.sh produces a valid SM_Extend.lua"
  artifacts:
    - entity/Player.lua
    - core/spell_trace_core.lua
    - core/spell_trace_immune.lua
    - macro_torch.lua
    - core/combat_context.lua
  key_links:
    - "SpellTrace:register → setSpellTracing(name) — name-based registration (spellId eliminated from cast recording)"
    - "loadSpellIdMap gating → SPELL_ID_AUTO_CORRECT guard — prevents unnecessary work when disabled"
---

<objective>
Remove the `current_casting_spell` bridge variable logic from `Player._castSpell` and mark all spellId-related infrastructure as deprecated. Gate the `loadSpellIdMap()` call in combat_context.lua behind the `SPELL_ID_AUTO_CORRECT` switch.

Purpose: Since Plan 24-01 eliminated the cast recording path's dependency on spellId (now using UNIT_SPELLCAST_SUCCEEDED which provides spellName directly), the `current_casting_spell` bridge that connected `_castSpell` → `UNIT_CASTEVENT` for cast recording is no longer needed. The spellId auto-correction infrastructure is kept as deprecated legacy for SuperWoW users who may still want it.
Output: Cleaned Player.lua, deprecated annotations in 3 files, gated loadSpellIdMap call in combat_context.lua.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@entity/Player.lua
@core/spell_trace_core.lua
@core/spell_trace_immune.lua
@macro_torch.lua
@core/combat_context.lua
@core/events.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove current_casting_spell bridge from Player._castSpell</name>
  <files>entity/Player.lua</files>
  <read_first>
File: entity/Player.lua, lines 77-116 (_castSpell function, the current_casting_spell bridge block)

The bridge block starts at `if mode ~= 'ready' then` (line 85) and ends at `end` (line 116).
Inside this block:
- Lines 86-107: stale detection (within `if macroTorch.SPELL_ID_AUTO_CORRECT then`)
- Lines 108-114: whitelist guard that sets macroTorch.current_casting_spell

The entire block from line 85 (`if mode ~= 'ready' then`) through line 116 (`end` — the closing `end` for the outer if) must be removed.
  </read_first>
  <action>
Remove the entire `current_casting_spell` bridge block from `_castSpell()` in entity/Player.lua.

The block to remove spans from line 85 (`if mode ~= 'ready' then`) through line 116 (`end`). This includes:
- The stale detection logic (lines 86-107) which warned when current_casting_spell wasn't cleared
- The whitelist guard (lines 108-114) which conditionally set `macroTorch.current_casting_spell = localeNames.en`

After removal, the `_castSpell` function's structure at that point should be:
1. Locale selection (lines 43-49)
2. Readiness check (lines 52-57)
3. Distance + resource checks (lines 60-75)
4. Unified cast path: `obj.cast(spellName, rank)` + `return true` (lines 117-123)

The lines after the removal (currently 117-123: the unified cast path) simply follow the distance/resource check block without any intervening condition.

CRITICAL: Do NOT remove the comment on line 117 (`-- Unified cast path: both self-cast and target-cast use obj.cast`) or the `obj.cast(spellName, rank)` call on line 122 / `return true` on line 123. These are the actual cast execution and must remain.

After removal, the code flow is: locale selection → readiness check → distance+resource checks → obj.cast + return true. No bridge variable is set.
  </action>
  <verify>
    <automated>! grep -n 'current_casting_spell' entity/Player.lua</automated>
    <automated>grep 'obj.cast(spellName, rank)' entity/Player.lua</automated>
    <automated>./build.sh && echo "Build OK"</automated>
  </verify>
  <done>
- `_castSpell()` no longer references `current_casting_spell` anywhere
- The stale detection block and whitelist guard block are completely removed
- The unified cast path (`obj.cast(spellName, rank)` + `return true`) is preserved and executes unconditionally for non-ready modes
- Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark spellId infrastructure as deprecated; gate loadSpellIdMap call</name>
  <files>core/spell_trace_core.lua, core/spell_trace_immune.lua, macro_torch.lua, core/combat_context.lua</files>
  <read_first>
Files to read before starting:
- core/spell_trace_core.lua: lines 21-23 (_spellIdMonitored init), lines 63-73 (resolveSpellId), lines 98-111 (monitorSpellId whitelist block)
- core/spell_trace_immune.lua: lines 108-133 (loadSpellIdMap function)
- macro_torch.lua: lines 25-35 (SPELL_ID_AUTO_CORRECT definition)
- core/combat_context.lua: line 40 (loadSpellIdMap call)
  </read_first>
  <action>
Mark four spellId-related code blocks as DEPRECATED and gate the loadSpellIdMap call. Do NOT delete any code — only add deprecation markers and a guard.

**File 1: macro_torch.lua (line 25-35)**

Add a `-- DEPRECATED: spellId no longer used in cast→land→immune chain since Phase 24.` comment above the `SPELL_ID_AUTO_CORRECT` definition:
```
-- DEPRECATED: spellId no longer used in cast→land→immune chain since Phase 24.
-- Retained for legacy SuperWoW spellId auto-correction (opt-in via /run).
macroTorch.SPELL_ID_AUTO_CORRECT = false
```

**File 2: core/spell_trace_core.lua**

a) Add `-- DEPRECATED: _spellIdMonitored whitelist is no longer used since Phase 24.` above the `_spellIdMonitored` initialization block (around line 18-23):
```
-- DEPRECATED: _spellIdMonitored whitelist is no longer used since Phase 24.
-- Retained for legacy spellId auto-correction compatibility.
if not macroTorch._spellIdMonitored then
    macroTorch._spellIdMonitored = {}
end
```

b) Add `-- DEPRECATED: resolveSpellId is no longer needed for cast recording since Phase 24.` above the `resolveSpellId` function (around line 63):
```
-- DEPRECATED: resolveSpellId is no longer needed for cast recording since Phase 24.
-- Retained for legacy spellId auto-correction compatibility.
function macroTorch.resolveSpellId(spellName)
```

c) Add `-- DEPRECATED: monitorSpellId whitelist maintenance is no longer needed since Phase 24.` above the `shouldMonitor` block (around line 98):
```
    -- DEPRECATED: monitorSpellId whitelist maintenance is no longer needed since Phase 24.
    -- Retained for legacy spellId auto-correction compatibility.
    local shouldMonitor
```

**File 3: core/spell_trace_immune.lua**

Add `-- DEPRECATED: loadSpellIdMap is no longer needed since Phase 24.` above the `loadSpellIdMap` function (around line 108):
```
-- DEPRECATED: loadSpellIdMap is no longer needed since Phase 24.
-- Retained for legacy spellId auto-correction compatibility.
function macroTorch.loadSpellIdMap()
```

**File 4: core/combat_context.lua (line 40)**

Gate the `loadSpellIdMap()` call behind the `SPELL_ID_AUTO_CORRECT` switch, and add a deprecation comment:
```
    -- DEPRECATED: spellId map loading only needed for legacy auto-correction.
    -- No-op when SPELL_ID_AUTO_CORRECT is false (the default since Phase 24).
    if macroTorch.SPELL_ID_AUTO_CORRECT then
        macroTorch.loadSpellIdMap()
    end
```

This replaces the bare `macroTorch.loadSpellIdMap()` call on line 40.

**CRITICAL INVARIANT:** After marking deprecated but before removing, verify `grep -c 'recordCastTable\|tracingSpells'` across all files shows NO references to spellId-based patterns. The cast→land→immune chain must be fully name-driven.
  </action>
  <verify>
    <automated>grep 'DEPRECATED.*spellId.*Phase 24' macro_torch.lua core/spell_trace_core.lua core/spell_trace_immune.lua core/combat_context.lua | wc -l</automated>
    <automated>grep 'Retained for legacy' core/spell_trace_core.lua core/spell_trace_immune.lua macro_torch.lua | wc -l</automated>
    <automated>grep 'SPELL_ID_AUTO_CORRECT.*loadSpellIdMap' core/combat_context.lua</automated>
    <automated>./build.sh && echo "Build OK"</automated>
  </verify>
  <done>
- SPELL_ID_AUTO_CORRECT in macro_torch.lua is marked DEPRECATED with Phase 24 reference
- _spellIdMonitored init in spell_trace_core.lua is marked DEPRECATED
- resolveSpellId function in spell_trace_core.lua is marked DEPRECATED
- monitorSpellId whitelist block in spell_trace_core.lua is marked DEPRECATED
- loadSpellIdMap function in spell_trace_immune.lua is marked DEPRECATED
- loadSpellIdMap call in combat_context.lua is gated behind SPELL_ID_AUTO_CORRECT
- All deprecated code is retained (not deleted) for legacy compatibility
- Build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only removes dead code paths and adds deprecation annotations; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Denial of Service | loadSpellIdMap gate (combat_context.lua) | low | accept | When SPELL_ID_AUTO_CORRECT is false (default), loadSpellIdMap is skipped entirely — this is the intended behavior. The function's internal migration of tracingSpells keys was security-neutral and was the only side effect of calling it on login. Accept. |
</threat_model>

<verification>
- [ ] `grep 'current_casting_spell' entity/Player.lua` returns no results
- [ ] Five DEPRECATED markers exist across 4 files
- [ ] loadSpellIdMap call is gated behind SPELL_ID_AUTO_CORRECT check
- [ ] `./build.sh` produces valid SM_Extend.lua
</verification>

<success_criteria>
- Player._castSpell is free of current_casting_spell references
- All spellId infrastructure is annotated DEPRECATED (not deleted)
- loadSpellIdMap is a no-op when SPELL_ID_AUTO_CORRECT is false
- Build succeeds with clean SM_Extend.lua
</success_criteria>

<output>
Create `.planning/phases/24-unit-spellcast-succeeded-unit-castevent-cast-spellid/24-02-SUMMARY.md` when done
</output>