---
phase: 25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/hunter/Hunter.lua
autonomous: true
requirements:
  - H-01
  - H-02
  - H-09
  - H-10
  - D-09
  - D-10
  - D-11
  - D-12
  - D-20

estimate:
  tokens: 65000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Hunter.lua 包含完整的类定义框架（classMetatable + singleton + registerPlayerClass），与 Druid.lua 架构对齐
    - 25 个技能方法全部可用，10 个既有方法的远程射击 range 参数从 nil 修正为 30
    - Serpent Sting 和 Scorpid Sting 的 SpellTrace 注册包含 spellName、land=true、immune=true、debuffTexture
    - Hunter's Mark 不在 SpellTrace 注册中
    - SelfTest 注册覆盖 3 项基础设施测试 + 25 项技能方法存在性测试，全部 isOptional=true + UnitClass guard
    - 构建系统执行 `./build.sh` 成功生成 SM_Extend.lua
  artifacts:
    - classes/hunter/Hunter.lua
  key_links:
    - obj._castSpell({en=..., zh=...}, mode, range, nil, onSelf, rank) 参数正确传递到 entity/Player.lua _castSpell
    - SpellTrace:register() spellName 字段匹配技能英文名，land=true 触发 setSpellTracing
---

<objective>
将 classes/hunter/Hunter.lua 从过时的测试代码（156 行，10 技能方法，13 SelfTest）完全重写为 Druid 对齐架构（~350 行，25 技能方法，28 SelfTest，修正的 SpellTrace 注册）。

Purpose: 建立 Hunter 职业的技能方法基础层，为 combo.lua 的 5 个一键宏提供类型安全的技能释放接口。
Output: classes/hunter/Hunter.lua 完整重写文件，构建验证通过。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-CONTEXT.md
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-RESEARCH.md
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-PATTERNS.md
@classes/hunter/Hunter.lua
@classes/druid/Druid.lua
@entity/Player.lua
@core/spell_trace_core.lua
@core/selftest.lua
</context>

<tasks>

<task type="tracer">
  <name>Hunter.lua 完全重写 — 类框架 + 25 技能方法 + SpellTrace + 构建验证</name>
  <files>classes/hunter/Hunter.lua</files>
  <read_first>
    - classes/hunter/Hunter.lua — 当前文件（需完整读取，了解现有代码位置和结构）
    - classes/druid/Druid.lua — 技能方法定义模式参考（_castSpell 参数签名、Type A/Type B 分类、方法内联写法）
    - entity/Player.lua — _castSpell 六个参数的顺序和语义（localeNames, mode, range, resourceCost, onSelf, rank）
    - core/spell_trace_core.lua — SpellTrace:register() 签名确认：{spellName, land, immune, debuffTexture}
  </read_first>
  <action>
用 Write 工具完整重写 classes/hunter/Hunter.lua（不是 Edit —— 现有关键结构保留但内容全部替换）。按以下顺序组织：

**A) 版权头部（保持现有 1-16 行不变）：**
```lua
--[[ Apache 2.0 license block ]] --
---猎人专用 start---
```

**B) 类构造框架（保持现有 17-22 行模式，D-10）：**
- `macroTorch.Hunter = macroTorch.Player:new()` 不变
- `function macroTorch.Hunter:new()` — local obj + setmetatable 调用不变
- 所有 25 个技能方法放在 `function macroTorch.Hunter:new()` 内部，return obj 之前

**C) 10 个既有技能方法（修正 5 个远程技能的 range 参数，D-06/D-07/D-08 不改变方法定义）：**
保持每个方法的 locale 双表不变。对以下 5 个远程射击方法，将 range 参数从 nil 改为 30：
- `obj.arcane_shot(mode, rank)` → 第四参数改为 `30`
- `obj.multi_shot(mode, rank)` → 第四参数改为 `30`
- `obj.hunters_mark(mode, rank)` → 第四参数改为 `30`
- `obj.serpent_sting(mode, rank)` → 第四参数改为 `30`
- `obj.concussive_shot(mode, rank)` → 第四参数改为 `30`

以下 5 个方法不变（melee 无 range 检查、self-target）：
- `obj.raptor_strike(mode, rank)` — 保持 `nil`
- `obj.mongoose_bite(mode, rank)` — 保持 `nil`
- `obj.wing_clip(mode, rank)` — 保持 `nil`
- `obj.disengage(mode, rank)` — 保持 `nil`，onSelf=true
- `obj.call_pet(mode)` — 保持现有条件逻辑

**D) 15 个新技能方法（D-09），按 Type A（enemy target, onSelf=false）和 Type B（self target, onSelf=true）分组：**

Type A — enemy target, onSelf=false（7 个）：
- `obj.aimed_shot(mode, rank)` → `_castSpell({en='Aimed Shot', zh='瞄准射击'}, mode, 30, nil, false, rank)`
- `obj.scorpid_sting(mode, rank)` → `_castSpell({en='Scorpid Sting', zh='毒蝎钉刺'}, mode, 30, nil, false, rank)`
- `obj.viper_sting(mode, rank)` → `_castSpell({en='Viper Sting', zh='蝰蛇钉刺'}, mode, 30, nil, false, rank)`
- `obj.scatter_shot(mode, rank)` → `_castSpell({en='Scatter Shot', zh='驱散射击'}, mode, 15, nil, false, rank)`

Type B — self target, onSelf=true（11 个）：
- `obj.volley(mode, rank)` → `_castSpell({en='Volley', zh='乱射'}, mode, nil, nil, true, rank)`
- `obj.immolation_trap(mode, rank)` → `_castSpell({en='Immolation Trap', zh='献祭陷阱'}, mode, nil, nil, true, rank)`
- `obj.explosive_trap(mode, rank)` → `_castSpell({en='Explosive Trap', zh='爆炸陷阱'}, mode, nil, nil, true, rank)`
- `obj.freezing_trap(mode, rank)` → `_castSpell({en='Freezing Trap', zh='冰冻陷阱'}, mode, nil, nil, true, rank)`
- `obj.deterrence(mode, rank)` → `_castSpell({en='Deterrence', zh='威慑'}, mode, nil, nil, true, rank)`
- `obj.feign_death(mode, rank)` → `_castSpell({en='Feign Death', zh='假死'}, mode, nil, nil, true, rank)`
- `obj.mend_pet(mode, rank)` → `_castSpell({en='Mend Pet', zh='治疗宠物'}, mode, nil, nil, true, rank)`
- `obj.revive_pet(mode, rank)` → `_castSpell({en='Revive Pet', zh='复活宠物'}, mode, nil, nil, true, rank)`
- `obj.rapid_fire(mode, rank)` → `_castSpell({en='Rapid Fire', zh='急速射击'}, mode, nil, nil, true, rank)`
- `obj.aspect_of_the_hawk(mode, rank)` → `_castSpell({en='Aspect of the Hawk', zh='雄鹰守护'}, mode, nil, nil, true, rank)`
- `obj.aspect_of_the_monkey(mode, rank)` → `_castSpell({en='Aspect of the Monkey', zh='灵猴守护'}, mode, nil, nil, true, rank)`

**E) FIELD_FUNC_MAP（保持现有空表结构，不变）：**
```lua
macroTorch.HUNTER_FIELD_FUNC_MAP = { -- basic props -- conditional props }
```

**F) singleton + registerPlayerClass（保持现有 80-81 行，不变）：**
- `macroTorch.hunter = macroTorch.Hunter:new()`
- `macroTorch.registerPlayerClass("Hunter", macroTorch.Hunter)`

**G) SpellTrace 注册（D-11, D-12）：**
现有 Serpent Sting 注册（83-86 行）只有 `{immune=true, debuffTexture='Ability_Hunter_SniperShot'}`，按 D-11 改为：
```lua
macroTorch.SpellTrace:register('Serpent Sting', {
    spellName = 'Serpent Sting', land = true,
    immune = true, debuffTexture = 'Ability_Hunter_SniperShot'
})
```

新增 Scorpid Sting 注册（D-11）：
```lua
macroTorch.SpellTrace:register('Scorpid Sting', {
    spellName = 'Scorpid Sting', land = true,
    immune = true, debuffTexture = 'INV_Misc_QuestionMark'
})
```

不注册 Hunter's Mark（D-12 — 用户明确表示不需要）。

**H) SelfTest 基础设施测试（3 项，保持现有 92-105 行不变）：**
- "Hunter: HUNTER_FIELD_FUNC_MAP is table"
- "Hunter: singleton hunter exists"
- "Hunter: registered in PLAYER_CLASS_REGISTRY"

每个测试使用 `if UnitClass('player') ~= 'Hunter' then return end` guard + `isOptional = true`。

**I) SelfTest 技能方法存在性测试（25 项，D-20）：**
保留现有 10 项（raptor_strike, mongoose_bite, arcane_shot, multi_shot, hunters_mark, serpent_sting, wing_clip, concussive_shot, disengage, call_pet）。

新增 15 项，命名格式 `"Hunter: skill method {method_name} exists"`：
- aimed_shot, scorpid_sting, viper_sting, scatter_shot
- volley, immolation_trap, explosive_trap, freezing_trap
- deterrence, feign_death, mend_pet, revive_pet
- rapid_fire, aspect_of_the_hawk, aspect_of_the_monkey

每个测试模式：
```lua
macroTorch.SelfTest:register("Hunter: skill method aimed_shot exists", function()
    if UnitClass('player') ~= 'Hunter' then return end
    assert(type(macroTorch.hunter.aimed_shot) == "function", "aimed_shot is not a function")
end, true)
```
总计 28 SelfTest 注册（3 infrastructure + 25 skill methods）。

**J) 文件末尾无多余空行。**

**K) 构建验证：** 执行 `./build.sh`，确认 SM_Extend.lua 生成成功（退出码 0）。
</action>
  <verify>
    <automated>
      -- 验证文件存在且行数合理
      wc -l classes/hunter/Hunter.lua | awk '{if ($1 >= 300 && $1 <= 420) exit 0; else exit 1}'
      -- 验证所有 25 个技能方法函数定义
      for method in raptor_strike mongoose_bite arcane_shot multi_shot hunters_mark serpent_sting wing_clip concussive_shot disengage call_pet aimed_shot scorpid_sting viper_sting scatter_shot volley immolation_trap explosive_trap freezing_trap deterrence feign_death mend_pet revive_pet rapid_fire aspect_of_the_hawk aspect_of_the_monkey; do
        grep -q "function obj.${method}(" classes/hunter/Hunter.lua || { echo "MISSING: obj.${method}"; exit 1; }
      done
      -- 验证 5 个远程技能 range=30
      grep -q "Arcane Shot.*30" classes/hunter/Hunter.lua || { echo "MISSING: arcane_shot range=30"; exit 1; }
      grep -q "Multi-Shot.*30" classes/hunter/Hunter.lua || { echo "MISSING: multi_shot range=30"; exit 1; }
      grep -q "Hunter's Mark.*30" classes/hunter/Hunter.lua || { echo "MISSING: hunters_mark range=30"; exit 1; }
      grep -q "'Serpent Sting'.*30" classes/hunter/Hunter.lua || { echo "MISSING: serpent_sting range=30"; exit 1; }
      grep -q "'Concussive Shot'.*30" classes/hunter/Hunter.lua || { echo "MISSING: concussive_shot range=30"; exit 1; }
      -- 验证 SpellTrace 注册包含 land=true + spellName
      grep -q "spellName = 'Serpent Sting'.*land = true" classes/hunter/Hunter.lua || { echo "MISSING: Serpent Sting SpellTrace land+spellName"; exit 1; }
      grep -q "spellName = 'Scorpid Sting'.*land = true" classes/hunter/Hunter.lua || { echo "MISSING: Scorpid Sting SpellTrace"; exit 1; }
      -- 验证 Hunter's Mark 不在 SpellTrace 中
      grep -v '^#' classes/hunter/Hunter.lua | grep -c "Hunter's Mark" | awk '{if ($1 > 1) exit 1; else exit 0}' || { echo "FAIL: Hunter's Mark should NOT be in SpellTrace"; exit 1; }
      <!-- planner-discipline-allow: Hunter's Mark — 负向检查使用的技能名与 SpellTrace 注册的 spellName 字段不同，不会出现在被 grep 对象的正文中 -->
      -- 验证 SelfTest 注册数量 >= 28
      grep -c "SelfTest:register" classes/hunter/Hunter.lua | awk '{if ($1 >= 28) exit 0; else exit 1}' || { echo "FAIL: SelfTest registrations < 28"; exit 1; }
      -- 验证构建成功
      ./build.sh && echo "BUILD OK"</automated>
  </verify>
  <done>
classes/hunter/Hunter.lua 包含 25 个技能方法定义（10 既有 + 15 新增），5 个既有远程技能 range=30，SpellTrace 注册 Serpent Sting + Scorpid Sting（含 spellName/land），28 项 SelfTest，构建成功。
  </done>
</task>

<task type="auto" tdd="true">
  <name>SelfTest 完善 — 验证 25 技能方法 SelfTest 注册完整性 + guard 正确性</name>
  <files>classes/hunter/Hunter.lua</files>
  <read_first>
    - classes/hunter/Hunter.lua — Task 1 写入后的文件（需确认当前状态）
    - classes/druid/Druid.lua — SelfTest 注册顺序和命名约定参考
    - core/selftest.lua — SelfTest:register 签名确认
  </read_first>
  <behavior>
    - Test 1: grep "SelfTest:register" classes/hunter/Hunter.lua 计数 >= 28
    - Test 2: 每个 SelfTest 注册包含 `UnitClass('player') ~= 'Hunter'` guard
    - Test 3: 每个 SelfTest 注册的第三个参数为 `true`（isOptional）
    - Test 4: 所有 25 个技能方法名在 SelfTest 中出现一次
    - Test 5: infrastructure 3 项测试（FIELD_FUNC_MAP, singleton, PLAYER_CLASS_REGISTRY）在技能方法测试之前
  </behavior>
  <action>
在 Task 1 写入的 Hunter.lua 文件末尾的 SelfTest 块中，确认所有 28 项测试已按以下顺序排列：

**顺序约定（匹配 Druid 模式）：**
1. 3 项 infrastructure（FIELD_FUNC_MAP, singleton, PLAYER_CLASS_REGISTRY）
2. 25 项技能方法（按方法定义顺序：10 既有 → 15 新增）

**对每个测试逐项核实：**
- 测试名以 `"Hunter: skill method {name} exists"` 或 `"Hunter: {description}"` 开头
- 函数体首行：`if UnitClass('player') ~= 'Hunter' then return end`
- assert 为 `assert(type(macroTorch.hunter.{method_name}) == "function", "{method_name} is not a function")`
- 第三参数：`true`

**如果 Task 1 遗漏了任何测试，补充完整。确保正好 28 项。**

**不添加行为代码**——此任务仅为验证和完善 SelfTest 注册，已在 Task 1 中创建的 SelfTest 注册不可删减。构建验证：`./build.sh` 后确认退出码 0。
  </action>
  <verify>
    <automated>
      -- 计数 == 28
      COUNT=$(grep -c "SelfTest:register" classes/hunter/Hunter.lua)
      [ "$COUNT" -eq 28 ] || { echo "FAIL: expected 28 SelfTest registrations, got $COUNT"; exit 1; }
      -- 所有注册都以 isOptional=true 结尾
      grep "SelfTest:register" classes/hunter/Hunter.lua | grep -v ', true)$' | head -1 | grep -q . && { echo "FAIL: found SelfTest without isOptional=true"; exit 1; } || true
      -- 25 个技能方法各有对应的 SelfTest
      for method in raptor_strike mongoose_bite arcane_shot multi_shot hunters_mark serpent_sting wing_clip concussive_shot disengage call_pet aimed_shot scorpid_sting viper_sting scatter_shot volley immolation_trap explosive_trap freezing_trap deterrence feign_death mend_pet revive_pet rapid_fire aspect_of_the_hawk aspect_of_the_monkey; do
        grep -q "skill method ${method} exists" classes/hunter/Hunter.lua || { echo "MISSING SelfTest: ${method}"; exit 1; }
      done
      -- 构建验证
      ./build.sh && echo "BUILD OK"</automated>
  </verify>
  <done>
28 项 SelfTest 注册完整：3 基础设施 + 25 技能方法，全部 isOptional=true + UnitClass guard，命名格式 "Hunter: ..."，构建成功。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lua addon → WoW Client API | 所有技能名称通过 locale 双表从 WoW 客户端查询，无用户输入注入路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | SpellTrace:register spellName 字段 | medium | mitigate | spellName 字段值与注册 key 名一致（SpellTrace:register 内部有 guard invariant 检查，core/spell_trace_core.lua:59-62），规划时确保 spellName 匹配技能英文名 |
| T-25-02 | Denial of Service | 技能名 locale 不匹配 | medium | mitigate | 每个技能方法同时提供 en/zh 双表，SelfTest 在 Hunter 登录时验证所有 25 个方法为 function 类型 |
| T-25-03 | Information Disclosure | SpellTrace land 事件误识别 | low | accept | Serpent Sting 和 Scorpid Sting 的 SpellTrace land trace 依赖技能名的 chat 消息解析；如果两名猎人同时释放同名牌的 Serpent Sting，可能无法区分——这是 SpellTrace 框架的已知限制，D-11 设计的 land trace 已是最佳缓解 |
| T-25-SC | Tampering | npm/pip/cargo installs | N/A | N/A | 无外部包安装——本项目为 WoW Lua addon，不使用包管理器 |
</threat_model>

<verification>
构建验证：`./build.sh && echo "BUILD OK"` 成功生成 SM_Extend.lua。
符号验证：`grep -c "function obj\.\(raptor_strike\|mongoose_bite\|arcane_shot\|multi_shot\|hunters_mark\|serpent_sting\|wing_clip\|concussive_shot\|disengage\|call_pet\|aimed_shot\|scorpid_sting\|viper_sting\|scatter_shot\|volley\|immolation_trap\|explosive_trap\|freezing_trap\|deterrence\|feign_death\|mend_pet\|revive_pet\|rapid_fire\|aspect_of_the_hawk\|aspect_of_the_monkey\)" SM_Extend.lua` 返回 25。
SpellTrace 验证：`grep "SpellTrace:register.*Serpent Sting\|SpellTrace:register.*Scorpid Sting" SM_Extend.lua` 各有结果。
SelfTest 验证：`grep -c "SelfTest:register.*Hunter:" SM_Extend.lua` 返回 28。
</verification>

<success_criteria>
classes/hunter/Hunter.lua 作为唯一的 Hunter 职业类文件，包含完整的 25 个技能方法、2 个 SpellTrace 注册（land trace 启用）、28 项 SelfTest，构建成功。与 Druid.lua 架构完全对齐。
</success_criteria>

<output>
Create .planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-01-SUMMARY.md when done
</output>

## Artifacts This Phase Produces

**Modified file:**
- `classes/hunter/Hunter.lua` — 从 156 行扩展到 ~350 行

**Skill methods (25 total — 10 modified range params, 15 new):**
- Modified: `obj.raptor_strike`, `obj.mongoose_bite`, `obj.arcane_shot` (range=30), `obj.multi_shot` (range=30), `obj.hunters_mark` (range=30), `obj.serpent_sting` (range=30), `obj.wing_clip`, `obj.concussive_shot` (range=30), `obj.disengage`, `obj.call_pet`
- New: `obj.aimed_shot`, `obj.scorpid_sting`, `obj.viper_sting`, `obj.scatter_shot`, `obj.volley`, `obj.immolation_trap`, `obj.explosive_trap`, `obj.freezing_trap`, `obj.deterrence`, `obj.feign_death`, `obj.mend_pet`, `obj.revive_pet`, `obj.rapid_fire`, `obj.aspect_of_the_hawk`, `obj.aspect_of_the_monkey`

**SpellTrace registrations (1 modified, 1 new):**
- Modified: `SpellTrace:register('Serpent Sting', {spellName='Serpent Sting', land=true, immune=true, debuffTexture='Ability_Hunter_SniperShot'})`
- New: `SpellTrace:register('Scorpid Sting', {spellName='Scorpid Sting', land=true, immune=true, debuffTexture='INV_Misc_QuestionMark'})`

**SelfTest registrations (28 total — 3 existing, 10 kept, 15 new):**
- 3 infrastructure tests preserved
- 10 existing skill method tests preserved
- 15 new skill method tests added
</output>