---
phase: 25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - classes/hunter/combo.lua
autonomous: true
requirements:
  - H-03
  - H-04
  - H-05
  - H-06
  - H-07
  - H-08
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-13
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19

estimate:
  tokens: 75000
  raw_tokens: 43000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - hunterAtk() 作为单入口按 target.distance < 8 路由到 hunterAtkMelee() 或 hunterAtkRanged()
    - hunterAtkRanged() 执行 8 模块优先级链：combatUrgentHPRestore -> targetEnemy -> startAutoShoot -> burstMod(Shift) -> openerMod(Hunter's Mark) -> stingMod(Serpent/Scorpid) -> coreDPSMod(Arcane/Multi) -> otMod(Disengage)
    - hunterAtkMelee() 执行 6 模块优先级链：combatUrgentHPRestore -> targetEnemy -> startAutoAtk -> burstMod(Shift) -> coreMeleeMod(Raptor/Mongoose) -> otMod(Disengage)
    - startAutoShoot() 在 hunterAtkRanged() 中每键调用，startAutoAtk() 在 hunterAtkMelee() 中每键调用
    - Aimed Shot 仅在 burstMod 中通过 IsShiftKeyDown() 触发
    - hunterAtk 模块链中不包含陷阱、守护、宠物逻辑
    - hunterAoe() 距离路由：远程 Multi-Shot→Volley，近战 Explosive Trap→Immolation Trap
    - hunterDefend() 仅检查 Deterrence 并释放
    - hunterControl() 距离路由：近战 Wing Clip/Freezing Trap，远程 Concussive Shot/Scatter Shot
    - hunterMobTagging() 含 PvP 过滤（选到玩家 ClearTarget）+ 距离路由 tag + tag 成功后自动衔接 hunterAtk()
  artifacts:
    - classes/hunter/combo.lua
  key_links:
    - hunterAtk() → hunterAtkRanged() / hunterAtkMelee() 距离路由分支
    - hunterMobTagging() → hunterAtk() 自动衔接链
    - 所有 macroTorch.hunter.xxx() 技能方法调用通过 hunter singleton 实例
---

<objective>
创建 classes/hunter/combo.lua（~350 行），包含 5 个 Hunter 一键宏函数：hunterAtk（练级输出，含远程/近战距离路由）、hunterAoe（范围攻击）、hunterDefend（保命—仅 Deterrence）、hunterControl（控制）、hunterMobTagging（抢怪，含远程/近战）。

Purpose: 为 Hunter 职业提供与 Druid combo.lua 架构对齐的一键宏系统，Hunter 以"距离路由"替代 Druid 的"形态路由"。
Output: classes/hunter/combo.lua 完整文件，构建验证通过。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-CONTEXT.md
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-RESEARCH.md
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-PATTERNS.md
@classes/druid/combo.lua
@classes/hunter/Hunter.lua
@entity/Player.lua
@entity/Unit.lua
</context>

<tasks>

<task type="tracer">
  <name>combo.lua 核心架构 — hunterAtk 距离路由 + hunterAtkRanged 模块链 + hunterAtkMelee + hunterDefend</name>
  <files>classes/hunter/combo.lua</files>
  <read_first>
    - classes/druid/combo.lua — druidAtk 形态路由模式、catAtk 模块优先级链模式、druidDefend 简版模式（D-14 的 hunterDefend 以 Barkskin-check 为参照）
    - classes/hunter/Hunter.lua — 25 个技能方法的 obj.xxx() 签名（确认方法名、参数个数）
    - entity/Player.lua — targetEnemy()、startAutoShoot()、startAutoAtk() 实现（确认全局可调用性）
    - entity/Unit.lua — isCanAttack、isPlayerControlled、isAttackingMe、distance（确认字段存在）
  </read_first>
  <action>
用 Write 工具创建 classes/hunter/combo.lua。按以下顺序组织（遵循 Druid combo.lua 模式 — 参见 PATTERNS.md §2）：

**A) 文件头部注释：**
```lua
-- Hunter one-button combo macro methods (routing layer)
```

**B) hunterAtk() 入口路由（D-01, D-03）：**
按 target.distance < 8 路由到近战/远程分支，参照 druidAtk 形态路由模式（classes/druid/combo.lua:183-195）：
```lua
function macroTorch.hunterAtk()
    if macroTorch.target.distance < 8 then
        return macroTorch.hunterAtkMelee()
    else
        return macroTorch.hunterAtkRanged()
    end
end
```

**C) hunterAtkRanged() 8 模块优先级链（D-02, D-04, D-05, D-06, D-07, D-08）：**
参照 catAtk 模块链模式（classes/druid/combo.lua:49-181），但无能量/连击点/形态逻辑。

clickContext 缓存字段（单键新建表，避免重复 WoW API 调用）：
- `PLAYER_URGENT_HP_THRESHOLD = 15`（生存急救血量百分比阈值）
- `isTargetDummy = target.isCanAttack and string.find(target.name, 'Training Dummy')`

模块链（按优先级顺序，第一个成功动作 return）：

**模块 1 — combatUrgentHPRestore：** 复用 `macroTorch.combatUrgentHPRestore(clickContext)`（Druid.lua 全局函数），仅在 `macroTorch.isFightStarted(clickContext)` 后执行。

**模块 2 — targetEnemy：** `if not target.isCanAttack then player.targetEnemy(); if not target.isCanAttack then return end end`

**模块 3 — startAutoShoot（D-04）：** 每键调用 `player.startAutoShoot()`。放在 targetEnemy 之后确保有目标。

**模块 4 — burstMod（D-05）：** Shift 触发 Aimed Shot + Rapid Fire：
- `if IsShiftKeyDown() then`
- 使用 flags 模式（参照 Druid burstMod, cat.lua:2-44）：先设置 macroTorch.context.burstFlags，再次 Shift 时消费
- flag 消耗顺序：Rapid Fire（先开爆发加速）→ Aimed Shot（大招射击）
- `Rapid Fire` 检查：`isSpellExist + isSpellReady + not buffed(nil, 'Ability_Hunter_RunningShot')`
- `Aimed Shot` 检查：`isSpellExist`
- 每个 flag 消费后 `return`
- 所有 flags 消费完毕设置 `macroTorch.context.burstFlags = nil`

**模块 5 — openerMod：** 战斗开始后，如果目标没有 Hunter's Mark debuff 则释放：
- gate: `macroTorch.isFightStarted(clickContext)`
- 技能存在性: `isSpellExist("Hunter's Mark", 'spell')`
- debuff 检查: `not target.buffed("Hunter's Mark", 'Ability_Hunter_SniperShot')`
- 调用: `player.hunters_mark('ready')` + `return`

**模块 6 — stingMod：** 战斗开始后，按优先级维持钉刺 debuff：
- gate: `macroTorch.isFightStarted(clickContext)`
- Serpent Sting 优先（伤害钉刺）: `isSpellExist + not target.buffed('Serpent Sting', 'Ability_Hunter_SniperShot')` → `player.serpent_sting()` + `return`
- Scorpid Sting 次之（debuff 钉刺）: `isSpellExist + not target.buffed('Scorpid Sting', 'INV_Misc_QuestionMark')` → `player.scorpid_sting()` + `return`
- 注意：`buffed(textureName)` 调用使用 debuffTexture 字符串，非 spellName

**模块 7 — coreDPSMod：** 战斗开始后，按优先级释放伤害技能：
- gate: `macroTorch.isFightStarted(clickContext)`
- Arcane Shot 优先: `isSpellExist('Arcane Shot', 'spell')` → `player.arcane_shot()` + `return`
- Multi-Shot 次之: `isSpellExist('Multi-Shot', 'spell')` → `player.multi_shot()` + `return`
- 技能使用默认 mode（nil = safe，包含 readiness + range 检查）

**模块 8 — otMod：** Disengage 降仇恨（参照 Druid otMod 模式，cat.lua:62-111）：
- gate: `macroTorch.isFightStarted(clickContext) and not clickContext.isTargetDummy`
- 技能检查: `isSpellExist('Disengage', 'spell') and player.isSpellReady('Disengage')`
- 触发条件: `target.isAttackingMe and (player.isInGroup or player.isInRaid)`
- 调用: `player.disengage('ready')`

**重要约束验证（D-06/D-07/D-08）：**
- 模块链中不出现任何陷阱方法（immolation_trap, explosive_trap, freezing_trap）
- 不出现任何守护方法（aspect_of_the_hawk, aspect_of_the_monkey）
- 不出现任何宠物方法（pet.attack, mend_pet, revive_pet, call_pet）

**D) hunterAtkMelee() 6 模块优先级链（D-03, D-06, D-07, D-08）：**
简化的近战链。clickContext 同 ranged。

模块 1: combatUrgentHPRestore — 同 ranged
模块 2: targetEnemy — 同 ranged
模块 3: startAutoAtk（非 startAutoShoot）— `player.startAutoAtk()`，仅在 `isFightStarted(clickContext)` 后
模块 4: burstMod — 简化版（仅 Rapid Fire, Shift-gated, 无 Aimed Shot — 近战无法用瞄准射击）
模块 5: coreMeleeMod — Raptor Strike 优先 → Mongoose Bite 次之，gate: `isFightStarted(clickContext)`
模块 6: otMod — 同 ranged 的 Disengage 逻辑

**E) hunterDefend()（D-14）：**
参照 druidDefend 单检查模式（combo.lua:260-277），仅 Deterrence：
```lua
function macroTorch.hunterDefend()
    if macroTorch.isSpellExist('Deterrence', 'spell')
            and macroTorch.player.isSpellReady('Deterrence') then
        macroTorch.player.deterrence('ready')
    end
end
```
不包含 Feign Death、Disengage、守护切换。

**F) SelfTest 注册（2 项，参照 Druid combo.lua:414-457）：**
```lua
macroTorch.SelfTest:register("Hunter: combo methods -- hunterAtk exists", function()
    if UnitClass('player') ~= 'Hunter' then return end
    assert(type(macroTorch.hunterAtk) == "function", "hunterAtk not a function")
end, true)
```
为 hunterAtk 和 hunterDefend 各注册一项。

**G) 构建验证：** 执行 `./build.sh`，确认 SM_Extend.lua 生成成功。
</action>
  <verify>
    <automated>
      -- 验证文件存在
      test -f classes/hunter/combo.lua || { echo "FAIL: combo.lua not found"; exit 1; }
      -- 验证核心函数定义
      grep -q "function macroTorch.hunterAtk()" classes/hunter/combo.lua || { echo "MISSING: hunterAtk"; exit 1; }
      grep -q "function macroTorch.hunterAtkRanged()" classes/hunter/combo.lua || { echo "MISSING: hunterAtkRanged"; exit 1; }
      grep -q "function macroTorch.hunterAtkMelee()" classes/hunter/combo.lua || { echo "MISSING: hunterAtkMelee"; exit 1; }
      grep -q "function macroTorch.hunterDefend()" classes/hunter/combo.lua || { echo "MISSING: hunterDefend"; exit 1; }
      -- 验证距离路由
      grep -q "distance < 8" classes/hunter/combo.lua || { echo "MISSING: distance routing"; exit 1; }
      -- 验证 Auto Shoot 每键调用
      grep -q "startAutoShoot" classes/hunter/combo.lua || { echo "MISSING: startAutoShoot"; exit 1; }
      -- 验证 Shift 触发 Aimed Shot
      grep -q "IsShiftKeyDown" classes/hunter/combo.lua || { echo "MISSING: IsShiftKeyDown"; exit 1; }
      grep -q "Aimed Shot" classes/hunter/combo.lua || { echo "MISSING: Aimed Shot in burstMod"; exit 1; }
      <!-- planner-discipline-allow: Aimed Shot — Skill name used in burstMod isSpellExist check per D-05, not in an acceptance_criteria grep -->
      -- 验证 Deterrence 在 hunterDefend 中
      grep -q "Deterrence" classes/hunter/combo.lua || { echo "MISSING: Deterrence in hunterDefend"; exit 1; }
      -- 验证不含 Aspect/Pet/Trap（D-06/D-07/D-08 负向检查 — 在 hunterAtkRanged/hunterAtkMelee 中不应出现）
      grep -n "function macroTorch.hunterAtkRanged" classes/hunter/combo.lua | head -1 | read START_LINE
      grep -n "function macroTorch.hunterAtkMelee" classes/hunter/combo.lua | head -1 | read END_LINE
      sed -n "${START_LINE},$ p" classes/hunter/combo.lua | grep -c "aspect_\|Aspect\|pet\.\|call_pet\|mend_pet\|revive_pet" | awk '{if ($1 > 0) exit 1; else exit 0}' || { echo "FAIL: aspect/pet logic found in hunterAtk modules (violates D-07/D-08)"; exit 1; }
      <!-- planner-discipline-allow: aspect_ — Negative grep for prohibited symbols in hunterAtk, not a positive criterion -->
      <!-- planner-discipline-allow: pet\. — Negative grep for prohibited pet method calls in hunterAtk per D-08 -->
      -- 验证 SelfTest 注册
      grep -c "SelfTest:register" classes/hunter/combo.lua | awk '{if ($1 >= 2) exit 0; else exit 1}' || { echo "FAIL: SelfTest registrations < 2"; exit 1; }
      -- 构建验证
      ./build.sh && echo "BUILD OK"</automated>
  </verify>
  <done>
combo.lua 包含 hunterAtk()（距离路由入口）、hunterAtkRanged()（8 模块链，含 burstMod/openerMod/stingMod/coreDPSMod/otMod）、hunterAtkMelee()（6 模块链）、hunterDefend()（Deterrence 单检查）。不含 Aspect/Pet/Trap 逻辑。2 项 SelfTest。构建成功。
  </done>
</task>

<task type="auto" tdd="true">
  <name>combo.lua 扩展 — hunterAoe + hunterControl + hunterMobTagging + 完整 SelfTest</name>
  <files>classes/hunter/combo.lua</files>
  <read_first>
    - classes/hunter/combo.lua — Task 1 写入后的文件（确认当前结构，在 hunterDefend 之后追加）
    - classes/druid/combo.lua — druidMobTagging PvP 过滤 + tag + auto-chain 模式（combo.lua:343-412）、druidAoe 距离路由（combo.lua:197-206）、druidControl 距离路由（combo.lua:279-309）
    - entity/Unit.lua — isAttackingMe、isPlayerControlled 字段确认
  </read_first>
  <behavior>
    - Test 1: hunterAoe() 距离路由：<8yd 调用 explosive_trap/immolation_trap，≥8yd 调用 multi_shot/volley
    - Test 2: hunterControl() 距离路由：<8yd 调用 wing_clip/freezing_trap，≥8yd 调用 concussive_shot/scatter_shot
    - Test 3: hunterMobTagging() PvP 过滤：选到玩家→ClearTarget
    - Test 4: hunterMobTagging() 距离 tag：<8yd Wing Clip，≥8yd Arcane Shot rank=1
    - Test 5: hunterMobTagging() auto-chain：tag 成功后 isAttackingMe → 调用 hunterAtk()
    - Test 6: 5 项 SelfTest 完整（hunterAtk, hunterAoe, hunterDefend, hunterControl, hunterMobTagging）
  </behavior>
  <action>
在 Task 1 写入的 classes/hunter/combo.lua 文件中，在 hunterDefend() 函数之后追加以下 3 个函数。使用 Edit 工具追加，不修改已有内容。

**A) hunterAoe()（D-13, ~20 行，参照 druidAoe 模式）：**
```lua
function macroTorch.hunterAoe()
    if not macroTorch.target.isCanAttack then
        macroTorch.player.targetEnemy()
        if not macroTorch.target.isCanAttack then return end
    end

    if macroTorch.target.distance < 8 then
        -- Melee AoE: Explosive Trap -> Immolation Trap
        if macroTorch.isSpellExist('Explosive Trap', 'spell')
                and macroTorch.player.isSpellReady('Explosive Trap') then
            macroTorch.player.explosive_trap('ready')
        elseif macroTorch.isSpellExist('Immolation Trap', 'spell')
                and macroTorch.player.isSpellReady('Immolation Trap') then
            macroTorch.player.immolation_trap('ready')
        end
    else
        -- Ranged AoE: Multi-Shot -> Volley
        macroTorch.player.startAutoShoot()
        if macroTorch.isSpellExist('Multi-Shot', 'spell') then
            macroTorch.player.multi_shot('ready')
        end
        if macroTorch.isSpellExist('Volley', 'spell')
                and macroTorch.player.isSpellReady('Volley') then
            macroTorch.player.volley('ready')
        end
    end
end
```

**B) hunterControl()（D-15, ~30 行，参照 druidControl 距离分支模式）：**
```lua
function macroTorch.hunterControl()
    local target = macroTorch.target
    if not target.isCanAttack then
        macroTorch.player.targetEnemy()
        if not target.isCanAttack then return end
    end

    if target.distance < 8 then
        -- Melee control: Wing Clip (slow + damage) -> Freezing Trap (freeze)
        if macroTorch.isSpellExist('Wing Clip', 'spell') then
            macroTorch.player.wing_clip('ready')
        end
        if macroTorch.isSpellExist('Freezing Trap', 'spell')
                and macroTorch.player.isSpellReady('Freezing Trap') then
            macroTorch.player.freezing_trap('ready')
        end
    else
        -- Ranged control: Concussive Shot (slow) -> Scatter Shot (confuse, talent-gated)
        if macroTorch.isSpellExist('Concussive Shot', 'spell') then
            macroTorch.player.concussive_shot('ready')
        end
        if macroTorch.isSpellExist('Scatter Shot', 'spell') then
            macroTorch.player.scatter_shot('ready')
        end
    end
end
```

**C) hunterMobTagging()（D-16, D-17, D-18, D-19, ~60 行，参照 druidMobTagging 模式）：**
```lua
function macroTorch.hunterMobTagging()
    local player = macroTorch.player
    local target = macroTorch.target

    -- PvP filter (D-18): skip player targets, refer to druidMobTagging double-check pattern
    if not target.isCanAttack or target.isPlayerControlled then
        player.targetEnemy()
        if target.isCanAttack and target.isPlayerControlled then
            ClearTarget()
        end
        return
    end

    if target.distance < 8 then
        -- Melee tag: Wing Clip per D-17 (instant melee damage + slow)
        if macroTorch.isSpellExist('Wing Clip', 'spell') then
            player.wing_clip('ready')
        end
        player.startAutoAtk()
    else
        -- Ranged tag: Arcane Shot rank 1 per D-16 (instant, 30yd, lowest mana)
        player.startAutoShoot()
        if macroTorch.isSpellExist('Arcane Shot', 'spell') then
            player.arcane_shot('ready', 1)  -- rank=1, lowest mana cost
        end
    end

    -- Auto-chain to hunterAtk if tag confirmed per D-19
    if target.isAttackingMe then
        macroTorch.hunterAtk()
    end
end
```

**D) SelfTest 扩展 — 在文件末尾已有 2 项基础上追加 3 项，达成完整 5 项：**
```lua
macroTorch.SelfTest:register("Hunter: combo methods -- hunterAoe exists", function()
    if UnitClass('player') ~= 'Hunter' then return end
    assert(type(macroTorch.hunterAoe) == "function", "hunterAoe not a function")
end, true)

macroTorch.SelfTest:register("Hunter: combo methods -- hunterControl exists", function()
    if UnitClass('player') ~= 'Hunter' then return end
    assert(type(macroTorch.hunterControl) == "function", "hunterControl not a function")
end, true)

macroTorch.SelfTest:register("Hunter: combo methods -- hunterMobTagging exists", function()
    if UnitClass('player') ~= 'Hunter' then return end
    assert(type(macroTorch.hunterMobTagging) == "function", "hunterMobTagging not a function")
end, true)
```
总计 5 项 SelfTest（hunterAtk + hunterAoe + hunterDefend + hunterControl + hunterMobTagging），全部 isOptional=true + UnitClass guard。

**E) 构建验证：** `./build.sh` 确认退出码 0。
</action>
  <verify>
    <automated>
      -- 验证所有 5 个公共宏函数定义
      for fn in hunterAtk hunterAoe hunterDefend hunterControl hunterMobTagging; do
        grep -q "function macroTorch.${fn}()" classes/hunter/combo.lua || { echo "MISSING: ${fn}"; exit 1; }
      done
      -- 验证 hunterMobTagging 包含 PvP 过滤
      grep -q "ClearTarget" classes/hunter/combo.lua || { echo "MISSING: PvP filter ClearTarget"; exit 1; }
      grep -q "isPlayerControlled" classes/hunter/combo.lua || { echo "MISSING: isPlayerControlled check"; exit 1; }
      -- 验证 hunterMobTagging 包含 Arcane Shot rank 1
      grep -q "arcane_shot.*1" classes/hunter/combo.lua || { echo "MISSING: Arcane Shot rank 1"; exit 1; }
      <!-- planner-discipline-allow: arcane_shot.*1 — Rank=1 call in hunterMobTagging per D-16, checking comma+1 to avoid false match on argument count -->
      -- 验证 auto-chain
      grep -q "isAttackingMe" classes/hunter/combo.lua || { echo "MISSING: isAttackingMe auto-chain"; exit 1; }
      -- 验证 SelfTest 精确 5 项
      COUNT=$(grep -c "SelfTest:register" classes/hunter/combo.lua)
      [ "$COUNT" -eq 5 ] || { echo "FAIL: expected 5 SelfTest, got $COUNT"; exit 1; }
      -- 验证所有 SelfTest 带 isOptional=true
      grep "SelfTest:register" classes/hunter/combo.lua | grep -v ', true)$' | head -1 | grep -q . && { echo "FAIL: SelfTest without isOptional=true"; exit 1; } || true
      -- 构建验证
      ./build.sh && echo "BUILD OK"</automated>
  </verify>
  <done>
combo.lua 包含全部 5 个 Hunter 一键宏函数 + 5 项 SelfTest。hunterAoe 距离路由（陷阱/多重+乱射），hunterControl 距离路由（摔绊+冰冻陷阱/震荡+驱散），hunterMobTagging PvP 过滤 + 距离 tag + 自动衔接。构建成功。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combo.lua → WoW Target API | 距离判断依赖 `target.distance`（Unit.lua FIELD_FUNC_MAP），PvP 过滤依赖 `target.isPlayerControlled` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-04 | Information Disclosure | hunterMobTagging PvP 过滤 | medium | mitigate | D-18 要求：检查 target.isPlayerControlled 后 ClearTarget()；参照 druidMobTagging 二次确认模式；即使过滤失败也只是选中玩家，不会自动攻击 |
| T-25-05 | Denial of Service | hunterAoe 陷阱 CD 并发 | low | accept | 陷阱技能使用 isSpellReady() 检查，失败时 fallback 到下一个选项；陷阱 CD 是 WoW 客户端固有机制 |
| T-25-06 | Elevation of Privilege | burstMod 状态残留 | low | mitigate | burstMod 使用 macroTorch.context.burstFlags 模式，所有 flags 消费完毕后设为 nil；Shift 键释放则重新设置 flags |
| T-25-07 | Repudiation | hunterControl 对玩家释放控制技能 | medium | mitigate | hunterControl 以 target.isCanAttack 为 gate（entity/Unit.lua:186 的定义包含 UnitCanAttack 检查，对同阵营玩家返回 false）；即使选中玩家也不会释放控制技能 |
| T-25-SC | Tampering | npm/pip/cargo installs | N/A | N/A | 无外部包安装 |
</threat_model>

<verification>
构建验证：`./build.sh && echo "BUILD OK"` 成功。
符号验证（构建产物中）：`grep -c "function macroTorch\.\(hunterAtk\|hunterAoe\|hunterDefend\|hunterControl\|hunterMobTagging\)\b" SM_Extend.lua` 返回 5。
模块完整性：hunterAtkRanged 包含 8 个模块（combatUrgentHPRestore, targetEnemy, startAutoShoot, burstMod, openerMod, stingMod, coreDPSMod, otMod）。
D-06/D-07/D-08 合规：hunterAtkRanged/hunterAtkMelee 中无 Aspect/Pet/Trap 方法调用。
</verification>

<success_criteria>
classes/hunter/combo.lua 包含 5 个 Hunter 一键宏公共函数，按 Druid combo.lua 架构对齐。距离路由替代形态路由。hunterAtk 12 模块优先级链（8 ranged + 6 melee，含共享模块）。构建成功，5 项 SelfTest 注册完整。
</success_criteria>

<output>
Create .planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-02-SUMMARY.md when done
</output>

## Artifacts This Phase Produces

**Created file:**
- `classes/hunter/combo.lua` — new, ~350 lines

**Public macro functions (5):**
- `macroTorch.hunterAtk()` — 距离路由入口（<8yd → hunterAtkMelee(), ≥8yd → hunterAtkRanged()）
- `macroTorch.hunterAoe()` — 范围攻击（远程 Multi-Shot→Volley，近战 Explosive Trap→Immolation Trap）
- `macroTorch.hunterDefend()` — 保命减伤（仅 Deterrence）
- `macroTorch.hunterControl()` — 控制目标（近战 Wing Clip/Freezing Trap，远程 Concussive Shot/Scatter Shot）
- `macroTorch.hunterMobTagging()` — 抢怪（PvP 过滤 + 远程 Arcane Shot R1 + 近战 Wing Clip + 自动衔接 hunterAtk）

**Internal helper functions (2):**
- `macroTorch.hunterAtkRanged()` — 远程 8 模块优先级链
- `macroTorch.hunterAtkMelee()` — 近战 6 模块优先级链

**SelfTest registrations (5):**
- 5 × `SelfTest:register("Hunter: combo methods -- ...", ..., true)` covering all 5 public functions
</output>