---
phase: 25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol
plan: 03
type: execute
wave: 3
depends_on:
  - 25-02
files_modified:
  - build_order.txt
  - classes/hunter/combat.lua
  - classes/hunter/utility.lua
autonomous: true
requirements:
  - H-11
  - H-12

estimate:
  tokens: 16000
  raw_tokens: 9000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - build_order.txt 包含 classes/hunter/combo.lua（在 classes/hunter/Hunter.lua 之后）
    - build_order.txt 不再包含 classes/hunter/combat.lua 和 classes/hunter/utility.lua
    - classes/hunter/combat.lua 和 classes/hunter/utility.lua 文件已删除
    - ./build.sh 成功生成 SM_Extend.lua
    - SM_Extend.lua 中包含 hunterAtk/hunterAoe/hunterDefend/hunterControl/hunterMobTagging 全部 5 个函数
    - SM_Extend.lua 中不再包含旧 hunterSting/hunterCtrl/htOtMod 等废弃函数引用
  artifacts:
    - build_order.txt (modified)
  key_links:
    - build_order.txt classes/hunter/combo.lua 行位置：紧跟 classes/hunter/Hunter.lua 之后
---

<objective>
更新 build_order.txt 以纳入新的 classes/hunter/combo.lua，移除废弃的 classes/hunter/combat.lua 和 classes/hunter/utility.lua，删除这两个旧文件，最终构建验证。

Purpose: 完成 Phase 25 文件结构重组，确保构建系统反映新的 Druid 对齐架构。
Output: 更新后的 build_order.txt，删除 2 个废弃文件，构建成功验证。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@build_order.txt
@.planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>build_order.txt 更新 — 添加 combo.lua + 移除 combat.lua/utility.lua</name>
  <files>build_order.txt</files>
  <read_first>
    - build_order.txt — 当前完整文件（确认 lines 36-39 的 Hunter 条目精确内容和行号）
  </read_first>
  <action>
使用 Edit 工具修改 build_order.txt，将当前 Hunter 块（lines 36-39）：

```
# classes/ — Hunter (Phase 8)
classes/hunter/Hunter.lua
classes/hunter/combat.lua
classes/hunter/utility.lua
```

替换为：

```
# classes/ — Hunter (Phase 8 / Phase 25 combo refactor)
classes/hunter/Hunter.lua
classes/hunter/combo.lua
```

具体规则：
- `classes/hunter/Hunter.lua` 行保留不动
- `classes/hunter/combat.lua` 行删除
- `classes/hunter/utility.lua` 行删除
- 新增 `classes/hunter/combo.lua` 行，插入在 `classes/hunter/Hunter.lua` 之后
- 注释行更新为 `# classes/ — Hunter (Phase 8 / Phase 25 combo refactor)` 以记录变更历史

使用一次 Edit 调用完成全部替换（old_string 为当前 4 行块，new_string 为目标 3 行块）。
</action>
  <verify>
    <automated>
      -- 验证 combo.lua 条目存在且在 Hunter.lua 之后
      grep -n "classes/hunter/combo.lua" build_order.txt | head -1 | read COMBO_LINE
      grep -n "classes/hunter/Hunter.lua" build_order.txt | head -1 | read HUNTER_LINE
      [ "${COMBO_LINE%:*}" -gt "${HUNTER_LINE%:*}" ] || { echo "FAIL: combo.lua not after Hunter.lua"; exit 1; }
      -- 验证旧文件条目已删除
      grep -q "classes/hunter/combat.lua" build_order.txt && { echo "FAIL: combat.lua still in build_order.txt"; exit 1; } || true
      grep -q "classes/hunter/utility.lua" build_order.txt && { echo "FAIL: utility.lua still in build_order.txt"; exit 1; } || true</automated>
  </verify>
  <done>
build_order.txt 包含 classes/hunter/Hunter.lua + classes/hunter/combo.lua，不含 combat.lua/utility.lua。
  </done>
</task>

<task type="auto">
  <name>删除废弃文件 + 构建验证</name>
  <files>classes/hunter/combat.lua, classes/hunter/utility.lua</files>
  <read_first>
    - classes/hunter/combat.lua — 确认文件存在（待删除）
    - classes/hunter/utility.lua — 确认文件存在（待删除）
    - build_order.txt — 确认 Task 1 更新已生效
  </read_first>
  <action>
**A) 删除废弃文件：**
- 用 Bash: `rm classes/hunter/combat.lua classes/hunter/utility.lua`

**B) 构建验证：**
- 执行 `./build.sh`，确认退出码 0
- 验证 SM_Extend.lua 产物中存在所有 5 个 Hunter 一键宏函数：
  ```bash
  grep -c "function macroTorch\.\(hunterAtk\|hunterAoe\|hunterDefend\|hunterControl\|hunterMobTagging\)\b" SM_Extend.lua
  ```
  期望返回 5

- 验证旧符号不再出现：
  ```bash
  grep "hunterSting\|hunterCtrl\|htOtMod" SM_Extend.lua
  ```
  期望无结果（旧 combat.lua/utility.lua 的废弃函数不再被构建）

- 验证 Hunter.lua 完整（25 个技能方法在产物中）：
  ```bash
  grep -c "function obj\.\(raptor_strike\|mongoose_bite\|arcane_shot\|multi_shot\|hunters_mark\|serpent_sting\|wing_clip\|concussive_shot\|disengage\|call_pet\|aimed_shot\|scorpid_sting\|viper_sting\|scatter_shot\|volley\|immolation_trap\|explosive_trap\|freezing_trap\|deterrence\|feign_death\|mend_pet\|revive_pet\|rapid_fire\|aspect_of_the_hawk\|aspect_of_the_monkey\)" SM_Extend.lua
  ```
  期望返回 25
</action>
  <verify>
    <automated>
      -- 验证旧文件已删除
      test ! -f classes/hunter/combat.lua || { echo "FAIL: combat.lua not deleted"; exit 1; }
      test ! -f classes/hunter/utility.lua || { echo "FAIL: utility.lua not deleted"; exit 1; }
      -- 构建验证
      ./build.sh || { echo "FAIL: build.sh failed"; exit 1; }
      -- 验证产物中 5 个 Hunter combo 函数
      grep -c "function macroTorch\.\(hunterAtk\|hunterAoe\|hunterDefend\|hunterControl\|hunterMobTagging\)\b" SM_Extend.lua | awk '{if ($1 == 5) exit 0; else exit 1}' || { echo "FAIL: expected 5 hunter combo functions in build"; exit 1; }
      -- 验证产物中 25 个技能方法
      grep -c "function obj\.\(raptor_strike\|mongoose_bite\|arcane_shot\|multi_shot\|hunters_mark\|serpent_sting\|wing_clip\|concussive_shot\|disengage\|call_pet\|aimed_shot\|scorpid_sting\|viper_sting\|scatter_shot\|volley\|immolation_trap\|explosive_trap\|freezing_trap\|deterrence\|feign_death\|mend_pet\|revive_pet\|rapid_fire\|aspect_of_the_hawk\|aspect_of_the_monkey\)" SM_Extend.lua | awk '{if ($1 == 25) exit 0; else exit 1}' || { echo "FAIL: expected 25 hunter skill methods in build"; exit 1; }
      -- 验证旧符号不存在
      grep "hunterSting\|hunterCtrl\|htOtMod" SM_Extend.lua && { echo "FAIL: old symbols still in build"; exit 1; } || true</automated>
  </verify>
  <done>
classes/hunter/combat.lua 和 classes/hunter/utility.lua 已删除。build.sh 成功。SM_Extend.lua 产物包含 25 个 Hunter 技能方法 + 5 个 combo 函数，不含旧废弃符号。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_order.txt → build.sh | 文件顺序决定 SM_Extend.lua 拼接顺序，顺序错误导致运行时 undefined reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-08 | Denial of Service | build_order.txt 顺序错误 | medium | mitigate | combo.lua 必须在 Hunter.lua 之后（combo 调用 hunter singleton 技能方法）；构建后 grep 验证产物符号完整性 |
| T-25-09 | Information Disclosure | 废弃文件残留 | low | mitigate | combat.lua/utility.lua 显式删除；build_order.txt 中移除引用；构建后 negative-grep 确认旧符号不存在 |
| T-25-SC | Tampering | npm/pip/cargo installs | N/A | N/A | 无外部包安装 |
</threat_model>

<verification>
构建完整性：`./build.sh && echo "BUILD OK"`。
产物符号：SM_Extend.lua 中包含 25 个 `function obj.xxx(` + 5 个 `function macroTorch.hunterXxx(`。
产物无残留：`grep "hunterSting\|hunterCtrl\|htOtMod" SM_Extend.lua` 返回空。
旧文件已删除：`test ! -f classes/hunter/combat.lua && test ! -f classes/hunter/utility.lua` 成功。
</verification>

<success_criteria>
Phase 25 文件结构重组完成。build_order.txt 反映新的 Hunter 架构（Hunter.lua + combo.lua）。废弃文件已删除。构建验证通过，产物符号完整。
</success_criteria>

<output>
Create .planning/phases/25-druid-hunter-hunteratk-hunteraoe-hunterdefend-huntercontrol/25-03-SUMMARY.md when done
</output>

## Artifacts This Phase Produces

**Modified file:**
- `build_order.txt` — Hunter 块更新：添加 `classes/hunter/combo.lua`，移除 `classes/hunter/combat.lua` 和 `classes/hunter/utility.lua`

**Deleted files:**
- `classes/hunter/combat.lua` — 废弃（逻辑迁移到 combo.lua）
- `classes/hunter/utility.lua` — 废弃（逻辑迁移到 combo.lua）

**No new symbols created** — this plan performs build system housekeeping only.
</output>