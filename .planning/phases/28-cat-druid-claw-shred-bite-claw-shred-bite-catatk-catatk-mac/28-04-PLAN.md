---
phase: 28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac
plan: "04"
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - classes/druid/HUMAN-UAT.md
autonomous: true
requirements: [D-10, D-14, D-15, D-16, R7, R8]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  # spec-less probe skipped: phase has no requirement IDs (visible skip) — truths/prohibitions hand-derived from D-01..D-21 + R7 + R8
  truths:
    - "HUMAN-UAT.md 含 Phase 28 段：前提、--selftest 双解释器、/mt Category U、打桩协议、分析运行、期望信号、故障处理（含 .toc 检查与 rawdiag2 勿同开提醒）"
    - "全阶段验证电池 BATTERY_FAIL=0：8 个触及 .lua 文件 bbcheck 全 BALANCED、build.sh exit 0、产物 cpDamage 函数数=5、Cat U 注册数=9、pairLandIntent 保留 1 处（Phase 27 语义在位）、R8 九符号全在"
    - "用户实机闭环：/run 开关 → 木桩 1 分钟 → ReloadUI → 拷回 SV → 分析器出真表（human-check 项，由 verifier 汇入 28-UAT.md）"
  artifacts:
    - classes/druid/HUMAN-UAT.md
  key_links:
    - "游戏内采样 → SavedVariables → tools/cpdamage.lua --selftest/UAT → 决策建议行（全链路最终验证位）"
    - "R8 验收 grep（REQUIREMENTS.md 既有标准）与 Phase 27 land 符号保留断言（并行语义未排挤）"
  prohibitions:
    - "决策文件与构建清单/构建脚本零改动（R7/R8）— flagged-unverified"
    - "分析器不进入构建清单、不写回游戏数据 — flagged-unverified"
    - "不新开 SavedVariables 子表（复用 MACRO_TORCH_LOG，D-10）— flagged-unverified"
    - "cpDamageLog 与 rawdiag2 不建议同开（环容量提醒写入 UAT 文档）— flagged-unverified"
---

<objective>
本计划为 Phase 28 收口：把 HUMAN-UAT.md 追加 Phase 28 实机验收段，并跑一次性全阶段验证电池（bbcheck 全文件 + build.sh + 产物符号/注册计数 + Phase 27 land 语义保留 + R8 九符号回验 + 构建清单零改动审计），最后以 human-check 形式给出用户实机验收协议（human_verify_mode=end-of-phase：由 verifier 在阶段末汇入 28-UAT.md，不在计划内中途停顿）。

Purpose: 给「机器侧静态门全绿」补上「用户实机端到端闭环」的验收剧本——这是本 phase 唯一无法在构建机验证的一环（游戏客户端与 lua 解释器均在用户机，28-VALIDATION.md Manual-Only Verifications）。
Output: HUMAN-UAT.md Phase 28 段 + 全电池绿 + 实机验收协议（human-check）。
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-CONTEXT.md   # D-05/D-10/D-11/D-14 原文口径
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-RESEARCH.md   # §2 A2（.toc 存疑 → 人工确认）、§9 UAT 建议、Security Domain T-28-05
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-PATTERNS.md   # UAT 模板段（Prerequisites / Pre-Test / 分型 checklist 结构）
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-VALIDATION.md   # Manual-Only Verifications 表（本计划落地它）
@.planning/phases/27-catatk-event-driven-land-tracing-refactor/27-03-SUMMARY.md   # 电池形状先例（27-03 收尾电池）
@classes/druid/HUMAN-UAT.md   # 追加对象（先读其 Phase 27 段结构，本计划照同结构追加）
@.planning/REQUIREMENTS.md    # R8 验收 grep（第 116 行起既有标准）
</context>

<tasks>

<task type="auto">
  <name>Task 1: HUMAN-UAT.md 追加 Phase 28 实机验收段（含 human-check 验收协议）</name>
  <files>classes/druid/HUMAN-UAT.md</files>
  <read_first>
    - classes/druid/HUMAN-UAT.md 全文件（既有各 Phase 段的结构与格式，照同款追加）
    - 28-VALIDATION.md「Manual-Only Verifications」表（两条手动项的完整动作）
    - 28-RESEARCH.md §2（A2 .toc 存疑结论与检查法）与 Security Domain（T-28-05 提醒）
    - 28-CONTEXT.md D-05/D-10/D-11（木桩硬门/环上限自调/进脱战自动分批的验收预设）
  </read_first>
  <action>
    HUMAN-UAT.md 末尾追加「Phase 28」段（中文文档，照该文件既有中文/结构惯例；命令行与标识符保持原样）。小节与内容：

    1. Prerequisites：WoW 1.12.1 + SuperWoW 客户端；`./build.sh` 成功；至少一个可运行 lua 解释器（理想 5.0 一个 + 5.1+ 一个，Cygwin 环境即有）；Training Dummy 可用；`/run macroTorch.LOG_MAX_SIZE=3000` 建议（记录量增大，D-10）。
    2. Pre-Test：`lua tools/cpdamage.lua --selftest` 于两个解释器各跑一次，期望 `selftest: ALL N PASSED`；游戏内 `/mt`，期望自检汇总无红、Category U 9 条全过（warnings 可容忍非 U 项）；横幅应含 `macroTorch.cpDamageLog = false` 第五项（D-06 可见性确认）。
    3. SavedVariables 声明确认（RESEARCH A2）：游戏机端实际部署的 SuperMacro 变体 .toc 必须含 `MACRO_TORCH_LOG` 声明；检查法——打开 `SuperMacro.lua` SavedVariables 文件确认 `MACRO_TORCH_LOG` 段存在；缺失的修复——在该变体 SuperMacro.toc 的 `## SavedVariables:` 行末补一个空格加 `MACRO_TORCH_LOG` 后完全退出游戏重进（不补则打点静默零持久化）。
    4. 打桩协议：`/run macroTorch.cpDamageLog=true` → 以 catAtk 正常循环打骷髅级 Training Dummy 约 1 分钟（claw/shred/bite 都会自然出现）；可补手动命令（`/run macroTorch.player.claw('ready')` 等）确保三技能都有样本；停手待脱战（木桩场景约 5s 自动脱战=批次结束，D-11）；最后 `/reload`；从 `WTF/Account/<账号>/SavedVariables/SuperMacro.lua` 拷出文件。提醒：cpDamageLog 与 rawdiag2Enabled 勿同开（环容量争抢，T-28-05 / D-10）。
    5. 分析运行：`lua tools/cpdamage.lua <拷出的 SuperMacro.lua 路径> --json-out <归档 json 路径>`；期望：每批次节 + 聚合节出现 claw/shred 四档表（0/1/2/3）、OOC 背位分档表、bite 回归节（a/b/n）、三类决策建议行（每档 builder / OOC 技能 / bite 泄能与否）；坏行计数 0（或极小）。
    6. Expected outcomes / Troubleshooting：条目 11 字段数值合理（dmg 正值、e≈42/54/35 附近）；无 `[cpDamage]` 行时的排查顺序——先查 .toc、再查开关是否在 /reload 前仍为 true（每 login 复位默认 false，D-06）、再查 GCD probe 黄色警告（Rake 法术须在动作条上）；crit 全 false 或缺失时记录客户端语言环境（A3 假设）。
  </action>
  <verify>
    <automated>git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>打印任何 whitespace-error 行（退出码非零）</fails_when>
    <automated>grep -c 'cpdamage.lua --selftest' /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md && grep -c 'MACRO_TORCH_LOG' /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md && grep -c 'cpDamageLog=true' /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md</automated>
    <fails_when>三个 grep 任一输出为 0（--selftest 命令位、SV 声明检查位、开关命令位三者缺一即文档不完整）</fails_when>
    <human-check>
      用户实机按 HUMAN-UAT.md Phase 28 段执行四项闭环（阶段末 UAT，由 verifier 汇入 28-UAT.md）：
      1) 确认游戏机部署的 SuperMacro .toc 含 MACRO_TORCH_LOG 声明（RESEARCH A2）；缺失则按文档第 3 节补一行后重测
      2) `lua tools/cpdamage.lua --selftest` 在 5.0 与 5.1+ 两类解释器各跑一次，均 ALL PASSED（D-15 语法自验）
      3) `/run macroTorch.cpDamageLog=true` 打骷髅木桩 1 分钟（三技能均有样本）→ 脱战 → ReloadUI → 拷回 SuperMacro.lua
      4) `lua tools/cpdamage.lua <路径> --json-out <归档>` 出两层报表 + 三类决策建议行，条目 ≥ 30 且三技能齐、坏行计数 0/极小
      完成信号：四项全部达成或按 Troubleshooting 修复后达成；把第一张真实统计表的批量打印（或 json 归档路径）回复给 verifier
    </human-check>
  </verify>
  <acceptance_criteria>
    - UAT 段含 6 个小节且三枚 grep 锚（--selftest 命令、MACRO_TORCH_LOG 检查、开关命令）非零
    - UAT 段覆盖 28-VALIDATION.md 两条 Manual-Only 行的全部动作（实机打桩闭环 + SV 变体人工确认）
    - 文档改动仅 HUMAN-UAT.md 一个文件；diff --check 干净
  </acceptance_criteria>
  <done>HUMAN-UAT.md Phase 28 段完整（前提/自测/声明确认/打桩协议/分析运行/排查），实机验收协议四步已具备；用户拿到文档即可在游戏机独立完成闭环。</done>
</task>

<task type="auto">
  <name>Task 2: 全阶段验证电池 + 收尾（R7/R8 审计与 Phase 27 语义共存断言）</name>
  <files>macro_torch.lua, impl_util.lua, core/combat_context.lua, core/events.lua, core/spell_trace_core.lua, classes/druid/Druid.lua, classes/druid/selftest.lua, tools/cpdamage.lua</files>
  <read_first>
    - 28-PATTERNS.md 验证电池段（bbcheck 绝对路径 + grep 期望数）与 28-VALIDATION.md Sampling Rate
    - /home/admin/workspace/macro-torch/.planning/REQUIREMENTS.md R8 验收 grep（第 116 行起的既有标准命令）
    - Phase 27 收尾电池先例：/home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/27-03-selftest-verification-cleanup-PLAN.md 的电池任务（形状照抄，勿重推目录）
    - 本 phase 前三个计划的 verify 计数锚（28-01：cpDamage 数 5、SELF_DAMAGE 2 处；28-02：Cat U 数 9、cpDamageSample×4）
  </read_first>
  <action>    跑一次性全阶段电池（本任务零文件写入；发现失败才修，修复仅限本 phase 触及文件，修复后重跑直至全绿）：

    1. bbcheck 全触及文件（8 个 .lua，绝对路径前缀 /home/admin/workspace/macro-torch/，用 Phase 27 已验证的 bbcheck.js 绝对路径），逐文件断言 BALANCED。
    2. `bash /home/admin/workspace/macro-torch/build.sh` 断言 exit 0。
    3. 产物断言组（build.sh 是唯一证据来源的产物 SM_Extend.lua）：cpDamage 函数定义数恰 5；Category U 注册数恰 9；cpDamageSample 与 cpDamageCast 出现数各 4；`function macroTorch.pairLandIntent` 恰 1（Phase 27 land 配对语义未被排挤）；R8 九符号（catAtk/regularAttack/keepRip/keepRake/keepFF/shouldUseShred/shouldCastRip/shouldUseBite/canDoReshift）计数 ≥ 9（REQUIREMENTS.md 既有验收标准原样）。
    4. R7/R8 审计：本 phase 最近 8 条 commit 的文件列表（`git -C <repo> log --format= --name-only -8`）不含构建清单与构建脚本路径，也不含两个决策逻辑文件路径；任一出现即 FAIL。
    5. `git -C <repo> diff --check` 全净。
    6. 输出末行 `echo "BATTERY_FAIL=$FAIL"`。全绿后按 autoCommit 提交（本任务若产生修复 commit，message 英文 conventional；若零修复，电池通过状态随任务完成记录进 SUMMARY）。
  </action>
  <verify>
    <automated>export BB=/home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js; export R=/home/admin/workspace/macro-torch; export FAIL=0; for f in macro_torch.lua impl_util.lua core/combat_context.lua core/events.lua core/spell_trace_core.lua classes/druid/Druid.lua classes/druid/selftest.lua tools/cpdamage.lua; do node "$BB" "$R/$f" | grep -q BALANCED || FAIL=1; done; bash "$R/build.sh" || FAIL=1; [ "$(grep -c 'function macroTorch.cpDamage' "$R/SM_Extend.lua")" = "5" ] || FAIL=1; [ "$(grep -c 'SelfTest:register("Cat U-0' "$R/SM_Extend.lua")" = "9" ] || FAIL=1; [ "$(grep -c 'macroTorch.cpDamageSample' "$R/SM_Extend.lua")" = "4" ] || FAIL=1; [ "$(grep -c 'macroTorch.cpDamageCast' "$R/SM_Extend.lua")" = "4" ] || FAIL=1; [ "$(grep -c 'function macroTorch.pairLandIntent' "$R/SM_Extend.lua")" = "1" ] || FAIL=1; [ "$(grep -c 'function macroTorch.\(catAtk\|regularAttack\|keepRip\|keepRake\|keepFF\|shouldUseShred\|shouldCastRip\|shouldUseBite\|canDoReshift\)' "$R/SM_Extend.lua")" -ge "9" ] || FAIL=1; L=$(git -C "$R" log --format= --name-only -8 | grep -Ec '^(build_order\.txt|build\.sh|classes/druid/cat\.lua|classes/druid/combo\.lua)$'); [ "$L" = "0" ] || FAIL=1; git -C "$R" diff --check || FAIL=1; echo "BATTERY_FAIL=$FAIL"</automated>
    <fails_when>末行打印 BATTERY_FAIL=1（任一断言未过：bbcheck MISMATCH、build.sh 非零、产物 cpDamage 数≠5、Cat U≠9、cpDamageSample/Cast≠4、pairLandIntent≠1、R8 符号<9、构建清单或决策文件出现在最近 8 条 commit、diff --check 脏）</fails_when>
  </verify>
  <acceptance_criteria>
    - BATTERY_FAIL=0 且各断言计数与 28-01/28-02 计划中的锚一致（5 / 9 / 4 / 4 / 1 / ≥9 / 0）
    - 电池命令复用 Phase 27 已验证的 bbcheck.js 绝对路径与 build.sh 形状（未重造目录）
    - 产物中既有 land 符号（pairLandIntent）与全部 9 个 R8 符号完好——证明并行队列与观测通道没有排挤任何既有语义
  </acceptance_criteria>
  <done>全阶段验证电池全绿（8 文件平衡 + 构建成功 + 6 项产物计数 + R7/R8 审计零瑕疵 + diff 净）；Phase 28 机器侧可验证面尽数关闭，剩余仅为用户实机 UAT（Task 1 human-check）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 阶段验收文档 → 用户实机操作 | UAT 指令可被误操作（同开开关/漏补 .toc）→ 静默零数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05 | DoS | 持久化环 + rawdiag2 同开 | low | accept | UAT 文档明确提醒勿同开（T-28-05 落为文档缓解，D-10 /run 自调兜底） |
| T-28-A2 | Information Disclosure | .toc 缺失致数据静默丢失 | low | mitigate | UAT 第 3 节：SV 段存在性检查 + 补声明修复法（RESEARCH A2 人工确认落地） |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | 本 phase 无任何包管理器安装 |

配置：ASVS level 1（block on high severity）——无 high 级威胁，无阻塞护栏触发。
</threat_model>

## Artifacts this phase produces (Phase 28 全量盘点 — 28-04 定稿)

采集端（游戏内）：
- `macroTorch.cpDamageLog` bool 开关（默认 false）+ CONFIG_OPTIONS 第 5 项 + `macroTorch._cpDamageProbeWarned` 复位行 — macro_torch.lua
- `macroTorch.jsonEncodeScalar(v)` — impl_util.lua
- `macroTorch.context._cpDamageBatch` — core/combat_context.lua
- `macroTorch.cpDamageSample(skillToken, energyCost)` / `macroTorch.cpDamageCast(sample)` — classes/druid/Druid.lua
- obj.claw / obj.shred / obj.ferocious_bite 三个技能方法挂接（token+e：claw=computeClaw_E()、shred=computeShred_E()、bite=35）— classes/druid/Druid.lua
- `loginContext.cpDamageIntents`（LRUStack 容量 8，独立于 Phase 27 intentTable）
- `macroTorch.pairCpDamageIntent(guid, now)` / `macroTorch.onCpDamageLine(eventMsg, now)` / `macroTorch.cpDamageEvent(sample, dmg, crit)` — core/spell_trace_core.lua
- SELF_DAMAGE 通道分派（tier-1 白名单前）— core/events.lua
- Category U SelfTest 注册 9 条（`Cat U-01..U-09`，isOptional=true）— classes/druid/selftest.lua
- 条目数据契约：`[cpDamage] ` + `{"spell":..,"dmg":..,"crit":..,"e":..,"energyPool":..,"bleedCount":..,"isOoc":..,"isBehind":..,"cp":..,"t":..,"batch":..}` 固定 11 字段序

分析端（离线）：
- `tools/cpdamage.lua` — 单文件自包含 CLI：`lua tools/cpdamage.lua <SuperMacro.lua 路径> [--json-out <file>] [--selftest]`；模块：readAll / extractMacroTorchLog / loadBlockSandboxed / getMessages / decodeJson / parseEntries / buildClawShredBuckets / buildOocTiers / computeBiteRegression / printReport / decisionLines / writeJsonOut / runSelftest / main

文档：
- `classes/druid/HUMAN-UAT.md` Phase 28 实机验收段

<verification>本计划即最终电池：6 项产物计数 + 9 个 R8 符号 + Phase 27 land 符号共存 + 8 commit 窗口审计 R7/R8 + diff 净。用户实机闭环为 human-check 项（阶段末汇入 28-UAT.md）。</verification>

<success_criteria>
- 两任务 `<done>` 达成、BATTERY_FAIL=0、diff 净
- Phase 28 全部 4 计划完成；执行编排按 Wave 1(28-01)→2(28-02‖28-03)→3(28-04) 推进
- 用户拿到 UAT 剧本后可随时（装备/天赋变化后多次）复用整套打桩—分析闭环（D 系列复用性设计的最终交付）
</success_criteria>

<output>
Create `.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-04-SUMMARY.md` when done
</output>