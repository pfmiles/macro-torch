---
phase: 28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - macro_torch.lua
  - impl_util.lua
  - core/combat_context.lua
  - classes/druid/Druid.lua
  - core/spell_trace_core.lua
  - core/events.lua
  - tools/cpdamage.lua
autonomous: true
requirements: [D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, R8]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  # spec-less probe skipped: phase has no requirement IDs (visible skip) — truths/prohibitions hand-derived from D-01..D-21 + R7 + R8
  truths:
    - "登录会话内 macroTorch.cpDamageLog 默认 false（nil-guard），CONFIG_OPTIONS 横幅含第 5 项与 /run 设置命令（D-06）"
    - "claw 对木桩的一次 cast 落地后，持久化环内出现恰好一条 [cpDamage] 前缀 JSON 行，含 D-07 固定 11 字段序（spell,dmg,crit,e,energyPool,bleedCount,isOoc,isBehind,cp,t,batch）"
    - "miss/dodge/parry 行不产生任何条目（D-03 零附加代码——句式不匹配即丢弃）"
    - "SM_Extend.lua 产物含 cpDamageSample/cpDamageCast/pairCpDamageIntent/onCpDamageLine/cpDamageEvent 五个新函数定义"
    - "tools/cpdamage.lua 以 Lua 5.0 方言写成，能从 SV 形态文本中定位提取 MACRO_TORCH_LOG 段并打印解析出的条目"
    - "cast 侧采样与事件侧配对完全并行于 Phase 27 intentTable/pairLandIntent，共用 LAND_INTENT_TTL=2（D-02 红线）"
  artifacts:
    - macro_torch.lua
    - impl_util.lua
    - core/combat_context.lua
    - classes/druid/Druid.lua
    - core/spell_trace_core.lua
    - core/events.lua
    - tools/cpdamage.lua
  key_links:
    - "events.lua SELF_DAMAGE 通道门 → onCpDamageLine：插在 RAWDIAG2 scout 之后、tier-1 periodic 白名单 return 之前，按通道分派互不扰动 Phase 27 语义"
    - "cast 侧 intent（guid+castAt 快照，LRUStack 容量 8）→ RAW 伤害行 guid 小写比较（processRawAuraApply 同法）→ pairCpDamageIntent 2s 窗 → cpDamageEvent → macroTorch.log"
  prohibitions:
    - "仅 Training Dummy 打点：isTargetDummy 硬门 + 战斗 batch 存在，双门不符即静默不采样 — flagged-unverified"
    - "不修改任何 catAtk 决策逻辑（决策文件零改动，R8）— flagged-unverified"
    - "不修改 Phase 27 intentTable/pairLandIntent 语义——cpDamage 队列完全并行独立 — flagged-unverified"
    - "分析器只读，不写回任何游戏数据；tools/ 不进构建清单；不新开 SavedVariables 子表（复用 MACRO_TORCH_LOG）— flagged-unverified"
---

<objective>
本计划为 Phase 28 的 tracer 切片：把「开 `macroTorch.cpDamageLog` 开关 → 对木桩打一发 Claw → RAW_COMBATLOG SELF_DAMAGE 通道配对 → 持久化环写入一条 `[cpDamage] ` JSON 行 → 离线脚本从 SuperMacro.lua SV 文件提取并打印该条目」这一条竖切路径在全栈打通并验证，之后 Wave 2 计划在此已证明的骨架上外扩（shred/bite、完整统计报表、自测）。

Purpose: 用本 agent 早期高质量上下文证明整条观测通道的架构成立（cast 侧采样 → 独立 intent 队列 → 通道门 → 配对 → 前缀落盘 → 提取解析），避免在已提交多层后才撞上架构死巷；本切片全部部件均为最终产物（非原型）。
Output: 7 个文件的可构建改动 + `[cpDamage]` 单条路径端到端成立的静态证据（bbcheck 全平衡 + build.sh 产出含全链路符号 + 脚本锚点齐全）。注意本机无 lua 解释器（28-VALIDATION.md 已确立）：端到端运行级验证落在 28-03 的 `--selftest` 与 28-04 的用户机 UAT，本计划以 bbcheck + build.sh 为机器侧门。

实现事实以 28-RESEARCH.md 为准（覆盖 CONTEXT D-01 措辞）：
- D-01 真实落点 = RAW_COMBATLOG 的 `arg1='CHAT_MSG_SPELL_SELF_DAMAGE'` 通道，伤害行句式用两条 Lua 5.0 合法 `string.find`（Lua pattern 魔法字符仅 ^$()%.[]*+-?，`|` 是字面量——`(hits|crits)` 只匹配字面文本、捕获也不可接量词，故禁止单条组合模式）：hits 线 `^Your .+ hits (0x[0-9A-Fa-f]+) for (%d+)%.` 与 crits 线 `^Your .+ crits (0x[0-9A-Fa-f]+) for (%d+)%.`（均不锚定行尾），命中者确定 verb/crit（verb ∈ {hits, crits} 白名单由模式字面量构造性保证，A3 容错保留），crit 完全可检测；miss/dodge/parry 行同通道但两条均不匹配 → D-03 自动成立。
- D-02 红线：严禁复用 Phase 27 intentTable/pairLandIntent——新建并行队列 `loginContext.cpDamageIntents = LRUStack:new(8)`，元素 `{ spell, guid, castAt, sample }`，配对复用 `LAND_INTENT_TTL = 2` + purge/逆序骨架。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-CONTEXT.md
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-RESEARCH.md
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-PATTERNS.md
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-VALIDATION.md

# 关键锚点（已核实到 file:line，执行时逐字对照）
@macro_torch.lua           # cpBuildLog nil-guard 22-28；CONFIG_OPTIONS 65-94
@impl_util.lua             # toBoolean/tableLen 纯函数全局样式 29-31, 92-101
@core/combat_context.lua   # onCombatEnter/Exit 21-39
@classes/druid/Druid.lua   # 技能方法三件套 25-58；cpBuildLogSample/Event 328-355；computeClaw_E/shred_E 501-509, 642-645
@core/spell_trace_core.lua # LAND_INTENT_TTL=2 @15；pairLandIntent 175-210；processRawAuraApply guid 小写比较 261-306
@core/events.lua           # RAW_COMBATLOG 分支 135-205；rawdiag2 scout 束 ~181；tier-1 白名单 early-return 187-189
@core/periodic.lua         # LRUStack:new/push/removeMatch 21-75
@entity/Unit.lua           # hasBuff 26-34；guid map 103-109；mana map 114-116
@entity/Player.lua         # isBehindTarget 602-605
@classes/druid/combo.lua   # isTargetDummy 表达式 104-106；BITE_E=35 @62（只读引用，不改）
@.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js  # Lua 括号平衡验证门（本计划 verify 复用其绝对路径）
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 配置与工具层 — cpDamageLog 开关（D-06）+ JSON 微编码器 + batch 戳（D-11）</name>
  <files>macro_torch.lua, impl_util.lua, core/combat_context.lua</files>
  <read_first>
    - macro_torch.lua 全文件（cpBuildLog/rawdiag2Enabled/COWER_THREAT_THRESHOLD/LOG_MAX_SIZE 四段 nil-guard 与注释风格、CONFIG_OPTIONS 65-94 与 printConfigBanner 97-110）
    - impl_util.lua 全文件（纯函数全局声明样式与 Apache 文件头）
    - core/combat_context.lua 全文件（onCombatEnter/Exit 21-39、onPlayerEnteringWorld 中 loginContext 重置）
    - 28-PATTERNS.md §4/§5/§6（batch 戳、编码器约束、per-session bool 三惯例）
  </read_first>
  <action>    （本任务是 tracer 竖切的第一段：配置与工具层，三文件全部是「只加不删」的净增量。）

    macro_torch.lua（per D-06，逐条照抄 cpBuildLog 三大惯例）：
    1. 在 LOG_MAX_SIZE nil-guard 块之后、`macroTorch._cpBuildLogProbeWarned = nil` 之前，新增 `if macroTorch.cpDamageLog == nil then macroTorch.cpDamageLog = false end`；上方注释风格照抄 cpBuildLog 块（英文，说明：默认 false、游戏内 SuperMacro body 里置 true、nil-guard 每次 login 复位默认值、mid-session 切换无需 reload）。
    2. 紧邻 `macroTorch._cpBuildLogProbeWarned = nil` 与其注释，新增一行 `macroTorch._cpDamageProbeWarned = nil`（照抄同款注释：Per-login re-arm of the one-time GCD-probe diagnostic，供 Task 2 的 cpDamageSample 一次性警告旗标使用）。
    3. 紧随注释「A complete survey of user-tunable globals yields exactly these four entries」之上更新为 five，并在 CONFIG_OPTIONS 表尾（LOG_MAX_SIZE 条目之后）追加第 5 项：`name = 'macroTorch.cpDamageLog'`、`default = false`、`desc = 'cat claw/shred/bite damage sample log switch ([cpDamage] entries for the offline analyzer)'`、`cmd = '/run macroTorch.cpDamageLog=true'`、`get = function() return macroTorch.cpDamageLog end`。printConfigBanner 不改。

    impl_util.lua（per D-07/D-12 与 RESEARCH §3，~30 行纯函数）：
    4. 新增 `function macroTorch.jsonEncodeScalar(v)`：nil → 'null'；boolean → 'true'/'false'；number → tostring(v)；string → 双引号包裹 + `string.gsub` 的 function 分支转义（反斜杠 → \\，双引号 → \""，控制字符 < 32 → \u00XX 四字符宽大写十六进制形式）；非上述类型原样 tostring。明确禁止使用 `%q`（产 Lua 引号 \ddd 形式，JSON 非法）与 gsub 的 table 替换（Lua 5.0 不支持）。函数注释注明「strict JSON emit contract with tools/cpdamage.lua decode side; only 6 scalar forms flow through here」（RESEARCH §3 互认契约）。

    core/combat_context.lua（per D-11，一行）：
    5. onCombatEnter 末尾（`macroTorch.show('Entering combat!')` 之后）追加 `macroTorch.context._cpDamageBatch = GetTime()`，带一行英文注释说明：进战斗时刻即本批 `[cpDamage]` 条目的 batch 字段；脱战 onCombatExit 整体重建 context 表 → 下一批自动取新值，零额外清空逻辑。

    约束：三个文件不删除、不改动任何既有行内容（只插入）；注释与 commit 一律英文。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/macro_torch.lua /home/admin/workspace/macro-torch/impl_util.lua /home/admin/workspace/macro-torch/core/combat_context.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 对任一文件输出 MISMATCH（node 非零退出），或 git diff --check 打印任何 whitespace-error 行（退出码非零）</fails_when>
    <automated>grep -v -- '--' /home/admin/workspace/macro-torch/macro_torch.lua | grep -c 'macroTorch.cpDamageLog == nil'</automated>
    <fails_when>输出不是 1（0 表示 nil-guard 缺失，>1 表示重复守卫）</fails_when>
    <automated>grep -c "name = 'macroTorch.cpDamageLog'" /home/admin/workspace/macro-torch/macro_torch.lua && grep -c "macroTorch._cpDamageProbeWarned = nil" /home/admin/workspace/macro-torch/macro_torch.lua && grep -c 'function macroTorch.jsonEncodeScalar' /home/admin/workspace/macro-torch/impl_util.lua && grep -c '_cpDamageBatch' /home/admin/workspace/macro-torch/core/combat_context.lua</automated>
    <fails_when>任一 grep 输出不是 1（四个期望值依次为 1、1、1、1；0 或 ≥2 均为失败信号）</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v -- '--' macro_torch.lua | grep -c 'macroTorch.cpDamageLog == nil'` 输出 1；`grep -c "name = 'macroTorch.cpDamageLog'" macro_torch.lua` 输出 1
    - `grep -c 'function macroTorch.jsonEncodeScalar' impl_util.lua` 输出 1；编码器不含 `%q` JSON 输出用法
    - `grep -c '_cpDamageBatch' core/combat_context.lua` 输出 1，且该行在 onCombatEnter 函数体内（文件 21-39 区间）
    - CONFIG_OPTIONS 表恰有 5 项（`grep -c 'name = ' macro_torch.lua` 输出 5，即 4 旧 + 1 新）；bbcheck 三文件全 BALANCED；git diff --check 干净
    - 激活条件：cpDamageLog 默认 false（D-06）；batch 戳在进战斗时刻取 GetTime（D-11）；编码器严格互认契约（D-12 链路前置）
  </acceptance_criteria>
  <done>cpDamageLog 开关与第 5 条横幅注册、jsonEncodeScalar 编码器、_cpDamageBatch batch 戳三处净增量落地并通过静态门；本任务三个清单文件的 diff 均为纯插入。</done>
</task>

<task type="auto">
  <name>Task 2: claw 唯一路径实现 — 采样/独立 intent/通道门/配对/发射（D-01/D-02/D-04/D-05/D-07/D-08/D-09）</name>
  <files>classes/druid/Druid.lua, core/spell_trace_core.lua, core/events.lua</files>
  <read_first>
    - classes/druid/Druid.lua 25-58（技能方法三件套，claw/shred 已有 cpBuild、ferocious_bite 无）、328-355（cpBuildLogSample/Event 全文，WR-01 GCD probe 注释）、501-509 与 642-645（computeClaw_E/computeShred_E）
    - core/spell_trace_core.lua 1-40（LAND_INTENT_TTL=2 @15）、86-142（recordCastTable 懒初始化）、175-210（pairLandIntent purge/pair 骨架全文）、261-306（processRawAuraApply 的 guid 小写比较与 RAWDIAG2 pair 日志）
    - core/events.lua 135-205（RAW_COMBATLOG 分支全文：rawdiag2 scout → `-- end of RAWDIAG2 scout block` → tier-1 白名单 early-return）
    - core/periodic.lua 21-75（LRUStack push/removeMatch/elements 结构）
    - entity/Unit.lua 26-34（hasBuff 含 1..40 扫描，勿改）与 103-109（guid = UnitExists 第二返回值，SuperWoW 门，无 SuperWoW 时为 nil）与 114-116（mana）；entity/Player.lua 602-605（isBehindTarget 定义）；texture_map.lua 40-46（未映射纹理透传）
    - classes/druid/combo.lua 104-106（isTargetDummy 表达式的唯一权威原文，逐字照抄其 `string.find` 与 `toBoolean` 用法）
    - 28-PATTERNS.md §1/§2/§3（本任务三文件的模式与行位置）
  </read_first>
  <action>
    本任务是 tracer 竖切的第二段：claw 一条路径贯穿 cast 侧（采样 + 独立 intent）与事件侧（通道门 + 解析 + 配对 + 发射）。全部新函数带英文注释；不触碰 Phase 27 既有函数体。

    classes/druid/Druid.lua —— 三个新函数 + obj.claw 挂两行（全在 cpBuildLogSample/Event 附近并排落位）：
    1. `function macroTorch.cpDamageSample(skillToken, energyCost)` —— cast 前全量快照。门序必须为「廉价门先行」：a) `if not macroTorch.cpDamageLog then return nil end`（未开开关零开销）；b) isTargetDummy 硬门——逐字复用 combo.lua:104-106 表达式 `macroTorch.toBoolean(macroTorch.target.isCanAttack and string.find(macroTorch.target.name, 'Training Dummy'))`，false 即 return nil（D-05 硬门）；c) batch 门——`local batch = macroTorch.context and macroTorch.context._cpDamageBatch`，batch 为 nil 即 return nil（脱战不采集，D-11）；d) GCD probe——`local gcdOk = macroTorch.player.isActionCooledDown('Ability_Druid_Rake')`，nil 时用 `macroTorch._cpDamageProbeWarned` 一次性黄色警告（文案以 `[cpDamage]` 开头，说明 Rake 法术须在动作条上否则无记录——WR-01 同款）；e) bleedCount 快照——三次 `macroTorch.target.hasBuff(texture)` 计数 0-3：`Ability_Druid_Disembowel`（Rake）、`Ability_GhoulFrenzy`（Rip）、`Ability_Druid_SupriseAttack`（Pounce），每次 `macroTorch.toBoolean(...)` 包一层（D-04 任意来源口径，hasBuff 现成实现）。返回表字段全集（D-07 口径）：`spell = skillToken`（小写 ASCII token）、`guid = macroTorch.target.guid`、`t = GetTime()`、`cp = macroTorch.player.comboPoints`、`energyPool = macroTorch.player.mana`（D-09 咬击必录）、`bleedCount`、`isOoc = macroTorch.player.isOoc`、`isBehind = macroTorch.player.isBehindTarget`、`e = energyCost`（调用方传入的该技能理论能耗，D-08）、`batch = batch`、`gcdOk = gcdOk`。函数注释注明 WR-01 约束：probe 必须先于 cast（cast 启动 GCD 会污染读数）。
    2. `function macroTorch.cpDamageCast(sample)` —— 种独立 intent：`if not macroTorch.loginContext then return end`；`if sample.guid == nil then return end`（无 SuperWoW 时 guid 为 nil，不种——RAW 通道同样不存在）；懒初始化 `if not macroTorch.loginContext.cpDamageIntents then macroTorch.loginContext.cpDamageIntents = macroTorch.LRUStack:new(8) end`；`push({ spell = sample.spell, guid = sample.guid, castAt = sample.t, sample = sample })`。注释声明红线：本队列与 intentTable/pairLandIntent 完全并行独立（Ferocious Bite 已被 land 配对占用，fail-wins 语义冲突，D-02）。
    3. obj.claw（25-32 行）改动：既有 cpBuild 两行保持不变；在其后追加 `local cpDmg = macroTorch.cpDamageSample('claw', macroTorch.computeClaw_E())`（置于 `_castSpell` 调用之前）与 `if cast and cpDmg and cpDmg.gcdOk then macroTorch.cpDamageCast(cpDmg) end`。只挂 claw（shred/bite 属 28-02）。token 用小写 `'claw'`（D-07 spell 字段 ASCII token），e 传 `macroTorch.computeClaw_E()` 返回值（D-08）。

    core/spell_trace_core.lua —— 三个新函数 + 一个懒初始化自助段（与 pairLandIntent 并排）：
    4. `function macroTorch.pairCpDamageIntent(guid, now)` —— 骨架照抄 pairLandIntent 的两趟结构但不带 state/landAt：`if not guid or not macroTorch.loginContext or not macroTorch.loginContext.cpDamageIntents then return nil end`；purge 趟：逆序 for i = macroTorch.tableLen(stack.elements), 1, -1，`(now - intent.castAt) > macroTorch.LAND_INTENT_TTL` 的条目 `table.remove(stack.elements, i)`；pair 趟：逆序找 `intent.guid ~= nil and intent.castAt <= now and (now - intent.castAt) <= macroTorch.LAND_INTENT_TTL and string.lower(intent.guid) == string.lower(guid)`，命中即 remove 并 `return intent.sample`，否则 return nil。复用宏：`macroTorch.LAND_INTENT_TTL`（2s 窗口，D-02）。注释说明：技能名维度由「一 cast 帧恰好一条 intent」不变式保证（每按键一 action 红线），故键为 guid+窗口。
    5. `function macroTorch.onCpDamageLine(eventMsg, now)` —— 入口 `if not macroTorch.cpDamageLog or not eventMsg then return end`；两条 Lua 5.0 合法 `string.find` 依序尝试（**严禁 `(hits|crits)` 单组合模式：Lua pattern 无 `|` 或运算、括号捕获禁止接量词，单组合模式按字面文本匹配、永不命中真实伤害行 → 采集链零产出**）：先 `string.find(eventMsg, '^Your .+ hits (0x[0-9A-Fa-f]+) for (%d+)%.')`，命中则 verb='hits'、crit=false；未命中再试 `string.find(eventMsg, '^Your .+ crits (0x[0-9A-Fa-f]+) for (%d+)%.')`，命中则 verb='crits'、crit=true。每条模式仅捕获 GUID、伤害数两个子模式（verb 即模式字面量本身，∈ {hits, crits} 白名单构造性成立，A3 容错保留），均不锚定行尾（容忍 `(N absorbed/blocked)` 尾缀）；两条皆无匹配即 return（miss/dodge/parry/resist 行天然不匹配 → D-03 零附加代码）；`local sample = macroTorch.pairCpDamageIntent(guid, now)`，nil 即 return（无 intent 对应，如白字平砍行）；`macroTorch.cpDamageEvent(sample, tonumber(dmgStr), crit)`（crit 真值由命中哪条模式唯一确定；D-01 crit 可检测）。
    6. `function macroTorch.cpDamageEvent(sample, dmg, crit)` —— 组装固定 11 字段序 JSON（D-07 钦定序）并 `macroTorch.log('[cpDamage] ' .. json)`：spell 经 `macroTorch.jsonEncodeScalar`、dmg、crit（true/false 字面量）、e、energyPool、bleedCount、isOoc、isBehind、cp、t、batch 依次以 `"键":值` 逗号连接。注释注明字段序为编码/解码互认契约的一部分（RESEARCH §3）。

    core/events.lua —— 通道门（唯一改动点）：
    7. 在 RAW_COMBATLOG 分支内、`-- end of RAWDIAG2 scout block` 注释行之后、`-- Tier 1 (channel whitelist)` 注释与 tier-1 early-return 之前，插入通道分派：`if macroTorch.cpDamageLog and arg1 == 'CHAT_MSG_SPELL_SELF_DAMAGE' and arg2 then macroTorch.onCpDamageLine(arg2, GetTime()) end`，带 `[cpDamage]` 英文注释说明（SELF_DAMAGE 行在此被消费，随后 tier-1 return 挡住 land 消费者——两者按通道分派，tier-1 白名单集合一字不改）。注意：既有 `CHAT_MSG_SPELL_SELF_DAMAGE` 聊天过滤分支（events.lua 99-107）不动，本门只处理 RAW 流。

    约束：不写构建清单、不改构建脚本（7 文件全在既有清单内，RESEARCH §8）；两大猫德决策逻辑文件零改动；代码注释英文。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/Druid.lua /home/admin/workspace/macro-torch/core/spell_trace_core.lua /home/admin/workspace/macro-torch/core/events.lua</automated>
    <fails_when>bbcheck 对任一文件输出 MISMATCH（node 非零退出）</fails_when>
    <automated>bash /home/admin/workspace/macro-torch/build.sh && grep -c 'function macroTorch.cpDamage' /home/admin/workspace/macro-torch/SM_Extend.lua</automated>
    <fails_when>build.sh 打印 ERROR: File not found in build_order.txt 并以非零退出；或后续 grep 输出不是 5（cpDamageSample/cpDamageCast/pairCpDamageIntent/onCpDamageLine/cpDamageEvent 五个定义应恰在产物中）</fails_when>
    <automated>grep -c "arg1 == 'CHAT_MSG_SPELL_SELF_DAMAGE'" /home/admin/workspace/macro-torch/core/events.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>grep 输出不是 1（RAW 分支唯一通道门恰 1 处；既有 4 处 SELF_DAMAGE 引用——RegisterEvent/聊天分支两处/注释一处——不被修改仅增此行，总数 4→5 且门行精确可数）；或 git diff --check 打印 whitespace-error 行</fails_when>
  </verify>
  <acceptance_criteria>
    - SM_Extend.lua（build.sh 产物）中 `grep -c 'function macroTorch.cpDamage'` == 5；`grep -c 'cpDamageIntents'` ≥ 2（懒初始化 + 配对消费）
    - events.lua RAW 分支中 SELF_DAMAGE 通道门位于 `-- end of RAWDIAG2 scout block` 与 `-- Tier 1 (channel whitelist)` 之间；tier-1 白名单集合字符串零变化
    - obj.claw 内 `cpDamageSample('claw', macroTorch.computeClaw_E())` 在 `_castSpell` 之前调用；cast 为真且 gcdOk 为真才 cpDamageCast
    - pairCpDamageIntent 中 purge 条件用 `macroTorch.LAND_INTENT_TTL`（不新定义常量）；guid 比较用 string.lower 两侧；
    - D-07 十个字段 + batch 全部写入快照表；miss 行 dispose 为 return（D-03）；isTargetDummy 硬门（D-05）
  </acceptance_criteria>
  <done>claw 一条路径全链接通并静态验证：开关 → 采样（含 isTargetDummy/batch/GCD probe/bleedCount 四级门）→ 独立 intent 队列 → SELF_DAMAGE 通道门 → hits/crits 解析 → 2s 窗 guid 配对 → `[cpDamage] ` 11 字段 JSON 落盘；Phase 27 land 语义零扰动。</done>
</task>

<task type="auto">
  <name>Task 3: 远端终点 — tools/cpdamage.lua 提取→过滤→解码→吐条目骨架（D-14/D-15 tracer 段）</name>
  <files>tools/cpdamage.lua</files>
  <read_first>
    - 28-PATTERNS.md §8（间接 pattern 源：bbcheck.js strip 逻辑、SV 序列化形态、防御清单）与「No Analog Found」条目
    - 28-RESEARCH.md §2（提取策略与沙箱双路径）、§3（解码契约）、§6（Lua 5.0 兼容清单全表）
    - /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js（括号配平的字符串/注释感知 strip 实现全文）
    - /home/admin/workspace/macro-torch/build.sh 与 build_order.txt（确认 tools/ 不被迭代——只读确认即可）
  </read_first>
  <action>    新建独立脚本（tools/ 目录一并创建）。本任务交付「可提取、可过滤、可打印」的骨架；28-03 在此骨架上外扩统计/拟合/报表/--json-out/完整 --selftest。

    1. 文件头：Apache 2.0 头（`]] --` 收尾，照抄 core/events.lua:1-15 款式）。全文 Lua 5.0 方言（D-15）：禁 `#` 长度运算符（数组长用 ipairs 计数）、禁 goto、禁 gsub table 替换（用 function 分支）、禁 `%q` 产 JSON、禁 unpack（`arg[1]` 起直取）、禁 // 与 \x/\u{} 转义、数学取余用 `%`。
    2. 顶层函数清单（自包含无任何外部依赖）：
       - `readAll(path)` — io.open + `file:read("*a")`；先 `file:seek("end")` 查大小，超 32MB 即声明拒绝（DoS 上限，RESEARCH Security Domain）。
       - `extractMacroTorchLog(text)` — `string.find(text, 'MACRO_TORCH_LOG', 1, true)` 定位 marker → 向后扫到首个 `{` → 字符串/注释感知的括号配平（跳过 `"`/`'` 字符串字面量含 `\ddd` 与 `\\`、`\"`、`\'` 转义、`--` 行注释与 `--[[ ]]` 块注释，同 bbcheck.js strip 顺序）到配平闭合 `}` → 返回 `MACRO_TORCH_LOGOG = <block>` 整段文本；未找到返回 nil。配平失配则在报错信息中给出行号（RESEARCH §2 关键陷阱：JSON body 内含 `{}`/引号，裸计数必错配）。
       - `loadBlockSandboxed(block)` — `local load_chunk = loadstring or load` 垫片（5.0/5.1 基库 loadstring；5.2+ load）；5.0/5.1 走 `setfenv(fn, env)`，5.2+ 走 `load(block, '@sv_block', 't', env)`；env 为只含安全函数的空环境表（不含任何 io/os/dofile 等危险全局，RESEARCH T-28-01 沙箱）。
       - `getMessages(block)` — 沙箱执行后按 `MACRO_TORCH_LOG` 与 `MACRO_TORCH_LOG["messages"]` 两种键形式取 messages（负载处理后清空 env）；`MACRO_TORCH_LOG = nil` 或 `{ }` 时返回空表 + 友好提示（RESEARCH §2 防御）。
       - `decodeJson(str)` — 递归下降迷你解析器（仅 object/array/string/number/true/false/null 六形态；递归深度上限 ~16 防御）；错误返回 nil, err。
       - `main(arg)` — 参数解析：`arg[1]` 为 SV 路径；`--selftest` 分支输出「selftest runs in the full analyzer build」（占位，28-03 完成实现）；`--json-out` 同样提示占位。无参数打 usage 并 exit 1。流程：readAll → extractMacroTorchLog → loadBlockSandboxed → getMessages → 逐条 `[cpDamage] ` 前缀精确过滤 → 剥前缀 → decodeJson → 按固定 11 字段打印条目（`spell: claw | dmg: 210 | crit: false | e: 42 | energyPool: 60 | bleedCount: 1 | isOoc: false | isBehind: true | cp: 2 | t: 1234.56 | batch: 1000.00` 式样）+ 结尾打印条数与坏行计数（坏行 pcall 跳过 + 计数警告，RESEARCH T-28-03；条数上限 50k 截断并警告，T-28-02）。
    3. 落位契约（D-14/R7）：本脚本绝不进入构建清单——不修改构建脚本与清单文件内容；脚本读的是用户手工指定的 WTF 路径，不写回任何游戏数据。

    代码注释与脚本内文案英文；输出数字统一 `string.format` 定格式（RESEARCH §6 跨版本对齐）。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/tools/cpdamage.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 对脚本输出 MISMATCH（node 非零退出），或 git diff --check 打印 whitespace-error 行</fails_when>
    <automated>grep -c 'function extractMacroTorchLog\|function loadBlockSandboxed\|function decodeJson\|function main' /home/admin/workspace/macro-torch/tools/cpdamage.lua</automated>
    <fails_when>输出不是 4（四个函数定义各一条；0 表示脚本骨架缺失）</fails_when>
    <automated>grep -cn '#\|goto ' /home/admin/workspace/macro-torch/tools/cpdamage.lua</automated>
    <fails_when>grep 匹配到任何 `#` 长度运算符或 `goto `（Lua 5.0 禁令，D-15；输出非 0 即失败）。注：本 grep 对脚本全部行匹配，注释内出现上述 token 亦视为失败</fails_when>
    <automated>bash /home/admin/workspace/macro-torch/build.sh && git -C /home/admin/workspace/macro-torch diff --stat | tail -1</automated>
    <fails_when>build.sh 非零退出；或 diff --stat 末行显示构建清单/构建脚本被修改（本任务约定二者零改动）</fails_when>
  </verify>
  <acceptance_criteria>
    - 脚本含 Apache 头、四个顶层函数（extractMacroTorchLog/loadBlockSandboxed/decodeJson/main）、`loadstring or load` 垫片与 setfenv/load-t 双路径
    - 括号配平扫描跳过字符串/注释（不含裸字符计数实现）；32MB 文件上限与 50k 条数上限均有检查代码
    - 前缀过滤使用 `[cpDamage] ` 精确前缀；decodeJson 仅支持 object/array/string/number/true/false/null
    - 不含 `#`/goto/unpack/gsub-table 用法；bbcheck BALANCED；diff --check 干净；构建清单与构建脚本零改动
  </acceptance_criteria>
  <done>tools/cpdamage.lua 骨架存在且静态门全绿：可读 SV 形态文本 → 提取 MACRO_TORCH_LOG 段（字符串/注释感知配平）→ 沙箱执行 → 前缀过滤 → 解码 → 逐条打印 + 条数/坏行计数；为 28-03 的统计与报表扩展预留 main 内的分派位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAW_COMBATLOG 事件流 → addon | 客户端内部事件，本 phase 只在 SELF_DAMAGE 通道上消费自有 intent 对应的行 |
| 持久化环 → SavedVariables 文件 | `[cpDamage]` 条目走既有 macroTorch.log 写路径 |
| 外部 SV 文件 → tools/cpdamage.lua | 分析器吃任意路径指定的文件，loadstring 执行其提取段（主要攻击面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering / Elevation | tools/cpdamage.lua loadBlockSandboxed | medium | mitigate | 沙箱执行：5.0/5.1 setfenv 空环境 + 禁用 io/os/dofile 等危险全局；5.2+ load(block, nil, 't', env)；执行后仅读 MACRO_TORCH_LOG["messages"] 键（RESEARCH Security Domain） |
| T-28-02 | DoS | tools/cpdamage.lua readAll / 条目循环 | low | mitigate | 文件上限 32MB 拒绝；条目上限 50k 截断并警告 |
| T-28-03 | DoS | decodeJson 坏行 | low | mitigate | pcall 包裹逐行解码，坏行跳过 + 计数告警，不中断整批 |
| T-28-04 | Spoofing | 环内 `[cpDamage]` 相似文本 | low | mitigate | 前缀精确匹配 + decode 成功双门槛；环为本 addon 私有表，仅 macroTorch.log 写过 |
| T-28-05 | DoS | 持久化环与 rawdiag2 同开挤爆 | low | accept | LOG_MAX_SIZE 用户 /run 自调（D-10）；28-04 UAT 文档提醒两者勿同开 |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | 本 phase 无任何包管理器安装（bbcheck.js 为 Phase 27 既有资产，只读复用，不新增依赖） |

配置：安全策略 ASVS level 1（block on high severity）——上表无 high 级威胁，全部 medium/low，无阻塞护栏触发。
</threat_model>

## Artifacts this phase produces (本 phase 产出清单 — 28-01 部分)

| 符号 / 文件 | 类型 | 定义处 | 说明 |
|---|---|---|---|
| `macroTorch.cpDamageLog` | bool 全局开关 | macro_torch.lua | nil-guard 默认 false；CONFIG_OPTIONS 第 5 项（D-06） |
| `macroTorch._cpDamageProbeWarned` | 一次性警告旗标 | macro_torch.lua | 每次 login re-arm（Task 2 GCD probe 用它） |
| `macroTorch.jsonEncodeScalar(v)` | 纯函数 | impl_util.lua | Lua 值 → JSON 标量（nil/bool/number/string 转义） |
| `macroTorch.context._cpDamageBatch` | 会话字段 | core/combat_context.lua | 进战斗时刻 GetTime()（D-11 batch 字段源） |
| `macroTorch.cpDamageSample(skillToken, energyCost)` | cast 前快照 | classes/druid/Druid.lua | 11 字段 + guid + gcdOk；D-05 木桩硬门 |
| `macroTorch.cpDamageCast(sample)` | intent 种植 | classes/druid/Druid.lua | 写 loginContext.cpDamageIntents（LRUStack 容量 8，D-02） |
| `macroTorch.pairCpDamageIntent(guid, now)` | 配对 | core/spell_trace_core.lua | purge + 逆序 guid 配对，TTL=LAND_INTENT_TTL |
| `macroTorch.onCpDamageLine(eventMsg, now)` | 行解析入口 | core/spell_trace_core.lua | 双模式依序尝试：`^Your .+ hits (0x[0-9A-Fa-f]+) for (%d+)%.` / `^Your .+ crits (0x[0-9A-Fa-f]+) for (%d+)%.`（Lua 5.0 合法，verb 由命中模式确定）；D-03 天然过滤 |
| `macroTorch.cpDamageEvent(sample, dmg, crit)` | 条目发射 | core/spell_trace_core.lua | `[cpDamage] ` + 11 字段 JSON 固定序 |
| channel gate | events.lua | `arg1 == 'CHAT_MSG_SPELL_SELF_DAMAGE'` 通道分派 | tier-1 白名单之前、scout 之后 |
| `tools/cpdamage.lua` | 独立脚本 | tools/ | CLI：`lua tools/cpdamage.lua <path>`（骨架版） |
| `[cpDamage] ` 行格式 | 数据契约 | spell_trace_core.lua | `{"spell":"claw","dmg":N,"crit":bool,"e":N,"energyPool":N,"bleedCount":0..3,"isOoc":bool,"isBehind":bool,"cp":N,"t":T,"batch":T}` |

<verification>本计划的端到端证明（机器侧无 lua，运行级验证后置）：
1. 三任务各自的 bbcheck + grep 断言全绿（逐任务 fail 信号见上）
2. build.sh 成功重建 SM_Extend.lua 且含 5 个 cpDamage 函数定义 + 既有 land 符号（pairLandIntent/processRawAuraApply）同时存在——证明并行队列未排挤 Phase 27 语义
3. git diff 范围审计：仅 7 个声明文件被改动，决策文件与构建清单零改动
4. tools/cpdamage.lua 括号平衡 + 无 Lua 5.1+ 特性（bbcheck + grep 门）
用户机运行级验证（28-03 --selftest 与 28-04 UAT）由后续计划携带。
</verification>

<success_criteria>
- 28-01 三任务全部 `<done>` 达成且 verify 全绿
- git 提交存在（commit message 英文、conventional）
- Wave 2 计划（28-02/28-03）的前置资产全部就位：开关、编码器、batch、采样/配对/发射函数、events 通道门、脚本骨架
</success_criteria>

<output>
Create `.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-01-SUMMARY.md` when done
</output>