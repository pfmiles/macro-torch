---
phase: 28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac
plan: "02"
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/selftest.lua
autonomous: true
requirements: [D-03, D-05, D-06, D-07, D-08, D-09, R8]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  # spec-less probe skipped: phase has no requirement IDs (visible skip) — truths/prohibitions hand-derived from D-01..D-21 + R8
  truths:
    - "三技能 claw/shred/bite 的 cast 前采样结构同构（同一 cpDamageSample/cpDamageCast 对），bite 的 e=35 且 energyPool 为施法时刻能量池快照（D-08/D-09）"
    - "classes/druid/selftest.lua 含 Category U 注册 9 条，全部 isOptional=true 且处于 UnitClass('player') == 'Druid' 文件级守卫之内"
    - "未命中行不发射且不消费 intent（U-07 固化 D-03）；crit=true 随 crit 行落盘（U-08 固化 D-01 crit 可检测）"
    - "编码器/发射函数的 JSON 输出与 28-03 解码器互认（U-02/U-03 逐字断言固定 11 字段序）"
    - "obj.ferocious_bite 新增结构仅含 cpDamage 两行，不新增 cpBuild 采样（bite 从未属于 cpBuild 范围）"
  artifacts:
    - classes/druid/Druid.lua
    - classes/druid/selftest.lua
  key_links:
    - "cpDamageSample → cpDamageCast → loginContext.cpDamageIntents → events.lua 通道门 → onCpDamageLine → cpDamageEvent → macroTorch.log（链在 28-01 已证，本计划为三技能同构外扩）"
    - "Category U 桩测试 → fakeLoginContext/fakeTarget + rawget/rawset 还原纪律（Cat S-03/Q-02 先例）"
  prohibitions:
    - "不新增 cpBuild 行为到 bite（cpBuild 仅 claw/shred/rake 既有范围）— flagged-unverified"
    - "测试桩一律 rawget 快照 + 断言前还原，禁止泄漏影子字段到会话（CR-01 纪律）— flagged-unverified"
    - "不修改任何 catAtk 决策逻辑（决策文件零改动，R8）— flagged-unverified"
    - "未命中/非木桩/脱战不产生任何条目（D-03/D-05/D-11）— flagged-unverified"
---

<objective>
本计划为 tracer（28-01）之后的横向外扩之一：把 claw 已证的采样/发射结构同构铺到 shred 与 ferocious_bite（三技能全覆盖，D-07/D-08/D-09 的咬击特例），并在 classes/druid/selftest.lua 落地 Category U 9 条自测（开关默认值、编码器、发射逐字 JSON、配对窗、TTL 过期、未命中零发射、crit 标记、木桩硬门）。

Purpose: 采集端从「单路径证明」扩展为「三门技能齐备」，同时用游戏内 SelfTest 把本 phase 全部决策面（D-03/D-05/D-06/D-07/D-08/D-09）固化为登录可跑断言——编码器/发射的逐字 JSON 断言是 28-03 解码器互认契约的验证基础。
Output: Druid.lua 三技能采样全覆盖 + selftest.lua Category U（9 注册）。
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-01-PLAN.md   # 前置：开关/编码器/采样与配对函数定义均在本计划之前落地
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-PATTERNS.md   # §1 三件套骨架、§7 Category/桩纪律
@.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-RESEARCH.md   # §4 采样时机 WR-01 约束、§9 Category 字母现状
@classes/druid/Druid.lua        # 25-58 技能方法、328-355 cpBuildLogSample（28-01 已在此并排落 cpDamage 函数）
@classes/druid/selftest.lua     # 19 文件级守卫、745-884 Cat Q 桩纪律、987-1048 Cat S-03、1070-1096 Cat T-01 与收尾 end
@core/selftest.lua              # SelfTest:register API 34-40、isOptional 语义 49-89
@core/spell_trace_core.lua      # 28-01 后的 pairCpDamageIntent/onCpDamageLine/cpDamageEvent（测试对象本体）
@classes/druid/combo.lua        # 62 BITE_E=35（只读引用）
</context>

<tasks>

<task type="auto">
  <name>Task 1: shred + ferocious_bite 采样同构挂接（三技能全覆盖，D-07/D-08/D-09）</name>
  <files>classes/druid/Druid.lua</files>
  <read_first>
    - classes/druid/Druid.lua 25-58（obj.shred/obj.ferocious_bite 现状）与 328-355 附近（28-01 已落位的 cpDamageSample/cpDamageCast 定义，本任务零改动它们）
    - classes/druid/Druid.lua 501-509 与 642-645（computeClaw_E/computeShred_E）
    - classes/druid/combo.lua 57-65（BITE_E=35 权威出处）
    - 28-PATTERNS.md §1（claw 已挂两行的目标形态）
  </read_first>
  <action>    Druid.lua 三个技能方法的内容对账（28-01 后 claw 已是目标形态；本任务补齐其余两门）：

    1. obj.shred（34-41 行）：在其 `_castSpell` 调用之前追加 `local cpDmg = macroTorch.cpDamageSample('shred', macroTorch.computeShred_E())`；在既有 `if cast and cpLog and cpLog.gcdOk then ... cpBuildLogEvent ... end` 块之后追加 `if cast and cpDmg and cpDmg.gcdOk then macroTorch.cpDamageCast(cpDmg) end`。cpBuild 两行保持原样不动。
    2. obj.ferocious_bite（56-58 行）：现在是单行返回——改造为同构三件式：`_castSpell` 调用前 `local cpDmg = macroTorch.cpDamageSample('bite', 35)`；cast 之后 `if cast and cpDmg and cpDmg.gcdOk then macroTorch.cpDamageCast(cpDmg) end`。注意：bite 只加 cpDamage 结构，不新增任何 cpBuild 采样行（cpBuild 范围恒为 Claw/Shred/Rake，bite 从未属于其范围）。token 用 `'bite'`，能耗传字面量无函数引用 35（BITE_E 硬编码门槛常数，combo.lua:62）。
    3. 三技能回读自查：claw/shred/bite 三个方法内 `cpDamageSample` 与 `cpDamageCast` 的 token/energyCost 三元组依次为 `('claw', macroTorch.computeClaw_E())`、`('shred', macroTorch.computeShred_E())`、`('bite', 35)`——ids 与 D-07/D-08 口径逐字一致。energyPool/isOoc 由 28-01 的 cpDamageSample 统一快照（D-09 咬击必录，无需本任务额外代码）。
    4. 不触碰 cpBuildLogSample/cpDamageSample/cpDamageCast 函数体（28-01 已验收）；注释英文。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/Druid.lua && bash /home/admin/workspace/macro-torch/build.sh</automated>
    <fails_when>bbcheck 输出 MISMATCH（node 非零退出）；或 build.sh 打印 ERROR: File not found in build_order.txt 并非零退出</fails_when>
    <automated>grep -c 'macroTorch.cpDamageSample' /home/admin/workspace/macro-torch/SM_Extend.lua && grep -c 'macroTorch.cpDamageCast' /home/admin/workspace/macro-torch/SM_Extend.lua</automated>
    <fails_when>两输出不是 4（每次 = 1 定义 + 3 个技能方法调用点，共 4 处；少于 4 表示有技能未挂或定义丢失）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>打印任何 whitespace-error 行（退出码非零）</fails_when>
  </verify>
  <acceptance_criteria>
    - SM_Extend.lua 中 cpDamageSample 与 cpDamageCast 各出现 4 次（1 定义 + claw/shred/bite 3 调用点）
    - `grep "cpDamageSample('bite', 35)" classes/druid/Druid.lua` 在 obj.ferocious_bite 体内且位于 `_castSpell` 之前（WR-01 probe 先于 cast 约束）
    - `grep "cpDamageSample('shred', macroTorch.computeShred_E())" classes/druid/Druid.lua` 命中；obj.shred 的 cpBuild 两行原样未变
    - bite 内不存在 cpBuildLog 采样调用（D-06/D-07 范围钳制）；git diff 仅 Druid.lua 一个文件
  </acceptance_criteria>
  <done>三技能（claw/shred/bite）cast 前采样与 intent 种植同构齐备；energyCost 三元组 `computeClaw_E() / computeShred_E() / 35` 逐字正确（D-08）；bite 的 energyPool/isOoc 经统一快照满足 D-09。</done>
</task>

<task type="auto">
  <name>Task 2: Category U SelfTest 9 条注册（U-01..U-09，D-03/D-05/D-06/D-07 断言固化）</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua 全文件重点：19（UnitClass 门）、745-884（Cat Q-02/Q-04 fakeLoginContext/fakeTarget + pcall 桩纪律）、987-1048（Cat S-03 rawget 快照/还原全流程）、1070-1096（Cat T-01 与收尾 end 结构）
    - core/selftest.lua 34-89（register/run 语义）
    - classes/druid/Druid.lua 25-58（本计划的 Task 1 后的技能方法形态，测试对象）
    - core/spell_trace_core.lua 中 28-01 落位的 pairCpDamageIntent/onCpDamageLine/cpDamageEvent（测试对象本体）
    - impl_util.lua 中 28-01 落位的 jsonEncodeScalar
    - 28-RESEARCH.md §3（编码器断言口径）与 §9（Category U 现状：O/Q/R/S/T 已占用）
  </read_first>
  <action>    在 classes/druid/selftest.lua 末尾（Cat T-01 注册之后、文件收尾 `end` 之前）插入 Category U 注册段，标题注释格式照抄既有「Category T: ...」块。全部注册 `isOptional = true`；命名 `"Cat U-0N: <desc>"`（N=1..9）。每条独立，失败仅单条。

    - U-01 开关默认值（纯函数）：`assert(macroTorch.cpDamageLog == false, ...)`，镜像 Cat T-01 的 trade-off 注释（session 内被 /run 覆盖时失败属预期且具信息量）。
    - U-02 编码器字面量（纯函数，无游戏 API）：jsonEncodeScalar 输入 nil/true/42/`he said "hi"\path` 这类含引号与反斜杠的串 → 断言输出逐字等于 `null`/`true`/`42`/含转义 `\"` 与 `\\` 的期望串（期望串由执行者按 28-01 编码器实现推导并亲手写出，写死进断言）。
    - U-03 发射 JSON（纯逻辑 + 桩 macroTorch.log）：构造固定 sample 表（`spell='claw', t=1234.5, cp=2, energyPool=60, bleedCount=1, isOoc=false, isBehind=true, e=42, batch=1000`）调 cpDamageEvent(sample, 210, false)，断言捕获的 exactly 1 条且逐字等于 `[cpDamage] {"spell":"claw","dmg":210,...,"batch":1000}` 的 11 字段固定序字面量（期望串同样亲手写死）。
    - U-04 配对窗内命中：fake loginContext + `cpDamageIntents = macroTorch.LRUStack:new(8)`，push `{ spell='claw', guid='0xABC...F00', castAt=1.0, sample=<引用表> }`；pairCpDamageIntent('0xABC...F00', 1.5) 断言返回该 sample 且栈空（elements 计数 0——用 ipairs 计数，禁 `#`）。
    - U-05 配对 TTL 过期：同 U-04 种子但 pair 于 now=5.0（超 LAND_INTENT_TTL）→ 返回 nil 且栈空（purge 已清）。
    - U-06 hits 行端到端：临时置 cpDamageLog=true（CR-01 savedSwitch 快照/还原）+ 桩 macroTorch.log 捕获 + 种子 intent（castAt=GetTime()）→ onCpDamageLine('Your Claw hits <同 guid> for 200.', GetTime()) → 断言捕获 exactly 1 条且 string.find 命中 `"crit":false`。
    - U-07 未命中零发射（D-03）：存 intent → onCpDamageLine('Your Claw was dodged by <同 guid>.', GetTime()) → 断言捕获 0 条且 intent 栈仍有 1 元素（意图不被未命中消费）。
    - U-08 crit 标记：如 U-06 但行文本 hits→crits → 捕获 1 条且 `"crit":true` 命中。
    - U-09 木桩硬门（D-05）+ 非木桩拒收：fake target1 = `{ name='Training Dummy', isCanAttack=true, guid='0xTEST', hasBuff=function() return false end }` + fake context `_cpDamageBatch=GetTime()` + 桩 `player._castSpell=function() return true end`、`player.isActionCooledDown=function() return true end`（rawget 快照/rawset 还原，Cat S-03 纪律）→ `player.claw('ready')` 后断言 loginContext.cpDamageIntents 非空且 1 元素；再 fake target2 改 name='QTestMob'（其余同）→ 重跑 → 断言栈无新增（or 计数 0）。桩完成后全部还原（含 macroTorch.loginContext、macroTorch.target、cpDamageLog、macroTorch.log、_castSpell、isActionCooledDown）。
    每条测试 pcall 包裹断言过程；桩还原放在 pcall 之外 finally 式位置（断言前还原，Cat S-03 纪律原样）。末尾更新「Registration count」注释（Category U adds 9 tests, quick 260908...）。commit message 英文。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/selftest.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH 因测试段括号不平衡（node 非零退出）；或 diff --check 打印 whitespace-error 行</fails_when>
    <automated>grep -c -v -- '--' /home/admin/workspace/macro-torch/classes/druid/selftest.lua | head -1; grep -c 'SelfTest:register("Cat U-0' /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>第二个 grep 输出不是 9（U-01..U-09 恰 9 条注册；0 或 <9 表示注册段缺失）。首个 head -1 仅供行数参照，不计成败</fails_when>
    <automated>bash /home/admin/workspace/macro-torch/build.sh && grep -c 'Cat U-0' /home/admin/workspace/macro-torch/SM_Extend.lua</automated>
    <fails_when>build.sh 非零退出；或产物 grep 输出不是 9（注册被构建链丢失）</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SelfTest:register("Cat U-0' classes/druid/selftest.lua` 输出 9，且全部位于 `if UnitClass('player') == 'Druid' then` 守卫内、Cat T-01 之后、收尾 end 之前
    - 每条注册第三参 true（isOptional）；Cat U 段注释块存在
    - U-03 断言含 11 键固定序完整字面量；U-09 断言含 'Training Dummy' 与非木桩两条路径
    - SM_Extend.lua 中 Cat U 注册数为 9；既有 Cat Q/S/T 计数值不变（未误删旧注册）
  </acceptance_criteria>
  <done>Category U 9 条注册落地并进入构建产物：默认值、编码、发射 JSON 逐字、配对窗、TTL、未命中零发射、crit、木桩硬门八面断言全齐；桩/影子一律快照与还原（CR-01 纪律）；测试仅读不改任何生产函数。</done>
</task>

</tasks>

<threat_model>## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 游戏内 SelfTest 运行沙盒 → 会话全局 | 测试临时装桩（_castSpell/log/target 影子），泄露即污染会话 |
| RAW_COMBATLOG 事件流 → addon | 三技能采样与配对（28-01 已建模） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-06 | Tampering | selftest 桩/影子字段泄漏到会话 | low | mitigate | 全部桩经 rawget 快照 + 断言前 rawset/还原，断言未还原不得提交（Cat S-03/CR-01 纪律，P-02 先例） |
| T-28-04 | Spoofing | 环内 `[cpDamage]` 相似文本 | low | mitigate | 前缀精确匹配 + decode 成功双门槛（28-01 已落地，U-03 固化发射侧契约） |
| T-28-05 | DoS | 持久化环与 rawdiag2 同开挤爆 | low | accept | LOG_MAX_SIZE 用户自调（D-10）；28-04 UAT 文档提醒勿同开 |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | 本 phase 无任何包管理器安装 |

配置：ASVS level 1（block on high severity）——无 high 级威胁，无阻塞护栏触发。
</threat_model>

## Artifacts this phase produces (本 phase 产出清单 — 28-02 部分)

| 符号 / 文件 | 类型 | 定义处 | 说明 |
|---|---|---|---|
| obj.shred 挂接 | 技能方法扩展 | classes/druid/Druid.lua | `cpDamageSample('shred', macroTorch.computeShred_E())` + `cpDamageCast`（cpBuild 原样保留） |
| obj.ferocious_bite 挂接 | 技能方法扩展 | classes/druid/Druid.lua | `cpDamageSample('bite', 35)` + `cpDamageCast`；首次获得采样结构，但不含 cpBuild（范围钳制） |
| Category U 注册段 | SelfTest | classes/druid/selftest.lua | `Cat U-01..U-09`，isOptional=true；覆盖 9 项断言面 |
| U-03 期望字面量 | 契约固化 | classes/druid/selftest.lua | 11 字段固定序 JSON 逐字断言（编码/解码互认契约的验证基础，供 28-03 解码器对照） |

<verification>本计划自己的验证锚：三技能挂接计数（产物 4=1 定义+3 调用点）+ Category U 注册计数 9 + 旧 Category 计数不变（无删减回归）+ bbcheck/diff --check。</verification>

<success_criteria>
- Task 1/2 全部 `<done>` 达成且 verify 全绿
- Druid.lua 三技能采样齐备；selftest.lua Category U 恰 9 条并进入产物
- 为 28-04 的最终电池提供两枚计数锚（cpDamageSample×4、Cat U×9）
</success_criteria>

<output>
Create `.planning/phases/28-cat-druid-claw-shred-bite-claw-shred-bite-catatk-catatk-mac/28-02-SUMMARY.md` when done
</output>