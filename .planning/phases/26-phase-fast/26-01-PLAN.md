---
phase: 26-phase-fast
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/combo.lua
  - classes/druid/cat.lua
  - core/selftest.lua
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - macroTorch.isFastBattleNotPvp(clickContext) exists in classes/druid/Druid.lua and returns boolean (D-01)
    - fast battle (willDieInSeconds(8.5) true, not PvP) returns true; PvP target always returns false (D-01)
    - fast==true implies isTrivialBattle==true (8.5 < 25, same conditions + PvP exclusion) (code_context)
    - fast battle: opener never casts Pounce, falls through to Ravage when prowling (D-03)
    - fast battle: no Rip is ever cast — keepRip/quickKeepRip skipped AND keepRake/energyDischargeBeforeBite guard against Rake (D-04, D-05, D-06)
    - fast battle: 5 combo points triggers Bite even with no Rip and no Rip immunity (D-08)
    - normal (non-fast) battle behavior is byte-identical to pre-phase logic: every new guard is additive (D-09)
    - catLeveling function body is untouched (D-13)
    - 2 Category P SelfTest registrations with isOptional=true exist in core/selftest.lua (D-12 items 1-2 of 6; remaining 4 in 26-02)
  artifacts:
    - macroTorch.isFastBattleNotPvp function in classes/druid/Druid.lua placed immediately after isTrivialBattle (Druid.lua ~805)
    - Modified Pounce-opener condition in macroTorch.catAtk (combo.lua:137)
    - Modified Rip branch in macroTorch.catAtk (combo.lua:160-167)
    - Modified regularAttack precondition in macroTorch.catAtk (combo.lua:173)
    - Modified comboPoints==5 condition in macroTorch.cp5Bite (cat.lua:115)
    - Modified Rake-discharge fallback in macroTorch.energyDischargeBeforeBite (cat.lua:159)
    - Modified guard chain in macroTorch.keepRake (cat.lua:332)
    - 2 Category P SelfTest registrations (P-01, P-02) appended to core/selftest.lua
  key_links:
    - isFastBattleNotPvp is called at combo.lua:137 (opener), combo.lua:160 (Rip branch), combo.lua:173 (regularAttack), cat.lua:115 (cp5Bite), cat.lua:159 (energyDischarge), cat.lua:332 (keepRake)
    - isFastBattleNotPvp reuses macroTorch.target.willDieInSeconds (entity/Target.lua:86) and macroTorch.estimatePlayerDPS, exactly like isTrivialBattle (Druid.lua:794)
    - clickContext lazy cache pattern identical to isTrivialBattle (Druid.lua:795) — recomputed each frame because clickContext is rebuilt in catAtk (combo.lua:55) per D-11
    - SelfTest:register(name, fn, isOptional) API at core/selftest.lua:34
---

<objective>
Implement the cat druid "fast battle" logic: a per-frame judgment `macroTorch.isFastBattleNotPvp(clickContext)` (8.5s death prediction, non-PvP) that switches catAtk into a pure-direct-damage strategy — skip all bleeds (Pounce/Rake/Rip), build combo points with Shred (behind) / Claw (front) to 5, then Bite or KillShot.

**Purpose:** For weak mobs dying within 8.5s, bleed skills (Rake 9s / Rip 12s durations) cannot tick enough to repay their energy; every point of energy converts to burst damage instead. D-01 through D-13 from 26-CONTEXT.md.

**Output:** New function in Druid.lua, 7 additive guard modifications across combo.lua/cat.lua, 2 Category P SelfTest registrations (P-01, P-02) in core/selftest.lua (4 more Category P tests in 26-02). Build must succeed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-phase-fast/26-CONTEXT.md

# Source files to modify
@classes/druid/Druid.lua
@classes/druid/combo.lua
@classes/druid/cat.lua
@core/selftest.lua

# Reference implementations (read, do NOT modify)
@entity/Target.lua          # willDieInSeconds (86-98), maintainTHV/currentHRPS (114-158)
</context>

<tasks>

<task type="tracer">
  <name>End-to-end fast battle path — isFastBattleNotPvp function + cp5Bite 5CP trigger</name>
  <reversibility rating="reversible">New standalone judgment function plus one additive condition in cp5Bite; both revertible by single-edit rollback without touching shared functions.</reversibility>
  <files>classes/druid/Druid.lua, classes/druid/cat.lua</files>
  <read_first>
    - classes/druid/Druid.lua lines 788-851: isTrivialBattleOrPvp, isTrivialBattle (dual-condition + lazy cache pattern to replicate), isKillShotOrLastChance (reference for HRPS-first judgment)
    - entity/Target.lua lines 86-98: willDieInSeconds signature and return semantics
    - classes/druid/cat.lua lines 104-143: termMod / cp5Bite current state
    - classes/druid/combo.lua lines 49-113: clickContext construction in catAtk (where per-frame fields are populated)
    - core/selftest.lua lines 653-707: Category M registration pattern (UnitClass guard, isOptional=true, cite comment)
  </read_first>
  <action>
**Part A — Add isFastBattleNotPvp to Druid.lua (D-01, D-02, D-11):**

Insert a new function immediately after the closing `end` of macroTorch.isTrivialBattle (after Druid.lua line 805, before `function macroTorch.combatUrgentHPRestore`). Required structure, mirroring isTrivialBattle exactly:

1. Signature: `function macroTorch.isFastBattleNotPvp(clickContext)`
2. First line: PvP exclusion — `if macroTorch.target.isPlayerControlled then return false end` (D-01: 排除 PvP; deliberately checked BEFORE the cache so a PvP player target can never cache true)
3. Lazy cache per D-11: `if clickContext.isFastBattleNotPvp == nil then ... end` wrapping the computation of the local fastDieTime = 8.5 dual condition
4. Dual condition inside the cache block, copying the two arms of isTrivialBattle but with 8.5 instead of 25:
   - Condition A (HRPS): `macroTorch.target.willDieInSeconds(localFastDieTime)`
   - Condition B (health estimate): `macroTorch.target.healthMax <= (macroTorch.player.mateNearMyTargetCount + 1) * macroTorch.estimatePlayerDPS() * localFastDieTime`
5. Assign `clickContext.isFastBattleNotPvp = (A or B)` and return it after the if-nil block
6. Comment block in English above the function explaining the economics: 8.5s threshold is below Rake's 9s duration, so bleeds cannot repay; pure direct damage is optimal. Cite D-01/D-02.
7. Use a local variable named localFastDieTime for the threshold (do NOT name it trivialDieTime — that name belongs to isTrivialBattle's 25s threshold and grep checks must not collide)
8. Threshold value appears as the numeric literal 8.5 exactly once in this function

**Part B — Arm cp5Bite for fast battles (D-08):**

In macroTorch.cp5Bite (cat.lua line 115), change the trigger condition:

- Old: `if clickContext.comboPoints == 5 and (clickContext.isImmuneRip or macroTorch.isRipPresent(clickContext)) then`
- New: `if clickContext.comboPoints == 5 and (clickContext.isImmuneRip or macroTorch.isRipPresent(clickContext) or macroTorch.isFastBattleNotPvp(clickContext)) then`

Add a one-line English comment above citing D-08 explaining: in fast battles Rip is never cast, so isRipPresent/isImmuneRip are both false and 5CP would never bite without this clause. No other lines of cp5Bite body change (the discharge check at cat.lua:128 stays isRipPresent-only — a fast battle without Rip naturally skips it).

**Part C — Register 2 tracer SelfTests in core/selftest.lua (D-12 items 1 and 2):**

Append a new section after Category M (before `-- Module 4: /mt SLASH command`) with header `-- Category P — Phase 26 fast-battle judgment (2 tests in this task, 4 more in 26-02-PLAN.md)` plus cite comment `-- [CITED: 26-CONTEXT.md D-12]`. The header uses Category P (letter P is unused in core/selftest.lua; avoids collision with Phase 23's "Category O: Idol Dance" section in classes/druid/selftest.lua:667) and deliberately omits the substring "P: fast battle" so that the 26-02 phase grep `grep -c "P: isFastBattleNotPvp\|P: fast battle\|P: cp5Bite" core/selftest.lua` counts exactly the 6 test-name lines. Register:

- P-01 "P: isFastBattleNotPvp is a function" — UnitClass guard `if UnitClass('player') ~= 'Druid' then return end`, assert `type(macroTorch.isFastBattleNotPvp) == "function"` (D-12 item 1)
- P-02 "P: isFastBattleNotPvp returns false for PvP target (player-controlled)" (D-12 item 2) — same non-Druid guard as P-01, then stub/restore discipline identical to P-03~P-06 in 26-02: save `local origPvp = macroTorch.target.isPlayerControlled`, set `macroTorch.target.isPlayerControlled = true`, then inside pcall: `local ctx = {}; local verdict = macroTorch.isFastBattleNotPvp(ctx); ok = (verdict == false and ctx.isFastBattleNotPvp == nil)` — the false verdict proves the PvP exclusion, and the nil-cache half proves the PvP-first guard fires before the lazy cache (D-01 ordering); then restore `macroTorch.target.isPlayerControlled = origPvp` BEFORE the asserts, then `assert(pcallRes)` and `assert(ok)`. isPlayerControlled is a plain boolean field on macroTorch.target (read at Druid.lua:789/821), so stubbing and restoring it is exact; restoring before asserting means a stumbled call still cannot poison later tests.

Both registered with third argument `true` (isOptional=true per D-12).
  </action>
  <verify>
    <automated>grep -c "function macroTorch.isFastBattleNotPvp" classes/druid/Druid.lua returns exactly 1; grep "localFastDieTime = 8.5" classes/druid/Druid.lua returns exactly 1</automated>
    <automated>grep "isFastBattleNotPvp" classes/druid/cat.lua returns exactly 1 match on the new cp5Bite condition line: grep "or macroTorch.isFastBattleNotPvp(clickContext)" classes/druid/cat.lua returns 1</automated>
    <automated>grep -c "P: isFastBattleNotPvp" core/selftest.lua returns 2</automated>
    <automated>./build.sh && echo "Build OK" succeeds</automated>
  </verify>
  <acceptance_criteria>
    - isFastBattleNotPvp defined exactly once in Druid.lua, positioned after isTrivialBattle's closing end and before combatUrgentHPRestore (D-01)
    - PvP exclusion (macroTorch.target.isPlayerControlled) is the first statement of the function, before the cache (D-01)
    - Lazy cache uses clickContext.isFastBattleNotPvp nil-check pattern identical to isTrivialBattle (D-11)
    - was no anti-jitter added anywhere (D-10)
    - Dual condition mirrors isTrivialBattle arms with localFastDieTime = 8.5 exactly once (D-02)
    - shouldUseShred, shouldCastRip, shouldUseBite, isTrivialBattle, isKillShotOrLastChance bodies unchanged — `git diff` on Druid.lua shows only the new function inserted (D-09)
    - cp5Bite condition line 115 now ends with `or macroTorch.isFastBattleNotPvp(clickContext)`; body otherwise unchanged (D-08)
    - 2 Category P SelfTest registrations (P-01, P-02) with isOptional=true appended before Module 4 header (D-12)
    - Build succeeds (./build.sh exits 0)
  </acceptance_criteria>
  <done>
isFastBattleNotPvp exists with PvP-first guard and lazy 8.5s dual-condition cache; cp5Bite triggers Bite at 5CP in fast battles; 2 tracer SelfTests registered; build passes. The fast-battle detection-to-kill path is wired and committed.
  </done>
</task>

<task type="auto">
  <name>Arm catAtk main flow — opener Pounce skip, Rip branch bypass, regularAttack precondition (combo.lua)</name>
  <reversibility rating="reversible">Three additive guard edits inside one function; each revertible independently without touching shared decision functions.</reversibility>
  <files>classes/druid/combo.lua</files>
  <read_first>
    - classes/druid/combo.lua lines 120-181: opener block (136-147), module 10 debuffMod block (160-171), module 11 regularAttack block (172-175)
    - classes/druid/Druid.lua lines 788-812: post-Task-1 state of isFastBattleNotPvp (must exist before this task runs)
    - classes/druid/cat.lua lines 307-345: quickKeepRip / keepRake (context for what the Rip bypass skips)
  </read_first>
  <action>
**Part A — Opener Pounce skip (D-03, combo.lua:137):**

In macroTorch.catAtk, modify the Pounce condition (currently combo.lua line 137):

- Old: `if hasPounce and not target.isImmune('Pounce') and target.health >= macroTorch.getOpenerHealthThreshold() then`
- New: `if hasPounce and not target.isImmune('Pounce') and target.health >= macroTorch.getOpenerHealthThreshold() and not macroTorch.isFastBattleNotPvp(clickContext) then`

Add a short English comment above citing D-03: fast battles skip the Pounce bleed opener and fall through to `elseif hasRavage` for direct-damage Ravage. Do not modify the elseif hasRavage branch (combo.lua:144-146) — fast battle lands there naturally.

**Part B — Rip branch bypass (D-04, combo.lua:160-167):**

In the module 10 debuffMod block, wrap the existing two-branch structure:

- Old:
  - `if macroTorch.isTrivialBattleOrPvp(clickContext) then macroTorch.quickKeepRip(clickContext) else macroTorch.keepRip(clickContext) end`
- New: wrap the whole existing if/else inside `if not macroTorch.isFastBattleNotPvp(clickContext) then ... end` so that in fast battles neither keepRip nor quickKeepRip executes

Add a short English comment above the new wrapper citing D-04. The inner if/else structure (isTrivialBattleOrPvp → quickKeepRip, else keepRip) stays byte-identical. Comment numbering of modules 10/11 does not change.

**Part C — regularAttack precondition (D-07, combo.lua:173):**

Modify the module 11 condition:

- Old: `if macroTorch.isFightStarted(clickContext) and clickContext.comboPoints < 5 and (macroTorch.isRakePresent(clickContext) or clickContext.isImmuneRake) then`
- New: `if macroTorch.isFightStarted(clickContext) and clickContext.comboPoints < 5 and (macroTorch.isRakePresent(clickContext) or clickContext.isImmuneRake or macroTorch.isFastBattleNotPvp(clickContext)) then`

Add a short English comment above citing D-07: in fast battles Rake is absent and the target is not immune, so without this clause the main combo-point builder would never run. The rest of module 11 (call to regularAttack) stays unchanged.
  </action>
  <verify>
    <automated>grep "not macroTorch.isFastBattleNotPvp(clickContext)" classes/druid/combo.lua returns exactly 2 (opener Pounce condition + Rip-branch wrapper)</automated>
    <automated>grep "or macroTorch.isFastBattleNotPvp(clickContext)" classes/druid/combo.lua returns exactly 1 (regularAttack precondition)</automated>
    <automated>grep -c "isFastBattleNotPvp" classes/druid/combo.lua returns exactly 3</automated>
    <automated>./build.sh && echo "Build OK" succeeds</automated>
  </verify>
  <acceptance_criteria>
    - Pounce opener condition (combo.lua:137) contains `and not macroTorch.isFastBattleNotPvp(clickContext)` as its last conjunct (D-03)
    - Ravage elseif branch at combo.lua:144-146 unchanged (D-03)
    - Rip strategy block (combo.lua:160-167) fully wrapped in `if not macroTorch.isFastBattleNotPvp(clickContext) then ... end` with inner isTrivialBattleOrPvp quick/keep split intact (D-04)
    - regularAttack condition (combo.lua:173) contains `or macroTorch.isFastBattleNotPvp(clickContext)` inside the parenthesized bleed group (D-07)
    - catLeveling() body (classes/druid/leveling.lua:23; moved out of combo.lua in Phase 16) untouched (D-13)
    - git diff --stat shows only classes/druid/combo.lua changed in this task's commit
    - Build succeeds (./build.sh exits 0)
  </acceptance_criteria>
  <done>
catAtk skips Pounce and all Rip in fast battles, and regularAttack runs without Rake present. catLeveling untouched. Build passes.
  </done>
</task>

<task type="auto">
  <name>Close remaining bleed leaks — keepRake guard + energyDischargeBeforeBite (cat.lua)</name>
  <reversibility rating="reversible">Two additive guard edits in cat.lua; single-edit rollback each.</reversibility>
  <files>classes/druid/cat.lua</files>
  <read_first>
    - classes/druid/cat.lua lines 144-162: energyDischargeBeforeBite current state (post-Task-1 diff must show cp5Bite edit only)
    - classes/druid/cat.lua lines 326-345: keepRake current state
    - classes/druid/Druid.lua: confirm isFastBattleNotPvp exists (Task 1 artifact)
  </read_first>
  <action>
**Part A — keepRake guard (D-05, cat.lua:332):**

In macroTorch.keepRake, append `or macroTorch.isFastBattleNotPvp(clickContext)` to the end of the existing guard `or`-chain:

- Old: `if not macroTorch.isFightStarted(clickContext) or clickContext.comboPoints == 5 or macroTorch.isRakePresent(clickContext) or clickContext.isImmuneRake or macroTorch.isKillShotOrLastChance(clickContext) then`
- New: same chain plus ` or macroTorch.isFastBattleNotPvp(clickContext)` appended at the end

Add a short English comment above the guard citing D-05: fast battles never apply Rake. No other line of keepRake changes — especially keep the [SIDE EFFECT] block (cat.lua:335-343) and the final `macroTorch.safeRake(clickContext)` call.

**Part B — energyDischargeBeforeBite Rake fallback (D-06, cat.lua:159):**

In macroTorch.energyDischargeBeforeBite, modify the Rake-discharge fallback:

- Old: `if not macroTorch.isRakePresent(clickContext) and macroTorch.player.mana >= clickContext.BITE_E + clickContext.RAKE_E then`
- New: `if not macroTorch.isRakePresent(clickContext) and not macroTorch.isFastBattleNotPvp(clickContext) and macroTorch.player.mana >= clickContext.BITE_E + clickContext.RAKE_E then`

Add a short English comment above citing D-06: in fast battles discharge energy with Shred/Claw only — the first regularAttack branch (cat.lua:151-156) still runs because it has an energy safety valve, but the Rake fallback is forbidden. Never alter the first branch of the function.
  </action>
  <verify>
    <automated>grep "isFastBattleNotPvp" classes/druid/cat.lua returns exactly 3 in total (cp5Bite from Task 1 + keepRake + energyDischargeBeforeBite)</automated>
    <automated>grep "or macroTorch.isFastBattleNotPvp(clickContext)" classes/druid/cat.lua returns exactly 2 (cp5Bite, keepRake)</automated>
    <automated>grep "and not macroTorch.isFastBattleNotPvp(clickContext)" classes/druid/cat.lua returns exactly 1 (energyDischargeBeforeBite Rake fallback)</automated>
    <automated>./build.sh && echo "Build OK" succeeds</automated>
  </verify>
  <acceptance_criteria>
    - keepRake guard at cat.lua:332 now ends with `or macroTorch.isFastBattleNotPvp(clickContext)` (D-05)
    - energyDischargeBeforeBite Rake fallback at cat.lua:159 contains `not macroTorch.isFastBattleNotPvp(clickContext)` (D-06)
    - regularAttack-discharge first branch (cat.lua:151-156) unchanged (D-06)
    - shouldDoReshift, shouldCastRip and quickKeepRip bodies unchanged — git diff on cat.lua shows only cp5Bite (Task 1), keepRake and energyDischargeBeforeBite edits (D-09)
    - git diff --stat shows only classes/druid/cat.lua changed in this task's commit
    - Build succeeds (./build.sh exits 0)
  </acceptance_criteria>
  <done>
No Rake can be applied through any catAtk path in a fast battle: keepRake blocked, energyDischarge Rake fallback blocked. Build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client API → macro_torch Lua | Untrusted game-state inputs (target health, HRPS samples, combat flags) read via read-only Unit* APIs |
| config.json / build inputs | Local files only; generated SM_Extend.lua assembled by build.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Spoofing | isFastBattleNotPvp HRPS input (currentHRPS linear-regression from maintainTHV samples) | low | accept | willDieInSeconds already requires >= 2 samples (returns 0 → false → conservative); purely additive gameplay heuristic with no auth or state-safety impact |
| T-26-02 | Tampering | build.sh output SM_Extend.lua | low | accept | Local trusted inputs; no package manager involved; build verified via ./build.sh after every task |
| T-26-03 | DoS | Lazy cache recomputation in clickContext each frame | low | accept | Same per-frame pattern already used by isTrivialBattle/isKillShotOrLastChance; one extra boolean field + two existing function calls per click |
| T-26-SC | Tampering | npm/pip/cargo installs | high | not-applicable | No package-manager install in this phase (pure Lua, no new dependencies) |
</threat_model>

<verification>
**Overall phase verification:**

1. Build: `./build.sh && echo "Build OK"` succeeds
2. Total isFastBattleNotPvp call sites: 6 — combo.lua (3: opener, Rip wrapper, regularAttack) + cat.lua (3: cp5Bite, keepRake, energyDischargeBeforeBite)
3. `grep -c "isFastBattleNotPvp" classes/druid/Druid.lua classes/druid/combo.lua classes/druid/cat.lua` == 10 (1 definition + 3 references in combo + 3 in cat + 3 in the Druid.lua function body's own lines)
4. Normal-battle equivalence: every new guard is additive (and/or-append) or a wrapping if — no pre-existing branch condition was removed or reordered (D-09)
5. catLeveling integrity: `git diff` shows no hunk inside macroTorch.catLeveling (classes/druid/leveling.lua:23) (D-13)
6. No anti-jitter: diff contains no new timer/debounce construct (D-10)
7. Threshold: `grep -c "localFastDieTime = 8.5" classes/druid/Druid.lua` == 1 (D-02)
8. Files touched: Druid.lua, combo.lua, cat.lua, core/selftest.lua only
</verification>

<success_criteria>
- macroTorch.isFastBattleNotPvp defined once with PvP-first guard and lazy 8.5s dual-condition cache (D-01/D-02/D-11)
- All 7 modification points applied exactly as specified: combo.lua:137/160-167/173, cat.lua:115/159/332 (D-03 through D-08)
- Shared decision functions (shouldUseShred/shouldUseBite/shouldCastRip) untouched; catLeveling untouched (D-09/D-13)
- 2 Category P tracer SelfTests registered with isOptional=true (D-12 partial; remaining 4 in 26-02)
- Build succeeds and SM_Extend.lua contains the new symbol
- No new dependencies installed
</success_criteria>

<output>
Create .planning/phases/26-phase-fast/26-01-SUMMARY.md when done
</output>

<!-- Artifacts this phase produces -->
<section id="artifacts">
## Artifacts this phase produces

Symbols / code values created in Plan 26-01:

| Kind | Identifier | Location |
|------|-----------|----------|
| function | `macroTorch.isFastBattleNotPvp(clickContext)` | classes/druid/Druid.lua (after isTrivialBattle) |
| local var | `localFastDieTime` (= 8.5) inside isFastBattleNotPvp | classes/druid/Druid.lua |
| field | `clickContext.isFastBattleNotPvp` (lazy cache) | set in Druid.lua, read at 6 call sites |
| condition | `and not macroTorch.isFastBattleNotPvp(clickContext)` — opener Pounce | classes/druid/combo.lua:137 |
| condition | `if not macroTorch.isFastBattleNotPvp(clickContext) then ... end` — Rip wrapper | classes/druid/combo.lua:160-167 |
| condition | `or macroTorch.isFastBattleNotPvp(clickContext)` — regularAttack precondition | classes/druid/combo.lua:173 |
| condition | `or macroTorch.isFastBattleNotPvp(clickContext)` — cp5Bite 5CP trigger | classes/druid/cat.lua:115 |
| condition | `and not macroTorch.isFastBattleNotPvp(clickContext)` — energyDischarge Rake fallback | classes/druid/cat.lua:159 |
| condition | `or macroTorch.isFastBattleNotPvp(clickContext)` — keepRake guard | classes/druid/cat.lua:332 |
| SelfTest | "P: isFastBattleNotPvp is a function" (isOptional=true) | core/selftest.lua Category P |
| SelfTest | "P: isFastBattleNotPvp returns false for PvP target (player-controlled)" (isOptional=true) | core/selftest.lua Category P |
| comment | Category P section header + `-- [CITED: 26-CONTEXT.md D-12]` | core/selftest.lua |

Plan 26-02 (wave 2) adds the remaining 4 Category P SelfTests (D-12 items 3-6): HRPS path, health-estimate path, fast⇒trivial priority relation, cp5Bite behavior regression.
</section>