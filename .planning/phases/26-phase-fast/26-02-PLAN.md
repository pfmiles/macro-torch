---
phase: 26-phase-fast
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - core/selftest.lua
autonomous: true
requirements:
  - D-09
  - D-10
  - D-12
  - D-13
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Category O in core/selftest.lua holds exactly 6 registrations, all isOptional=true (D-12)
    - HRPS stub path: willDieInSeconds(8.5)=true drives isFastBattleNotPvp to true (D-12 item 3)
    - Health-estimate stub path: huge estimatePlayerDPS drives isFastBattleNotPvp to true with willDieInSeconds stubbed false (D-12 item 4)
    - Priority relation: under identical stubs isFastBattleNotPvp and isTrivialBattle return the same verdict (D-12 item 5, code_context)
    - cp5Bite regression: fast battle + 5CP + no Rip + no immunity reaches safeBite with real cast chain stubbed out (D-12 item 6, D-08)
    - All 6 SelfTests carry UnitClass('player') ~= 'Druid' guard or equivalent so non-Druid logins skip cleanly
    - catLeveling body unchanged and no new jitter constructs anywhere in the phase diff (D-13, D-10)
  artifacts:
    - 4 additional Category O SelfTest registrations appended to core/selftest.lua (O-03 through O-06)
    - Stub/restore helper pattern (local orig + pcall + restore before assert) used in every stubbing test
  key_links:
    - Stub targets are global functions: macroTorch.target.willDieInSeconds (entity/Target.lua:86, instance field), macroTorch.estimatePlayerDPS (Druid.lua:513), macroTorch.isRipPresent (Druid.lua:1059), macroTorch.safeBite (cat.lua:397), macroTorch.readyBite (cat.lua:400), macroTorch.energyDischargeBeforeBite (cat.lua:144)
    - SelfTest:run executes registrations in order at PLAYER_ENTERING_WORLD; every stub must be restored before any assert fires, otherwise a failed test poisons all later tests
---

<objective>
Complete Phase 26 SelfTest coverage: register the remaining 4 Category O tests (D-12 items 3-6) in core/selftest.lua, verifying the HRPS path, the health-estimate path, the fast⇒trivial priority relation, and the cp5Bite 5CP behavioral regression — then run the final phase-wide verification (D-09, D-10, D-13 negative confirmations).

**Purpose:** The judgment logic ships in Plan 01; this plan proves every decision path of isFastBattleNotPvp and the one behavior-bearing edit (cp5Bite) are observable in /mt self-test, without any game-side cast side effects.

**Output:** core/selftest.lua with 6 total Category O registrations, all stubs restored safely, build succeeds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-phase-fast/26-CONTEXT.md
@.planning/phases/26-phase-fast/26-01-PLAN.md    # produces isFastBattleNotPvp + 2 Category O tests (O-01, O-02)
@.planning/phases/26-phase-fast/26-01-SUMMARY.md  # read if present: what 26-01 actually committed

# Source files to modify
@core/selftest.lua

# Stub targets (read to confirm signatures, do NOT modify)
@entity/Target.lua            # willDieInSeconds instance field at line 86
@classes/druid/Druid.lua      # estimatePlayerDPS at 513, isRipPresent at 1059, isFastBattleNotPvp after 805
@classes/druid/cat.lua        # safeBite 397, readyBite 400, energyDischargeBeforeBite 144, cp5Bite 113-143
</context>

<tasks>

<task type="auto">
  <name>Register judgment-path SelfTests O-03/O-04/O-05 (HRPS, health estimate, priority relation)</name>
  <reversibility rating="reversible">Test-code-only addition to core/selftest.lua; delete-if-broken rollback.</reversibility>
  <files>core/selftest.lua</files>
  <read_first>
    - core/selftest.lua lines 1-98: SelfTest:register signature (name, fn, isOptional) and runner semantics
    - core/selftest.lua lines 653-707: Category M registrations (UnitClass guard pattern, isOptional=true) and Category O section added by 26-01 Task 1 (O-01, O-02)
    - classes/druid/Druid.lua lines 788-825: isTrivialBattle + isFastBattleNotPvp final bodies (both dual-condition arms)
    - entity/Target.lua lines 86-98: willDieInSeconds signature — instance field, called as macroTorch.target.willDieInSeconds(8.5), stub must accept (self, s)
  </read_first>
  <action>
Append the 3 registrations below inside the Category O section (after O-02, before the Module 4 header). Each is `isOptional = true`, wrapped with `if UnitClass('player') ~= 'Druid' then return end`, follows the stub/restore discipline: save original in a local, run the body inside pcall, RESTORE the original BEFORE the first assert, then assert. Edit nothing else in the file.

**O-03: HRPS path — "O: fast battle true when willDieInSeconds(8.5) is true":**
1. If non-Druid or `macroTorch.target.isPlayerControlled` → return
2. `local orig = macroTorch.target.willDieInSeconds` — then assign `macroTorch.target.willDieInSeconds = function(self, s) return true end` (stub ignores s: both the 8.5 and any probe hit true)
3. Inside pcall: `local ctx = {}; ok = macroTorch.isFastBattleNotPvp(ctx) == true`
4. Restore `macroTorch.target.willDieInSeconds = orig`, then `assert(pcallRes)` and `assert(ok)`.
The health-estimate arm also exists but the stub makes Condition A true regardless, so this isolates the HRPS arm.

**O-04: Health-estimate path — "O: fast battle true when healthMax below team DPS x 8.5":**
1. Guard: non-Druid or not `macroTorch.target.isCanAttack` → return (healthMax must be a real number)
2. Stub `macroTorch.target.willDieInSeconds = function(self, s) return false end` — kills Condition A so Condition B decides
3. Stub `macroTorch.estimatePlayerDPS = function(level) return 1000000000 end` — 1e9 DPS makes `(mateNearMyTargetCount + 1) * 1e9 * 8.5` exceed any real healthMax, so `macroTorch.target.healthMax <= that` is true
4. Inside pcall: `local ctx = {}; ok = macroTorch.isFastBattleNotPvp(ctx) == true`
5. Restore both originals, then assert pcall and ok.

**O-05: Priority relation — "O: fast battle verdict implies trivial battle verdict (8.5 is below 25)":**
1. Guard: non-Druid → return
2. Stub `macroTorch.target.willDieInSeconds = function(self, s) return true end` — identical stub for both functions
3. Inside pcall: `fast = macroTorch.isFastBattleNotPvp({})`, `trivial = macroTorch.isTrivialBattle({})`, `ok = (fast == trivial)` (identical inputs → identical verdicts; because 8.5 < 25 the trivial die-time 25 covers everything the 8.5 window covers, plus the PvP exclusion only makes fast narrower)
4. Restore original, then assert pcall and ok.

All three: cite `-- Phase 26 D-12` in a short comment line above each registration.
  </action>
  <verify>
    <automated>grep -c "O: fast battle true when willDieInSeconds(8.5) is true" core/selftest.lua returns 1</automated>
    <automated>grep -c "O: fast battle true when healthMax below team DPS" core/selftest.lua returns 1</automated>
    <automated>grep -c "O: fast battle verdict implies trivial battle verdict" core/selftest.lua returns 1</automated>
    <automated>grep -c "Category O" core/selftest.lua returns exactly 1 (section header added by 26-01 — this task appends inside it, no second header)</automated>
    <automated>./build.sh && echo "Build OK" succeeds</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 3 new registrations appended, all with third argument true (isOptional=true) (D-12)
    - Each registration opens with `if UnitClass('player') ~= 'Druid' then return end`
    - O-03 stubs willDieInSeconds only, asserts isFastBattleNotPvp == true, restores before assert
    - O-04 stubs willDieInSeconds to false AND estimatePlayerDPS to 1e9, guards on target.isCanAttack, asserts true, restores before assert
    - O-05 compares fast vs trivial under one shared stub, asserting equality, restores before assert
    - Every stub restore line executes even if the pcall body fails (restore is outside the pcall call, before asserts)
    - git diff --stat shows only core/selftest.lua changed in this task's commit
    - Build succeeds
  </acceptance_criteria>
  <done>
O-03/O-04/O-05 registered with safe stub/restore discipline. HRPS arm, health-estimate arm, and the fast⇒trivial priority relation are each observable in /mt.
  </done>
</task>

<task type="auto">
  <name>Register cp5Bite regression SelfTest O-06 + final phase-wide verification</name>
  <reversibility rating="reversible">Test-code-only addition plus verification commands; single-edit rollback.</reversibility>
  <files>core/selftest.lua</files>
  <read_first>
    - core/selftest.lua: Category O section with O-01 through O-05 (post-Task-1 state)
    - classes/druid/cat.lua lines 104-162: cp5Bite full body — confirm the exact call chain to stub: energyDischargeBeforeBite (132-134), readyBite (137-139) ooc branch, safeBite (139-141) non-ooc branch
    - classes/druid/cat.lua lines 397-405: safeBite / readyBite definitions (global functions, safely stub-able)
    - classes/druid/Druid.lua lines 1059-1075: isRipPresent + ripLeft (stub isRipPresent only)
  </read_first>
  <action>
<!-- planner-discipline-allow: catLeveling -->
**Part A — Register O-06 (D-12 item 6): "O: cp5Bite triggers bite at 5CP in fast battle without Rip or immunity":**

Append inside Category O after O-05. `isOptional = true`, non-Druid guard on top, cite comment `-- Phase 26 D-12 item 6 / D-08`.

Stub set (save each original into a local first):
1. `macroTorch.isRipPresent = function(clickContext) return false end`
2. `macroTorch.safeBite = function(clickContext) biteCalled = true end`
3. `macroTorch.readyBite = function(clickContext) biteCalled = true end`
4. `macroTorch.energyDischargeBeforeBite = function(clickContext) end` — defense-in-depth: even if discharge happened, the cast chain stays stubbed

Test body:
1. Build `ctx = { comboPoints = 5, isImmuneRip = false, ooc = false, isPseudoInfiniteEnergy = true }` and `ctx.isFastBattleNotPvp = true` — the field is set directly to bypass live HRPS, isolating the cp5Bite guard from game state
2. The condition under test is the modified line cat.lua:115; the stub of isRipPresent makes the two original alternates false so only the D-08 clause can admit the branch; isPseudoInfiniteEnergy=true makes the discharge check skip (shouldDischarge=false) without relying on the discharge stub
3. Inside pcall: `biteCalled = false; macroTorch.cp5Bite(ctx); ok = (biteCalled == true)`
4. Restore ALL four originals BEFORE the asserts, then `assert(pcallRes)` and `assert(ok, 'cp5Bite did not call bite in fast battle at 5CP')`

This proves the exact regression D-08 fixes: without the added clause, isRipPresent=false + isImmuneRip=false blocks the 5CP branch forever.

**Part B — Final phase-wide verification:**

Run in order, all must pass:
1. `./build.sh && echo "Build OK"`
2. `grep -c "function macroTorch.isFastBattleNotPvp" classes/druid/Druid.lua` == 1
3. `grep -c "isFastBattleNotPvp" classes/druid/combo.lua` == 3; `grep -c "isFastBattleNotPvp" classes/druid/cat.lua` == 3
4. `grep -c "O: isFastBattleNotPvp\|O: fast battle\|O: cp5Bite" core/selftest.lua` == 6 (all six Category O names)
5. `grep -c "localFastDieTime = 8.5" classes/druid/Druid.lua` == 1 (D-02)
6. D-09/D-13/D-10 negative confirmations: `git diff HEAD --stat` over the whole phase shows ONLY the 4 planned files; `git diff HEAD -- classes/druid/combo.lua | grep -c "catLeveling"` == 0 confirms catLeveling untouched (D-13); the diff contains 1 brand-new function + 7 guard edits and nothing else, which leaves no room for any jitter construct (D-10)
7. If 26-01-SUMMARY.md exists, cross-check its reported edit set equals {Druid.lua, combo.lua, cat.lua, core/selftest.lua}
  </action>
  <verify>
    <automated>grep -c "O: cp5Bite triggers bite at 5CP in fast battle" core/selftest.lua returns 1</automated>
    <automated>grep -c "O: isFastBattleNotPvp\|O: fast battle\|O: cp5Bite" core/selftest.lua returns 6</automated>
    <automated>grep -c "isOptional" core/selftest.lua in Category O region: all 6 registrations end with `end, true)` — run grep -A14 "Category O: fast battle" to eyeball the region</automated>
    <automated>./build.sh && echo "Build OK" and git diff HEAD --stat shows only the 4 planned files</automated>
  </verify>
  <acceptance_criteria>
    - O-06 registered with isOptional=true and non-Druid guard (D-12 item 6)
    - O-06 stubs isRipPresent/safeBite/readyBite/energyDischargeBeforeBite, all 4 restored before any assert
    - O-06 asserts biteCalled == true for ctx {comboPoints=5, isImmuneRip=false, isFastBattleNotPvp=true, isPseudoInfiniteEnergy=true, ooc=false} (D-08)
    - Total Category O registrations in core/selftest.lua == 6, all isOptional=true (D-12)
    - Build.succeeds; SM_Extend.lua contains function macroTorch.isFastBattleNotPvp
    - git diff HEAD shows only classes/druid/Druid.lua, classes/druid/combo.lua, classes/druid/cat.lua, core/selftest.lua (D-09)
    - No diff hunk inside macroTorch.catLeveling (D-13)
    - No new jitter construct: diff contains 1 function + 7 guard edits, zero new conditionals outside those (D-10)
  </acceptance_criteria>
  <done>
All 6 Category O SelfTests registered and build-verified. Every decision path of isFastBattleNotPvp plus the cp5Bite behavior regression is observable via /mt, with zero game-side cast side effects.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client API → macro_torch Lua | Untrusted game-state inputs; SelfTest stubs replace live functions only inside a pcall scope and are restored on the same tick |
| SelfTest → live cast chain | O-06 stubs safeBite/readyBite/energyDischargeBeforeBite so no real spell is cast during login self-test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04 | Spoofing | Test runtime produces false assurance via leaked stubs | medium | mitigate | Stub/restore discipline: originals captured in locals, restored BEFORE asserts; restore runs even when the pcall body fails |
| T-26-05 | DoS | SelfTest casts real spells (Bite/Rake) at login | high | mitigate | O-06 stubs safeBite/readyBite/energyDischargeBeforeBite; isRipPresent stubbed false; no test ever calls the live cast path |
| T-26-06 | Spoofing | Stub poisoning later tests if restore is skipped | medium | mitigate | Same discipline as T-26-04; every stubbing test restores all originals before asserting |
| T-26-SC | Tampering | npm/pip/cargo installs | high | not-applicable | No package-manager activity in this phase |
</threat_model>

<verification>
**Overall phase verification (runs at end of plan 02):**

1. `./build.sh && echo "Build OK"` succeeds
2. Six Category O registrations: `grep -c "O: isFastBattleNotPvp\|O: fast battle\|O: cp5Bite" core/selftest.lua` == 6
3. All call sites armed: combo.lua == 3, cat.lua == 3 isFastBattleNotPvp references
4. Threshold literal: `grep -c "localFastDieTime = 8.5" classes/druid/Druid.lua` == 1
5. Scope discipline: `git diff HEAD --stat` limited to the 4 planned files; zero hunks inside macroTorch.catLeveling
6. No new dependencies; no build_order.txt change needed (all 4 files already listed)
</verification>

<success_criteria>
- 6/6 D-12 SelfTests present, isOptional=true, non-Druid-guarded, stub-safe
- Judgment paths (HRPS, health estimate), priority relation, and cp5Bite regression all observable in /mt without side effects
- Build succeeds with isFastBattleNotPvp in SM_Extend.lua
- Whole-phase diff limited to Druid.lua + combo.lua + cat.lua + core/selftest.lua; catLeveling untouched (D-13)
- All 8 locked decisions (D-01 through D-08) plus D-09/D-10/D-11/D-12/D-13 verifiably implemented
</success_criteria>

<output>
Create .planning/phases/26-phase-fast/26-02-SUMMARY.md when done
</output>

<!-- Artifacts this phase produces -->
<section id="artifacts">
## Artifacts this phase produces

Symbols / code values created in Plan 26-02:

| Kind | Identifier | Location |
|------|-----------|----------|
| SelfTest | "O: fast battle true when willDieInSeconds(8.5) is true" (isOptional=true) | core/selftest.lua Category O |
| SelfTest | "O: fast battle true when healthMax below team DPS x 8.5" (isOptional=true) | core/selftest.lua Category O |
| SelfTest | "O: fast battle verdict implies trivial battle verdict (8.5 is below 25)" (isOptional=true) | core/selftest.lua Category O |
| SelfTest | "O: cp5Bite triggers bite at 5CP in fast battle without Rip or immunity" (isOptional=true) | core/selftest.lua Category O |
| pattern | Stub/restore discipline (local orig capture → pcall body → restore before assert) used across O-03/O-04/O-05/O-06 | core/selftest.lua |
| comment | `-- Phase 26 D-12` cite lines above each new registration | core/selftest.lua |

Combined with Plan 26-01, the phase produces: `macroTorch.isFastBattleNotPvp` (Druid.lua), 6 guard modifications across combo.lua (opener/Rip-branch/regularAttack) and cat.lua (cp5Bite/energyDischarge/keepRake), and 6 Category O SelfTests in core/selftest.lua.
</section>