---
phase: 26-phase-fast
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - core/selftest.lua
  - classes/druid/Druid.lua
autonomous: true
gap_closure: true
requirements:
  - D-01
  - D-04
  - D-05
  - D-08
  - D-09
  - D-12
  - D-13
user_setup: []

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "P-02 snapshots the raw own-key state with rawget and restores it with a raw assignment, so running /mt never leaves an own-key shadow over the function-computed macroTorch.target.isPlayerControlled (CR-01)"
    - "getNextAbilityCost computes the fast-battle verdict once per click and skips its Bite/Rip/Rake steps when that verdict is true, so shouldDoReshift and shouldCastFFDuringWaitWindow benchmark real castable costs (Shred/Claw/Tiger) in fast battles (WR-01)"
    - "shouldCastRip and shouldUseBite bodies are untouched and leveling.lua has zero diff hunks — the catLeveling anchor is preserved (D-13, WR-01)"
    - "isFastBattleNotPvp caches false whenever macroTorch.target.isCanAttack is false, so a dead or missing target can never produce a true fast-battle verdict (IN-04)"
    - "P-06 stubs macroTorch.isFastBattleNotPvp like its other collaborators and no longer pre-seeds the lazy-cache field; every stub is restored before any assert (IN-03)"
    - "P-03 and P-05 carry the same isCanAttack skip guard as P-04, so the IN-04 precondition cannot make them fail on a login with no valid target"
    - "Category P still registers exactly 6 isOptional tests; the section header states (6 tests, 2 from 26-01 + 4 from 26-02) and the Rip duration comment reads 'at least 10s (18s at 5 CP)' (IN-02, IN-01)"
    - "./build.sh exits 0 and the whole-plan diff is limited to core/selftest.lua and classes/druid/Druid.lua"
  artifacts:
    - "core/selftest.lua — revised P-02 (rawget snapshot / raw restore), revised P-06 (judgment function stub, no pre-seeded ctx field), P-03/P-05 isCanAttack guards, corrected Category P header"
    - "classes/druid/Druid.lua — getNextAbilityCost with fast-battle guard (WR-01), isFastBattleNotPvp with isCanAttack precondition (IN-04), corrected Rip-duration comment (IN-01)"
  key_links:
    - "getNextAbilityCost → macroTorch.isFastBattleNotPvp (verdict lazy-cached on the clickContext; at most one HRPS computation per click)"
    - "isFastBattleNotPvp → macroTorch.target.isCanAttack (IN-04 precondition runs after the D-01 PvP exclusion, before either judgment arm)"
    - "P-02 raw restore (nil) → entity/Unit.lua FIELD_FUNC_MAP isPlayerControlled via classMetatable __index (core/class.lua:21-33) — deleting the own-key re-exposes the live accessor"
    - "P-06 judgment stub → cp5Bite guard call site at classes/druid/cat.lua:117 (macroTorch.isFastBattleNotPvp(clickContext))"
---

<objective>
Close the defects found by Phase 26 code review and verification. This is a gap-closure plan: it repairs two mandatory findings — CR-01 (P-02 permanently shadow-taints the function-computed `macroTorch.target.isPlayerControlled`, corrupting all session PvP detection) and WR-01 (getNextAbilityCost reports phantom Rip/Rake/Bite costs in fast battles, skewing reshift/FF energy budgeting by up to 30 energy) — plus the four recommended/cosmetic findings IN-01 through IN-04.

Purpose: CR-01 breaks the D-01 PvP exclusion for every PvP-sensitive call site after `/mt` runs; WR-01 systematically misjudges DPS decisions inside exactly the battles Phase 26 targets. Both fall under the user mandate that every logic bug and every DPS-lowering issue must be fixed.

Output: repaired `core/selftest.lua` (P-02, P-03, P-05, P-06, header) and `classes/druid/Druid.lua` (`getNextAbilityCost`, `isFastBattleNotPvp`, comment), build green.

## Findings Trace (26-REVIEW.md → tasks)

| Finding | Severity | Fix location | Task |
|---------|----------|--------------|------|
| CR-01 P-02 shadow-taints isPlayerControlled | Critical | core/selftest.lua P-02 (704-721) | Task 1 |
| IN-03 P-06 couples to lazy-cache internals | Info | core/selftest.lua P-06 (777-807) | Task 1 |
| WR-01 phantom next-ability costs in fast battles | Warning | classes/druid/Druid.lua getNextAbilityCost (950-982) | Task 2 |
| IN-04 healthMax arm true for dead/missing targets | Info | classes/druid/Druid.lua isFastBattleNotPvp (817-824) | Task 3 |
| IN-01 Rip duration comment says 12s | Info | classes/druid/Druid.lua (807-811) | Task 3 |
| IN-02 stale Category P header | Info | core/selftest.lua (694) | Task 3 |
| (consequence) P-03/P-05 need isCanAttack guard | derived | core/selftest.lua (726, 761) | Task 3 |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-phase-fast/26-REVIEW.md       # Source of truth for every fix; contains the exact corrected code snippets
@.planning/phases/26-phase-fast/26-VERIFICATION.md  # Confirms the same P-02 defect (Anti-Patterns) and the 15/16 verification status
@.planning/phases/26-phase-fast/26-CONTEXT.md       # Decisions D-01..D-13 the fixes must preserve

# Source files to modify
@core/selftest.lua
@classes/druid/Druid.lua

# Reference implementations (read, do NOT modify)
@entity/Unit.lua              # FIELD_FUNC_MAP: isPlayerControlled/isCanAttack are function-computed (180-195)
@core/class.lua               # classMetatable __index only, no __newindex (21-33)
@classes/druid/cat.lua        # cp5Bite guard call site (115-117); shouldDoReshift consumer (215-244)
@classes/druid/selftest.lua   # existing R2/R8 tests calling getNextAbilityCost (140-253, 570-663)
@classes/druid/leveling.lua   # D-13 anchor — must show zero diff hunks; never calls getNextAbilityCost
</context>

<tasks>

<task type="auto">
  <name>Fix P-02 shadow-field corruption (CR-01) and P-06 lazy-cache coupling (IN-03) in core/selftest.lua</name>
  <files>core/selftest.lua</files>
  <read_first>
    - core/selftest.lua lines 693-721: Category P header + P-02 current body (snapshot at 708, rawset stub at 709, restore at 718)
    - core/selftest.lua lines 775-807: P-06 current body (ctx pre-seed at 795, four cast-chain stubs, pcall, restores, asserts)
    - entity/Unit.lua lines 180-195: FIELD_FUNC_MAP entries — macroTorch.target.isPlayerControlled and isCanAttack are computed fields, NOT plain booleans
    - core/class.lua lines 18-40: classMetatable defines only __index (no __newindex), so a raw assignment creates a permanent own-key shadow
    - classes/druid/Druid.lua lines 812-826: isFastBattleNotPvp body — confirm the D-01 PvP exclusion is the first statement (what P-02 exercises)
  </read_first>
  <action>
This task fixes two test-defect findings. CR-01 (critical): P-02 saves the *computed* value of the PvP field and restores it as a raw boolean, which leaves a permanent own-key over the functional accessor — a session-wide freeze of all PvP detection since /mt runs at every login (core/events.lua:51-69). IN-03: P-06 pre-seeds the lazy-cache field on ctx instead of stubbing the judgment function, coupling the test to cache internals.

**Part A — CR-01: rewrite P-02 with rawget snapshot / raw restore (selftest.lua:704-721).** Keep the registration wrapper, guards, pcall body, and asserts byte-identical. Change only the snapshot, the pre-stub comment, and the restore:

1. Replace the snapshot line `local origPvp = macroTorch.target.isPlayerControlled` with:
   `local rawOrigPvp = rawget(macroTorch.target, 'isPlayerControlled')` followed by the comment
   `-- normally nil: the field is function-computed via __index, so the raw snapshot reads the own-key state only`
2. Keep the stub assignment `macroTorch.target.isPlayerControlled = true` and its preceding comment explaining the D-01 ordering test.
3. Replace the restore line `macroTorch.target.isPlayerControlled = origPvp` with:
   `macroTorch.target.isPlayerControlled = rawOrigPvp` followed by the comment
   `-- normally assigns nil, deleting the own-key so the __index accessor is live again; a stale boolean would shadow PvP detection for the whole session (CR-01)`
4. Keep the two asserts exactly as they are: the pcall assert and the false-verdict/nil-cache assert.

Rationale for rawget (per 26-REVIEW.md): when the snapshot is nil, assigning nil removes the own-key, re-exposing the functional accessor; if any future code ever intentionally raw-sets the key, the rawget form still round-trips correctly.

**Part B — IN-03: stub the judgment function in P-06 instead of pre-seeding ctx (selftest.lua:777-807).** This test's intent is cp5Bite's guard composition (D-08), not the judgment function; the judgment must be stubbed like the other collaborators (isRipPresent, safeBite, readyBite, energyDischargeBeforeBite).

1. In the ctx table construction (currently lines 790-796), delete the line `isFastBattleNotPvp = true`. The ctx keeps exactly four fields: comboPoints = 5, isImmuneRip = false, ooc = false, isPseudoInfiniteEnergy = true.
2. Next to the four existing original captures, add: `local origFastBattle = macroTorch.isFastBattleNotPvp`
3. Next to the four existing stub assignments, add: `macroTorch.isFastBattleNotPvp = function(clickContext) return true end`
4. Next to the four existing restores (all before the asserts), add: `macroTorch.isFastBattleNotPvp = origFastBattle`
5. Update the comment above the registration: the stub set now includes the judgment function, so the test no longer depends on the lazy-cache field being pre-seeded (IN-03); keep the "no real spell fires" comment.

Do not touch P-01, P-03, P-04, P-05, or the isOptional `true` third argument on any P registration. Do not edit any file other than core/selftest.lua in this task.
  </action>
  <verify>
    <automated>grep -c "rawget(macroTorch.target, 'isPlayerControlled')" core/selftest.lua returns exactly 1</automated>
    <automated>grep -c "macroTorch.target.isPlayerControlled = rawOrigPvp" core/selftest.lua returns exactly 1</automated>
    <automated>grep -c "local origFastBattle = macroTorch.isFastBattleNotPvp" core/selftest.lua returns exactly 1</automated>
    <automated>grep -c "macroTorch.isFastBattleNotPvp = function(clickContext) return true end" core/selftest.lua returns exactly 1</automated>
    <automated>sed -n '/P: cp5Bite/,/end, true)/p' core/selftest.lua | grep -c "isFastBattleNotPvp" returns exactly 3 (capture + stub + restore only — the pre-seeded ctx occurrence is gone)</automated>
    <automated>grep -c "P: isFastBattleNotPvp\|P: fast battle\|P: cp5Bite" core/selftest.lua returns 6 (all six registrations intact)</automated>
    <automated>./build.sh exits 0 (run from /home/admin/workspace/macro-torch)</automated>
  </verify>
  <acceptance_criteria>
    - P-02 snapshots via rawget and restores via raw assignment; when the snapshot is nil the own-key is deleted, never leaving a shadow over the __index accessor (CR-01)
    - P-02 pcall body and both asserts unchanged: still proves the D-01 PvP exclusion returns false and fires before the lazy cache writes
    - P-06 ctx carries only comboPoints / isImmuneRip / ooc / isPseudoInfiniteEnergy (IN-03)
    - All five P-06 stubs (four cast-chain + new judgment stub) are captured into locals and restored before the first assert
    - Category P unit count stays 6, each still registered with isOptional=true (D-12)
    - git diff --stat for this task shows only core/selftest.lua
    - Build succeeds
  </acceptance_criteria>
  <done>
P-02 can no longer shadow the computed PvP field across the session, and P-06 is decoupled from the lazy-cache internals via the same stub/restore discipline as its sibling tests. All five P-06 stubs restored before asserts. Build green.
  </done>
</task>

<task type="auto">
  <name>Guard getNextAbilityCost against phantom fast-battle abilities (WR-01) in Druid.lua</name>
  <files>classes/druid/Druid.lua</files>
  <read_first>
    - classes/druid/Druid.lua lines 945-985: getNextAbilityCost current body — six steps (Bite / Tiger / Rip / Rake / Shred / Claw) to modify
    - classes/druid/Druid.lua lines 807-826: isFastBattleNotPvp (the verdict to consult; lazy-cached on clickContext per D-11)
    - classes/druid/cat.lua lines 215-244: shouldDoReshift — consumer at line 230, benchmarks projected energy against the reported cost
    - classes/druid/Druid.lua lines 917-948: shouldCastFFDuringWaitWindow — consumer at line 928, uses the reported cost as minAbilityCost
    - classes/druid/Druid.lua lines 987-1037: shouldCastRip / shouldUseBite bodies — confirm they are NOT to be edited (D-09/D-13)
    - classes/druid/selftest.lua lines 140-253 and 570-663 + classes/druid/Druid.lua lines 1379-1392: existing optional tests that call getNextAbilityCost — confirm none asserts Rip/Rake/Bite as the reported move name
    - classes/druid/combo.lua lines 160-172 and classes/druid/cat.lua lines 332-345: the D-04/D-05 cast sites that make Rip/Rake uncastable in fast battles
  </read_first>
  <action>
Fix the WR-01 decision-economics defect inside `macroTorch.getNextAbilityCost` ONLY — do not edit shouldCastRip, shouldUseBite, shouldUseShred, shouldDoReshift, or shouldCastFFDuringWaitWindow (per 26-REVIEW.md: this single shared consumer feeds the 60-level catAtk chain, while catLeveling calls shouldCastRip/shouldUseBite directly and never calls getNextAbilityCost, so the leveling anchor is preserved; D-09/D-13).

1. Immediately after the `function macroTorch.getNextAbilityCost(clickContext)` line, add the comment:
   `-- [FAST] WR-01: in fast battles Rip/Rake can never be cast (D-04/D-05) and the 3-4CP quick-bite branch reports a Bite only tryBiteKillShot casts; skip all three so reshift/FF timing benchmarks real costs (Shred/Claw, Tiger)`
   then the line:
   `local fastBattle = macroTorch.isFastBattleNotPvp(clickContext)`
   The verdict is lazy-cached on clickContext (D-11), so this costs at most one HRPS computation per click.
2. Step 1 (Bite): change the condition `if macroTorch.shouldUseBite(clickContext) then` to:
   `if macroTorch.shouldUseBite(clickContext) and not fastBattle then`
   At 5 CP in a fast battle shouldUseBite is already false (its 5-CP branch requires isRipPresent or isImmuneRip, both false), so this guard is behavior-neutral at 5 CP and removes the CP 3-4 phantom Bite from the trivial-battle quick-bite branch (Druid.lua:1024-1029).
3. Step 3 (Rip): change `if macroTorch.shouldCastRip(clickContext) then` to:
   `if not fastBattle and macroTorch.shouldCastRip(clickContext) then`
4. Step 4 (Rake): change `if not macroTorch.isRakePresent(clickContext) and not clickContext.isImmuneRake then` to:
   `if not fastBattle and not macroTorch.isRakePresent(clickContext) and not clickContext.isImmuneRake then`
5. Do NOT touch step 2 (Tiger — Tiger's Fury direct-damage buff stays beneficial in fast battles per 26-CONTEXT.md) and do NOT touch steps 5/6 (Shred/Claw are the real fast-battle builders; the fall-through now lands on them, reporting honest 60/45 or position-dependent costs instead of phantom 30/40).
6. Preserve every existing comment in the function body verbatim (the Tiger cost note for reshift callers stays).

No other function in Druid.lua changes in this task. This makes shouldDoReshift and shouldCastFFDuringWaitWindow benchmark real castable costs so they neither prematurely skip a profitable reshift nor cast FF when the real next ability is unaffordable.
  </action>
  <verify>
    <automated>grep -c "local fastBattle = macroTorch.isFastBattleNotPvp(clickContext)" classes/druid/Druid.lua returns exactly 1</automated>
    <automated>grep -c "not fastBattle" classes/druid/Druid.lua returns exactly 3 (Bite, Rip, Rake steps)</automated>
    <automated>grep -c "and not fastBattle" classes/druid/Druid.lua returns exactly 1 (Bite step)</automated>
    <automated>git diff HEAD -- classes/druid/leveling.lua produces empty output (D-13 anchor)</automated>
    <automated>./build.sh exits 0 (run from /home/admin/workspace/macro-torch)</automated>
  </verify>
  <acceptance_criteria>
    - getNextAbilityCost is the only function changed in this task; the git diff hunk is bounded to its body (lines ~950-985)
    - Bite, Rip, Rake steps each carry the fast-battle exclusion; Tiger, Shred, Claw steps are byte-identical
    - shouldCastRip / shouldUseBite / shouldUseShred bodies show zero diff hunks; their direct callers in catLeveling are unaffected (D-09/D-13)
    - shouldDoReshift (cat.lua:230) and shouldCastFFDuringWaitWindow (Druid.lua:928) consume the corrected cost without code change
    - Existing optional getNextAbilityCost tests (Druid.lua:1379, classes/druid/selftest.lua R2/R8 series) are untouched and remain valid — none asserts a reported move name, and all tolerate the extra lazy-cached verdict
    - Build succeeds
  </acceptance_criteria>
  <done>
Fast-battle phantom costs are eliminated at the single shared reporter: reshift and FF-timing decisions now benchmark only abilities that can actually fire (Tiger, Shred, Claw). Build green.
  </done>
</task>

<task type="auto">
  <name>Add isCanAttack precondition (IN-04), fix Rip-duration comment (IN-01), stale header (IN-02), and P-03/P-05 guards</name>
  <files>classes/druid/Druid.lua, core/selftest.lua</files>
  <read_first>
    - classes/druid/Druid.lua lines 805-830: isFastBattleNotPvp body incl. the comment block at 807-811 (IN-01 target line is 808-809) and the cache block 817-824
    - entity/Unit.lua lines 185-195: isCanAttack computed field semantics — UnitExists and not UnitIsDead and UnitCanAttack('player', t), so it is false exactly for missing and dead targets
    - core/selftest.lua lines 693-696: Category P banner + header (IN-02 target is line 694) and the accurate trailing count at line 809 (unchanged)
    - core/selftest.lua lines 723-756: P-03 and P-04 guard lines — P-04 already has the isCanAttack skip guard at 742; P-03 does not
    - core/selftest.lua lines 758-773: P-05 guard lines — needs the same guard as P-04
    - classes/druid/Druid.lua lines 950-985: post-Task-2 getNextAbilityCost — the IN-04 precondition also hardens this new caller against missing/dead targets
  </read_first>
  <action>
**Part A — IN-04: add an isCanAttack precondition to the cache computation (classes/druid/Druid.lua:817-824).** Today `UnitHealthMax` is 0 for dead or missing targets, so the health-estimate arm yields `0 <= (mates+1) * DPS * 8.5` → true — a latent wrong verdict for every caller. All current call sites sit behind isCanAttack (combo.lua:122-187), so this is latent, but the function is a public judgment on macroTorch and must not return true without a valid target.

Restructure the `if clickContext.isFastBattleNotPvp == nil then ... end` block so that a non-attackable target caches false and the two judgment arms run only in the else branch:

1. Inside the nil-check, first add:
   `if not macroTorch.target.isCanAttack then`
   `    clickContext.isFastBattleNotPvp = false`
   preceded by an English comment explaining: no valid attackable target (missing or dead — UnitHealthMax is 0 for both) can never be a fast battle, and the health-estimate arm would otherwise wrongly return true; cite IN-04.
2. Then `else` with the existing computation unchanged inside it:
   keep the existing comment `-- recomputed each frame: catAtk rebuilds clickContext per click (D-11)`, keep `local localFastDieTime = 8.5`, and keep the `willDieInSeconds(localFastDieTime) or healthMax <= (mateNearMyTargetCount + 1) * estimatePlayerDPS() * localFastDieTime` assignment exactly as-is.
3. The D-01 PvP exclusion above the cache stays the FIRST statement of the function — the new precondition must come after it, not before (a PvP target still returns false immediately without any cache write).

**Part B — IN-01: correct the Rip-duration comment (classes/druid/Druid.lua:808-809).** The comment claims "Rip 12s" — RIP_BASE_DURATION is 10 (Druid.lua:853) and a 5-CP Rip lasts 18s (ripLeft adds (cp-1)*2). Change the phrase "Rake lasts 9s and Rip 12s, both exceeding the threshold" to:
`Rake lasts 9s and Rip at least 10s (18s at 5 CP), both exceeding the threshold`
Keep the rest of the four-line comment block and the ladder sentence (8.5s between 2s and 25s) unchanged.

**Part C — IN-02: fix the stale Category P header (core/selftest.lua:694).** Replace the header text "(2 tests in this task, 4 more in 26-02-PLAN.md)" with "(6 tests, 2 from 26-01 + 4 from 26-02)" — all six are now registered in this file. Leave the banner lines, the `-- [CITED: 26-CONTEXT.md D-12]` line, and the accurate trailing count comment at line 809 untouched.

**Part D — test coherence with IN-04: add the isCanAttack skip guard to P-03 and P-05 (core/selftest.lua:726 and 761).** This is a required consequence of Part A: before IN-04, P-03 and P-05 passed at login with no valid target because the healthMax=0 bug made the verdict true; after IN-04 the verdict is cached false, which would fail both tests on logins with no target (test #2 at UAT.md rows 24-30 depends on /mt staying green). Insert `if not macroTorch.target.isCanAttack then return end` immediately after the existing `if macroTorch.target.isPlayerControlled then return end` guard in P-03 and in P-05, exactly matching the pattern P-04 already uses. Do not change any other line inside P-03/P-04/P-05.
  </action>
  <verify>
    <automated>sed -n '/^function macroTorch.isFastBattleNotPvp/,/^end/p' classes/druid/Druid.lua | grep -c "isCanAttack" returns exactly 1 (the new precondition line)</automated>
    <automated>grep -c "localFastDieTime = 8.5" classes/druid/Druid.lua returns exactly 1 (D-02 threshold literal unchanged by the restructuring)</automated>
    <automated>grep -c "Rip at least 10s (18s at 5 CP)" classes/druid/Druid.lua returns exactly 1 (IN-01)</automated>
    <automated>grep -c "(6 tests, 2 from 26-01 + 4 from 26-02)" core/selftest.lua returns exactly 1 (IN-02)</automated>
    <automated>grep -c "if not macroTorch.target.isCanAttack then return end" core/selftest.lua returns exactly 3 (P-03, P-04, P-05)</automated>
    <automated>grep -c "P: isFastBattleNotPvp\|P: fast battle\|P: cp5Bite" core/selftest.lua returns 6 (all registrations intact, D-12)</automated>
    <automated>grep -c "function macroTorch.isFastBattleNotPvp" classes/druid/Druid.lua returns exactly 1 and the PvP exclusion remains the first statement of the function (eye-check via sed -n '812,838p')</automated>
    <automated>./build.sh exits 0 (run from /home/admin/workspace/macro-torch)</automated>
  </verify>
  <acceptance_criteria>
    - isFastBattleNotPvp caches false when macroTorch.target.isCanAttack is false; the two judgment arms and the localFastDieTime = 8.5 literal are unchanged inside the else branch (IN-04, D-02)
    - D-01 PvP exclusion still precedes the cache and the new precondition — a player-controlled target still returns false without writing the cache
    - Rip-lifetime comment states at least 10s (18s at 5 CP) (IN-01); ladder sentence preserved
    - Category P header states (6 tests, 2 from 26-01 + 4 from 26-02); trailing count and [CITED] lines unchanged (IN-02)
    - P-03, P-04, P-05 all skip when macroTorch.target.isCanAttack is false; no other test-body line changed
    - git diff --stat across the whole plan shows only core/selftest.lua and classes/druid/Druid.lua; classes/druid/leveling.lua has zero hunks (D-13)
    - Build succeeds
  </acceptance_criteria>
  <done>
The judgment is immune to dead/missing targets, the self-test suite stays coherent with the new precondition, and both stale comment artifacts are corrected. Build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client API → macro_torch Lua | Untrusted game-state inputs (unit flags, health, HRPS samples) read via read-only Unit* APIs |
| SelfTest stubs → live addon state | Test code mutates global functions and the session-persistent Target singleton; a leaked stub becomes live behavior for the whole session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07 | Tampering | core/selftest.lua P-02 global-field stub | critical | mitigate | CR-01 fix: rawget snapshot + raw assignment restore (nil deletes the own-key, re-exposing the __index accessor); restore executes before any assert so a stumbled call cannot leak either |
| T-26-08 | DoS | classes/druid/Druid.lua getNextAbilityCost cost reporting | high | mitigate | WR-01 fix: Bite/Rip/Rake steps skipped in fast battles so shouldDoReshift/shouldCastFFDuringWaitWindow benchmark real castable costs (Shred/Claw/Tiger); guard added only at the single shared reporter, preserving the catLeveling path |
| T-26-09 | Spoofing | isFastBattleNotPvp healthMax arm on dead/missing targets | medium | mitigate | IN-04 fix: isCanAttack precondition caches false before either judgment arm runs; removes the UnitHealthMax=0 → true vector |
| T-26-10 | Spoofing | P-06 test relying on lazy-cache pre-seed (order-dependent false assurance) | low | mitigate | IN-03 fix: judgment function stubbed like all other collaborators; test meaning no longer depends on cache internals or on the CR-01-frozen field |
| T-26-SC | Tampering | npm/pip/cargo installs | high | not-applicable | No package-manager activity in this phase (pure Lua, no new dependencies) |
</threat_model>

<verification>
**Overall plan verification (run at end of plan 03):**

1. Build: `./build.sh` exits 0 from /home/admin/workspace/macro-torch (runs in all three tasks; final run confirmed green)
2. CR-01 closed: `grep -c "rawget(macroTorch.target, 'isPlayerControlled')" core/selftest.lua` == 1 and the P-02 restore assigns the raw snapshot — no own-key survives /mt
3. WR-01 closed: `grep -c "not fastBattle" classes/druid/Druid.lua` == 3 inside getNextAbilityCost; shouldCastRip/shouldUseBite bodies show zero diff hunks
4. IN-03 closed: `sed -n '/P: cp5Bite/,/end, true)/p' core/selftest.lua | grep -c "isFastBattleNotPvp"` == 3 (capture/stub/restore only)
5. IN-04 closed: `sed -n '/^function macroTorch.isFastBattleNotPvp/,/^end/p' classes/druid/Druid.lua | grep -c "isCanAttack"` == 1; D-01 PvP exclusion remains first; `grep -c "localFastDieTime = 8.5"` == 1
6. IN-01/IN-02 closed: `grep -c "Rip at least 10s (18s at 5 CP)" classes/druid/Druid.lua` == 1; `grep -c "(6 tests, 2 from 26-01 + 4 from 26-02)" core/selftest.lua` == 1
7. Test coherence: `grep -c "if not macroTorch.target.isCanAttack then return end" core/selftest.lua` == 3; six Category P registrations intact (`grep -c "P: isFastBattleNotPvp\|P: fast battle\|P: cp5Bite"` == 6)
8. Scope discipline: `git diff` for this plan touches only core/selftest.lua and classes/druid/Druid.lua; classes/druid/leveling.lua zero hunks (D-13); no new timer/debounce constructs (D-10 unaffected)
9. Remaining human-verification item (NOT a code gap — already persisted in 26-UAT.md): truth #10 P-06 in-game behavior (D-08 state transition) requires the WoW client; no Lua interpreter exists in this environment, so this plan cannot execute it. The UAT rows for P-06, the PvP shadow-field observation, and fast⇄normal frame switching remain open for the user.
</verification>

<success_criteria>
- CR-01 and WR-01 (both user-mandated) fixed exactly per 26-REVIEW.md; IN-03, IN-04, IN-01, IN-02 all closed
- getNextAbilityCost reports only castable next-ability costs in fast battles, restoring honest reshift/FF energy budgeting (WR-01)
- Running /mt can no longer freeze PvP detection for the session (CR-01)
- isFastBattleNotPvp cannot return true for a dead or missing target (IN-04)
- All six Category P SelfTests remain registered with isOptional=true and stay logically coherent with the new preconditions (D-12)
- catLeveling untouched: leveling.lua zero diff hunks (D-13)
- Build green: ./build.sh exits 0
</success_criteria>

<output>
Create .planning/phases/26-phase-fast/26-03-SUMMARY.md when done
</output>