---
phase: 20-spell-id-auto-correct-spellid
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - core/selftest.lua
autonomous: true
requirements:
  - REQ-20-SELFTEST

must_haves:
  truths:
    - "SPELL_ID_AUTO_CORRECT default value is verified as true by a selftest"
    - "Setting SPELL_ID_AUTO_CORRECT = false causes resolveSpellId() to skip spellIdMap lookup"
    - "Setting SPELL_ID_AUTO_CORRECT = true and having a corrected spellId in spellIdMap causes resolveSpellId() to return the corrected value"
    - "loadSpellIdMap() is callable (function exists) and does not error"
    - "All selftests are isOptional=true (Druid-only), registered after Category M"
    - "Gwakd check: list neo-small-gwakd-cuda-14 cross references are NOT touched (unrelated file)"
  artifacts:
    - path: "core/selftest.lua"
      provides: "Category N selftest registrations for SPELL_ID_AUTO_CORRECT mechanism"
      contains: "Category N: SPELL_ID_AUTO_CORRECT switch verification"
  key_links:
    - from: "Category N selftests in core/selftest.lua"
      to: "macroTorch.SPELL_ID_AUTO_CORRECT in macro_torch.lua"
      via: "Tests read and temporarily modify the global variable to verify guard behavior"
    - from: "Category N selftests in core/selftest.lua"
      to: "macroTorch.resolveSpellId in core/spell_trace_core.lua"
      via: "Tests call resolveSpellId() with controlled switch states to verify behavior"
---
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-spell-id-auto-correct-spellid/20-CONTEXT.md
@core/selftest.lua
@macro_torch.lua
@core/spell_trace_core.lua
@core/spell_trace_immune.lua
@entity/Player.lua
@core/events.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Category N selftests for SPELL_ID_AUTO_CORRECT in core/selftest.lua</name>
  <files>core/selftest.lua</files>
  <read_first>
    core/selftest.lua
    macro_torch.lua
    core/spell_trace_core.lua
    core/spell_trace_immune.lua
  </read_first>
  <action>
    In core/selftest.lua, add a new Category N section after Category M (after line 787 "Registration count: Category M adds 4 tests (4 optional)") and before Module 4 (/mt SLASH command at line 789).

    Add the following selftest registrations, following the exact pattern established by Category M (Category letter prefix, function existence/behavior tests, isOptional=true for Druid-only):

    **N1: SPELL_ID_AUTO_CORRECT default value is true**
    - Test: Assert macroTorch.SPELL_ID_AUTO_CORRECT == true
    - isOptional: true (UnitClass guard not needed — this is global, but keep optional for consistency with non-core tests)
    - Category prefix: "N: SPELL_ID_AUTO_CORRECT default value is true"

    **N2: resolveSpellId() returns static value when switch is false**
    - Test: Temporarily set SPELL_ID_AUTO_CORRECT = false. Choose a spell name known to exist in SPELL_NAME_TO_ID (e.g., "Rake"). Call resolveSpellId(spellName). Assert result equals SPELL_NAME_TO_ID[spellName]. Then restore SPELL_ID_AUTO_CORRECT = true.
    - isOptional: true (Druid-only — uses Druid spell names in SPELL_NAME_TO_ID)
    - Guard: if UnitClass('player') ~= 'Druid' then return end
    - Use pcall to ensure restoration even on assertion failure

    **N3: resolveSpellId() returns corrected value when switch is true and corrected value exists**
    - Test: Create a temporary loginContext.spellIdMap entry for a known spell name (e.g., "Rake") with a test value (e.g., SPELL_NAME_TO_ID["Rake"] + 10000). Call resolveSpellId(spellName). Assert result equals the test value (the corrected one). Then clean up by removing the temporary entry from loginContext.spellIdMap.
    - isOptional: true (Druid-only + requires loginContext to exist)
    - Guard: if UnitClass('player') ~= 'Druid' then return end. Also guard with `if not macroTorch.loginContext then return end`.
    - Use pcall for cleanup

    **N4: loadSpellIdMap() function exists and is callable without error**
    - Test: Assert type(macroTorch.loadSpellIdMap) == "function". Then pcall(macroTorch.loadSpellIdMap) and assert it does not error.
    - isOptional: true (depends on loginContext being available)
    - Guard: if not macroTorch.loginContext then return end

    **N5: current_casting_spell is nil after mode='ready' _castSpell when switch is true**
    - Test: Verify that a mode='ready' call to _castSpell does NOT set current_casting_spell. Before test: ensure current_casting_spell is nil. Call a Druid skill method in 'ready' mode (e.g., player.rake('ready')). Assert current_casting_spell remains nil after the call.
    - isOptional: true (Druid-only, depends on skill methods existing)
    - Guard: if UnitClass('player') ~= 'Druid' then return end. Also guard `if not macroTorch.player.rake then return end`.

    **Section header format (follow Category M pattern):**
    ```
    -- ============================================================
    -- Category N: SPELL_ID_AUTO_CORRECT switch verification (5 tests, all isOptional=true)
    -- ============================================================
    -- [CITED: 20-CONTEXT.md]
    ```

    **Registration count footer format (follow Category M pattern):**
    ```
    -- Registration count: Category N adds 5 tests (5 optional)
    ```

    **Important implementation notes:**
    - Tests that temporarily modify SPELL_ID_AUTO_CORRECT MUST use pcall and MUST restore the original value in a finally-style block (restore both in success and error paths)
    - Tests that create temporary spellIdMap entries MUST clean them up (set to nil) in both success and error paths
    - All 5 tests use isOptional=true to match the pattern established by Categories K, L, and M for Druid-specific features
    - Do NOT use the # length operator — use macroTorch.tableLen() if counting is needed
    - All assert() calls use descriptive error messages following existing patterns
  </action>
  <verify>
    <automated>grep -c 'Category N: SPELL_ID_AUTO_CORRECT' core/selftest.lua | grep -v '^#' || test $(grep -c 'Category N: SPELL_ID_AUTO_CORRECT' core/selftest.lua) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Category N section header exists in selftest.lua between Category M and Module 4
    - 5 SelfTest:register calls with "N:" prefix are present
    - N1 verifies SPELL_ID_AUTO_CORRECT default is true
    - N2 verifies resolveSpellId() returns static value when switch is false
    - N3 verifies resolveSpellId() returns corrected value when switch is true
    - N4 verifies loadSpellIdMap() is a function and callable without error
    - N5 verifies mode='ready' _castSpell does not set current_casting_spell
    - All tests have isOptional=true
    - Tests that modify globals restore them in error paths (pcall + cleanup)
    - Registration count comment at end says "Category N adds 5 tests (5 optional)"
    - Build succeeds: ./build.sh && echo "Build OK"
  </acceptance_criteria>
  <done>5 Category N selftests registered; all verify SPELL_ID_AUTO_CORRECT switch behavior with proper cleanup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selftest temporary state modification | Tests temporarily change SPELL_ID_AUTO_CORRECT and loginContext.spellIdMap; pcall guards ensure restoration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Denial of Service | Selftest state pollution | low | mitigate | All tests use pcall with cleanup in both success and error paths; SPI variables restored after each test |
| T-20-SC | Tampering | npm/pip/cargo installs | N/A | accept | No package manager installs in WoW addon project |
</threat_model>

<verification>
```bash
# Verify Category N exists in selftest.lua
grep -c 'Category N: SPELL_ID_AUTO_CORRECT' core/selftest.lua | grep -v '^#' || test $(grep -c 'Category N: SPELL_ID_AUTO_CORRECT' core/selftest.lua) -eq 1

# Verify 5 registrations with N: prefix
grep -c 'SelfTest:register("N:' core/selftest.lua | grep -v '^#' || test $(grep -c 'SelfTest:register("N:' core/selftest.lua) -eq 5

# Verify all isOptional=true
grep 'SelfTest:register("N:' core/selftest.lua | grep -v 'true' && echo "WARNING: some N tests may not be optional" || echo "All N tests are optional"

# Build must succeed
./build.sh && echo "Build OK"
```
</verification>

<success_criteria>
- 5 Category N selftest registrations in core/selftest.lua
- N1: default value verification
- N2: switch-off static resolution verification
- N3: switch-on corrected resolution verification
- N4: loadSpellIdMap callability verification
- N5: mode='ready' no-bridge verification
- All tests have proper pcall+cleanup for state restoration
- ./build.sh succeeds
</success_criteria>

<output>
Create .planning/phases/20-spell-id-auto-correct-spellid/20-03-SUMMARY.md when done
</output>

## Artifacts this phase produces

### Plan 03

| Artifact | Type | File | Description |
|----------|------|------|-------------|
| Category N selftests (N1-N5) | test registrations | core/selftest.lua | Verify SPELL_ID_AUTO_CORRECT switch: default, resolveSpellId both modes, loadSpellIdMap callability, _castSpell ready-mode no-bridge |
</output>