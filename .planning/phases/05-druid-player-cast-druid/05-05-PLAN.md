---
phase: 05-druid-player-cast-druid
plan: 05
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - classes/druid/utility.lua
autonomous: true
requirements:
  - R8
  - D-01
  - D-03
  - D-07

must_haves:
  truths:
    - "druidBuffs calls player.mark_of_the_wild(nil, true) instead of player.cast('Mark of the Wild', true)"
    - "druidBuffs calls player.thorns(nil, true) instead of player.cast('Thorns', true)"
    - "druidBuffs calls player.natures_grasp() instead of player.cast('Nature\'s Grasp', true)"
    - "druidStun calls player.dire_bear_form(), player.reshift(), player.bash(), player.feral_charge()"
    - "druidDefend calls player.barkskin(), player.dire_bear_form(), player.enrage(), player.frenzied_regeneration()"
    - "druidControl calls player.hibernate() and player.entangling_roots()"
    - "No player.cast() calls remain in utility.lua"
  artifacts:
    - path: "classes/druid/utility.lua"
      provides: "Druid utility functions updated to use skill method calls"
      contains: "player.mark_of_the_wild(nil, true)"
      contains: "player.thorns(nil, true)"
      contains: "player.natures_grasp()"
      contains: "player.dire_bear_form()"
      contains: "player.barkskin()"
      contains: "player.hibernate()"
      contains: "player.frenzied_regeneration()"
  key_links:
    - from: "classes/druid/utility.lua druidBuffs"
      to: "classes/druid/Druid.lua obj.mark_of_the_wild"
      via: "macroTorch.player.mark_of_the_wild(nil, true) — Type C, onSelf=true"
    - from: "classes/druid/utility.lua druidDefend"
      to: "classes/druid/Druid.lua obj.barkskin"
      via: "macroTorch.player.barkskin() — Type B, onSelf=true"
---

<objective>
Replace all 13 `player.cast()` call sites in `classes/druid/utility.lua` with typed skill method calls. The utility module handles buffs (druidBuffs), stun (druidStun), defense (druidDefend), and crowd control (druidControl). Most calls are Type C (flexible target with onSelf=true for self-buffs) or Type B (self-targeting forms/buffs). No safe/ready wrapper functions exist in this file — only direct `player.cast()` calls with optional `onSelf` parameter.

Purpose: Complete D-01 full replacement in the utility module. This is the final wave 3 file migration, closing the last `player.cast()` call sites in the Druid codebase.

Output: `classes/druid/utility.lua` modified — 13 call sites updated, no deletions (no safe/ready wrappers in this file).
</objective>

<execution_context>
The executor reads this plan as its primary instruction. All @file references are mandatory reads before any editing begins.
</execution_context>

<context>
@classes/druid/utility.lua (current source — 93 lines: READ BEFORE EDITING — druidBuffs lines 2-13, druidStun lines 14-33, druidDefend lines 34-49, druidControl lines 50-59, pokemonLoad lines 60-92)
@classes/druid/Druid.lua (after Plan 05-02 — verify all skill methods exist: mark_of_the_wild, thorns, natures_grasp, dire_bear_form, reshift, bash, feral_charge, barkskin, enrage, frenzied_regeneration, hibernate, entangling_roots)
@.planning/phases/05-druid-player-cast-druid/05-RESEARCH.md (lines 681-697: call-site replacement table for utility.lua lines 5-57)
@.planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (Section 5: exact before/after code for utility.lua replacements; lines 614-662)
@docs/spell_refactor_plan_druid.txt (Section 5 step 3: migration guidance)
</context>

## Artifacts this phase produces

**Modified functions in `classes/druid/utility.lua`:**

- `macroTorch.druidBuffs` — 3 call sites: mark_of_the_wild, thorns, natures_grasp
- `macroTorch.druidStun` — 4 call sites: dire_bear_form, reshift, bash, feral_charge
- `macroTorch.druidDefend` — 4 call sites: barkskin, dire_bear_form, enrage, frenzied_regeneration
- `macroTorch.druidControl` — 2 call sites: hibernate, entangling_roots
- `macroTorch.pokemonLoad` — no spell casts (item usage only), unchanged

<tasks>

<task type="auto">
  <name>Task 1: Replace player.cast calls in druidBuffs</name>
  <files>classes/druid/utility.lua</files>
  <read_first>
    - classes/druid/utility.lua (lines 2-13: druidBuffs function)
    - classes/druid/Druid.lua (after Plan 05-02 — verify obj.mark_of_the_wild, obj.thorns, obj.natures_grasp exist)
  </read_first>
  <behavior>
    - Behavior 1: druidBuffs calls player.mark_of_the_wild(nil, true) when player lacks MotW buff — nil mode (ready check only), onSelf=true
    - Behavior 2: druidBuffs calls player.thorns(nil, true) when player lacks Thorns buff — nil mode, onSelf=true
    - Behavior 3: druidBuffs calls player.natures_grasp() when player lacks NG buff — nil mode (ready check), Type B skill (onSelf=true hardcoded). Note: Type B methods don't accept onSelf parameter, so we call player.natures_grasp() without onSelf.
    - Edge case: Nature's Grasp was originally called with `player.cast('Nature\'s Grasp', true)`. The new skill method is Type B (onSelf=true hardcoded in Druid.lua), so we just call `player.natures_grasp()` — no onSelf parameter needed.
  </behavior>
  <action>
    In `macroTorch.druidBuffs()` (lines 2-13), replace three calls:

    1. Line 5: `macroTorch.player.cast('Mark of the Wild', true)` → `macroTorch.player.mark_of_the_wild(nil, true)`. mark_of_the_wild is Type C with onSelf exposed. nil mode = ready check only. onSelf=true to cast on self.

    2. Line 8: `macroTorch.player.cast('Thorns', true)` → `macroTorch.player.thorns(nil, true)`. thorns is Type C with onSelf exposed. nil mode = ready check only.

    3. Line 11: `macroTorch.player.cast('Nature\'s Grasp', true)` → `macroTorch.player.natures_grasp()`. natures_grasp is Type B — onSelf is hardcoded to true in Druid.lua, so no onSelf parameter needed. nil mode = ready check only.

    Also optionally remove the unused `local clickContext = {}` on line 3 — it's an empty variable that serves no purpose.
  </action>
  <verify>
    <automated>grep -c "player\.mark_of_the_wild\|player\.thorns\|player\.natures_grasp" classes/druid/utility.lua</automated>
    <automated>grep -c "player\.cast('Mark of the Wild')\|player\.cast('Thorns')\|player\.cast('Nature" classes/druid/utility.lua</automated>
    <automated>./build.sh</automated>
  </verify>
  <acceptance_criteria>
    - druidBuffs: calls player.mark_of_the_wild(nil, true) — grep confirms
    - druidBuffs: calls player.thorns(nil, true) — grep confirms
    - druidBuffs: calls player.natures_grasp() (no onSelf param) — grep confirms
    - No player.cast('Mark of the Wild') or player.cast('Thorns') or player.cast('Nature\'s Grasp') in utility.lua
    - ./build.sh exits 0
  </acceptance_criteria>
  <done>druidBuffs uses skill method calls with correct onSelf semantics. Nature's Grasp uses Type B (onSelf implicit).</done>
</task>

<task type="auto">
  <name>Task 2: Replace player.cast calls in druidStun, druidDefend, druidControl</name>
  <files>classes/druid/utility.lua</files>
  <read_first>
    - classes/druid/utility.lua (lines 14-59: druidStun, druidDefend, druidControl functions)
    - classes/druid/Druid.lua (after Plan 05-02 — verify all 10 skill methods exist)
    - .planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (lines 648-661: utility replacement table)
  </read_first>
  <behavior>
    - Behavior 1: druidStun calls player.dire_bear_form() (Type B, ready check)
    - Behavior 2: druidStun calls player.reshift() (Type B, ready check)
    - Behavior 3: druidStun calls player.bash() (Type A, nil mode = ready check)
    - Behavior 4: druidStun calls player.feral_charge() (Type A, nil mode = ready check)
    - Behavior 5: druidDefend calls player.barkskin() (Type B, ready check)
    - Behavior 6: druidDefend calls player.dire_bear_form() (Type B, ready check)
    - Behavior 7: druidDefend calls player.enrage() (Type B, ready check)
    - Behavior 8: druidDefend calls player.frenzied_regeneration() (Type B, ready check)
    - Behavior 9: druidControl calls player.hibernate() (Type A, ready check)
    - Behavior 10: druidControl calls player.entangling_roots() (Type A, ready check)
    - Edge case: druidDefend checks `isSpellReady('Barkskin (Feral)')` but the skill method uses `{en='Barkskin',zh='树皮术'}`. Per RESEARCH.md (line 692), the spell name for Barkskin may be 'Barkskin (Feral)' on some clients. We keep the external `isSpellReady('Barkskin (Feral)')` check and call `player.barkskin('raw')` — raw mode skips _castSpell's internal isSpellReady check, relying on the external check doing the right thing. If the spell name is actually 'Barkskin', the external check returns true and the raw cast goes through. If it's 'Barkskin (Feral)', _castSpell's locale lookup for 'Barkskin' would fail — using raw bypasses this.
    - Edge case: druidDefend checks `macroTorch.player.mana == 0` before calling Enrage. This is a rage check, not energy. The `player.enrage()` call already does internal ready check via _castSpell. The external mana(rage)==0 check remains outside.
  </behavior>
  <action>
    Replace all 10 remaining `player.cast()` calls in utility.lua:

    **druidStun** (lines 14-33):
    1. Line 20: `macroTorch.player.cast('Dire Bear Form')` → `macroTorch.player.dire_bear_form()`
    2. Line 24: `macroTorch.player.cast('Reshift')` → `macroTorch.player.reshift()`
    3. Line 27: `macroTorch.player.cast('Bash')` → `macroTorch.player.bash()`
    4. Line 30: `macroTorch.player.cast('Feral Charge')` → `macroTorch.player.feral_charge()`

    **druidDefend** (lines 34-49):
    5. Line 38: `macroTorch.player.cast('Barkskin (Feral)')` → `macroTorch.player.barkskin('raw')`. Reason: external isSpellReady('Barkskin (Feral)') check is already done on the same line (line 37). Using 'raw' mode avoids double-checking with possibly mismatched spell name.
    6. Line 43: `macroTorch.player.cast('Dire Bear Form')` → `macroTorch.player.dire_bear_form()`
    7. Line 46: `macroTorch.player.cast('Enrage')` → `macroTorch.player.enrage()`
    8. Line 48: `macroTorch.player.cast('Frenzied Regeneration')` → `macroTorch.player.frenzied_regeneration()`

    **druidControl** (lines 50-59):
    9. Line 55: `macroTorch.player.cast('Hibernate')` → `macroTorch.player.hibernate()`
    10. Line 57: `macroTorch.player.cast('Entangling Roots')` → `macroTorch.player.entangling_roots()`

    All replacements use nil mode (= ready check only, which is the default mode). These utility functions don't need safe mode since they have their own external checks (form checks, mana checks, isSpellReady checks). The _castSpell ready check provides an additional safety net.

    For Barkskin: using 'raw' mode is important because the external check uses 'Barkskin (Feral)' but the skill method's locale table uses 'Barkskin'. We don't want _castSpell to double-check isSpellReady with potentially wrong name. Raw mode bypasses _castSpell's ready check entirely, trusting the external isSpellReady.
  </action>
  <verify>
    <automated>grep -c "player\.cast(" classes/druid/utility.lua</automated>
    <automated>grep -c "player\.dire_bear_form\|player\.reshift\|player\.bash\|player\.feral_charge\|player\.barkskin\|player\.enrage\|player\.frenzied_regeneration\|player\.hibernate\|player\.entangling_roots" classes/druid/utility.lua</automated>
    <automated>./build.sh</automated>
  </verify>
  <acceptance_criteria>
    - druidStun: calls player.dire_bear_form(), player.reshift(), player.bash(), player.feral_charge()
    - druidDefend: calls player.barkskin('raw'), player.dire_bear_form(), player.enrage(), player.frenzied_regeneration()
    - druidControl: calls player.hibernate(), player.entangling_roots()
    - Barkskin uses 'raw' mode because external isSpellReady check uses different name variant
    - Zero remaining player.cast() calls in utility.lua — grep returns 0
    - ./build.sh exits 0
  </acceptance_criteria>
  <done>All 13 player.cast() calls in utility.lua replaced with skill method calls. Barkskin handled specially with 'raw' mode for name mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure call-site replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | utility.lua — Barkskin name mismatch | mitigate | External isSpellReady uses 'Barkskin (Feral)'; skill method locale uses 'Barkskin'; raw mode bypasses _castSpell's internal check, relying on external check |
| T-05-13 | Tampering | utility.lua — Nature's Grasp onSelf semantics | mitigate | Old code passed onSelf=true; new Type B skill method has onSelf hardcoded to true; no parameter needed |
| T-05-SC | Tampering | Package installs (npm/pip/cargo) | accept | No external packages installed |
</threat_model>

<verification>
After both tasks complete, verify:
```bash
# Zero player.cast() calls in utility.lua
grep -c "player\.cast(" classes/druid/utility.lua
# Expected: 0

# All 13 skill method calls present
grep -c "player\.\(mark_of_the_wild\|thorns\|natures_grasp\|dire_bear_form\|reshift\|bash\|feral_charge\|barkskin\|enrage\|frenzied_regeneration\|hibernate\|entangling_roots\)" classes/druid/utility.lua
# Expected: >= 13 (some called twice like dire_bear_form)

# Specific diffs
grep -c "player\.barkskin('raw')" classes/druid/utility.lua
# Expected: 1

grep -c "player\.mark_of_the_wild(nil, true)" classes/druid/utility.lua
# Expected: 1

# Build succeeds
./build.sh && echo "Build OK"
```
</verification>

<success_criteria>
- [ ] All 13 `player.cast()` calls in utility.lua replaced with skill method calls
- [ ] druidBuffs: mark_of_the_wild(nil, true), thorns(nil, true), natures_grasp()
- [ ] druidStun: dire_bear_form(), reshift(), bash(), feral_charge()
- [ ] druidDefend: barkskin('raw'), dire_bear_form(), enrage(), frenzied_regeneration()
- [ ] druidControl: hibernate(), entangling_roots()
- [ ] No `player.cast()` calls remain in utility.lua
- [ ] `./build.sh` exits 0
</success_criteria>

<output>
Create `.planning/phases/05-druid-player-cast-druid/05-05-SUMMARY.md` when done
</output>