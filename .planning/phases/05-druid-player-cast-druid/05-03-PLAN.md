---
phase: 05-druid-player-cast-druid
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - classes/druid/cat.lua
autonomous: true
requirements:
  - R8
  - D-01
  - D-03
  - D-06

must_haves:
  truths:
    - "regularAttack uses player.shred()/player.shred('safe') and player.claw()/player.claw('safe') instead of safeShred/readyShred functions"
    - "readyBite uses player.ferocious_bite('ready') instead of player.cast('Ferocious Bite')"
    - "safePounce is deleted — catAtk opener mod calls player.pounce('safe') directly"
    - "readyCower uses player.cower('ready') instead of player.cast('Cower')"
    - "safeRake records lastRakeEquippedSavagery snapshot before calling player.rake()"
    - "safeRip records lastRipEquippedSavagery and lastRipAtCp before calling player.rip()"
    - "safeTigerFury records loginContext.tigerTimer before calling player.tiger_fury()"
    - "reshiftMod uses player.reshift('ready') instead of player.cast('Reshift')"
    - "All 12 safe/ready functions deleted from cat.lua (safeShred through safeCower)"
    - "No player.cast('...') calls remain in cat.lua"
  artifacts:
    - path: "classes/druid/cat.lua"
      provides: "Druid cat form combat logic updated to use skill method calls"
      contains: "player.shred()"
      contains: "player.shred('safe')"
      contains: "player.claw()"
      contains: "player.claw('safe')"
      contains: "player.rake()"
      contains: "player.ferocious_bite('ready')"
      contains: "player.cower('ready')"
  key_links:
    - from: "classes/druid/cat.lua regularAttack"
      to: "classes/druid/Druid.lua obj.shred / obj.claw"
      via: "macroTorch.player.shred('safe') or macroTorch.player.shred()"
    - from: "classes/druid/cat.lua keepRake"
      to: "classes/druid/Druid.lua obj.rake"
      via: "macroTorch.player.rake() after snapshot recording"
    - from: "classes/druid/cat.lua oocMod"
      to: "classes/druid/Druid.lua obj.ferocious_bite"
      via: "macroTorch.player.ferocious_bite('raw')"
---

<objective>
Replace all 11 `player.cast()` call sites in `classes/druid/cat.lua` with typed skill method calls (`player.shred()`, `player.claw('safe')`, etc.) and delete all 12 safe/ready wrapper functions. Preserve snapshot side effects (lastRakeEquippedSavagery, lastRipEquippedSavagery, lastRipAtCp, tigerTimer, ffTimer) by moving them into the calling context. This is the most complex call-site migration — cat.lua has the most calls and the most side-effect dependencies.

Purpose: Complete D-01 full replacement of string-based spell casting in the cat form combat module. The skill methods defined in Plan 05-02 now serve as the sole spell casting interface.

Output: `classes/druid/cat.lua` modified — 11 call sites updated, 12 functions deleted, side effects preserved in callers.
</objective>

<execution_context>
The executor reads this plan as its primary instruction. All @file references are mandatory reads before any editing begins.
</execution_context>

<context>
@classes/druid/cat.lua (current source — 410 lines: READ BEFORE EDITING — focus on lines 46-57 regularAttack, 142-170 oocMod, 171-177 reshiftMod, 195-300 safe/ready functions, 370-390 safePounce/safeCower)
@classes/druid/Druid.lua (after Plan 05-02 — contains all ~43 skill methods; check method names before editing cat.lua)
@.planning/phases/05-druid-player-cast-druid/05-RESEARCH.md (lines 654-703: complete call-site replacement table for cat.lua + Druid.lua; lines 414-444: example migration; Pitfall 6: safeFF GCD handling)
@.planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (Section 3: exact before/after code for each replacement; Section 6: safePounce replacement)
@docs/spell_refactor_plan_druid.txt (Section 5: implementation steps)
</context>

## Artifacts this phase produces

**Modified functions in `classes/druid/cat.lua`:**
- `macroTorch.regularAttack` — replaced safeShred/readyShred/safeClaw/readyClaw with mode-based calls
- `macroTorch.readyBite` — replaced player.cast('Ferocious Bite') with player.ferocious_bite('ready')
- `macroTorch.safeBite` — unchanged logic body, delegates to readyBite (which now uses skill method)
- `macroTorch.termMod` / `macroTorch.oocMod` / `macroTorch.cp5Bite` / `macroTorch.tryBiteKillShot` — call chain updated through readyBite
- `macroTorch.reshiftMod` / `macroTorch.readyReshift` — replaced player.cast('Reshift') with player.reshift('ready')
- `macroTorch.safeRake` — snapshot recording stays inline, player.cast('Rake') replaced with player.rake('ready')
- `macroTorch.safeRip` — snapshot recording stays inline, player.cast('Rip') replaced with player.rip('ready')
- `macroTorch.safeTigerFury` — timer recording stays inline, player.cast() replaced with player.tiger_fury('ready')
- `macroTorch.safePounce` — DELETED, caller (catAtk in Druid.lua) inlined
- `macroTorch.readyCower` — replaced player.cast('Cower') with player.cower('ready')
- `macroTorch.safeCower` — deleted, caller uses player.cower('safe') directly

**Deleted functions:**
- `macroTorch.safeShred` (lines 301-303)
- `macroTorch.readyShred` (lines 304-310)
- `macroTorch.safeClaw` (lines 311-313)
- `macroTorch.readyClaw` (lines 314-320)
- `macroTorch.safePounce` (lines 370-376)

<tasks>

<task type="auto">
  <name>Task 1: Replace regularAttack with mode-based calls and delete safeShred/readyShred/safeClaw/readyClaw</name>
  <files>classes/druid/cat.lua</files>
  <read_first>
    - classes/druid/cat.lua (lines 46-57: regularAttack current implementation)
    - classes/druid/cat.lua (lines 301-320: safeShred/readyShred/safeClaw/readyClaw to be deleted)
    - .planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (lines 428-457: regularAttack replacement pattern)
    - classes/druid/Druid.lua (after Plan 05-02 — verify obj.shred and obj.claw exist with correct signatures)
  </read_first>
  <behavior>
    - Behavior 1: regularAttack when shouldUseShred=true and ooc=true calls macroTorch.player.shred() (nil mode = ready check only)
    - Behavior 2: regularAttack when shouldUseShred=true and ooc=false calls macroTorch.player.shred('safe') (safe mode = ready + energy check)
    - Behavior 3: regularAttack when shouldUseShred=false and ooc=true calls macroTorch.player.claw()
    - Behavior 4: regularAttack when shouldUseShred=false and ooc=false calls macroTorch.player.claw('safe')
    - Behavior 5: After deletion, safeShred/readyShred/safeClaw/readyClaw no longer exist in cat.lua or SM_Extend.lua
  </behavior>
  <action>
    Replace the body of `macroTorch.regularAttack(clickContext)` (lines 46-57). Remove the `shredMethod`/`clawMethod` function reference pattern. Replace with direct if/else branches using mode parameter:

    When `macroTorch.shouldUseShred(clickContext)` is true: call `macroTorch.player.shred()` if `clickContext.ooc` (ready mode, no energy consumed by ooc), else `macroTorch.player.shred('safe')` (checks energy cost).
    
    When false: call `macroTorch.player.claw()` if ooc, else `macroTorch.player.claw('safe')`.

    Then DELETE the four functions `macroTorch.safeShred` (lines 301-303), `macroTorch.readyShred` (lines 304-310), `macroTorch.safeClaw` (lines 311-313), `macroTorch.readyClaw` (lines 314-320).

    The new regularAttack returns nil (no return value), consistent with the old behavior where shredMethod/clawMethod return values were not used.
  </action>
  <verify>
    <automated>grep -c "macroTorch.safeShred\|macroTorch.readyShred\|macroTorch.safeClaw\|macroTorch.readyClaw" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.shred\|player\.claw" classes/druid/cat.lua</automated>
    <automated>./build.sh</automated>
  </verify>
  <acceptance_criteria>
    - regularAttack body replaced with 4-branch if/else using player.shred()/player.shred('safe')/player.claw()/player.claw('safe')
    - No more safeShred/readyShred/safeClaw/readyClaw function references
    - Four functions deleted from file
    - ./build.sh exits 0
  </acceptance_criteria>
  <done>regularAttack uses mode-based player.shred/player.claw calls. Four wrapper functions deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Replace player.cast calls in Bite/Pounce/Cower/Reshift and delete safePounce</name>
  <files>classes/druid/cat.lua</files>
  <read_first>
    - classes/druid/cat.lua (lines 87-170: termMod, cp5Bite, oocMod, tryBiteKillShot — bite call chain)
    - classes/druid/cat.lua (lines 171-300: reshiftMod, readyReshift)
    - classes/druid/cat.lua (lines 348-390: readyBite, safeBite, safeTigerFury, safePounce, readyCower, safeCower)
    - .planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (lines 471-534: bite/reshift replacements)
  </read_first>
  <behavior>
    - Behavior 1: readyBite calls player.ferocious_bite('ready') instead of player.cast('Ferocious Bite') — GCD+proximity checks remain external
    - Behavior 2: tryBiteKillShot calls player.ferocious_bite('raw') when cp>0 — raw mode skips all checks per D-06 (kill shot urgency)
    - Behavior 3: readyReshift calls player.reshift('ready') instead of player.cast('Reshift')
    - Behavior 4: readyCower calls player.cower('ready') instead of player.cast('Cower')
    - Behavior 5: safeTigerFury calls player.tiger_fury('ready') instead of player.cast('Tiger\'s Fury') — tigerTimer side effect preserved
    - Behavior 6: safePounce function DELETED — caller (Druid.lua catAtk line 189) will be updated in a separate task (Task 3)
    - Behavior 7: safeCower and readyCower remain as caller functions but use skill methods internally
  </behavior>
  <action>
    Replace `player.cast()` calls in the following functions. Each replacement uses the appropriate mode:

    1. `macroTorch.readyBite` (lines 351-358): Change `macroTorch.player.cast('Ferocious Bite')` to `macroTorch.player.ferocious_bite('ready')`. The external GCD+proximity checks (`isGcdOk`, `isNearBy`) remain in readyBite. The spellReady check is also kept external since it's part of the bite decision logic.

    2. `macroTorch.tryBiteKillShot` (lines 161-169): Change `macroTorch.player.cast('Ferocious Bite')` to `macroTorch.player.ferocious_bite('raw')`. Kill shot phase uses 'raw' mode — skips all checks since the target is about to die and we want to cast immediately.

    3. `macroTorch.readyReshift` (lines 288-300): Change `macroTorch.player.cast('Reshift')` to `macroTorch.player.reshift('ready')`. Keep the show() debug call before it.

    4. `macroTorch.readyCower` (lines 377-384): Change `macroTorch.player.cast('Cower')` to `macroTorch.player.cower('ready')`. Keep the show() debug call.

    5. `macroTorch.safeTigerFury` (lines 359-368): Change `macroTorch.player.cast('Tiger\'s Fury')` to `macroTorch.player.tiger_fury('ready')`. Keep the `macroTorch.loginContext.tigerTimer = GetTime()` side effect.

    6. `macroTorch.safeBite` (lines 348-349): NO change to the function body — it only does the energy check then delegates to `macroTorch.readyBite(clickContext)` which is already updated. The safeBite function stays as-is because it provides the energy gate that `_castSpell` can't cover (safeBite's energy check is clickContext.BITE_E, not the direct player.mana >= BITE_E — it's part of a chain where the caller decides to use safe vs ready).

    7. DELETE `macroTorch.safePounce` (lines 370-376). The caller in Druid.lua (catAtk line 189: `macroTorch.safePounce(clickContext)`) will be updated in Task 3 below.
  </action>
  <verify>
    <automated>grep -c "player\.cast(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.ferocious_bite(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.reshift(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.cower(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.tiger_fury(" classes/druid/cat.lua</automated>
    <automated>grep -c "macroTorch\.safePounce" classes/druid/cat.lua</automated>
    <automated>./build.sh</automated>
  </verify>
  <acceptance_criteria>
    - readyBite: calls player.ferocious_bite('ready') — grep confirms
    - tryBiteKillShot: calls player.ferocious_bite('raw') — grep confirms
    - readyReshift: calls player.reshift('ready') — grep confirms
    - readyCower: calls player.cower('ready') — grep confirms
    - safeTigerFury: calls player.tiger_fury('ready') with tigerTimer side effect — grep confirms
    - safePounce function deleted — grep returns 0
    - No remaining player.cast('...') calls in cat.lua — grep returns 0
    - safeBite and safeCower/readyCower (safe/ready chain) functions are KEPT
    - ./build.sh exits 0
  </acceptance_criteria>
  <done>All player.cast() calls in cat.lua replaced with skill method calls. safePounce deleted. readyBite/readyReshift/readyCower/safeTigerFury all use skill methods.</done>
</task>

<task type="auto">
  <name>Task 3: Replace safeRake/safeRip player.cast calls AND inline safePounce in Druid.lua catAtk caller</name>
  <files>classes/druid/cat.lua, classes/druid/Druid.lua</files>
  <read_first>
    - classes/druid/cat.lua (lines 321-347: safeRake and safeRip — snapshot side effects)
    - classes/druid/Druid.lua (lines 186-193: catAtk opener mod — calls safePounce and player.cast('Ravage'))
    - .planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (lines 503-506: safeRake/safeRip replacement pattern; lines 671-698: safePounce caller replacement)
    - classes/druid/Druid.lua (after Plan 05-02 — verify obj.ravage exists)
  </read_first>
  <behavior>
    - Behavior 1: safeRake still checks isSpellReady/isGcdOk/energy/isNearBy, then records lastRakeEquippedSavagery snapshot, then calls player.rake('ready') instead of player.cast('Rake')
    - Behavior 2: safeRip still checks isSpellReady/isGcdOk/energy/isNearBy, then records lastRipEquippedSavagery and lastRipAtCp snapshots, then calls player.rip('ready') instead of player.cast('Rip')
    - Behavior 3: catAtk opener mod in Druid.lua: safePounce call is inlined — GCD/proximity checks remain at call site, then player.pounce('safe') is called
    - Behavior 4: catAtk opener mod: player.cast('Ravage') becomes player.ravage() (ready mode, no side effects)
    - Edge case: safeRake and safeRip are KEPT as functions — they contain GCD checks and snapshot side effects that _castSpell does not handle. Only the final player.cast() call changes.
    - Edge case: safePounce deletion leaves catAtk line 189 referencing a deleted function. This must be replaced with inline logic.
  </behavior>
  <action>
    Three changes across two files:

    **In classes/druid/cat.lua:**

    1. `macroTorch.safeRake` (lines 321-332): Change `macroTorch.player.cast('Rake')` to `macroTorch.player.rake('ready')`. Keep the sidebar effect: `macroTorch.loginContext.lastRakeEquippedSavagery = macroTorch.player.isRelicEquipped('Idol of Savagery')`. Keep the external checks (isSpellReady, isGcdOk, energy, isNearBy) and the show() debug call. safeRake must stay as a function because it has complex preconditions (GCD check) and side effects (snapshot recording).

    2. `macroTorch.safeRip` (lines 333-347): Change `macroTorch.player.cast('Rip')` to `macroTorch.player.rip('ready')`. Keep: snapshot recording (`lastRipEquippedSavagery`, `lastRipAtCp`), show() call, external checks. safeRip must stay for its side effects.

    **In classes/druid/Druid.lua (catAtk, lines 186-193):**

    3. Replace the safePounce call (line 189) and the Ravage cast (line 191):
    The safePounce function performed: isSpellReady + isGcdOk + mana >= POUNCE_E + isNearBy, then cast. Since safePounce is being deleted (Task 2), we inline its logic at the call site. The `_castSpell` 'safe' mode covers ready (isSpellReady) + energy (mana check via _hasResource) + distance (nil = melee, checks target exists). But it does NOT cover isGcdOk. So we add the GCD check at the caller:

    ```
    if not target.isImmune('Pounce') and target.health >= 1500 then
        if macroTorch.isGcdOk(clickContext) and macroTorch.isNearBy(clickContext) then
            macroTorch.player.pounce('safe')
        end
    else
        player.ravage()
    end
    ```

    The `player.ravage()` call uses nil mode (= ready check only). The original `player.cast('Ravage')` had no checks, and Ravage is cast from stealth where energy is available. Using ready mode adds a spell-ready check which is beneficial.

    The `player.pounce('safe')` call checks: isSpellReady + energy >= 50 (POUNCE_E) + nil range (melee, checks target exists). Combined with the external isGcdOk + isNearBy checks, this is equivalent to the old safePounce behavior.
  </action>
  <verify>
    <automated>grep -c "player\.cast(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.cast(" classes/druid/Druid.lua</automated>
    <automated>grep -c "player\.rake(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.rip(" classes/druid/cat.lua</automated>
    <automated>grep -c "player\.pounce(" classes/druid/Druid.lua</automated>
    <automated>grep -c "player\.ravage(" classes/druid/Druid.lua</automated>
    <automated>./build.sh</automated>
  </verify>
  <acceptance_criteria>
    - safeRake calls player.rake('ready') — grep confirms
    - safeRip calls player.rip('ready') — grep confirms
    - lastRakeEquippedSavagery snapshot still recorded in safeRake
    - lastRipEquippedSavagery and lastRipAtCp still recorded in safeRip
    - catAtk opener mod: player.pounce('safe') with external isGcdOk + isNearBy checks
    - catAtk opener mod: player.ravage() replacing player.cast('Ravage')
    - Zero remaining player.cast() calls in cat.lua
    - ./build.sh exits 0
  </acceptance_criteria>
  <done>safeRake/safeRip use skill methods with side effects preserved. catAtk opener mod inlined with player.pounce('safe') and player.ravage(). No player.cast() calls remain in cat.lua.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure call-site replacement — no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | cat.lua — incorrect mode parameter | mitigate | Mode values documented per D-06; 'raw' for kill shot (no checks needed), 'ready' for ooc paths, 'safe' for normal paths; grep verification of each mode usage |
| T-05-08 | Information Disclosure | cat.lua — snapshot side effect loss | mitigate | LastRakeEquippedSavagery, lastRipEquippedSavagery, lastRipAtCp, tigerTimer explicitly preserved at call sites per Pitfall 6; grep verification that these assignments still exist |
| T-05-09 | Denial of Service | cat.lua — safePounce caller breakage | mitigate | Druid.lua catAtk line 189 safePounce call explicitly inlined with equivalent logic in Task 3 |
| T-05-SC | Tampering | Package installs (npm/pip/cargo) | accept | No external packages installed |
</threat_model>

<verification>
After all three tasks complete, verify:
```bash
# Zero player.cast() calls in cat.lua
grep -c "player\.cast(" classes/druid/cat.lua
# Expected: 0

# Key skill method calls present in cat.lua
grep -c "player\.shred\|player\.claw\|player\.rake\|player\.ferocious_bite\|player\.reshift\|player\.cower\|player\.tiger_fury" classes/druid/cat.lua
# Expected: >= 7 (at least one call per skill)

# Snapshot side effects preserved
grep -c "lastRakeEquippedSavagery" classes/druid/cat.lua
# Expected: >= 1

grep -c "lastRipEquippedSavagery" classes/druid/cat.lua
# Expected: >= 1

grep -c "tigerTimer" classes/druid/cat.lua
# Expected: >= 1

# safePounce gone from entire codebase
grep -c "macroTorch\.safePounce" classes/druid/cat.lua classes/druid/Druid.lua
# Expected: 0

# player.pounce in Druid.lua
grep -c "player\.pounce(" classes/druid/Druid.lua
# Expected: >= 1

# player.ravage in Druid.lua
grep -c "player\.ravage(" classes/druid/Druid.lua
# Expected: >= 1

# Build succeeds
./build.sh && echo "Build OK"
```
</verification>

<success_criteria>
- [ ] All `player.cast()` calls in cat.lua replaced with skill method calls
- [ ] safeShred, readyShred, safeClaw, readyClaw, safePounce: 5 functions deleted
- [ ] regularAttack uses mode-based player.shred()/player.claw()
- [ ] readyBite uses player.ferocious_bite('ready'), tryBiteKillShot uses player.ferocious_bite('raw')
- [ ] readyReshift uses player.reshift('ready'), readyCower uses player.cower('ready')
- [ ] safeTigerFury uses player.tiger_fury('ready') with tigerTimer preserved
- [ ] safeRake/safeRip use player.rake('ready')/player.rip('ready') with snapshots preserved
- [ ] Druid.lua catAtk opener mod uses player.pounce('safe') and player.ravage()
- [ ] No `player.cast()` calls remain in cat.lua
- [ ] `./build.sh` exits 0
- [ ] SM_Extend.lua cat functions still present and complete
</success_criteria>

<output>
Create `.planning/phases/05-druid-player-cast-druid/05-03-SUMMARY.md` when done
</output>