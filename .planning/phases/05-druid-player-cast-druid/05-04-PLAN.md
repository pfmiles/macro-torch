---
phase: 05-druid-player-cast-druid
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - classes/druid/bear.lua
autonomous: true
requirements:
  - R8
  - D-01
  - D-03
  - D-06

must_haves:
  truths:
    - "bearOocMod uses player.ferocious_bite('ready') instead of readySavageBite"
    - "bearOtMod uses player.growl('ready') and player.ferocious_bite('safe') instead of readyGrowl/safeSavageBite"
    - "bearDebuffMod uses player.demoralizing_roar('safe') instead of safeDemoralizingRoar"
    - "bearRegularAttack uses player.ferocious_bite('safe') and player.maul('safe') instead of safeSavageBite/safeMaul"
    - "bearAoe uses player.swipe('safe') instead of safeSwipe"
    - "bearReshiftMod uses player.reshift('ready') instead of player.cast('Reshift')"
    - "All 9 safe/ready functions deleted (safeMaul through readySwipe)"
    - "No player.cast() calls remain in bear.lua"
  artifacts:
    - path: "classes/druid/bear.lua"
      provides: "Druid bear form combat logic updated to use skill method calls"
      contains: "player.maul('safe')"
      contains: "player.ferocious_bite('safe')"
      contains: "player.swipe('safe')"
      contains: "player.growl('ready')"
      contains: "player.demoralizing_roar('safe')"
      contains: "player.reshift('ready')"
  key_links:
    - from: "classes/druid/bear.lua bearOocMod"
      to: "classes/druid/Druid.lua obj.ferocious_bite"
      via: "macroTorch.player.ferocious_bite('ready')"
    - from: "classes/druid/bear.lua bearRegularAttack"
      to: "classes/druid/Druid.lua obj.maul"
      via: "macroTorch.player.maul('safe')"
    - from: "classes/druid/bear.lua bearReshiftMod"
      to: "classes/druid/Druid.lua obj.reshift"
      via: "macroTorch.player.reshift('ready')"
---

<objective>
Replace all 6 `player.cast()` call sites in `classes/druid/bear.lua` with typed skill method calls and delete all 9 safe/ready wrapper functions. The bear module uses 'Savage Bite' as the spell name — use `player.ferocious_bite()` for this per RESEARCH.md assumption (line 676) that 'Savage Bite' is the bear form spell name for Ferocious Bite.

Purpose: Complete D-01 full replacement in the bear form combat module. Bear calls are simpler than cat — no snapshot side effects, just resource/presence checks.

Output: `classes/druid/bear.lua` modified — 6 call sites updated, 9 functions deleted.
</objective>

<execution_context>
The executor reads this plan as its primary instruction. All @file references are mandatory reads before any editing begins.
</execution_context>

<context>
@classes/druid/bear.lua (current source — 194 lines: READ BEFORE EDITING — all safe/ready functions lines 2-48, caller functions lines 49-127)
@classes/druid/Druid.lua (after Plan 05-02 — verify all bear skill methods exist: growl, swipe, maul, demoralizing_roar, feral_charge, challenging_roar, ferocious_bite, reshift)
@.planning/phases/05-druid-player-cast-druid/05-RESEARCH.md (lines 671-680: call-site replacement table for bear.lua)
@.planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (Section 4: exact before/after code for bear.lua replacements)
</context>

## Artifacts this phase produces

**Modified functions in `classes/druid/bear.lua`:**
- `macroTorch.bearOocMod` — readySavageBite(clickContext) replaced with player.ferocious_bite('ready')
- `macroTorch.bearOtMod` — readyGrowl/safeSavageBite replaced with player.growl('ready')/player.ferocious_bite('safe')
- `macroTorch.bearDebuffMod` — safeDemoralizingRoar replaced with player.demoralizing_roar('safe')
- `macroTorch.bearFFMod` — no change (reuses safeFF from Druid.lua)
- `macroTorch.bearRegularAttack` — safeSavageBite/safeMaul replaced with player.ferocious_bite('safe')/player.maul('safe')
- `macroTorch.bearReshiftMod` — player.cast('Reshift') replaced with player.reshift('ready')
- `macroTorch.bearAoe` — safeSwipe replaced with player.swipe('safe')

**Deleted functions:**
- safeMaul, readyMaul (lines 2-11)
- safeSavageBite, readySavageBite (lines 12-21)
- readyGrowl (lines 22-28)
- safeDemoralizingRoar, readyDemoralizingRoar (lines 29-38)
- safeSwipe, readySwipe (lines 39-48)

<tasks>

<task type="auto">
  <name>Task 1: Delete all 9 bear safe/ready wrapper functions</name>
  <files>classes/druid/bear.lua</files>
  <read_first>
    - classes/druid/bear.lua (lines 1-48: all 9 safe/ready functions to be deleted)
    - classes/druid/Druid.lua (after Plan 05-02 — verify all bear skill methods exist)
  </read_first>
  <behavior>
    - After deletion: bear.lua starts with `function macroTorch.bearOocMod(clickContext)` (old line 49, becomes line 1 after header)
    - After deletion: no safeMaul/readyMaul/safeSavageBite/readySavageBite/readyGrowl/safeDemoralizingRoar/readyDemoralizingRoar/safeSwipe/readySwipe exist anywhere in the codebase
    - All caller functions (bearOocMod, bearOtMod, bearDebuffMod, bearRegularAttack, bearAoe, bearReshiftMod) will be updated in Task 2 to use skill methods directly
  </behavior>
  <action>
    Delete all 9 functions in the following line ranges:
    - Lines 2-11: `macroTorch.safeMaul` + `macroTorch.readyMaul`
    - Lines 12-21: `macroTorch.safeSavageBite` + `macroTorch.readySavageBite`
    - Lines 22-28: `macroTorch.readyGrowl`
    - Lines 29-38: `macroTorch.safeDemoralizingRoar` + `macroTorch.readyDemoralizingRoar`
    - Lines 39-48: `macroTorch.safeSwipe` + `macroTorch.readySwipe`

    These are all simple energy+readiness wrapper pairs that no longer serve a purpose — the `_castSpell` method in Player.lua handles both readiness checking and resource checking via the mode parameter. The caller functions will use `player.xxx('safe')` for normal paths and `player.xxx('ready')` for ooc/GCD-checked paths.

    After deletion, bear.lua should start with `function macroTorch.bearOocMod(...)` (keeping the file header comment from line 1).
  </action>
  <verify>
    <automated>grep -c "macroTorch\.safeMaul\|macroTorch\.readyMaul\|macroTorch\.safeSavageBite\|macroTorch\.readySavageBite\|macroTorch\.readyGrowl\|macroTorch\.safeDemoralizingRoar\|macroTorch\.readyDemoralizingRoar\|macroTorch\.safeSwipe\|macroTorch\.readySwipe" classes/druid/bear.lua</automated>
  </verify>
  <acceptance_criteria>
    - All 9 function names no longer exist in bear.lua (grep returns 0 for each)
    - bear.lua starts with function macroTorch.bearOocMod after file header comment
    - build.sh will fail at this point (Task 2 hasn't updated callers yet — expected)
  </acceptance_criteria>
  <done>All 9 bear safe/ready wrapper functions deleted from bear.lua. Callers will reference deleted symbols until Task 2 completes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all bear caller functions with mode-based player skill method calls</name>
  <files>classes/druid/bear.lua</files>
  <read_first>
    - classes/druid/bear.lua (after Task 1 — callers reference deleted symbols, need update)
    - .planning/phases/05-druid-player-cast-druid/05-PATTERNS.md (lines 584-605: exact caller replacements for bear.lua)
    - classes/druid/Druid.lua (after Plan 05-02 — verify skill method signatures)
  </read_first>
  <behavior>
    - Behavior 1: bearOocMod calls `macroTorch.player.ferocious_bite('ready')` — ready mode for ooc (no rage cost, just check spell is ready)
    - Behavior 2: bearOtMod calls `macroTorch.player.growl('ready')` then `macroTorch.player.ferocious_bite('safe')` — safe mode for FB (needs rage check)
    - Behavior 3: bearDebuffMod calls `macroTorch.player.demoralizing_roar('safe')` — safe mode checks rage
    - Behavior 4: bearRegularAttack calls `macroTorch.player.ferocious_bite('safe')` then `macroTorch.player.maul('safe')` — both safe mode for rage checks
    - Behavior 5: bearAoe calls `macroTorch.player.swipe('safe')` — safe mode for rage check
    - Behavior 6: bearReshiftMod calls `macroTorch.player.reshift('ready')` instead of `macroTorch.player.cast('Reshift')`
    - Edge case: 'Savage Bite' spell name — `player.ferocious_bite` uses `{en='Ferocious Bite',zh='凶猛撕咬'}` locale table. Per RESEARCH.md (line 676), assumption is 'Savage Bite' is the bear form Ferocious Bite — same spell name lookup handles both forms.
  </behavior>
  <action>
    Replace all call sites in the 6 caller functions. Exact changes:

    1. `macroTorch.bearOocMod` (lines 49-55 after Task 1): Change `macroTorch.readySavageBite(clickContext)` to `macroTorch.player.ferocious_bite('ready')`. It becomes a single call line inside the ooc guard.

    2. `macroTorch.bearOtMod` (lines 56-76 after Task 1): Change `macroTorch.readyGrowl(clickContext)` to `macroTorch.player.growl('ready')` (line ~69). Change `macroTorch.safeSavageBite(clickContext)` to `macroTorch.player.ferocious_bite('safe')` (line ~74). Keep the if/else control flow.

    3. `macroTorch.bearDebuffMod` (lines 77-81 after Task 1): Change `macroTorch.safeDemoralizingRoar(clickContext)` to `macroTorch.player.demoralizing_roar('safe')`. Simple one-call replacement.

    4. `macroTorch.bearRegularAttack` (lines 92-103 after Task 1): Change `macroTorch.safeSavageBite(clickContext)` to `macroTorch.player.ferocious_bite('safe')` (line ~95). Change `macroTorch.safeMaul(clickContext)` to `macroTorch.player.maul('safe')` (line ~100). Keep the `if ... then return end` guard patterns.

    5. `macroTorch.bearReshiftMod` (lines 104-110 after Task 1): Change `macroTorch.player.cast('Reshift')` to `macroTorch.player.reshift('ready')`.

    6. `macroTorch.bearAoe` (lines 111-127 after Task 1): Change `macroTorch.safeSwipe(clickContext)` to `macroTorch.player.swipe('safe')`.

    Note: `bearFFMod` (line ~83-90 after Task 1) is UNCHANGED — it calls `macroTorch.safeFF(clickContext)` which is defined in Druid.lua (not in bear.lua). safeFF will still exist after this phase.

    After all replacements, verify no remaining references to the 9 deleted function names in bear.lua.
  </action>
  <verify>
    <automated>grep -c "player\.ferocious_bite\|player\.growl\|player\.maul\|player\.demoralizing_roar\|player\.swipe\|player\.reshift" classes/druid/bear.lua</automated>
    <automated>grep -c "player\.cast(" classes/druid/bear.lua</automated>
    <automated>./build.sh</automated>
  </verify>
  <acceptance_criteria>
    - No residuals: no reference to safeMaul, readyMaul, safeSavageBite, readySavageBite, readyGrowl, safeDemoralizingRoar, readyDemoralizingRoar, safeSwipe, readySwipe in bear.lua
    - bearOocMod: calls player.ferocious_bite('ready')
    - bearOtMod: calls player.growl('ready') and player.ferocious_bite('safe')
    - bearDebuffMod: calls player.demoralizing_roar('safe')
    - bearRegularAttack: calls player.ferocious_bite('safe') and player.maul('safe')
    - bearAoe: calls player.swipe('safe')
    - bearReshiftMod: calls player.reshift('ready')
    - bearFFMod: unchanged (calls safeFF from Druid.lua)
    - Zero player.cast() calls in bear.lua
    - ./build.sh exits 0
  </acceptance_criteria>
  <done>All bear form callers use player skill method calls. No player.cast() calls or deleted function references remain. bearFFMod calls safeFF (unchanged, defined in Druid.lua).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure call-site replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | bear.lua — 'Savage Bite' spell name mismatch | mitigate | RESEARCH.md assumes Ferocious Bite covers both cat and bear forms via same spell name lookup; if wrong, add separate savage_bite skill method |
| T-05-11 | Denial of Service | bear.lua — deleted function references in callers | mitigate | Task 2 explicitly updates all 6 caller functions before build verification; grep for deleted function names after Task 2 confirms zero residuals |
| T-05-SC | Tampering | Package installs (npm/pip/cargo) | accept | No external packages installed |
</threat_model>

<verification>
After both tasks complete, verify:
```bash
# Zero player.cast() calls in bear.lua
grep -c "player\.cast(" classes/druid/bear.lua
# Expected: 0

# Zero deleted function references
grep -c "safeMaul\|readyMaul\|safeSavageBite\|readySavageBite\|readyGrowl\|safeDemoralizingRoar\|readyDemoralizingRoar\|safeSwipe\|readySwipe" classes/druid/bear.lua
# Expected: 0

# Key skill method calls present
grep -c "player\.\(ferocious_bite\|growl\|maul\|demoralizing_roar\|swipe\|reshift\)" classes/druid/bear.lua
# Expected: >= 6

# Build succeeds
./build.sh && echo "Build OK"
```
</verification>

<success_criteria>
- [ ] All 9 safe/ready functions deleted from bear.lua
- [ ] All 6 callers updated to use skill method calls with correct mode
- [ ] bearOocMod: player.ferocious_bite('ready')
- [ ] bearOtMod: player.growl('ready'), player.ferocious_bite('safe')
- [ ] bearDebuffMod: player.demoralizing_roar('safe')
- [ ] bearRegularAttack: player.ferocious_bite('safe'), player.maul('safe')
- [ ] bearAoe: player.swipe('safe')
- [ ] bearReshiftMod: player.reshift('ready')
- [ ] No `player.cast()` calls in bear.lua
- [ ] `./build.sh` exits 0
</success_criteria>

<output>
Create `.planning/phases/05-druid-player-cast-druid/05-04-SUMMARY.md` when done
</output>