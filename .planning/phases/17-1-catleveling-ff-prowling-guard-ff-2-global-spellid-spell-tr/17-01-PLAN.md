---
phase: 17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/spell_id_map.lua
  - core/spell_trace_core.lua
  - core/spell_trace_immune.lua
  - core/combat_context.lua
  - classes/druid/leveling.lua
  - build_order.txt
autonomous: true
requirements:
  - REQ-17-SPELLID-MAP
  - REQ-17-FF-PROWL-GUARD

must_haves:
  truths:
    - "SPELL_NAME_TO_ID 静态映射表存在，包含 Pounce/Rake/Rip/Ferocious Bite 的中英文名称→spellId 映射"
    - "resolveSpellId() 函数存在，先查 runtime 更正后查静态基线"
    - "SpellTrace:register 支持 config.spellName 字段，内部通过 resolveSpellId 解析再调 setSpellTracing"
    - "loadSpellIdMap() 函数存在，仿 loadImmuneTable 模式绑定到 loginContext"
    - "onPlayerEnteringWorld 调用 loadSpellIdMap"
    - "catLeveling 在 isProwling 为 true 时不释放 FF"
    - "build_order.txt 中 core/spell_id_map.lua 位于 core/spell_trace_core.lua 之前"
  artifacts:
    - core/spell_id_map.lua
    - build_order.txt
  key_links:
    - "SPELL_NAME_TO_ID → resolveSpellId → SpellTrace:register spellName 分支"
    - "SM_EXTEND.spellIdMap → loadSpellIdMap → loginContext.spellIdMap → resolveSpellId runtime check"
---

<objective>
建立 global spellId 静态映射表（SPELL_NAME_TO_ID）和名称→spellId 解析基础设施，
同时在 catLeveling 中添加 FF 潜行守卫（D-01）。

Purpose: 为 Druid land tracing 提供名称驱动的 spellId 解析，消除硬编码 spellId 的跨客户端不稳定问题；
防范 catLeveling 在潜行状态下非法释放 FF。
Output: core/spell_id_map.lua（新文件），spell_trace_core.lua/spell_trace_immune.lua/combat_context.lua/leveling.lua/build_order.txt（修改）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md
@.planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md
@.planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md
@texture_map.lua
@core/spell_trace_core.lua
@core/spell_trace_immune.lua
@core/combat_context.lua
@classes/druid/leveling.lua
@build_order.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 core/spell_id_map.lua + 插入 build_order.txt</name>
  <files>core/spell_id_map.lua, build_order.txt</files>
  <read_first>
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md — 读取 D-02 和 D-11 决策，了解扁平双 key table 的数据结构和静态基准 spellId
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md — 读取 "Static Spell ID Map (new file: core/spell_id_map.lua)" 代码示例，了解完整的数据表内容
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md — 读取 Pattern 1 (spell_id_map.lua) 和 Shared Patterns (Copyright/License Header, Global Variable Initialization)
    - texture_map.lua — 参考文件结构和版权头模板
    - build_order.txt — 当前构建顺序，找到插入位置
    - classes/druid/Druid.lua — 读取 lines 25-46 确认中文技能名称 (zh entries of _castSpell locale tables)，验证 spellId 值 (lines 614-627)
  </read_first>
  <action>
    创建 core/spell_id_map.lua，仿 texture_map.lua 的纯数据文件模式：

    1. 版权头：使用 Apache 2.0 License 头（15 行，与 texture_map.lua 完全一致）

    2. 数据表直接赋值（无 `if not ... then` guard，与 texture_map.lua 一致）：
       ```
       macroTorch.SPELL_NAME_TO_ID = { ... }
       ```

    3. 表名：UPPER_SNAKE_CASE — `macroTorch.SPELL_NAME_TO_ID`

    4. 数据内容（D-02 扁平双 key table + D-11 60 级静态基准）：
       英文名称 → spellId：
         ["Pounce"] = 9827
         ["Rake"] = 1822
         ["Rip"] = 9492
         ["Ferocious Bite"] = 22557
       中文名称 → spellId（从 Druid.lua _castSpell locale tables 确认）：
         ["突袭"] = 9827        (Pounce, Druid.lua:46 zh = '突袭')
         ["斜掠"] = 1822        (Rake, Druid.lua:33 zh = '斜掠')
         ["撕扯"] = 9492        (Rip, Druid.lua:37 zh = '撕扯')
         ["凶猛撕咬"] = 22557    (Ferocious Bite, Druid.lua:41 zh = '凶猛撕咬')

    5. 注释说明：Faerie Fire (Feral) 不在此表中（D-08: land=false, immune tracing only, 不需要 spellId）

    然后修改 build_order.txt：
    - 在 line 20 (`core/combat_context.lua`) 和 line 21 (`core/spell_trace_core.lua`) 之间插入 `core/spell_id_map.lua`
    - 确保 SPELL_NAME_TO_ID 在 SpellTrace:register 执行前可用
  </action>
  <acceptance_criteria>
    - core/spell_id_map.lua 文件存在，包含 Apache 2.0 版权头
    - `grep -c "macroTorch.SPELL_NAME_TO_ID" core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"Pounce"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"突袭"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"Rake"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"斜掠"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"Rip"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"撕扯"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"Ferocious Bite"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c '"凶猛撕咬"' core/spell_id_map.lua` 结果 >= 1
    - `grep -c "9827" core/spell_id_map.lua` 结果 >= 1（Pounce 对应的 spellId）
    - `grep -c "1822" core/spell_id_map.lua` 结果 >= 1（Rake 对应的 spellId）
    - `grep -c "9492" core/spell_id_map.lua` 结果 >= 1（Rip 对应的 spellId）
    - `grep -c "22557" core/spell_id_map.lua` 结果 >= 1（Ferocious Bite 对应的 spellId）
    - `grep -c "core/spell_id_map.lua" build_order.txt` 结果 >= 1
    - build_order.txt 中 core/spell_id_map.lua 位于 core/combat_context.lua 之后且 core/spell_trace_core.lua 之前
    - `./build.sh` 成功，`grep -c "SPELL_NAME_TO_ID" SM_Extend.lua` 结果 >= 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"Pounce"' core/spell_id_map.lua && grep -c '"Rake"' core/spell_id_map.lua && grep -c '"Rip"' core/spell_id_map.lua && grep -c '"Ferocious Bite"' core/spell_id_map.lua && grep -c "core/spell_id_map.lua" build_order.txt && ./build.sh && echo "Task 1 OK"</automated>
  </verify>
  <done>
    core/spell_id_map.lua 包含 8 条中英文名称→spellId 映射（4 个技能 x 2 语言），build_order.txt 正确插入新文件，构建成功且产物包含 SPELL_NAME_TO_ID
  </done>
</task>

<task type="auto">
  <name>Task 2: FF prowling guard + resolveSpellId + SpellTrace:register spellName 支持</name>
  <files>classes/druid/leveling.lua, core/spell_trace_core.lua</files>
  <read_first>
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md — D-01 (FF guard), D-10 (spellName 字段), D-02 (映射表结构)
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md — "FF Prowling Guard", "Extended SpellTrace:register", "Pattern 2: SpellId Resolution Chain"
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md — Pattern 8 (leveling.lua FF guard) 和 Pattern 2 (spell_trace_core.lua resolveSpellId + SpellTrace:register 修改)
    - classes/druid/leveling.lua — 读取 Module 9 FF block (lines 212-224)
    - core/spell_trace_core.lua — 读取现有 SpellTrace:register API (lines 41-63)
    - core/spell_id_map.lua — Task 1 创建的映射表，确认数据已就绪
  </read_first>
  <action>
    修改两个文件：

    **Part A: classes/druid/leveling.lua — FF prowling guard (D-01)**
    在 Module 9（精灵之火(野性)，当前 lines 212-224）的条件判断中，在 `not clickContext.ooc` 行之后插入：
      `and not player.isProwling`
    更新模块注释，添加一行说明："FF cannot be cast while prowling (stealthed) — WoW game mechanic constraint"
    注意：Module 9 是 catLeveling 最后一个模块，return 后没有后续模块，所以 prowling guard 只跳过 FF 本身，不会误阻断后续逻辑。

    **Part B: core/spell_trace_core.lua — resolveSpellId + SpellTrace:register 扩展**

    B1. 在 `macroTorch.SpellTrace = {}`（line 43）和 `function macroTorch.SpellTrace:register`（line 51）之间，
       添加 `macroTorch.resolveSpellId(spellName)` 函数。
       签名：`function macroTorch.resolveSpellId(spellName)`
       逻辑：先查 runtime correction（macroTorch.loginContext.spellIdMap[spellName]），若存在则返回；
             否则返回 macroTorch.SPELL_NAME_TO_ID[spellName]（可能为 nil，调用方需处理）。

    B2. 修改 `SpellTrace:register` 的 land 分支（lines 53-58），替换 `if not config.spellId` guard：
       新逻辑：
         - 若 `config.spellName` 存在，调用 `macroTorch.resolveSpellId(config.spellName)` 获取 spellId
         - 若解析失败，fallback 到 `config.spellId`（旧式，保持向后兼容）
         - 若两者都为 nil/未解析到，输出红色错误并 return
         - 非 nil 时调用 `macroTorch.setSpellTracing(spellId, name)`（原有逻辑保持不变）
       immune 分支（lines 60-62）完全不变。

    注意：resolveSpellId 引用 macroTorch.loginContext.spellIdMap — 这个引用在 Task 3 的 loadSpellIdMap 中建立。
    在 loadSpellIdMap 调用之前（addon 初始加载时），loginContext.spellIdMap 可能为 nil，此时 resolveSpellId
    回退到 SPELL_NAME_TO_ID 静态基线。这是正确的行为 — 首次加载时没有 runtime 更正数据。
  </action>
  <acceptance_criteria>
    - leveling.lua Module 9 条件中 `not player.isProwling` 出现在 `not clickContext.ooc` 之后
    - `grep -c "not player.isProwling" classes/druid/leveling.lua` 结果 >= 1
    - Module 9 注释中包含 "prowling" 或 "stealthed" 说明
    - `grep -c "function macroTorch.resolveSpellId" core/spell_trace_core.lua` 结果 >= 1
    - resolveSpellId 位于 SpellTrace = {} 和 SpellTrace:register 之间
    - SpellTrace:register 的 land 分支引用 config.spellName
    - SpellTrace:register 的 land 分支保留 config.spellId 作为 fallback
    - `grep -c "resolveSpellId" core/spell_trace_core.lua` 结果 >= 2（定义 + 调用）
    - `grep -c "SPELL_NAME_TO_ID" core/spell_trace_core.lua` 结果 >= 1
    - `./build.sh` 成功
  </acceptance_criteria>
  <verify>
    <automated>grep -c "not player.isProwling" classes/druid/leveling.lua && grep -c "function macroTorch.resolveSpellId" core/spell_trace_core.lua && grep -c "config.spellName" core/spell_trace_core.lua && grep -c "macroTorch.SPELL_NAME_TO_ID" core/spell_trace_core.lua && ./build.sh && echo "Task 2 OK"</automated>
  </verify>
  <done>
    catLeveling FF 模块在潜行时跳过 FF 释放；resolveSpellId 函数实现名称→spellId 解析链（runtime > static）；SpellTrace:register 支持 config.spellName 字段并在 land=true 时通过 resolveSpellId 解析 spellId
  </done>
</task>

<task type="auto">
  <name>Task 3: loadSpellIdMap + onPlayerEnteringWorld 集成</name>
  <files>core/spell_trace_immune.lua, core/combat_context.lua</files>
  <read_first>
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md — D-03 (SM_EXTEND.spellIdMap 结构), D-04 (加载时机), D-05 (写入时机)
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md — "Pattern 1: SavedVariable Reference Binding" 和 "loadSpellIdMap follows the identical pattern"
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md — Pattern 3 (spell_trace_immune.lua loadSpellIdMap) 和 Pattern 5 (combat_context.lua 修改)
    - core/spell_trace_immune.lua — 读取 loadImmuneTable (lines 67-82) 和 loadDefiniteBleedingTable (lines 86-102) 作为复制模板
    - core/combat_context.lua — 读取 onPlayerEnteringWorld (lines 37-39)
  </read_first>
  <action>
    修改两个文件：

    **Part A: core/spell_trace_immune.lua — 添加 loadSpellIdMap()**

    在 `loadDefiniteBleedingTable` 函数之后（line 102 之后）添加新函数 `macroTorch.loadSpellIdMap()`。
    完全仿照 loadImmuneTable (lines 67-82) 的模式，关键差异：

    1. 函数名：`macroTorch.loadSpellIdMap`
    2. 第一个 guard：`if not macroTorch.loginContext then return end`（注意：用 loginContext，不是 context！
       因为 spellId 更正必须跨战斗持续保存；context 在 onCombatExit 被重置为 {}）
    3. SM_EXTEND 初始化：`if not SM_EXTEND then SM_EXTEND = {} end`
    4. 子表初始化：`if not SM_EXTEND.spellIdMap then SM_EXTEND.spellIdMap = {} end`
    5. 按职业初始化：`if not SM_EXTEND.spellIdMap[playerCls] then SM_EXTEND.spellIdMap[playerCls] = {} end`
    6. 引用绑定：`macroTorch.loginContext.spellIdMap = SM_EXTEND.spellIdMap[playerCls]`
       （注意：绑定到 loginContext.spellIdMap，不是 context.spellIdMap）

    代码结构与 loadImmuneTable 完全一致 — 5 步初始化 + 引用绑定，仅表名和目标 context 不同。

    **Part B: core/combat_context.lua — 在 onPlayerEnteringWorld 中添加 loadSpellIdMap 调用**

    在 `macroTorch.onPlayerEnteringWorld()` 函数中（当前 lines 37-39），在 `macroTorch.loginContext = {}` 之后添加：
      `macroTorch.loadSpellIdMap()`

    调用顺序很重要：loginContext 必须先被重置为 {}，然后 loadSpellIdMap 绑定引用到它。
  </action>
  <acceptance_criteria>
    - `grep -c "function macroTorch.loadSpellIdMap" core/spell_trace_immune.lua` 结果 >= 1
    - loadSpellIdMap 位于 loadDefiniteBleedingTable 之后
    - loadSpellIdMap 内第一个 guard 检查 `macroTorch.loginContext`（非 context）
    - loadSpellIdMap 内绑定引用到 `macroTorch.loginContext.spellIdMap`
    - `grep -c "SM_EXTEND.spellIdMap" core/spell_trace_immune.lua` 结果 >= 4（3 个 nil guard + 1 个赋值）
    - `grep -c "loadSpellIdMap" core/combat_context.lua` 结果 >= 1
    - `grep -c "loadSpellIdMap" core/combat_context.lua` 行位于 `macroTorch.loginContext = {}` 行之后
    - `./build.sh` 成功
    - `grep -c "function macroTorch.loadSpellIdMap" SM_Extend.lua` 结果 >= 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function macroTorch.loadSpellIdMap" core/spell_trace_immune.lua && grep -c "macroTorch.loginContext" core/spell_trace_immune.lua && grep -c "macroTorch.loadSpellIdMap" core/combat_context.lua && ./build.sh && echo "Task 3 OK"</automated>
  </verify>
  <done>
    loadSpellIdMap 按 loadImmuneTable 模式实现，绑定到 loginContext（跨 combat 持久），onPlayerEnteringWorld 在 loginContext 初始化后调用 loadSpellIdMap
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addon code → SM_EXTEND SavedVariable | Persisted spellId corrections loaded at startup |
| UNIT_CASTEVENT event data → spellId map | External SuperWoW spellId values written to persistent store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | SM_EXTEND.spellIdMap SavedVariable | low | accept | SM_EXTEND is auto-serialized by WoW client; WTF file tampering is outside threat model for a client-side addon |
| T-17-02 | Information Disclosure | SM_EXTEND.spellIdMap content | low | accept | Contains only spellId mappings (not secrets); WTF directory is local-only |
| T-17-SC | Tampering | npm/pip/cargo installs | none | accept | No external packages are installed in this phase |
</threat_model>

<verification>
- `./build.sh` 成功，产物包含 SPELL_NAME_TO_ID、resolveSpellId、loadSpellIdMap
- `grep -c "not player.isProwling" SM_Extend.lua` 结果 >= 1（FF guard 在产物中）
- `grep -c "SPELL_NAME_TO_ID" SM_Extend.lua` 结果 >= 1
- `grep -c "function macroTorch.resolveSpellId" SM_Extend.lua` 结果 >= 1
- `grep -c "function macroTorch.loadSpellIdMap" SM_Extend.lua` 结果 >= 1
</verification>

<success_criteria>
- SPELL_NAME_TO_ID 静态映射表可用（中英文 8 条）
- resolveSpellId 实现 runtime > static 解析链
- SpellTrace:register 支持 config.spellName（保留 config.spellId fallback）
- loadSpellIdMap 绑定到 loginContext.spellIdMap（跨 combat 持久）
- catLeveling FF 模块在潜行时跳过 FF
- build_order.txt 中 core/spell_id_map.lua 位于正确位置
- build.sh 成功
</success_criteria>

<output>
创建 .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-01-SUMMARY.md
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `macroTorch.SPELL_NAME_TO_ID` | table | core/spell_id_map.lua | Flat dual-key name→spellId map (EN + ZH) for 4 Druid land-tracing spells |
| `macroTorch.resolveSpellId(spellName)` | function | core/spell_trace_core.lua | Resolution chain: loginContext correction → static baseline |
| `macroTorch.loadSpellIdMap()` | function | core/spell_trace_immune.lua | Load persisted spellIdMap from SM_EXTEND, bind to loginContext |
| `config.spellName` field support | API extension | core/spell_trace_core.lua | New optional field in SpellTrace:register config — replaces config.spellId for land tracing |
| FF prowling guard | guard | classes/druid/leveling.lua | `not player.isProwling` condition in Module 9 |
</output>