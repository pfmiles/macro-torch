---
phase: 17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - entity/Player.lua
  - core/events.lua
  - classes/druid/Druid.lua
  - core/selftest.lua
autonomous: true
requirements:
  - REQ-17-CURRENT-CASTING
  - REQ-17-SPELLID-CORRECTION
  - REQ-17-DRUID-MIGRATE
  - REQ-17-SELFTEST

must_haves:
  truths:
    - "_castSpell sets current_casting_spell = spellName when mode is not ready"
    - "_castSpell does NOT set current_casting_spell when mode is ready"
    - "UNIT_CASTEVENT CAST checks current_casting_spell, corrects spellId mismatch and persists"
    - "UNIT_CASTEVENT CAST clears current_casting_spell = nil after processing"
    - "spellId correction syncs tracingSpells key (old staticId to new event spellId)"
    - "Druid SpellTrace:register uses spellName instead of spellId for 4 land-tracing spells"
    - "Faerie Fire (Feral) SpellTrace:register remains unchanged"
    - "Selftest Category K includes SPELL_NAME_TO_ID/resolveSpellId/loadSpellIdMap verification"
  artifacts:
    - entity/Player.lua
    - core/events.lua
    - classes/druid/Druid.lua
    - core/selftest.lua
  key_links:
    - "_castSpell spellName -> macroTorch.current_casting_spell -> UNIT_CASTEVENT resolveSpellId -> SM_EXTEND.spellIdMap write"
    - "SM_EXTEND.spellIdMap correction -> tracingSpells key migration -> subsequent spell trace matching uses new key"
    - "SpellTrace:register spellName -> resolveSpellId -> SPELL_NAME_TO_ID -> setSpellTracing(spellId, name)"
---

<objective>
在 _castSpell 施法瓶颈设置 current_casting_spell，在 UNIT_CASTEVENT 中实现 spellId 动态更正与持久化，
将 Druid 的 4 个 land-tracing 技能从硬编码 spellId 迁移到名称驱动注册，添加 Category K 自检验证。

Purpose: 桥接"我们刚刚施放了什么技能"（_castSpell 知道名称）和"UNIT_CASTEVENT 上报了什么 spellId"（事件知道 ID），
实现自动检测不同客户端版本的 spellId 差异并持久化更正。
Output: entity/Player.lua, core/events.lua, classes/druid/Druid.lua, core/selftest.lua（修改）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md
@.planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md
@.planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md
@entity/Player.lua
@core/events.lua
@classes/druid/Druid.lua
@core/selftest.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: _castSpell 设置 current_casting_spell + UNIT_CASTEVENT spellId 更正 + Druid SpellTrace:register 迁移</name>
  <files>entity/Player.lua, core/events.lua, classes/druid/Druid.lua</files>
  <read_first>
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md — D-06 (current_casting_spell 设置), D-07 (清除), D-08 (迁移范围), D-09 (其他职业不改), D-10 (spellName 字段), D-12 (runtime 更正流程)
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md — "current_casting_spell set in _castSpell", "UNIT_CASTEVENT spellId correction", "Updated Druid SpellTrace registrations", "Pattern 3: current_casting_spell Lifecycle", Anti-Patterns to Avoid (setting outside _castSpell, clearing on FAILED, binding to context)
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md — Pattern 6 (Player.lua _castSpell 修改), Pattern 4 (events.lua UNIT_CASTEVENT 修改), Pattern 7 (Druid.lua SpellTrace:register 迁移)
    - entity/Player.lua — 读取 _castSpell 函数 (lines 42-87)，理解 mode 参数语义和施法执行点
    - core/events.lua — 读取 UNIT_CASTEVENT 处理 (lines 87-97)，理解现有 trace 匹配逻辑
    - classes/druid/Druid.lua — 读取 SpellTrace:register 调用 (lines 611-633) 和 TODO 注释 (line 611)
  </read_first>
  <action>
    修改三个文件，实现 current_casting_spell 生命周期和 Druid 注册迁移：

    **Part A: entity/Player.lua — _castSpell 设置 current_casting_spell (D-06)**

    在 _castSpell 函数中，在 "4. Execute the cast" 注释块（line 77）之后、
    实际施法逻辑（lines 81-85）之前，插入 current_casting_spell 设置：

    精确位置：line 77 `-- 4. Execute the cast` 注释后，line 78 `-- self-cast:` 注释前（或等效的行位置）。

    代码：
      ```
      if mode ~= 'ready' then
          macroTorch.current_casting_spell = spellName
      end
      ```

    关键语义：
    - `mode='ready'`：仅检查可用性，不施法 → 不设置 current_casting_spell
    - `mode='raw'`：直接施法（无检查） → 设置 current_casting_spell
    - `mode='safe'`（默认 nil）：检查后施法 → 设置 current_casting_spell
    - `spellName` 变量已通过 locale 解析（lines 44-50），与 SPELL_NAME_TO_ID 的 key 匹配

    此改动覆盖所有 40+ Druid 技能方法 — 它们都通过 _castSpell 施法。

    **Part B: core/events.lua — UNIT_CASTEVENT spellId 更正 (D-12, D-07)**

    在 UNIT_CASTEVENT 处理中（lines 87-97），在现有的 `recordCastTable` 调用（line 94-96）之后、
    同一个 `castType == 'CAST'` 条件块之内，添加 spellId 更正逻辑。

    新逻辑（插入在 line 96 之后，line 97 的 `end` 之前）：

    1. 检查 `macroTorch.current_casting_spell` 是否非 nil（表示刚施放了一个技能）
    2. 若非 nil：
       a. 调用 `macroTorch.resolveSpellId(macroTorch.current_casting_spell)` 获取静态 spellId
       b. 若静态 spellId 存在且与事件的 spellId 不一致 → 执行更正：
          - 使用与 loadImmuneTable 相同的懒初始化模式初始化 SM_EXTEND.spellIdMap：
            `if not SM_EXTEND then SM_EXTEND = {} end`
            `if not SM_EXTEND.spellIdMap then SM_EXTEND.spellIdMap = {} end`
            `local playerCls = macroTorch.player.class`
            `if not SM_EXTEND.spellIdMap[playerCls] then SM_EXTEND.spellIdMap[playerCls] = {} end`
          - 写入：`SM_EXTEND.spellIdMap[playerCls][macroTorch.current_casting_spell] = spellId`
          - 同步 loginContext（可能尚未绑定）：若 `macroTorch.loginContext` 存在且 `spellIdMap` 尚未绑定，则绑定引用
          - 更新 tracingSpells key 映射：
            `macroTorch.tracingSpells[spellId] = macroTorch.tracingSpells[staticSpellId]`
            `macroTorch.tracingSpells[staticSpellId] = nil`
          - 聊天框输出：`macroTorch.show(string.format("[macro-torch] spellId corrected: %s %d -> %d", ...), 'yellow')`
       c. 清除：`macroTorch.current_casting_spell = nil`（无论是否更正都清除 — D-07）

    注意：
    - 不要清除 current_casting_spell 在 SPELLCAST_FAILED 或其他事件中（Anti-Pattern: 失败的施法不会产生 UNIT_CASTEVENT CAST 事件，
      清除它会导致后续成功的施法丢失 spellName）
    - 现有的 spell trace 匹配逻辑（lines 94-96）完全保持不变
    - current_casting_spell 永远不会为 stale — _castSpell 在每次施法时覆盖它

    **Part C: classes/druid/Druid.lua — SpellTrace:register 迁移 (D-08, D-10)**

    修改 lines 614-627 的 4 个 SpellTrace:register 调用：
    - `spellId = 9827` → `spellName = 'Pounce'`（Pounce 注册）
    - `spellId = 1822` → `spellName = 'Rake'`（Rake 注册）
    - `spellId = 9492` → `spellName = 'Rip'`（Rip 注册）
    - `spellId = 22557` → `spellName = 'Ferocious Bite'`（Ferocious Bite 注册）

    Faerie Fire (Feral) 的注册（lines 630-633）完全不修改 — land=false，不需要 spellId/spellName。

    删除 line 611 的 TODO 注释：
    `-- TODO 这里的spellId并不稳定，实践发现不同的客户端可能有不同的spellId，最好能找到稳定获取spellId的方法`
    替换为简短注释说明新的名称驱动系统：
    `-- spellId resolved via macroTorch.resolveSpellId() -> SPELL_NAME_TO_ID static map with runtime correction`

    spellName 的值与 SpellTrace:register 的第一个参数（name）一致，也与 SPELL_NAME_TO_ID 的 key 一致。
    这保证了 resolveSpellId 能正确查找到对应的 Global Spell ID。
  </action>
  <acceptance_criteria>
    - Player.lua: _castSpell 中在施法前设置 current_casting_spell，且仅在 mode ~= 'ready' 时设置
    - Player.lua: `grep -c "macroTorch.current_casting_spell" entity/Player.lua` 结果 >= 1
    - events.lua: UNIT_CASTEVENT CAST 处理中包含 spellId 更正逻辑
    - events.lua: `grep -c "macroTorch.current_casting_spell" core/events.lua` 结果 >= 2（检查 + 清除）
    - events.lua: 更正逻辑中使用 `macroTorch.resolveSpellId` 解析静态 spellId
    - events.lua: 更正逻辑中包含 tracingSpells key 迁移（删除旧 key + 添加新 key）
    - events.lua: 更正逻辑后立即清除 current_casting_spell = nil
    - events.lua: 更正使用与 loadImmuneTable 相同的 SM_EXTEND 懒初始化模式
    - Druid.lua: Pounce/Rake/Rip/Ferocious Bite 的 SpellTrace:register 使用 spellName 替代 spellId
    - Druid.lua: `grep -c "spellName" classes/druid/Druid.lua` 结果 >= 4
    - Druid.lua: `grep -c "spellId = [0-9]" classes/druid/Druid.lua` 在 SpellTrace:register 区域的结果为 0
    - Druid.lua: line 611 的 TODO 注释已替换或删除
    - Faerie Fire (Feral) 的 SpellTrace:register 保持不变（无 spellId/spellName）
    - `./build.sh` 成功
    - `grep -c "macroTorch.current_casting_spell" SM_Extend.lua` 结果 >= 3（_castSpell 设置 + events 检查 + events 清除）
    - `grep -c "spellName = 'Pounce'" SM_Extend.lua` 结果 >= 1
    - `grep -c "resolveSpellId" SM_Extend.lua` 结果 >= 1（events.lua 中调用）
  </acceptance_criteria>
  <verify>
    <automated>grep -c "macroTorch.current_casting_spell" entity/Player.lua && grep -c "macroTorch.current_casting_spell" core/events.lua && grep -c "spellName" classes/druid/Druid.lua && grep -c "resolveSpellId" core/events.lua && ./build.sh && echo "Task 1 OK"</automated>
  </verify>
  <done>
    _castSpell 在施法前设置 current_casting_spell；UNIT_CASTEVENT 实现 spellId 更正（检测→写入 SM_EXTEND→更新 tracingSpells→清除 current_casting_spell）；Druid 4 个技能从 spellId 迁移到 spellName；Faerie Fire (Feral) 注册不变；build 成功
  </done>
</task>

<task type="auto">
  <name>Task 2: Category K 自检注册 — spellId 映射系统验证</name>
  <files>core/selftest.lua</files>
  <read_first>
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-CONTEXT.md — Claude's Discretion (自检内容由 planner 决定)
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-RESEARCH.md — "Self-Test Registration Pattern", "Phase Requirements Test Map" 中的 Wave 0 缺口
    - .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-PATTERNS.md — "Self-Test Registration Pattern" 部分
    - core/selftest.lua — 读取 Category J 注册模式 (lines 574-611) 作为模板
    - core/spell_id_map.lua — 确认 SPELL_NAME_TO_ID 的内容用于测试断言
    - core/spell_trace_core.lua — 确认 resolveSpellId 函数签名
    - core/spell_trace_immune.lua — 确认 loadSpellIdMap 函数签名
  </read_first>
  <action>
    在 core/selftest.lua 中添加 Category K: spellId mapping system tests。
    插入位置：Category J (lines 574-611) 之后，"Module 4: /mt SLASH command" (line 613) 之前。

    需要注册以下自检测试（采用与 Category J 相同的风格和缩进）：

    **K1: SPELL_NAME_TO_ID table existence + key coverage (core, isOptional=false)**
    ```
    macroTorch.SelfTest:register("K: SPELL_NAME_TO_ID table exists with all 8 keys (4 spells x 2 locales)", function()
        assert(type(macroTorch.SPELL_NAME_TO_ID) == "table",
            "SPELL_NAME_TO_ID is not a table, got: " .. type(macroTorch.SPELL_NAME_TO_ID))
        -- English names
        assert(macroTorch.SPELL_NAME_TO_ID["Pounce"] == 9827, "Pounce spellId mismatch")
        assert(macroTorch.SPELL_NAME_TO_ID["Rake"] == 1822, "Rake spellId mismatch")
        assert(macroTorch.SPELL_NAME_TO_ID["Rip"] == 9492, "Rip spellId mismatch")
        assert(macroTorch.SPELL_NAME_TO_ID["Ferocious Bite"] == 22557, "Ferocious Bite spellId mismatch")
        -- Chinese names
        assert(macroTorch.SPELL_NAME_TO_ID["突袭"] == 9827, "突袭 spellId mismatch")
        assert(macroTorch.SPELL_NAME_TO_ID["斜掠"] == 1822, "斜掠 spellId mismatch")
        assert(macroTorch.SPELL_NAME_TO_ID["撕扯"] == 9492, "撕扯 spellId mismatch")
        assert(macroTorch.SPELL_NAME_TO_ID["凶猛撕咬"] == 22557, "凶猛撕咬 spellId mismatch")
    end, false)
    ```

    **K2: resolveSpellId function exists and resolves correctly (core, isOptional=false)**
    ```
    macroTorch.SelfTest:register("K: resolveSpellId function exists and resolves known spells", function()
        assert(type(macroTorch.resolveSpellId) == "function",
            "resolveSpellId is not a function, got: " .. type(macroTorch.resolveSpellId))
        assert(macroTorch.resolveSpellId("Pounce") == 9827,
            "resolveSpellId('Pounce') expected 9827, got: " .. tostring(macroTorch.resolveSpellId("Pounce")))
        assert(macroTorch.resolveSpellId("Rake") == 1822,
            "resolveSpellId('Rake') expected 1822, got: " .. tostring(macroTorch.resolveSpellId("Rake")))
        assert(macroTorch.resolveSpellId("Rip") == 9492,
            "resolveSpellId('Rip') expected 9492, got: " .. tostring(macroTorch.resolveSpellId("Rip")))
        assert(macroTorch.resolveSpellId("Ferocious Bite") == 22557,
            "resolveSpellId('Ferocious Bite') expected 22557, got: " .. tostring(macroTorch.resolveSpellId("Ferocious Bite")))
    end, false)
    ```

    **K3: resolveSpellId returns nil for unknown spell (core, isOptional=true)**
    ```
    macroTorch.SelfTest:register("K: resolveSpellId returns nil for unknown spell name", function()
        local result = macroTorch.resolveSpellId("NonexistentSpell_XYZ123")
        assert(result == nil,
            "resolveSpellId for unknown spell should return nil, got: " .. tostring(result))
    end, true)
    ```

    **K4: loadSpellIdMap function exists (core, isOptional=false)**
    ```
    macroTorch.SelfTest:register("K: loadSpellIdMap function exists and is callable", function()
        assert(type(macroTorch.loadSpellIdMap) == "function",
            "loadSpellIdMap is not a function, got: " .. type(macroTorch.loadSpellIdMap))
        -- call it — should not error (nil-safe with loginContext guard)
        local ok, err = pcall(macroTorch.loadSpellIdMap)
        assert(ok, "loadSpellIdMap should not error: " .. tostring(err))
    end, false)
    ```

    **K5: current_casting_spell lifecycle functions accessible (core, isOptional=true)**
    ```
    macroTorch.SelfTest:register("K: current_casting_spell is defined (set by _castSpell, cleared by events)", function()
        -- current_casting_spell is nil when no cast in progress (expected initial state)
        -- We only verify the global variable exists in the namespace (it's set/cleared at runtime)
        -- Not asserting on value since it depends on whether a spell is being cast
        assert(type(macroTorch.current_casting_spell) == "nil" or type(macroTorch.current_casting_spell) == "string",
            "current_casting_spell should be nil or string, got: " .. type(macroTorch.current_casting_spell))
    end, true)
    ```

    共 5 个自检注册：3 核心 (isOptional=false) + 2 可选 (isOptional=true)。
    遵循现有代码风格：SelfTest:register 的 name 以 "K:" 前缀开头，缩进 4 空格与 Category J 一致。
  </action>
  <acceptance_criteria>
    - `grep -c "Category K" core/selftest.lua` 结果 >= 1
    - `grep -c '"K:"' core/selftest.lua` 结果 >= 5
    - K1 测试验证 SPELL_NAME_TO_ID 表存在且 8 个 key 的值正确
    - K2 测试验证 resolveSpellId 函数存在且对 4 个技能返回正确的 spellId
    - K3 测试验证 resolveSpellId 对未知技能返回 nil
    - K4 测试验证 loadSpellIdMap 函数存在且调用不出错
    - K5 测试验证 current_casting_spell 变量存在且类型正确
    - K1/K2/K4 为 isOptional=false（核心），K3/K5 为 isOptional=true（可选）
    - Category K 位于 Category J 之后，"/mt SLASH command" 之前
    - `./build.sh` 成功
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"K: SPELL_NAME_TO_ID"' core/selftest.lua && grep -c '"K: resolveSpellId function"' core/selftest.lua && grep -c '"K: resolveSpellId returns nil"' core/selftest.lua && grep -c '"K: loadSpellIdMap"' core/selftest.lua && grep -c '"K: current_casting_spell"' core/selftest.lua && ./build.sh && echo "Task 2 OK"</automated>
  </verify>
  <done>
    5 个 Category K 自检注册到 selftest.lua，验证 SPELL_NAME_TO_ID 映射完整性、resolveSpellId 正确解析、loadSpellIdMap 函数可调用、current_casting_spell 存在性
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _castSpell → macroTorch.current_casting_spell | Spell name state shared between cast and event handler |
| UNIT_CASTEVENT → SM_EXTEND.spellIdMap | External spellId values persisted to SavedVariable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Spoofing | UNIT_CASTEVENT spellId arg4 | low | accept | SuperWoW API is the trusted event source; malicious spellId injection would require WoW client modification |
| T-17-04 | Denial of Service | current_casting_spell stale state | low | mitigate | _castSpell overwrites on every cast attempt; UNIT_CASTEVENT clears on CAST match; worst case is a single incorrect correction churn, not a hang |
| T-17-05 | Elevation of Privilege | SM_EXTEND.spellIdMap write access | low | accept | Same process writes that reads; no privilege boundary crossed |
| T-17-SC | Tampering | npm/pip/cargo installs | none | accept | No external packages are installed in this phase |
</threat_model>

<verification>
- `./build.sh` 成功
- `grep -c "macroTorch.current_casting_spell" SM_Extend.lua` 结果 >= 3（Player + events 检查 + events 清除）
- `grep -c "spellName = 'Pounce'" SM_Extend.lua` 结果 >= 1
- `grep -c "spellName = 'Ferocious Bite'" SM_Extend.lua` 结果 >= 1
- `grep -c "resolveSpellId" SM_Extend.lua` 结果 >= 1
- `grep -c '"K:"' SM_Extend.lua` 结果 >= 5（Category K 自检在产物中）
</verification>

<success_criteria>
- _castSpell 在 mode ~= 'ready' 时设置 current_casting_spell
- UNIT_CASTEVENT 检测 spellId 不匹配时自动更正并持久化到 SM_EXTEND.spellIdMap
- UNIT_CASTEVENT 处理后立即清除 current_casting_spell
- Druid 4 个 land-tracing 技能使用 spellName 替代硬编码 spellId
- Faerie Fire (Feral) 注册保持不变
- 5 个 Category K 自检通过（内置于 selftest.lua）
- build.sh 成功
</success_criteria>

<output>
创建 .planning/phases/17-1-catleveling-ff-prowling-guard-ff-2-global-spellid-spell-tr/17-02-SUMMARY.md
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `macroTorch.current_casting_spell` | variable (global) | entity/Player.lua (set), core/events.lua (clear) | Bridge variable: set to spellName before CastSpellByName, cleared on UNIT_CASTEVENT CAST match |
| `macroTorch.resolveSpellId(spellName)` invocation | call site | core/events.lua | Used in UNIT_CASTEVENT handler to resolve static spellId for comparison |
| `SM_EXTEND.spellIdMap[playerCls][spellName]` write | persistence mutation | core/events.lua | Runtime spellId correction written immediately on mismatch detection |
| `macroTorch.tracingSpells` key migration | table mutation | core/events.lua | Old static spellId key removed, new event spellId key added on correction |
| `spellName = 'Pounce'` / `'Rake'` / `'Rip'` / `'Ferocious Bite'` | config field | classes/druid/Druid.lua | spellName replaces hardcoded spellId in 4 SpellTrace:register calls |
| Category K self-tests (5 items) | test registrations | core/selftest.lua | K1-K5: SPELL_NAME_TO_ID, resolveSpellId, loadSpellIdMap, current_casting_spell verification |
</output>