---
phase: 27-catatk-event-driven-land-tracing-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/spell_trace_core.lua
  - core/events.lua
  - core/periodic.lua
autonomous: false
requirements: []          # no requirement IDs assigned for phase 27 (recorded visible skip, see <recorded_choices>)
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low         # derived: 0 calibration samples

must_haves:
  truths:
    - "Land events for every registered spell are generated by game events only; the 0.1s OnUpdate polling that generated land tables is gone from core (debug decision #5, core half)"
    - "Cast bridging is untouched: UNIT_CASTEVENT + _pendingCastSpellName and UNIT_SPELLCAST_SUCCEEDED branches keep their exact semantics (debug decision #2)"
    - "Intent state machine exists with states pending/landed/failed/expired and a 2s expiration, keyed spell x target-name inside loginContext (debug decision #2)"
    - "Fail is final and wins regardless of arrival order: it consumes the intent, records failTable as before, and revokes any land the intent produced (debug decision #2)"
    - "RAW_COMBATLOG production handler obeys a three-tier filter — channel whitelist on arg1, then O(N) substring finds over precompiled per-spell patterns, N = registered aura-apply spells; no per-event allocation or logging (debug decision #6)"
    - "self-hit landSource lands from 'Your <skill> hits/crits <target>' on CHAT_MSG_SPELL_SELF_DAMAGE without requiring intent pairing; aura-apply landSource requires pairing with an unconsumed cast intent within the 2s window and stamps land with the apply event time (debug decision #2)"
    - "Lua 5.0 / WoW 1.12 compliance: no # length operator, no goto/labels, no 4-argument string.find plain mode; table.insert / macroTorch.tableLen used; English comments (debug decision #7)"
    - "SM_Extend.lua remains a build artifact regenerated by build.sh; only source files are committed (debug decision #7)"
  artifacts:
    - core/spell_trace_core.lua (extended with the event-driven framework)
    - core/events.lua (production RAW_COMBATLOG handler, self-hit dispatch)
    - core/periodic.lua (LRUStack:removeMatch additive method)
  key_links:
    - recordCastTable -> loginContext.intentTable push -> pairLandIntent -> recordLandEvent -> landTable top -> ripLeft/rakeLeft (peekLandEvent consumers)
    - events.lua RAW_COMBATLOG branch -> macroTorch.auraApplySpellPatterns -> processRawAuraApply -> pairLandIntent/recordLandEvent
    - recordFailTable -> finalizeFail -> intent state flipped to failed -> landTable revocation via LRUStack:removeMatch
---

<objective>
Deliver the production-quality tracer slice of the event-driven land-tracing rework: the generic core framework that replaces polling-based land generation with game-event-driven land generation, wired end-to-end for the aura-apply land source (Rip's path) and the self-hit land source (default), plus fail-as-final revocation. Verified statically per repo convention (no Lua interpreter exists locally).

Purpose: Prove the event-driven land architecture on the core layer before druid integration and symptom-diagnosis cleanup expand it in plans 27-02/27-03. The previous mechanism (maintainLandTables + computeLandTable driven by a 0.1s periodic task, blip window (0.02,0.9]) is fragile under machine lag and is the root cause of premature Rip recasts (debug session catatk-premature-rip-recast).
Output: core/spell_trace_core.lua + core/events.lua reworked; core/periodic.lua extended; old core polling machinery deleted; old RAWDIAG recon scout replaced by the production handler.
</objective>

<recorded_choices>
- Phase 27 has no requirement IDs assigned (init.plan-phase returns phase_req_ids: null) and no SPEC/CONTEXT/RESEARCH artifacts; the spec-less probe fallback is skipped visibly per orchestrator instruction. Correctness predicates are authored from the locked debug-session decisions #1-#8 (source: .planning/debug/catatk-premature-rip-recast.md) into must_haves.truths, as directed.
- Dual-mechanism transient: between this plan (core side) and plan 27-02 (druid side) there remains one legacy land consumer (consumeDruidBattleEvents in Druid.lua). Its pseudo-cast calls into recordCastTable would now create lasting intents instead of blip-consumed casts; plan 27-02 deletes it together with all other leftover machinery before the phase ends. The phase is coherent only as a whole; no intermediate commit reaches the live game client because the user regenerates SM_Extend.lua via build.sh after the phase completes. Plan 27-02 wave ordering is the mitigation.
</recorded_choices>

<assumption_delta_decision>
- Noun promoted: the land event (multi-source — self-hit chat evidence and aura-apply RAW evidence).
- Decision: promote. The event-derived land is the primary representation; the old specific representation (polling-derived pseudo-land via computeLandTable blip) is deleted entirely rather than kept alongside, per locked debug decision #5. A future third source would register through the same landSource registry without changing the land-event consumer contract.
- Invariant test (accepted into plan 27-03, Category Q): the landTable top advances only through recordLandEvent / direct event paths, and the three legacy generators assert nil at runtime (Q-09).
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/debug/catatk-premature-rip-recast.md  (LOCKED design decisions #1-#8 — authoritative)
@.planning/STATE.md  (Phases 24-26 architecture: tracingSpells name-keyed, UNIT_SPELLCAST_SUCCEEDED, _pendingCastSpellName bridge)
@core/spell_trace_core.lua
@core/events.lua
@core/periodic.lua

Key current facts (verified at planning time):
- spell_trace_core.lua owns: SpellTrace:register, setSpellTracing, recordCastTable (dedup 0.2s + RAWDIAG arm hook at lines ~106-120), recordFailTable, maintainLandTables (registered as periodic task, 0.1s), computeLandTable (blip window 0.02/0.9), CheckDodgeParryBlockResist (6 fail patterns -> recordFailTable), consumeLandEvent/consumeFailEvent/peekCastEvent/peekFailEvent/peekLandEvent, landTableAnyMatch/landTableAllMatch, DEBUFF_LAND_LAG = 0.2.
- events.lua registers RAW_COMBATLOG under `if SUPERWOW_STRING ~= nil`; RAW_ usage: arg1 = original event name, arg2 = event text with GUIDs. The RAW_COMBATLOG branch currently holds the RAWDIAG recon scout (temporary diagnostic, decision #5 marks it for replacement). CHAT_MSG_SPELL_SELF_DAMAGE and CHAT_MSG_COMBAT_SELF_MISSES share one elseif branch that calls CheckDodgeParryBlockResist.
- periodic.lua LRUStack:new provides push, pop, anyMatch, allMatch, top/size/isEmpty accessors on an `elements` array; no removal-by-value yet.
- macroTorch.target.guid exists (entity/Unit.lua UNIT_FIELD_FUNC_MAP, SuperWoW UnitExists second return, nil without SuperWoW).
- spell_trace_immune.lua (spellsImmuneTracing periodic consumer) is NOT in scope: it consumes land/fail events produced here and must keep working unchanged.
- Sample RAW lines (evidence rows in the debug doc): "0xF130... is afflicted by Rip." arriving on CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE 1-40ms after the cast record; PvP channel variant is CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE.

NOTE on transient (see recorded_choices): after this plan and before 27-02, Druid.lua consumeDruidBattleEvents still processes Ferocious Bite land events and calls recordCastTable('Rip'/'Rake'). With intents introduced here those pseudo-casts become pending intents that die at the 2s TTL. Never reaches the live client; 27-02 removes it.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm deletion scope of the old polling land machinery (debug decision #5)</name>
  <read_first>
Read .planning/debug/catatk-premature-rip-recast.md (Resolution/fix + locked decision #5) — the full deletion list.
Read core/spell_trace_core.lua lines 67-188 (maintainLandTables, recordCastTable, computeLandTable) — what exactly disappears.
Read classes/druid/Druid.lua lines 702-736 (consumeDruidBattleEvents) — what plan 27-02 will delete as the druid half of the same decision.
  </read_first>
  <decision>Whether to permanently delete the polling-based land machinery (core here, druid side in plan 27-02), replacing it with the event-driven mechanism, exactly as locked debug decision #5 prescribes.</decision>
  <context>
The user confirmed during the debug session (catatk-premature-rip-recast) that: maintainLandTables + computeLandTable periodic machinery, consumeDruidBattleEvents periodic task, and the RAWDIAG recon scout are all deleted, replaced by the production event handler. Deleting removes the only fallback path — after this the land path depends entirely on the new framework (git history retains the old implementation). The user also decided "no SuperWoW = unsupported" for the aura-apply source, which this plan ships (debug decision #2).
  </context>
  <options>
    <option id="option-a">
      <name>Proceed per locked decision #5 (recommended)</name>
      <pros>Matches the user-confirmed design; eliminates the fragile polling path that caused premature Rip recasts; keeps the phase to the approved 3-plan shape.</pros>
      <cons>Deletions are mechanical but irreversible in this phase (recoverable only via git history); if a future defect needs the blip-window heuristic back, it must be re-implemented.</cons>
    </option>
    <option id="option-b">
      <name>Keep the deleted code as dead/disabled fallback</name>
      <pros>Instant revert possible by flipping a flag.</pros>
      <cons>Contradicts locked decision #5 (explicit "Deletions ... any dangling references"); dead code would still run pseudo-cast writes into the new intent machinery if mistakenly re-armed; requires replanning this phase.</cons>
    </option>
  </options>
  <resume-signal>Reply with the chosen option id ("option-a" expected) and any scope notes; "option-b" requires restating the desired fallback behavior so the planner can re-slice.</resume-signal>
  <acceptance_criteria>
Executor's run log records an explicit "option-a" (or "option-b" with user rationale) answer. Option-a unblocks Tasks 2-3. Option-b halts this plan and returns CHANGES REQUESTED to the planner.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Core event-driven land framework in spell_trace_core.lua + LRUStack:removeMatch</name>
  <files>core/spell_trace_core.lua, core/periodic.lua</files>
  <read_first>
Read core/spell_trace_core.lua (whole file, 300 lines).
Read core/periodic.lua (LRUStack:new factory and ES_FIELD_FUNC_MAP).
Read core/events.lua lines 98-194 (the two cast-record call sites and the RAW branch you must stay compatible with).
Read .planning/debug/catatk-premature-rip-recast.md Evidence 2026-08-28T00:50:00Z + Resolution/fix (intent state machine contract).
  </read_first>
  <action>
Implement in core/periodic.lua first:
1. Inside macroTorch.LRUStack:new (after allMatch), add a new method removeMatch(predicate): scan the elements array newest-to-oldest with a numeric for loop from macroTorch.tableLen(obj.elements) down to 1; on first element where predicate(element) is true, return table.remove(obj.elements, index); otherwise return nil. Pure addition — push, pop, anyMatch, allMatch, and the ES_FIELD_FUNC_MAP accessors stay byte-for-byte. English comment noting it exists for land-event revocation.

Then core/spell_trace_core.lua:
2. Add macroTorch.LAND_INTENT_TTL = 2 immediately after the DEBUFF_LAND_LAG line, with an English comment: intent pending-window seconds before expiry.
3. Add macroTorch.landSources = {} and macroTorch.auraApplySpellPatterns = {} next to the tracingSpells init block, guarded by `if not macroTorch.landSources then` style init like the existing tables.
4. Extend macroTorch.SpellTrace:register: inside the existing `if config.land then` branch, after setSpellTracing(name), store macroTorch.landSources[name] = config.landSource or 'self-hit'; then, only when config.landSource == 'aura-apply', precompute macroTorch.auraApplySpellPatterns[name] = ' is afflicted by ' .. name .. '%.'. Keep the two guard-invariant warning branches (spellName mismatch, immune) untouched.
5. Modify macroTorch.recordCastTable: keep the existing guards, table init, 0.2s dedup and push(GetTime()) EXACTLY as they are, then append intent registration on the same success path: ensure loginContext.intentTable[spell][mob] exists as macroTorch.LRUStack:new(32) and push a fresh intent table with fields state='pending', castAt=GetTime(), landAt=nil. Then remove the entire RAWDIAG scout arm hook block (comment + the `if spell == 'Rip' and macroTorch.context then ... end` block that writes _rawScoutActive/_rawScoutLines/_rawScoutSamples/_rawScoutStart) and the commented-out show lines can stay or go — do not leave any _rawScout reference.
6. Add macroTorch.pairLandIntent(spell, landTime): guards — spell non-nil, macroTorch.target.isCanAttack, loginContext, intentTable[spell][mob] all present, else return nil. Purge pass: iterate the intent stack newest-to-oldest; any intent with state=='pending' and (landTime - castAt) > macroTorch.LAND_INTENT_TTL becomes state='expired'. Pair pass: newest intent with state=='pending', castAt <= landTime, and (landTime - castAt) <= macroTorch.LAND_INTENT_TTL wins: set its state='landed', landAt=landTime, return it. Return nil if none. Never pairs an intent already failed/landed (that is the fail-wins guarantee).
7. Add macroTorch.recordLandEvent(spell, landTime): same guards as existing consume/peek functions (spell, target.isCanAttack, loginContext); ensure landTable[spell][mob] exists as LRUStack:new(100) and push landTime; then dispatch registered listeners: if macroTorch.landListeners and macroTorch.landListeners[spell] exist, iterate with ipairs and call each listener(spell, landTime). NOTE: this function must NOT touch intent state — intent pairing is the caller's job. English comment explaining the split (aura-apply pairs first and fails the land on no intent; self-hit pairs best-effort; bleed-renewal rewrites in 27-02 pair nothing).
8. Add macroTorch.onLandEvent(spell, fn): ensure macroTorch.landListeners is a table, then ensure macroTorch.landListeners[spell] is a list and table.insert(fn).
9. Add macroTorch.processRawAuraApply(spellName, rawText, targetGuid, now): guards spellName/rawText non-nil. Find marker ' is afflicted by ' in rawText via string.find (2 args); if not found return nil. guid = string.sub(rawText, 1, markerPos-1). If targetGuid is nil or string.lower(guid) ~= string.lower(targetGuid), return nil (current-target ownership check). Then local intent = macroTorch.pairLandIntent(spellName, now); if intent then macroTorch.recordLandEvent(spellName, now) end — land timestamp IS the apply event time (the now argument). Return intent (nil when unpaired). No logging, no table allocation beyond what pairing does.
10. Add macroTorch.finalizeFail(spell, failTime): guards like pairLandIntent. Find newest intent with state=='pending' or state=='landed' and (failTime - castAt) <= macroTorch.LAND_INTENT_TTL; none -> return. If it has state=='landed' and landAt is non-nil, remove that land entry from loginContext.landTable[spell][mob] via removeMatch with a predicate comparing equality against intent.landAt (revoke). Then set its state='failed' and landAt=nil. Fail is final regardless of whether land came first.
11. Modify macroTorch.recordFailTable: at the very end (after the existing show line), call macroTorch.finalizeFail(spell, item[1]). Everything else in it stays.
12. Add macroTorch.onSelfDamageLine(eventMsg, now): two string.find patterns in order — 'Your (.-) hits ([^%.]+)%.' and 'Your (.-) crits ([^%.]+)%.'; capture the spell name from the first capture group. Guard: if not macroTorch.tracingSpells[spell] or (macroTorch.landSources[spell] or 'self-hit') ~= 'self-hit' then return. Then call macroTorch.pairLandIntent(spell, now) best-effort (ignore its result) and macroTorch.recordLandEvent(spell, now). The 'Your <skill>' prefix is client-authenticated — no intent pairing is required to record the land.
13. DELETE the polling machinery: remove the function macroTorch.maintainLandTables together with its registerPeriodicTask('maintainLandTables', { interval = 0.1, task = ... }) line, and remove the function macroTorch.computeLandTable in full. Do NOT touch: CheckDodgeParryBlockResist and its six fail patterns, recordFailTable's failTable push, consumeLandEvent/consumeFailEvent, peekCastEvent/peekFailEvent/peekLandEvent, landTableAnyMatch/landTableAllMatch, setSpellTracing/setTraceSpellImmune/setTraceSpellImmuneByName, DEBUFF_LAND_LAG (spell_trace_immune.lua still consumes it).
14. Conventions (debug decision #7): Lua 5.0 — no # length operator (macroTorch.tableLen if a length is needed; removeMatch's numeric loop uses macroTorch.tableLen), no goto/labels, string.find without a 4th plain argument, string.sub for slicing, string.lower for the guid compare. All new comments in English. LF line endings.
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js core/spell_trace_core.lua core/periodic.lua; git -C /home/admin/workspace/macro-torch diff --check</automated>
    <human-check>None at this plan: behavioral truth comes from Category Q selftests (plan 27-03) run in-game by the user via /mt after the phase completes.</human-check>
  </verify>
  <done>core/spell_trace_core.lua defines the 6 new framework functions and the intent-bearing recordCastTable; core/periodic.lua defines removeMatch; the communication-tower polling pair is deleted; both files pass bbcheck and git diff --check.</done>
  <acceptance_criteria>
1) git grep -n 'function macroTorch%.pairLandIntent\|function macroTorch%.recordLandEvent\|function macroTorch%.processRawAuraApply\|function macroTorch%.onSelfDamageLine\|function macroTorch%.finalizeFail\|function macroTorch%.onLandEvent' -- core/spell_trace_core.lua returns 6 lines (one per function).
2) git grep -n 'removeMatch' -- core/periodic.lua returns exactly the method definition plus at most one comment line.
3) git grep -n 'function macroTorch%.maintainLandTables\|registerPeriodicTask(.maintainLandTables.\|function macroTorch%.computeLandTable' -- '*.lua' returns nothing.
4) git grep -n '_rawScout\|RAWDIAG' -- core/spell_trace_core.lua returns nothing.
5) bbcheck reports BALANCED for both files; git diff --check is empty.
6) git diff --stat shows exactly core/spell_trace_core.lua and core/periodic.lua modified (no other file, no SM_Extend.lua staged) — unless the executor already ran Task 3, in which case events.lua joins the stat.
7) ./build.sh completes and the regenerated SM_Extend.lua contains 'pairLandIntent' (grep on SM_Extend.lua); SM_Extend.lua itself stays out of the commit.
8) git grep -n 'string%.find' -- core/spell_trace_core.lua shows no 4-argument call in the newly added hunks (scan the 12 new function bodies by eye during review).
  </acceptance_criteria>
  <reversibility rating="one-way">
Deletes the polling land machinery (decision #5 core half). Authorized by Task 1 checkpoint plus locked debug decision; rollback path is git history (commit before this task). One-way because the blip-window heuristic will not be re-implemented.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: Production RAW_COMBATLOG handler + self-hit dispatch in events.lua (scout replacement)</name>
  <files>core/events.lua</files>
  <read_first>
Read core/events.lua (whole file — the RAW_COMBATLOG branch at lines 128-189 and the self-damage/misses branch at lines 98-100).
Read core/spell_trace_core.lua (the functions Task 2 added — exact names/guards to call).
Read .planning/debug/catatk-premature-rip-recast.md Evidence rows for RAW layout (arg1 = channel name, arg2 = GUID text; apply lines observed on CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE and CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE).
  </read_first>
  <action>
Rework core/events.lua exactly two places; everything else (UNIT_CASTEVENT bridge branch, UNIT_SPELLCAST_SUCCEEDED branch, combat/UI branches, event registrations including the SuperWoW-conditional RAW_COMBATLOG frame:RegisterEvent) stays byte-for-byte.

1. Replace the RAWDIAG recon scout: delete the RAWDIAG_KEYWORDS local table (lines 23-25) and replace the whole body of the existing elseif event == "RAW_COMBATLOG" branch with the production handler implementing the three-tier filter from debug decision #6:
   - Tier 1 (channel whitelist): the handler runs only when arg1 equals 'CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE' or 'CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE'; any other channel returns immediately without touching arg2.
   - Tier 2 + Tier 3 (substring precheck + registered spell match): iterate `for spellName, pattern in pairs(macroTorch.auraApplySpellPatterns)` — at most 4 precompiled literal patterns of the form ' is afflicted by ' .. spell .. '%.' — and run string.find(arg2, pattern); on the first hit call macroTorch.processRawAuraApply(spellName, arg2, macroTorch.target.guid, GetTime()) and break. The pattern embeds the 'is afflicted by ' precheck so a non-apply line costs at most N finds with zero allocation. No logging, no SavedVariables writes, no scout state anywhere.
   - guard arg2 against nil before the loop (rawLines for other channels may leave arg2 nil; the channel check already shields most, keep `if arg2 then` for safety).
2. Self-hit dispatch: in the existing `elseif event == "CHAT_MSG_COMBAT_SELF_MISSES" or event == "CHAT_MSG_SPELL_SELF_DAMAGE"` branch, keep the existing macroTorch.CheckDodgeParryBlockResist("target", event, arg1) call first (fail parse unchanged), then directly after it add: `if event == 'CHAT_MSG_SPELL_SELF_DAMAGE' then macroTorch.onSelfDamageLine(arg1, GetTime()) end`. This routes 'Your <skill> hits/crits <target> for N.' lines into the framework for every registered self-hit spell, with zero pairing requirement (decision #2).
3. English comments explaining the three tiers and the ownership rationale (hits/crits come from the client's own chat filter; RAW apply lines are target-owned so they pair with our cast intents).
4. Conventions: Lua 5.0 (no # operator, no goto), LF endings, English only.
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js core/events.lua; git -C /home/admin/workspace/macro-torch diff --check; bash /home/admin/workspace/macro-torch/build.sh</automated>
  </verify>
  <done>The RAW_COMBATLOG branch holds only the three-tier production handler; the self-damage branch dispatches onSelfDamageLine after CheckDodgeParryBlockResist; the recon scout and its keyword table are gone; events.lua passes bbcheck and the build completes.</done>
  <acceptance_criteria>
1) git grep -n 'RAWDIAG\|_rawScout\|RAWDIAG_KEYWORDS' -- core/events.lua returns nothing.
2) git grep -n "auraApplySpellPatterns\|processRawAuraApply\|onSelfDamageLine" -- core/events.lua returns the three expected call-site lines (pairs loop, process call, dispatch call).
3) grep -c "CHAT_MSG_SPELL_PERIODIC_CREATURE_DAMAGE" core/events.lua is 2 (one frame:RegisterEvent, one in the tier-1 whitelist); same for CHAT_MSG_SPELL_PERIODIC_HOSTILEPLAYER_DAMAGE.
4) git grep -n 'macroTorch%.CheckDodgeParryBlockResist' -- core/events.lua still returns its one call site (fail parse untouched).
5) The UNIT_CASTEVENT and UNIT_SPELLCAST_SUCCEEDED branch hunks are absent from git diff (bridge stays byte-for-byte).
6) bbcheck BALANCED on core/events.lua; git diff --check empty; ./build.sh exits 0 and SM_Extend.lua contains 'processRawAuraApply' (build artifact is not committed).
7) Core plan-level end-to-end link check: grep on SM_Extend.lua shows the definition and the call site of processRawAuraApply both present (framework + wiring land together in the artifact).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW combat log → addon | RAW_COMBATLOG carries every nearby unit's combat lines (untrusted text, huge volume); CHAT_MSG_SPELL_SELF_DAMAGE carries the client-filtered own-cast lines (semi-trusted, self-owned) |
| addon state | loginContext/intentTable/landTable — only addon code writes; consumers are druid clocks (ripLeft/rakeLeft) and immune tracing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Spoofing | processRawAuraApply intent pairing | medium | mitigate | Another druid's Rip apply on our target cannot produce a land: land requires pairing with our own unconsumed cast intent (created only by recordCastTable from our cast bridge) AND a case-insensitive current-target guid match (debug decision #2 ownership auth) |
| T-27-02 | Tampering | RAW_COMBATLOG untrusted text parsing | low | mitigate | String-only pattern matching (string.find/string.sub/string.lower); no loadstring/eval, no concatenation into code; malformed lines return nil harmlessly |
| T-27-03 | DoS | RAW_COMBATLOG event flood | medium | mitigate | Three-tier filter (debug decision #6): channel whitelist reduces the stream to 2 channels; per-event cost is O(N) string.find with N = registered aura-apply spells (currently 2, at most ~4 after 27-02); zero allocation/logging on the hot path |
| T-27-04 | Information Disclosure | diagnostic persistence to SavedVariables | low | accept | The recon scout (which bulk-logged raw combat lines into MACRO_TORCH_LOG) is deleted in this plan; the production handler logs nothing |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — bbcheck.js is committed source using the repo's existing node runtime only |

ASVS level: 1 (config security_asvs_level=1, security_block_on=high). No threat is unmitigated at high severity; the medium spoofing threat's mitigation IS the pairing design the plan implements.
</threat_model>

<verification>
Phase-plan level (after Tasks 2-3):
1. node .planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js core/spell_trace_core.lua core/events.lua core/periodic.lua — all BALANCED.
2. bash build.sh — strict build passes; grep pairLandIntent + processRawAuraApply in the SM_Extend.lua artifact (artifact not committed).
3. git diff --check — clean (LF endings).
4. git grep -n '_rawScout\|RAWDIAG' -- core/*.lua returns nothing (scout fully replaced).
5. git diff --stat limited to core/spell_trace_core.lua, core/events.lua, core/periodic.lua.
6. Lua 5.0 spot audit on the new hunks: no # length operator, no goto, no 4-arg string.find.
7. Behavioral layer (deferred to plan 27-03 Category Q in-game selftests, marked here as the Nyquist-aware target): intent pairing TTL, guid mismatch, fail revocation order-independence, self-hit land, FB listener presence.
</verification>

<success_criteria>
- The phrase-level goal half is met: land events for registered spells now come exclusively from game events (CHAT hit/crit lines, RAW aura-apply lines, fail events); the core polling pair and the RAWDIAG scout are gone.
- recordCastTable still records casts for any consumer and now also seeds 2s-expiring intents; fail is final and revokes landed intents in either arrival order.
- All three core files pass bbcheck and git diff --check; ./build.sh succeeds; no build artifact committed.
- Plan commit is English-conventional-message and contains only source files.
</success_criteria>

<output>
Create .planning/phases/27-catatk-event-driven-land-tracing-refactor/27-01-SUMMARY.md when done
</output>