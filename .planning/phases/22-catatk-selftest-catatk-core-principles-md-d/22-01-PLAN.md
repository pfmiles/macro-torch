---
phase: 22-catatk-selftest-catatk-core-principles-md-d
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/selftest.lua
  - build_order.txt
  - .planning/catAtk-core-principles.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "SelfTest:register calls exist for PF-01 through PF-07 pure-function tests"
    - "SelfTest:register calls exist for R9-01 through R9-03 kill-shot threshold tests"
    - "All Batch 1 tests use UnitClass('player') ~= 'Druid' guard with isOptional=true"
    - "computeReshiftEnergy tests (PF-01~03) use conditional skip when Furor==0 and no Wolfsheart"
    - "catAtk-core-principles.md Appendix D Rule 13 reads isPseudoInfiniteEnergy (not isInfiniteEnergy)"
    - "build_order.txt contains classes/druid/selftest.lua after classes/druid/combo.lua"
  artifacts:
    - path: "classes/druid/selftest.lua"
      provides: "Batch 1 SelfTest registrations (~10 tests) for catAtk pure functions"
      min_lines: 120
      contains: "UnitClass('player') == 'Druid'"
  key_links:
    - from: "build_order.txt"
      to: "classes/druid/selftest.lua"
      via: "build_order entry after classes/druid/combo.lua"
      pattern: "classes/druid/selftest.lua"
---

<objective>
Create `classes/druid/selftest.lua` with Batch 1 (~10 pure-function SelfTest registrations), fix `catAtk-core-principles.md` Appendix D Rule 13 naming per D-09, and add the new file to `build_order.txt` per D-08.

Purpose: Establish the test file skeleton with the easiest-to-verify pure function tests (no game-state dependency beyond talent/equipment checks). The doc fix ensures Appendix D matches the Phase 21 isPseudoInfiniteEnergy rename. Side-effect tests (R1-01~04, R12-01~03) are excluded per D-02. All tests preserve zero behavior change per D-06. This is the first of two commits per D-10.
Output: `classes/druid/selftest.lua` (~120 lines, license header + ~10 tests), updated `build_order.txt`, updated `.planning/catAtk-core-principles.md`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@core/selftest.lua
@classes/druid/Druid.lua
@classes/druid/combo.lua
@.planning/catAtk-core-principles.md
@.planning/catAtk-phaseB-quality.md
@.planning/phases/22-catatk-selftest-catatk-core-principles-md-d/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create classes/druid/selftest.lua with Batch 1 pure-function tests</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
Read the following files to understand existing patterns, function signatures, and clickContext fields before writing:

1. core/selftest.lua — the SelfTest:register API, existing Druid test patterns (Category F, J, M), license header format, and isOptional=true usage. Note: existing tests use `"Druid: ..."` or `"F: ..."` naming; Batch 1 tests use the new `"Principle R<n>-<nn>: ..."` convention per D-03.

2. classes/druid/Druid.lua — focus on the target functions and their clickContext field dependencies:
   - computeReshiftEnergy (line 451): reads player.talentRank('Furor') and isKeywordInEquippedItemTooltip(1, 'Wolfsheart'). No clickContext dependency; returns integer.
   - estimatePlayerDPS (line 466): accepts level parameter, returns integer. Pure math, no game state.
   - getKSThreshold (line 489): accepts level parameter, returns integer. Pure math, no game state.
   - computeErps (line 801): accepts clickContext, reads AUTO_TICK_ERPS, TIGER_ERPS, RAKE_ERPS, RIP_ERPS, POUNCE_ERPS, BERSERK_ERPS, berserk, hasEssenceOfTheRed, and calls isTigerPresent(clickContext), isRakePresent(clickContext), isRipPresent(clickContext), isPouncePresent(clickContext). Caches result in clickContext.computeErps.

3. classes/druid/combo.lua — the clickContext initialization (lines 52-109) that defines the field names these tests will preset. Key energy fields: AUTO_TICK_ERPS=10, TIGER_ERPS=10/3, RAKE_ERPS=computeRake_Erps(), RIP_ERPS=computeRip_Erps(), POUNCE_ERPS=computePounce_Erps(), BERSERK_ERPS=10.
  </read_first>
  <action>
Create new file `classes/druid/selftest.lua` with the Apache 2.0 license header (copy format from core/selftest.lua lines 1-15) and the following structure:

**File header comment:**
```
--- catAtk 原则回归测试（Phase 22）---
```

**Main guard block:**
Wrap ALL test registrations inside `if UnitClass('player') == 'Druid' then ... end` — single guard at file level per D-07. All tests use `isOptional = true`.

**Batch 1 Section header:**
```
-- Batch 1: Pure Functions (PF-01 ~ PF-07)
```

**PF-01: computeReshiftEnergy — Furor 0 + no Wolfsheart → 0**
Pattern C (conditional skip). Guard: `if player.talentRank('Furor') == 0 and not isKeywordInEquippedItemTooltip(1, 'Wolfsheart') then return end`. Assert: computeReshiftEnergy() == 0. Register as `"Principle PF-01: computeReshiftEnergy returns 0 when no Furor and no Wolfsheart"`.

**PF-02: computeReshiftEnergy — Furor 5 + Wolfsheart → 60**
Pattern C. Guard: skip if talentRank('Furor') != 5 or no Wolfsheart. Assert: computeReshiftEnergy() == 60. Register as `"Principle PF-02: computeReshiftEnergy returns 60 with Furor rank 5 + Wolfsheart"`.

**PF-03: computeReshiftEnergy — Furor 3 + no Wolfsheart → 24**
Pattern C. Guard: skip if talentRank('Furor') != 3 or has Wolfsheart. Assert: computeReshiftEnergy() == 24. Register as `"Principle PF-03: computeReshiftEnergy returns 24 with Furor rank 3 and no Wolfsheart"`.

**PF-04: estimatePlayerDPS(60) → 500**
Pattern A (pure function). Assert: estimatePlayerDPS(60) == 500. Register as `"Principle PF-04: estimatePlayerDPS(60) returns 500"`.

**PF-05: estimatePlayerDPS(40) → 200**
Pattern A. Assert: estimatePlayerDPS(40) == 200. Register as `"Principle PF-05: estimatePlayerDPS(40) returns 200"`.

**PF-06: computeErps with no buffs → 10**
Pattern B (clickContext preset). Construct ctx with: `{ AUTO_TICK_ERPS = 10, TIGER_ERPS = 10/3, RAKE_ERPS = 0, RIP_ERPS = 0, POUNCE_ERPS = 0, BERSERK_ERPS = 10, berserk = false, hasEssenceOfTheRed = false }`. Also preset `isTigerPresent`, `isRakePresent`, `isRipPresent`, `isPouncePresent` as false on ctx (computeErps reads from `macroTorch.isTigerPresent(clickContext)` etc., and those functions check ctx fields first). Assert: computeErps(ctx) == 10. Register as `"Principle PF-06: computeErps returns baseline 10 erps with no active buffs"`.

**PF-07: computeErps with Tiger + Rake → 10 + 3.33 + rakeErps**
Pattern B. Preset ctx: `{ AUTO_TICK_ERPS = 10, TIGER_ERPS = 10/3, RAKE_ERPS = 15 --[[example value]], RIP_ERPS = 0, POUNCE_ERPS = 0, BERSERK_ERPS = 10, berserk = false, hasEssenceOfTheRed = false }`. Preset `isTigerPresent = true` and `isRakePresent = true` on ctx. Assert: computeErps(ctx) == 10 + (10/3) + 15, with a tolerance of 0.01 for floating point. Register as `"Principle PF-07: computeErps adds Tiger and Rake erps to baseline"`.

**Between-section divider:**
```
-- Batch 1 Continued: Kill Shot Thresholds (R9-01 ~ R9-03)
```

**R9-01: getKSThreshold(60) → 1750**
Pattern A. Assert: getKSThreshold(60) == 1750. Register as `"Principle R9-01: getKSThreshold(60) returns 1750"`.

**R9-02: getKSThreshold(50) → 725**
Pattern A. Assert: getKSThreshold(50) == 725. Register as `"Principle R9-02: getKSThreshold(50) returns 725"`.

**R9-03: getKSThreshold(15) → 100**
Pattern A. Assert: getKSThreshold(15) == 100. Register as `"Principle R9-03: getKSThreshold(15) returns 100"`.

**End marker comment:**
```
-- End of Batch 1 -- Batch 2 tests go below (added in plan 22-02)
```

**Style conventions from existing codebase to follow:**
- License header matches core/selftest.lua exactly
- Two-space indentation
- Multi-line assert messages: `"expected X, got " .. tostring(val)`
- Each test on its own `macroTorch.SelfTest:register(...)` line
- Tests separated by blank lines within each section
- No trailing whitespace
  </action>
  <acceptance_criteria>
1. **File exists:** `test -f classes/druid/selftest.lua`
2. **License header present:** `grep "Apache License" classes/druid/selftest.lua` returns a match
3. **Druid guard present:** `grep "UnitClass('player') == 'Druid'" classes/druid/selftest.lua` returns a match
4. **10 SelfTest registrations:** `grep -c "SelfTest:register" classes/druid/selftest.lua` returns 10
5. **PF-01 conditional skip:** `grep "talentRank.*Furor.*== 0" classes/druid/selftest.lua` returns a match on a computeReshiftEnergy test
6. **PF-04 exact value:** `grep "estimatePlayerDPS(60)" classes/druid/selftest.lua` returns a match, and the same test contains `== 500`
7. **PF-06 baseline erps:** a test register call contains both `AUTO_TICK_ERPS = 10` and an assertion comparing computeErps result to 10
8. **R9-03 threshold:** a test register call contains `getKSThreshold(15)` and asserts `== 100`
9. **Batch 2 end marker:** `grep "End of Batch 1" classes/druid/selftest.lua` returns a match
10. **All isOptional=true:** no test in this file uses `isOptional = false` (all Druid-specific tests use `true`)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "SelfTest:register" /home/admin/workspace/macro-torch/classes/druid/selftest.lua 2>/dev/null && echo ">=10 expected"'</automated>
  </verify>
  <done>File classes/druid/selftest.lua exists with 10 SelfTest registrations covering PF-01 through PF-07 and R9-01 through R9-03. All tests use `isOptional = true` with `UnitClass('player') == 'Druid'` guard. PF-01~03 use conditional skip for talent/equipment checks. File ends with Batch 2 anchor comment.</done>
</task>

<task type="auto">
  <name>Task 2: Fix catAtk-core-principles.md Appendix D Rule 13 + update build_order.txt</name>
  <files>.planning/catAtk-core-principles.md, build_order.txt</files>
  <read_first>
Read the following files to see current state before editing:

1. .planning/catAtk-core-principles.md — locate Appendix D Rule 13 row (line ~458): the table cell currently reads `isInfiniteEnergy`. Per D-09, this must become `isPseudoInfiniteEnergy` to match the Phase 21 rename (D-04 in 21-CONTEXT.md). The exact text to find is `| 13 | 无限能量 | \`computeErps\`、\`isInfiniteEnergy\` | \`Druid.lua:802\`、\`combo.lua\` |`.

2. build_order.txt — locate the insertion point. D-08 says place `classes/druid/selftest.lua` after `classes/druid/cat.lua`. The safest position is after `classes/druid/combo.lua` (line 32 of current build_order.txt) since selftest.lua tests call functions from both Druid.lua, cat.lua, AND combo.lua. Insert before the `# classes/ -- Druid diagnostics` comment (line 33).
  </read_first>
  <action>
**Step A — Fix catAtk-core-principles.md (D-09):**
In `.planning/catAtk-core-principles.md`, locate the Appendix D Rule 13 row where the table cell contains `isInfiniteEnergy`. Replace `isInfiniteEnergy` with `isPseudoInfiniteEnergy` in that single location. The change is exactly one token: `isInfiniteEnergy` → `isPseudoInfiniteEnergy`. The surrounding text (backtick formatting, pipe characters, `computeErps`, file references) remains unchanged.

**Step B — Update build_order.txt (D-08):**
Insert a new line `classes/druid/selftest.lua` into `build_order.txt`. The insertion point is:
- After: `classes/druid/combo.lua` (currently line 32)
- Before: the comment `# classes/ -- Druid diagnostics (quick task 260623-wrh-druid)` (currently line 33)

Add as a single new line with no trailing whitespace. The resulting sequence should be:
```
classes/druid/combo.lua
classes/druid/selftest.lua
# classes/ -- Druid diagnostics (quick task 260623-wrh-druid)
```
  </action>
  <acceptance_criteria>
1. **D-09 token replaced:** `grep "isPseudoInfiniteEnergy" .planning/catAtk-core-principles.md` returns at least one match (the Appendix D Rule 13 entry)
2. **D-09 old token gone:** `grep "isInfiniteEnergy" .planning/catAtk-core-principles.md` returns zero matches
3. **build_order has new entry:** `grep "^classes/druid/selftest.lua$" build_order.txt` returns exactly one match
4. **build_order correct position:** the line `classes/druid/selftest.lua` appears after `classes/druid/combo.lua` and before the Druid diagnostics comment (check via `grep -n` relative line numbers)
5. **build_order integrity:** `wc -l build_order.txt` is exactly one more than before the edit (was 57 lines, should become 58)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep "isPseudoInfiniteEnergy" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md && ! grep "isInfiniteEnergy" /home/admin/workspace/macro-torch/.planning/catAtk-core-principles.md && grep -n "^classes/druid/selftest.lua$" /home/admin/workspace/macro-torch/build_order.txt && echo "ALL CHECKS PASSED"'</automated>
  </verify>
  <done>catAtk-core-principles.md Appendix D Rule 13 reads `isPseudoInfiniteEnergy` (matching Phase 21 rename). build_order.txt contains `classes/druid/selftest.lua` inserted after `classes/druid/combo.lua`. Both changes are single-token/one-line edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lua file --> WoW client | All Lua code runs within the WoW client's trust boundary. No new external inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | Test function side effects | low | accept | All tests are `isOptional=true` and use the SelfTest framework's pcall wrapping. Test functions assert on return values only; no cast/skill calls. If any test somehow triggers a side effect, the SelfTest framework isolates the crash to that test only. |
| T-22-SC | Tampering | New file creation | low | accept | The file `classes/druid/selftest.lua` is authored by the executor agent. It contains only SelfTest:register calls — no new external dependencies, no code loaded from external sources. |
</threat_model>

<verification>
After both tasks:
1. `grep -c "SelfTest:register" classes/druid/selftest.lua` returns 10
2. `grep "isInfiniteEnergy" .planning/catAtk-core-principles.md` returns zero matches (replaced by isPseudoInfiniteEnergy)
3. `grep "^classes/druid/selftest.lua$" build_order.txt` returns 1 match
4. `./build.sh && echo "Build OK"` succeeds (verifies build_order integrity)
</verification>

<success_criteria>
- `classes/druid/selftest.lua` exists with Apache 2.0 header + Batch 1 section + 10 SelfTest registrations
- All Batch 1 tests implement the test designs from `.planning/catAtk-phaseB-quality.md` section 5.2 tables:
  - PF-01 through PF-07 (pure function tests: computeReshiftEnergy, estimatePlayerDPS, computeErps)
  - R9-01 through R9-03 (kill shot threshold tests: getKSThreshold)
- `catAtk-core-principles.md` Appendix D Rule 13 updated per D-09
- `build_order.txt` includes `classes/druid/selftest.lua` after `classes/druid/combo.lua`
- Build succeeds: `./build.sh` produces valid SM_Extend.lua
</success_criteria>

<output>
Create `.planning/phases/22-catatk-selftest-catatk-core-principles-md-d/22-01-SUMMARY.md` when done
</output>