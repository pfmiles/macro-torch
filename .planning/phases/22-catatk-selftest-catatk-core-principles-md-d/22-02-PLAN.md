---
phase: 22-catatk-selftest-catatk-core-principles-md-d
plan: "02"
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - classes/druid/selftest.lua
autonomous: true
requirements: []

must_haves:
  truths:
    - "SelfTest:register calls exist for all R2-01 through R2-07 reshift decision tests"
    - "SelfTest:register calls exist for all R4-01 through R4-04 and R5-01 through R5-04 bleed primacy tests"
    - "SelfTest:register calls exist for all R6-01 through R6-06 builder choice tests"
    - "SelfTest:register calls exist for all R7-01 through R7-06 bite decision tests"
    - "SelfTest:register calls exist for all R8-01 through R8-06 FF fill tests"
    - "Game-dependent tests use conditional skip guards (Pattern C from CONTEXT.md)"
    - "All tests follow 'Principle R<n>-<nn>: description' naming per D-03"
    - "selftest.lua contains Batch 1 + Batch 2 tests, ordered by rule number 2->4->5->6->7->8"
  artifacts:
    - path: "classes/druid/selftest.lua"
      provides: "Complete SelfTest registrations: Batch 1 (~10) + Batch 2 (~28) = ~38 total tests"
      min_lines: 250
      contains: "End of Batch 2"
  key_links:
    - from: "classes/druid/selftest.lua"
      to: "classes/druid/Druid.lua"
      via: "SelfTest functions call macroTorch.shouldDoReshift, shouldCastRip, shouldUseBite, shouldUseShred, shouldCastFFDuringWaitWindow"
      pattern: "macroTorch\\.(shouldDoReshift|shouldCastRip|shouldUseBite|shouldUseShred|shouldCastFFDuringWaitWindow)"
    - from: "classes/druid/selftest.lua"
      to: "classes/druid/combo.lua"
      via: "Tests preset clickContext fields matching combo.lua clickContext initialization"
      pattern: "AUTO_TICK_ERPS|CLAW_E|SHRED_E|BITE_E|RIP_E|RAKE_E|TIGER_E"
---

<objective>
Append Batch 2 (~28 conditional decision tests) to `classes/druid/selftest.lua`, covering Rules 2/4/5/6/7/8 from catAtk-core-principles.md. Tests verify shouldDoReshift (R2), shouldCastRip (R4+R5), shouldUseShred (R6), shouldUseBite (R7), and shouldCastFFDuringWaitWindow (R8).

Purpose: Complete the full ~38-test regression suite per D-01. Batch 2 tests validate the boolean decision functions that govern catAtk module behavior, using clickContext presets (D-05) and conditional skip guards (D-04) to bypass game-state dependencies. Side-effect tests excluded per D-02. No function signature changes per D-06. This is the second of two commits per D-10.
Output: Updated `classes/druid/selftest.lua` with Batch 1 + Batch 2 = ~38 total tests, ~250+ lines
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@classes/druid/selftest.lua
@classes/druid/Druid.lua
@classes/druid/cat.lua
@classes/druid/combo.lua
@.planning/catAtk-core-principles.md
@.planning/catAtk-phaseB-quality.md
@.planning/phases/22-catatk-selftest-catatk-core-principles-md-d/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Batch 2A — R2 reshift + R4+R5 bleed primacy tests (~15 tests)</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
Read these files to understand the functions under test and their clickContext field dependencies:

1. classes/druid/selftest.lua — the file created by Plan 22-01. Contains Batch 1 tests ending with the comment `-- End of Batch 1 -- Batch 2 tests go below (added in plan 22-02)`. All new test registrations go AFTER this line, inside the existing `if UnitClass('player') == 'Druid' then ... end` block. The guard block is already open; DO NOT add another Druid guard.

2. classes/druid/cat.lua — shouldDoReshift (line 197): reads clickContext.RESHIFT_ENERGY (early exit if 0), player.isInCombat (game state), clickContext.prowling, clickContext.ooc, isKillShotOrLastChance(clickContext), computeErps(clickContext), player.mana (game state), getNextAbilityCost(clickContext). Returns `boolean, nextMove, nextAbilityCost`.

3. classes/druid/Druid.lua — focus on these functions and their clickContext field reads:
   - shouldCastRip (line 908): reads isSpellExist('Rip'), isRipPresent(clickContext), clickContext.isImmuneRip, isFightStarted(clickContext), isKillShotOrLastChance(clickContext), isNearBy(clickContext), isTrivialBattleOrPvp(clickContext), clickContext.rough, clickContext.comboPoints.
   - isKillShotOrLastChance (line 782): reads target.willDieInSeconds(2) (game state), target.health, target.healthPercent, clickContext.comboPoints (for worldboss path), getKSThreshold().
   - isTrivialBattleOrPvp (line 733): reads target.isPlayerControlled, isTrivialBattle(clickContext).
   - isTrivialBattle (line 739): caches in clickContext.isTrivialBattle; reads target.willDieInSeconds(25), target.healthMax, player.mateNearMyTargetCount, estimatePlayerDPS().
   - getNextAbilityCost (line 874): reads clickContext.BITE_E, TIGER_E, RIP_E, RAKE_E, SHRED_E, CLAW_E; calls shouldUseBite(clickContext), isTigerPresent(clickContext), shouldCastRip(clickContext), isRakePresent(clickContext), shouldUseShred(clickContext).
   - computeErps (line 801): reads clickContext.AUTO_TICK_ERPS, TIGER_ERPS, RAKE_ERPS, RIP_ERPS, POUNCE_ERPS, BERSERK_ERPS, berserk, hasEssenceOfTheRed; calls isTigerPresent/RakePresent/RipPresent/PouncePresent(clickContext).

4. .planning/catAtk-phaseB-quality.md — sections 5.2 "Rule 2", "Rule 4+5" tables contain the complete test case designs (preset ctx fields, expected results).

5. .planning/phases/22-catatk-selftest-catatk-core-principles-md-d/22-CONTEXT.md — Patterns A/B/C from code_context section for test styling.
  </read_first>
  <action>
Append Batch 2A tests to `classes/druid/selftest.lua` AFTER the `-- End of Batch 1` comment but BEFORE the `end` of the Druid guard block.

**Section header:**
```
-- Batch 2: Conditional Decision Tests

-- Rule 2: Energy Starvation — shouldDoReshift (R2-01 ~ R2-07)
```

**R2-01: shouldDoReshift with RESHIFT_ENERGY=0 → false**
Construct ctx with `{ RESHIFT_ENERGY = 0 }`. Assert: shouldDoReshift(ctx) == false. The function's first guard at line 199 returns false immediately. No game state dependencies for this path. Register as `"Principle R2-01: reshift energy 0 — no reshift triggered"`.

**R2-02: shouldDoReshift when not in combat → false**
Pattern C (conditional skip). Construct ctx with `{ RESHIFT_ENERGY = 40 }`. Guard: `if player.isInCombat then return end` (skip if we ARE in combat; this test needs the player NOT in combat). Assert: shouldDoReshift(ctx) == false. Register as `"Principle R2-02: not in combat — no reshift triggered"`.

**R2-03: shouldDoReshift when prowling → false**
Pattern C. Construct ctx with `{ RESHIFT_ENERGY = 40, prowling = true }`. Guard: `if not player.isInCombat then return end` (this test only works in combat — oocMod checks require combat context). Assert: shouldDoReshift(ctx) == false. The function's line 203 guard (`clickContext.prowling`) catches this. Register as `"Principle R2-03: prowling — no reshift triggered"`.

**R2-04: shouldDoReshift when OoC active → false**
Pattern C. Construct ctx with `{ RESHIFT_ENERGY = 40, ooc = true }`. Guard: `if not player.isInCombat then return end`. Assert: shouldDoReshift(ctx) == false. Line 203 guard catches `clickContext.ooc`. Register as `"Principle R2-04: Omen of Clarity active — no reshift triggered"`.

**R2-05: shouldDoReshift during KillShot → false**
Pattern C (heavy game-state dependency). Construct ctx with `{ RESHIFT_ENERGY = 40 }`. This test depends on `isKillShotOrLastChance(clickContext)` returning true, which requires a target dying in ≤2s (game state) or a very-low-HP target. Since this is rarely true in practice, the test's pcall-assert path is guarded: compute whether killshot is active; if not, skip with `return`. Assert: shouldDoReshift(ctx) == false if killshot is active. Register as `"Principle R2-05: kill shot phase — no reshift triggered"`.

**R2-06: shouldDoReshift with sufficient 1.5s recovery → false**
Pattern B + conditional check. Construct ctx: `{ RESHIFT_ENERGY = 40, comboPoints = 0, AUTO_TICK_ERPS = 10, TIGER_ERPS = 10/3, RAKE_ERPS = 0, RIP_ERPS = 0, POUNCE_ERPS = 0, BERSERK_ERPS = 10, berserk = false, hasEssenceOfTheRed = false, isRipPresent = true, isImmuneRip = false, isRakePresent = true, isImmuneRake = false }`. Also preset isTigerPresent = false on ctx (to force getNextAbilityCost past Tiger check; this makes getNextAbilityCost return CLAW_E=45 if Shred/Bite checks also fail). Guard: compute projected energy from actual player.mana + erps*1.5; if ceil(projected) < getNextAbilityCost, skip (test expects sufficient energy, but game state doesn't match). Assert: shouldDoReshift(ctx) == false. Register as `"Principle R2-06: 1.5s natural recovery sufficient — no reshift triggered"`.

**R2-07: shouldDoReshift with insufficient 1.5s recovery → true**
Same ctx construction as R2-06. Guard: compute projected energy; if ceil(projected) >= getNextAbilityCost, skip (test expects insufficient energy). Assert: shouldDoReshift(ctx) (the first return value is truthy). Register as `"Principle R2-07: 1.5s natural recovery insufficient — reshift triggered"`.

Note: R2-06 and R2-07 are complementary — together they fully characterize the reshift decision. In practice, the player's current mana determines which of the two actually asserts; the other skips. Since mana fluctuates during gameplay, running `/mt` multiple times may exercise both paths over time.

**Section divider:**
```
-- Rule 4+5: Bleed Primacy + Duration-Adaptive Rip — shouldCastRip (R4-01 ~ R5-04)
```

**R4-01: shouldCastRip — 5CP + no Rip + normal battle → true**
Pattern B. Construct ctx: `{ comboPoints = 5, isRipPresent = false, isImmuneRip = false, rough = false, isTrivialBattle = false }`. Note: preset `isTrivialBattle = false` directly on ctx (isTrivialBattle caches in clickContext.isTrivialBattle). Preset `isFightStarted = true` on ctx (isFightStarted also caches). Preset `isNearBy = true` on ctx. Must also handle isKillShotOrLastChance — it reads game state; preset ctx fields don't control it. The test protects against this by ensuring isKillShotOrLastChance returns false under normal conditions (target not dying). Guard: if isKillShotOrLastChance(ctx) returns true, skip. Assert: shouldCastRip(ctx) == true. Register as `"Principle R4-01: 5CP without Rip in normal battle — should cast Rip"`.

**R4-02: shouldCastRip — 5CP + Rip already present → false**
Pattern B. Construct ctx: `{ comboPoints = 5, isRipPresent = true, isImmuneRip = false }`. Also preset `isFightStarted = true, isNearBy = true` on ctx. Guard against isKillShotOrLastChance. Assert: shouldCastRip(ctx) == false. Register as `"Principle R4-02: 5CP with Rip present — should not cast Rip"`.

**R4-03: shouldCastRip — 5CP + immune to Rip → false**
Pattern B. Construct ctx: `{ comboPoints = 5, isRipPresent = false, isImmuneRip = true }`. Same fightStarted/nearBy/ks guard as above. Assert: shouldCastRip(ctx) == false. Register as `"Principle R4-03: 5CP immune to Rip — should not cast Rip"`.

**R4-04: shouldCastRip — KillShot phase → false**
Pattern B + conditional. Construct ctx: `{ comboPoints = 5, isRipPresent = false, isImmuneRip = false }`. Same fightStarted/nearBy preset. Guard: if isKillShotOrLastChance(ctx) returns false, skip (test needs killshot active). Assert: shouldCastRip(ctx) == false. Register as `"Principle R4-04: kill shot phase — should not cast Rip"`.

**R5-01: shouldCastRip — trivial battle + 1CP + no Rip → true**
Pattern B. Construct ctx: `{ comboPoints = 1, isRipPresent = false, isImmuneRip = false, isTrivialBattle = true }`. Preset `isFightStarted = true, isNearBy = true` on ctx. Guard against isKillShotOrLastChance. Assert: shouldCastRip(ctx) == true. Register as `"Principle R5-01: trivial battle 1CP without Rip — should cast Rip"`.

**R5-02: shouldCastRip — trivial battle + 2CP + no Rip → true**
Pattern B. Construct ctx: `{ comboPoints = 2, isRipPresent = false, isImmuneRip = false, isTrivialBattle = true }`. Same pattern as R5-01. Assert: shouldCastRip(ctx) == true. Register as `"Principle R5-02: trivial battle 2CP without Rip — should cast Rip"`.

**R5-03: shouldCastRip — trivial battle + 3CP + no Rip → false**
Pattern B. Construct ctx: `{ comboPoints = 3, isRipPresent = false, isImmuneRip = false, isTrivialBattle = true }`. Same guards. Assert: shouldCastRip(ctx) == false (trivial battle only Rips at CP 1-2; CP >= 3 should Bite instead). Register as `"Principle R5-03: trivial battle 3CP — should not cast Rip, should Bite instead"`.

**R5-04: shouldCastRip — normal battle + 3CP + no Rip → false**
Pattern B. Construct ctx: `{ comboPoints = 3, isRipPresent = false, isImmuneRip = false, isTrivialBattle = false }`. Same guards. Assert: shouldCastRip(ctx) == false (normal battle only Rips at CP >= 5). Register as `"Principle R5-04: normal battle 3CP — should not cast Rip (need 5CP)"`.

**End marker for Task 1:**
```
-- Rules 6+7+8: Builder Choice, Bite Trigger, FF Fill (R6-01 ~ R8-06) — continued in next task
```
  </action>
  <acceptance_criteria>
1. **R2 section present:** `grep "Rule 2: Energy Starvation" classes/druid/selftest.lua` returns a match
2. **R4+5 section present:** `grep "Rule 4+5: Bleed Primacy" classes/druid/selftest.lua` returns a match
3. **~15 new registrations:** `grep -c "SelfTest:register" classes/druid/selftest.lua` returns ~25 (10 from Batch 1 + ~15 from this task)
4. **R2-01 exists:** `grep "R2-01" classes/druid/selftest.lua` returns a match containing `RESHIFT_ENERGY` and `0`
5. **R2-07 exists:** `grep "R2-07" classes/druid/selftest.lua` returns a match
6. **R4-01 exists:** `grep "R4-01" classes/druid/selftest.lua` returns a match containing `comboPoints = 5` and `isRipPresent = false`
7. **R5-03 exists:** `grep "R5-03" classes/druid/selftest.lua` returns a match containing `comboPoints = 3` and `isTrivialBattle = true`
8. **Conditional skip guards:** at least 2 R2 tests contain `talentRank` or `isInCombat` skip guards (Pattern C)
9. **Druid guard not duplicated:** there is exactly ONE `if UnitClass('player') == 'Druid' then` block in the file (the one from Batch 1)
10. **Test ordering:** R2 tests appear before R4 tests, which appear before R5 tests (rule number order per D-01)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=/home/admin/workspace/macro-torch/classes/druid/selftest.lua; COUNT=$(grep -c "SelfTest:register" "$F" 2>/dev/null); echo "Total registrations: $COUNT"; grep -c "R2-01\|R2-02\|R2-03\|R2-04\|R2-05\|R2-06\|R2-07" "$F" && echo "R2 tests found"; grep -c "R4-01\|R4-02\|R4-03\|R4-04\|R5-01\|R5-02\|R5-03\|R5-04" "$F" && echo "R4+R5 tests found"'</automated>
  </verify>
  <done>selftest.lua contains R2-01 through R2-07 (reshift decision tests) and R4-01 through R5-04 (bleed primacy + adaptive Rip tests). All tests use Pattern B (clickContext preset) where possible and Pattern C (conditional skip) for game-state dependencies. Tests are ordered by rule number within Batch 2.</done>
</task>

<task type="auto">
  <name>Task 2: Append Batch 2B — R6 builder choice + R7 bite trigger + R8 FF fill tests (~18 tests)</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
Read these files to understand the functions under test:

1. classes/druid/selftest.lua — contains Batch 1 tests + Task 1 tests ending with the marker `-- Rules 6+7+8: Builder Choice, Bite Trigger, FF Fill (R6-01 ~ R8-06) — continued in next task`. New registrations go AFTER this line, inside the existing Druid guard block.

2. classes/druid/Druid.lua — focus on:
   - shouldUseShred (line 681): reads isSpellExist('Shred'), isRakePresent(clickContext), isRipPresent(clickContext), isPouncePresent(clickContext), computeErps(clickContext), clickContext.isPseudoInfiniteEnergy, clickContext.ooc, clickContext.isBehind, player.isBehindAttackJustFailed (game state), clickContext.CLAW_E, isTrivialBattleOrPvp(clickContext), clickContext.isImmuneRip, isRipPresent(clickContext).
   - shouldUseBite (line 933): reads isSpellExist('Ferocious Bite'), isKillShotOrLastChance(clickContext), clickContext.comboPoints, isTrivialBattleOrPvp(clickContext), clickContext.rough, clickContext.isImmuneRip, isRipPresent(clickContext).
   - shouldCastFFDuringWaitWindow (line 841): reads clickContext.ooc, target.isImmune('Faerie Fire (Feral)') (game state), shouldDoReshift(clickContext), computeErps(clickContext), player.mana (game state), getNextAbilityCost(clickContext).

3. .planning/catAtk-phaseB-quality.md — sections 5.2 "Rule 6", "Rule 7", "Rule 8" tables.
  </read_first>
  <action>
Replace the Task 1 end marker `-- Rules 6+7+8: Builder Choice, Bite Trigger, FF Fill (R6-01 ~ R8-06) — continued in next task` with the actual test registrations, then append the remaining tests and a final end marker.

**Section header:**
```
-- Rule 6: Builder Choice — shouldUseShred (R6-01 ~ R6-06)
```

**R6-01: shouldUseShred — 0 bleeds + OoC + behind → Shred (true)**
Pattern B. Construct ctx: `{ ooc = true, isBehind = true, isRakePresent = false, isRipPresent = false, isPouncePresent = false, isPseudoInfiniteEnergy = false, CLAW_E = 45 }`. Preset `isBehindAttackJustFailed = false` on player. Set computeErps to return a value < CLAW_E (e.g., ctx fields so computeErps=10; energyIn1s=10 < CLAW_E=45). The bleedCount==0 branch at line 703: `ooc=true` → returns `isBehind and not isBehindAttackJustFailed` → true. Assert: shouldUseShred(ctx) == true. Register as `"Principle R6-01: 0 bleeds OoC behind — use Shred"`.

**R6-02: shouldUseShred — 0 bleeds + infinite energy + behind → Shred (true)**
Pattern B. Construct ctx: `{ isPseudoInfiniteEnergy = true, isBehind = true, isRakePresent = false, isRipPresent = false, isPouncePresent = false }`. Preset `isBehindAttackJustFailed = false`. BleedCount=0 branch: `infiniteEnergy=true` → returns `isBehind and not isBehindAttackJustFailed`. Assert: shouldUseShred(ctx) == true. Register as `"Principle R6-02: 0 bleeds infinite energy behind — use Shred"`.

**R6-03: shouldUseShred — 2 bleeds + OoC + behind → Shred (true)**
Pattern B. Construct ctx: `{ ooc = true, isBehind = true, isPseudoInfiniteEnergy = false, isRakePresent = true, isRipPresent = true, isPouncePresent = false }`. Preset `isBehindAttackJustFailed = false`. BleedCount=2 branch (line 724): `ooc=true` → returns true. Assert: shouldUseShred(ctx) == true. Register as `"Principle R6-03: 2 bleeds OoC behind — use Shred"`.

**R6-04: shouldUseShred — 2 bleeds + no OoC + no infinite → Claw (false)**
Pattern B. Construct ctx: `{ ooc = false, isPseudoInfiniteEnergy = false, isRakePresent = true, isRipPresent = true, isPouncePresent = false }`. BleedCount=2 branch: `ooc=false, infiniteEnergy=false` → returns false (use Claw). Assert: shouldUseShred(ctx) == false. Register as `"Principle R6-04: 2 bleeds no OoC no infinite — use Claw"`.

**R6-05: shouldUseShred — 3+ bleeds → always Claw (false)**
Pattern B. Construct ctx: `{ ooc = true, isBehind = true, isPseudoInfiniteEnergy = true, isRakePresent = true, isRipPresent = true, isPouncePresent = true }`. BleedCount=3 branch (line 728): returns false regardless of OoC/infinite energy. Assert: shouldUseShred(ctx) == false. Register as `"Principle R6-05: 3+ bleeds always Claw regardless of OoC/infinite"`.

**R6-06: shouldUseShred — Rip absent + normal battle → Claw for faster CP (false)**
Pattern B. Construct ctx: `{ isBehind = true, isPseudoInfiniteEnergy = false, ooc = false, isRakePresent = true, isRipPresent = false, isPouncePresent = false, isImmuneRip = false, CLAW_E = 45 }`. Preset `isTrivialBattle = false` on ctx (so isTrivialBattleOrPvp returns false). Set computeErps so energyIn1s < CLAW_E (computeErps=10, energyIn1s=10 < 45). The bleedCount<=1 branch at line 717: `not isTrivialBattleOrPvp AND not isImmuneRip AND not isRipPresent` → returns false (Claw for faster CP). Assert: shouldUseShred(ctx) == false. Register as `"Principle R6-06: Rip absent normal battle — use Claw for faster CP generation"`.

**Section divider:**
```
-- Rule 7: GCD Priority / Bite Trigger — shouldUseBite (R7-01 ~ R7-06)
```

**R7-01: shouldUseBite — KillShot + any CP → true**
Pattern B + conditional. Construct ctx: `{ comboPoints = 3 }`. Guard: if isKillShotOrLastChance(ctx) returns false, skip. Assert: shouldUseBite(ctx) == true. Register as `"Principle R7-01: kill shot with combo points — should Bite"`.

**R7-02: shouldUseBite — KillShot + 0 CP → false**
Pattern B + conditional. Construct ctx: `{ comboPoints = 0 }`. Same killshot guard. Assert: shouldUseBite(ctx) == false (line 940: `comboPoints > 0` fails). Register as `"Principle R7-02: kill shot zero combo points — should not Bite"`.

**R7-03: shouldUseBite — 5CP + Rip present + normal → true**
Pattern B. Construct ctx: `{ comboPoints = 5, isRipPresent = true, isImmuneRip = false, isTrivialBattle = false }`. Preset `rough = false` on ctx. Guard against isKillShotOrLastChance by ensuring ctx doesn't trigger killshot. Assert: shouldUseBite(ctx) == true. Register as `"Principle R7-03: 5CP Rip present normal battle — should Bite"`.

**R7-04: shouldUseBite — 5CP + immune Rip → true**
Pattern B. Construct ctx: `{ comboPoints = 5, isImmuneRip = true, isTrivialBattle = false }`. Guard against killshot. Assert: shouldUseBite(ctx) == true (line 953: `isImmuneRip` OR `isRipPresent` — immune satisfies the condition). Register as `"Principle R7-04: 5CP immune to Rip — should Bite (no Rip option)"`.

**R7-05: shouldUseBite — trivial battle + 3CP + no Rip + not immune → true**
Pattern B. Construct ctx: `{ comboPoints = 3, isTrivialBattle = true, isRipPresent = false, isImmuneRip = false }`. Guard against killshot. Assert: shouldUseBite(ctx) == true (line 945-950: trivial + not immune + not rip + CP>=3). Register as `"Principle R7-05: trivial battle 3CP no Rip — should Bite"`.

**R7-06: shouldUseBite — trivial battle + 2CP + no Rip → false**
Pattern B. Construct ctx: `{ comboPoints = 2, isTrivialBattle = true, isRipPresent = false, isImmuneRip = false }`. Guard against killshot. Assert: shouldUseBite(ctx) == false (CP < 3, and not 5CP with Rip/immune). Register as `"Principle R7-06: trivial battle 2CP no Rip — should not Bite (need 3+ CP)"`.

**Section divider:**
```
-- Rule 8: FF Fill During Wait Window — shouldCastFFDuringWaitWindow (R8-01 ~ R8-06)
```

**R8-01: shouldCastFFDuringWaitWindow — OoC active → false**
Pattern B. Construct ctx: `{ ooc = true }`. The first guard at line 843 returns false. Assert: shouldCastFFDuringWaitWindow(ctx) == false. Register as `"Principle R8-01: Omen of Clarity active — no FF fill"`.

**R8-02: shouldCastFFDuringWaitWindow — immune to FF → false**
Pattern B + conditional. Construct ctx: `{ ooc = false }`. Guard: if `target.isImmune('Faerie Fire (Feral)')` returns false, skip (this test needs the target to be FF-immune, which is game-state dependent). Assert: shouldCastFFDuringWaitWindow(ctx) == false if guard passes. Register as `"Principle R8-02: target immune to FF — no FF fill"`.

**R8-03: shouldCastFFDuringWaitWindow — shouldDoReshift returns true → false**
Pattern B. Construct ctx: `{ ooc = false, RESHIFT_ENERGY = 40 }`. Guard: the function reads `macroTorch.shouldDoReshift(ctx)` which depends on player.mana and other game state. Skip if shouldDoReshift returns false (test expects it to return true). If the test's condition is met, assert: shouldCastFFDuringWaitWindow(ctx) == false. Register as `"Principle R8-03: reshift pending — no FF fill (reshift takes priority)"`.

**R8-04: shouldCastFFDuringWaitWindow — energy enough, no wait needed → false**
Pattern B. Construct ctx: `{ ooc = false }`. The function reads `player.mana` directly; the test checks whether current mana >= minAbilityCost. If true, the function returns false (no waiting needed). Guard: if `player.mana < minAbilityCost`, skip. Otherwise assert false. Register as `"Principle R8-04: energy sufficient — no wait window, no FF fill"`.

**R8-05: shouldCastFFDuringWaitWindow — wait < 1s → false**
Pattern B + conditional. Construct ctx: `{ ooc = false, AUTO_TICK_ERPS = 10, TIGER_ERPS = 10/3, RAKE_ERPS = 0, RIP_ERPS = 0, POUNCE_ERPS = 0, BERSERK_ERPS = 10, berserk = false, hasEssenceOfTheRed = false }`. Preset isTigerPresent=true, isRakePresent=false, isRipPresent=false, isPouncePresent=false on ctx. The test depends on specific `player.mana` and computed minAbilityCost producing a wait time <1s. Guard: compute wait seconds; skip if >= 1s. Assert false. Register as `"Principle R8-05: wait window too short (< 1s) — no FF fill"`.

**R8-06: shouldCastFFDuringWaitWindow — wait >= 1s → true**
Pattern B + conditional. Construct ctx: same energy fields as R8-05 but adjust so getNextAbilityCost and player.mana produce a wait >= 1s. Guard: compute wait seconds; skip if < 1s. Assert true. Register as `"Principle R8-06: wait window sufficient (>= 1s) — cast FF"`.

Note: R8-04 through R8-06 depend on player.mana and getNextAbilityCost, making them game-state sensitive. R8-05 and R8-06 are complementary like R2-06/R2-07 — only one asserts depending on current mana.

**Final end markers:**
```
-- End of Batch 2 -- all catAtk principle regression tests complete
```

Close the `end` of the `if UnitClass('player') == 'Druid' then` block (this `end` should already exist from Batch 1; verify it's there and the new tests are inside it).
  </action>
  <acceptance_criteria>
1. **R6 section present:** `grep "Rule 6: Builder Choice" classes/druid/selftest.lua` returns a match
2. **R7 section present:** `grep "Rule 7: GCD Priority" classes/druid/selftest.lua` returns a match
3. **R8 section present:** `grep "Rule 8: FF Fill" classes/druid/selftest.lua` returns a match
4. **~38 total registrations:** `grep -c "SelfTest:register" classes/druid/selftest.lua` returns ~38 (10 Batch 1 + ~28 Batch 2)
5. **R6-01 exists:** `grep "R6-01" classes/druid/selftest.lua` returns a match containing `shouldUseShred`
6. **R6-05 exists:** `grep "R6-05" classes/druid/selftest.lua` returns a match that asserts `false` (always Claw at 3+)
7. **R6-06 exists:** `grep "R6-06" classes/druid/selftest.lua` returns a match verifying the Rip-absent CP-acceleration logic
8. **R7-03 exists:** `grep "R7-03" classes/druid/selftest.lua` returns a match with `isRipPresent = true` and `comboPoints = 5`
9. **R8-01 exists:** `grep "R8-01" classes/druid/selftest.lua` returns a match containing `ooc = true` asserting `false`
10. **R8-06 exists:** `grep "R8-06" classes/druid/selftest.lua` returns a match
11. **All tests named per D-03:** `grep "Principle R[0-9]-[0-9][0-9]" classes/druid/selftest.lua | wc -l` returns >= 30 (all Batch 1+2 tests use this prefix)
12. **End marker present:** `grep "End of Batch 2" classes/druid/selftest.lua` returns a match
13. **Single Druid guard:** `grep -c "UnitClass.*Druid" classes/druid/selftest.lua` returns 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=/home/admin/workspace/macro-torch/classes/druid/selftest.lua; echo "=== Registration count ==="; grep -c "SelfTest:register" "$F"; echo "=== R6 tests ==="; grep -c "R6-0[1-6]" "$F"; echo "=== R7 tests ==="; grep -c "R7-0[1-6]" "$F"; echo "=== R8 tests ==="; grep -c "R8-0[1-6]" "$F"; echo "=== Druid guard count ==="; grep -c "UnitClass.*Druid" "$F"; echo "=== End markers ==="; grep -c "End of Batch" "$F"'</automated>
  </verify>
  <done>selftest.lua contains all ~38 SelfTest registrations: Batch 1 (PF-01~07, R9-01~03) + Batch 2 (R2-01~07, R4-01~04, R5-01~04, R6-01~06, R7-01~06, R8-01~06). Tests are ordered by rule number (R2 → R4 → R5 → R6 → R7 → R8) within a single Druid guard block. Game-dependent tests use conditional skip guards (Pattern C). Build succeeds: `./build.sh` produces valid SM_Extend.lua.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lua file --> WoW client | All Lua code runs within the WoW client's trust boundary. Tests only read from clickContext presets and assert on return values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-02 | Tampering | Test side effects from game-state reads | low | accept | R2-06/R2-07 and R8-04~R8-06 tests read `player.mana` (a game-state value). These are read-only operations; no mutations. The SelfTest framework's pcall wrapping isolates failures. |
| T-22-03 | Information Disclosure | Test assertion messages containing game state | low | accept | Assert error messages include `tostring(val)` for debugging — these are printed to the local chat frame only, not broadcast. Same pattern as existing Selftest tests (Category F, J, K, L, M, N). |
</threat_model>

<verification>
After both tasks:
1. `grep -c "SelfTest:register" classes/druid/selftest.lua` returns ~38 (confirm batches complete)
2. Each rule section (R2, R4+5, R6, R7, R8) has tests with appropriately named registers
3. All tests use `isOptional = true` (third argument to SelfTest:register)
4. All tests use `"Principle R<n>-<nn>: description"` naming per D-03
5. Single `if UnitClass('player') == 'Druid' then` guard block wraps all tests
6. `./build.sh && echo "Build OK"` succeeds
7. No behavior changes to any existing function (verify via git diff: only `classes/druid/selftest.lua` is modified, and it only contains SelfTest:register calls)
</verification>

<success_criteria>
- `classes/druid/selftest.lua` contains ~38 total SelfTest registrations (~250+ lines)
- Batch 2 tests cover all 6 rules (R2, R4, R5, R6, R7, R8) as specified in `.planning/catAtk-phaseB-quality.md` section 5.2 tables
- All test implementations follow the three patterns from 22-CONTEXT.md (Pattern A for pure functions, Pattern B for clickContext preset, Pattern C for conditional skip)
- Tests are ordered by rule number within Batch 2: R2-01..07, R4-01..04, R5-01..04, R6-01..06, R7-01..06, R8-01..06
- Build succeeds and no existing functions are modified
</success_criteria>

<output>
Create `.planning/phases/22-catatk-selftest-catatk-core-principles-md-d/22-02-SUMMARY.md` when done
</output>