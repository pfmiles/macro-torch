---
phase: 23-idol-dance-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/Druid.lua
  - classes/druid/selftest.lua
autonomous: true
requirements:
  - REQ-23-GAP1
  - REQ-23-GAP2
  - REQ-23-GAP4
  - REQ-23-TEST

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - computeNormalRelic returns Fero/Rot for fast combat/PvP targets regardless of Rip status (Gap 1 fixed)
    - computeNormalRelic returns Fero/Rot for all immune Rip targets in combat (Gap 2 fixed)
    - computeNormalRelic returns Fero/Rot when Rip is present on non-immune target in normal combat
    - computeNormalRelic returns Idol of Savagery when Rip absent, not immune, normal combat
    - computeNormalRelic returns Idol of Savagery for non-combat non-immune targets (pre-switch preserved per D-02)
    - computeNormalRelic returns Fero/Rot for non-combat immune Rip targets (preserved)
    - recoverNormalRelic bypasses energy check and directly equips when target.distance >= 20 (Gap 4 fixed per D-03/D-04)
    - All 7 Category O SelfTest registrations exist and reference their decision IDs in comments
  artifacts:
    - Rewritten macroTorch.computeNormalRelic function body in classes/druid/Druid.lua replacing lines 362-388
    - Distance bypass insertion (4 lines) in macroTorch.recoverNormalRelic in classes/druid/Druid.lua before line 435
    - 7 Category O SelfTest registrations appended to classes/druid/selftest.lua
  key_links:
    - computeNormalRelic() called at classes/druid/combo.lua:103 to populate clickContext.normalRelic
    - recoverNormalRelic() called at classes/druid/combo.lua:112 with clickContext.normalRelic
    - selectFerocityOrEmeraldRot() unchanged, called by computeNormalRelic
    - SelfTest:register(name, fn, isOptional) API at core/selftest.lua:34
---

<objective>
Fix two confirmed idol dance logic gaps in computeNormalRelic() and add distance optimization to recoverNormalRelic(). Add Category O SelfTest coverage.

**Purpose:** Gap 1 wastes 1.5s GCD switching to Savagery in fast combat/PvP when Builder idol (Fero/Rot) is always better. Gap 2 uses useless Savagery on immune Rip targets. Gap 4 forces a needless energy wait at 20+ yd distance when running time already covers the GCD.

**Output:** Modified macroTorch.computeNormalRelic (Druid.lua ~362-387), modified macroTorch.recoverNormalRelic (Druid.lua ~435), and 7 new SelfTest registrations (selftest.lua)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-idol-dance-refactor/23-CONTEXT.md
@.planning/phases/23-idol-dance-refactor/23-DESIGN.md

# Source files to modify
@classes/druid/Druid.lua
@classes/druid/selftest.lua

# Caller context (read for understanding, do NOT modify)
@classes/druid/combo.lua

# Test framework reference
@core/selftest.lua
</context>

<tasks>

<task type="tracer">
  <name>End-to-end idol dance fix — rewrite computeNormalRelic + distance bypass + 2 tracer tests</name>
  <reversibility rating="reversible">Both changes are local to one function each; reverting to the old logic is a single-edit rollback.</reversibility>
  <files>classes/druid/Druid.lua, classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/Druid.lua lines 362-439: current computeNormalRelic + recoverNormalRelic functions
    - classes/druid/Druid.lua lines 395-422: selectFerocityOrEmeraldRot (unchanged, for context)
    - classes/druid/Druid.lua lines 735-738: isTrivialBattleOrPvp definition
    - classes/druid/combo.lua lines 95-113: caller context (Module 0 of catAtk)
    - classes/druid/selftest.lua lines 1-20: existing test registry patterns and guard structure
  </read_first>
  <action>
**Part A — Rewrite computeNormalRelic (D-01):**

Replace the entire function body of macroTorch.computeNormalRelic (current lines 362-388 in Druid.lua) with the new flat if-else chain per CONTEXT.md D-01:

1. Keep the function signature: `function macroTorch.computeNormalRelic(clickContext)`
2. Replace the body with:
   - First guard: `if not macroTorch.player.isInCombat` — non-combat path. If `clickContext.isImmuneRip` → return `macroTorch.selectFerocityOrEmeraldRot()`, else return `'Idol of Savagery'`. (This preserves the non-combat pre-switch behavior per D-02.)
   - Second guard: `if macroTorch.isTrivialBattleOrPvp(clickContext)` → return `macroTorch.selectFerocityOrEmeraldRot()`. (Gap 1 fix: fast combat never wastes GCD on Savagery.)
   - Third guard: `if clickContext.isImmuneRip` → return `macroTorch.selectFerocityOrEmeraldRot()`. (Gap 2 fix: Savagery provides zero benefit on immune targets.)
   - Fourth guard: `if macroTorch.isRipPresent(clickContext)` → return `macroTorch.selectFerocityOrEmeraldRot()`. (Rip exists, use Builder idol.)
   - Final fallback: `return 'Idol of Savagery'`. (No Rip, not immune, normal combat — prepare Savagery snapshot.)
3. There must be exactly ONE `end` statement closing the function (not the old nested if-else structure).
4. Use English comments matching the existing codebase style. Include a comment referencing D-01 for each new branch (Gap 1 fix, Gap 2 fix).

**Part B — Insert distance bypass in recoverNormalRelic (D-03, D-04):**

In macroTorch.recoverNormalRelic (Druid.lua), insert the distance bypass immediately before the `if not macroTorch.isFightStarted(clickContext) or ...` line (currently line 435). The new code block:

```
    -- Distance break: running time covers relic GCD, skip energy check per D-03/D-04
    if macroTorch.target.distance >= 20 then
        macroTorch.player.ensureRelicEquipped(relicName)
        return
    end
```

This follows D-03 (distance bypass before energy check) and D-04 (hardcoded 20yd threshold). The existing isFightStarted energy check below remains unchanged.

**Part C — Add 2 tracer verification tests to selftest.lua:**

Append to classes/druid/selftest.lua, inside the existing `if UnitClass('player') == 'Druid' then ... end` block, before the closing `end`. Add a `-- Category O: Idol Dance (Phase 23)` section header comment followed by these 2 tests:

Test O-01: "Cat O-01: fast combat returns Fero/Rot (Gap 1 fix) — per D-01". Construct clickContext with `isTrivialBattle = true` and `isImmuneRip = false`. Assert `macroTorch.computeNormalRelic(ctx) ~= 'Idol of Savagery'`. (Cannot assert exact name because player may not own either idol; asserting it is NOT Savagery proves Gap 1 is fixed.)

Test O-05: "Cat O-05: Rip absent in normal combat returns Savagery — per D-01". Construct clickContext with `isTrivialBattle = false`, `isImmuneRip = false`, and set up `macroTorch.isRipPresent` to return false (or use isRipPresent=false in ctx). Assert `macroTorch.computeNormalRelic(ctx) == 'Idol of Savagery'`.

Both tests: `isOptional = true`, placed after the Category O section header. Use the existing test pattern from Category R2-R8: construct a clickContext table with the relevant boolean fields and call the function under test.
  </action>
  <verify>
    <automated>grep -c "function macroTorch.computeNormalRelic" classes/druid/Druid.lua reports exactly 1, and the function body contains the flat chain: isInCombat guard, isTrivialBattleOrPvp, isImmuneRip, isRipPresent, fallback Savagery in that exact order, with exactly one `end` closing the function.</automated>
    <automated>grep "macroTorch.target.distance >= 20" classes/druid/Druid.lua reports 1 match at the new position inside recoverNormalRelic</automated>
    <automated>grep -c "Cat O-0[15]:" classes/druid/selftest.lua returns 2</automated>
  </verify>
  <acceptance_criteria>
    - computeNormalRelic function signature unchanged: `function macroTorch.computeNormalRelic(clickContext)`
    - Function has exactly one `end` statement (flat chain, not nested if-else)
    - isInCombat guard is the outermost check; non-combat path preserves D-02 equivalence (isImmuneRip→Fero/Rot, else→Savagery)
    - isTrivialBattleOrPvp check returns selectFerocityOrEmeraldRot() unconditionally (no Savagery sub-branch for non-immune)
    - isImmuneRip check returns selectFerocityOrEmeraldRot() unconditionally
    - isRipPresent check returns selectFerocityOrEmeraldRot()
    - Fallback returns 'Idol of Savagery'
    - recoverNormalRelic has distance >= 20 bypass inserted before the isFightStarted condition
    - Existing energy-check logic below the distance bypass is unchanged
    - selectFerocityOrEmeraldRot() function body is NOT modified
    - selftest.lua has 2 new Category O registrations with isOptional=true and UnitClass guard
    - O-01 test verifies non-Savagery return for trivial battle (Gap 1)
    - O-05 test verifies Savagery return when Rip absent (baseline behavior preserved)
  </acceptance_criteria>
  <done>
computeNormalRelic uses the flat decision structure from D-01. recoverNormalRelic has distance bypass inserted per D-03/D-04. Two tracer tests (O-01, O-05) exist in selftest.lua. The end-to-end slice is committed and verifiable.
  </done>
</task>

<task type="auto">
  <name>Complete Category O SelfTest coverage — add remaining 5 tests</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua: read the full file to see existing patterns (Category R2-R8 test style) and the new Category O section header + O-01/O-05 from Task 1
    - core/selftest.lua lines 34-40: SelfTest:register API signature confirmation
    - classes/druid/Druid.lua: re-read computeNormalRelic (lines ~362-387) and recoverNormalRelic (lines ~424-442) to see the post-Task-1 state
    - classes/druid/combo.lua lines 95-113: confirm caller context for clickContext shape
  </read_first>
  <action>
Append 5 additional Category O tests to classes/druid/selftest.lua, placed after the O-01/O-05 tests from Task 1 and before the closing `end` of the `if UnitClass('player') == 'Druid'` block. All tests use `isOptional = true`.

**O-02: PvP target returns Fero/Rot (Gap 1 fix) — per D-01.** Construct clickContext with `isTrivialBattle = false`, `isImmuneRip = false`, and mock `macroTorch.target.isPlayerControlled` need NOT be set directly — instead, verify that when `isTrivialBattleOrPvp` returns true (via isPlayerControlled path), computeNormalRelic returns non-Savagery. Use clickContext fields to force isTrivialBattleOrPvp=true: set `isTrivialBattle = true` is the simplest proxy since isTrivialBattleOrPvp returns target.isPlayerControlled OR isTrivialBattle. Assert return is NOT 'Idol of Savagery'.

**O-03: Immune Rip in combat returns Fero/Rot (Gap 2 fix) — per D-01.** Construct clickContext with `isImmuneRip = true`, `isTrivialBattle = false`. Assert return is NOT 'Idol of Savagery'.

**O-04: Rip present returns Fero/Rot — per D-01.** Construct clickContext with `isImmuneRip = false`, `isTrivialBattle = false`, and set up conditions so `macroTorch.isRipPresent(ctx)` returns true. The isRipPresent function checks `macroTorch.pendingCasts ~= nil and macroTorch.pendingCasts['Rip'] ~= nil`. To force isRipPresent=true in a self-test context, set `macroTorch.pendingCasts = { Rip = true }` before calling computeNormalRelic, then restore it after. Assert return is NOT 'Idol of Savagery'.

**O-06: Non-combat non-immune returns Savagery (pre-switch preserved) — per D-01/D-02.** Construct clickContext with `isImmuneRip = false`. Mock `macroTorch.player.isInCombat` cannot be overridden in SelfTest, so instead verify that when isInCombat is false AND isImmuneRip is false, computeNormalRelic returns Savagery. Since player.isInCombat is a live WoW API, use a guard: `if macroTorch.player.isInCombat then return end` to skip if in-combat. Then construct clickContext and assert return is 'Idol of Savagery'.

**O-07: Distance >= 20yd bypasses energy check (Gap 4 fix) — per D-03.** This tests recoverNormalRelic behavior indirectly by verifying the function has the distance guard. Since we cannot mock macroTorch.target.distance or ensureRelicEquipped in Selftest, verify the code structure: use `assert(type(macroTorch.recoverNormalRelic) == 'function')` as a basic existence check, and verify distance is available: `assert(macroTorch.target.distance ~= nil, 'distance API not available')`. This is a smoke test; the actual distance logic is verified by the grep check in Task 1 verify.

NOTE on O-04: isRipPresent checks `macroTorch.pendingCasts['Rip']`. In SelfTest, you can temporarily set `macroTorch.pendingCasts = { Rip = true }`, call computeNormalRelic, then restore `macroTorch.pendingCasts = nil`. Wrap in pcall to ensure cleanup even on failure.
  </action>
  <verify>
    <automated>grep -c "Cat O-0[1-7]:" classes/druid/selftest.lua returns 7</automated>
    <automated>grep -c "SelfTest:register.*Cat O-" classes/druid/selftest.lua returns 7</automated>
    <automated>grep -c "isOptional = true" classes/druid/selftest.lua in the Category O region: the 7 new registrations all use isOptional=true (verify by counting SelfTest:register calls with 'true' as third arg in the appended region)</automated>
  </verify>
  <acceptance_criteria>
    - selftest.lua contains exactly 7 Category O SelfTest registrations: O-01 through O-07
    - All 7 tests use isOptional = true (third argument to SelfTest:register)
    - All 7 tests are inside the `if UnitClass('player') == 'Druid' then ... end` guard block
    - O-02 asserts non-Savagery for trivialBattle=true (proxy for isTrivialBattleOrPvp)
    - O-03 asserts non-Savagery for isImmuneRip=true in combat
    - O-04 sets pendingCasts.Rip temporarily, asserts non-Savagery, cleans up pendingCasts after
    - O-06 skips if inCombat, asserts Savagery for non-combat non-immune
    - O-07 asserts recoverNormalRelic is a function and target.distance is not nil
    - Each test's registration name matches the pattern "Cat O-NN: description"
    - No other files modified
  </acceptance_criteria>
  <done>
All 7 Category O SelfTest registrations exist in selftest.lua. The complete idol dance test suite covers Gaps 1, 2, and 4 plus baseline behavior preservation.
  </done>
</task>

</tasks>

<verification>
**Overall phase verification:**

1. Build check: `./build.sh && echo "Build OK"` succeeds (classes/druid/Druid.lua and classes/druid/selftest.lua are already in build_order.txt per D-07)

2. Function structure check:
   - `grep -A 30 "function macroTorch.computeNormalRelic" classes/druid/Druid.lua` shows the flat chain: isInCombat guard → isTrivialBattleOrPvp → isImmuneRip → isRipPresent → fallback Savagery
   - `grep -c "function macroTorch.computeNormalRelic" classes/druid/Druid.lua` returns exactly 1

3. Distance bypass check: `grep "macroTorch.target.distance >= 20" classes/druid/Druid.lua` returns exactly 1 match inside recoverNormalRelic

4. Unchanged functions check:
   - `grep "function macroTorch.selectFerocityOrEmeraldRot" classes/druid/Druid.lua` returns 1 (function NOT modified)
   - `grep "function macroTorch.dischargeEnergyChangeRelicAndRip" classes/druid/cat.lua` returns 1 (NOT modified)
   - `grep "function macroTorch.onCombatExit\|function macroTorch.onCombatEnter" core/combat_context.lua` returns 2 (NOT modified)

5. Test count check: `grep -c "Cat O-0[1-7]:" classes/druid/selftest.lua` returns 7

6. No unintended modifications:
   - `git diff --stat` shows only classes/druid/Druid.lua and classes/druid/selftest.lua changed
</verification>

<success_criteria>
- computeNormalRelic flat chain matches CONTEXT.md D-01 structure exactly (5 paths: isInCombat guard, isTrivialBattleOrPvp, isImmuneRip, isRipPresent, fallback)
- recoverNormalRelic has 20yd distance bypass before energy check
- 7 Category O SelfTest registrations exist and follow D-05/D-06 naming conventions
- Build succeeds (build.sh produces valid SM_Extend.lua)
- Only 2 files modified (Druid.lua, selftest.lua)
- ~25 lines of net change
</success_criteria>

<output>
Create .planning/phases/23-idol-dance-refactor/23-01-SUMMARY.md when done per D-08 (single commit: `fix(druid): fix idol dance gaps + distance optimization`)
</output>

