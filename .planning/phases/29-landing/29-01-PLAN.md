---
phase: 29-landing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - core/spell_trace_core.lua
  - classes/druid/Druid.lua
  - classes/hunter/Hunter.lua
  - classes/druid/selftest.lua
autonomous: false
requirements: [D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-15]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3                     # 计数口径：3 个代码任务；结构验证 task_count 4 含 1 个 checkpoint:decision 决策检查点（非代码任务，不计入 estimate.tasks）
  confidence: low

must_haves:
  # spec-less probe skipped: phase has no requirement IDs — no probe-derived predicates generated (visible skip);
  # truths/prohibitions hand-derived from DESIGN-CONTEXT 8 locked decisions + CONTEXT D-01..D-18
  truths:
    - "任何 land=true 技能不再按 per-spell 来源分派：Rake 的 self-hit 行与 Rip/Pounce 的 apply 行都进同一 landTable，FB 续期推新锚不被 cast 谓词去重拦截（D-01/D-02/D-08/D-11）"
    - "register 接受 intentTtl 可选项并写入 per-spell ttl 注册表；intent 播种携带 ttl；配对窗与 fail 否决窗改读 intent 自身 ttl，nil 时回落 LAND_INTENT_TTL（D-05/D-07/D-09）"
    - "宏 LAND_INTENT_TTL 值 0.9；猫德四个 skill 吃默认，猎人双钉刺显式 2（D-10/D-12）"
    - "同 cast 单条 land、同质量保留最早；fail 窗口 [cast, cast+ttl] 内仍毁已推 land（D-02/D-04/D-06）"
    - "Q-01 断言统一 OR 注册形态（不再断言 per-spell 来源注册表）；Q-02 在 0.9 窗下仍通过（D-15）"
    - "SM_Extend.lua 全程字节等于 HEAD（本阶段不跑 build）"
  artifacts:
    - core/spell_trace_core.lua
    - classes/druid/Druid.lua
    - classes/hunter/Hunter.lua
    - classes/druid/selftest.lua
  key_links:
    - "register(land=true) → tracingSpells 写入 + landIntentTtls 写入 + 自然属性驱动 auraApplySpellPatterns 写入（events.lua tier-2 仍消费该表，本阶段 events.lua 零改动）"
    - "recordCastTable 播种 intent.ttl → pairLandIntent（purge/pair 双趟读 intent.ttl）与 finalizeFail（否决窗读 intent.ttl）"
    - "recordLandEvent（带去重谓词）与 recordLandEventRenewal（豁免入口）共享同一守卫/push/dispatch 骨架；FB 监听器走豁免入口"
  prohibitions:
    - "不修改 core/events.lua 与 core/spell_trace_immune.lua（VERIFICATION-ONLY）— flagged-unverified"
    - "不修改 cpDamage 五函数结构；pairCpDamageIntent 继续引用 LAND_INTENT_TTL 全局常量（仅数值随 0.9 自动生效）— flagged-unverified"
    - "不跑 build.sh、不触碰 SM_Extend.lua（build 产物，用户 Windows+Cygwin 侧）— flagged-unverified"
    - "不引入任何新 spell、不改 catAtk/hunterAtk 决策逻辑 — flagged-unverified"
---

<objective>
本计划为 Phase 29 的 tracer 切片：把「register(land=true) 统一三证据通道 OR 语义 → cast intent 携带 per-spell ttl 播种 → self-hit / 配对 apply 正推落地（cast 维去重谓词）→ FB 续期经豁免入口直推新锚 → fail 窗口化否决仍为末路」这条竖切路径在 spell_trace_core.lua + 两个职业注册点 + selftest 基线三处打通并静态验证。Wave 2（29-02）在此已证明的 ttl/去重骨架上外扩反推兜底层（maintainLandTables/computeLandTable）；Wave 3（29-03）补齐六组边界用例与实机 UAT 协议。

Purpose: 用本 agent 早期高质量上下文证明统一 OR 证据链的架构成立（register 形态 → intent 播种 → 双正推通道 → 去重 → 续期豁免 → fail-wins），避免在已提交多层后才撞上架构死巷；本切片全部部件均为最终产物（非原型）。
Output: 4 个文件的改动 + 统一 OR 证据链端到端成立的静态证据（bbcheck 全平衡 + 源码断言 grep + selftest 基线夹具重新对齐 0.9 窗）。本机无 lua 解释器：运行级验证落在用户机 /mt 自检（Q-01..Q-10 全绿）与 29-03 的 HUMAN-UAT 协议，本计划以 bbcheck + 源码断言为机器侧门。

实现事实以 DESIGN-CONTEXT.md（8 条锁定决策）与 29-CONTEXT.md（D-01..D-18）为权威，行级锚点以 29-PATTERNS.md §1-§4 为准：
- D-01 去掉 register 的逐技能来源字段与其模块级注册表；证据通道按技能自然属性自动在场（有直伤则 self-hit 行存在，有 debuff 纹理则 apply 行存在）。
- D-02 去重 = cast 维谓词（lastLand >= lastCast 则后到正推证据丢弃）；同 cast 单条 land；同质量层内保留最早到达。谓词仅在 lastCast 存在时生效（无 cast 记录的自 hit 落点与 Q-07/Q-10 夹具不受损）。
- D-04/D-07 fail 否决窗口化：`0 <= failTime - castAt <= intent.ttl`（替换负差容差注释块）；revoke 机器（removeMatch + 红字取消行）逐字保留（D-06 fail-wins）。
- D-09/D-10 register 新参数 intentTtl（与 intent.ttl 播种字段同名）；LAND_INTENT_TTL = 0.9 保留为默认值来源，注释改为"默认证据接收窗，register 参数可逐技能覆盖"。
- D-11 续期豁免走独立函数（内部绕过去重直推新锚），普通三通道只走带去重的记录入口。
- D-12 猎人双钉刺显式 intentTtl = 2，猫德吃默认 0.9（Druid.lua 不写该字段）。
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-landing/29-CONTEXT.md
@.planning/phases/29-landing/DESIGN-CONTEXT.md
@.planning/phases/29-landing/29-PATTERNS.md

# 关键锚点（已核实到 file:line，执行时逐字对照；行号以 HEAD 为准，漂移时用引用的模式文本定位）
@core/spell_trace_core.lua        # LAND_INTENT_TTL=2 @15；per-spell 来源注册表 init @20-24；register @60-84；recordCastTable 播种 @114-125；pairLandIntent purge/pair @176-193；finalizeFail 窗 @378-405；recordLandEvent @291-314；onLandEvent @316-324；onSelfDamageLine 早退门 @425-427；peek 三件套 @462-491；pairCpDamageIntent 勿动 @202-229
@classes/druid/Druid.lua          # register 块 @802-828（Pounce 806 / Rip 817 两行待删字段）；FB 续期监听器 @840-856（847/854 两处续期调用）
@classes/hunter/Hunter.lua        # register 块 @151-165（Serpent 155-159 / Scorpid 161-165）
@classes/druid/selftest.lua       # Category Q 头注释+Q-01 @763-784；Q-02 @786-825；Q-03..Q-08 @827-962；Q-10 @980-1011
@core/periodic.lua                # LRUStack:new/push/removeMatch @21-75（.top 经 ES_FIELD_FUNC_MAP 访问）
@core/events.lua                  # tier-2 auraApplySpellPatterns 消费 @156-164（只读确认，零改动）
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>确认 D-01 执行幅度：移除 register 的逐技能来源字段、其模块级注册表与两处 dispatch 门（评价为 costly）</decision>
  <context>
    本任务随后将从 core/spell_trace_core.lua 中删除 Phase 27 引入的 per-spell 来源注册机制（register 的 landSource 字段写入、宏 landSources 表的模块初始化、onSelfDamageLine 中的来源早退门三行），并同步删除 Druid/Hunter 注册点上的字段。撤销代价（CONTEXT.md costly 评价）：需恢复注册字段声明、恢复模块初始化块、恢复早退门与 register 写入两处 dispatch 门。方向本身已被 DESIGN-CONTEXT 决定 1 锁定（统一三通道 OR 语义），本检查点仅确认"现在执行这次不可廉价撤销的删除"，不做方向重开。
  </context>
  <options>
    <option id="option-a">
      <name>按锁定范围删除（推荐）</name>
      <pros>与 DESIGN-CONTEXT 决定 1 一致；统一 OR 通道 + 反推兜底依赖该删除完成；deferred 项（FF 开 land、anchorBias）不受影响</pros>
      <cons>撤销需恢复注册字段与两处 dispatch 门（约 6 个代码点）；Q-01 断言随之重写</cons>
    </option>
    <option id="option-b">
      <name>暂缓并改用兼容垫片</name>
      <pros>保留旧字段存活，撤销更便宜</pros>
      <cons>违背锁定设计；统一通道语义无法落地；与 D-01 直接冲突，后续 phase 仍需重做</cons>
    </option>
  </options>
  <resume-signal>选 a（按锁定范围执行）或 b（说明暂缓理由）</resume-signal>
</task>

<task type="tracer">
  <name>Task 1: 统一 OR 证据链核心 — register 改签名/去注册表/intent.ttl 贯通/去重谓词/续期豁免/self-hit 门拆除（D-01/D-02/D-04/D-05/D-06/D-07/D-09/D-10/D-11）</name>
  <reversibility rating="costly">D-01 撤销需恢复 register 字段声明 + 模块初始化块 + 早退门/写入两处 dispatch 门（约 6 个代码点，跨 4 文件）</reversibility>
  <files>core/spell_trace_core.lua</files>
  <read_first>
    - core/spell_trace_core.lua 全文件（85-553 行逐段对照下述改动锚点）
    - DESIGN-CONTEXT.md 全文（8 条锁定决策 + 落地范围）
    - 29-PATTERNS.md §1（1a-1d 四段：常量+register、intent 播种、窗口读 ttl、去重+豁免）与 Shared Patterns（guard shape / LRUStack / announcement 纪律）
    - core/periodic.lua 21-75（LRUStack push/removeMatch/top 访问器——本任务不调用但理解 .top 语义）
    - core/events.lua 156-164（tier-2 仍消费 auraApplySpellPatterns 表——表本体保留，仅填充触发条件变）
  </read_first>
  <action>
    <!-- planner-discipline-allow: landSource -->

    <!-- planner-discipline-allow: landSources -->

    本任务是 tracer 竖切的第一段（单文件全链）：正推证据从"按来源分派"切换到"统一 OR + cast 维去重"。所有新/改注释英文；本文件不改 cpDamage 五函数（pairCpDamageIntent/onCpDamageLine/cpDamageEvent @202-286 逐字保留——D-17 依赖其继续引用全局常量）。

    1. 常量与模块级表（行 13-28 块）：
       a. 宏 LAND_INTENT_TTL 值 2 → 0.9；其上注释改为英文"default evidence window (s) before a silent cast is inferred landed; the intentTtl register parameter overrides it per-spell；cpDamage pairing keeps referencing this constant (D-17)"（D-10）。
       b. 删除 per-spell 来源注册表的模块初始化块（现 20-24 行整体，含其上方两行注释）——D-01。
       c. 在其原位置新增模块级 per-spell ttl 注册表：nil-guard 形状照抄相邻 tracingSpells 的 init 块（`if not ... then ... = {} end`），键名 landIntentTtls；带英文注释"per-spell evidence/ttl registry written by register, read by recordCastTable seeding and the inference layer"。
       d. auraApplySpellPatterns init（现 25-28）保留，注释更新为"precompiled find patterns for aura-apply evidence lines of land-tracing spells that carry a tracked debuff texture (natural-attribute driver, no per-spell source field)"。
    2. register 改签名（现 60-84；D-01/D-09）：
       a. config 字段注释（55-59）追加一行"intentTtl (number, optional): per-spell evidence window override; defaults to LAND_INTENT_TTL"。
       b. land 分支内删去来源表写入行（现 64）；新增 `macroTorch.landIntentTtls[name] = config.intentTtl or macroTorch.LAND_INTENT_TTL`（D-07/D-09）。
       c. pattern 填充驱动改为自然属性：`if config.immune and config.debuffTexture then macroTorch.auraApplySpellPatterns[name] = ' is afflicted by ' .. name .. '%.' end`。落点集= Pounce/Rip/Rake（Druid）+ Serpent/Scorpid（Hunter），FF（land=false）不入——与现 4 个落点比多出 Rake（统一 OR 下 Rake 的 apply 行同样是合法证据通道）。
       d. pattern-safe 断言（1a 守卫）：在拼 pattern 前对 name 做 Lua find-pattern 元字符断言——用 string.find(name, '[...]') 的括号字符类一次性检测全部魔法字符 `( ) . % + - * ? [ ] ^ $`（% 在类内写作 %%，^ 不得置于类首）；命中即 macroTorch.show("[macro-torch] SpellTrace:register(" .. name .. "): name carries Lua find-pattern metacharacters, aura-apply channel skipped", 'red') 并跳过本名的 pattern 填充（tracing/ttl 注册照常——五个现役名字全部无元字符，守卫纯防御）。
       e. spellName guard invariant 与 immune 分支（72-83）逐字保留。
    3. intent 播种携带 ttl（recordCastTable 现 125；D-07）：push 表带新增 `ttl = macroTorch.landIntentTtls[spell] or macroTorch.LAND_INTENT_TTL`；其上注释（114-115）更新为"expires after the intent's own ttl if no land/fail event arrives"。
    4. pairLandIntent 两趟读 intent.ttl（现 176-193；D-05/D-07）：
       a. purge 趟条件改为 `(landTime - intent.castAt) > (intent.ttl or macroTorch.LAND_INTENT_TTL)`（per-intent 兜底：钉刺 2s intent 不在 0.9 处被清）。
       b. pair 趟同类替换：`(landTime - intent.castAt) <= (intent.ttl or macroTorch.LAND_INTENT_TTL)`。
       c. 函数头注释两处 2s/TTL 措辞改为 per-intent ttl 措辞。
    5. finalizeFail 窗口化（现 378-385；D-04/D-06/D-07）：消费条件改为 `(intent.state == 'pending' or intent.state == 'landed') and (failTime - intent.castAt) >= 0 and (failTime - intent.castAt) <= (intent.ttl or macroTorch.LAND_INTENT_TTL)`；现 380-383 的负差容差注释块整段替换为英文窗口化注释（cast <= failTime <= cast + intent.ttl，D-04 公式；同帧到达差 0 仍满足下界）。386-405 的 revoke 机器（removeMatch 按 landTime == intent.landAt、红字取消行）逐字保留——fail-wins 不变（D-06）。
    6. recordLandEvent 加 cast 维去重层（现 291-314；D-02）：
       a. 在 per-spell/mob landTable 栈 lazy-init 之后、push 之前插入谓词：取 `local lastCast = macroTorch.peekCastEvent(spell)`；`local lastLand = macroTorch.peekLandEvent(spell) or 0`；`if lastCast and lastLand and lastLand >= lastCast then return end`。谓词仅在 lastCast 存在时生效（无 cast 记录的自 hit 落点照常入表——Q-07/Q-10 夹具依赖此形态）。
       b. 紧邻加英文注释：cast-dimension dedup keeps one land per cast, earliest arrival wins；renewal bypasses via the dedicated exempt entry。
    7. 新增续期豁免入口（D-11）：定义名 recordLandEventRenewal、签名 `(spell, landTime)` —— 守卫形状、lazy-init、push、landListeners 派发与现 recordLandEvent 292-314 逐字同构，唯不含第 6 步去重谓词；英文注释注明"renewal deliberately writes a new anchor past the cast-dimension dedup (FB listener call sites only)"。落位于 recordLandEvent 之后、onLandEvent 之前。
    8. onSelfDamageLine 拆早退门（现 425-427；D-01/D-02）：删除三行来源早退判断（含其上无注释、其下空行）；tracingSpells 门（422-424）保留；其上函数头注释（408-411）改为统一通道措辞（every traced spell's self-hit line is land evidence, pairing attempted best-effort）；429-440 的 announce-before-record 绿字块与其因果序注释逐字保留——recordLandEvent 内部去重负责同 cast 单条（双证据技能如 Pounce 可能双绿通告、表只落一条，属可接受观感，勿在此层去抑制）。
    9. 全文检查：现 288-291 recordLandEvent 函数头注释更新为"unified evidence entry; cast-dimension dedup inside; renewal goes through the exempt entry"语义；processRawAuraApply（330-360）函数体零改动（其内部 pair→announce→record 顺序已满足去重层），仅其头注释中来源措辞按统一通道语义微调。

    约束：本文件不新增对 GetTime() 之外时钟的依赖；所有新增逻辑沿用 guard shape（spell/target.isCanAttack/loginContext 三守卫）；注释与 commit 英文；LF 行尾；不触碰 SM_Extend.lua 与 build 脚本。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/core/spell_trace_core.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH（node 非零退出），或 git diff --check 打印任何 whitespace-error 行</fails_when>
    <automated>grep -c 'function macroTorch.recordLandEventRenewal' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c 'macroTorch.LAND_INTENT_TTL = 0.9' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c 'if not macroTorch.landIntentTtls' /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>三个输出任一不是 1（依次：豁免入口恰 1 个定义、常量 0.9 恰 1 处、注册表 init 恰 1 处；0 或 >=2 均为失败信号）</fails_when>
    <automated>grep -c 'landIntentTtls\[spell\] or macroTorch.LAND_INTENT_TTL' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c '(intent.ttl or macroTorch.LAND_INTENT_TTL)' /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>第一个不是 1（recordCastTable 播种恰 1 处读注册表）；第二个不是 3（pairLandIntent purge/pair 两趟 + finalizeFail 窗口恰三处 intent.ttl 兜底引用；< 3 才是失败信号）</fails_when>
    <automated>grep -c 'lastLand >= lastCast' /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>输出不是 1（cast 维去重谓词在 recordLandEvent 恰 1 处；0=缺失，>=2=谓词被复制到续期入口）</fails_when>
    <automated>grep -c 'failTime - intent.castAt) >= 0' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c "auraApplySpellPatterns\[name\] = ' is afflicted by '" /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>任一不是 1（窗口下界恰 1 处；pattern 填充恰 1 处——0=改签名丢失填充，>=2=残留旧分支）</fails_when>
    <automated>grep -c "macroTorch.landSources" /home/admin/workspace/macro-torch/core/spell_trace_core.lua; grep -c "macroTorch.landSource\b" /home/admin/workspace/macro-torch/core/spell_trace_core.lua; grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>任一行输出 > 0（注册表名、来源字段名、Lua 5.0 违禁令牌三者必须为 0——grep -c 无匹配输出 0，注意命令自身返回码 1 属预期）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>任何输出行（build 产物必须与 HEAD 字节一致；空输出=通过）</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'if not macroTorch.landIntentTtls' core/spell_trace_core.lua` = 1；`grep -c 'macroTorch.LAND_INTENT_TTL = 0.9'` = 1；`grep -c 'function macroTorch.recordLandEventRenewal'` = 1
    - 提起的模块级表名与 per-spell 字段名全文件为 0（含注释行）；pairCpDamageIntent 仍引用宏 LAND_INTENT_TTL（cpDamage 段落 zero-diff，D-17）
    - pairLandIntent 两趟与 finalizeFail 消费条件皆读 intent.ttl（带 LAND_INTENT_TTL 兜底），finalizeFail 消费条件含下界 >= 0
    - recordLandEvent 去重谓词 `lastCast and lastLand and lastLand >= lastCast then return` 在 lazy-init 之后 push 之前恰 1 处；recordLandEventRenewal 无该谓词
    - onSelfDamageLine 无三行早退门、tracingSpells 门与绿字 announce-before-record 块仍在；register 的 config 字段注释含 intentTtl 条目
    - bbcheck 全 BALANCED；git diff --check 干净；令牌门 0；SM_Extend.lua 无 diff
  </acceptance_criteria>
  <done>spell_trace_core.lua 的统一 OR 证据链落地：register 新形态（ttl 注册表 + 自然属性 pattern 驱动 + pattern-safe 断言）、intent 携带 ttl 贯通配对/否决双窗、cast 维去重谓词、续期豁免入口、self-hit 门拆除；cpDamage 五函数零改动；SM_Extend.lua 字节一致。</done>
</task>

<task type="auto">
  <name>Task 2: 职业注册点迁移 — Druid register/FB 续期监听器 + Hunter 双钉刺 intentTtl=2（D-08/D-12）</name>
  <files>classes/druid/Druid.lua, classes/hunter/Hunter.lua</files>
  <read_first>
    - classes/druid/Druid.lua 802-856（register 块 + FB 续期监听器全文）
    - classes/hunter/Hunter.lua 151-165（双钉刺 register 块全文）
    - 29-PATTERNS.md §2/§3（两文件的逐行改动清单）
    - core/spell_trace_core.lua 60-84（Task 1 落地的 register 新形态——本任务字段必须与之对齐）
  </read_first>
  <action>
    <!-- planner-discipline-allow: landSource -->

    classes/druid/Druid.lua（802-828）：
    1. Pounce register（804-808）：删除来源字段行（现第 3 行位置）；第 2 行注释（803）改为统一 OR 英语语义——Pounce 既有直伤（self-hit 行）又有 debuff（apply 行），两条自然证据通道并列在场，落地取先到者。
    2. Rake register（810-813）：注释（809）改为——Rake 恒有 self-hit 行；因带跟踪 debuff 纹理，其 apply 行同样加入统一证据通道（比旧语义多一条通道，属有意变更）。无字段改动。
    3. Rip register（815-819）：删除来源字段行；注释（814）改为——Rip 无直伤行，apply 行为其自然证据通道；apply 被抑制时由反推兜底（29-02 落地）接管，本文件不需感知。
    4. FB register（821-824）与 FF register（825-828）零改动；第 802 行总注释与块内引用"default self-hit source"措辞同步为统一通道语言。
    5. FB 续期监听器（840-856）：847 与 854 两处 `macroTorch.recordLandEvent(` 调用改为 `macroTorch.recordLandEventRenewal(`（'Rake' 与 'Rip' 各一处）；830-839 的监听器头注释补一句英文"renewals dispatch through the exempt entry that bypasses the cast-dimension dedup；isRakePresent/isRipPresent preconditions unchanged"。CR-01 两参签名（spell, landTime）与 Savagery 快照只读不写的注释原样保留。

    classes/hunter/Hunter.lua（151-165）：
    6. Serpent Sting（155-159）：删除来源字段行，追加 `intentTtl = 2,`（置于 immune 行之前或之后保持一字段一行）；152-154 注释改为英文弹道窗语义——sting carries no self-hit line；apply evidence can trail the cast by a ballistic flight up to ~2s, so the register pins the 2s window explicitly（D-12）。
    7. Scorpid Sting（161-165）：同款删除 + `intentTtl = 2,`；160 行注释同步弹道窗语义。

    约束：两文件仅动 register 块与 FB 监听器行；能量常量/技能方法/decision 逻辑零改动；注释英文、LF 行尾；不触碰 SM_Extend.lua。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/Druid.lua /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 对任一文件输出 MISMATCH，或 git diff --check 打印任何行</fails_when>
    <automated>grep -c "macroTorch.recordLandEventRenewal('" /home/admin/workspace/macro-torch/classes/druid/Druid.lua && grep -c 'intentTtl = 2' /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua</automated>
    <fails_when>第一个不是 2（Rake/Rip 续期调用恰各 1 处——用带括号引号的调用形态计数，注释不入数）；第二个不是 2（Serpent/Scorpid 各恰 1 处；0 或 >=3 均为失败信号）</fails_when>
    <automated>grep -c "macroTorch.landSource\b" /home/admin/workspace/macro-torch/classes/druid/Druid.lua; grep -c "macroTorch.landSource\b" /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua; grep -c "macroTorch.recordLandEvent('" /home/admin/workspace/macro-torch/classes/druid/Druid.lua</automated>
    <fails_when>前两行任一 > 0（来源字段在两职业文件清零）；第三行 > 0（FB 监听器内不得残留普通记录入口调用）；注意 grep -c 无匹配输出 0 且返回码 1 属预期</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/classes/druid/Druid.lua /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>grep 任一行输出 > 0；或 git status 出现任何行（SM_Extend.lua 必须零变动）</fails_when>
  </verify>
  <acceptance_criteria>
    - Druid.lua 中 `grep -c "macroTorch.recordLandEventRenewal('"` = 2（847/854 两处换入豁免入口）；`grep -c "macroTorch.recordLandEvent('"` = 0（监听器内无残留）
    - Hunter.lua 中 `grep -c 'intentTtl = 2'` = 2；两个 register 块不再含来源字段行
    - 两文件零处 per-spell 来源字段引用；FF register 块零改动；isRipPresent/isRakePresent 前置条件原样（D-08）
    - bbcheck 双文件 BALANCED；令牌门 0；git diff --check 干净；SM_Extend.lua 无 diff
  </acceptance_criteria>
  <done>Druid/Hunter 注册点完成迁移：Pounce/Rip/Serpent/Scorpid 齐删来源字段（D-01），Hunter 双钉刺带 intentTtl = 2（D-12），FB 续期走豁免入口（D-08/D-11），猫德四技能无显式 ttl（吃默认 0.9）。</done>
</task>

<task type="auto">
  <name>Task 3: selftest 基线重对齐 — Q-01 重写为统一 OR 断言 + Q-02 夹具重调到 0.9 窗（D-15/D-16 前置）</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua 763-1011（Category Q 头注释 + Q-01..Q-10 全文）
    - 29-PATTERNS.md §4（Q-01 重写目标 + CR-01 夹具纪律）与 Shared Patterns「Selftest registration + isolation」
    - core/spell_trace_core.lua Task 1 落地后的 register/pairLandIntent/finalizeFail 现状（窗口值行为基准）
  </read_first>
  <action>
    <!-- planner-discipline-allow: landSources -->

    classes/druid/selftest.lua（Category Q）：

    1. Q-01 重写（现 771-784；D-15）——测试名改为 `"Cat Q-01: unified three-channel OR registration (no per-spell source registry)"`，断言换为统一 OR 形态（保持非 stub 风格与 isOptional=true）：
       a. `assert(macroTorch.landSources == nil, ...)`（注册表必须不存在——连贯性）
       b. tracingSpells 六项为 true：'Pounce' / 'Rake' / 'Rip' / 'Ferocious Bite' / 'Serpent Sting' / 'Scorpid Sting'（注册名全部供统一通道——跨职业字段在文件加载期即注册，可在 druid 自检中直查）
       c. `assert(macroTorch.LAND_INTENT_TTL == 0.9, ...)`
       d. landIntentTtls 取值：['Serpent Sting'] == 2、['Scorpid Sting'] == 2、['Pounce'] == 0.9、['Rake'] == 0.9、['Rip'] == 0.9、['Ferocious Bite'] == 0.9（显式比对字面，不引宏）
       e. auraApplySpellPatterns 表：['Rake'] / ['Pounce'] / ['Rip'] 三项为字符串且 string.find 它们含 ' is afflicted by '；['Faerie Fire (Feral)'] == nil（land=false 不入）
    2. Category Q 头注释（763-769）更新：不变式列表去掉已删除的注册表名（该全局已不存在，注释提及即虚假信息）；新六条边界用例（Q-11..Q-16）待 29-02/29-03 挂入的措辞替换"N Q-02..Q-07"旧描述为 Q-02..Q-16 全序列。
    3. Q-02 夹具重调（786-825）：第 804 行种植值 `castAt = 999.0` → `castAt = 1000.0`（apply 1000.5 − cast 1000.0 = 0.5 <= 新默认 0.9；旧 1.5 差会因窗口收窄被 purge 标记 expired 而失配）。其上方两行注释同步改为"align the seeded castAt 0.5s before the applied 1000.5 — inside the 0.9 default window"。断言行（1000.5/landed/landAt）原样不动。
    4. Q-03..Q-08 与 Q-10 静默复核（不改动，除非复核反例）：各夹具相对 0.9 窗的差值为 Q-04 4.0>0.9 过期论据成立、Q-05 配对 0.1/否决 0.3 均在窗内、Q-06 否决 0.4 在窗内、Q-07 无 cast 记录时去重谓词不触发、Q-10 经 FB 监听器走豁免入口续期——预期全部保持绿色。若执行时任一断言与预测不符，按 CR-01 纪律就地修夹具（只改种植数值/注释，不改被测函数断言语义），并在 SUMMARY 注明偏离。

    约束：不写 tracingSpells/landListeners/真实 loginContext 子表（CR-01 纪律原样）；全部改动英文注释；保持 isOptional=true；不触碰 SM_Extend.lua。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/selftest.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH，或 git diff --check 打印任何行</fails_when>
    <automated>grep -c "Cat Q-01: unified three-channel OR registration" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "castAt = 1000.0" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "landIntentTtls\['" /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>第一个不是 1（Q-01 新名恰 1 处）；第二个不是 1（Q-02 夹具新种植值恰 1 处）；第三个 < 6（Pounce/Rake/Rip/FB/Serpent/Scorpid 六项 ttl 断言至少 6 次注册表键读取）</fails_when>
    <automated>grep -nc "macroTorch.landSources" /home/admin/workspace/macro-torch/classes/druid/selftest.lua; grep -c "macroTorch.landSources == nil" /home/admin/workspace/macro-torch/classes/druid/selftest.lua; grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/classes/druid/selftest.lua; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>第一条 != 1（旧注册表名全文件恰 1 处——Q-01 阴性断言行是唯一合法保留，头注释与其他位置均不得再提；grep -c 无匹配输出 0、返回码 1 属预期）；第二条 != 1（唯一保留的命中必须恰为阴性断言形态 `macroTorch.landSources == nil`——Q-01 重写 1a 的落地断言；两门联立钉死"有且仅有该一条阴性断言"）；第三条 > 0；第四条输出任何行</fails_when>
  </verify>
  <acceptance_criteria>
    - Q-01 测试名与断言全集换为统一 OR 形态：旧注册表 nil、六 tracingSpells 项 true、LAND_INTENT_TTL == 0.9、六项 per-spell ttl 取值（钉刺 2 / 猫德 0.9）、三 apply pattern 存在且 FF 缺席
    - Q-02 种植 castAt 1000.0（0.5s <= 0.9 窗），断言期望值不变；Q-03..Q-08/Q-10 经窗口复核无需改动
    - Category Q 头注释不再提及已删除的表；bbcheck BALANCED；grep -nc 旧表名 = 1（唯一命中 = Q-01 阴性断言 `macroTorch.landSources == nil` 行，其形态由配套门钉死）；令牌门 0；git diff --check 干净；SM_Extend.lua 无 diff
  </acceptance_criteria>
  <done>selftest 基线对准新架构：Q-01 成为统一 OR 注册断言（D-15），Q-02 在 0.9 窗下重新对齐，Q-03..Q-10 夹具经逐差复核仍绿；Category Q 头注释与注册表语义同步。</done>
</task>

</tasks>

## Source Coverage Audit（本计划集 29-01..29-03 四类来源全覆盖抽查）

| SOURCE | ID | Feature/Requirement | Plan | Status |
|--------|----|---------------------|------|--------|
| GOAL | — | Phase 29 统一 landing 判定重构（ROADMAP 目标五子句） | 29-01/29-02/29-03 | COVERED |
| REQ | — | （phase_req_ids 为空，REQUIREMENTS.md 无本 phase 映射） | — | N/A（无 ID） |
| RESEARCH | — | （本 phase 用户批准跳过 RESEARCH） | — | N/A |
| CONTEXT | D-01..D-18 | 18 条锁定决策逐一落任务 | 见各 plan requirements/truths | COVERED（D-01..D-12, D-15 → 29-01；D-03/D-13/D-14 → 29-02；D-16..D-18 → 29-02/29-03） |
| CONTEXT | Deferred | FF 开 land / anchorBias / 动态 TTL 学习 | — | EXCLUDED（Deferred Ideas，不入计划） |

## Artifacts this phase produces（29-01 段）

- `macroTorch.landIntentTtls` — 新模块级 per-spell ttl 注册表（register 写入默认 0.9，钉刺显式 2；recordCastTable/computeLandTable 读取）
- `macroTorch.recordLandEventRenewal(spell, landTime)` — 新函数：续期豁免入口（绕 cast 维去重直推新锚，D-11）
- `macroTorch.LAND_INTENT_TTL` — 值 2 → 0.9（语义改为"默认证据接收窗，register 可逐技能覆盖"，D-10）
- `SpellTrace:register` 签名变更 — 新增可选 `intentTtl`；移除逐技能来源字段（D-01/D-09）
- 新增 recordLandEvent 内 cast 维去重谓词（lastCast/lastLand 比较，D-02）；finalizeFail 消费条件加下界 >= 0（D-04）
- 移除：`macroTorch.landSources` 表（模块 init + register 写入 + onSelfDamageLine 早退门三处）
- `classes/druid/selftest.lua` — Q-01 重写（统一 OR 断言）、Q-02 夹具种植值 1000.0

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 游戏事件流 → spell_trace_core（self-hit/apply/fail 三类聊天/RAW 行） | 不可信输入经 pattern 匹配进 land 判定；fail 行为终局语义 |
| spell_trace_core → landTable 消费者（Druid ripLeft/keepRip、immune 路径、cpDamage 并行队列） | 本计划写入的 land 时序是下游持续时长的依据 |
| 源码 → SM_Extend.lua（build 产物） | 产物不参与本计划任何写操作（字节一致性门每任务执行） |

## STRIDE Threat Register（ASVS Level 1；block-on-high 阈）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Spoofing（逻辑） | processRawAuraApply / pairLandIntent 统一 OR 后多猫归因 | medium | mitigate | guid 双端 string.lower 归属检查与 fail 终局（D-06）原样保留；猫德窗口 2→0.9 使归因窗收窄一半；cast 维去重限制同 cast 单拓 |
| T-29-02 | Tampering（逻辑） | recordLandEvent cast 维去重谓词 | medium | mitigate | 谓词仅在 lastCast 存在时生效（无 cast 记录不抑制）；同质量保留最早；续期豁免入口（D-11）隔离改写锚；Q-12 夹具（29-03）行为钉死 |
| T-29-03 | Repudiation（逻辑） | finalizeFail 窗口下界 0（负差容差移除） | medium | mitigate | D-04 锁定公式（同帧差 0 仍满足下界）；fail 事件无论如何先落 failTable（证据不丢）；Q-13 窗内/窗外双场景（29-02）钉死 |
| T-29-04 | Information disclosure（构建完整性） | SM_Extend.lua build 产物 | medium | mitigate | 本计划全部 verify 含 `git status --porcelain -- SM_Extend.lua` 字节一致性门；build 在用户侧执行 |

无 high 级威胁——无需阻断任务。
</threat_model>

<verification>
- 本计划三任务 verify 全绿（bbcheck 5 文件、源码断言 grep、令牌门、SM_Extend.lua 字节一致）
- 汇总断言：`grep -c 'function macroTorch.recordLandEventRenewal' core/spell_trace_core.lua` = 1；`grep -c 'intentTtl = 2' classes/hunter/Hunter.lua` = 2；`grep -c 'castAt = 1000.0' classes/druid/selftest.lua` = 1
- 零残留断言（全源码目录不含 build 产物）：旧注册表名/字段名在 core/ 与 classes/ 生产文件中为 0（唯一例外 = classes/druid/selftest.lua 的 Q-01 阴性断言行 `macroTorch.landSources == nil`，由 29-01 Task 3 配套门钉死为恰 1 处、由 29-03 Task 3 电池按豁免过滤）
</verification>

<success_criteria>统一 OR 正推证据链成立：register 新形态 + per-spell ttl 注册表 + intent.ttl 贯通配对/否决双窗 + cast 维去重 + 续期豁免 + self-hit 门拆除，Q-01/Q-02 基线重对齐；catAtk/hunterAtk 决策逻辑与 cpDamage 结构零改动；SM_Extend.lua 字节等于 HEAD。
</success_criteria>

<output>
Create `.planning/phases/29-landing/29-01-SUMMARY.md` when done
</output>