---
phase: 29-landing
plan: "03"
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - classes/druid/selftest.lua
  - classes/druid/HUMAN-UAT.md
autonomous: true
requirements: [D-16, D-17, D-18]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  # truths hand-derived from DESIGN-CONTEXT + CONTEXT D-16/D-17/D-18（spec-less probe skipped：无 requirement ID）
  truths:
    - "D-16 六组边界用例全落地：Q-11/Q-13（29-02）+ Q-12/Q-14/Q-15/Q-16（本计划），Category Q 序列 Q-01..Q-16 共 16 条注册"
    - "去重谓词拒绝后到同 cast 正推证据（Q-12）；0.9/2 窗边界逐差分（Q-14）；续期豁免直推新锚（Q-15）；2s 窗收 1s 迟到 apply（Q-16）"
    - "cpDamage 配对零结构改动、窗随 0.9 自动生效（D-17）；远程反推锚偏早一个飞行时间接受为最终形态、不引入 anchorBias（D-18——记录于 UAT 文档）"
    - "HUMAN-UAT.md 携带 Phase 29 实机验收协议（重建/横幅/单人木桩/多猫抑制/猎人钉刺五节）"
    - "全源码无 per-spell 来源注册残留；events.lua 与 spell_trace_immune.lua 零 diff；SM_Extend.lua 全程字节等于 HEAD"
  artifacts:
    - classes/druid/selftest.lua
    - classes/druid/HUMAN-UAT.md
  key_links:
    - "Q-12/Q-14/Q-15/Q-16 夹具 → recordLandEvent 去重谓词 / pairLandIntent per-intent ttl / recordLandEventRenewal 豁免 / processRawAuraApply 2s 配对（29-01 落地机制的直接行为钉）"
    - "HUMAN-UAT Phase 29 节 → 用户 Windows+Cygwin rebuild 后的 /mt 与实机观察（end-of-phase human-verify 汇聚到 verifier 的 29-UAT.md）"
  prohibitions:
    - "不修改 core/ 任何文件（本计划纯测试+文档+battery）— flagged-unverified"
    - "不跑 build.sh、不触碰 SM_Extend.lua — flagged-unverified"
---

<objective>
本计划为 Phase 29 的收尾切片：把 D-16 六组边界用例的后四条（Q-12 去重拒绝后到证据 / Q-14 ttl 0.9-2 边界 / Q-15 续期豁免 / Q-16 远程迟到 2s 配对）落地到 selftest，写出 Phase 29 实机验收协议到 HUMAN-UAT.md（含 D-18 远程锚偏差接受注记与 D-17 cpDamage 无改动声明），并跑全阶段静态收尾电池（bbcheck 全文件、Lua 5.0 令牌门、来源注册残留清零、VERIFICATION-ONLY 文件零 diff、SM_Extend.lua 字节一致、Q 总数 16）。

Purpose: 让 29-01/29-02 已落地的每条机制都有行为断言钉死，并把只能在用户机验证的世界证据整理成可勾选的 UAT 协议；同时以一次电池确认整个 phase 的源码面干净。
Output: 2 个文件的改动 + 全阶段验证电池绿色证据。本计划不产生新的运行态逻辑（纯测试注册 + 文档 + 断言）。

实现事实以 29-CONTEXT D-16/D-17/D-18 + DESIGN-CONTEXT「用户侧实机验证要点」为权威，夹具纪律以 29-PATTERNS.md §4 与 Q-02/Q-04/Q-05 模板为准。
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-landing/29-CONTEXT.md
@.planning/phases/29-landing/DESIGN-CONTEXT.md
@.planning/phases/29-landing/29-PATTERNS.md

# 关键锚点（HEAD 行号，漂移时用模式文本定位）
@classes/druid/selftest.lua       # Category Q 全套（Q-11 @29-02 落地后）；Q-02 夹具模板 786-825；Q-04/Q-05 种植形式 850-907
@classes/druid/HUMAN-UAT.md       # Phase 28 节的六段结构（追加 Phase 29 节于文件末尾）
@core/spell_trace_core.lua        # 已落地的 recordLandEvent/recordLandEventRenewal/pairLandIntent/processRawAuraApply（battery 锚点）
@core/periodic.lua                # LRUStack push/top/elements @17-92
</context>

<tasks>

<task type="auto">
  <name>Task 1: 边界用例后半程 — Q-12 去重拒后到 / Q-14 ttl 边界 / Q-15 续期豁免 / Q-16 远程迟到 2s 配对（D-16）</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua 763-1011（Category Q 全段 + Q-02 纪律模板）
    - core/spell_trace_core.lua recordLandEvent / recordLandEventRenewal / pairLandIntent / finalizeFail / processRawAuraApply 现状全文（本任务夹具必须对准其谓词）
    - 29-PATTERNS.md §4 六组新用例清单（2/3/4/5/6 条对应本任务四注册）
  </read_first>
  <action>    在 Q-13 之后追加四条注册（isOptional=true、CR-01 纪律、英文；编号 Q-12/Q-14/Q-15/Q-16，Q-12 补插到其编号语义位——注册顺序按数字排列 Q-11 之后）：

    1. Q-12 `"Cat Q-12: late evidence for an already-landed cast is rejected (cast-dimension dedup)"`：
       - fakes 同 Q-11 形态 + show stub；`local seedCast = 5.0`（纯种植值，无真实时钟依赖）。
       - castTable['Rip']['QTestMob'] = LRUStack:new(100) 并 push(seedCast)；intentTable['Rip']['QTestMob'] = LRUStack:new(32) 并 push({ state = 'pending', castAt = seedCast, landAt = nil, ttl = macroTorch.LAND_INTENT_TTL })。
       - pcall 内：`macroTorch.processRawAuraApply('Rip', '0xF1300000000000AB is afflicted by Rip.', '0xf1300000000000ab', 5.1)`（配对成功 path 落第一条 land）→ 再 `macroTorch.recordLandEvent('Rip', 5.4)`（第二通道后到证据，应被谓词丢弃）。
       - 捕获 intentState 与 landTop 与 landSize（`macroTorch.tableLen(fakeLoginContext.landTable['Rip']['QTestMob'].elements)`）。
       - 恢复后断言：pcallRes；intentState == 'landed'；landTop == 5.1（5.4 未覆盖）；landSize == 1（同 cast 单条）。
    2. Q-14 `"Cat Q-14: per-intent ttl boundary — 2s window pairs, 0.9 expires at a 1.5s delta"`：
       - fakes（勿需 castTable/landTable——本测试只驱动 pairLandIntent）；种植两支 intent：['Serpent Sting']['QTestMob'].push({ state = 'pending', castAt = 999.0, landAt = nil, ttl = 2 })；['Rake']['QTestMob'].push({ state = 'pending', castAt = 999.0, landAt = nil, ttl = 0.9 })。
       - pcall：`serpentRes = macroTorch.pairLandIntent('Serpent Sting', 1000.5)`（1.5 <= 2 → 配对）；`rakeRes = macroTorch.pairLandIntent('Rake', 1000.5)`（1.5 > 0.9 → purge 标记 expired、返回 nil）。
       - 恢复后断言：serpentRes ~= nil 且 serpentRes.state == 'landed' 且 landAt == 1000.5；rakeRes == nil；Rake 种植表 state == 'expired'。
    3. Q-15 `"Cat Q-15: renewal entry bypasses the cast-dimension dedup"`：
       - fakes + show stub；seedCast = 5.0；castTable['Rake']['QTestMob'] push(5.0)；landTable['Rake']['QTestMob'] push(5.3)。
       - pcall 内：`macroTorch.recordLandEvent('Rake', 5.6)`（lastLand 5.3 >= lastCast 5.0 → 丢弃）；再 `macroTorch.recordLandEventRenewal('Rake', 5.6)`（豁免入口 → 直推）。
       - 恢复后断言：landTop == 5.6；landSize == 2（5.3 保留 + 5.6 新锚）。
    4. Q-16 `"Cat Q-16: remote late arrival — apply 1s after cast pairs under the 2s sting window"`：
       - fakes + show stub；intentTable['Serpent Sting']['QTestMob'] push({ state = 'pending', castAt = 999.0, landAt = nil, ttl = 2 })（不种 castTable——同时验证无 cast 记录时去重谓词不拦截该 land）。
       - pcall：`result = macroTorch.processRawAuraApply('Serpent Sting', '0xF1300000000000AB is afflicted by Serpent Sting.', '0xf1300000000000ab', 1000.0)`（差 1.0 > 0.9 默认、<= 2 → 钉刺窗收下）。
       - 捕获 intentResult 状态与 landTop（应 1000.0）。
       - 恢复后断言：pcallRes；result ~= nil 且 .state == 'landed' 且 .landAt == 1000.0；landTop == 1000.0。
    5. Category Q 头注释（若有前瞻措辞）更新为新总述：Q-01..Q-16 全序列覆盖【统一注册 / 正推配对 / 拒外来 / 过期 / fail 终局 / pairing-free / 监听器 / 复活断言 / 续期数值时间 / 无证据反推 / 去重拒后到 / 否决窗 / ttl 边界 / 续期豁免 / 远程迟到】。

    约束：全部断言在全局恢复之后（CR-01）；不写 tracingSpells/landListeners/真实 loginContext 子表；英文注释；isOptional=true；不触碰 SM_Extend.lua。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/selftest.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH，或 git diff --check 打印任何行</fails_when>
    <automated>grep -c "Cat Q-12: late evidence" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "Cat Q-14: per-intent ttl boundary" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "Cat Q-15: renewal entry bypasses" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "Cat Q-16: remote late arrival" /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>任一不是 1（四注册各恰 1 处）</fails_when>
    <automated>grep -c 'recordLandEventRenewal(' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "push({ state = 'pending', castAt = 999.0" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c 'SelfTest:register("Cat Q-' /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>第一个不是 1（Q-15 恰 1 处豁免入口调用）；第二个不是 3（Q-14 两支 + Q-16 一支 = 3 处 999.0 种植）；第三个不是 16（Category Q 序列 Q-01..Q-16 恰 16 条——0-15 说明有注册丢失，17+ 说明编号重复）</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/classes/druid/selftest.lua; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>grep 输出 > 0；或 git status 输出任何行</fails_when>
  </verify>
  <acceptance_criteria>
    - Q-12：processRawAuraApply 5.1 落 land 后 recordLandEvent 5.4 被拒，top == 5.1、size == 1
    - Q-14：Serpent（ttl 2）在 +1.5s 配对成功；Rake（ttl 0.9）同差被 purge 为 expired 且返回 nil
    - Q-15：普通入口 5.6 被拒（top 保持 5.3），豁免入口 5.6 直推（top == 5.6、size == 2）
    - Q-16：无 cast 记录下 2s 窗收下 1.0s 迟到 apply，intent landed、landTop == 1000.0（同时验证缺席 cast 时去重不拦截）
    - Category Q 总数 16；bbcheck BALANCED；令牌门 0；git diff --check 干净；SM_Extend.lua 无 diff
  </acceptance_criteria>
  <done>D-16 全部六组边界用例落地（Q-11/Q-13 属 29-02，Q-12/Q-14/Q-15/Q-16 本任务）：去重拒后到、ttl 双边界、续期豁免、远程迟到四行为全部被夹具钉死。</done>
</task>

<task type="auto">
  <name>Task 2: HUMAN-UAT.md Phase 29 实机验收协议 + 关闭项清点 — 取证 todo no-op 确认 + 陈旧插桩注释扫描</name>
  <files>classes/druid/HUMAN-UAT.md</files>
  <read_first>
    - classes/druid/HUMAN-UAT.md 全文（Phase 28 节六段结构作本阶段模板）
    - DESIGN-CONTEXT.md「用户侧实机验证要点」三节
    - 29-CONTEXT.md Folded Todos 段与 Deferred Ideas 段（协议不得要求 defer 项）
  </read_first>
  <action>    1. 追加 UAT 协议（HUMAN-UAT.md 文件末尾，结构与 Phase 28 节同构）：

    `## Phase 29: 统一 landing 判定重构 -- 实机 UAT 闭环`，含六节（**英文小节标题可、正文中文列表**，沿用 Phase 28 节语言风格；全部为可勾选 `- [ ]` 项）：
    - **1. Prerequisites**：WoW 1.12.1（Turtle WoW）+ SuperWoW；Windows+Cygwin 下 `./build.sh` 成功重建 SM_EXTEND.lua（产物落盘后版控外生效）；无需新增 SavedVariables 声明（沿用现有 SM 变体 .toc），`macroTorch.LOG_MAX_SIZE` 若上次调过保持一致即可。
    - **2. Pre-Test**：游戏内 `/mt` 全部自检无红 FAIL（Category Q 16 条全过：Q-01..Q-16，非 Q 可选项的黄 warning 可容忍）；登录横幅仍为 CONFIG_OPTIONS 4 项（本 phase 未增删配置项）。
    - **3. 单人木桩**：catAtk 循环打骷髅 1 分钟观察——Rake/Ferocious Bite 恒绿『landed』；Pounce/Rip 有绿色『landed』或偶发蓝色『landed ... (inferred)』均可接受；无『failed on』红行；无 Rip 持续重放（ripLeft 正常启动即为通过）。
    - **4. 多猫同目标（远程网友合作且对方零配置要求时选做）**：我方 Rip 后观察 ~1 秒内出现蓝色 `(inferred)` 兜底通告（apply 行被抑制时此通道仍交付落地）、ripLeft 启动且不再每帧重放 Rip；对方技能行不触发我方任何通告（guid 归属检查）。
    - **5. 猎人钉刺**（hunter 角色）：Serpent/Scorpid Sting 落地仍可见（绿色配对或蓝色推断）；远程位钉刺在 ~2 秒窗内正常落地。
    - **6. Expected Outcomes / Troubleshooting**：三种诊断支路——a) Rip 全绿无蓝：apply 未被抑制，正常（抑制仅多猫场景）；b) 蓝色推断后紧跟红色『was cancelled by ...』：windowed fail 否决在生效，属 fail-wins 预期；c) 无任何 land 通告：查 tracingSpells 注册与 SuperWoW RAW 通道、查 `/mt` Q 段。末尾加**注记（D-18 锁定）**：远程钉刺的蓝色推断锚偏早于真实 apply 约一个飞行时间（保守提前重挂、不留空窗），本版本接受为最终形态、不做 anchorBias。
    2. 关闭项清点（本任务执行时现场确认）：
       - 取证 todo：`.planning/` 下递归搜索 `druid-rip-land-forensics-next-cd*`——预期不存在（规划期已确认不在工作树与 git 索引）；存在则执行 `git rm` 后删除，不存在则记录 no-op；两种结果均写入本任务 done 与 SUMMARY。
       - 陈旧插桩注释扫描：`grep -rn -i 'rawdiag' core/ classes/ macro_torch.lua impl_util.lua biz_util.lua entity/ tools/`——预期仅 spell_trace_core.lua 中宣扬通告纪律的一行注释（"Plain show() — not a DIAG/RAWDIAG marker"）命中且其为语义负载注释（勿删）；其余任何命中视为陈旧残留，删除注释/文字仅、不改逻辑。
    3. 全文英文代码引用与注释、中文正文；不触碰 SM_Extend.lua。
  </action>
  <verify>
    <automated>git -C /home/admin/workspace/macro-torch diff --check && grep -c "## Phase 29: 统一 landing 判定重构" /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md && grep -c 'anchorBias' /home/admin/workspace/macro-torch/classes/druid/HUMAN-UAT.md</automated>
    <fails_when>git diff --check 打印任何行；第二个输出不是 1（Phase 29 节标题恰 1 处）；第三个输出不是 1（D-18 注记行恰 1 处提及 anchorBias——0 或 >=2 均为失败信号）</fails_when>
    <automated>find /home/admin/workspace/macro-torch/.planning -name 'druid-rip-land-forensics-next-cd*' -not -path '*/quick/*' | wc -l</automated>
    <fails_when>输出 != 0（终态必须是 0：本无残留记 no-op；执行时存在则随本任务 git rm 删除后重跑本门至 0——任何非 0 = 取证 todo 关闭项未收口）</fails_when>
    <automated>grep -rn -i 'rawdiag' /home/admin/workspace/macro-torch/core/ /home/admin/workspace/macro-torch/classes/ | wc -l</automated>
    <fails_when>输出 > 1（预期恰 1：spell_trace_core 的语义负载注释行；多于 1 即有陈旧残留未清）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>任何输出行（SM_Extend.lua 必须零变动）</fails_when>
  </verify>
  <acceptance_criteria>
    - HUMAN-UAT.md 末尾新增 Phase 29 节（六节可勾选清单 + D-18 锚偏差注记）
    - 取证 todo 搜索含 no-op 结论；rawdiag 扫描仅 hammer 注释命中 1（core/ + classes/ 目录合计）
    - git diff --check 干净；SM_Extend.lua 无 diff；UAT 协议不含 Deferred（FF 开 land / anchorBias / 动态 TTL 学习）任何要求动作
  </acceptance_criteria>
  <done>Phase 29 实机验收协议六节落地 HUMAN-UAT.md（含 D-18 注记 + D-17 零改动声明位置）；取证 todo no-op/删除两种收口；陈旧插桩注释零残留。</done>
</task>

<task type="auto">
  <name>Task 3: 全阶段静态收尾电池 — bbcheck 全文件 / 来源注册残留清零 / VERIFICATION-ONLY 零 diff / cpDamage 零改动 / 产物字节一致（D-17/D-18 + 全决策面）</name>
  <files>core/spell_trace_core.lua, classes/druid/Druid.lua, classes/hunter/Hunter.lua, classes/druid/selftest.lua, classes/druid/HUMAN-UAT.md</files>
  <read_first>
    - 本阶段三个计划全部 verify 块（本电池是其汇总，不复写单个任务的断言；新增全局断言在此定义）
    - core/events.lua 与 core/spell_trace_immune.lua 当前 HEAD 内容（确认 diff 为空——VERIFICATION-ONLY 契约）
  </read_first>
  <action>    不新增生产代码；按下述全局断言一次性跑穿（本任务即 Phase 29 的收官验证）：

    1. bbcheck 四源码文件（spell_trace_core.lua / Druid.lua / Hunter.lua / selftest.lua）全 BALANCED + `git diff --check` 全仓干净。
    2. Lua 5.0 令牌门：`#`/`goto`/`::` 在四个源码文件零命中。
    <!-- planner-discipline-allow: landSource -->

    <!-- planner-discipline-allow: landSources -->

    3. 来源注册残留清零（D-01 收口）：`grep -rn 'landSource' core/ classes/ macro_torch.lua impl_util.lua biz_util.lua entity/ tools/ 2>/dev/null | grep -v 'macroTorch.landSources == nil'` 输出为空（单数子串模式同时扫净单/复数两形态、含注释行；唯一合法豁免 = classes/druid/selftest.lua 中 Q-01 阴性断言行 `assert(macroTorch.landSources == nil, ...)`——这是 29-01 Task 3 重写 1a 的落地断言，其全文件唯一性由 29-01 Task 3 配套门钉死，豁免之外的任何命中都是残留；build 产物 SM_Extend.lua 不在此目录范围，其内容在用户重建前仍含旧码、属预期）。
    4. VERIFICATION-ONLY 契约：`git -C /home/admin/workspace/macro-torch diff --exit-code -- core/events.lua core/spell_trace_immune.lua` 零输出（两文件全程零 diff）。
    5. cpDamage 零改动（D-17）：`git -C /home/admin/workspace/macro-torch diff -- core/spell_trace_core.lua | grep -c 'pairCpDamageIntent\|onCpDamageLine\|cpDamageEvent'` 为 0（diff 上下文中不得出现 cpDamage 段落改动；函数体仍引用宏 LAND_INTENT_TTL——其数值变化即 D-17 期望的自动跟随）。复核引用面 `grep -c 'macroTorch.LAND_INTENT_TTL' core/spell_trace_core.lua` 预期 9（定义 + register 写 + 播种 + purge/pair 两趟 + finalizeFail + computeLandTable + pairCpDamageIntent 两趟；更多 = 注释含常量名，可协议；更少 = 引用面缺口）。
    6. 签名面收口：`grep -c 'intentTtl = 2' classes/hunter/Hunter.lua` = 2；`grep -c 'macroTorch.LAND_INTENT_TTL = 0.9' core/spell_trace_core.lua` = 1；三函数定义（豁免入口/两复活函数——verify 链中的三函数合一模式 grep 复核）各恰 1 处 = 3。
    7. 测试面收口：`grep -c 'SelfTest:register("Cat Q-' classes/druid/selftest.lua` = 16；HUMAN-UAT.md 含 Phase 29 节且 D-18 注记行存在。
    8. 产物字节一致：`git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua` 空输出。
    9. 变更面清点：`git -C /home/admin/workspace/macro-torch diff --stat HEAD` 列为 5 个预期文件（4 源码 + 1 UAT 文档），无多余文件（29-PATTERNS.md 若仍在未跟踪状态，不在 diff --stat 头 5 文件之列，执行时以实际工作区为准并在 SUMMARY 注明）。
    10. LF 行尾与注释语言门（CONTEXT (b) 两项约定的电池级落地——前者全文件扫描、后者仅对 diff 新增注释行抽查；均不跑 build.sh、不 grep SM_Extend.lua 内容）：
        a. CRLF 扫描：`grep -rl $'\r' core/spell_trace_core.lua core/events.lua classes/druid/Druid.lua classes/hunter/Hunter.lua classes/druid/selftest.lua`（相对仓库根执行；含 events.lua 的 VERIFICATION-ONLY 文件一并受检——只读；空输出 = 五文件 LF 行尾全绿）。
        b. 注释语言抽查（四源码文件 diff 新增注释行）：`git -C /home/admin/workspace/macro-torch diff -U0 HEAD -- core/spell_trace_core.lua classes/druid/Druid.lua classes/hunter/Hunter.lua classes/druid/selftest.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep '^[+][[:space:]]*--'` 提取整行注释（代码尾注释不在此列），随后 `| grep -c '[一-龥]'` 须为 0（新增注释零 CJK 字符）且 `| grep -cv '[a-z]'` 须为 0（每条新增注释行至少一个英文小写词元——纯符号分隔行同样改写为带英文措辞）。grep 为 ugrep 环境：锚定与加号须用 `^[+]`、`[[:space:]]*` 字符类形态（裸 `^+` 会被当作量化符报错）。

    约束：本任务绝不运行 build.sh、绝不写 SM_Extend.lua；任何断言失败即修复对应源头（非本任务的回归修复仍走原属计划的准则）后重跑电池至全绿。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/core/spell_trace_core.lua /home/admin/workspace/macro-torch/classes/druid/Druid.lua /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua /home/admin/workspace/macro-torch/classes/druid/selftest.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 任一文件输出 MISMATCH；或 diff --check 打印任何行</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/core/spell_trace_core.lua /home/admin/workspace/macro-torch/classes/druid/Druid.lua /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>任一文件计数 > 0</fails_when>
    <automated>grep -rn 'landSource' /home/admin/workspace/macro-torch/core/ /home/admin/workspace/macro-torch/classes/ /home/admin/workspace/macro-torch/macro_torch.lua /home/admin/workspace/macro-torch/impl_util.lua /home/admin/workspace/macro-torch/biz_util.lua /home/admin/workspace/macro-torch/entity/ /home/admin/workspace/macro-torch/tools/ 2>/dev/null | grep -v 'macroTorch.landSources == nil'; echo "RESIDUAL_SCAN_DONE"</automated>
    <fails_when>RESIDUAL_SCAN_DONE 之前出现任何输出行（单/复数两形态含注释行零残留；唯一合法豁免 = classes/druid/selftest.lua 的 Q-01 阴性断言行 `macroTorch.landSources == nil`，其全文件唯一性由 29-01 Task 3 配套门钉死——豁免之外的任何命中仍失败）；空输出后仅回显哨兵行</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff --exit-code -- core/events.lua core/spell_trace_immune.lua</automated>
    <fails_when>任何输出行（两 VERIFICATION-ONLY 文件必须与 HEAD 零 diff）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff -- core/spell_trace_core.lua | grep -c 'pairCpDamageIntent\|onCpDamageLine\|cpDamageEvent'; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>第一条 > 0（cpDamage 段落零改动）；第二条输出任何行（产物字节必须等于 HEAD）</fails_when>
    <automated>grep -c 'macroTorch.LAND_INTENT_TTL' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c 'SelfTest:register("Cat Q-' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "function macroTorch.recordLandEventRenewal\|function macroTorch.maintainLandTables\|function macroTorch.computeLandTable" /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>第一条 < 9（常量 9 处引用面：定义/register 写/播种/purge/pair/否决/computeLandTable/pairCpDamageIntent 两趟；更多 = 注释含常量名可协议）；第二条不是 16；第三条不是 3</fails_when>
    <automated>grep -rl $'\r' /home/admin/workspace/macro-torch/core/spell_trace_core.lua /home/admin/workspace/macro-torch/core/events.lua /home/admin/workspace/macro-torch/classes/druid/Druid.lua /home/admin/workspace/macro-torch/classes/hunter/Hunter.lua /home/admin/workspace/macro-torch/classes/druid/selftest.lua; echo "CRLF_GATE_DONE"</automated>
    <fails_when>CRLF_GATE_DONE 之前出现任何输出行 = 含 \r 的文件清单（LF 行尾违约，CONTEXT (b)）；空输出后仅回显哨兵行 = 通过</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff -U0 HEAD -- core/spell_trace_core.lua classes/druid/Druid.lua classes/hunter/Hunter.lua classes/druid/selftest.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep '^[+][[:space:]]*--' | grep -c '[一-龥]'; echo "COMMENT_CJK_GATE_DONE"</automated>
    <fails_when>COMMENT_CJK_GATE_DONE 之前输出 > 0（diff 新增注释行含 CJK 字符——英文注释约定违约；无匹配输出 0 且 grep 返回码 1 属预期）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff -U0 HEAD -- core/spell_trace_core.lua classes/druid/Druid.lua classes/hunter/Hunter.lua classes/druid/selftest.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep '^[+][[:space:]]*--' | grep -cv '[a-z]'; echo "COMMENT_WORD_GATE_DONE"</automated>
    <fails_when>COMMENT_WORD_GATE_DONE 之前输出 > 0（存在不含 [a-z] 词元的新增注释行——应为英文措辞；无匹配输出 0 且 grep 返回码 1 属预期）</fails_when>
  </verify>
  <acceptance_criteria>
    - 四项逐个绿：bbcheck 4 文件 + diff --check；令牌门 4 文件全 0；来源注册残留扫描（8 个目录，豁免 Q-01 阴性断言后）空；VERIFICATION-ONLY 两文件零 diff
    - cpDamage 段 zero-diff；LAND_INTENT_TTL 引用计数 ≥ 9；Hunter intentTtl=2 x2；三新/复活函数定义各 1；Category Q = 16；UAT Phase 29 节含 D-18 注记
    - LF/注释语言门（CONTEXT (b) 电池级落地）：CRLF 扫描 5 文件（含 events.lua）空输出；diff 新增注释行 CJK 计数 0 且缺 [a-z] 词元行计数 0
    - SM_Extend.lua 全程字节等于 HEAD（三个计划所有任务累计成立）
    - 变更面 = 5 个预期文件；无 build 脚本/清单改动
  </acceptance_criteria>
  <done>Phase 29 收尾电池全绿：统一 OR 落地（29-01）、反推兜底（29-02）、16 条 Q 夹具 + UAT 协议（29-03）全部就位；事件层与 immune 层零扰动；cpDamage 零结构改动（D-17）；SM_Extend.lua 字节一致待用户侧重建。</done>
</task>

</tasks>

## Artifacts this phase produces（29-03 段）

- `classes/druid/selftest.lua` — Q-12 / Q-14 / Q-15 / Q-16 四条新注册（Category Q 总量 16）
- `classes/druid/HUMAN-UAT.md` — 新增「Phase 29: 统一 landing 判定重构 — 实机 UAT 闭环」六节协议（含 D-18 锚偏差锁定注记与 D-17 零改动声明）
- 变动行为声明（D-17/D-18 以文档+断言呈现，无新生产代码）：cpDamage 配对窗随 LAND_INTENT_TTL = 0.9 自动生效；远程推断锚偏早一个飞行时间为最终形态
- 取证 todo `druid-rip-land-forensics-next-cd.md` — 关闭项确认（工作树与索引均不存在 → no-op 记录；若执行时发现即 git rm）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试夹具 → 生产代码 | Q-12..Q-16 以种植数据驱动真实入口（recordLandEvent/pairLandIntent/processRawAuraApply/recordLandEventRenewal）；任何夹具对全局的写入均受 CR-01 快照/恢复纪律约束 |
| UAT 文档 → 用户操作 | 协议指引用户在游戏机执行重建与实机观察；协议不得要求降级行为 |
| 源码 → SM_Extend.lua（build 产物） | 严格产物不触碰（字节一致性门每任务执行） |

## STRIDE Threat Register（ASVS Level 1；block-on-high 阈）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-08 | Spoofing（多猫归因） | 2s 钉刺窗交叉配对（D-12） | low | accept | GCD 间隔下同目标双钉刺 2 秒窗重叠概率低；guid 归属与最早到达去重限损；属既有 WR-02 残余风险类 |
| T-29-09 | Repudiation（观感） | 远程推断锚偏早（D-18） | low | accept | 锁定为最终形态——保守提前重挂、不留空窗；UAT 注记与 troubleshooting 支路 b 覆盖误读风险 |
| T-29-10 | Information disclosure（构建完整性） | SM_Extend.lua build 产物 | medium | mitigate | 三计划全部任务含字节一致性门；本计划电池第 8 条收口 |

无 high 级威胁——无需阻断任务。
</threat_model>

<verification>
- 三任务 verify 全绿即 Phase 29 机器侧验收通过；人类侧验收走 HUMAN-UAT.md（end-of-phase human-verify 汇聚、verifier 汇总至 29-UAT.md）
- 与 29-01/29-02 的计划级 provided 断言合并后，全阶段 18 条锁定决策均至少有一条验收锚（D-01..D-15 见 29-01/29-02；D-16 本计划任务 1；D-17/D-18 本计划任务 2/3）
</verification>

<success_criteria>D-16 六组边界用例全数落地（Category Q = 16）；Phase 29 UAT 协议入档；全源零来源注册残留；VERIFICATION-ONLY 两文件零 diff；cpDamage 零结构改动且窗口随 0.9 生效；SM_Extend.lua 全程字节等于 HEAD；变更面恰 5 个预期文件。
</success_criteria>

<output>
Create `.planning/phases/29-landing/29-03-SUMMARY.md` when done
</output>