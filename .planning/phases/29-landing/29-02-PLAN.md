---
phase: 29-landing
plan: "02"
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - core/spell_trace_core.lua
  - classes/druid/selftest.lua
autonomous: true
requirements: [D-03, D-04, D-13, D-14, D-16]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  # truths/prohibitions hand-derived from DESIGN-CONTEXT 锁定决策 3/4 + CONTEXT D-03/D-04/D-13/D-14（spec-less probe skipped：无 requirement ID）
  truths:
    - "全窗口静默的 cast 在 ttl 到期后由 0.1s 周期任务推定 landed：锚 = cast 时刻、蓝色通告带 (inferred) 后缀（D-03/D-13/D-14）"
    - "反推永不与游戏事件竞争：blip <= ttl 时直接返回；已有证据覆盖的 cast（lastLand >= lastCast）不再反推（D-03/D-02 谓词复用）"
    - "存在 cast <= failTime <= cast + ttl 的 fail 时反推被否决；窗口外 fail 不否决（D-04）"
    - "maintainLandTables 以 tracingSpells + inCombat 双门守卫，任务键 'maintainLandTables' 0.1s 注册（unique key）"
    - "Q-09 断言双函数复活与周期注册；Q-11/Q-13 钉死无证据反推与窗口化否决两行为（D-16 前两条）"
    - "SM_Extend.lua 全程字节等于 HEAD"
  artifacts:
    - core/spell_trace_core.lua
    - classes/druid/selftest.lua
  key_links:
    - "registerPeriodicTask('maintainLandTables', {interval = 0.1, task = ...}) → onPeriodicUpdate 0.1s 帧循环 → computeLandTable(spell) per tracingSpells 条目"
    - "computeLandTable ttl 读取 landIntentTtls[spell]（钉刺 2 / 猫德 0.9）→ 与 pairLandIntent/finalizeFail 同一 per-spell 窗语义（D-05 一参三用）"
  prohibitions:
    - "不修改 core/events.lua 与 core/spell_trace_immune.lua（VERIFICATION-ONLY）— flagged-unverified"
    - "不跑 build.sh、不触碰 SM_Extend.lua — flagged-unverified"
---

<objective>
本计划为 Phase 29 的 Wave-2 扩展切片：在 29-01 已证明的 per-spell ttl + cast 维去重骨架上，复活「反推兜底层」——0.1s 周期任务 maintainLandTables 驱动 computeLandTable，在全窗口静默（无 self-hit / 无配对 apply / 无窗口内 fail）时以 cast 时刻为锚推定 landed，蓝色通告带 (inferred) 后缀。此层是 DESIGN-CONTEXT 决定 3 的多猫抑制解药（apply 被抑制也能拿到 land，双假设均正确），并附带重新激活 immune 路径 2 的 land 供给。

Purpose: 用已验证的 ttl/去重基建外扩唯一的"反推"能力，同时把 D-16 六组边界用例的前两组（无证据反推、fail 窗口否决）与 Q-09 复活断言一并落地，使该层一落地即被行为钉死。
Output: 2 个文件的改动 + 反推层静态证据（bbcheck + 源码断言 + Q-09/Q-11/Q-13 三个注册）。运行级验证仍落在用户机 /mt（Q 全绿）与 29-03 HUMAN-UAT。

实现事实以 DESIGN-CONTEXT 锁定决策 3/4 + 29-CONTEXT D-03/D-04/D-13/D-14 为权威，锚点以 29-PATTERNS.md §1e（eb56a257 旧 computeLandTable 原文的现代化清单）为准：
- 旧 blip 三重硬编码（0.02 下界 / 0.9 上界 / ±0.05 邻接）全部被窗口模型取代：下界即 ttl 触发点、上界取消（迟到即迟判，锚仍为 cast 时刻）、邻接失败判定换成窗口化否决——不新增任何魔法数字常量。
- "只在全窗口静默时触发"由三谓词构造性保证：blip > ttl（事件早应到达）、lastLand < lastCast（未被真实证据/先前反推覆盖）、无窗口内 fail。
- 通告样式：`{spell} cast on {mob} landed: {time} (inferred)`，颜色 'blue'（D-13/D-14）。
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-landing/29-CONTEXT.md
@.planning/phases/29-landing/DESIGN-CONTEXT.md
@.planning/phases/29-landing/29-PATTERNS.md

# 关键锚点（HEAD 行号，漂移时用模式文本定位）
@core/spell_trace_core.lua        # 29-01 落地后的 landIntentTtls / recordLandEventRenewal / peek 三件套；pairCpDamageIntent（勿动）@202-229；insert 点位 = recordLandEventRenewal 之后
@core/spell_trace_immune.lua      # spellsImmuneTracing 守卫形态 @31 + 周期注册 @63-64（同包先例，只读参考）
@core/periodic.lua                # registerPeriodicTask @127-129（unique key 契约）
@classes/druid/selftest.lua       # Category Q 头 + Q-09 现文 @971-978；Q-02 夹具纪律模板 @786-825；Q-04/Q-05 种植 intent 形式 @855-907
@.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js  # verify 复用其绝对路径
</context>

<tasks>

<task type="auto">
  <name>Task 1: 反推兜底层复活 — maintainLandTables + computeLandTable 窗口化推断（D-03/D-04/D-13/D-14）</name>
  <files>core/spell_trace_core.lua</files>
  <read_first>
    - core/spell_trace_core.lua 全文件（29-01 落地后的 register/landIntentTtls/recordLandEventRenewal/peek 三件套现状）
    - git 历史 `eb56a257:core/spell_trace_core.lua` 68-76 与 150-197（旧 maintainLandTables/computeLandTable 原文——守卫/懒初始化/push 形状照抄，逻辑按 D-03/D-04 换新）
    - 29-PATTERNS.md §1e（现代化清单）与 Shared Patterns（guard shape / announcement 纪律）
    - core/spell_trace_immune.lua 31-34 与 63-64（同包周期任务的守卫形态与注册行先例）
    - core/periodic.lua 127-129（registerPeriodicTask unique key 契约）
  </read_first>
  <action>
    本任务在 recordLandEventRenewal 定义之后、onLandEvent 之前插入两个函数与一行周期注册（反推兜底层完整复活；29-01 的 ttl 注册表与去重谓词是其全部依赖，本任务不新增模块级表）。全部注释英文。

    1. `function macroTorch.maintainLandTables()` 照抄 archived 守卫与循环形状：守卫 `if not macroTorch.tracingSpells or macroTorch.tableLen(macroTorch.tracingSpells) == 0 or not macroTorch.inCombat then return end`；循环 `for spellName in pairs(macroTorch.tracingSpells) do macroTorch.computeLandTable(spellName) end`。函数体上方一行英文注释（set up the land event generation for traced spells；silent-window inference rescue）。
    2. 紧随其后模块级注册行：`macroTorch.registerPeriodicTask('maintainLandTables', { interval = 0.1, task = macroTorch.maintainLandTables })`。执行前先确认核心 periodicTasks 键无同名残留（Phase 27 已删，键空闲——只读确认，不得覆盖任何既有键）。
    3. `function macroTorch.computeLandTable(spell)` 按下列确定性谓词序实现（守卫形状与懒初始化照抄 archived 150-176）：
       a. 守卫：`if not spell or not macroTorch.target.isCanAttack then return end`；`if not macroTorch.loginContext then return end`。
       b. landTable/spell/mob 三级懒初始化 + `macroTorch.LRUStack:new(100)`（archived 同形）；`local mob = macroTorch.target.name` 在此取得，供 push 与通告用。
       c. `local lastCast = macroTorch.peekCastEvent(spell)`；`if not lastCast then return end`（无 cast 无从反推）。
       d. `local ttl = macroTorch.landIntentTtls[spell] or macroTorch.LAND_INTENT_TTL`（per-spell 窗：钉刺 2 / 猫德 0.9——与配对/否决同一语义，一参三用）。
       e. `local blip = GetTime() - lastCast`；`if blip <= ttl then return end`——窗口未静默，游戏证据仍可能到达；反推永远不与其竞争（D-03）。加英文注释注明：window-still-open return replaces 旧的 blip 下界/上界双常量——触发边即 ttl，无魔法数字。
       f. 覆盖谓词（D-02 复用）：`local lastLand = macroTorch.peekLandEvent(spell) or 0`；`if lastLand >= lastCast then return end`——该 cast 已有真实证据或先前反推覆盖。
       g. 窗口化 fail 否决（D-04）：`local lastFail = macroTorch.peekFailEvent(spell)`；`if lastFail and lastFail[1] >= lastCast and (lastFail[1] - lastCast) <= ttl then return end`——存在 cast <= failTime <= cast + ttl 的 fail 即否决本次推断（替代旧 ±0.05s 邻接启发式，failTable 元素为 {time, failType} 对）。
       h. 推定：`macroTorch.loginContext.landTable[spell][mob].push(lastCast)`（锚 = cast 时刻，D-03）。
       i. 通告（D-13/D-14）：`macroTorch.show(spell .. ' cast on ' .. mob .. ' landed: ' .. lastCast .. ' (inferred)', 'blue')`；其上一行英文注释注明：blue reserved for the inferred fallback so real green evidence stays visually distinguishable；inferred 后缀供实机肉眼区分 apply 确认与反推兜底。
    4. 全文检查：新增代码零魔法数字（0.1 间隔属于周期任务注册先例）；不新增对 GetTime() 之外时钟的引用；cpDamage 函数体与 29-01 已落地段零改动。

    约束：注释英文、LF 行尾、Lua 5.0 令牌（#/goto/:: 零出现）——全文当前离线或新增行一律遵守；不触碰 SM_Extend.lua 与 build 脚本。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/core/spell_trace_core.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH（node 非零退出），或 git diff --check 打印任何行</fails_when>
    <automated>grep -c 'function macroTorch.maintainLandTables' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c 'function macroTorch.computeLandTable' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c "registerPeriodicTask('maintainLandTables'" /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>任一输出不是 1（两函数定义与周期注册行各恰 1 处；0=缺失，>=2=重复定义）</fails_when>
    <automated>grep -c 'landIntentTtls\[spell\] or macroTorch.LAND_INTENT_TTL' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c 'blip <= ttl' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c 'lastLand >= lastCast' /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>第一个不是 2（播种 + computeLandTable 读取恰两处）；第二个不是 1（静默窗返回恰 1 处）；第三个不是 2（recordLandEvent 去重 + computeLandTable 覆盖谓词恰两处——同一 cast 维谓词两个消费点）</fails_when>
    <automated>grep -c 'lastFail\[1\] >= lastCast' /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c "'(inferred)', 'blue'" /home/admin/workspace/macro-torch/core/spell_trace_core.lua && grep -c "landTable\[spell\]\[mob\].push(lastCast)" /home/admin/workspace/macro-torch/core/spell_trace_core.lua</automated>
    <fails_when>任一不是 1（否决谓词 / 蓝色 (inferred) 通告 / cast 锚 push 各恰 1 处）</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/core/spell_trace_core.lua; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>grep 输出 > 0；或 git status 输出任何行（SM_Extend.lua 必须零变动）</fails_when>
  </verify>
  <acceptance_criteria>
    - maintainLandTables/computeLandTable 两定义与 'maintainLandTables' 周期注册各恰 1 处；守卫含 tracingSpells（空集退出）与 macroTorch.inCombat 双门
    - computeLandTable 谓词序确定：no-cast 返回 → blip <= ttl 返回 → 覆盖谓词 lastLand >= lastCast 返回 → 窗口 fail 否决返回 → push(lastCast) → 蓝色 (inferred) 通告；全程零魔法数字（ttl 即触发边）
    - grep 三处谓词锚与备案值吻合；bbcheck BALANCED；git diff --check 干净；令牌门 0；SM_Extend.lua 无 diff
  </acceptance_criteria>
  <done>反推兜底层落地：全窗口静默的 cast 在 ttl 到期后由 0.1s 周期任务推定 landed（锚 = cast 时刻、蓝色 (inferred) 通告），窗口化 fail 否决与 cast 维覆盖谓词保证其永不与游戏事件竞争；immune 路径 2 的 land 供给随之复活。</done>
</task>

<task type="auto">
  <name>Task 2: Q-09 复活断言重写 + Q-11 无证据反推 + Q-13 窗口化否决双场景（D-16 前两条）</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua 763-1011（Category Q 全段与 Q-09 现文 @971-978）
    - classes/druid/selftest.lua 786-825（Q-02 CR-01 stub 纪律模板：fake 先建/pcall/show stub/恢复后断言）
    - classes/druid/selftest.lua 850-907（Q-04/Q-05 种植 intent + LRUStack 直灌形式）
    - core/spell_trace_core.lua Task 1 落地后的 computeLandTable 谓词序（夹具必须对其播种）
    - core/periodic.lua 17-92（LRUStack push/top/elements 访问）
  </read_first>
  <action>    在 Category Q 段追加/重写三条注册（全部 isOptional=true、CR-01 纪律、英文）：Q-09 原位重写；Q-11 与 Q-13 追加在 Q-10 之后（顺序编号 Q-11、Q-13——Q-12 留待 29-03）。

    1. Q-09 重写（现 971-978）——测试名改为 `"Cat Q-09: inference core revived with 0.1s periodic registration"`，断言换为复活形态：`type(macroTorch.maintainLandTables)` 与 `type(macroTorch.computeLandTable)` 均为 'function'；`macroTorch.periodicTasks['maintainLandTables'] ~= nil` 且其 `.interval == 0.1` 且 `.task == macroTorch.maintainLandTables`；`macroTorch.consumeDruidBattleEvents == nil`（旧消费者仍不存在——保持）
    2. Q-11 追加 `"Cat Q-11: silent-window inference fires after ttl with blue (inferred) announcement"`（CR-01 夹具）：
       - 快照 savedLoginContext/savedTarget/savedShow；fakes：`fakeLoginContext = {}`、`fakeTarget = { isCanAttack = true, name = 'QTestMob', hasBuff = function(self) return false end }`；`local captured = {}`；`macroTorch.show = function(msg, color) table.insert(captured, { msg = msg, color = color }) end`。
       - 同一时钟播种（防夹具漂移）：`local now0 = GetTime()`；`local seedCast = now0 - 2.0`（2.0 > 0.9 默认窗 → 窗口已静默）。建 `fakeLoginContext.castTable`/`['Rake']`/`['QTestMob']` = LRUStack:new(100) 并 push(seedCast)；同法建空 landTable 与空 failTable 两栈（守卫与懒初始化能找到即可）。
       - pcall 内 `macroTorch.computeLandTable('Rake')`；捕获 `rakeTop = fakeLoginContext.landTable['Rake']['QTestMob'].top`（pcall 后局部读取亦可）。
       - 恢复登录上下文/目标/show（raw assignment），恢复后断言：pcallRes；rakeTop == seedCast（锚 = cast 时刻）；`macroTorch.tableLen(captured) == 1`；captured[1].msg 以 'Rake cast on QTestMob landed:' 开头且含 '(inferred)'（用 string.find 第三个参数 true 做纯文本匹配或直接比对拼接段）；captured[1].color == 'blue'。
    3. Q-13 追加 `"Cat Q-13: windowed fail veto — inside vetoes inference, outside does not"`（单注册两阶段，每阶段重铺 fakes）：
       - 阶段 A（窗内否决）：fresh fakes 同 Q-11 形态 + captured 重铺；`local nowA = GetTime()`；cast 栈 push(nowA - 1.5)；fail 栈 push({ nowA - 1.0, 'resist' })（fail - cast = 0.5 ∈ [0, 0.9] → 否决）；pcall computeLandTable('Rake')；断言 landTopA == nil 且 capturedA 空（零通告）。
       - 阶段 B（窗外不否决）：同一注册内重铺三栈与 captured；`local nowB = GetTime()`；cast 栈 push(nowB - 1.5)；fail 栈 push({ nowB - 1.6, 'resist' })（fail 早于 cast → 窗下界排除）；pcall computeLandTable('Rake')；断言 landTopB == 该阶段种植的 cast 值、capturedB 恰 1 条蓝色且含 '(inferred)'。
       - 两阶段均在 pcall 内、全局恢复后统一断言（CR-01）。
    4. Category Q 段头注释（若有 29-01 遗留的 Q-11..Q-16 前瞻措辞）保持最新：三段新增后 Category Q 序列 = Q-01..Q-11 + Q-13（Q-12 待 29-03）。

    约束：不写 tracingSpells/landListeners/真实 loginContext 子表；英文注释；isOptional=true；不触碰 SM_Extend.lua。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/classes/druid/selftest.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH，或 git diff --check 打印任何行</fails_when>
    <automated>grep -c "Cat Q-09: inference core revived" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "Cat Q-11: silent-window inference" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "Cat Q-13: windowed fail veto" /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>任一不是 1（三条注册各恰 1 处）</fails_when>
    <automated>grep -c 'computeLandTable(' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "periodicTasks\['maintainLandTables'\]" /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c "'(inferred)'" /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>第一个 < 2（Q-11/Q-13 各至少一次直接驱动 computeLandTable）；第二个不是 1（Q-09 周期注册断言恰 1 处）；第三个 < 2（Q-11/Q-13 各至少一处反推通告断言）</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/classes/druid/selftest.lua; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>grep 输出 > 0；或 git status 输出任何行</fails_when>
  </verify>
  <acceptance_criteria>
    - Q-09 断言：双函数 type == 'function'、周期任务键存在且 interval 0.1 且 task 指向 maintainLandTables、consumeDruidBattleEvents 仍为 nil
    - Q-11 断言：无证据播种下 computeLandTable 推定 land，top == 种植 cast 值，show 恰 1 次、蓝色、消息含 '(inferred)'
    - Q-13 断言：窗内 fail（+0.5s）零通告零 land；窗外 fail（-0.1s 早于 cast）推断照发——两阶段同一注册内完成
    - CR-01 纪律：全局恢复先于一切 assert；bbcheck BALANCED；令牌门 0；git diff --check 干净；SM_Extend.lua 无 diff
  </acceptance_criteria>
  <done>反推层被三条注册钉死：Q-09 复活断言、Q-11 无证据反推、Q-13 窗口化否决双场景；D-16 六条边界用例完成第 1/3 条。</done>
</task>

</tasks>

## Artifacts this phase produces（29-02 段）

- `macroTorch.maintainLandTables()` — 复活函数：tracingSpells + inCombat 双门守卫，驱动 per-spell 推断（D-03）
- `macroTorch.computeLandTable(spell)` — 复活函数：静默窗（blip > ttl）且无覆盖且无窗内 fail 时以 cast 时刻为锚推定 landed，蓝色 "(inferred)" 通告（D-03/D-04/D-13/D-14）
- 周期任务注册 `'maintainLandTables'`（interval 0.1，unique key，重新进入 periodicTasks 注册表）
- `classes/druid/selftest.lua` — Q-09 重写（复活断言）、Q-11（无证据反推）、Q-13（窗口化否决双场景）
- 有意不产出：无新命名常量（0.02/0.9/0.05 三旧硬编码被 ttl 触发边取代，见 Task 1 注释——这是重构本征，非遗漏）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 游戏事件流 → spell_trace_core | 反推仅在正推管道全沉默时介入；fail 行与 apply 行仍是外部输入 |
| spell_trace_core → landTable 消费者 | 反推写入的 land（锚 = cast）改变 ripLeft/immune 判定供给——正是多猫抑制的修复路径 |
| 源码 → SM_Extend.lua（build 产物） | 产物不参与本计划任何写操作 |

## STRIDE Threat Register（ASVS Level 1；block-on-high 阈）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering（逻辑） | computeLandTable 误推（错把失败/竞争中的 cast 推定 landed） | medium | mitigate | 三谓词构造性防火：blip > ttl（窗口已静默）、lastLand < lastCast（未被覆盖）、窗内 fail 否决；Q-11/Q-13 行为钉死；写锚 = cast 时刻不回退既有 land |
| T-29-06 | Information disclosure（逻辑） | immune 路径 2（landed + 纹理）随 land 供给复活而行为变化 | low | accept | DESIGN-CONTEXT「附带收益」——纯 dot 技能真实免疫自检通道复位即设计目标；29-03 UAT 观察点覆盖 |
| T-29-07 | Information disclosure（构建完整性） | SM_Extend.lua build 产物 | medium | mitigate | 每任务 verify 含字节一致性门；build 在用户侧执行 |

无 high 级威胁——无需阻断任务。
</threat_model>

<verification>
- 本计划两任务 verify 全绿（bbcheck 2 文件、谓词锚 grep、令牌门、SM_Extend.lua 字节一致）
- 汇总断言：`grep -c 'blip <= ttl' core/spell_trace_core.lua` = 1；`grep -c 'lastFail[1] >= lastCast' core/spell_trace_core.lua` = 1；`grep -c "'(inferred)', 'blue'" core/spell_trace_core.lua` = 1；`grep -c 'Cat Q-1[13]:' classes/druid/selftest.lua` = 2
- 交叉文件：events.lua 与 spell_trace_immune.lua 在 `git diff --exit-code` 下零改动（VERIFICATION-ONLY 契约）
</verification>

<success_criteria>反推兜底层跑通并被钉死：周期任务 0.1s 驱动、静默窗 ttl 触发、cast 锚推定、蓝色 (inferred) 通告、窗口化 fail 否决、覆盖谓词复用；Q-09/Q-11/Q-13 三注册落地；immune 路径 2 复活；SM_Extend.lua 字节等于 HEAD。
</success_criteria>

<output>
Create `.planning/phases/29-landing/29-02-SUMMARY.md` when done
</output>