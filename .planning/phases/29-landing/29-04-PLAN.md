---
phase: 29-landing
plan: "04"
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - interface_debug.lua
  - classes/druid/selftest.lua
autonomous: true
requirements: [D-14]
gap_closure: true
gap_ids: [G-29-3]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  # truths hand-derived from 29-UAT.md Gaps G-29-3 + debug session Resolution（无 req ID；reqs 覆盖门由编排器跳过）
  truths:
    - "用户 Windows+Cygwin 重建后实机观感归位：rake/bite landed 行渲绿、pounce/rip (inferred) 行渲蓝（G-29-3 truth；HUMAN-UAT §247-248 协议）"
    - "interface_debug.lua 的 blue/green 两臂字面量 hue 与协议一致：blue 臂蓝主导、green 臂绿主导、OFFICER 键引用清零（静态可判，本机即验证）"
    - "Category T 就绪 2 条：T-02 以真实 macroTorch.show 为被测对象断言五臂色相，任何一臂颠倒即红/黄（映射层零覆盖的终态闭环）"
    - "静态电池全绿：bbcheck 两文件 BALANCED、Lua 5.0 令牌门 0、CRLF 0、diff 新增注释零 CJK、SM_Extend.lua 全程等于 HEAD"
  artifacts:
    - interface_debug.lua
    - classes/druid/selftest.lua
  key_links:
    - "macroTorch.show 映射表 → DEFAULT_CHAT_FRAME:AddMessage（唯一渲染出口）→ 用户实机观感（spell_trace_core 通告、macroTorch.log、diag、Target 同表受害/受益）"
    - "T-02 夹具 → 真实 show()（区别于 Q 系列的 show 整体 stub，直达映射层）"
    - "spell_trace_core.lua:451/499/598 调用点标签 → 修复后渲染（受害方零改动，标签正确性已由 debug session Eliminated 第 1 条钉死）"
  prohibitions:
    - "不修改 core/ 任何文件（spell_trace_core.lua 调用点为受害方，零改动）"
    - "不跑 build.sh、不触碰 SM_Extend.lua（用户 Windows+Cygwin 重建后自然带修复）"
    - "Category Q 16 条断言零改动（只新增 Category T T-02）"
---

<objective>
本计划唯一目标是关闭 UAT gap **G-29-3**（severity: minor）：Phase 29 判定逻辑与调用点颜色标签全部正确，颜色编码颠倒集中在渲染层 `macroTorch.show`（interface_debug.lua:87-99）的颜色名→色相翻译表绿/蓝两臂——'blue' 映射到 OFFICER 频道色（1.12 实渲绿色）、'green' 映射到自定义 RGB {0, 0.5, 0.9}（蓝通道主导，实渲蓝色）。缺陷为 GSD 重构前遗留（7d2369b/5594a09），Phase 29 建立绿/蓝协议（D-14 锁定蓝色 (inferred)）后首次可见。诊断闭环见 `.planning/debug/landing-color-inverted.md`（sysmptoms/evidence 双向吻合、根因证实）。

按 gap.missing 两条施工：修正两臂字面量；补一条对渲染映射表色相的回归自测（Q 系列测试期整体 stub 掉 show，渲染层零覆盖）。

Purpose: 让 HUMAN-UAT §247-248 的绿色=landed / 蓝色=inferred 协议在渲染层成立，并让映射层从此有行为断言钉死（颠倒即红）。这是渲染层共享通路（macroTorch.log、diag、Target 等同表），修一处全仓观感归位。
Output: 2 个文件的改动 + 静态电池绿色证据。本计划不产生新的判定逻辑（纯渲染字面量修正 + 测试注册）。
</objective>

<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-landing/29-UAT.md                    # Gaps G-29-3（truth/root_cause/artifacts/missing）
@.planning/debug/landing-color-inverted.md                # 根因诊断闭环（Resolution/Eliminated/Evidence）
@.planning/phases/29-landing/29-CONTEXT.md                # D-14 蓝色 (inferred) 颜色契约
@.planning/phases/29-landing/29-03-PLAN.md                # verify 命令继承源（bbcheck 路径/令牌门/CRLF/CJK 门写法）

# 关键锚点（HEAD 行号，漂移时用模式文本定位）
@interface_debug.lua                  # macroTorch.show @87-99（唯一渲染出口；全仓唯一 ChatTypeInfo/custom_green 引用面）
@classes/druid/selftest.lua           # T-01 @1822-1825 + Category T 计数注释 @1816-1827；Q-11 CR-01 夹具模板 @1054-1105
@core/selftest.lua                    # run() 失败透传 @49-98（isOptional=true → yellow warning）
@core/spell_trace_core.lua            # 受害方调用点：:451 'blue'/(inferred)、:499/:598 'green'/landed（只读，零改动）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修正 macroTorch.show 绿/蓝两臂色相——G-29-3 root_cause 直接修复（missing 第 1 条）</name>
  <files>interface_debug.lua</files>
  <read_first>
    - interface_debug.lua 85-100（macroTorch.show 现状全文）
    - .planning/debug/landing-color-inverted.md 的 Resolution 与 Evidence 全段（root_cause 字面 + fix 方向）
    - .planning/phases/29-landing/29-UAT.md Gaps G-29-3（truth 与 missing 两条）
    - classes/druid/HUMAN-UAT.md 中以 绿色=landed / 蓝色=inferred 为约定的段落（§247-248 附近，协议权威文本）
  </read_first>
  <!-- planner-discipline-allow: OFFICER -->
  <!-- planner-discipline-allow: r = 0, g = 1, b = 0 -->
  <action>按 G-29-3 missing 第 1 条修正 `macroTorch.show` 的映射表。允许触碰的范围严格限于：blue 臂 c 赋值行、green 臂 c 赋值行、函数头正上方新增的英文注释块。其余一律不动——`function macroTorch.show(a, color)` 签名、`local col = color or 'white'`、SAY 缺省臂、red→YELL 臂、yellow→SYSTEM 臂、`DEFAULT_CHAT_FRAME:AddMessage(tostring(a), c.r, c.g, c.b, c.id)` 调用行、log() 函数体与文件其余部分保持字节不变。

    1. blue 臂（现为 ChatTypeInfo 的 OFFICER 键引用——1.12 客户端实渲绿色，倒置源）改为复用现有已验证渲蓝的自定义 RGB：
       `c = { r = 0, g = 0.5, b = 0.9, id = 'custom_blue' }`
    2. green 臂（现 `{ r = 0, g = 0.5, b = 0.9, id = 'custom_green' }`——蓝通道主导实渲蓝色）改为纯绿色：
       `c = { r = 0, g = 1, b = 0, id = 'custom_green' }`
    3. 在 `function macroTorch.show(a, color)` 上一行插入注释块，措辞逐字（英文、ASCII、含防错语义）：
       `-- Color-label contract (HUMAN-UAT phase 29, D-14): the label must match`
       `-- the rendered hue - green = landed, blue = inferred. Each named arm is a`
       `-- hue-dominant literal so a future edit cannot silently invert it.`
       该注释及本文件其余新增文字不得出现 OFFICER 字样（配合 OFFICER 清零门的成立）。
    4. 明确不改（防过度扩散）：spell_trace_core.lua:451/499/598 三处调用点标签正确（受害方，debug session Eliminated 第 1 条已证），零改动。macroTorch.log 转发同表（interface_debug.lua:112），无需改动即随修复受益。
    5. 横向影响评估（计划期已完成，抄录至 SUMMARY）：全仓唯一 ChatTypeInfo 与 custom_green 引用面即本函数（grep 已证）；绿臂消费者 = spell_trace_core.lua:499/:598 直接 landed、Target.lua:65、diag.lua 十余处、macroTorch.log 链路——全部走同一 arm，修复后统一变真绿，正是协议期望；无任何文件依赖旧 OFFICER 臂或旧蓝调字面量。red/yellow 臂不在本次缺陷范围且用户已确认正常，保持不动。
    6. 不运行 build.sh；SM_Extend.lua 保持 HEAD 字节（用户下次 Windows+Cygwin rebuild 后自然带修复）。Lua 5.0：不得引入 `#`/goto/`::`。
  </action>
  <verify>
    <automated>grep -n -A 2 "'blue' == col" /home/admin/workspace/macro-torch/interface_debug.lua; grep -n -A 2 "'green' == col" /home/admin/workspace/macro-torch/interface_debug.lua</automated>
    <fails_when>blue 臂上下文不含 `c = { r = 0, g = 0.5, b = 0.9, id = 'custom_blue' }`；或 green 臂上下文不含 `c = { r = 0, g = 1, b = 0, id = 'custom_green' }`</fails_when>
    <automated>grep -c 'custom_blue' /home/admin/workspace/macro-torch/interface_debug.lua && grep -c "r = 0, g = 1, b = 0" /home/admin/workspace/macro-torch/interface_debug.lua && grep -c 'Color-label contract' /home/admin/workspace/macro-torch/interface_debug.lua</automated>
    <fails_when>任一 != 1（blue 字面量恰 1 / 纯绿字面量恰 1 / 契约注释恰 1）</fails_when>
    <automated>grep -c 'ChatTypeInfo\["OFFICER"\]' /home/admin/workspace/macro-torch/interface_debug.lua</automated>
    <fails_when>!= 0（OFFICER 键引用彻底清零，含注释不得出现）</fails_when>
    <automated>grep -c 'ChatTypeInfo\["SAY"\]' /home/admin/workspace/macro-torch/interface_debug.lua && grep -c 'ChatTypeInfo\["YELL"\]' /home/admin/workspace/macro-torch/interface_debug.lua && grep -c 'ChatTypeInfo\["SYSTEM"\]' /home/admin/workspace/macro-torch/interface_debug.lua && grep -c 'DEFAULT_CHAT_FRAME:AddMessage(tostring(a), c.r, c.g, c.b, c.id)' /home/admin/workspace/macro-torch/interface_debug.lua</automated>
    <fails_when>四项任一 != 1（SAY/YELL/SYSTEM 三臂与 AddMessage 出口行均保持恰 1 处原文——未被意外扰动）</fails_when>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/interface_debug.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 输出 MISMATCH；或 git diff --check 打印任何行</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/interface_debug.lua; grep -c $'\r' /home/admin/workspace/macro-torch/interface_debug.lua; echo "T1_TOKEN_CRLF_DONE"</automated>
    <fails_when>T1_TOKEN_CRLF_DONE 之前第一个输出 > 0（Lua 5.0 令牌门违约）；第二个输出 > 0（CRLF 行尾违约，仓库约定 LF）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff -U0 HEAD -- interface_debug.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep '^[+][[:space:]]*--' | grep -c '[一-龥]'; echo "T1_COMMENT_CJK_DONE"; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>T1_COMMENT_CJK_DONE 之前输出 > 0（diff 新增注释行含 CJK——英文注释约定违约）；git status 输出任何行（SM_Extend.lua 必须零变动）</fails_when>
  </verify>
  <acceptance_criteria>
    - interface_debug.lua 蓝绿双 arm 字面量与协议 hue 一致（blue 臂蓝主导、green 臂绿主导），OFFICER 键引用 0，契约注释在位
    - SAY/YELL/SYSTEM 三臂与 AddMessage 出口行原文保留（diff 仅两臂 + 注释块）
    - bbcheck BALANCED、令牌门 0、CRLF 0、新注释零 CJK、SM_Extend.lua == HEAD
  </acceptance_criteria>
  <done>G-29-3 root_cause 修复落地：interface_debug.lua 的 blue/green 两臂色相与 HUMAN-UAT §247-248 协议一致（D-14），渲染层共享通路的 landed/inferred 观感归位。</done>
</task>

<task type="auto">
  <name>Task 2: Category T-02 渲染映射回归自测——真实 show() 五臂色相断言 + 收尾静态电池（missing 第 2 条）</name>
  <files>classes/druid/selftest.lua</files>
  <read_first>
    - classes/druid/selftest.lua 1816-1827（T-01 现状 + Category T 两处计数注释）
    - classes/druid/selftest.lua 1054-1105（Q-11 CR-01 夹具纪律模板：快照/种植/pcall/恢复后断言）
    - core/selftest.lua 49-98（run() 失败透传语义：isOptional=true → yellow warning）
    - interface_debug.lua 85-100（任务 1 修复后的 show 映射——本任务夹具断言必须与之一致）
  </read_first>
  <action>语言工具可行性结论（已评估，抄录至 SUMMARY）：本机无 Lua 解释器（仅 node），色相行为断言无法在本机执行；已有工具链（bbcheck.balanced、Lua 5.0 令牌门、字面量 grep 门、CRLF/CJK 门）可静态验证语法与断言在位，行为级验证落在游戏内 /mt（本任务交付的 T-02）与用户下一次实机重建。

    1. 在 T-01（:1822-1825）之后、":1827 计数注释"之前插入新注册（isOptional=true、CR-01 快照/恢复纪律、全英文注释与断言）。被测对象是**真实 macroTorch.show**——区别于 Q 系列把 show 整体 stub 掉的写法，本测试只种植 show 的两个下游依赖（DEFAULT_CHAT_FRAME 与 ChatTypeInfo），映射表本体照真执行：

       注册名逐字（恰 1 处）：`"Cat T-02: show() color names resolve to matching hue-dominant channels"`

       夹具六步（全部在注册闭包内，局部队列 captured 承接捕获）：
       a. 快照两个下游依赖：`local savedDF = DEFAULT_CHAT_FRAME`、`local savedCTI = ChatTypeInfo`。
       b. 种植捕获桩：`DEFAULT_CHAT_FRAME = { AddMessage = function(self, msg, r, g, b, id) table.insert(captured, { r = r, g = g, b = b, id = id }) end }`。
       c. 种植 ChatTypeInfo（拟态 vanilla 默认、逐键 id 独一）：SAY `{ r = 0.5, g = 0.5, b = 0.5, id = 'planted_say' }`；YELL `{ r = 1, g = 0.5, b = 0, id = 'planted_yell' }`；SYSTEM `{ r = 1, g = 1, b = 0, id = 'planted_system' }`；OFFICER `{ r = 0.25, g = 1, b = 0.25, id = 'planted_officer' }`；GUILD `{ r = 0.25, g = 1, b = 0.25, id = 'planted_guild' }`（后两键为未来修复变体兼容预留，色相模拟 vanilla）。
       d. pcall 内顺序调用五臂并序标捕获：`macroTorch.show('probe', nil)`（缺省臂）；`macroTorch.show('probe', 'red')`；`macroTorch.show('probe', 'yellow')`；`macroTorch.show('probe', 'blue')`；`macroTorch.show('probe', 'green')`。
       e. 恢复（断言之前，CR-01——失败断言绝不污染会话）：`DEFAULT_CHAT_FRAME = savedDF`、`ChatTypeInfo = savedCTI`。
       f. 恢复后断言七条：pcallRes 非假；缺省臂 id == 'planted_say'；red 臂 r &gt;= g 且 r &gt;= b（断言报文含 "red-dominant"）；yellow 臂 r &gt;= b 且 g &gt;= b（报文含 "must stay warm"）；blue 臂 b &gt;= r 且 b &gt;= g（报文含 "blue-dominant"）；green 臂 g &gt;= r 且 g &gt;= b（报文含 "green-dominant"）。

       断言即协议：任务 1 修复后的 blue={0,0.5,0.9} 与 green={0,1,0} 全绿通过；若未来任臂被改回颠倒（green 渲蓝或 blue 渲绿）T-02 必红/黄。夹具禁止 `#` 长度运算符与 goto/`::`（Lua 5.0）。
    2. 更新 Category T 两处计数注释：":1817 段描述行"改为 `-- Category T: macroTorch.log persistence buffer cap (quick 260907-vve) + show() render-hue mapping (29-04 gap closure G-29-3, 2 tests)`；":1827 计数行"改为 `-- Registration count: Category T adds 2 tests (quick 260907-vve + 29-04 gap closure G-29-3)`。终态 `SelfTest:register("Cat T-` 恰 2 处（T-01 + T-02）。
    3. 收尾静态电池（两文件联合，命令同 29-03）：bbcheck interface_debug.lua + classes/druid/selftest.lua 全 BALANCED；Lua 5.0 令牌门两文件零命中；CRLF 两文件零命中；diff 新增注释行零 CJK；SM_Extend.lua 零 diff。
    4. 明确不改：Category Q 16 条断言零改动；core/ 零改动；spell_trace_core.lua 调用点零改动；不运行 build.sh。
  </action>
  <verify>
    <automated>node /home/admin/workspace/macro-torch/.planning/phases/27-catatk-event-driven-land-tracing-refactor/tools/bbcheck.js /home/admin/workspace/macro-torch/interface_debug.lua /home/admin/workspace/macro-torch/classes/druid/selftest.lua && git -C /home/admin/workspace/macro-torch diff --check</automated>
    <fails_when>bbcheck 任一文件输出 MISMATCH；或 git diff --check 打印任何行</fails_when>
    <automated>grep -c '"Cat T-02: show() color names resolve to matching hue-dominant channels"' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c 'SelfTest:register("Cat T-' /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>第一条 != 1（注册名恰 1 处）；第二条 != 2（Category T 恰 T-01 + T-02）</fails_when>
    <automated>grep -c 'planted_say' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c 'blue-dominant' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c 'green-dominant' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c 'red-dominant' /home/admin/workspace/macro-torch/classes/druid/selftest.lua && grep -c 'Category T adds 2 tests' /home/admin/workspace/macro-torch/classes/druid/selftest.lua</automated>
    <fails_when>第一条 != 2（种植 1 + 断言 1）；第二/三/四条任一 != 1（三臂主导断言报文各恰 1 处）；第五条 != 2（两处计数注释均更新）</fails_when>
    <automated>grep -cn '#\|goto \|::' /home/admin/workspace/macro-torch/classes/druid/selftest.lua /home/admin/workspace/macro-torch/interface_debug.lua; grep -rl $'\r' /home/admin/workspace/macro-torch/classes/druid/selftest.lua /home/admin/workspace/macro-torch/interface_debug.lua; echo "T2_TOKEN_CRLF_DONE"</automated>
    <fails_when>T2_TOKEN_CRLF_DONE 之前任一计数 > 0（Lua 5.0 令牌门双文件违约）；或出现任何文件路径行（CRLF 行尾违约）；空输出后仅回显哨兵行 = 通过</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff -U0 HEAD -- interface_debug.lua classes/druid/selftest.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep '^[+][[:space:]]*--' | grep -c '[一-龥]'; echo "T2_COMMENT_CJK_DONE"</automated>
    <fails_when>T2_COMMENT_CJK_DONE 之前输出 > 0（diff 新增注释行含 CJK 字符——英文注释约定违约；无匹配输出 0 且 grep 返回码 1 属预期）</fails_when>
    <automated>git -C /home/admin/workspace/macro-torch diff -U0 HEAD -- interface_debug.lua classes/druid/selftest.lua | grep '^[+]' | grep -v '^[+][+][+]' | grep '^[+][[:space:]]*--' | grep -cv '[a-z]'; echo "T2_COMMENT_WORD_DONE"; git -C /home/admin/workspace/macro-torch status --porcelain -- SM_Extend.lua</automated>
    <fails_when>T2_COMMENT_WORD_DONE 之前输出 > 0（存在不含 [a-z] 词元的新增注释行——应为英文措辞；无匹配输出 0 且 grep 返回码 1 属预期）；git status 输出任何行（SM_Extend.lua 必须零变动）</fails_when>
  </verify>
  <acceptance_criteria>
    - T-02 注册在位、走真实 show()、五臂全断言（含 blue-dominant/green-dominant/red-dominant 报文锚），颠倒即红/黄
    - Category T 计数注释两处更新为 2；Cat T- 注册恰 2 处；Q 系列 16 条零改动
    - bbcheck 两文件 + diff --check 绿；令牌门/CRLF/CJK/word 门全绿；SM_Extend.lua == HEAD
  </acceptance_criteria>
  <done>G-29-3 missing 第 2 条落地：T-02 以真实映射表为被测对象锁死五臂色相，Category T 增为 2 条——渲染映射层零覆盖的历史终态闭环。</done>
</task>

</tasks>

## Artifacts this plan produces（29-04 gap closure）

- `interface_debug.lua` — macroTorch.show 蓝绿双 arm 字面量修正 + 契约注释（D-14；G-29-3 root_cause 直接修复）
- `classes/druid/selftest.lua` — Category T-02 渲染映射回归自测 + 两处计数注释更新
- 无新生产逻辑；无文档协议改动（HUMAN-UAT §247-248 已与修复一致）；SM_Extend.lua 待用户 Windows+Cygwin 重建后带修复
- 用户侧复验（下次 UAT）：rebuild 后单人木桩观察 rake/bite landed 行绿、pounce/rip (inferred) 行蓝、无红——由 verify-work 会话汇入 29-UAT.md

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试夹具 → 生产渲染通路 | T-02 种植 DEFAULT_CHAT_FRAME 与 ChatTypeInfo 后驱动真实 show()；种植面严格限定两个下游依赖，CR-01 恢复先于断言 |
| 共享渲染通路 → 全体通告消费者 | macroTorch.show 双臂改动经唯一出口影响 spell_trace_core 通告、macroTorch.log、diag、Target 的观感 |
| 源码 → SM_Extend.lua（build 产物） | 产物不触碰（字节一致性门每任务执行） |

## STRIDE Threat Register（ASVS Level 1；block-on-high 阈）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-12 | Tampering（会话污染） | T-02 对两个全局（DEFAULT_CHAT_FRAME/ChatTypeInfo）的临时种植 | low | mitigate | CR-01 快照/恢复纪律（恢复先于断言，与 R-01/P-06 同律）；pcall 隔离；同步代码无 yield 窗口 |
| T-29-13 | Spoofing（观感回归） | interface_debug.lua 双臂改动与未来误改回颠倒 | low | mitigate | diff 严格限两臂+注释；静态字面量门（R/G/B 值 + OFFICER 清零）；T-02 五臂色相行为断言 |
| T-29-14 | Information disclosure（构建完整性） | SM_Extend.lua build 产物 | medium | mitigate | 两任务均含 status --porcelain 零输出门；修复经用户侧 rebuild 生效 |

无 high 级威胁——无需阻断任务。
</threat_model>

<verification>
- 两任务 verify 全绿即 G-29-3 机器侧闭环；行为级确认走 T-02（游戏内 /mt）+ 用户下次实机重建观察（27 重建/横幅 → UAT §3 单人木桩绿蓝归位），由 verify-work 会话将 UAT 更新回写 29-UAT.md
- 与 29-01/29-02/29-03 已执行的 Category Q 16 条互不覆盖：Q 断言标签字符串（受害方正确）、T-02 断言渲染色相（缺陷方），两侧合围恰好覆盖 G-29-3 全链路
</verification>

<success_criteria>interface_debug.lua 蓝绿双 arm 与协议 hue 一致（D-14）；OFFICER 键引用清零；T-02 以真实 show() 断五臂、Category T = 2；bbcheck/令牌门/CRLF/CJK/word 门全绿；Category Q 与 core/ 零扰动；SM_Extend.lua 全程等于 HEAD；变更面恰 2 个预期文件。
</success_criteria>

<output>Create `.planning/phases/29-landing/29-04-SUMMARY.md` when done
</output>