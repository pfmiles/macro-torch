---
phase: 08-druid-druid
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - build_order.txt
  - classes/Hunter.lua
  - classes/Warrior.lua
  - classes/Rogue.lua
  - classes/Mage.lua
  - classes/Priest.lua
  - classes/Warlock.lua
autonomous: true
requirements:
  - REQ-08-BUILD
  - REQ-08-NO-FLAT

must_haves:
  truths:
    - "D-07: All 6 old flat files replaced by subdirectory paths (classes/hunter/, classes/warrior/, etc.) — directory depth matches classes/druid/"
    - "build_order.txt lists all new subdirectory paths and no old flat paths"
    - "build.sh succeeds with new file paths"
    - "All 6 old flat classes/Xxx.lua files are deleted"
    - "Old flat file paths replaced by subdirectory paths in build_order.txt"
  artifacts:
    - path: "build_order.txt"
      provides: "Updated build order with classes/hunter/, classes/warrior/, classes/rogue/, classes/mage/, classes/priest/, classes/warlock/ paths"
    - path: "classes/Hunter.lua"
      provides: "DELETED — content migrated to classes/hunter/"
    - path: "classes/Warrior.lua"
      provides: "DELETED — content migrated to classes/warrior/"
    - path: "classes/Rogue.lua"
      provides: "DELETED — content migrated to classes/rogue/"
    - path: "classes/Mage.lua"
      provides: "DELETED — content migrated to classes/mage/"
    - path: "classes/Priest.lua"
      provides: "DELETED — content migrated to classes/priest/"
    - path: "classes/Warlock.lua"
      provides: "DELETED — content migrated to classes/warlock/"
  key_links:
    - from: "build_order.txt"
      to: "classes/hunter/Hunter.lua etc."
      via: "build.sh reads build_order.txt line by line"
      pattern: "classes/(hunter|warrior|rogue|mage|priest|warlock)/"
---

<objective>
Final integration step: update build_order.txt to replace 6 old flat class file paths with new subdirectory paths, then delete the old flat files. Also run build.sh to verify the build succeeds with the new structure.

Purpose: Complete the build system transition so that build.sh produces valid SM_Extend.lua from the new directory structure. This is the integration gate for Phase 8.
Output: Updated build_order.txt, 6 deleted files, verified build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-druid-druid/08-CONTEXT.md
@.planning/phases/08-druid-druid/08-RESEARCH.md
@build_order.txt
@build.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update build_order.txt with new subdirectory paths</name>
  <files>build_order.txt</files>
  <read_first>
Read build_order.txt (current state with 6 flat paths: classes/Hunter.lua through classes/Warrior.lua).
Read .planning/phases/08-druid-druid/08-RESEARCH.md lines 717-757 (build_order.txt Transition section — exact expected after state).
  </read_first>
  <action>
Update build_order.txt: replace the 6 old flat class paths (lines 31-36) with new subdirectory paths. The replacement block should look exactly like this:

Replace these 6 lines:
```
classes/Hunter.lua
classes/Mage.lua
classes/Priest.lua
classes/Rogue.lua
classes/Warlock.lua
classes/Warrior.lua
```

With these lines:
```
# classes/ — Hunter (Phase 8)
classes/hunter/Hunter.lua
classes/hunter/combat.lua
classes/hunter/utility.lua
# classes/ — Warrior (Phase 8)
classes/warrior/Warrior.lua
classes/warrior/combat.lua
classes/warrior/utility.lua
# classes/ — Rogue (Phase 8)
classes/rogue/Rogue.lua
classes/rogue/combat.lua
# classes/ — Mage (Phase 8)
classes/mage/Mage.lua
classes/mage/combat.lua
# classes/ — Priest (Phase 8)
classes/priest/Priest.lua
classes/priest/combat.lua
classes/priest/utility.lua
# classes/ — Warlock (Phase 8)
classes/warlock/Warlock.lua
classes/warlock/combat.lua
```

IMPORTANT — ordering rules:
- Druid files (lines 27-30) remain unchanged and come FIRST among classes/
- Within each class directory: <Class>.lua comes FIRST, then combat.lua, then utility.lua (if exists)
- The order of directories (Hunter, Warrior, Rogue, Mage, Priest, Warlock) follows the same order as the old flat files
- Each section has a `# classes/ — <Class> (Phase 8)` comment header

The final build_order.txt should have exactly 37 total lines (up from 36) — adding 13 new subdirectory paths, removing 6 old flat paths, adding 6 comment lines.

Verify the count: original has 36 lines. Remove 6 old paths = 30. Add 6 comment lines + 13 file paths = 49. Wait — let me recount.

Original build_order.txt:
- Lines 1-3: macro_torch.lua, impl_util.lua, biz_util.lua
- Line 4: comment
- Lines 5-6: core/class.lua, core/periodic.lua
- Line 7: comment
- Lines 8-16: entity/ files (9 files)
- Line 17: texture_map.lua
- Line 18: interface_debug.lua
- Line 19: comment
- Lines 20-22: core/ combat files (3 files)
- Line 23: comment
- Line 24: core/selftest.lua
- Line 25: core/events.lua
- Line 26: comment
- Lines 27-30: classes/druid/ (4 files)
- Lines 31-36: classes/Xxx.lua (6 files)

Total: 36 lines (including comments and blank-like lines).

Remove lines 31-36 (6 lines). Add: 6 comment lines + 13 file paths = 19 lines.
New total: 30 + 19 = 49 lines.

Actually, let me check if there are blank lines I'm missing. Looking at the build_order.txt content: there are no blank lines between entries, just comment lines with `#`. The file has exactly 37 lines (including the empty line 37 at end).

After edit: remove 6 old flat paths, add 6 comments + 13 file paths = +13 net. New total: 37 + 13 = 50 lines.
  </action>
  <verify>
    <automated>grep -c "classes/hunter/Hunter.lua\|classes/hunter/combat.lua\|classes/hunter/utility.lua\|classes/warrior/Warrior.lua\|classes/warrior/combat.lua\|classes/warrior/utility.lua\|classes/rogue/Rogue.lua\|classes/rogue/combat.lua\|classes/mage/Mage.lua\|classes/mage/combat.lua\|classes/priest/Priest.lua\|classes/priest/combat.lua\|classes/priest/utility.lua\|classes/warlock/Warlock.lua\|classes/warlock/combat.lua" /Users/yue.weny/finalanswer/macro-torch/macro-torch/build_order.txt</automated>
  </verify>
  <acceptance_criteria>
- build_order.txt lists exactly 13 new subdirectory paths across 6 class directories
- build_order.txt does NOT contain any of these old flat paths: classes/Hunter.lua, classes/Warrior.lua, classes/Rogue.lua, classes/Mage.lua, classes/Priest.lua, classes/Warlock.lua
- Druid paths (classes/druid/Druid.lua, cat.lua, bear.lua, utility.lua) remain unchanged
- Each class section has a `# classes/ — <Class> (Phase 8)` comment header
- Within each directory, <Class>.lua is listed first, then combat.lua, then utility.lua (if exists)
- grep -v '^#' confirms only file paths (no inline comments on path lines), count = 13 new paths
  </acceptance_criteria>
  <done>build_order.txt updated with all new subdirectory paths, old flat paths removed, build order preserved</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete old flat class files and verify build</name>
  <files>classes/Hunter.lua, classes/Warrior.lua, classes/Rogue.lua, classes/Mage.lua, classes/Priest.lua, classes/Warlock.lua</files>
  <read_first>
Read build_order.txt (verify the new paths are in place from Task 1).
Read build.sh (to understand how it reads build_order.txt — strict mode: file not found = error exit).
  </read_first>
  <action>
Delete the 6 old flat class files since their content has been migrated to subdirectories (Plans 01-03):

```bash
rm classes/Hunter.lua
rm classes/Warrior.lua
rm classes/Rogue.lua
rm classes/Mage.lua
rm classes/Priest.lua
rm classes/Warlock.lua
```

After deletion, run the build to verify the new structure works:

```bash
./build.sh && echo "Build OK"
```

If build.sh fails with "ERROR: File not found", investigate which path is missing in build_order.txt — it means a subdirectory file from Plans 01-03 hasn't been created yet.

IMPORTANT: build.sh is in strict mode (Phase 4). Any missing file in build_order.txt will cause a fatal error. This task MUST run after Plans 01-03 have created all 13+ files.

After successful build, verify the build output contains the new class structures:

```bash
grep -c "macroTorch.Hunter = macroTorch.Player:new()" SM_Extend.lua  # expect: 1
grep -c "macroTorch.Warrior = macroTorch.Player:new()" SM_Extend.lua  # expect: 1
grep -c "macroTorch.Rogue = macroTorch.Player:new()" SM_Extend.lua  # expect: 1
grep -c "macroTorch.Mage = macroTorch.Player:new()" SM_Extend.lua  # expect: 1
grep -c "macroTorch.Priest = macroTorch.Player:new()" SM_Extend.lua  # expect: 1
grep -c "macroTorch.Warlock = macroTorch.Player:new()" SM_Extend.lua  # expect: 1
grep -c "registerPlayerClass" SM_Extend.lua  # expect: >= 7 (Druid + 6 new)
```

Note: SM_Extend.lua is the build output — it should contain all 6 new class prototypes, all skill methods, all combat functions, and all utility functions concatenated in build_order.txt order.
  </action>
  <verify>
    <automated>test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/Hunter.lua && test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/Warrior.lua && test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/Rogue.lua && test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/Mage.lua && test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/Priest.lua && test ! -f /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/Warlock.lua && echo "ALL_DELETED"</automated>
  </verify>
  <acceptance_criteria>
- All 6 old flat class files are deleted: classes/Hunter.lua, classes/Warrior.lua, classes/Rogue.lua, classes/Mage.lua, classes/Priest.lua, classes/Warlock.lua
- `./build.sh` completes successfully (exit code 0)
- `echo $?` after build.sh is 0
- SM_Extend.lua contains all 6 new class prototypes (macroTorch.Xxx = macroTorch.Player:new())
- SM_Extend.lua contains 7+ registerPlayerClass calls (Druid + 6 new classes)
- No "ERROR: File not found" output from build.sh
  </acceptance_criteria>
  <done>All 6 old flat files deleted, build.sh succeeds with new directory structure, SM_Extend.lua contains all 7 class registrations</done>
</task>

</tasks>

<threat_model>
## Threat Model — No new threats

This is a build system update and file cleanup task within an existing WoW 1.12.1 addon. No new attack surfaces:
- No code changes — only build configuration (build_order.txt) and file deletion
- The build.sh strict mode (Phase 4) ensures no missing files — this is a safety feature, not a threat

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | — | build_order.txt, build.sh | accept | Build configuration update only; strict mode catches missing files |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase |
</threat_model>

<verification>
```bash
# Old flat files deleted
test ! -f classes/Hunter.lua && echo "Hunter.lua deleted"
test ! -f classes/Warrior.lua && echo "Warrior.lua deleted"
test ! -f classes/Rogue.lua && echo "Rogue.lua deleted"
test ! -f classes/Mage.lua && echo "Mage.lua deleted"
test ! -f classes/Priest.lua && echo "Priest.lua deleted"
test ! -f classes/Warlock.lua && echo "Warlock.lua deleted"

# build_order.txt integrity
grep -v '^#' build_order.txt | grep -c "classes/"  # expect: 21 (4 druid + 13 new + ... let me count)
# Druid: 4 files
# Hunter: 3 files
# Warrior: 3 files
# Rogue: 2 files
# Mage: 2 files
# Priest: 3 files
# Warlock: 2 files
# Total: 19 class files (non-comment lines)

# Build succeeds
./build.sh && echo "BUILD_OK"

# Build output contains all class prototypes
grep -c "macroTorch\.\(Hunter\|Warrior\|Rogue\|Mage\|Priest\|Warlock\) = macroTorch.Player:new()" SM_Extend.lua  # expect: 6

# All 7 class registrations (Druid + 6 new) in output
grep -c "registerPlayerClass" SM_Extend.lua  # expect: 7
```
</verification>

<success_criteria>
- All 6 old flat class files deleted
- build_order.txt contains correct subdirectory paths for all 6 new class directories
- build.sh completes successfully (exit code 0)
- SM_Extend.lua build output contains all 6 new class prototypes and 7 registerPlayerClass registrations
- Druid files in classes/druid/ remain untouched
</success_criteria>

<output>
Create `.planning/phases/08-druid-druid/08-04-SUMMARY.md` when done
</output>