---
phase: 08-druid-druid
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/rogue/Rogue.lua
  - classes/rogue/combat.lua
  - classes/mage/Mage.lua
  - classes/mage/combat.lua
autonomous: false
requirements:
  - REQ-08-CLASS-DEF
  - REQ-08-SKILL-METHODS
  - REQ-08-SPELLTRACE
  - REQ-08-SELFTEST
  - REQ-08-INITPLAYER

must_haves:
  truths:
    - "Rogue has classMetatable + ROGUE_FIELD_FUNC_MAP + registerPlayerClass in classes/rogue/Rogue.lua"
    - "Rogue combat logic in classes/rogue/combat.lua uses skill methods not CastSpellByName"
    - "Mage has classMetatable + MAGE_FIELD_FUNC_MAP + registerPlayerClass in classes/mage/Mage.lua"
    - "Mage combat logic in classes/mage/combat.lua uses skill methods where applicable, preserves castIfBuffAbsent"
  artifacts:
    - path: "classes/rogue/Rogue.lua"
      provides: "Rogue class definition, ~8 skill methods, ROGUE_FIELD_FUNC_MAP (with comboPoints), singleton, registerPlayerClass, SpellTrace:register, SelfTest:register"
      min_lines: 150
    - path: "classes/rogue/combat.lua"
      provides: "rogueAtk, rogueAtkBack, rogueSneak, rogueSneakBack, rogueBattle, rogueBattleBack, pickPocketBeforeCast, isTargetRogueFaint, restoreIfNeeded, lockNearestEnemyThenCast, readyVanish — refactored to use skill methods"
      min_lines: 150
    - path: "classes/mage/Mage.lua"
      provides: "Mage class definition, ~3 skill methods, MAGE_FIELD_FUNC_MAP, singleton, registerPlayerClass, SpellTrace:register, SelfTest:register"
      min_lines: 120
    - path: "classes/mage/combat.lua"
      provides: "mageAtk, mageRangedAtk, mageMeleeAtk, mageBuffs, mageCtrl — refactored with skill methods where applicable, castIfBuffAbsent preserved"
      min_lines: 80
  key_links:
    - from: "classes/rogue/Rogue.lua"
      to: "core/class.lua"
      via: "macroTorch.classMetatable(self, 'ROGUE_FIELD_FUNC_MAP')"
      pattern: "classMetatable.*ROGUE_FIELD_FUNC_MAP"
    - from: "classes/rogue/combat.lua"
      to: "classes/rogue/Rogue.lua"
      via: "macroTorch.player.sinister_strike() etc."
      pattern: "player\\.[a-z_]+\\("
    - from: "classes/mage/Mage.lua"
      to: "core/class.lua"
      via: "macroTorch.classMetatable(self, 'MAGE_FIELD_FUNC_MAP')"
      pattern: "classMetatable.*MAGE_FIELD_FUNC_MAP"
---

<objective>
Refactor Rogue (2 files: default mode per D-01) and Mage (2 files: default mode, combat+utility combined into combat.lua) to align with the Druid architecture. Create class subdirectories, extract class definitions with skill methods, replace CastSpellByName with _castSpell-based skill methods, add SpellTrace:register and SelfTest:register, and split combat logic into separate files.

Purpose: Extend the Druid-aligned architecture pattern to two more non-Druid classes. Rogue requires special handling for Chinese skill names with English translations, and a pickpocket state machine that must be preserved. Mage requires minimal skill method creation (only 2 CastSpellByName sites) with castIfBuffAbsent pattern preservation.
Output: 4 new files in classes/rogue/ and classes/mage/ subdirectories.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-druid-druid/08-CONTEXT.md
@.planning/phases/08-druid-druid/08-RESEARCH.md
@classes/druid/Druid.lua
@classes/Rogue.lua
@classes/Mage.lua

<interfaces>
<!-- Same interfaces as Plan 08-01 — refer to Plan 08-01 for full interface details -->

From core/class.lua:
- `macroTorch.classMetatable(cls, fieldMapName)` — metatable factory
- `macroTorch.registerPlayerClass(className, classTable)` — polymorphic registration
- `macroTorch.PLAYER_CLASS_REGISTRY` — registry table

From entity/Player.lua:
- `obj._castSpell(localeNames, mode, range, resourceCost, onSelf)` — locale-aware spell casting
- `obj.cast(spellName, onSelf)` — legacy wrapper
- `obj.isSpellReady(spellName)` — cooldown check
- `macroTorch.castIfBuffAbsent(unit, spellName, texture)` — conditional buff cast (PRESERVE for Mage)

From core/spell_trace_core.lua:
- `macroTorch.SpellTrace:register(name, config)` — declarative spell trace

From core/selftest.lua:
- `macroTorch.SelfTest:register(name, fn, isOptional)` — self-test framework

Rogue-specific notes:
- ROGUE_FIELD_FUNC_MAP needs comboPoints field (Rogue uses GetComboPoints() like Druid)
- Chinese skill names need English translations per RESEARCH.md Rogue table
- pickPocketBeforeCast uses a state machine (pickPocketState = 0/1) — preserve this logic
- lockNearestEnemyThenCast is a generic utility — preserve as-is, replace internal CastSpellByName

Mage-specific notes:
- Only 2 CastSpellByName('Frostbolt') call sites to convert
- castIfBuffAbsent calls for Frost Armor, Arcane Intellect — PRESERVE (per CONTEXT.md)
- Mage has pet management (PetDefensiveMode, PetAttack) — keep as-is
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Rogue class definition and combat files</name>
  <files>classes/rogue/Rogue.lua, classes/rogue/combat.lua</files>
  <read_first>
Read classes/Rogue.lua (extract ALL functions: isTargetRogueFaint, pickPocketBeforeCast, rogueSneak, restoreIfNeeded, rogueBattle, rogueAtk, rogueSneakBack, rogueBattleBack, rogueAtkBack, lockNearestEnemyThenCast, readyVanish).
Read classes/druid/Druid.lua lines 1-200 and lines 434-470 (Druid constructor pattern with skill methods, FIELD_FUNC_MAP pattern — replicate for Rogue; note comboPoints field definition).
Read entity/Player.lua lines 1-90 (_castSpell signature).
Read core/spell_trace_core.lua (SpellTrace:register API).
Read core/selftest.lua (SelfTest:register API).
  </read_first>
  <action>
Create classes/rogue/ directory, then create both files.

=== classes/rogue/Rogue.lua ===
1. Apache 2.0 license block
2. Comment: `---盗贼专用 start---`
3. Class prototype: `macroTorch.Rogue = macroTorch.Player:new()`
4. Constructor with skill methods. Rogue uses CHINESE names in original CastSpellByName — skill methods must use locale tables with ENGLISH names per RESEARCH.md A1 (need human verification on Turtle WoW English client). Include these skills:

Type A (enemy target, onSelf=false):
- pick_pocket: {en='Pick Pocket', zh='偷窃'}, mode, nil, nil, false — [ASSUMED] English name per RESEARCH.md
- ghostly_strike: {en='Ghostly Strike', zh='鬼魅攻击'}, mode, nil, nil, false — [ASSUMED]
- hemorrhage: {en='Hemorrhage', zh='出血'}, mode, nil, nil, false — [ASSUMED]
- sinister_strike: {en='Sinister Strike', zh='邪恶攻击'}, mode, nil, nil, false — [ASSUMED]
- backstab: {en='Backstab', zh='背刺'}, mode, nil, nil, false — [ASSUMED]

Type B (self target, onSelf=true):
- vanish: {en='Vanish', zh='消失'}, mode, nil, nil, true — [ASSUMED]
- preparation: {en='Preparation', zh='伺机待发'}, mode, nil, nil, true — [ASSUMED]

Return obj at end of constructor.

5. FIELD_FUNC_MAP with comboPoints (Rogue uses combo points like Druid):
```lua
macroTorch.ROGUE_FIELD_FUNC_MAP = {
    ['comboPoints'] = function(self)
        return GetComboPoints() or 0
    end,
}
```

6. Singleton: `macroTorch.rogue = macroTorch.Rogue:new()`
7. Registration: `macroTorch.registerPlayerClass("Rogue", macroTorch.Rogue)` — per D-03
8. SpellTrace registration: Rogue has NO spell trace in original code. Add placeholder comment: `-- SpellTrace: no Rogue spells currently traced`
9. SelfTest registrations (all isOptional=true, UnitClass('player') ~= 'Rogue' guard):
   - "Rogue: ROGUE_FIELD_FUNC_MAP is table" — assert type check
   - "Rogue: comboPoints field exists" — assert type(macroTorch.rogue.comboPoints) == "number"
   - "Rogue: singleton rogue exists" — assert type(macroTorch.rogue) == "table"
   - "Rogue: registered in PLAYER_CLASS_REGISTRY" — assert registry entry
   - For each of the 7 skill methods: "Rogue: skill method X exists" — assert type check
   - Total: 3 infra + 1 comboPoints + 7 skills = 11 SelfTest registrations

10. IMPORTANT — at END of file, add a comment noting English name assumptions:
```
-- Rogue locale table English names are [ASSUMED] and need verification on Turtle WoW English client:
-- Pick Pocket, Ghostly Strike, Hemorrhage, Sinister Strike, Backstab, Vanish, Preparation
-- User should verify these match the actual spell names on an English WoW 1.12.1 client
```

=== classes/rogue/combat.lua ===
1. Apache 2.0 license block
2. Comment: `-- Rogue combat rotation functions`
3. Migrate isTargetRogueFaint() (lines 19-28): keep as-is (uses debuff texture strings, no spell casting)
4. Migrate pickPocketBeforeCast(spell) (lines 32-46): PRESERVE the state machine logic (pickPocketState = 0/1). Replace CastSpellByName:
   - CastSpellByName("偷窃") (line 39) → local function call is tricky here because this function takes a spell name parameter and calls CastSpellByName on it. The function signature is pickPocketBeforeCast(spell) where spell is a string.
   
   SOLUTION: Keep pickPocketBeforeCast using CastSpellByName for the generic case since it receives arbitrary spell strings. Replace only the hardcoded "偷窃" call:
   - Line 39: CastSpellByName("偷窃") → macroTorch.player.pick_pocket()
   - Lines 36, 42: CastSpellByName(spell) — since 'spell' is a variable parameter (could be any opener), convert the function to receive a skill method function reference instead of a string. BUT this would change the function signature and break callers.

   BETTER SOLUTION: Keep pickPocketBeforeCast as-is with CastSpellByName for now. It's a state machine utility that takes variable spell names. Per CONTEXT.md: "pickPocketBeforeCast() 使用了状态机，迁移时保持该逻辑". Add a comment noting this is a deferred migration: `-- pickPocketBeforeCast: state machine with variable spell names, deferred CastSpellByName migration`
   
   Actually, re-reading the code more carefully: the function takes a `spell` parameter which is the opener spell. The only CastSpellByName calls are: (1) hardcoded "偷窃" for pickpocket, and (2) CastSpellByName(spell) for the provided opener. The caller always passes CastSpellByName-compatible spell name strings. 
   
   PRACTICAL SOLUTION: Replace only the hardcoded "偷窃" with macroTorch.player.pick_pocket(). For CastSpellByName(spell) — these remain as-is since 'spell' is a variable parameter. Document with comment.

5. Migrate rogueSneak(startSp) (lines 48-50): calls pickPocketBeforeCast — keep as-is
6. Migrate restoreIfNeeded() (lines 53-59): keep as-is (item usage, no spell casting)
7. Migrate rogueBattle() (lines 61-68):
   - CastSpellByName('鬼魅攻击') → player.ghostly_strike()
   - CastSpellByName('出血') → player.hemorrhage()
   - CastSpellByName('邪恶攻击') → player.sinister_strike()
   - macroTorch.startAutoAtk() — keep as-is
8. Migrate rogueAtk(startSp) (lines 72-91): keep logic, calls rogueSneak/rogueBattle — unchanged
9. Migrate rogueSneakBack(startSp) (lines 93-95): calls pickPocketBeforeCast — keep as-is
10. Migrate rogueBattleBack() (lines 97-100):
    - CastSpellByName('背刺') → player.backstab()
    - macroTorch.startAutoAtk() — keep as-is
11. Migrate rogueAtkBack(startSp) (lines 104-123): keep logic, calls rogueSneakBack/rogueBattleBack — unchanged
12. Migrate lockNearestEnemyThenCast(sp) (lines 127-137):
    - This generic utility takes a spell name string parameter. CastSpellByName(sp) on lines 130 and 134.
    - Keep as-is for now — it's a generic utility function taking variable spell names. Add comment: `-- lockNearestEnemyThenCast: generic utility with variable spell names, deferred CastSpellByName migration per CONTEXT.md`
13. Migrate readyVanish() (lines 140-150):
    - CastSpellByName(s) where s = '消失' (line 144) → player.vanish()
    - CastSpellByName('伺机待发') → player.preparation()
    - CastSpellByName(s) — Since 's' is '消失', replace the entire logic:
      * Replace: `local s = '消失'; if isActionCooledDown(...) then CastSpellByName(s) else CastSpellByName('伺机待发'); CastSpellByName(s) end`
      * With: `if macroTorch.isActionCooledDown('Ability_Vanish') then player.vanish() else player.preparation(); player.vanish() end`

In combat.lua, add `local player = macroTorch.player` at the start of each function that calls skill methods (rogueBattle, rogueBattleBack, readyVanish).
  </action>
  <verify>
    <automated>grep -c "classMetatable.*ROGUE_FIELD_FUNC_MAP" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/rogue/Rogue.lua && grep -c "registerPlayerClass.*Rogue" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/rogue/Rogue.lua && grep -c "SelfTest:register" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/rogue/Rogue.lua</automated>
  </verify>
  <acceptance_criteria>
- classes/rogue/Rogue.lua exists with full Druid-aligned class definition
- 7 skill methods: pick_pocket, ghostly_strike, hemorrhage, sinister_strike, backstab, vanish, preparation — all with locale tables {en, zh}
- ROGUE_FIELD_FUNC_MAP contains comboPoints (identical to Druid: GetComboPoints() or 0)
- registerPlayerClass("Rogue", ...) present — per D-03
- SelfTest:register count >= 11 (3 infra + 1 comboPoints + 7 skills)
- English name assumption comment at end of Rogue.lua
- SpellTrace section present (empty placeholder — no Rogue spells currently traced)
- classes/rogue/combat.lua: rogueBattle replaces CastSpellByName with ghostly_strike/hemorrhage/sinister_strike
- classes/rogue/combat.lua: rogueBattleBack replaces CastSpellByName('背刺') with player.backstab()
- classes/rogue/combat.lua: readyVanish replaces CastSpellByName with player.vanish()/player.preparation()
- classes/rogue/combat.lua: pickPocketBeforeCast preserves state machine, replaces hardcoded CastSpellByName("偷窃") with player.pick_pocket(), keeps variable CastSpellByName(spell)
- classes/rogue/combat.lua: lockNearestEnemyThenCast preserved as-is (generic utility with variable spell names)
- pickPocketState global variable preserved (macroTorch.pickPocketState)
- All dot syntax, all global namespace
  </acceptance_criteria>
  <done>Rogue class definition and combat files are complete with 7 skill methods, comboPoints in FIELD_FUNC_MAP, state machine preserved, CastSpellByName replaced where applicable</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Mage class definition and combat files</name>
  <files>classes/mage/Mage.lua, classes/mage/combat.lua</files>
  <read_first>
Read classes/Mage.lua (81 lines — extract all functions: mageRangedAtk, mageMeleeAtk, mageBuffs, mageAtk, mageCtrl).
Read classes/druid/Druid.lua lines 1-200 (constructor with skill methods pattern — replicate for Mage).
Read entity/Player.lua lines 1-90 (_castSpell signature).
Read biz_util.lua (castIfBuffAbsent definition — understand the function signature to know when to preserve it).
  </read_first>
  <action>
Create classes/mage/ directory, then create both files.

=== classes/mage/Mage.lua ===
1. Apache 2.0 license block
2. Comment: `---法师专用 start---`
3. Class prototype: `macroTorch.Mage = macroTorch.Player:new()`
4. Constructor with skill methods. Mage has only 2 direct CastSpellByName calls — create skill methods for those plus methods for castIfBuffAbsent-wrapped spells (for future migration). Per RESEARCH.md skill method table:

Type A (enemy target, onSelf=false):
- frostbolt: {en='Frostbolt', zh='寒冰箭'}, mode, 30, nil, false

Type B (self target, onSelf=true):
- frost_armor: {en='Frost Armor', zh='冰甲术'}, mode, nil, nil, true

Type C (flexible target, used by castIfBuffAbsent on friendly or self):
- arcane_intellect: {en='Arcane Intellect', zh='奥术智慧'}, mode, nil, nil, false
  (castIfBuffAbsent casts on target if friendly, else self — the skill method is Type A targeting but the buff logic selects target)

Return obj at end of constructor.

5. FIELD_FUNC_MAP: `macroTorch.MAGE_FIELD_FUNC_MAP = {}` (empty — Mage has no class-specific lazy-computed fields)
6. Singleton: `macroTorch.mage = macroTorch.Mage:new()`
7. Registration: `macroTorch.registerPlayerClass("Mage", macroTorch.Mage)` — per D-03
8. SpellTrace registration: Mage has NO spell trace in original code. Add placeholder comment: `-- SpellTrace: no Mage spells currently traced`
9. SelfTest registrations (all isOptional=true, UnitClass('player') ~= 'Mage' guard):
   - "Mage: MAGE_FIELD_FUNC_MAP is table" — assert type check
   - "Mage: singleton mage exists" — assert type(macroTorch.mage) == "table"
   - "Mage: registered in PLAYER_CLASS_REGISTRY" — assert registry entry
   - For each of the 3 skill methods: "Mage: skill method X exists" — assert type check
   - Total: 3 infra + 3 skills = 6 SelfTest registrations

=== classes/mage/combat.lua ===
1. Apache 2.0 license block
2. Comment: `-- Mage combat rotation and utility functions`
3. Migrate mageRangedAtk() (lines 17-25):
   - CastSpellByName('Frostbolt') → player.frostbolt()
   - Keep PetDefensiveMode(), PetAttack(), macroTorch.startAutoAtk() as-is
   - Add `local player = macroTorch.player` at function start
4. Migrate mageMeleeAtk() (lines 28-37):
   - CastSpellByName('Frostbolt') → player.frostbolt()
   - Keep PetDefensiveMode(), PetAttack(), macroTorch.startAutoAtk() as-is
   - Add `local player = macroTorch.player` at function start
5. Migrate mageBuffs() (lines 39-48):
   - castIfBuffAbsent(p, 'Frost Armor', 'Frost_FrostArmor02') — PRESERVE (castIfBuffAbsent pattern, per CONTEXT.md)
   - castIfBuffAbsent(t, 'Arcane Intellect', 'Holy_MagicalSentry') — PRESERVE
   - Function structure stays the same
6. Migrate mageAtk() (lines 50-77): keep logic, calls mageBuffs/mageRangedAtk/mageMeleeAtk — unchanged
7. Migrate mageCtrl() (lines 79-81): empty function, keep as-is

All functions maintain global macroTorch.* namespace and dot syntax.
  </action>
  <verify>
    <automated>grep -c "classMetatable.*MAGE_FIELD_FUNC_MAP" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/mage/Mage.lua && grep -c "registerPlayerClass.*Mage" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/mage/Mage.lua</automated>
  </verify>
  <acceptance_criteria>
- classes/mage/Mage.lua exists with full Druid-aligned class definition
- 3 skill methods: frostbolt, frost_armor, arcane_intellect — all with locale tables
- MAGE_FIELD_FUNC_MAP is empty table
- registerPlayerClass("Mage", ...) present — per D-03
- SelfTest:register count >= 6 (3 infra + 3 skills)
- SpellTrace section present (empty placeholder)
- classes/mage/combat.lua: mageRangedAtk replaces CastSpellByName('Frostbolt') with player.frostbolt()
- classes/mage/combat.lua: mageMeleeAtk replaces CastSpellByName('Frostbolt') with player.frostbolt()
- classes/mage/combat.lua: mageBuffs preserves castIfBuffAbsent calls for Frost Armor and Arcane Intellect
- classes/mage/combat.lua: mageAtk and mageCtrl preserved unchanged
- All dot syntax, all global namespace
  </acceptance_criteria>
  <done>Mage class definition and combat files are complete with 3 skill methods, castIfBuffAbsent preserved, Frostbolt CastSpellByName replaced</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Rogue English skill names</name>
  <files>classes/rogue/Rogue.lua</files>
  <read_first>
Read classes/rogue/Rogue.lua (the file just created in Task 1 — review the locale tables for all 7 Rogue skill methods).
Read .planning/phases/08-druid-druid/08-RESEARCH.md lines 442-449 (Rogue Chinese-to-English name translations marked [ASSUMED]).
  </read_first>
  <action>
User must verify the 7 Rogue English skill names in classes/rogue/Rogue.lua against Turtle WoW 1.12.1 English client spell names. The assumed names are: Pick Pocket, Ghostly Strike, Hemorrhage, Sinister Strike, Backstab, Vanish, Preparation. User should confirm or correct each name.
  </action>
  <what-built>
Rogue class definition file (classes/rogue/Rogue.lua) with 7 skill methods using locale tables. The English names are [ASSUMED] based on RESEARCH.md and need verification.
  </what-built>
  <how-to-verify>
Review the following Rogue English skill names in classes/rogue/Rogue.lua and confirm they match the actual spell names on a Turtle WoW 1.12.1 English client:

1. pick_pocket: {en='Pick Pocket', zh='偷窃'}
2. ghostly_strike: {en='Ghostly Strike', zh='鬼魅攻击'}
3. hemorrhage: {en='Hemorrhage', zh='出血'}
4. sinister_strike: {en='Sinister Strike', zh='邪恶攻击'}
5. backstab: {en='Backstab', zh='背刺'}
6. vanish: {en='Vanish', zh='消失'}
7. preparation: {en='Preparation', zh='伺机待发'}

Instructions:
1. Open classes/rogue/Rogue.lua
2. Check each locale table en value against what the English WoW client expects for those spell names
3. If any name is incorrect, note the correct English name

If you don't have access to an English WoW client, you can approve these as-is — they are standard WoW spell names and likely correct.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual verification only: user must confirm English skill names on Turtle WoW English client</automated>
  </verify>
  <done>All 7 Rogue English skill names confirmed correct by user (or corrections applied)</done>
  <resume-signal>Type "approved" to confirm all English names are correct, or provide corrections for specific skills</resume-signal>
</task>

</tasks>

<threat_model>
## Threat Model — No new threats

This is a pure code structure refactoring within an existing WoW 1.12.1 addon sandbox. No new attack surfaces are introduced:
- No file I/O beyond existing addon Lua files
- No network communication
- No user input handling changes
- No dependency changes

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | — | classes/rogue/*.lua, classes/mage/*.lua | accept | No new threats: pure internal refactoring within WoW addon sandbox |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase |
</threat_model>

<verification>
```bash
# Rogue: class definition completeness
grep -c "macroTorch.Rogue = macroTorch.Player:new()" classes/rogue/Rogue.lua  # expect: 1
grep -c "classMetatable.*ROGUE_FIELD_FUNC_MAP" classes/rogue/Rogue.lua  # expect: 1
grep -c "registerPlayerClass.*Rogue" classes/rogue/Rogue.lua  # expect: 1

# Rogue: FIELD_FUNC_MAP has comboPoints
grep -c "comboPoints" classes/rogue/Rogue.lua  # expect: >= 2 (FIELD_FUNC_MAP entry + SelfTest)

# Rogue: skill method count (>= 7)
grep -c "function obj\.[a-z_]*(" classes/rogue/Rogue.lua  # expect: >= 7

# Rogue: combat file CastSpellByName for spells replaced
# rogueBattle should have ghostly_strike/hemorrhage/sinister_strike
grep -c "player\.ghostly_strike\|player\.hemorrhage\|player\.sinister_strike" classes/rogue/combat.lua  # expect: >= 3

# Rogue: combat file preserves pickPocketBeforeCast state machine
grep -c "pickPocketState" classes/rogue/combat.lua  # expect: >= 2

# Mage: class definition completeness
grep -c "macroTorch.Mage = macroTorch.Player:new()" classes/mage/Mage.lua  # expect: 1
grep -c "classMetatable.*MAGE_FIELD_FUNC_MAP" classes/mage/Mage.lua  # expect: 1
grep -c "registerPlayerClass.*Mage" classes/mage/Mage.lua  # expect: 1

# Mage: Frostbolt CastSpellByName replaced
grep -c "player\.frostbolt" classes/mage/combat.lua  # expect: >= 2
grep -c "CastSpellByName.*Frostbolt" classes/mage/combat.lua  # expect: 0

# Mage: castIfBuffAbsent preserved
grep -c "castIfBuffAbsent" classes/mage/combat.lua  # expect: >= 2
```
</verification>

<success_criteria>
- Rogue class definition file (classes/rogue/Rogue.lua) exists with Druid-aligned structure
- Rogue combat file (classes/rogue/combat.lua) replaces CastSpellByName with skill method calls where applicable
- Rogue pickPocketBeforeCast state machine preserved
- Mage class definition file (classes/mage/Mage.lua) exists with Druid-aligned structure
- Mage combat file (classes/mage/combat.lua) replaces CastSpellByName('Frostbolt') with player.frostbolt()
- Mage castIfBuffAbsent calls preserved for Frost Armor and Arcane Intellect
- All files use dot syntax exclusively, global macroTorch.* namespace
- Rogue English skill names verified by user (checkpoint)
</success_criteria>

<output>
Create `.planning/phases/08-druid-druid/08-02-SUMMARY.md` when done
</output>