---
phase: 08-druid-druid
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/priest/Priest.lua
  - classes/priest/combat.lua
  - classes/priest/utility.lua
  - classes/warlock/Warlock.lua
  - classes/warlock/combat.lua
autonomous: true
requirements:
  - REQ-08-CLASS-DEF
  - REQ-08-SKILL-METHODS
  - REQ-08-SPELLTRACE
  - REQ-08-SELFTEST
  - REQ-08-INITPLAYER

must_haves:
  truths:
    - "D-05, D-06: All Priest/Warlock skill methods use _castSpell with locale tables, Type A/B/C classification, resourceCost support — full alignment with Druid Phase 5-7 patterns"
    - "D-07: Priest/Warlock subdirectories created at classes/priest/ and classes/warlock/ with identical depth to classes/druid/"
    - "Priest has classMetatable + PRIEST_FIELD_FUNC_MAP + registerPlayerClass in classes/priest/Priest.lua"
    - "Priest combat logic in classes/priest/combat.lua uses skill methods not CastSpellByName"
    - "Priest utility functions (healing, buffs, debuffs, ctrl) in classes/priest/utility.lua use skill methods where applicable, preserve castIfBuffAbsent"
    - "Warlock has classMetatable + WARLOCK_FIELD_FUNC_MAP + registerPlayerClass in classes/warlock/Warlock.lua"
    - "Warlock combat logic in classes/warlock/combat.lua preserves castIfBuffAbsent pattern, adds curse/buff/ctrl functions"
  artifacts:
    - path: "classes/priest/Priest.lua"
      provides: "Priest class definition, ~7 skill methods, PRIEST_FIELD_FUNC_MAP, singleton, registerPlayerClass, SpellTrace:register, SelfTest:register"
      min_lines: 150
    - path: "classes/priest/combat.lua"
      provides: "priestAtk, priestRangedAtk — refactored to use skill methods"
      min_lines: 80
    - path: "classes/priest/utility.lua"
      provides: "priestBuffs, priestDebuffs, priestCtrl, priestHeal — refactored with skill methods, castIfBuffAbsent preserved"
      min_lines: 70
    - path: "classes/warlock/Warlock.lua"
      provides: "Warlock class definition, ~4 skill methods, WARLOCK_FIELD_FUNC_MAP, singleton, registerPlayerClass, SpellTrace:register, SelfTest:register"
      min_lines: 120
    - path: "classes/warlock/combat.lua"
      provides: "wlkAtk, wlkRangedAtk, wlkMeleeAtk, wlkCurses, wlkBuffs, wlkCtrl — castIfBuffAbsent preserved"
      min_lines: 80
  key_links:
    - from: "classes/priest/Priest.lua"
      to: "core/class.lua"
      via: "macroTorch.classMetatable(self, 'PRIEST_FIELD_FUNC_MAP')"
      pattern: "classMetatable.*PRIEST_FIELD_FUNC_MAP"
    - from: "classes/priest/combat.lua"
      to: "classes/priest/Priest.lua"
      via: "macroTorch.player.holy_fire() etc."
      pattern: "player\\.[a-z_]+\\("
    - from: "classes/warlock/Warlock.lua"
      to: "core/class.lua"
      via: "macroTorch.classMetatable(self, 'WARLOCK_FIELD_FUNC_MAP')"
      pattern: "classMetatable.*WARLOCK_FIELD_FUNC_MAP"
---

<objective>
Refactor Priest (3 files: healing is a distinct dimension per D-01/D-02) and Warlock (2 files: default mode, curses+buffs+ctrl combined into combat.lua) to align with the Druid architecture. Create class subdirectories, extract class definitions with skill methods, replace CastSpellByName with _castSpell-based skill methods where applicable, add SpellTrace:register and SelfTest:register, and split combat/utility logic into separate files.

Purpose: Complete the Druid-aligned architecture pattern for the final two non-Druid classes. Priest is a 3-file split (healing dimension warrants separate utility file). Warlock is a 2-file default mode (no clear multi-dimensional split, curses+buffs go in combat.lua).
Output: 5 new files in classes/priest/ and classes/warlock/ subdirectories.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-druid-druid/08-CONTEXT.md
@.planning/phases/08-druid-druid/08-RESEARCH.md
@classes/druid/Druid.lua
@classes/Priest.lua
@classes/Warlock.lua

<interfaces>
<!-- Same interfaces as Plan 08-01/08-02 — refer to those plans for full details -->

From core/class.lua:
- `macroTorch.classMetatable(cls, fieldMapName)` — metatable factory
- `macroTorch.registerPlayerClass(className, classTable)` — polymorphic registration

From entity/Player.lua:
- `obj._castSpell(localeNames, mode, range, resourceCost, onSelf)` — locale-aware spell casting
- `macroTorch.castIfBuffAbsent(unit, spellName, texture)` — conditional buff cast (PRESERVE for almost all Priest/Warlock calls)

Priest-specific notes:
- Only 4 CastSpellByName call sites to convert: 1 Holy Fire, ~3 healing spells (Heal, Lesser Heal)
- castIfBuffAbsent used for: Power Word: Fortitude, Inner Fire, Shadow Word: Pain, Renew — PRESERVE all
- Healing spells are Type C (flexible target — can heal self or friendly target)
- priestHeal() has threshold-based logic (440/140 damage) — preserve this logic

Warlock-specific notes:
- 0 CastSpellByName calls in original code — all spell casts use castIfBuffAbsent
- castIfBuffAbsent used for: Immolate, Corruption, Curse of Agony, Demon Skin — PRESERVE all
- Skill methods are created for future migration but combat.lua does not change CastSpellByName (there are none to change)
- Warlock is the simplest class to refactor — almost entirely structural reorganization
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Priest class definition, combat, and utility files</name>
  <files>classes/priest/Priest.lua, classes/priest/combat.lua, classes/priest/utility.lua</files>
  <read_first>
Read classes/Priest.lua (111 lines — extract all functions: priestRangedAtk, priestBuffs, priestDebuffs, priestAtk, priestCtrl, priestHeal).
Read classes/druid/Druid.lua lines 1-200 (constructor pattern with skill methods — replicate for Priest).
Read entity/Player.lua lines 1-90 (_castSpell signature — note Type C skills use onSelf parameter for flexible targeting).
  </read_first>
  <action>
Create classes/priest/ directory, then create all 3 files.

=== classes/priest/Priest.lua ===
1. Apache 2.0 license block
2. Comment: `---牧师专用 start---`
3. Class prototype: `macroTorch.Priest = macroTorch.Player:new()`
4. Constructor with skill methods. Per RESEARCH.md skill method table:

Type A (enemy target, onSelf=false):
- holy_fire: {en='Holy Fire', zh='神圣之火'}, mode, 30, nil, false
- shadow_word_pain: {en='Shadow Word: Pain', zh='暗言术：痛'}, mode, 30, nil, false

Type B (self target, onSelf=true):
- inner_fire: {en='Inner Fire', zh='心灵之火'}, mode, nil, nil, true

Type C (flexible target):
- power_word_fortitude: {en='Power Word: Fortitude', zh='真言术：韧'}, mode, nil, nil, false
- heal: {en='Heal', zh='治疗术'}, mode, nil, nil, false
- lesser_heal: {en='Lesser Heal', zh='次级治疗术'}, mode, nil, nil, false
- renew: {en='Renew', zh='恢复'}, mode, nil, nil, false

Return obj at end of constructor.

5. FIELD_FUNC_MAP: `macroTorch.PRIEST_FIELD_FUNC_MAP = {}` (empty — Priest has no class-specific lazy-computed fields)
6. Singleton: `macroTorch.priest = macroTorch.Priest:new()`
7. Registration: `macroTorch.registerPlayerClass("Priest", macroTorch.Priest)` — per D-03
8. SpellTrace registration: Priest has NO spell trace in original code. Add placeholder comment: `-- SpellTrace: no Priest spells currently traced`
9. SelfTest registrations (all isOptional=true, UnitClass('player') ~= 'Priest' guard):
   - "Priest: PRIEST_FIELD_FUNC_MAP is table" — assert type check
   - "Priest: singleton priest exists" — assert type(macroTorch.priest) == "table"
   - "Priest: registered in PLAYER_CLASS_REGISTRY" — assert registry entry
   - For each of the 7 skill methods: "Priest: skill method X exists" — assert type check
   - Total: 3 infra + 7 skills = 10 SelfTest registrations

=== classes/priest/combat.lua ===
1. Apache 2.0 license block
2. Comment: `-- Priest combat rotation functions`
3. Migrate priestRangedAtk() (lines 17-33):
   - CastSpellByName('Holy Fire') (line 28) → player.holy_fire()
   - Keep macroTorch.startAutoShoot()/stopAutoShoot(), PetDefensiveMode(), PetAttack(), isActionCooledDown, isBuffOrDebuffPresent, getUnitHealthPercent, GetNumPartyMembers as-is
   - Add `local player = macroTorch.player` at function start
4. Migrate priestAtk() (lines 58-91): keep logic intact, calls priestBuffs/priestDebuffs/priestRangedAtk — unchanged

=== classes/priest/utility.lua ===
1. Apache 2.0 license block
2. Comment: `-- Priest utility functions`
3. Migrate priestBuffs() (lines 36-45):
   - castIfBuffAbsent(t, 'Power Word: Fortitude', 'Spell_Holy_WordFortitude') — PRESERVE
   - castIfBuffAbsent(p, 'Inner Fire', 'Spell_Holy_InnerFire') — PRESERVE
   - Function structure stays the same
4. Migrate priestDebuffs() (lines 47-56):
   - castIfBuffAbsent(t, 'Shadow Word: Pain', 'Spell_Shadow_ShadowWordPain') — PRESERVE
   - useItemIfManaPercentLessThan / useItemIfHealthPercentLessThan — keep as-is
   - Function structure stays the same
5. Migrate priestCtrl() (lines 93-94): empty function, keep as-is
6. Migrate priestHeal() (lines 97-111) — this has CastSpellByName calls to convert:
   - CastSpellByName('Heal') (line 105) → player.heal()
   - CastSpellByName('Lesser Heal') (line 107) → player.lesser_heal()
   - castIfBuffAbsent(t, 'Renew', 'Spell_Holy_Renew') (line 109) — PRESERVE
   - Keep threshold-based logic (>440 / >140) as-is
   - Keep isTargetValidFriendly check as-is
   - Add `local player = macroTorch.player` at function start

All functions maintain global macroTorch.* namespace and dot syntax.
  </action>
  <verify>
    <automated>grep -c "classMetatable.*PRIEST_FIELD_FUNC_MAP" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/priest/Priest.lua && grep -c "registerPlayerClass.*Priest" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/priest/Priest.lua && grep -c "SelfTest:register" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/priest/Priest.lua</automated>
  </verify>
  <acceptance_criteria>
- classes/priest/Priest.lua exists with full Druid-aligned class definition
- 7 skill methods: holy_fire, shadow_word_pain, inner_fire, power_word_fortitude, heal, lesser_heal, renew — all with locale tables
- PRIEST_FIELD_FUNC_MAP is empty table
- registerPlayerClass("Priest", ...) present — per D-03
- SelfTest:register count >= 10 (3 infra + 7 skills)
- SpellTrace section present (empty placeholder)
- classes/priest/combat.lua: priestRangedAtk replaces CastSpellByName('Holy Fire') with player.holy_fire()
- classes/priest/combat.lua: priestAtk preserved unchanged
- classes/priest/utility.lua: priestHeal replaces CastSpellByName with player.heal() and player.lesser_heal()
- classes/priest/utility.lua: castIfBuffAbsent calls preserved for Power Word: Fortitude, Inner Fire, Shadow Word: Pain, Renew
- priestHeal threshold logic preserved (>440 Heal, >140 Lesser Heal, else Renew)
- All dot syntax, all global namespace
  </acceptance_criteria>
  <done>Priest class definition, combat, and utility files are complete with 7 skill methods, healing CastSpellByName replaced, castIfBuffAbsent preserved</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Warlock class definition and combat files</name>
  <files>classes/warlock/Warlock.lua, classes/warlock/combat.lua</files>
  <read_first>
Read classes/Warlock.lua (92 lines — extract all functions: wlkCurses, wlkRangedAtk, wlkMeleeAtk, wlkBuffs, wlkAtk, wlkCtrl).
Read classes/druid/Druid.lua lines 1-200 (constructor with skill methods — replicate for Warlock).
Read entity/Player.lua lines 1-90 (_castSpell signature).
  </read_first>
  <action>
Create classes/warlock/ directory, then create both files.

=== classes/warlock/Warlock.lua ===
1. Apache 2.0 license block
2. Comment: `---术士专用 start---`
3. Class prototype: `macroTorch.Warlock = macroTorch.Player:new()`
4. Constructor with skill methods. Per RESEARCH.md skill method table (all currently used via castIfBuffAbsent):

Type A (enemy target, onSelf=false):
- immolate: {en='Immolate', zh='献祭'}, mode, 30, nil, false
- corruption: {en='Corruption', zh='腐蚀术'}, mode, 30, nil, false
- curse_of_agony: {en='Curse of Agony', zh='痛苦诅咒'}, mode, 30, nil, false

Type B (self target, onSelf=true):
- demon_skin: {en='Demon Skin', zh='恶魔皮肤'}, mode, nil, nil, true

Return obj at end of constructor.

5. FIELD_FUNC_MAP: `macroTorch.WARLOCK_FIELD_FUNC_MAP = {}` (empty — Warlock has no class-specific lazy-computed fields)
6. Singleton: `macroTorch.warlock = macroTorch.Warlock:new()`
7. Registration: `macroTorch.registerPlayerClass("Warlock", macroTorch.Warlock)` — per D-03
8. SpellTrace registration: Warlock has NO spell trace in original code. Add placeholder comment: `-- SpellTrace: no Warlock spells currently traced`
9. SelfTest registrations (all isOptional=true, UnitClass('player') ~= 'Warlock' guard):
   - "Warlock: WARLOCK_FIELD_FUNC_MAP is table" — assert type check
   - "Warlock: singleton warlock exists" — assert type(macroTorch.warlock) == "table"
   - "Warlock: registered in PLAYER_CLASS_REGISTRY" — assert registry entry
   - For each of the 4 skill methods: "Warlock: skill method X exists" — assert type check
   - Total: 3 infra + 4 skills = 7 SelfTest registrations

=== classes/warlock/combat.lua ===
1. Apache 2.0 license block
2. Comment: `-- Warlock combat rotation and utility functions`
3. Migrate wlkCurses() (lines 17-29):
   - castIfBuffAbsent(t, 'Immolate', 'Fire_Immolation') — PRESERVE
   - castIfBuffAbsent(t, 'Corruption', 'Shadow_AbominationExplosion') — PRESERVE
   - castIfBuffAbsent(t, 'Curse of Agony', 'Shadow_CurseOfSargeras') — PRESERVE
   - Keep getUnitHealthPercent threshold checks as-is
4. Migrate wlkRangedAtk() (lines 31-41): keep as-is (PetDefensiveMode, PetAttack, wlkCurses, startAutoShoot)
5. Migrate wlkMeleeAtk() (lines 43-53): keep as-is (PetDefensiveMode, PetAttack, wlkCurses, startAutoShoot)
6. Migrate wlkBuffs() (lines 55-59):
   - castIfBuffAbsent(p, 'Demon Skin', 'Shadow_RagingScream') — PRESERVE
7. Migrate wlkAtk() (lines 61-88): keep logic intact, calls wlkBuffs/wlkMeleeAtk/wlkRangedAtk — unchanged
8. Migrate wlkCtrl() (lines 90-92): empty function, keep as-is

IMPORTANT: Warlock has 0 CastSpellByName calls in the original code. All spell casts use castIfBuffAbsent. Per CONTEXT.md: "Warlock 当前 0 处 CastSpellByName。技能方法创建后，castIfBuffAbsent 调用点可逐步迁移但非强制". The skill methods are created in Warlock.lua for future migration; combat/utility files PRESERVE all castIfBuffAbsent calls unchanged.

Add a comment at the top of combat.lua:
`-- NOTE: All spell casts use castIfBuffAbsent (preserved per CONTEXT.md). Skill methods exist in Warlock.lua for future migration.`

All functions maintain global macroTorch.* namespace and dot syntax.
  </action>
  <verify>
    <automated>grep -c "classMetatable.*WARLOCK_FIELD_FUNC_MAP" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/warlock/Warlock.lua && grep -c "registerPlayerClass.*Warlock" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/warlock/Warlock.lua && grep -c "SelfTest:register" /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/warlock/Warlock.lua</automated>
  </verify>
  <acceptance_criteria>
- classes/warlock/Warlock.lua exists with full Druid-aligned class definition
- 4 skill methods: immolate, corruption, curse_of_agony, demon_skin — all with locale tables
- WARLOCK_FIELD_FUNC_MAP is empty table
- registerPlayerClass("Warlock", ...) present — per D-03
- SelfTest:register count >= 7 (3 infra + 4 skills)
- SpellTrace section present (empty placeholder)
- classes/warlock/combat.lua preserves ALL castIfBuffAbsent calls unchanged
- classes/warlock/combat.lua: wlkCurses, wlkRangedAtk, wlkMeleeAtk, wlkBuffs, wlkAtk, wlkCtrl all present and functionally equivalent to original
- NOTE comment about castIfBuffAbsent preservation at top of combat.lua
- No CastSpellByName calls anywhere (there were none in original either)
- All dot syntax, all global namespace
  </acceptance_criteria>
  <done>Warlock class definition and combat files are complete with 4 skill methods (for future migration), all castIfBuffAbsent calls preserved, no functional changes to combat/utility logic</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Priest/Warlock subdirectories (mkdir -p)</name>
  <files>classes/priest/.gitkeep, classes/warlock/.gitkeep</files>
  <read_first>
Verify parent directories exist. This is a pre-flight task to ensure subdirectories are created before files are written.
  </read_first>
  <action>
Create the two class subdirectories:
- `mkdir -p classes/priest`
- `mkdir -p classes/warlock`

No .gitkeep files needed — the actual .lua files will keep the directories in git.

Note: This is a lightweight setup task. The actual file creation happens in Tasks 1 and 2.
  </action>
  <verify>
    <automated>test -d /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/priest && test -d /Users/yue.weny/finalanswer/macro-torch/macro-torch/classes/warlock && echo "DIRS_OK"</automated>
  </verify>
  <acceptance_criteria>
- classes/priest/ directory exists
- classes/warlock/ directory exists
  </acceptance_criteria>
  <done>Priest and Warlock subdirectories created under classes/</done>
</task>

</tasks>

<threat_model>
## Threat Model — No new threats

This is a pure code structure refactoring within an existing WoW 1.12.1 addon sandbox. No new attack surfaces are introduced:
- No file I/O beyond existing addon Lua files
- No network communication
- No user input handling changes
- No dependency changes

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | — | classes/priest/*.lua, classes/warlock/*.lua | accept | No new threats: pure internal refactoring within WoW addon sandbox |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase |
</threat_model>

<verification>
```bash
# Priest: class definition completeness
grep -c "macroTorch.Priest = macroTorch.Player:new()" classes/priest/Priest.lua  # expect: 1
grep -c "classMetatable.*PRIEST_FIELD_FUNC_MAP" classes/priest/Priest.lua  # expect: 1
grep -c "registerPlayerClass.*Priest" classes/priest/Priest.lua  # expect: 1

# Priest: skill method count (>= 7)
grep -c "function obj\.[a-z_]*(" classes/priest/Priest.lua  # expect: >= 7

# Priest: Holy Fire CastSpellByName replaced
grep -c "player\.holy_fire" classes/priest/combat.lua  # expect: >= 1
grep -c "CastSpellByName.*Holy Fire" classes/priest/combat.lua  # expect: 0

# Priest: Heal/Lesser Heal CastSpellByName replaced
grep -c "player\.heal\b" classes/priest/utility.lua  # expect: >= 1
grep -c "player\.lesser_heal" classes/priest/utility.lua  # expect: >= 1

# Priest: castIfBuffAbsent preserved
grep -c "castIfBuffAbsent" classes/priest/utility.lua  # expect: >= 4

# Warlock: class definition completeness
grep -c "macroTorch.Warlock = macroTorch.Player:new()" classes/warlock/Warlock.lua  # expect: 1
grep -c "classMetatable.*WARLOCK_FIELD_FUNC_MAP" classes/warlock/Warlock.lua  # expect: 1
grep -c "registerPlayerClass.*Warlock" classes/warlock/Warlock.lua  # expect: 1

# Warlock: skill method count (>= 4)
grep -c "function obj\.[a-z_]*(" classes/warlock/Warlock.lua  # expect: >= 4

# Warlock: castIfBuffAbsent preserved (all spells use this pattern)
grep -c "castIfBuffAbsent" classes/warlock/combat.lua  # expect: >= 4

# Warlock: No CastSpellByName (there were none in original)
grep -c "CastSpellByName" classes/warlock/combat.lua  # expect: 0
```
</verification>

<success_criteria>
- Priest class definition file (classes/priest/Priest.lua) exists with Druid-aligned structure
- Priest combat file (classes/priest/combat.lua) replaces CastSpellByName('Holy Fire') with player.holy_fire()
- Priest utility file (classes/priest/utility.lua) replaces CastSpellByName for Heal/Lesser Heal, preserves castIfBuffAbsent
- Warlock class definition file (classes/warlock/Warlock.lua) exists with Druid-aligned structure
- Warlock combat file (classes/warlock/combat.lua) preserves ALL castIfBuffAbsent calls unchanged (no CastSpellByName in original)
- All files use dot syntax exclusively, global macroTorch.* namespace
- Priest healing threshold logic preserved, pickPocketBeforeCast state machine preserved
</success_criteria>

<output>
Create `.planning/phases/08-druid-druid/08-03-SUMMARY.md` when done
</output>