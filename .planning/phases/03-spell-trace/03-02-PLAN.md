---
phase: 03-spell-trace
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/spell_trace_core.lua
autonomous: true
requirements:
  - R4
must_haves:
  truths:
    - "SpellTrace:register() 声明式 API 存在且可被 Druid 文件调用"
    - "SpellTrace:register() 内部调用 setSpellTracing 和 setTraceSpellImmune"
    - "config.immune=true 时注册 immune tracing"
    - "config.land=true 时注册 spell tracing (需要 spellId)"
    - "底层 tracingSpells/traceSpellImmunes 核心表保持不变"
    - "现有 setSpellTracing/setTraceSpellImmune 函数签名和行为不变"
  artifacts:
    - path: "core/spell_trace_core.lua"
      provides: "SpellTrace:register() 声明式注册 API + 现有 spell trace 核心逻辑"
      contains: "function macroTorch.SpellTrace:register"
  key_links:
    - from: "SpellTrace:register(name, config)"
      to: "macroTorch.setSpellTracing(spellId, name)"
      via: "config.land 为 true 时调用"
    - from: "SpellTrace:register(name, config)"
      to: "macroTorch.setTraceSpellImmune(name, debuffTexture)"
      via: "config.immune 为 true 时调用"
    - from: "SpellTrace:register(name, config)"
      to: "tracingSpells / traceSpellImmunes 核心表"
      via: "setSpellTracing / setTraceSpellImmune 内部操作"
---

<objective>
在 `core/spell_trace_core.lua` 中添加 `macroTorch.SpellTrace` 命名空间和 `SpellTrace:register(name, config)` 声明式 API，内部委托给现有 `setSpellTracing`/`setTraceSpellImmune`。

Purpose: 提供配置驱动的 spell trace 注册方式替代分散的命令式调用对，不影响底层数据表和处理逻辑。
Output: `core/spell_trace_core.lua` — 添加 SpellTrace 表和 :register() 方法
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-spell-trace/03-CONTEXT.md
@.planning/phases/03-spell-trace/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@core/spell_trace_core.lua (现有文件 — SpellTrace:register 的插入位置)

### 关键上下文
- D-06: `macroTorch.SpellTrace:register(name, config)` 声明式 API，config 字段 `{immune, land, debuffTexture}`
- D-07: `setSpellTracing`/`setTraceSpellImmune` 改为内部实现细节（不鼓励直接调用），register() 内部调用它们
- D-08: `macroTorch.SpellTrace` 表作为命名空间，预留扩展点
- RESEARCH A3: `config.spellId` 字段是必须的，因为 GetSpellName() 在 addon 加载期间返回 nil
- RESEARCH Pitfall 1: 需要 spellId 支持 — config 需包含 spellId 字段用于 land tracing
- RESEARCH Pattern 2: 实现草图展示了具体的 register 内部逻辑

### 接口定义
<interfaces>
现有 setSpellTracing / setTraceSpellImmune 签名 (必须保持不变):

```lua
-- core/spell_trace_core.lua:18
function macroTorch.setSpellTracing(spellGuid, spellName)
    -- 操作 macroTorch.tracingSpells[spellGuid] = spellName
end

-- core/spell_trace_core.lua:35
function macroTorch.setTraceSpellImmune(spellName, spellDebuffTexture)
    -- 操作 macroTorch.traceSpellImmunes[spellName] = spellDebuffTexture
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 添加 SpellTrace 命名空间和 :register() API</name>
  <files>core/spell_trace_core.lua</files>
  <read_first>
@core/spell_trace_core.lua (读取现有 setSpellTracing/setTraceSpellImmune 实现，确认插入位置在 traceSpellImmunes 初始化之后)
@.planning/phases/03-spell-trace/03-RESEARCH.md (读取 Pattern 2: Wrapper/Delegation API 实现草图)
@.planning/phases/03-spell-trace/03-CONTEXT.md (读取 D-06 D-07 D-08 决策)
  </read_first>
  <action>
在 core/spell_trace_core.lua 的 `setTraceSpellImmuneByName` 函数定义之后 (约第 47 行，在 traceSpellImmunes 初始化完成之后)、`maintainLandTables` 函数定义之前插入以下代码:

**模块 1: SpellTrace 命名空间**
- 创建 `macroTorch.SpellTrace = {}` — 预留扩展点 (per D-08)

**模块 2: SpellTrace:register(name, config) (per D-06, D-07)**
- 接受 config 参数: `{spellId, immune, land, debuffTexture}` (per RESEARCH A3/Pitfall 1 — spellId 必须包含)
- `spellId` 是可选字段（仅当 `land=true` 时需要 — 用于调用 `setSpellTracing(spellId, name)`）
- `immune` (boolean): 为 true 时调用 `macroTorch.setTraceSpellImmune(name, config.debuffTexture)`
- `land` (boolean): 为 true 时调用 `macroTorch.setSpellTracing(config.spellId, name)`
- `debuffTexture` (string): immune tracing 所需的 debuff 贴图纹理，仅当 `immune=true` 时使用

**实现细节:**
- 函数使用 `:` 方法语法 (在 `macroTorch.SpellTrace` 表上) — 这是项目中首个 Table:method() 模式，建立先例
- 内部逻辑：
  1. 若 `config.land` 为 true，调用 `macroTorch.setSpellTracing(config.spellId, name)` — 注意 spellId 直接从 config 读取，不通过 getSpellUniqIdByName (addon 加载期间 spellbook 不可用)
  2. 若 `config.immune` 为 true，调用 `macroTorch.setTraceSpellImmune(name, config.debuffTexture)`
- 不引入新状态 — register() 仅委托到现有 setSpellTracing/setTraceSpellImmune
- 不修改 tracingSpells/traceSpellImmunes 表结构

**插入位置:** 在 `setSpellTracingByName` 和 `setTraceSpellImmuneByName` 函数定义之后（约第 47 行），`maintainLandTables` 函数定义之前（第 49 行）。

不要修改现有函数签名或行为。现有 `setSpellTracing`/`setTraceSpellImmune` 保持完全不变 — 它们转为内部实现细节但仍可直接调用（不鼓励但不阻断）。
  </action>
  <verify>
    <automated>grep -c "function macroTorch.SpellTrace:register" core/spell_trace_core.lua</automated>
  </verify>
  <acceptance_criteria>
    - grep "function macroTorch.SpellTrace:register" core/spell_trace_core.lua 有结果
    - grep "function macroTorch.setSpellTracing" core/spell_trace_core.lua 有结果 (现有函数未修改)
    - grep "function macroTorch.setTraceSpellImmune" core/spell_trace_core.lua 有结果 (现有函数未修改)
    - register() 内部调用 setSpellTracing 当 config.land=true 时
    - register() 内部调用 setTraceSpellImmune 当 config.immune=true 时
    - SpellTrace:register 使用 `:` 方法语法定义在 `macroTorch.SpellTrace` 表上
    - register() 直接从 config 读取 spellId/immune/land/debuffTexture (per D-06 + RESEARCH A3)
  </acceptance_criteria>
  <done>SpellTrace:register() API 已可用，等待 Druid 文件调用 (03-04 plan)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Druid 文件 -> SpellTrace:register() | config 表从 Druid 文件 (同一信任域) 传入，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | SpellTrace:register() 调用现有 setSpellTracing/setTraceSpellImmune | accept | register() 将 config 字段直接传递给现有函数，不对数据进行修改或转换。现有函数已有自己的重复注册保护 (if not tracingSpells[id] then)。威胁面与修改前相同。 |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | Phase 3 不引入外部包依赖 |
</threat_model>

<verification>
## 单元验证

```bash
# SpellTrace:register 定义存在
grep "function macroTorch.SpellTrace:register" core/spell_trace_core.lua

# 现有函数未受影响
grep "function macroTorch.setSpellTracing\b" core/spell_trace_core.lua
grep "function macroTorch.setTraceSpellImmune\b" core/spell_trace_core.lua

# 构建成功
./build.sh && echo "Build OK"
```
</verification>

<success_criteria>
- core/spell_trace_core.lua 包含 SpellTrace:register() 声明式 API
- 现有 setSpellTracing/setTraceSpellImmune 函数签名和逻辑完全不变
- ./build.sh 成功
</success_criteria>

<output>
Create `.planning/phases/03-spell-trace/03-02-SUMMARY.md` when done
</output>