---
phase: 03-spell-trace
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - core/events.lua
  - build_order.txt
autonomous: true
requirements:
  - R5
must_haves:
  truths:
    - "PLAYER_ENTERING_WORLD 事件处理中调用 SelfTest:run()"
    - "SelfTest:run() 调用在 onPlayerEnteringWorld() 之后"
    - "core/selftest.lua 在 build_order.txt 中出现在 core/events.lua 之前"
    - "构建成功，selftest.lua 内容进入 SM_Extend.lua"
  artifacts:
    - path: "core/events.lua"
      provides: "在 PLAYER_ENTERING_WORLD 事件处理中挂载 SelfTest:run()"
    - path: "build_order.txt"
      provides: "正确的文件加载顺序 — selftest.lua 在 events.lua 之前"
  key_links:
    - from: "core/events.lua eventHandle"
      to: "macroTorch.SelfTest:run()"
      via: "PLAYER_ENTERING_WORLD 事件分支"
    - from: "build_order.txt 加载顺序"
      to: "SM_Extend.lua 函数定义顺序"
      via: "build.sh 按序拼接"
---

<objective>
在 `core/events.lua` 的 `PLAYER_ENTERING_WORLD` 事件处理中挂载 `SelfTest:run()` 调用，并修正 `build_order.txt` 中 `core/selftest.lua` 的加载顺序使其在 `core/events.lua` 之前。

Purpose: 连接 SelfTest 框架到事件系统，确保登录时自动运行基础设施健康检查。build_order.txt 顺序修正确保函数定义先于引用加载。
Output: 修改 `core/events.lua` (第 52 行) 和 `build_order.txt` (第 23-25 行)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-spell-trace/03-CONTEXT.md
@.planning/phases/03-spell-trace/03-RESEARCH.md
@core/events.lua (现有文件 — 第 52 行有 Phase 3 预留注释)
@build_order.txt (现有文件 — 第 23-25 行需要调整顺序)

### 关键上下文
- CONTEXT.md code_context: "core/events.lua:52: -- Phase 3: macroTorch.SelfTest:run() 预留注释，直接替换为调用"
- RESEARCH Open Q3: "Move core/selftest.lua to BEFORE core/events.lua in build_order.txt per the explicit instruction in CONTEXT.md"
- Phase 1 D-09: build_order.txt 一次性全量文件列表
- Phase 2 D-03: 直接函数调用 + build_order 顺序保证，不引入 stub/中间抽象层
- Phase 2 D-05: 新文件顺序规则 — 函数定义先于调用方加载

### 现有代码 (core/events.lua:50-52)
```lua
    elseif event == 'PLAYER_ENTERING_WORLD' then
        macroTorch.onPlayerEnteringWorld()
        -- Phase 3: macroTorch.SelfTest:run()
```

### 现有代码 (build_order.txt:20-26)
```lua
# core/spell_trace_core.lua    -- line 21
# core/spell_trace_immune.lua  -- line 22
core/events.lua                -- line 23
# (blank?)
core/selftest.lua              -- line 25
```
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: events.lua 挂载 SelfTest:run()</name>
  <files>core/events.lua</files>
  <read_first>
@core/events.lua (读取第 48-53 行 PLAYER_ENTERING_WORLD 事件分支，确认现有代码结构)
@.planning/phases/03-spell-trace/03-CONTEXT.md (读取 D-01 自检触发策略)
  </read_first>
  <action>
在 core/events.lua 第 52 行，将现有注释 `-- Phase 3: macroTorch.SelfTest:run()` 替换为实际函数调用 `macroTorch.SelfTest:run()`。

**具体修改:**
- 行 52: `        -- Phase 3: macroTorch.SelfTest:run()` → `        macroTorch.SelfTest:run()`
- SelfTest:run() 调用放在 `macroTorch.onPlayerEnteringWorld()` 之后 (第 51 行)
- SelfTest:run() 内部已有 _selfTestRan session flag 检查 (per D-01)，events.lua 不需要重复检查

不要修改事件处理的任何其他部分。不要修改 eventHandle 的 if-elseif dispatch 结构。

**为什么放在 onPlayerEnteringWorld() 之后:** onPlayerEnteringWorld() 负责初始化 Player/Target 实体 (调用 initPlayer、loadingPlayer、loadingTarget 等)，SelfTest:run() 中的 Player 实体属性测试需要这些实体已初始化。
  </action>
  <verify>
    <automated>grep -n "SelfTest:run" core/events.lua</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SelfTest:run" core/events.lua 返回 1 (恰好一个调用)
    - SelfTest:run() 调用在 onPlayerEnteringWorld() 之后
    - 行 52 上的 -- Phase 3 注释已移除或替换为实际调用
    - SelfTest:run() 调用不在任何 if/macroTorch._selfTestRan 条件包裹内 (session flag 检查由 SelfTest:run() 内部处理 per D-01)
  </acceptance_criteria>
  <done>PLAYER_ENTERING_WORLD 事件触发时自动运行自检</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修正 build_order.txt 中 core/selftest.lua 顺序</name>
  <files>build_order.txt</files>
  <read_first>
@build_order.txt (读取第 18-26 行，确认当前 core/ 文件顺序)
@.planning/phases/03-spell-trace/03-RESEARCH.md (读取 Open Q3 推荐顺序)
@.planning/phases/02-events-system/02-CONTEXT.md (读取 D-05 新文件顺序规则)
  </read_first>
  <action>
修正 build_order.txt 中 core/selftest.lua 的插入位置，使其出现在 core/events.lua 之前。

**当前顺序 (第 20-25 行):**
```
core/combat_context.lua       (line 19)
core/spell_trace_core.lua     (line 21)
core/spell_trace_immune.lua   (line 22)
core/events.lua               (line 23)
(空行? 在 24)
core/selftest.lua             (line 25)
```

**目标顺序:**
```
core/combat_context.lua
core/spell_trace_core.lua
core/spell_trace_immune.lua
core/selftest.lua             ← 移到 events.lua 之前 (per CONTEXT.md + RESEARCH Open Q3)
core/events.lua
```

**具体操作:**
- 将 `core/selftest.lua` 行移动到 `core/events.lua` 行之前
- 目标是把 line 25 的 `core/selftest.lua` 放到 line 23 的 `core/events.lua` 之前 (即当前位置调换)
- 保持 core/spell_trace_immune.lua 到 core/events.lua 之间的顺序关系不变

**原则 (per Phase 2 D-03/D-05):** 函数定义需先于调用方加载。events.lua 调用 SelfTest:run()，因此 selftest.lua 必须在 events.lua 之前。虽然 Lua 的 `function Name()` 语法在全局作用域中有 hoisting 效果，但显式按依赖顺序排列是更稳妥的做法。

不要添加或删除任何其他行。不要修改注释格式。
  </action>
  <verify>
    <automated>grep -n "core/selftest\|core/events" build_order.txt</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "core/selftest" build_order.txt 输出的行号 < grep -n "core/events" build_order.txt 行号 (selftest 在 events 之前)
    - build_order.txt 总行数不变 (仅位置交换)
    - 其他 core/ 文件顺序不变 (combat_context, spell_trace_core, spell_trace_immune 顺序保持)
    - ./build.sh 成功
  </acceptance_criteria>
  <done>build_order.txt 中 selftest.lua 在 events.lua 之前，构建成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
无新信任边界引入。仅修改现有事件处理器和构建文件。

| Boundary | Description |
|----------|-------------|
| 无新增 | 本 plan 仅挂载调用和调整文件顺序 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | events.lua PLAYER_ENTERING_WORLD handler | accept | SelfTest:run() 通过 pcall 隔离执行，单个测试失败不影响事件处理链。_selfTestRan session flag 防止跨区域重复输出。风险可接受。 |
| T-03-06 | Tampering | build_order.txt 文件顺序 | mitigate | build.sh 使用 build_order.txt 控制拼接顺序。顺序错误可能导致运行时 nil 引用。通过 grep -n 验证 selftest 在 events 之前，再通过 ./build.sh 验证构建成功。 |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | 无外部包依赖 |
</threat_model>

<verification>
## 构建验证

```bash
./build.sh && echo "Build OK"
```

## 符号验证

```bash
# events.lua 中 SelfTest:run() 调用存在
grep -c "SelfTest:run" core/events.lua  # 期望: 1

# build_order.txt 顺序正确
grep -n "core/selftest\|core/events" build_order.txt
# 期望: core/selftest.lua 行号 < core/events.lua 行号

# 产物中 SelfTest 相关符号存在
grep "function macroTorch.SelfTest:run\|function macroTorch.SelfTest:register" SM_Extend.lua
```
</verification>

<success_criteria>
- core/events.lua 第 52 行调用 macroTorch.SelfTest:run()
- build_order.txt 中 core/selftest.lua 在 core/events.lua 之前
- ./build.sh 成功
- SM_Extend.lua 中包含 SelfTest:run 和 SelfTest:register 函数定义
</success_criteria>

<output>
Create `.planning/phases/03-spell-trace/03-03-SUMMARY.md` when done
</output>