---
phase: 19-druidcontrol-bash-druidcharge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/druid/combo.lua
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "druidControl() no longer contains a Bash (猛击) call or distance < 8 branch"
    - "druidControl() Hibernate/Entangling Roots branch is reachable (elseif promoted to if)"
    - "druidCharge() exists as a global function on macroTorch"
    - "druidCharge() auto-switches to bear form when not in bear form, then returns (one action per press)"
    - "druidCharge() uses Feral Charge when target distance >= 8 and distance < 8 uses Bash"
    - "druidCharge() has isSpellExist guards for both Feral Charge and Bash before calling them"
    - "druidCharge() follows target-check-then-form-check ordering (target first, form second)"
  artifacts:
    - "combo.lua contains function macroTorch.druidCharge() after druidControl()"
    - "combo.lua contains modified druidControl() without bash('ready') call and without distance < 8 branch"
  key_links:
    - "druidCharge() target check uses macroTorch.target.isCanAttack + targetEnemy() fallback (same as druidControl)"
    - "druidCharge() form check uses macroTorch.player.isInBearForm (not isFormActive directly)"
    - "druidCharge() form auto-switch uses dire_bear_form('ready') with isSpellExist fallback to bear_form('ready')"
    - "druidCharge() uses feral_charge('safe') for Charge, bash('ready') for Bash"
    - "druidControl() uses if isBeastOrDragonkin() then hibernate() else entangling_roots() (no distance check)"
---

<objective>
[What this plan accomplishes]

Modify `druidControl()` to remove the Bash (猛击) interrupt branch, keeping only Hibernate + Entangling Roots control skills. Create new `druidCharge()` global one-button macro method with distance-driven Feral Charge/Bash dual-branch, form auto-switch, and isSpellExist guards for low-level safety.

Purpose: Separate Bash interrupt from control skills into dedicated keybinds. druidCharge provides auto-form-switch + distance-adaptive Charge/Bash. druidControl becomes a pure CC method.

Output: Modified `druidControl()` (Bash branch removed, D-06, D-07, D-08), new `druidCharge()` method (D-01, D-02, D-03, D-04, D-05), druidCharge self-test registration in combo.lua.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-druidcontrol-bash-druidcharge/19-CONTEXT.md
@.planning/phases/19-druidcontrol-bash-druidcharge/19-RESEARCH.md
@.planning/phases/19-druidcontrol-bash-druidcharge/19-PATTERNS.md
@classes/druid/combo.lua
@classes/druid/Druid.lua
@entity/Player.lua
@biz_util.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Bash branch from druidControl and promote elseif to if (D-06, D-07, D-08)</name>
  <files>classes/druid/combo.lua</files>
  <read_first>
    - classes/druid/combo.lua:254-271 — current druidControl function to modify
    - .planning/phases/19-druidcontrol-bash-druidcharge/19-CONTEXT.md:76-97 — exact before/after code structure
    - .planning/phases/19-druidcontrol-bash-druidcharge/19-PATTERNS.md:90-140 — detailed diff for druidControl
  </read_first>
  <acceptance_criteria>
    - `grep -c "target.distance < 8" classes/druid/combo.lua` returns 0 (distance check removed from druidControl)
    - `grep -c "macroTorch.player.bash" classes/druid/combo.lua` returns exactly 1 (only in new druidCharge, not in druidControl)
    - `grep "target.isBeastOrDragonkin" classes/druid/combo.lua | grep -c "elseif"` returns 0 (elseif promoted to if)
    - `grep "target.isBeastOrDragonkin" classes/druid/combo.lua | grep -c "^[[:space:]]*if target.isBeastOrDragonkin"` returns 1 (standalone if, not elseif)
    - druidControl function still contains hibernate() and entangling_roots() calls
    - druidControl function still contains the target check (isCanAttack + targetEnemy) preamble unchanged
    - druidControl does NOT contain any form check or isSpellExist guard (per D-08)
  </acceptance_criteria>
  <action>Delete lines 264-265 of combo.lua (the `if target.distance < 8 then macroTorch.player.bash('ready')` block) and change the `elseif` on what becomes the next line to `if`. The target check preamble (lines 254-262) stays identical. Do NOT add form checks, isSpellExist guards, or any new logic to druidControl. The resulting structure:

1. Target check (unchanged): get local target, check isCanAttack, targetEnemy fallback, return if no target
2. Single if/else: `if target.isBeastOrDragonkin() then macroTorch.player.hibernate() else macroTorch.player.entangling_roots() end`

Key constraints per D-06/D-07/D-08: No form checking, no isSpellExist guards, no distance check, no bash call. Pure Hibernate vs Entangling Roots dispatch on target type.</action>
  <verify>
    <automated>grep -n "target.distance < 8\|bash.*ready\|elseif" classes/druid/combo.lua</automated>
  </verify>
  <done>druidControl function is reduced to target check + if/else for Hibernate/Entangling Roots only. No distance check, no bash call, no elseif, no form check.</done>
</task>

<task type="auto">
  <name>Task 2: Create druidCharge() global macro method with distance-driven Charge/Bash dual-branch (D-01, D-02, D-03, D-04, D-05)</name>
  <files>classes/druid/combo.lua</files>
  <read_first>
    - classes/druid/combo.lua:238-252 — druidDefend form-switch pattern (exact analog for form check)
    - classes/druid/combo.lua:254-271 — druidControl target check pattern (exact analog for target acquisition)
    - classes/druid/Druid.lua:66-68 — bash(mode, rank) method signature
    - classes/druid/Druid.lua:82-84 — feral_charge(mode, rank) method signature (25 yd range)
    - classes/druid/Druid.lua:123-130 — bear_form(mode, rank) and dire_bear_form(mode, rank) signatures
    - classes/druid/Druid.lua:331-332 — isInBearForm = Bear Form OR Dire Bear Form
    - biz_util.lua:63-65 — isSpellExist(spellName, bookType) implementation
    - entity/Player.lua:42-75 — _castSpell mode semantics (raw/ready/safe: line 53-62 for readiness check, line 62-65 for distance+resource)
    - .planning/phases/19-druidcontrol-bash-druidcharge/19-CONTEXT.md:159-167 — druidCharge pseudo-code structure
    - .planning/phases/19-druidcontrol-bash-druidcharge/19-PATTERNS.md:17-87 — complete pattern map for druidCharge
    - .planning/phases/19-druidcontrol-bash-druidcharge/19-RESEARCH.md:219-236 — anti-patterns to avoid
  </read_first>
  <acceptance_criteria>
    - `grep -c "function macroTorch.druidCharge" classes/druid/combo.lua` returns 1
    - druidCharge does target check BEFORE form check (target.isCanAttack check comes before isInBearForm check)
    - `grep "macroTorch.player.isInBearForm" classes/druid/combo.lua` returns 1 in druidCharge (not isFormActive directly)
    - Form switch block: `macroTorch.player.dire_bear_form('ready')` with `macroTorch.isSpellExist("Dire Bear Form")` guard, else `bear_form('ready')`, followed by `return`
    - Distance branch: `target.distance >= 8` goes to Feral Charge branch, `else` (i.e. < 8) goes to Bash branch
    - Feral Charge branch contains: `macroTorch.isSpellExist("Feral Charge")` guard, then `macroTorch.player.feral_charge('safe')`
    - Bash branch contains: `macroTorch.isSpellExist("Bash")` guard, then `macroTorch.player.bash('ready')`
    - Feral Charge uses `'safe'` mode, Bash uses `'ready'` mode (never reversed)
    - `grep -c "#" classes/druid/combo.lua` — verify the `#` unary length operator does NOT appear in new code
    - `./build.sh` exit code 0, `grep "function macroTorch.druidCharge" SM_Extend.lua` returns 1
  </acceptance_criteria>
  <action>Insert the new druidCharge global function immediately after druidControl's closing `end` at line 271 in combo.lua. The method structure, in execution order per D-01/D-02/D-03/D-04/D-05:

1. Target check (copy druidControl pattern lines 254-262): `local target = macroTorch.target`, check `target.isCanAttack`, call `targetEnemy()` if false, re-check, return if still not targetable.

2. Form check (copy druidDefend pattern lines 244-246): `if not macroTorch.player.isInBearForm` → `if macroTorch.isSpellExist("Dire Bear Form") then dire_bear_form('ready') else bear_form('ready') end` → `return`. Always pair form switch with return (one action per press). Use `isInBearForm`, never `isFormActive('Bear Form')` directly.

3. Distance branch with isSpellExist guards: `if target.distance >= 8 then` → `if not macroTorch.isSpellExist("Feral Charge") then return end; macroTorch.player.feral_charge('safe')`; `else` → `if not macroTorch.isSpellExist("Bash") then return end; macroTorch.player.bash('ready')`.

Key mode choices: Feral Charge uses `'safe'` (range=25, _castSpell checks _isInRange at Player.lua:62; range check, GCD check, resource check). Bash uses `'ready'` (melee, skips distance check at Player.lua:62; CD-only check). Never reverse these.

Key structural rules: Target check before form check (avoid wasted form switch with no target). Form switch always paired with return (druidDefend precedent at combo.lua:245-246). Do NOT use the `#` operator (WoW 1.12.1 Lua limitation).</action>
  <verify>
    <automated>grep -c "function macroTorch.druidCharge" classes/druid/combo.lua && ./build.sh && grep -c "function macroTorch.druidCharge" SM_Extend.lua</automated>
  </verify>
  <done>New druidCharge() method exists with: target check, isInBearForm check + bear_form auto-switch + return, distance >= 8 → isSpellExist("Feral Charge") → feral_charge('safe'), else → isSpellExist("Bash") → bash('ready'). Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Register druidCharge self-test in combo.lua alongside existing combo method tests</name>
  <files>classes/druid/combo.lua</files>
  <read_first>
    - classes/druid/combo.lua:303-306 — existing druidControl self-test pattern (exact analog)
    - core/selftest.lua:28-34 — SelfTest:register(name, fn, isOptional) API signature
  </read_first>
  <acceptance_criteria>
    - `grep -c "druidCharge exists" classes/druid/combo.lua` returns 1
    - The test uses `UnitClass('player') ~= 'Druid' then return end` guard per existing pattern
    - The test uses `assert(type(macroTorch.druidCharge) == "function", ...)` per existing pattern
    - The test passes `true` as third argument (isOptional=true) per existing combo method tests
    - The test is placed after the druidControl self-test (line 306), as a new adjacent block before the file end
  </acceptance_criteria>
  <action>Insert a new self-test registration block immediately after the druidControl self-test (after line 306 of combo.lua). Follow the exact same pattern:

```
macroTorch.SelfTest:register("Druid: combo methods -- druidCharge exists", function()
    if UnitClass('player') ~= 'Druid' then return end
    assert(type(macroTorch.druidCharge) == "function", "druidCharge not a function")
end, true)
```

Use the identifier "Druid: combo methods -- druidCharge exists" matching the existing naming convention. The `true` third argument marks it as an optional test (yellow warning on failure, not red error). The `UnitClass` guard ensures non-Druid characters skip silently.</action>
  <verify>
    <automated>grep -c "druidCharge exists" classes/druid/combo.lua</automated>
  </verify>
  <done>druidCharge self-test is registered in combo.lua with UnitClass('player') guard, optional flag, and function type assertion, matching existing combo method test patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Druid → WoW API | druidCharge calls isInBearForm (form detection), isSpellExist (spellbook lookup), bear_form/dire_bear_form/feral_charge/bash (skill methods). All input validated by WoW client. |
| druidControl → WoW API | druidControl calls isCanAttack, targetEnemy, isBeastOrDragonkin, hibernate, entangling_roots. No user-supplied strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | druidCharge form check bypass | low | accept | isInBearForm reads WoW client state — cannot be spoofed from addon code. If WoW client is compromised, addon is not the attack surface. |
| T-19-02 | Denial of Service | druidCharge infinite recursion | low | accept | Method is one-action-per-press (no loops, no OnUpdate, no recursive calls). Form switch + return prevents re-entry in same key press. |
| T-19-03 | Information Disclosure | druidControl target type leak | low | accept | isBeastOrDragonkin() reads combat data already visible in WoW UI. No new information exposure. |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external packages installed in this phase. Pure Lua code modifications. |
</threat_model>

<verification>
After Plan 01 execution, verify:
- `grep "function macroTorch.druidCharge" classes/druid/combo.lua` returns 1
- `grep "target.distance < 8" classes/druid/combo.lua` returns 0
- `grep "elseif" classes/druid/combo.lua | grep -c "druidControl"` returns 0 (no elseif in druidControl)
- `grep "druidCharge exists" classes/druid/combo.lua` returns 1
- `./build.sh` exit code 0
- `grep "function macroTorch.druidCharge" SM_Extend.lua` returns 1
</verification>

<success_criteria>
- druidControl is reduced to: target check → Hibernate (if beast/dragonkin) or Entangling Roots (otherwise)
- druidCharge implements: target check → form check + auto-switch → isSpellExist guards → distance >= 8: Feral Charge, < 8: Bash
- Both methods follow "one action per press" (form switch always paired with return)
- druidCharge self-test registered in combo.lua alongside existing combo method tests
- Build passes with ./build.sh
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location | Notes |
|--------|------|----------|-------|
| `macroTorch.druidCharge` | Global function | classes/druid/combo.lua (new, after druidControl) | One-button bear charge/bash macro method |
| (modified) `macroTorch.druidControl` | Global function | classes/druid/combo.lua (lines 254-269) | Bash branch removed; pure CC dispatch |
| `"Druid: combo methods -- druidCharge exists"` | SelfTest registration | classes/druid/combo.lua (after line 306) | Optional function existence test |

<output>
Create .planning/phases/19-druidcontrol-bash-druidcharge/19-01-SUMMARY.md when done
</output>